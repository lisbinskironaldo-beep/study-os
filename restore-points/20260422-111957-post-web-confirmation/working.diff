diff --git a/.env.example b/.env.example
index db7efa3..38eaf09 100644
--- a/.env.example
+++ b/.env.example
@@ -12,7 +12,7 @@ MERCADO_PAGO_MONTHLY_PRICE=
 MERCADO_PAGO_ANNUAL_PRICE=
 
 # URLs usadas quando conectarmos o checkout real no backend/serverless.
-STUDY_OS_BASE_URL=http://127.0.0.1:5500
+ROTANOTA_BASE_URL=http://127.0.0.1:5500
 MERCADO_PAGO_SUCCESS_URL=http://127.0.0.1:5500/index.html?premiumPayment=success
 MERCADO_PAGO_FAILURE_URL=http://127.0.0.1:5500/index.html?premiumPayment=failure
 MERCADO_PAGO_PENDING_URL=http://127.0.0.1:5500/index.html?premiumPayment=pending
@@ -25,3 +25,46 @@ MERCADO_PAGO_WEBHOOK_SECRET=
 # Use a Service Role Key apenas no ambiente do servidor/Vercel.
 SUPABASE_URL=
 SUPABASE_SERVICE_ROLE_KEY=
+ROTANOTA_AUTH_SECRET=
+GOOGLE_CLIENT_ID=
+
+# Gemini para bundle do produto e copiloto da retaguarda.
+GEMINI_API_KEY=
+GEMINI_FREE_API_KEY=
+GEMINI_PAID_API_KEY=
+ROTANOTA_AI_MODEL=gemini-2.5-flash-lite
+
+# OpenAI / ChatGPT para acesso autorizado ao NorthStar via Apps SDK + MCP.
+# OPENAI_API_KEY e opcional nesta fase; o circuito minimo usa app URL + MCP URL + MCP bearer.
+OPENAI_API_KEY=
+OPENAI_APP_PUBLIC_URL=
+OPENAI_MCP_SERVER_URL=
+OPENAI_MCP_API_KEY=
+
+# GitHub para automacoes operacionais.
+GITHUB_TOKEN=
+
+# Google Ads para campanhas, relatorios e automacoes futuras.
+GOOGLE_ADS_DEVELOPER_TOKEN=
+GOOGLE_ADS_MANAGER_CUSTOMER_ID=
+GOOGLE_ADS_CUSTOMER_ID=
+GOOGLE_ADS_CLIENT_ID=
+GOOGLE_ADS_CLIENT_SECRET=
+GOOGLE_ADS_REFRESH_TOKEN=
+GOOGLE_ADS_SERVICE_ACCOUNT_KEY=
+
+# Meta Ads para campanhas externas e insights.
+META_APP_ID=
+META_AD_ACCOUNT_ID=
+META_ACCESS_TOKEN=
+
+# Retaguarda /ops
+OPS_PANEL_PASSWORD=
+OPS_SESSION_SECRET=
+OPS_ALERT_SECRET=
+OPS_COPILOT_ENABLED=true
+OPS_COPILOT_DEFAULT_MODEL=gemini-2.5-flash-lite
+OPS_COPILOT_STRATEGY_MODEL=gemini-2.5-flash
+OPS_PROMOTION_MODE=suggest
+OPS_PROMOTION_CHANNELS=internal_site,meta_ads,google_ads
+CRON_SECRET=
diff --git a/.gitignore b/.gitignore
index 0ecfdfc..b12d9e6 100644
--- a/.gitignore
+++ b/.gitignore
@@ -4,3 +4,5 @@ test-results/
 .env
 .env.local
 .env.*.local
+
+.vercel
diff --git a/README.md b/README.md
index ce09118..9588f7f 100644
--- a/README.md
+++ b/README.md
@@ -1 +1,116 @@
 # RotaNota Workspace
+
+## Estado atual
+
+- localhost validado com `vercel dev`
+- `/ops` local funcional com login, overview, copiloto Gemini, growth, promocoes e relatorio semanal
+- producao publicada em `https://rota-nota.vercel.app`
+- incidente da retaguarda publicado em 2026-04-20 foi corrigido com novo deploy de producao
+
+## NorthStar
+
+`/ops` e o hub de retaguarda do `NorthStar`, o ponto central para operar o RotaNota e preparar o ecossistema para novos apps no mesmo shell administrativo.
+
+O plano atual do hub cobre:
+
+- portfolio de apps e registry operacional
+- workspace multi-app com modulos de desenvolvimento, analise, melhorias, promocoes, financas e bugs
+- Vercel, Supabase e GitHub como base de deploy, dados e codigo
+- Gemini e OpenAI / ChatGPT como trilhas separadas de IA e assistencia autorizada
+- Google Ads e Meta Ads como canais externos para growth, promocao e automacao futura
+- change requests, auditoria e review recorrente a cada 3 dias
+- trilha de app para ChatGPT via `Apps SDK + MCP`
+
+Fase atual:
+
+- o host tecnico continua no RotaNota
+- nao houve migracao de dominio ou subdominios ainda
+- a UX e a estrutura de dados ja foram preparadas para a futura topologia `NorthStar + subdominios`
+
+Documento principal de retomada:
+
+- `docs/rotanota_execution_phases.md`
+- `docs/rotanota_go_live_status.md`
+- `docs/rotanota_2026-04-20_handoff.md`
+- `docs/northstar_current_state.md`
+- `docs/northstar_go_live_monetization.md`
+- `docs/northstar_ai_ops_policy.md`
+- `docs/northstar_three_day_growth_loop.md`
+- `docs/northstar_chatgpt_app.md`
+- `docs/northstar_launch_execution.md`
+
+## Rodar localmente
+
+Para o site publico com APIs `/api/*` no mesmo ambiente, use `vercel dev`.
+
+```powershell
+vercel dev
+```
+
+Abrir:
+- `http://localhost:3000/`
+- `http://localhost:3000/ops/`
+
+## Checagem de prontidao
+
+Antes de testar checkout, premium, Gemini e retaguarda:
+
+```powershell
+node scripts/rotanota-readiness-check.js
+```
+
+O script verifica:
+- envs obrigatorias
+- URL canonica `ROTANOTA_BASE_URL`
+- Gemini
+- Supabase
+- senha da retaguarda
+
+## Banco
+
+Aplicar o schema:
+
+```text
+docs/supabase_premium_schema.sql
+```
+
+## Publicacao atual
+
+Alias oficial:
+
+- `https://rota-nota.vercel.app`
+
+Retaguarda publicada:
+
+- `https://rota-nota.vercel.app/ops/`
+
+Status validado em 2026-04-20 apos o deploy corrigido:
+
+- `POST /api/ops/login` funciona
+- `GET /api/ops/overview` funciona
+- `GET /api/ops/growth/overview` funciona
+- `GET /api/ops/reports/weekly` funciona
+- `GET /ops/app.js` publica o frontend novo da retaguarda
+- `POST /api/premium/ai-generate` responde `200` com bundle real por IA
+- `GET /api/northstar-app-manifest` responde `200`
+- `POST /api/northstar-mcp` responde `200` com bearer valido
+- `POST /api/ops/reviews/run` respondeu `200` e persistiu review run
+
+Historico importante:
+
+- antes do deploy corrigido, a producao estava servindo um build intermediario da retaguarda
+- o problema nao era senha nem Gemini
+- a causa era publicacao/versionamento do `/ops`
+
+## Proximo passo operacional
+
+Com a falha de requisicao resolvida, os proximos passos voltam a ser de evolucao operacional:
+
+- validar um pagamento real completo e monitorar activations
+- fechar a trilha externa de registro do NorthStar como app do ChatGPT
+- configurar Google Ads e Meta Ads para sair de `not_configured`
+- usar `docs/rotanota_execution_phases.md` como trilha oficial de execucao
+
+## Observacao
+
+O nome do produto e da marca e `RotaNota`. Qualquer ambiente antigo deve migrar para `ROTANOTA_BASE_URL`.
diff --git a/ambient/ambientUI.js b/ambient/ambientUI.js
index 6a7ac99..b9aad98 100644
--- a/ambient/ambientUI.js
+++ b/ambient/ambientUI.js
@@ -1,5 +1,25 @@
 const AmbientUI = {
 
+icon(name) {
+
+const icons = {
+library: `<svg class="ambient-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M6.5 5.25h2.4a1.1 1.1 0 0 1 1.1 1.1v11.4a1.1 1.1 0 0 1-1.1 1.1H6.5a1.1 1.1 0 0 1-1.1-1.1V6.35a1.1 1.1 0 0 1 1.1-1.1Z"/><path d="M11.1 4.5h2.5a1.1 1.1 0 0 1 1.1 1.1v12.9a1.1 1.1 0 0 1-1.1 1.1h-2.5a1.1 1.1 0 0 1-1.1-1.1V5.6a1.1 1.1 0 0 1 1.1-1.1Z"/><path d="M15.8 7h1.8a1 1 0 0 1 1 1v9.7a1 1 0 0 1-1 1h-1.8"/></svg>`,
+help: `<svg class="ambient-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="12" cy="12" r="8.75"/><path d="M9.7 9.45a2.52 2.52 0 0 1 4.8 1.02c0 1.8-2.35 2.16-2.35 3.95"/><circle cx="12" cy="17.2" r="1" fill="currentColor" stroke="none"/></svg>`,
+star: `<svg class="ambient-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="m12 4.9 2.2 4.45 4.92.71-3.56 3.48.84 4.9L12 16.08l-4.4 2.36.84-4.9L4.88 10.06l4.92-.71Z"/></svg>`,
+starFilled: `<svg class="ambient-icon ambient-icon--filled" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><path d="m12 4.9 2.2 4.45 4.92.71-3.56 3.48.84 4.9L12 16.08l-4.4 2.36.84-4.9L4.88 10.06l4.92-.71Z"/></svg>`,
+categories: `<svg class="ambient-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M4.75 8.25V5.8a1.05 1.05 0 0 1 1.05-1.05h2.45"/><path d="M15.75 4.75h2.45a1.05 1.05 0 0 1 1.05 1.05v2.45"/><path d="M19.25 15.75v2.45a1.05 1.05 0 0 1-1.05 1.05h-2.45"/><path d="M8.25 19.25H5.8a1.05 1.05 0 0 1-1.05-1.05v-2.45"/><path d="M8 8h3.2v3.2H8z"/><path d="M12.8 8H16v3.2h-3.2z"/><path d="M8 12.8h3.2V16H8z"/><path d="M12.8 12.8H16V16h-3.2z"/></svg>`,
+shuffle: `<svg class="ambient-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M16.5 6H19v2.5"/><path d="M5 7.5h3.7c1.1 0 2.14.5 2.83 1.36l4.94 6.28c.69.87 1.73 1.36 2.84 1.36H19"/><path d="M16.5 18H19v-2.5"/><path d="M5 16.5h3.7c1.1 0 2.14-.5 2.83-1.36l1.12-1.43"/><path d="m16.3 7.7 2.7-2.7"/></svg>`,
+close: `<svg class="ambient-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.9" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M6.5 6.5 17.5 17.5"/><path d="M17.5 6.5 6.5 17.5"/></svg>`,
+play: `<svg class="ambient-icon ambient-icon--filled" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><path d="M9 7.4a.8.8 0 0 1 1.22-.68l7.1 4.6a.8.8 0 0 1 0 1.34l-7.1 4.6A.8.8 0 0 1 9 16.52z"/></svg>`,
+pause: `<svg class="ambient-icon ambient-icon--filled" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><rect x="7.25" y="6.8" width="3.7" height="10.4" rx="1.2"/><rect x="13.05" y="6.8" width="3.7" height="10.4" rx="1.2"/></svg>`,
+prev: `<svg class="ambient-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M7 6.8v10.4"/><path d="m17 7.3-6.2 4.7L17 16.7V7.3Z" fill="currentColor" stroke="none"/></svg>`,
+next: `<svg class="ambient-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M17 6.8v10.4"/><path d="m7 7.3 6.2 4.7L7 16.7V7.3Z" fill="currentColor" stroke="none"/></svg>`
+}
+
+return icons[name] || ""
+
+},
+
 init() {
 
 const root = document.getElementById("ambientRoot")
@@ -15,7 +35,9 @@ root.innerHTML = `
 
 <div id="ambientMini" class="ambient-mini" title="Abrir player">
   <div class="ambient-mini-main">
-    <button id="ambientLibraryToggle" class="ambient-mini-icon" title="Abrir biblioteca">~</button>
+    <button id="ambientLibraryToggle" class="ambient-mini-icon" type="button" title="Abrir biblioteca" aria-label="Abrir biblioteca">
+      ${this.icon("library")}
+    </button>
 
     <div class="ambient-now">
       <div id="ambientTicker" class="ambient-ticker" aria-live="polite">
@@ -27,10 +49,10 @@ root.innerHTML = `
     </div>
 
     <div class="ambient-mini-actions">
-      <button id="ambientPrev" class="ambient-secondary-control" title="Anterior"><<</button>
-      <button id="ambientNext" class="ambient-secondary-control" title="Proxima">>></button>
-      <button id="ambientPlay" class="ambient-primary-control" title="Play ou pause">></button>
-      <button id="ambientHideMini" class="ambient-secondary-control" title="Esconder player">x</button>
+      <button id="ambientPrev" class="ambient-secondary-control" type="button" title="Faixa anterior" aria-label="Faixa anterior">${this.icon("prev")}</button>
+      <button id="ambientNext" class="ambient-secondary-control" type="button" title="Próxima faixa" aria-label="Próxima faixa">${this.icon("next")}</button>
+      <button id="ambientPlay" class="ambient-primary-control" type="button" title="Reproduzir" aria-label="Reproduzir">${this.icon("play")}</button>
+      <button id="ambientHideMini" class="ambient-secondary-control" type="button" title="Ocultar player" aria-label="Ocultar player">${this.icon("close")}</button>
     </div>
   </div>
 
@@ -56,11 +78,11 @@ root.innerHTML = `
     </div>
 
     <div class="ambient-header-actions">
-      <button id="ambientHelpBtn" title="Atalhos">?</button>
-      <button id="ambientFavBtn" title="Favoritos">*</button>
-      <button id="ambientCatBtn" title="Categorias">#</button>
-      <button id="ambientShuffle" title="Embaralhar">~</button>
-      <button id="ambientPanelHide" title="Fechar">x</button>
+      <button id="ambientHelpBtn" type="button" title="Atalhos" aria-label="Atalhos">${this.icon("help")}</button>
+      <button id="ambientFavBtn" type="button" title="Mostrar favoritos" aria-label="Mostrar favoritos">${this.icon("star")}</button>
+      <button id="ambientCatBtn" type="button" title="Categorias" aria-label="Categorias">${this.icon("categories")}</button>
+      <button id="ambientShuffle" type="button" title="Embaralhar" aria-label="Embaralhar">${this.icon("shuffle")}</button>
+      <button id="ambientPanelHide" type="button" title="Fechar biblioteca" aria-label="Fechar biblioteca">${this.icon("close")}</button>
     </div>
   </div>
 
@@ -134,7 +156,11 @@ const title = item?.title || "Escolha uma trilha para acompanhar o estudo"
 const tickerText = `${label} / ${title}`
 
 if (playButton) {
-playButton.textContent = AmbientState.playing ? "||" : ">"
+const isPlaying = AmbientState.playing
+playButton.innerHTML = this.icon(isPlaying ? "pause" : "play")
+playButton.title = isPlaying ? "Pausar" : "Reproduzir"
+playButton.setAttribute("aria-label", isPlaying ? "Pausar" : "Reproduzir")
+playButton.dataset.state = isPlaying ? "pause" : "play"
 }
 
 if (tickerPrimary) {
@@ -149,6 +175,30 @@ if (mini) {
 mini.classList.toggle("is-playing", AmbientState.playing)
 }
 
+const libraryToggle = document.getElementById("ambientLibraryToggle")
+if (libraryToggle) {
+libraryToggle.classList.toggle("is-active", AmbientState.panelMode === 0)
+}
+
+const favoriteButton = document.getElementById("ambientFavBtn")
+if (favoriteButton) {
+favoriteButton.classList.toggle("is-active", AmbientState.favoritesMode)
+favoriteButton.title = AmbientState.favoritesMode
+? "Voltar para a lista aleatória"
+: "Mostrar favoritos"
+favoriteButton.setAttribute(
+"aria-label",
+AmbientState.favoritesMode
+? "Voltar para a lista aleatória"
+: "Mostrar favoritos"
+)
+}
+
+const categoryButton = document.getElementById("ambientCatBtn")
+if (categoryButton) {
+categoryButton.classList.toggle("is-active", AmbientState.ui.categoryMenuOpen)
+}
+
 if (peek) {
 peek.classList.toggle("is-playing", AmbientState.playing)
 peek.classList.toggle("is-visible", AmbientState.panelMode === 2)
@@ -230,6 +280,7 @@ if (!selector) return
 
 selector.classList.add("hidden")
 AmbientState.ui.categoryMenuOpen = false
+this.updateShell()
 
 },
 
@@ -241,6 +292,7 @@ if (!selector) return
 selector.classList.toggle("hidden")
 AmbientState.ui.categoryMenuOpen =
 !selector.classList.contains("hidden")
+this.updateShell()
 
 },
 
@@ -267,7 +319,7 @@ const isCursor = index === AmbientState.cursor
 
 return `
 <div class="ambient-item${isPlaying ? " playing" : ""}${isCursor ? " cursor" : ""}" data-index="${index}" data-id="${item.id}">
-  <button class="fav-btn${favorite ? " active" : ""}" data-id="${item.id}" type="button">*</button>
+  <button class="fav-btn${favorite ? " active" : ""}" data-id="${item.id}" type="button" title="${favorite ? "Remover dos favoritos" : "Salvar nos favoritos"}" aria-label="${favorite ? "Remover dos favoritos" : "Salvar nos favoritos"}">${this.icon(favorite ? "starFilled" : "star")}</button>
   <img src="https://i.ytimg.com/vi/${item.id}/mqdefault.jpg" alt="${item.title}">
   <div class="ambient-item-copy">
     <div class="ambient-item-meta">${item.category || "mix"}</div>
@@ -383,7 +435,7 @@ let help = document.createElement("div")
 help.id = "ambientHelp"
 help.innerHTML = `
 <div class="ambient-help-box">
-  <button id="ambientHelpClose" type="button">x</button>
+  <button id="ambientHelpClose" type="button" aria-label="Fechar ajuda" title="Fechar ajuda">${this.icon("close")}</button>
   <h3>Atalhos do player</h3>
   <div>Alt + M: alterna painel, mini e oculto</div>
   <div>Alt + F: abre favoritos</div>
@@ -438,7 +490,7 @@ const blocked = document.createElement("div")
 blocked.id = "ambientBlocked"
 blocked.innerHTML = `
 <div class="ambient-blocked-box">
-  <button id="ambientBlockedClose" type="button">x</button>
+  <button id="ambientBlockedClose" type="button" aria-label="Fechar lista" title="Fechar lista">${this.icon("close")}</button>
   <div class="ambient-blocked-header">
     <div class="ambient-blocked-kicker">Alt + T</div>
     <h3>Faixas indisponiveis</h3>
@@ -585,6 +637,11 @@ this.togglePanel()
 
 if (peek) {
 peek.addEventListener("click", () => {
+if (document.body.classList.contains("clock-immersion-mode")) {
+AmbientPlayer.toggle()
+return
+}
+
 this.setPanelMode(1)
 })
 }
diff --git a/api/_lib/premium-entitlements.js b/api/_lib/premium-entitlements.js
index 57c7f11..500a929 100644
--- a/api/_lib/premium-entitlements.js
+++ b/api/_lib/premium-entitlements.js
@@ -1,18 +1,26 @@
 const { isSupabaseConfigured, supabaseRequest } = require("./supabase");
+const {
+    sanitizeUserId,
+    sanitizeCustomerId,
+    listLinkedCustomers,
+    findUserIdByCustomerId
+} = require("./user-accounts");
 
 const PLAN_DURATIONS_DAYS = {
     premium_monthly: 31,
     premium_annual: 366
 };
 
-function sanitizeCustomerId(value) {
-    const customerId = String(value || "").trim();
+function sanitizePaymentId(value) {
+    const paymentId = String(value || "").trim();
 
-    if (!customerId || customerId.length > 120) {
+    if (!paymentId || paymentId.length > 120) {
         return "";
     }
 
-    return /^[a-zA-Z0-9:_-]+$/.test(customerId) ? customerId : "";
+    return /^[a-zA-Z0-9:_-]+$/.test(paymentId)
+        ? paymentId
+        : "";
 }
 
 function parseExternalReference(value) {
@@ -49,17 +57,35 @@ function getCustomerIdFromPayment(payment = {}) {
     );
 }
 
+function getUserIdFromPayment(payment = {}) {
+    const metadata = getPaymentMetadata(payment);
+
+    return sanitizeUserId(
+        metadata.user_id ||
+        metadata.userId ||
+        ""
+    );
+}
+
 function getPlanIdFromPayment(payment = {}) {
     const metadata = getPaymentMetadata(payment);
     const parsedReference = parseExternalReference(payment.external_reference);
 
-    return metadata.plan_id || metadata.planId || parsedReference.planId || "premium_monthly";
+    return metadata.plan_id ||
+        metadata.planId ||
+        parsedReference.planId ||
+        "premium_monthly";
 }
 
 function getValidUntil(planId, approvedAt) {
     const days = PLAN_DURATIONS_DAYS[planId] || PLAN_DURATIONS_DAYS.premium_monthly;
-    const baseDate = approvedAt ? new Date(approvedAt) : new Date();
-    const validUntil = new Date(baseDate.getTime() + (days * 24 * 60 * 60 * 1000));
+    const baseDate = approvedAt
+        ? new Date(approvedAt)
+        : new Date();
+    const validUntil = new Date(
+        baseDate.getTime() +
+        (days * 24 * 60 * 60 * 1000)
+    );
 
     return validUntil.toISOString();
 }
@@ -74,17 +100,62 @@ function normalizeEntitlement(row) {
         };
     }
 
-    const validUntilTime = row.valid_until ? new Date(row.valid_until).getTime() : 0;
-    const isActive = row.status === "active" && (!validUntilTime || validUntilTime > Date.now());
+    const validUntilTime = row.valid_until
+        ? new Date(row.valid_until).getTime()
+        : 0;
+    const isActive = row.status === "active" &&
+        (!validUntilTime || validUntilTime > Date.now());
 
     return {
-        accessTier: isActive ? "premium" : "free",
-        subscriptionStatus: isActive ? "premium_active" : row.status || "registered_free",
+        accessTier: isActive
+            ? "premium"
+            : "free",
+        subscriptionStatus: isActive
+            ? "premium_active"
+            : row.status || "registered_free",
         premiumActive: isActive,
         entitlement: row
     };
 }
 
+function pickBestEntitlement(rows = []) {
+    const list = Array.isArray(rows)
+        ? rows.filter(Boolean)
+        : [];
+
+    if (!list.length) {
+        return null;
+    }
+
+    const scored = list
+        .map((row) => {
+            const normalized = normalizeEntitlement(row);
+            return {
+                row,
+                active: normalized.premiumActive,
+                updatedAt: row.updated_at
+                    ? new Date(row.updated_at).getTime()
+                    : 0,
+                validUntil: row.valid_until
+                    ? new Date(row.valid_until).getTime()
+                    : 0
+            };
+        })
+        .sort((left, right) => {
+            if (left.active !== right.active) {
+                return left.active ? -1 : 1;
+            }
+
+            if (left.updatedAt !== right.updatedAt) {
+                return right.updatedAt - left.updatedAt;
+            }
+
+            return right.validUntil - left.validUntil;
+        });
+
+    return scored[0].row;
+}
+
 async function recordCheckoutSession(data = {}) {
     if (!isSupabaseConfigured() || !data.customerId) {
         return {
@@ -96,6 +167,7 @@ async function recordCheckoutSession(data = {}) {
 
     const body = {
         customer_id: data.customerId,
+        user_id: sanitizeUserId(data.userId || "") || null,
         plan_id: data.planId,
         preference_id: data.preferenceId,
         external_reference: data.externalReference,
@@ -122,6 +194,7 @@ async function updateCheckoutSessionFromPayment(payment = {}) {
 
     const preferenceId = payment.preference_id || "";
     const customerId = getCustomerIdFromPayment(payment);
+    const userId = getUserIdFromPayment(payment) || await findUserIdByCustomerId(customerId);
 
     if (!preferenceId && !customerId) {
         return {
@@ -141,15 +214,44 @@ async function updateCheckoutSessionFromPayment(payment = {}) {
         body: {
             status: payment.status || "unknown",
             payment_id: String(payment.id || ""),
+            user_id: userId || null,
             provider_payload: payment
         }
     });
 }
 
-async function getPremiumStatus(customerId) {
-    const normalizedCustomerId = sanitizeCustomerId(customerId);
+async function fetchEntitlementRowsByCustomerIds(customerIds = []) {
+    if (!isSupabaseConfigured() || !customerIds.length) {
+        return [];
+    }
+
+    const ids = customerIds
+        .map((value) => sanitizeCustomerId(value))
+        .filter(Boolean);
+
+    if (!ids.length) {
+        return [];
+    }
+
+    const path = ids.length === 1
+        ? `premium_entitlements?customer_id=eq.${encodeURIComponent(ids[0])}&select=*`
+        : `premium_entitlements?customer_id=in.(${ids.map((value) => encodeURIComponent(value)).join(",")})&select=*`;
+    const response = await supabaseRequest(path);
+
+    return response.ok && Array.isArray(response.data)
+        ? response.data
+        : [];
+}
+
+async function getPremiumStatus(input = {}) {
+    const customerId = typeof input === "string"
+        ? sanitizeCustomerId(input)
+        : sanitizeCustomerId(input.customerId || input.customer_id || "");
+    const userId = typeof input === "object"
+        ? sanitizeUserId(input.userId || input.user_id || "")
+        : "";
 
-    if (!normalizedCustomerId) {
+    if (!customerId && !userId) {
         return {
             ok: false,
             configured: isSupabaseConfigured(),
@@ -171,8 +273,50 @@ async function getPremiumStatus(customerId) {
         };
     }
 
+    if (userId) {
+        const directResponse = await supabaseRequest(
+            `premium_entitlements?user_id=eq.${encodeURIComponent(userId)}&select=*`
+        );
+        const directRow = directResponse.ok && Array.isArray(directResponse.data)
+            ? pickBestEntitlement(directResponse.data)
+            : null;
+
+        if (directRow) {
+            return {
+                ok: true,
+                configured: true,
+                ...normalizeEntitlement(directRow)
+            };
+        }
+
+        const linkedCustomers = await listLinkedCustomers(userId);
+        const linkedRows = await fetchEntitlementRowsByCustomerIds(
+            linkedCustomers.map((row) => row.customer_id)
+        );
+        const linkedRow = pickBestEntitlement(linkedRows);
+
+        if (linkedRow) {
+            return {
+                ok: true,
+                configured: true,
+                ...normalizeEntitlement(linkedRow)
+            };
+        }
+    }
+
+    if (!customerId) {
+        return {
+            ok: true,
+            configured: true,
+            accessTier: "free",
+            subscriptionStatus: "registered_free",
+            premiumActive: false,
+            entitlement: null
+        };
+    }
+
     const result = await supabaseRequest(
-        `premium_entitlements?customer_id=eq.${encodeURIComponent(normalizedCustomerId)}&select=*`
+        `premium_entitlements?customer_id=eq.${encodeURIComponent(customerId)}&select=*`
     );
 
     if (!result.ok) {
@@ -187,7 +331,9 @@ async function getPremiumStatus(customerId) {
         };
     }
 
-    const row = Array.isArray(result.data) ? result.data[0] : null;
+    const row = Array.isArray(result.data)
+        ? result.data[0]
+        : null;
 
     return {
         ok: true,
@@ -196,9 +342,98 @@ async function getPremiumStatus(customerId) {
     };
 }
 
+async function fetchMercadoPagoPayment(paymentId) {
+    const normalizedPaymentId = sanitizePaymentId(paymentId);
+    const accessToken = process.env.MERCADO_PAGO_ACCESS_TOKEN;
+
+    if (!normalizedPaymentId || !accessToken) {
+        return {
+            ok: false,
+            payment: null,
+            status: !normalizedPaymentId
+                ? "missing_payment_id"
+                : "missing_access_token"
+        };
+    }
+
+    const response = await fetch(
+        `https://api.mercadopago.com/v1/payments/${encodeURIComponent(normalizedPaymentId)}`,
+        {
+            headers: {
+                Authorization: `Bearer ${accessToken}`
+            }
+        }
+    );
+
+    if (!response.ok) {
+        return {
+            ok: false,
+            payment: null,
+            status: "payment_lookup_failed"
+        };
+    }
+
+    return {
+        ok: true,
+        payment: await response.json(),
+        status: "payment_loaded"
+    };
+}
+
+async function reconcilePaymentById(paymentId) {
+    const normalizedPaymentId = sanitizePaymentId(paymentId);
+
+    if (!normalizedPaymentId) {
+        return {
+            ok: false,
+            skipped: true,
+            status: "missing_payment_id",
+            customerId: "",
+            planId: "",
+            userId: ""
+        };
+    }
+
+    const paymentResult = await fetchMercadoPagoPayment(normalizedPaymentId);
+
+    if (!paymentResult.ok || !paymentResult.payment) {
+        return {
+            ok: false,
+            skipped: false,
+            status: paymentResult.status || "payment_lookup_failed",
+            customerId: "",
+            planId: "",
+            userId: ""
+        };
+    }
+
+    const payment = paymentResult.payment;
+    const activation = await applyPaymentToEntitlement(payment);
+
+    return {
+        ok: Boolean(activation && activation.ok),
+        skipped: Boolean(activation && activation.skipped),
+        status: activation && activation.status
+            ? activation.status
+            : "payment_reconciled",
+        customerId: activation && activation.customerId
+            ? activation.customerId
+            : getCustomerIdFromPayment(payment),
+        userId: activation && activation.userId
+            ? activation.userId
+            : getUserIdFromPayment(payment),
+        planId: activation && activation.planId
+            ? activation.planId
+            : getPlanIdFromPayment(payment),
+        paymentStatus: payment.status || "",
+        payment
+    };
+}
+
 async function applyPaymentToEntitlement(payment = {}) {
     const customerId = getCustomerIdFromPayment(payment);
     const planId = getPlanIdFromPayment(payment);
+    const userId = getUserIdFromPayment(payment) || await findUserIdByCustomerId(customerId);
 
     await updateCheckoutSessionFromPayment(payment);
 
@@ -208,6 +443,7 @@ async function applyPaymentToEntitlement(payment = {}) {
             skipped: true,
             status: "supabase_not_configured",
             customerId,
+            userId,
             planId
         };
     }
@@ -218,6 +454,7 @@ async function applyPaymentToEntitlement(payment = {}) {
             skipped: true,
             status: "missing_customer_id",
             customerId,
+            userId,
             planId
         };
     }
@@ -228,6 +465,7 @@ async function applyPaymentToEntitlement(payment = {}) {
             skipped: true,
             status: `payment_${payment.status || "unknown"}`,
             customerId,
+            userId,
             planId
         };
     }
@@ -241,13 +479,16 @@ async function applyPaymentToEntitlement(payment = {}) {
         prefer: "resolution=merge-duplicates,return=representation",
         body: {
             customer_id: customerId,
+            user_id: userId || null,
             access_tier: "premium",
             status: "active",
             plan_id: planId,
             provider: "mercado_pago",
             payment_id: String(payment.id || ""),
             preference_id: String(preferenceId || ""),
-            payer_email: payment.payer && payment.payer.email ? payment.payer.email : null,
+            payer_email: payment.payer && payment.payer.email
+                ? payment.payer.email
+                : null,
             valid_until: validUntil,
             provider_payload: payment
         }
@@ -256,8 +497,11 @@ async function applyPaymentToEntitlement(payment = {}) {
     return {
         ok: result.ok,
         skipped: false,
-        status: result.ok ? "premium_active" : "entitlement_upsert_failed",
+        status: result.ok
+            ? "premium_active"
+            : "entitlement_upsert_failed",
         customerId,
+        userId,
         planId,
         validUntil,
         error: result.error
@@ -266,8 +510,11 @@ async function applyPaymentToEntitlement(payment = {}) {
 
 module.exports = {
     sanitizeCustomerId,
+    sanitizeUserId,
+    sanitizePaymentId,
     recordCheckoutSession,
     getPremiumStatus,
     applyPaymentToEntitlement,
-    parseExternalReference
+    parseExternalReference,
+    reconcilePaymentById
 };
diff --git a/api/mercado-pago/checkout.js b/api/mercado-pago/checkout.js
index f90e8b3..f21a38a 100644
--- a/api/mercado-pago/checkout.js
+++ b/api/mercado-pago/checkout.js
@@ -1,5 +1,7 @@
 const { sendJson, readJsonBody } = require("../_lib/json");
 const { recordCheckoutSession, sanitizeCustomerId } = require("../_lib/premium-entitlements");
+const { readAppSession } = require("../_lib/auth-session");
+const { recordGrowthEvent } = require("../_lib/ops-service");
 
 const MERCADO_PAGO_PREFERENCES_URL = "https://api.mercadopago.com/checkout/preferences";
 
@@ -115,7 +117,7 @@ module.exports = async function handler(req, res) {
         });
     }
 
-    const origin = process.env.STUDY_OS_BASE_URL || getOrigin(req);
+    const origin = process.env.ROTANOTA_BASE_URL || getOrigin(req);
     const successUrl = getBackUrl("MERCADO_PAGO_SUCCESS_URL", origin);
     const failureUrl = getBackUrl("MERCADO_PAGO_FAILURE_URL", origin);
     const pendingUrl = getBackUrl("MERCADO_PAGO_PENDING_URL", origin);
@@ -124,6 +126,17 @@ module.exports = async function handler(req, res) {
     const context = body.context && typeof body.context === "object"
         ? body.context
         : {};
+    const authSession = readAppSession(req);
+
+    if (!authSession.ok || !authSession.payload || !authSession.payload.userId) {
+        return sendJson(res, 401, {
+            ok: false,
+            status: "authentication_required",
+            message: "Entre com Google antes de iniciar o checkout premium."
+        });
+    }
+
+    const userId = authSession.payload.userId;
     const customerId = sanitizeCustomerId(body.customerId || context.customerId || "");
     const externalReference = `rotanota:${customerId || "guest"}:${plan.id}:${Date.now()}`;
 
@@ -142,8 +155,16 @@ module.exports = async function handler(req, res) {
         metadata: {
             plan_id: plan.id,
             customer_id: customerId,
+            user_id: userId,
             feature: context.feature || "",
-            source_step: context.sourceStep || ""
+            source_step: context.sourceStep || "",
+            promotion_campaign_id: context.promotionCampaignId || "",
+            promotion_mode: context.promotionMode || "",
+            promotion_surface: context.promotionSurface || "",
+            utm_source: context.utmSource || "",
+            utm_medium: context.utmMedium || "",
+            utm_campaign: context.utmCampaign || "",
+            utm_content: context.utmContent || ""
         },
         statement_descriptor: "ROTANOTA"
     };
@@ -212,22 +233,55 @@ module.exports = async function handler(req, res) {
 
     await recordCheckoutSession({
         customerId,
+        userId,
         planId: plan.id,
         preferenceId: mercadoPagoPayload.id,
         externalReference,
         metadata: {
             feature: context.feature || "",
             sourceStep: context.sourceStep || "",
+            materialHash: context.materialHash || "",
+            promotionCampaignId: context.promotionCampaignId || "",
+            promotionMode: context.promotionMode || "",
+            promotionSurface: context.promotionSurface || "",
+            utmSource: context.utmSource || "",
+            utmMedium: context.utmMedium || "",
+            utmCampaign: context.utmCampaign || "",
+            utmContent: context.utmContent || "",
+            landingPath: context.landingPath || "",
             checkoutUrlCreated: Boolean(checkoutUrl)
         }
     });
 
+    await recordGrowthEvent({
+        customerId,
+        eventType: "checkout_created",
+        materialHash: context.materialHash || "",
+        channel: context.channel || "internal_site",
+        utmSource: context.utmSource || "",
+        utmMedium: context.utmMedium || "",
+        utmCampaign: context.utmCampaign || "",
+        utmContent: context.utmContent || "",
+        landingPath: context.landingPath || "",
+        metadata: {
+            planId: plan.id,
+            userId,
+            feature: context.feature || "",
+            sourceStep: context.sourceStep || "",
+            preferenceId: mercadoPagoPayload.id,
+            promotionCampaignId: context.promotionCampaignId || "",
+            promotionMode: context.promotionMode || "",
+            promotionSurface: context.promotionSurface || ""
+        }
+    });
+
     return sendJson(res, 200, {
         ok: true,
         status: "checkout_created",
         provider: "mercado_pago",
         planId: plan.id,
         customerId,
+        userId,
         preferenceId: mercadoPagoPayload.id,
         checkoutUrl
     });
diff --git a/api/mercado-pago/webhook.js b/api/mercado-pago/webhook.js
index fef53bf..b77af40 100644
--- a/api/mercado-pago/webhook.js
+++ b/api/mercado-pago/webhook.js
@@ -1,6 +1,7 @@
 const crypto = require("crypto");
 const { sendJson } = require("../_lib/json");
-const { applyPaymentToEntitlement } = require("../_lib/premium-entitlements");
+const { applyPaymentToEntitlement, parseExternalReference, sanitizeCustomerId } = require("../_lib/premium-entitlements");
+const { recordGrowthEvent, recordOpsAlert } = require("../_lib/ops-service");
 
 async function readRawBody(req) {
     const chunks = [];
@@ -134,6 +135,18 @@ module.exports = async function handler(req, res) {
     const signature = verifyMercadoPagoSignature(req, body);
 
     if (!signature.ok && !signature.skipped) {
+        await recordOpsAlert({
+            eventType: "webhook_invalid_signature",
+            severity: "critical",
+            provider: "mercado_pago",
+            message: signature.message,
+            payload: {
+                headers: {
+                    requestId: req.headers["x-request-id"] || "",
+                    signature: req.headers["x-signature"] || ""
+                }
+            }
+        });
         return sendJson(res, 401, {
             ok: false,
             status: "invalid_signature",
@@ -149,6 +162,16 @@ module.exports = async function handler(req, res) {
     const payment = eventType === "payment" && paymentId
         ? await fetchPayment(paymentId)
         : null;
+    const paymentMetadata = payment && payment.metadata && typeof payment.metadata === "object"
+        ? payment.metadata
+        : {};
+    const parsedReference = parseExternalReference(payment && payment.external_reference ? payment.external_reference : "");
+    const customerId = sanitizeCustomerId(
+        paymentMetadata.customer_id ||
+        paymentMetadata.customerId ||
+        parsedReference.customerId ||
+        ""
+    );
     const premiumActivation = payment
         ? await applyPaymentToEntitlement(payment)
         : {
@@ -157,6 +180,58 @@ module.exports = async function handler(req, res) {
             status: "payment_not_loaded"
         };
 
+    if (paymentId && !payment) {
+        await recordOpsAlert({
+            eventType: "webhook_payment_not_loaded",
+            severity: "warning",
+            provider: "mercado_pago",
+            message: "O webhook chegou, mas o pagamento nao foi carregado no provider.",
+            payload: {
+                paymentId,
+                eventType,
+                action
+            }
+        });
+    }
+
+    await recordGrowthEvent({
+        customerId,
+        eventType: "webhook_received",
+        metadata: {
+            paymentId,
+            eventType,
+            action,
+            paymentStatus: payment ? payment.status || "" : "",
+            signature: signature.skipped ? "not_configured" : "valid"
+        }
+    });
+
+    if (premiumActivation && premiumActivation.ok && !premiumActivation.skipped) {
+        await recordGrowthEvent({
+            customerId: premiumActivation.customerId || customerId,
+            eventType: "premium_activated",
+            metadata: {
+                paymentId,
+                planId: premiumActivation.planId || "",
+                validUntil: premiumActivation.validUntil || ""
+            }
+        });
+    }
+
+    if (premiumActivation && !premiumActivation.ok && !premiumActivation.skipped) {
+        await recordOpsAlert({
+            eventType: "premium_activation_failed",
+            severity: "critical",
+            provider: "mercado_pago",
+            message: "Falha ao aplicar o pagamento ao entitlement premium.",
+            payload: {
+                paymentId,
+                status: premiumActivation.status || "",
+                error: premiumActivation.error || null
+            }
+        });
+    }
+
     return sendJson(res, 200, {
         ok: true,
         status: "webhook_received",
diff --git a/api/premium/status.js b/api/premium/status.js
index e238fe9..879398a 100644
--- a/api/premium/status.js
+++ b/api/premium/status.js
@@ -1,5 +1,29 @@
 const { sendJson, readJsonBody } = require("../_lib/json");
-const { getPremiumStatus, sanitizeCustomerId } = require("../_lib/premium-entitlements");
+const {
+    getPremiumStatus,
+    sanitizeCustomerId,
+    sanitizePaymentId,
+    reconcilePaymentById
+} = require("../_lib/premium-entitlements");
+const { readAppSession } = require("../_lib/auth-session");
+const { getPrimaryOpsState } = require("../_lib/ops-service");
+const { isGeminiConfigured } = require("../_lib/gemini");
+const { isSupabaseConfigured, supabaseRequest } = require("../_lib/supabase");
+
+const REQUIRED_SCHEMA_TABLES = [
+    "premium_checkout_sessions",
+    "premium_entitlements",
+    "premium_study_growth_events",
+    "premium_study_ops_alerts",
+    "northstar_change_requests",
+    "northstar_review_runs",
+    "northstar_audit_log"
+];
+
+let schemaCache = {
+    checkedAt: 0,
+    ready: false
+};
 
 function getCustomerIdFromRequest(req, body = {}) {
     const host = req.headers.host || "localhost";
@@ -13,6 +37,39 @@ function getCustomerIdFromRequest(req, body = {}) {
     );
 }
 
+function getPaymentIdFromRequest(req, body = {}) {
+    const host = req.headers.host || "localhost";
+    const url = new URL(req.url || "/", `https://${host}`);
+
+    return sanitizePaymentId(
+        url.searchParams.get("paymentId") ||
+        body.paymentId ||
+        body.payment_id ||
+        ""
+    );
+}
+
+async function getSchemaReady() {
+    if (!isSupabaseConfigured()) {
+        return false;
+    }
+
+    const now = Date.now();
+    if (schemaCache.checkedAt && now - schemaCache.checkedAt < 60 * 1000) {
+        return schemaCache.ready;
+    }
+
+    const results = await Promise.all(REQUIRED_SCHEMA_TABLES.map((table) => supabaseRequest(`${table}?select=*&limit=1`)));
+    const ready = results.every((result) => result.ok);
+
+    schemaCache = {
+        checkedAt: now,
+        ready
+    };
+
+    return ready;
+}
+
 module.exports = async function handler(req, res) {
     if (req.method === "OPTIONS") {
         res.setHeader("Allow", "GET, POST, OPTIONS");
@@ -42,11 +99,52 @@ module.exports = async function handler(req, res) {
         }
     }
 
-    const customerId = getCustomerIdFromRequest(req, body);
-    const status = await getPremiumStatus(customerId);
+    const requestedCustomerId = getCustomerIdFromRequest(req, body);
+    const paymentId = getPaymentIdFromRequest(req, body);
+    const authSession = readAppSession(req);
+    const userId = authSession.ok
+        ? authSession.payload.userId
+        : "";
+    const reconciliation = paymentId
+        ? await reconcilePaymentById(paymentId)
+        : null;
+    const customerId = requestedCustomerId
+        || (reconciliation && reconciliation.customerId)
+        || "";
+    const [status, opsState, schemaReady] = await Promise.all([
+        getPremiumStatus({
+            customerId,
+            userId
+        }),
+        getPrimaryOpsState(),
+        getSchemaReady()
+    ]);
+    const premiumLike = Boolean(status && status.premiumActive);
+    const generationPaused = premiumLike
+        ? Boolean(opsState.lanes && opsState.lanes.premiumLanePaused)
+        : Boolean(opsState.lanes && opsState.lanes.freeLanePaused);
+    const aiModel = String(process.env.ROTANOTA_AI_MODEL || process.env.GEMINI_MODEL || "gemini-2.5-flash-lite").trim();
 
     return sendJson(res, status.ok ? 200 : 400, {
         ...status,
-        customerId
+        customerId,
+        userId,
+        paymentId,
+        reconciliation,
+        authenticated: authSession.ok,
+        user: authSession.ok
+            ? {
+                userId: authSession.payload.userId,
+                email: authSession.payload.email,
+                name: authSession.payload.name,
+                picture: authSession.payload.picture
+            }
+            : null,
+        aiAvailable: isGeminiConfigured(),
+        aiModel,
+        schemaReady,
+        generationPaused,
+        lanes: opsState.lanes,
+        opsThresholds: opsState.thresholds
     });
 };
diff --git a/assets/rotanota-mark.svg b/assets/rotanota-mark.svg
index 19f342b..25246da 100644
--- a/assets/rotanota-mark.svg
+++ b/assets/rotanota-mark.svg
@@ -1,21 +1,12 @@
-<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 128 128" role="img" aria-label="RotaNota">
+<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 96 96" role="img" aria-label="RotaNota">
   <defs>
-    <linearGradient id="route" x1="18" y1="106" x2="108" y2="18" gradientUnits="userSpaceOnUse">
-      <stop offset="0" stop-color="#2563ff"/>
-      <stop offset="0.52" stop-color="#00d4ff"/>
-      <stop offset="1" stop-color="#39e6a1"/>
-    </linearGradient>
-    <linearGradient id="routeDark" x1="20" y1="96" x2="94" y2="24" gradientUnits="userSpaceOnUse">
-      <stop offset="0" stop-color="#001324"/>
-      <stop offset="1" stop-color="#081324"/>
+    <linearGradient id="rnMarkGradient" x1="16" y1="16" x2="80" y2="80" gradientUnits="userSpaceOnUse">
+      <stop offset="0" stop-color="#1f5dff"/>
+      <stop offset="0.58" stop-color="#1597ff"/>
+      <stop offset="1" stop-color="#14d6ff"/>
     </linearGradient>
   </defs>
-  <rect width="128" height="128" rx="24" fill="#081324"/>
-  <path d="M30 89c6-24 29-22 44-33 12-9 12-23-6-23H28" fill="none" stroke="url(#route)" stroke-width="13" stroke-linecap="round" stroke-linejoin="round"/>
-  <path d="M27 97c8-32 37-28 55-41 10-7 13-17 7-26" fill="none" stroke="#0b1220" stroke-width="26" stroke-linecap="round" stroke-linejoin="round" opacity=".86"/>
-  <path d="M30 89c6-24 29-22 44-33 12-9 12-23-6-23H28" fill="none" stroke="url(#route)" stroke-width="13" stroke-linecap="round" stroke-linejoin="round"/>
-  <path d="M37 92c8-16 25-17 41-29" fill="none" stroke="#dff8ff" stroke-width="4" stroke-linecap="round" stroke-dasharray="10 10" opacity=".58"/>
-  <circle cx="92" cy="31" r="15" fill="#39e6a1"/>
-  <circle cx="92" cy="31" r="6" fill="url(#routeDark)"/>
-  <circle cx="25" cy="89" r="8" fill="#2563ff"/>
+  <path fill="url(#rnMarkGradient)" d="M18 16H58.5C70.93 16 81 26.07 81 38.5c0 8.54-4.34 15.34-11.47 20.08l-7.8 5.18-8.28-10.32 7.4-4.92c4.02-2.67 6.9-5.55 6.9-10.02 0-5.38-4.37-9.75-9.75-9.75H29.1L18 16Z"/>
+  <path fill="url(#rnMarkGradient)" d="M35.9 48.6H51L80 77H62.4L25.1 40.5Z"/>
+  <path fill="url(#rnMarkGradient)" d="M18 60.4V77h16.6L18 60.4Z"/>
 </svg>
diff --git a/count_answers.js b/count_answers.js
index 1e5c01f..ec20eb0 100644
--- a/count_answers.js
+++ b/count_answers.js
@@ -1,4 +1,5 @@
 const fs = require('fs');
+const path = require('path');
 
 // Função para contar distribuição de respostas corretas
 function countCorrectAnswers(filePath) {
@@ -20,5 +21,5 @@ function countCorrectAnswers(filePath) {
   return counts;
 }
 
-console.log('cultura-corporal:', countCorrectAnswers('c:/dev/study-os/questions/banks/1-serie/educacao-fisica/cultura-corporal/index.js'));
-console.log('esportes-basicos:', countCorrectAnswers('c:/dev/study-os/questions/banks/1-serie/educacao-fisica/esportes-basicos/index.js'));
\ No newline at end of file
+console.log('cultura-corporal:', countCorrectAnswers(path.join(__dirname, 'questions/banks/1-serie/educacao-fisica/cultura-corporal/index.js')));
+console.log('esportes-basicos:', countCorrectAnswers(path.join(__dirname, 'questions/banks/1-serie/educacao-fisica/esportes-basicos/index.js')));
diff --git a/css/premium.css b/css/premium.css
index ae74780..b03c326 100644
--- a/css/premium.css
+++ b/css/premium.css
@@ -1,18 +1,18 @@
 :root {
-    --bg: #f6efe6;
-    --bg-deep: #d7e4ff;
-    --ink: #18141b;
-    --muted: #6d6570;
-    --line: rgba(42, 33, 53, 0.12);
-    --glass: rgba(255, 255, 255, 0.66);
-    --glass-strong: rgba(255, 255, 255, 0.82);
-    --surface: rgba(255, 255, 255, 0.56);
-    --surface-strong: rgba(255, 255, 255, 0.9);
-    --shadow: 0 24px 60px rgba(33, 25, 40, 0.12);
-    --shadow-soft: 0 14px 30px rgba(33, 25, 40, 0.08);
-    --accent: #2558f0;
-    --accent-soft: rgba(37, 88, 240, 0.14);
-    --accent-warm: #f36c4f;
+    --bg: #edf2f8;
+    --bg-deep: #d8e3ee;
+    --ink: #1b2430;
+    --muted: #667284;
+    --line: rgba(36, 52, 73, 0.12);
+    --glass: rgba(255, 255, 255, 0.7);
+    --glass-strong: rgba(255, 255, 255, 0.86);
+    --surface: rgba(255, 255, 255, 0.62);
+    --surface-strong: rgba(255, 255, 255, 0.94);
+    --shadow: 0 24px 60px rgba(44, 58, 78, 0.12);
+    --shadow-soft: 0 14px 30px rgba(44, 58, 78, 0.08);
+    --accent: #2f6dd8;
+    --accent-soft: rgba(47, 109, 216, 0.14);
+    --accent-warm: #d95f4f;
     --success: #1ea672;
     --danger: #c14f56;
     --radius-xl: calc(36px * var(--site-scale, 1));
@@ -20,22 +20,22 @@
     --radius-md: calc(22px * var(--site-scale, 1));
     --radius-sm: calc(16px * var(--site-scale, 1));
     --content-width: min(calc(1240px * var(--site-scale, 1)), calc(100vw - 52px));
-    --theme-primary-gradient: linear-gradient(135deg, rgba(37, 88, 240, 0.94), rgba(78, 135, 255, 0.92));
-    --theme-primary-shadow: rgba(37, 88, 240, 0.24);
-    --theme-warm-gradient: linear-gradient(135deg, rgba(196, 114, 70, 0.96), rgba(219, 155, 95, 0.94));
-    --theme-warm-shadow: rgba(196, 114, 70, 0.22);
+    --theme-primary-gradient: linear-gradient(135deg, rgba(47, 109, 216, 0.94), rgba(94, 145, 225, 0.92));
+    --theme-primary-shadow: rgba(47, 109, 216, 0.22);
+    --theme-warm-gradient: linear-gradient(135deg, rgba(195, 83, 70, 0.94), rgba(219, 119, 88, 0.92));
+    --theme-warm-shadow: rgba(195, 83, 70, 0.2);
     --theme-success-gradient: linear-gradient(135deg, rgba(31, 125, 117, 0.96), rgba(93, 171, 155, 0.94));
     --theme-success-shadow: rgba(31, 125, 117, 0.22);
     --theme-success-ring: rgba(31, 125, 117, 0.12);
-    --theme-body-glow-left: rgba(255, 208, 174, 0.75);
-    --theme-body-glow-right: rgba(153, 189, 255, 0.65);
-    --theme-body-glow-bottom: rgba(255, 255, 255, 0.66);
-    --theme-before-glow: rgba(88, 138, 255, 0.16);
-    --theme-after-glow: rgba(243, 108, 79, 0.12);
-    --theme-shell-light: linear-gradient(180deg, rgba(255, 255, 255, 0.72), rgba(255, 255, 255, 0.5));
-    --theme-shell-radial: radial-gradient(circle at top, rgba(37, 88, 240, 0.08), transparent 55%);
-    --theme-shell-dark: linear-gradient(180deg, rgba(20, 27, 41, 0.9), rgba(14, 20, 31, 0.74));
-    --theme-shell-dark-radial: radial-gradient(circle at top, rgba(81, 129, 255, 0.12), transparent 55%);
+    --theme-body-glow-left: rgba(210, 222, 238, 0.8);
+    --theme-body-glow-right: rgba(176, 204, 239, 0.66);
+    --theme-body-glow-bottom: rgba(255, 255, 255, 0.72);
+    --theme-before-glow: rgba(82, 132, 205, 0.14);
+    --theme-after-glow: rgba(217, 95, 79, 0.1);
+    --theme-shell-light: linear-gradient(180deg, rgba(255, 255, 255, 0.78), rgba(246, 249, 252, 0.58));
+    --theme-shell-radial: radial-gradient(circle at top, rgba(47, 109, 216, 0.07), transparent 55%);
+    --theme-shell-dark: linear-gradient(180deg, rgba(13, 22, 35, 0.94), rgba(8, 14, 24, 0.82));
+    --theme-shell-dark-radial: radial-gradient(circle at top, rgba(75, 126, 210, 0.14), transparent 55%);
     --ui-cut-xs: calc(10px * var(--site-scale, 1));
     --ui-cut-sm: calc(12px * var(--site-scale, 1));
     --ui-cut-md: calc(16px * var(--site-scale, 1));
@@ -85,8 +85,8 @@
     --ui-display-surface:
         linear-gradient(180deg, rgba(255, 255, 255, 0.24) 0%, rgba(255, 255, 255, 0.1) 38%, rgba(255, 255, 255, 0.02) 100%),
         var(--ui-informative-surface);
-    --ui-preset-title: rgba(74, 69, 66, 0.78);
-    --ui-preset-title-script: #3b2e2a;
+    --ui-preset-title: rgba(67, 79, 96, 0.78);
+    --ui-preset-title-script: #263749;
     --ui-preset-surface:
         linear-gradient(180deg, rgba(250, 252, 255, 0.96) 0%, rgba(237, 241, 249, 0.94) 100%);
     --ui-preset-border: rgba(199, 208, 227, 0.54);
@@ -99,18 +99,18 @@
     --ui-preset-sheen:
         linear-gradient(180deg, rgba(255, 255, 255, 0.74), rgba(255, 255, 255, 0.16) 42%, transparent 68%);
     --ui-preset-clickable-surface:
-        linear-gradient(180deg, rgba(247, 252, 248, 0.98) 0%, rgba(231, 243, 236, 0.98) 100%);
-    --ui-preset-clickable-border: rgba(204, 220, 211, 0.72);
-    --ui-preset-clickable-text: #22252c;
+        linear-gradient(180deg, rgba(247, 251, 255, 0.98) 0%, rgba(230, 239, 248, 0.98) 100%);
+    --ui-preset-clickable-border: rgba(191, 211, 230, 0.76);
+    --ui-preset-clickable-text: #223040;
     --ui-preset-clickable-shadow:
         inset 0 1px 0 rgba(255, 255, 255, 0.9),
-        0 12px 26px rgba(142, 180, 159, 0.16);
+        0 12px 26px rgba(116, 150, 183, 0.15);
     --ui-preset-clickable-hover-surface:
-        linear-gradient(180deg, rgba(250, 254, 251, 1) 0%, rgba(236, 246, 240, 1) 100%);
-    --ui-preset-clickable-hover-border: rgba(178, 208, 188, 0.9);
+        linear-gradient(180deg, rgba(251, 253, 255, 1) 0%, rgba(236, 245, 252, 1) 100%);
+    --ui-preset-clickable-hover-border: rgba(158, 192, 224, 0.9);
     --ui-preset-clickable-hover-shadow:
         inset 0 1px 0 rgba(255, 255, 255, 0.94),
-        0 15px 30px rgba(126, 166, 144, 0.18);
+        0 15px 30px rgba(102, 144, 184, 0.17);
     --ui-preset-active-surface:
         linear-gradient(180deg, rgba(196, 223, 247, 0.98) 0%, rgba(173, 206, 238, 0.98) 100%);
     --ui-preset-active-border: rgba(160, 193, 230, 0.94);
@@ -216,7 +216,7 @@ body {
         radial-gradient(circle at top left, var(--theme-body-glow-left), transparent 28%),
         radial-gradient(circle at top right, var(--theme-body-glow-right), transparent 30%),
         radial-gradient(circle at 50% 100%, var(--theme-body-glow-bottom), transparent 40%),
-        linear-gradient(180deg, #fff7ee 0%, var(--bg) 48%, #eef3ff 100%);
+        linear-gradient(180deg, #f8fbff 0%, var(--bg) 48%, #e6eef7 100%);
     padding: calc(14px * var(--site-scale, 1)) calc(18px * var(--site-scale, 1)) calc(96px * var(--site-scale, 1));
     overflow-x: hidden;
     transition: background 0.28s ease, color 0.24s ease;
@@ -277,22 +277,31 @@ body::after {
 }
 
 body.dark {
-    --bg: #11151c;
-    --bg-deep: #152336;
-    --ink: #f5f4f8;
-    --muted: #9da6b4;
-    --line: rgba(255, 255, 255, 0.11);
-    --glass: rgba(13, 18, 28, 0.72);
-    --glass-strong: rgba(16, 22, 34, 0.86);
-    --surface: rgba(18, 24, 37, 0.78);
-    --surface-strong: rgba(18, 24, 37, 0.94);
+    --bg: #0a101b;
+    --bg-deep: #111f34;
+    --ink: #f7f2f1;
+    --muted: #a8b1c2;
+    --line: rgba(255, 255, 255, 0.1);
+    --glass: rgba(9, 15, 26, 0.76);
+    --glass-strong: rgba(12, 20, 34, 0.9);
+    --surface: rgba(13, 22, 38, 0.82);
+    --surface-strong: rgba(15, 25, 42, 0.95);
     --shadow: 0 30px 80px rgba(0, 0, 0, 0.36);
     --shadow-soft: 0 18px 34px rgba(0, 0, 0, 0.28);
-    --theme-body-glow-left: rgba(66, 110, 255, 0.18);
-    --theme-body-glow-right: rgba(76, 164, 255, 0.08);
-    --theme-body-glow-bottom: rgba(24, 39, 62, 0.55);
-    --theme-before-glow: rgba(88, 138, 255, 0.18);
-    --theme-after-glow: rgba(53, 77, 140, 0.16);
+    --accent: #6fa7ff;
+    --accent-soft: rgba(111, 167, 255, 0.16);
+    --accent-warm: #e06055;
+    --theme-primary-gradient: linear-gradient(135deg, rgba(63, 102, 181, 0.96), rgba(111, 167, 255, 0.9));
+    --theme-primary-shadow: rgba(63, 102, 181, 0.28);
+    --theme-warm-gradient: linear-gradient(135deg, rgba(153, 57, 61, 0.96), rgba(224, 96, 85, 0.9));
+    --theme-warm-shadow: rgba(224, 96, 85, 0.24);
+    --theme-body-glow-left: rgba(83, 127, 218, 0.22);
+    --theme-body-glow-right: rgba(208, 72, 76, 0.12);
+    --theme-body-glow-bottom: rgba(18, 31, 52, 0.58);
+    --theme-before-glow: rgba(89, 143, 241, 0.2);
+    --theme-after-glow: rgba(208, 72, 76, 0.16);
+    --theme-shell-dark: linear-gradient(180deg, rgba(15, 25, 43, 0.94), rgba(9, 16, 29, 0.84));
+    --theme-shell-dark-radial: radial-gradient(circle at top, rgba(126, 171, 255, 0.13), transparent 55%);
     --ui-clickable-surface:
         linear-gradient(180deg, rgba(30, 35, 45, 0.96), rgba(20, 24, 33, 0.94));
     --ui-clickable-border: rgba(255, 255, 255, 0.08);
@@ -309,44 +318,44 @@ body.dark {
     --ui-display-surface:
         linear-gradient(180deg, rgba(255, 255, 255, 0.06) 0%, rgba(255, 255, 255, 0.018) 40%, rgba(255, 255, 255, 0.01) 100%),
         var(--ui-informative-surface);
-    --ui-preset-title: rgba(208, 211, 223, 0.76);
-    --ui-preset-title-script: rgba(243, 233, 230, 0.94);
+    --ui-preset-title: rgba(211, 218, 232, 0.78);
+    --ui-preset-title-script: rgba(255, 207, 200, 0.96);
     --ui-preset-surface:
-        linear-gradient(180deg, rgba(41, 48, 61, 0.96) 0%, rgba(31, 37, 49, 0.98) 100%);
-    --ui-preset-border: rgba(119, 132, 161, 0.42);
-    --ui-preset-text: rgba(233, 237, 246, 0.92);
+        linear-gradient(180deg, rgba(31, 43, 63, 0.96) 0%, rgba(20, 30, 47, 0.98) 100%);
+    --ui-preset-border: rgba(99, 129, 178, 0.44);
+    --ui-preset-text: rgba(236, 242, 252, 0.94);
     --ui-preset-shadow:
         inset 0 1px 0 rgba(255, 255, 255, 0.06),
         0 14px 28px rgba(0, 0, 0, 0.24);
     --ui-preset-line:
-        linear-gradient(90deg, rgba(156, 173, 205, 0.08), rgba(156, 173, 205, 0.62), rgba(156, 173, 205, 0.08));
+        linear-gradient(90deg, rgba(135, 174, 238, 0.08), rgba(135, 174, 238, 0.54), rgba(135, 174, 238, 0.08));
     --ui-preset-sheen:
         linear-gradient(180deg, rgba(255, 255, 255, 0.08), rgba(255, 255, 255, 0.02) 42%, transparent 68%);
     --ui-preset-clickable-surface:
-        linear-gradient(180deg, rgba(57, 72, 67, 0.96) 0%, rgba(42, 54, 50, 0.98) 100%);
-    --ui-preset-clickable-border: rgba(126, 160, 144, 0.56);
-    --ui-preset-clickable-text: rgba(244, 247, 245, 0.96);
+        linear-gradient(180deg, rgba(39, 56, 83, 0.96) 0%, rgba(28, 42, 66, 0.98) 100%);
+    --ui-preset-clickable-border: rgba(105, 146, 205, 0.58);
+    --ui-preset-clickable-text: rgba(239, 246, 255, 0.96);
     --ui-preset-clickable-shadow:
         inset 0 1px 0 rgba(255, 255, 255, 0.08),
         0 14px 28px rgba(0, 0, 0, 0.24);
     --ui-preset-clickable-hover-surface:
-        linear-gradient(180deg, rgba(63, 80, 73, 1) 0%, rgba(46, 60, 55, 1) 100%);
-    --ui-preset-clickable-hover-border: rgba(151, 187, 170, 0.78);
+        linear-gradient(180deg, rgba(45, 66, 99, 1) 0%, rgba(33, 50, 77, 1) 100%);
+    --ui-preset-clickable-hover-border: rgba(127, 172, 233, 0.78);
     --ui-preset-clickable-hover-shadow:
         inset 0 1px 0 rgba(255, 255, 255, 0.1),
         0 16px 30px rgba(0, 0, 0, 0.28);
     --ui-preset-active-surface:
-        linear-gradient(180deg, rgba(117, 151, 191, 0.98) 0%, rgba(92, 127, 170, 0.98) 100%);
-    --ui-preset-active-border: rgba(152, 186, 225, 0.72);
-    --ui-preset-active-text: #eef6ff;
+        linear-gradient(180deg, rgba(169, 77, 88, 0.98) 0%, rgba(125, 50, 65, 0.98) 100%);
+    --ui-preset-active-border: rgba(232, 114, 116, 0.72);
+    --ui-preset-active-text: #fff4f2;
     --ui-preset-active-shadow:
         inset 0 1px 0 rgba(255, 255, 255, 0.08),
-        0 16px 28px rgba(18, 33, 55, 0.32);
+        0 16px 30px rgba(28, 10, 18, 0.34);
     background:
         radial-gradient(circle at top left, var(--theme-body-glow-left), transparent 30%),
         radial-gradient(circle at top right, var(--theme-body-glow-right), transparent 28%),
         radial-gradient(circle at 50% 100%, var(--theme-body-glow-bottom), transparent 38%),
-        linear-gradient(180deg, #091018 0%, #0e141d 42%, #111a26 100%);
+        linear-gradient(180deg, #050b14 0%, #09121f 42%, #111a2b 100%);
     color: var(--ink);
 }
 
@@ -683,7 +692,12 @@ body.dark #ambientRoot .ambient-panel::before {
     min-width: 0;
 }
 
+.topbar-left > #homeBtn.icon-circle {
+    display: none;
+}
+
 .rotanota-topbar-brand {
+    appearance: none;
     display: inline-flex;
     align-items: center;
     gap: 9px;
@@ -696,54 +710,199 @@ body.dark #ambientRoot .ambient-panel::before {
     box-shadow: 0 10px 22px rgba(16, 33, 58, 0.08);
     clip-path: polygon(10px 0, calc(100% - 10px) 0, 100% 10px, 100% calc(100% - 10px), calc(100% - 10px) 100%, 10px 100%, 0 calc(100% - 10px), 0 10px);
     transition: opacity 0.18s ease, transform 0.18s ease, max-width 0.18s ease;
+    color: inherit;
+    cursor: pointer;
+    font: inherit;
 }
 
-.rotanota-topbar-brand img {
+.rotanota-topbar-brand .rotanota-topbar-mark {
     width: 34px;
     height: 34px;
     flex: 0 0 auto;
     filter: drop-shadow(0 8px 14px rgba(0, 212, 255, 0.14));
 }
 
-.rotanota-topbar-brand span {
+body.dark .rotanota-topbar-brand {
+    background:
+        radial-gradient(circle at top left, rgba(0, 212, 255, 0.16), transparent 48%),
+        rgba(7, 15, 28, 0.74);
+    border-color: rgba(0, 212, 255, 0.12);
+}
+
+.topbar-right {
+    flex-wrap: wrap;
+    justify-content: flex-end;
+}
+
+.rotanota-account-slot {
+    position: relative;
+    flex: 0 0 auto;
+}
+
+.rotanota-account-btn {
+    appearance: none;
+    min-height: 42px;
+    padding: 0 12px 0 8px;
+    border-radius: 16px;
+    border: 1px solid rgba(114, 137, 173, 0.18);
+    background: rgba(255, 255, 255, 0.74);
+    color: #1e2a39;
+    display: inline-flex;
+    align-items: center;
+    gap: 10px;
+    font: inherit;
+    cursor: pointer;
+    box-shadow: 0 10px 24px rgba(23, 33, 52, 0.08);
+}
+
+.rotanota-account-btn-guest {
+    padding-right: 14px;
+}
+
+.rotanota-account-avatar,
+.rotanota-account-avatar-image {
+    width: 28px;
+    height: 28px;
+    border-radius: 50%;
+    flex: 0 0 28px;
+}
+
+.rotanota-account-avatar {
+    display: inline-flex;
+    align-items: center;
+    justify-content: center;
+    background: linear-gradient(135deg, #4a90ff, #6fd5ff);
+    color: #f7fbff;
+    font-size: 0.82rem;
+    font-weight: 800;
+}
+
+.rotanota-account-avatar-guest {
+    background: #ffffff;
+    border: 1px solid rgba(221, 228, 237, 0.98);
+    color: #4285f4;
+}
+
+.rotanota-account-avatar-image {
+    object-fit: cover;
+}
+
+.rotanota-account-copy {
     display: grid;
     gap: 1px;
-    min-width: 0;
+    text-align: left;
 }
 
-.rotanota-topbar-brand strong {
-    font-family: "Space Grotesk", "Manrope", sans-serif;
-    font-size: 1.02rem;
-    line-height: 1;
-    letter-spacing: -0.04em;
-    color: var(--ink);
+.rotanota-account-copy strong {
+    font-size: 0.88rem;
+    line-height: 1.1;
 }
 
-.rotanota-topbar-brand strong span {
-    display: inline;
-    color: #008dff;
+.rotanota-account-copy span {
+    color: #607086;
+    font-size: 0.74rem;
+    line-height: 1.2;
+    max-width: 140px;
+    overflow: hidden;
+    text-overflow: ellipsis;
+    white-space: nowrap;
 }
 
-.rotanota-topbar-brand small {
-    color: var(--muted);
-    font-size: 0.62rem;
+.rotanota-account-shell {
+    position: relative;
+}
+
+.rotanota-account-menu {
+    position: absolute;
+    top: calc(100% + 10px);
+    right: 0;
+    width: min(260px, calc(100vw - 32px));
+    padding: 10px;
+    border-radius: 18px;
+    border: 1px solid rgba(207, 216, 230, 0.96);
+    background: rgba(255, 255, 255, 0.96);
+    box-shadow: 0 24px 50px rgba(17, 24, 39, 0.16);
+    display: none;
+    gap: 8px;
+    z-index: 80;
+}
+
+.rotanota-account-menu.is-open {
+    display: grid;
+}
+
+.rotanota-account-menu-head {
+    display: grid;
+    gap: 4px;
+    padding: 4px 4px 8px;
+}
+
+.rotanota-account-menu-head strong {
+    color: #1f2937;
+    font-size: 0.92rem;
+}
+
+.rotanota-account-menu-head span {
+    color: #677588;
+    font-size: 0.78rem;
+    line-height: 1.35;
+}
+
+.rotanota-account-plan {
+    min-height: 24px;
+    width: fit-content;
+    padding: 0 10px;
+    border-radius: 999px;
+    background: rgba(92, 215, 168, 0.16);
+    color: #1f7f67;
+    display: inline-flex;
+    align-items: center;
+    font-size: 0.72rem;
     font-weight: 800;
-    line-height: 1;
-    letter-spacing: 0.1em;
+    letter-spacing: 0.06em;
     text-transform: uppercase;
-    white-space: nowrap;
 }
 
-body.dark .rotanota-topbar-brand {
-    background:
-        radial-gradient(circle at top left, rgba(0, 212, 255, 0.16), transparent 48%),
-        rgba(7, 15, 28, 0.74);
-    border-color: rgba(0, 212, 255, 0.12);
+.rotanota-account-menu-btn {
+    min-height: 42px;
+    padding: 0 12px;
+    border-radius: 12px;
+    border: 1px solid rgba(223, 229, 238, 1);
+    background: #ffffff;
+    color: #233142;
+    text-align: left;
+    font: inherit;
+    font-weight: 700;
+    cursor: pointer;
 }
 
-.topbar-right {
-    flex-wrap: wrap;
-    justify-content: flex-end;
+body.dark .rotanota-account-btn {
+    background: rgba(15, 23, 36, 0.84);
+    border-color: rgba(255, 255, 255, 0.08);
+    color: #eef5ff;
+}
+
+body.dark .rotanota-account-copy span {
+    color: rgba(206, 219, 238, 0.72);
+}
+
+body.dark .rotanota-account-menu {
+    background: rgba(12, 18, 29, 0.98);
+    border-color: rgba(255, 255, 255, 0.08);
+}
+
+body.dark .rotanota-account-menu-head strong,
+body.dark .rotanota-account-menu-btn {
+    color: #eef5ff;
+}
+
+body.dark .rotanota-account-menu-head span {
+    color: rgba(206, 219, 238, 0.72);
+}
+
+body.dark .rotanota-account-menu-btn {
+    background: rgba(255, 255, 255, 0.04);
+    border-color: rgba(255, 255, 255, 0.08);
 }
 
 .topbar-collapse-btn {
@@ -792,14 +951,26 @@ body.topbar-collapsed .topbar-left {
     gap: 0;
 }
 
-body.topbar-collapsed #homeBtn {
+body.topbar-collapsed .rotanota-topbar-brand {
     width: 46px;
+    max-width: 46px;
     height: 46px;
+    padding: 6px;
     border-radius: 17px;
+    justify-content: center;
+}
+
+body.topbar-collapsed .rotanota-topbar-logo {
+    display: none !important;
+}
+
+body.topbar-collapsed .rotanota-topbar-mark {
+    display: block;
+    width: 28px;
+    height: 28px;
 }
 
 body.topbar-collapsed .timezone-topbar,
-body.topbar-collapsed .rotanota-topbar-brand,
 body.topbar-collapsed .topbar-right {
     opacity: 0;
     pointer-events: none;
@@ -982,7 +1153,6 @@ body.dark .icon-circle {
     box-shadow: var(--ui-clickable-shadow);
 }
 
-#homeBtn,
 #printQtsBtn,
 #focusModeBtn {
     color: #2f3137;
@@ -1000,19 +1170,6 @@ body.dark .icon-circle {
     border-color: rgba(159, 206, 185, 0.34);
 }
 
-#homeBtn {
-    background:
-        linear-gradient(180deg, rgba(183, 215, 240, 0.94), rgba(159, 197, 230, 0.88));
-    border-color: rgba(159, 197, 230, 0.34);
-}
-
-body.dark #homeBtn {
-    background:
-        linear-gradient(180deg, rgba(92, 120, 156, 0.38), rgba(72, 92, 120, 0.32));
-    border-color: rgba(159, 197, 230, 0.16);
-    color: #eef2fa;
-}
-
 body.dark #printQtsBtn {
     background:
         linear-gradient(180deg, rgba(129, 97, 89, 0.34), rgba(96, 73, 67, 0.3));
@@ -1038,13 +1195,15 @@ body.dark .footer-icon:not(.active-footer):not(.utility-active) {
 }
 
 .timezone-topbar {
-    min-width: 188px;
-    padding: 10px 16px;
+    width: clamp(148px, 18vw, 188px);
+    min-width: 0;
+    height: var(--site-control-size);
+    padding: 0 14px;
     border-radius: 0;
     border: 1px solid var(--ui-clickable-border);
     background: var(--ui-clickable-surface);
     color: var(--ink);
-    font-size: 0.92rem;
+    font-size: 0.84rem;
     font-weight: 600;
     box-shadow: var(--ui-clickable-shadow);
     clip-path: polygon(12px 0, calc(100% - 12px) 0, 100% 12px, 100% calc(100% - 12px), calc(100% - 12px) 100%, 12px 100%, 0 calc(100% - 12px), 0 12px);
@@ -1331,8 +1490,8 @@ body.dark[data-style="pulse"] .time-display::before {
 #pomodoroPresets {
     display: grid;
     justify-items: center;
-    gap: 14px;
-    width: min(100%, 980px);
+    gap: 12px;
+    width: min(100%, 1020px);
     margin: 0 auto;
     margin-bottom: 0;
 }
@@ -1374,35 +1533,53 @@ body.dark[data-style="pulse"] .time-display::before {
 }
 
 .pomodoro-presets-grid {
-    display: flex;
-    flex-wrap: wrap;
-    justify-content: center;
+    display: grid;
+    grid-template-columns: repeat(6, minmax(112px, 1fr));
     align-items: center;
     gap: 10px;
     width: 100%;
-    max-width: 820px;
+    max-width: 980px;
+}
+
+.pomodoro-presets-custom {
+    display: flex;
+    justify-content: center;
+    width: 100%;
+    margin-top: -2px;
 }
 
 .preset-chip {
     position: relative;
     overflow: hidden;
-    flex: 0 1 auto;
+    display: inline-flex;
+    align-items: center;
+    justify-content: center;
     max-width: 100%;
     min-height: 40px;
+    width: 100%;
     padding: 6px 15px;
     border-radius: 0;
     border: 1px solid var(--ui-preset-border);
     background: var(--ui-preset-surface);
     color: var(--ui-preset-text);
+    font-family: inherit;
     font-size: 0.76rem;
     font-weight: 700;
     box-shadow: var(--ui-preset-shadow);
     clip-path: polygon(10px 0, calc(100% - 10px) 0, 100% 10px, 100% calc(100% - 10px), calc(100% - 10px) 100%, 10px 100%, 0 calc(100% - 10px), 0 10px);
     cursor: pointer;
     transition: transform 0.18s ease, box-shadow 0.18s ease, background 0.18s ease, color 0.18s ease, border-color 0.18s ease, opacity 0.18s ease;
-    white-space: nowrap;
+    white-space: normal;
     text-align: center;
-    line-height: 1.1;
+    line-height: 1.12;
+    text-wrap: balance;
+}
+
+.pomodoro-presets-custom .preset-chip {
+    width: auto;
+    min-width: 146px;
+    min-height: 36px;
+    padding-inline: 18px;
 }
 
 .preset-chip::before {
@@ -1459,14 +1636,12 @@ body.dark[data-style="pulse"] .time-display::before {
     }
 
     .pomodoro-presets-grid {
-        max-width: min(100%, 720px);
+        grid-template-columns: repeat(3, minmax(0, 1fr));
+        max-width: min(100%, 680px);
         gap: 8px;
     }
 
     .preset-chip {
-        display: inline-flex;
-        align-items: center;
-        justify-content: center;
         min-height: 36px;
         padding: 5px 13px;
         font-size: 0.74rem;
@@ -1484,7 +1659,7 @@ body.dark[data-style="pulse"] .time-display::before {
     }
 
     .pomodoro-presets-grid {
-        max-width: 100%;
+        max-width: min(100%, 620px);
         gap: 7px;
     }
 
@@ -1507,6 +1682,7 @@ body.dark[data-style="pulse"] .time-display::before {
     }
 
     .pomodoro-presets-grid {
+        grid-template-columns: repeat(3, minmax(0, 1fr));
         gap: 7px 8px;
     }
 
@@ -1534,6 +1710,7 @@ body.dark[data-style="pulse"] .time-display::before {
     }
 
     .pomodoro-presets-grid {
+        grid-template-columns: repeat(2, minmax(0, 1fr));
         gap: 6px;
     }
 
@@ -1542,6 +1719,10 @@ body.dark[data-style="pulse"] .time-display::before {
         padding: 4px 9px;
         font-size: 0.66rem;
     }
+
+    .pomodoro-presets-custom .preset-chip {
+        min-width: 132px;
+    }
 }
 
 #cycleProgress,
@@ -2539,45 +2720,36 @@ body.dark .home-launchpad-tools {
     border-color: transparent;
 }
 
-.home-launchpad-main {
-    display: grid;
-    justify-items: center;
-    margin-bottom: 14px;
+.home-shortcuts-shell {
+    width: min(100%, 980px);
+    margin: 0 auto 14px;
 }
 
-.launchpad-primary-grid {
+.home-shortcuts-grid {
     display: grid;
-    grid-template-columns: repeat(4, minmax(0, 1fr));
-    gap: 14px;
+    grid-template-columns: repeat(5, minmax(0, 1fr));
+    gap: 12px;
 }
 
-.launchpad-card,
-.launchpad-tool {
+.home-shortcut-card {
     position: relative;
     overflow: hidden;
+    min-height: 176px;
+    padding: 16px 16px 18px;
     border: 1px solid var(--ui-clickable-border);
     background: var(--ui-clickable-surface);
     color: var(--ink);
     box-shadow: var(--ui-clickable-shadow);
     cursor: pointer;
+    display: grid;
+    align-content: start;
+    gap: 8px;
+    text-align: left;
     transition: transform 0.18s ease, box-shadow 0.18s ease, border-color 0.18s ease, background 0.18s ease;
-    border-radius: 0;
-}
-
-.launchpad-card {
-    min-height: 78px;
-    padding: 18px 20px 16px;
-    text-align: center;
-    display: flex;
-    align-items: center;
-    justify-content: center;
-    gap: 10px;
     clip-path: polygon(16px 0, calc(100% - 16px) 0, 100% 16px, 100% calc(100% - 16px), calc(100% - 16px) 100%, 16px 100%, 0 calc(100% - 16px), 0 16px);
 }
 
-.launchpad-card::before,
-.launchpad-tool::before,
-#ambientRoot .ambient-mini::after {
+.home-shortcut-card::before {
     content: "";
     position: absolute;
     left: 12px;
@@ -2588,4868 +2760,8173 @@ body.dark .home-launchpad-tools {
     background: var(--ui-clickable-line);
 }
 
-.launchpad-card:hover,
-.launchpad-tool:hover {
+.home-shortcut-card:hover {
     transform: translateY(-3px);
     box-shadow: 0 20px 34px rgba(33, 25, 40, 0.14);
     border-color: rgba(37, 88, 240, 0.22);
 }
 
-body.dark .launchpad-card:hover,
-body.dark .launchpad-tool:hover {
+body.dark .home-shortcut-card:hover {
     box-shadow: 0 20px 34px rgba(0, 0, 0, 0.34);
 }
 
-.launchpad-card-eyebrow {
-    display: none;
+.home-shortcut-kicker {
+    font-size: 0.68rem;
+    font-weight: 800;
+    letter-spacing: 0.14em;
+    text-transform: uppercase;
+    color: var(--muted);
 }
 
-.launchpad-card-title {
-    font-size: 1.06rem;
+.home-shortcut-card strong {
+    font-size: 1rem;
     font-weight: 800;
     letter-spacing: -0.03em;
+    color: var(--ink);
 }
 
-.launchpad-card-text {
-    display: none;
+.home-shortcut-status {
+    min-height: 2.5em;
+    font-size: 0.82rem;
+    line-height: 1.35;
+    color: var(--ink);
+    opacity: 0.92;
 }
 
-.home-launchpad-tools {
-    display: grid;
-    gap: 8px;
-    justify-items: center;
-    text-align: center;
+.home-shortcut-card p {
+    margin: 0;
+    font-size: 0.84rem;
+    line-height: 1.45;
+    color: var(--muted);
 }
 
-.launchpad-tool-row {
+.home-shortcut-signals {
     display: flex;
     flex-wrap: wrap;
-    gap: 10px;
-    justify-content: center;
+    gap: 8px;
+    margin-top: auto;
+    padding-top: 4px;
 }
 
-.launchpad-tool {
-    min-height: 44px;
-    padding: 0 16px;
-    font-weight: 700;
-    text-align: center;
-    justify-content: center;
-    clip-path: polygon(12px 0, calc(100% - 12px) 0, 100% 12px, 100% calc(100% - 12px), calc(100% - 12px) 100%, 12px 100%, 0 calc(100% - 12px), 0 12px);
-}
-
-.launchpad-card[data-module="pomodoro"] {
-    background:
-        linear-gradient(145deg, rgba(185, 222, 207, 0.42), rgba(255, 255, 255, 0.92)),
-        var(--surface-strong);
-    border-color: rgba(159, 206, 185, 0.26);
-}
-
-.launchpad-card[data-module="qts"] {
-    background:
-        linear-gradient(145deg, rgba(183, 215, 240, 0.42), rgba(255, 255, 255, 0.92)),
-        var(--surface-strong);
-    border-color: rgba(159, 197, 230, 0.26);
+.home-shortcut-signal {
+    display: inline-flex;
+    align-items: center;
+    min-height: 28px;
+    max-width: 100%;
+    padding: 5px 9px;
+    border: 1px solid rgba(37, 88, 240, 0.14);
+    background: rgba(255, 255, 255, 0.52);
+    color: var(--ink);
+    font-size: 0.72rem;
+    font-weight: 700;
+    line-height: 1.2;
 }
 
-.launchpad-card[data-module="questions"] {
-    background:
-        linear-gradient(145deg, rgba(230, 193, 182, 0.38), rgba(255, 255, 255, 0.92)),
-        var(--surface-strong);
-    border-color: rgba(216, 174, 160, 0.24);
+.home-stats-shell {
+    width: min(100%, 980px);
+    margin: 0 auto 18px;
+    display: none;
 }
 
-.launchpad-card[data-module="premium-study"] {
+.rotanota-home-band {
+    width: min(100%, 980px);
+    min-height: 114px;
+    margin: 0 auto 14px;
+    padding: 14px 18px;
+    border: 1px solid rgba(120, 214, 255, 0.14);
     background:
-        linear-gradient(145deg, rgba(183, 196, 255, 0.44), rgba(255, 255, 255, 0.92)),
-        var(--surface-strong);
-    border-color: rgba(117, 132, 255, 0.24);
+        radial-gradient(circle at top left, rgba(120, 214, 255, 0.12), transparent 30%),
+        linear-gradient(135deg, rgba(20, 29, 44, 0.98), rgba(34, 42, 58, 0.98));
+    box-shadow: 0 22px 54px rgba(18, 24, 37, 0.2);
+    display: grid;
+    grid-template-columns: minmax(0, 1fr) 300px;
+    gap: 16px;
+    align-items: center;
+    border-radius: 16px;
 }
 
-body.dark .launchpad-card[data-module="pomodoro"] {
-    background:
-        linear-gradient(135deg, rgba(26, 40, 67, 0.94), rgba(17, 28, 48, 0.9)),
-        var(--surface-strong);
-    border-color: rgba(89, 123, 207, 0.22);
+.rotanota-home-band-copy {
+    display: grid;
+    align-content: center;
+    gap: 8px;
+    min-width: 0;
 }
 
-body.dark .launchpad-card[data-module="qts"] {
-    background:
-        linear-gradient(135deg, rgba(24, 45, 42, 0.94), rgba(15, 31, 29, 0.9)),
-        var(--surface-strong);
-    border-color: rgba(89, 163, 144, 0.18);
+.rotanota-home-lockup {
+    display: flex;
+    align-items: center;
 }
 
-body.dark .launchpad-card[data-module="questions"] {
-    background:
-        linear-gradient(135deg, rgba(52, 35, 28, 0.94), rgba(35, 24, 20, 0.9)),
-        var(--surface-strong);
-    border-color: rgba(191, 143, 96, 0.18);
+.rotanota-home-logo {
+    width: clamp(146px, 18vw, 220px);
+    max-width: 100%;
+    height: auto;
 }
 
-body.dark .launchpad-card[data-module="premium-study"] {
-    background:
-        radial-gradient(circle at top left, rgba(121, 213, 255, 0.1), transparent 38%),
-        linear-gradient(135deg, rgba(22, 30, 57, 0.96), rgba(14, 18, 32, 0.94));
-    border-color: rgba(121, 213, 255, 0.18);
+.rotanota-home-band-text strong {
+    display: block;
+    margin-bottom: 0;
+    color: #f6fbff;
+    font-size: clamp(1rem, 1.4vw, 1.24rem);
+    line-height: 1.04;
+    letter-spacing: -0.03em;
 }
 
-.launchpad-tool[data-module="stopwatch"] {
-    border-color: rgba(79, 115, 195, 0.18);
+.rotanota-home-band-widget {
+    width: 100%;
+    min-width: 0;
+    min-height: 104px;
+    padding: 0 0 0 14px;
+    border: 0;
+    border-left: 1px solid rgba(120, 214, 255, 0.14);
+    background: transparent;
+    color: #eef4ff;
+    cursor: pointer;
+    display: grid;
+    gap: 10px;
+    text-align: left;
 }
 
-.launchpad-tool[data-module="timer"] {
-    border-color: rgba(75, 132, 123, 0.18);
+.rotanota-home-band-panel {
+    height: 102px;
+    display: grid;
+    align-content: center;
 }
 
-.launchpad-tool[data-module="calculator"] {
-    border-color: rgba(153, 111, 80, 0.18);
+.rotanota-home-band-widget-head {
+    display: flex;
+    align-items: center;
+    justify-content: space-between;
+    gap: 10px;
 }
 
-.launchpad-tool[data-module="calendar"] {
-    border-color: rgba(108, 96, 165, 0.18);
+.rotanota-home-band-widget-head span {
+    color: rgba(223, 231, 247, 0.72);
+    font-size: 0.7rem;
+    font-weight: 800;
+    letter-spacing: 0.14em;
+    text-transform: uppercase;
 }
 
-.launchpad-card.active-footer,
-.launchpad-tool.active-footer {
-    background:
-        linear-gradient(180deg, rgba(183, 215, 240, 0.88), rgba(159, 197, 230, 0.82));
-    color: #2f3137;
-    border-color: rgba(159, 197, 230, 0.32);
-    box-shadow: 0 16px 28px rgba(159, 197, 230, 0.18);
+.rotanota-home-band-widget-head strong {
+    color: #f9fbff;
+    font-size: clamp(1rem, 1.5vw, 1.34rem);
+    line-height: 1;
 }
 
-.launchpad-card.active-footer .launchpad-card-eyebrow,
-.launchpad-card.active-footer .launchpad-card-text {
-    color: rgba(255, 255, 255, 0.82);
+.rotanota-home-band-consistency {
+    display: grid;
+    grid-template-columns: repeat(7, minmax(0, 1fr));
+    grid-template-rows: repeat(4, minmax(0, 1fr));
+    gap: 5px;
+    height: 102px;
+    align-content: center;
 }
 
-.icon-circle.utility-active,
-.launchpad-tool.utility-active {
-    background:
-        linear-gradient(180deg, rgba(183, 215, 240, 0.88), rgba(159, 197, 230, 0.82));
-    color: #2f3137;
-    border-color: rgba(159, 197, 230, 0.32);
-    box-shadow: 0 16px 28px rgba(159, 197, 230, 0.18);
+.rotanota-home-band-consistency .home-stats-consistency-cell {
+    aspect-ratio: auto;
+    min-height: 0;
+    height: 100%;
+    border-color: rgba(173, 188, 218, 0.14);
+    background: rgba(255, 255, 255, 0.06);
 }
 
-.icon-circle.utility-live,
-.launchpad-tool.utility-live {
-    box-shadow:
-        0 0 0 4px var(--theme-success-ring),
-        0 18px 30px var(--theme-success-shadow);
+.rotanota-home-band-trend {
+    height: 102px;
+    display: grid;
+    grid-template-columns: repeat(6, minmax(0, 1fr));
+    gap: 7px;
+    align-items: end;
 }
 
-#utilityRoot {
-    position: fixed;
-    inset: 0;
-    pointer-events: none;
-    z-index: 1280;
+.rotanota-home-band-trend-bar-wrap {
+    height: 100%;
+    display: flex;
+    align-items: flex-end;
+    border-radius: 8px 8px 2px 2px;
+    background: rgba(255, 255, 255, 0.04);
 }
 
-#utilityRoot .utility-window {
-    position: fixed;
-    top: 140px;
-    left: calc(100vw - 320px);
-    width: min(292px, calc(100vw - 24px));
-    padding: 11px;
-    border-radius: 24px;
-    background: var(--glass);
-    border: 1px solid var(--line);
-    box-shadow: var(--shadow);
-    backdrop-filter: blur(20px);
-    color: var(--ink);
-    display: grid;
-    gap: 12px;
-    pointer-events: auto;
-    user-select: none;
+.rotanota-home-band-trend-bar-wrap.is-zero {
+    background: rgba(255, 255, 255, 0.05);
 }
 
-#utilityRoot .utility-window.utility-window-calc {
-    width: min(308px, calc(100vw - 24px));
-    border-radius: 0;
-    clip-path: polygon(18px 0, calc(100% - 18px) 0, 100% 18px, 100% calc(100% - 18px), calc(100% - 18px) 100%, 18px 100%, 0 calc(100% - 18px), 0 18px);
+.rotanota-home-band-trend-bar {
+    width: 100%;
+    border-radius: 8px 8px 2px 2px;
+    background: linear-gradient(180deg, rgba(120, 214, 255, 0.98), rgba(84, 159, 255, 0.42));
+    box-shadow: 0 10px 24px rgba(67, 144, 255, 0.16);
 }
 
-#utilityRoot .utility-window.utility-window-calc.is-scientific {
-    width: min(384px, calc(100vw - 24px));
+.rotanota-home-band-trend-empty {
+    color: rgba(223, 231, 247, 0.64);
+    font-size: 0.84rem;
+    line-height: 1.4;
 }
 
-#utilityRoot .utility-window[data-tool="stopwatch"],
-#utilityRoot .utility-window[data-tool="timer"] {
-    width: min(336px, calc(100vw - 24px));
-    gap: 14px;
+.rotanota-home-band-rhythm {
+    display: grid;
+    gap: 7px;
+    height: 102px;
+    align-content: center;
 }
 
-#utilityRoot .utility-window.is-dragging {
-    cursor: grabbing;
+.rotanota-home-band-rhythm-row {
+    display: grid;
+    gap: 6px;
 }
 
-#utilityRoot .utility-window-header {
+.rotanota-home-band-rhythm-meta {
     display: flex;
     align-items: center;
     justify-content: space-between;
-    gap: 12px;
-    cursor: grab;
+    gap: 10px;
 }
 
-#utilityRoot .utility-window-copy {
-    display: grid;
-    gap: 2px;
+.rotanota-home-band-rhythm-meta span {
+    color: rgba(223, 231, 247, 0.68);
+    font-size: 0.74rem;
+    font-weight: 700;
+    letter-spacing: 0.08em;
+    text-transform: uppercase;
 }
 
-#utilityRoot .utility-window-kicker {
-    font-size: 0.64rem;
+.rotanota-home-band-rhythm-meta strong {
+    color: #f5fbff;
+    font-size: 0.9rem;
     font-weight: 800;
-    letter-spacing: 0.16em;
-    text-transform: uppercase;
-    color: var(--muted);
 }
 
-#utilityRoot .utility-window-title {
-    font-size: 1rem;
-    font-weight: 800;
-    letter-spacing: -0.04em;
+.rotanota-home-band-rhythm-track {
+    height: 7px;
+    border-radius: 999px;
+    background: rgba(255, 255, 255, 0.08);
+    overflow: hidden;
 }
 
-#utilityRoot .utility-window-actions {
-    display: inline-flex;
-    align-items: center;
-    gap: 6px;
+.rotanota-home-band-rhythm-fill {
+    display: block;
+    height: 100%;
+    border-radius: inherit;
+    background: linear-gradient(90deg, rgba(120, 214, 255, 0.94), rgba(77, 180, 132, 0.82));
 }
 
-#utilityRoot .utility-window-close,
-#utilityRoot .utility-window-expand,
-#utilityRoot .utility-actions button,
-#utilityRoot .utility-calc-grid button,
-#utilityRoot .utility-calc-science-grid button,
-#utilityRoot .utility-calc-mode-toggle {
-    border: 1px solid var(--line);
-    background: var(--surface-strong);
-    color: var(--ink);
-    box-shadow: var(--shadow-soft);
-    transition: transform 0.18s ease, background 0.18s ease, box-shadow 0.18s ease;
+.rotanota-home-band-comparison {
+    display: grid;
+    gap: 10px;
+    height: 102px;
+    align-content: center;
 }
 
-#utilityRoot .utility-window-close,
-#utilityRoot .utility-window-expand {
-    width: 34px;
-    height: 34px;
+.rotanota-home-band-comparison-grid {
+    display: grid;
+    grid-template-columns: repeat(3, minmax(0, 1fr));
+    gap: 8px;
+}
+
+.rotanota-home-band-comparison-card {
+    display: grid;
+    gap: 4px;
+    min-height: 58px;
+    padding: 9px 10px;
     border-radius: 12px;
-    font-size: 0.78rem;
+    background: rgba(255, 255, 255, 0.06);
+    border: 1px solid rgba(255, 255, 255, 0.08);
+}
+
+.rotanota-home-band-comparison-card span {
+    color: rgba(223, 231, 247, 0.66);
+    font-size: 0.64rem;
+    font-weight: 700;
+    letter-spacing: 0.08em;
+    text-transform: uppercase;
+}
+
+.rotanota-home-band-comparison-card strong {
+    color: #f5fbff;
+    font-size: 0.92rem;
     font-weight: 800;
+    line-height: 1.15;
 }
 
-#utilityRoot .utility-window-expand.is-active {
-    background: var(--theme-success-gradient);
-    color: #fff;
-    border-color: transparent;
+.rotanota-home-band-comparison-note {
+    color: rgba(223, 231, 247, 0.72);
+    font-size: 0.78rem;
+    line-height: 1.35;
 }
 
-#utilityRoot .utility-window-body {
+.rotanota-home-band-login {
     display: grid;
     gap: 10px;
+    height: 102px;
+    align-content: center;
 }
 
-#utilityRoot .utility-window[data-tool="stopwatch"] .utility-window-body,
-#utilityRoot .utility-window[data-tool="timer"] .utility-window-body {
+.rotanota-home-band-login-badge {
+    display: grid;
+    grid-template-columns: 38px minmax(0, 1fr);
     gap: 12px;
+    align-items: center;
 }
 
-#utilityRoot .utility-calc-toolbar {
-    display: flex;
-    justify-content: flex-end;
-}
-
-#utilityRoot .utility-display,
-#utilityRoot .utility-calc-display {
-    display: flex;
-    align-items: center;
-    justify-content: center;
-    min-height: 92px;
-    padding: 18px 16px;
-    border-radius: 22px;
+.rotanota-home-band-login-g {
+    position: relative;
+    width: 38px;
+    height: 38px;
+    display: block;
+    border-radius: 50%;
     background:
-        linear-gradient(135deg, rgba(255, 255, 255, 0.78), rgba(255, 247, 238, 0.58)),
-        radial-gradient(circle at top, rgba(37, 88, 240, 0.08), transparent 56%);
-    border: 1px solid rgba(255, 255, 255, 0.48);
-    box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.42);
-    font-family: "Space Grotesk", sans-serif;
-    font-size: clamp(2.1rem, 6vw, 2.9rem);
-    font-weight: 700;
-    letter-spacing: -0.08em;
-    text-align: center;
+        conic-gradient(from 10deg, #4285f4 0 25%, #ea4335 25% 48%, #fbbc05 48% 72%, #34a853 72% 100%);
+    box-shadow: 0 10px 24px rgba(66, 133, 244, 0.16);
 }
 
-#utilityRoot .utility-window.utility-is-live .utility-display {
-    box-shadow:
-        inset 0 1px 0 rgba(255, 255, 255, 0.42),
-        0 0 0 4px rgba(18, 162, 112, 0.08);
+.rotanota-home-band-login-g::before {
+    content: "";
+    position: absolute;
+    inset: 7px;
+    border-radius: 50%;
+    background: rgba(28, 34, 49, 0.98);
 }
 
-#utilityRoot .utility-window[data-tool="stopwatch"] .utility-display,
-#utilityRoot .utility-window[data-tool="timer"] .utility-display {
-    min-height: 106px;
-    padding: 18px 18px;
-    border-radius: 0;
-    clip-path: polygon(18px 0, calc(100% - 18px) 0, 100% 18px, 100% calc(100% - 18px), calc(100% - 18px) 100%, 18px 100%, 0 calc(100% - 18px), 0 18px);
-    font-size: clamp(2.4rem, 7vw, 3rem);
-    letter-spacing: -0.07em;
+.rotanota-home-band-login-g::after {
+    content: "";
+    position: absolute;
+    top: 16px;
+    right: 4px;
+    width: 16px;
+    height: 7px;
+    border-radius: 999px;
+    background: #4285f4;
 }
 
-#utilityRoot .utility-actions {
+.rotanota-home-band-login-copy {
     display: grid;
-    grid-template-columns: repeat(3, minmax(0, 1fr));
-    gap: 8px;
+    gap: 3px;
+    min-width: 0;
 }
 
-#utilityRoot .utility-window[data-tool="stopwatch"] .utility-actions,
-#utilityRoot .utility-window[data-tool="timer"] .utility-actions {
-    gap: 10px;
+.rotanota-home-band-login-copy strong {
+    color: #f9fbff;
+    font-size: 0.95rem;
+    line-height: 1.08;
 }
 
-#utilityRoot .utility-actions button,
-#utilityRoot .utility-calc-grid button {
-    min-height: 44px;
-    border-radius: 14px;
-    font-weight: 800;
+.rotanota-home-band-login-copy span {
+    color: rgba(223, 231, 247, 0.68);
+    font-size: 0.77rem;
+    line-height: 1.3;
 }
 
-#utilityRoot .utility-window.utility-window-calc .utility-window-close,
-#utilityRoot .utility-window.utility-window-calc .utility-window-expand {
-    border-radius: 0;
-    clip-path: polygon(10px 0, calc(100% - 10px) 0, 100% 10px, 100% calc(100% - 10px), calc(100% - 10px) 100%, 10px 100%, 0 calc(100% - 10px), 0 10px);
+.rotanota-home-band-login-points {
+    display: grid;
+    grid-template-columns: repeat(3, minmax(0, 1fr));
+    gap: 6px;
+    justify-items: center;
+    align-items: center;
 }
 
-#utilityRoot .utility-window[data-tool="stopwatch"] .utility-actions button,
-#utilityRoot .utility-window[data-tool="timer"] .utility-actions button {
-    min-height: 42px;
-    border-radius: 0;
-    clip-path: polygon(12px 0, calc(100% - 12px) 0, 100% 12px, 100% calc(100% - 12px), calc(100% - 12px) 100%, 12px 100%, 0 calc(100% - 12px), 0 12px);
-    font-size: 0.94rem;
+.rotanota-home-band-login-points span {
+    display: inline-flex;
+    align-items: center;
+    justify-content: center;
+    min-height: 24px;
+    width: 100%;
+    padding: 0 9px;
+    border-radius: 999px;
+    border: 1px solid rgba(120, 214, 255, 0.14);
+    background: rgba(255, 255, 255, 0.05);
+    color: rgba(223, 231, 247, 0.78);
+    font-size: 0.7rem;
+    font-weight: 700;
+    text-align: center;
 }
 
-#utilityRoot .utility-calc-grid button,
-#utilityRoot .utility-calc-science-grid button {
-    min-height: 46px;
-    border-radius: 0;
-    background:
-        linear-gradient(180deg, rgba(255, 255, 255, 0.98), rgba(243, 243, 243, 0.94));
-    border: 1px solid rgba(214, 218, 224, 0.92);
-    color: var(--ink);
-    box-shadow: 0 10px 20px rgba(15, 23, 42, 0.04);
-    clip-path: polygon(12px 0, calc(100% - 12px) 0, 100% 12px, 100% calc(100% - 12px), calc(100% - 12px) 100%, 12px 100%, 0 calc(100% - 12px), 0 12px);
+.home-stats-stage {
+    display: grid;
+    grid-template-columns: 210px minmax(0, 1fr);
+    gap: 22px;
+    align-items: stretch;
 }
 
-#utilityRoot .utility-actions button:hover,
-#utilityRoot .utility-window-close:hover,
-#utilityRoot .utility-window-expand:hover,
-#utilityRoot .utility-calc-grid button:hover {
-    transform: translateY(-2px);
-    background: rgba(255, 255, 255, 0.96);
+.home-stats-nav {
+    display: grid;
+    gap: 10px;
+    align-content: start;
+    padding: 22px 18px;
+    background: rgba(255, 255, 255, 0.72);
+    border: 1px solid rgba(140, 159, 192, 0.18);
+    box-shadow: 0 22px 50px rgba(64, 84, 116, 0.14);
+    backdrop-filter: blur(18px);
 }
 
-#utilityRoot .utility-calc-science-grid button:hover,
-#utilityRoot .utility-calc-mode-toggle:hover {
-    transform: translateY(-2px);
-    background: rgba(255, 255, 255, 0.96);
+.home-stats-nav-btn {
+    min-height: 46px;
+    padding: 0 16px;
+    border: 1px solid rgba(130, 151, 189, 0.2);
+    background: rgba(255, 255, 255, 0.76);
+    color: var(--ink);
+    text-align: left;
+    font-size: 0.98rem;
+    font-weight: 700;
+    cursor: pointer;
+    transition: transform 0.16s ease, border-color 0.16s ease, background 0.16s ease;
 }
 
-#utilityRoot .utility-timer-fields {
-    display: grid;
-    grid-template-columns: repeat(3, minmax(0, 1fr));
-    gap: 8px;
+.home-stats-nav-btn.is-active,
+.home-stats-nav-btn:hover {
+    transform: translateX(3px);
+    border-color: rgba(41, 193, 108, 0.35);
+    background: rgba(255, 255, 255, 0.92);
 }
 
-#utilityRoot .utility-window[data-tool="timer"] .utility-timer-fields {
-    gap: 10px;
+.home-stats-widget {
+    width: 100%;
+    padding: 28px;
+    border: 1px solid rgba(124, 143, 182, 0.18);
+    background:
+        radial-gradient(circle at top right, rgba(120, 214, 255, 0.1), transparent 24%),
+        linear-gradient(135deg, rgba(21, 26, 39, 0.98), rgba(28, 34, 49, 0.98));
+    color: #eef4ff;
+    box-shadow: 0 28px 70px rgba(16, 20, 33, 0.26);
+    cursor: pointer;
+    text-align: left;
+    display: grid;
+    gap: 20px;
 }
 
-#utilityRoot .utility-timer-field {
-    display: grid;
-    gap: 6px;
+.home-stats-widget:hover {
+    transform: translateY(-2px);
+    box-shadow: 0 34px 84px rgba(16, 20, 33, 0.3);
 }
 
-#utilityRoot .utility-window[data-tool="timer"] .utility-timer-field {
-    gap: 5px;
+.home-stats-head {
+    display: flex;
+    justify-content: space-between;
+    gap: 18px;
+    align-items: flex-start;
 }
 
-#utilityRoot .utility-timer-field span {
-    font-size: 0.64rem;
+.home-stats-kicker,
+.home-login-gate-kicker {
+    display: inline-block;
+    margin-bottom: 10px;
+    color: rgba(227, 234, 248, 0.6);
+    font-size: 0.78rem;
     font-weight: 800;
     letter-spacing: 0.16em;
     text-transform: uppercase;
-    color: var(--muted);
-}
-
-#utilityRoot .utility-timer-field input {
-    width: 100%;
-    min-height: 44px;
-    padding: 10px 12px;
-    border-radius: 14px;
-    border: 1px solid var(--line);
-    background: var(--surface-strong);
-    color: var(--ink);
-    text-align: center;
-    font-size: 1rem;
-    font-weight: 700;
-    user-select: text;
 }
 
-#utilityRoot .utility-window[data-tool="timer"] .utility-timer-field input {
-    min-height: 42px;
-    border-radius: 0;
-    clip-path: polygon(12px 0, calc(100% - 12px) 0, 100% 12px, 100% calc(100% - 12px), calc(100% - 12px) 100%, 12px 100%, 0 calc(100% - 12px), 0 12px);
+.home-stats-head strong {
+    display: block;
+    margin-bottom: 6px;
+    font-size: clamp(1.45rem, 2.3vw, 2.15rem);
+    line-height: 1.05;
+    color: #f8fbff;
 }
 
-#utilityRoot .utility-timer-field input:disabled {
-    opacity: 0.56;
+.home-stats-head p {
+    margin: 0;
+    max-width: 560px;
+    color: rgba(227, 234, 248, 0.72);
+    line-height: 1.5;
 }
 
-#utilityRoot .utility-calc-display {
-    justify-content: flex-end;
-    padding-right: 18px;
-    min-height: 96px;
-    font-size: clamp(2.1rem, 6vw, 2.7rem);
-    overflow: hidden;
-    text-overflow: ellipsis;
-    background:
-        linear-gradient(180deg, rgba(255, 255, 255, 0.96), rgba(245, 245, 245, 0.9));
-    border-color: rgba(214, 218, 224, 0.8);
-    box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.72);
-    border-radius: 0;
-    clip-path: polygon(18px 0, calc(100% - 18px) 0, 100% 18px, 100% calc(100% - 18px), calc(100% - 18px) 100%, 18px 100%, 0 calc(100% - 18px), 0 18px);
+.home-stats-gate-chip {
+    display: inline-flex;
+    align-items: center;
+    justify-content: center;
+    min-height: 38px;
+    padding: 0 14px;
+    border: 1px solid rgba(122, 212, 255, 0.28);
+    background: rgba(122, 212, 255, 0.08);
+    color: #cfefff;
+    font-size: 0.82rem;
+    font-weight: 700;
+    white-space: nowrap;
 }
 
-#utilityRoot .utility-calc-grid {
+.home-stats-metrics {
     display: grid;
     grid-template-columns: repeat(4, minmax(0, 1fr));
-    gap: 8px;
+    gap: 12px;
 }
 
-#utilityRoot .utility-calc-science-grid {
+.home-stats-metric {
+    min-height: 98px;
+    padding: 16px 16px 14px;
+    border: 1px solid rgba(132, 151, 188, 0.16);
+    background: rgba(255, 255, 255, 0.04);
     display: grid;
-    grid-template-columns: repeat(5, minmax(0, 1fr));
     gap: 8px;
 }
 
-#utilityRoot .utility-calc-science-grid[hidden] {
-    display: none;
-}
-
-#utilityRoot .utility-calc-mode-toggle {
-    min-height: 34px;
-    padding: 0 14px;
-    border-radius: 0;
-    font-size: 0.72rem;
-    font-weight: 800;
-    letter-spacing: 0.12em;
+.home-stats-metric span {
+    color: rgba(223, 231, 247, 0.66);
+    font-size: 0.8rem;
+    font-weight: 700;
+    letter-spacing: 0.08em;
     text-transform: uppercase;
-    clip-path: polygon(12px 0, calc(100% - 12px) 0, 100% 12px, 100% calc(100% - 12px), calc(100% - 12px) 100%, 12px 100%, 0 calc(100% - 12px), 0 12px);
 }
 
-#utilityRoot .utility-calc-equals {
-    background: var(--surface-strong) !important;
-    color: var(--ink) !important;
-    border-color: var(--line) !important;
-    box-shadow: var(--shadow-soft) !important;
+.home-stats-metric strong {
+    font-size: clamp(1.8rem, 2.4vw, 2.55rem);
+    line-height: 1;
+    color: #ffffff;
 }
 
-#utilityRoot .utility-window:fullscreen {
-    inset: 0 !important;
-    width: 100vw !important;
-    max-width: none !important;
-    height: 100vh !important;
-    max-height: none !important;
-    padding: clamp(22px, 3vw, 36px);
-    border-radius: 0;
-    background:
-        radial-gradient(circle at top left, rgba(255, 208, 174, 0.18), transparent 30%),
-        radial-gradient(circle at top right, rgba(153, 189, 255, 0.16), transparent 30%),
-        linear-gradient(180deg, rgba(255, 252, 247, 0.96), rgba(240, 245, 255, 0.96));
+.home-stats-metric small {
+    color: rgba(223, 231, 247, 0.68);
+    font-size: 0.84rem;
+    line-height: 1.35;
 }
 
-#utilityRoot .utility-window:fullscreen::backdrop {
-    background: rgba(9, 14, 24, 0.72);
+.home-stats-content {
+    display: grid;
+    grid-template-columns: 1.1fr 0.92fr 1.08fr;
+    gap: 14px;
 }
 
-#utilityRoot .utility-window:fullscreen .utility-window-body {
-    width: min(720px, 100%);
-    margin: auto;
+.home-stats-panel {
+    min-height: 210px;
+    padding: 18px;
+    border: 1px solid rgba(132, 151, 188, 0.14);
+    background: rgba(255, 255, 255, 0.035);
+    display: grid;
+    align-content: start;
     gap: 14px;
 }
 
-#utilityRoot .utility-window[data-tool="stopwatch"]:fullscreen .utility-display,
-#utilityRoot .utility-window[data-tool="timer"]:fullscreen .utility-display {
-    min-height: clamp(220px, 34vh, 340px);
-    border-radius: 38px;
-    font-size: clamp(4.6rem, 15vw, 9rem);
+.home-stats-panel-head {
+    display: flex;
+    align-items: center;
+    justify-content: space-between;
+    gap: 12px;
 }
 
-#utilityRoot .utility-window.utility-window-calc:fullscreen .utility-window-body {
-    width: min(720px, 100%);
+.home-stats-panel-head span {
+    color: rgba(223, 231, 247, 0.66);
+    font-size: 0.82rem;
+    font-weight: 800;
+    letter-spacing: 0.12em;
+    text-transform: uppercase;
 }
 
-#utilityRoot .utility-window[data-tool="stopwatch"]:fullscreen .utility-window-body,
-#utilityRoot .utility-window[data-tool="timer"]:fullscreen .utility-window-body {
-    width: min(620px, 100%);
+.home-stats-panel-head strong {
+    color: #f5fbff;
+    font-size: 1rem;
+    font-weight: 800;
 }
 
-#utilityRoot .utility-window.utility-window-calc:fullscreen .utility-calc-display {
-    min-height: 138px;
-    font-size: clamp(3rem, 8vw, 5rem);
+.home-stats-tags {
+    display: flex;
+    flex-wrap: wrap;
+    gap: 10px;
 }
 
-#utilityRoot .utility-window.utility-window-calc:fullscreen .utility-calc-science-grid {
-    gap: 12px;
+.home-stats-tag {
+    display: inline-flex;
+    align-items: center;
+    min-height: 34px;
+    padding: 0 12px;
+    border: 1px solid rgba(130, 151, 189, 0.18);
+    background: rgba(255, 255, 255, 0.05);
+    color: #edf4ff;
+    font-size: 0.84rem;
+    font-weight: 700;
 }
 
-#utilityRoot .utility-window.utility-window-calc:fullscreen .utility-calc-grid {
-    gap: 12px;
+.home-stats-tag.is-muted {
+    color: rgba(227, 234, 248, 0.7);
 }
 
-#utilityRoot .utility-window.utility-window-calc:fullscreen .utility-calc-grid button,
-#utilityRoot .utility-window.utility-window-calc:fullscreen .utility-calc-science-grid button {
-    min-height: 72px;
-    font-size: 1.15rem;
+.home-stats-trend {
+    min-height: 140px;
+    display: grid;
+    place-items: center;
 }
 
-body.dark #utilityRoot .utility-window {
-    background: var(--glass);
-    border-color: var(--line);
+.home-stats-trend-bars {
+    width: 100%;
+    height: 146px;
+    display: grid;
+    grid-template-columns: repeat(5, minmax(0, 1fr));
+    gap: 10px;
+    align-items: end;
 }
 
-body.dark #utilityRoot .utility-window:fullscreen {
-    background:
-        radial-gradient(circle at top left, rgba(65, 109, 255, 0.16), transparent 30%),
-        radial-gradient(circle at top right, rgba(76, 164, 255, 0.12), transparent 30%),
-        linear-gradient(180deg, rgba(9, 15, 27, 0.98), rgba(13, 20, 33, 0.96));
+.home-stats-trend-bar-wrap {
+    height: 100%;
+    display: flex;
+    align-items: flex-end;
 }
 
-body.dark #utilityRoot .utility-window:fullscreen::backdrop {
-    background: rgba(2, 4, 10, 0.84);
+.home-stats-trend-bar {
+    width: 100%;
+    border-radius: 0;
+    background: linear-gradient(180deg, rgba(120, 214, 255, 0.98), rgba(84, 159, 255, 0.64));
+    box-shadow: 0 12px 26px rgba(67, 144, 255, 0.18);
 }
 
-body.dark #utilityRoot .utility-display,
-body.dark #utilityRoot .utility-calc-display {
-    background:
-        linear-gradient(135deg, rgba(20, 27, 41, 0.94), rgba(12, 18, 29, 0.86)),
-        radial-gradient(circle at top, rgba(81, 129, 255, 0.12), transparent 56%);
-    border-color: rgba(255, 255, 255, 0.08);
-    color: var(--ink);
+.home-stats-trend-empty {
+    color: rgba(223, 231, 247, 0.62);
+    line-height: 1.45;
 }
 
-body.dark #utilityRoot .utility-window-close,
-body.dark #utilityRoot .utility-window-expand,
-body.dark #utilityRoot .utility-actions button,
-body.dark #utilityRoot .utility-calc-grid button,
-body.dark #utilityRoot .utility-calc-science-grid button,
-body.dark #utilityRoot .utility-calc-mode-toggle,
-body.dark #utilityRoot .utility-timer-field input {
-    background: var(--surface-strong);
-    color: var(--ink);
-    border-color: var(--line);
+.home-stats-consistency {
+    display: grid;
+    grid-template-columns: repeat(7, minmax(0, 1fr));
+    gap: 8px;
 }
 
-.control-btn {
-    width: 62px;
-    height: 42px;
-    display: inline-flex;
-    align-items: center;
-    justify-content: center;
-    border-radius: 0;
-    border: 1px solid var(--ui-clickable-border);
-    background: var(--ui-clickable-surface);
-    color: var(--ink);
-    font-size: 1.02rem;
-    line-height: 1;
-    box-shadow: var(--ui-clickable-shadow);
-    position: relative;
-    z-index: 2;
-    overflow: hidden;
-    clip-path: polygon(var(--ui-cut-sm) 0, calc(100% - var(--ui-cut-sm)) 0, 100% var(--ui-cut-sm), 100% calc(100% - var(--ui-cut-sm)), calc(100% - var(--ui-cut-sm)) 100%, var(--ui-cut-sm) 100%, 0 calc(100% - var(--ui-cut-sm)), 0 var(--ui-cut-sm));
-    cursor: pointer;
+.home-stats-consistency-cell {
+    aspect-ratio: 1;
+    border: 1px solid rgba(132, 151, 188, 0.12);
+    background: rgba(255, 255, 255, 0.04);
 }
 
-.control-btn::before {
-    content: "";
-    position: absolute;
-    top: 0;
-    left: var(--ui-cut-sm);
-    right: var(--ui-cut-sm);
-    height: 1px;
-    background: var(--ui-clickable-line);
-    pointer-events: none;
+.home-stats-consistency-cell.is-active {
+    border-color: rgba(61, 213, 129, 0.34);
+    background: linear-gradient(180deg, rgba(44, 217, 128, 0.9), rgba(28, 167, 95, 0.7));
+    box-shadow: 0 12px 24px rgba(33, 173, 99, 0.18);
 }
 
-.control-btn::after {
-    content: "";
+.home-login-gate {
+    position: fixed;
+    inset: 0;
+    z-index: 1200;
+    display: grid;
+    place-items: center;
+}
+
+.home-login-gate.hidden {
+    display: none;
+}
+
+.home-login-gate-backdrop {
     position: absolute;
-    inset: 1px;
-    clip-path: polygon(calc(var(--ui-cut-sm) - 1px) 0, calc(100% - (var(--ui-cut-sm) - 1px)) 0, 100% calc(var(--ui-cut-sm) - 1px), 100% calc(100% - (var(--ui-cut-sm) - 1px)), calc(100% - (var(--ui-cut-sm) - 1px)) 100%, calc(var(--ui-cut-sm) - 1px) 100%, 0 calc(100% - (var(--ui-cut-sm) - 1px)), 0 calc(var(--ui-cut-sm) - 1px));
-    background: linear-gradient(180deg, rgba(255, 255, 255, 0.7), transparent 44%);
-    pointer-events: none;
-    opacity: 0.38;
+    inset: 0;
+    background: rgba(7, 10, 16, 0.52);
+    backdrop-filter: blur(6px);
 }
 
-.control-btn:hover {
-    transform: translateY(-2px);
+.home-login-gate-card {
+    position: relative;
+    z-index: 1;
+    width: min(460px, calc(100vw - 28px));
+    padding: 22px 22px 18px;
+    border: 1px solid rgba(207, 216, 230, 0.92);
+    border-radius: 20px;
+    background: linear-gradient(180deg, rgba(255, 255, 255, 0.98), rgba(248, 250, 253, 0.98));
+    box-shadow: 0 28px 70px rgba(16, 24, 40, 0.2);
+    color: #1f2937;
 }
 
-.control-btn:disabled,
-.controls.is-locked .control-btn {
-    opacity: 0.42;
-    pointer-events: none;
-    transform: none;
+.home-login-gate-shell {
+    display: grid;
+    gap: 12px;
+    align-content: start;
 }
 
-body.dark .control-btn {
-    background: var(--ui-clickable-surface);
-    color: var(--ink);
-    border-color: var(--ui-clickable-border);
+.home-login-gate-kicker {
+    color: #9aa4b2;
+    font-size: 0.72rem;
+    font-weight: 800;
+    letter-spacing: 0.18em;
+    text-transform: uppercase;
 }
 
-body.dark .control-btn:hover {
-    background: var(--ui-clickable-surface);
+.home-login-gate-card h2 {
+    margin: 0;
+    font-size: clamp(1.18rem, 2.8vw, 1.45rem);
+    line-height: 1.08;
+    color: #1f2937;
+    text-wrap: balance;
 }
 
-#moduleArea {
-    padding: 0;
-    margin-top: 16px;
+.home-login-gate-card p {
+    margin: 0;
+    color: #5f6d82;
+    line-height: 1.45;
+    font-size: 0.96rem;
 }
 
-.module.active {
-    display: block;
-    padding: 22px;
-    border-radius: var(--radius-xl);
+.home-login-gate-lockup {
+    display: grid;
+    grid-template-columns: minmax(0, 1fr) auto auto;
+    align-items: center;
+    gap: 10px;
+    min-height: 72px;
+    padding: 12px 14px;
+    border-radius: 18px;
+    overflow: hidden;
     background:
-        linear-gradient(180deg, rgba(255, 255, 255, 0.74), rgba(255, 255, 255, 0.54)),
-        radial-gradient(circle at top right, rgba(37, 88, 240, 0.07), transparent 38%);
-    backdrop-filter: blur(18px);
-    border: 1px solid var(--line);
-    box-shadow: var(--shadow);
-    min-height: 360px;
+        linear-gradient(135deg, rgba(247, 250, 255, 1), rgba(255, 255, 255, 1));
+    border: 1px solid rgba(228, 232, 239, 1);
 }
 
-body.dark .module.active {
-    background:
-        linear-gradient(180deg, rgba(18, 24, 37, 0.92), rgba(13, 18, 28, 0.82)),
-        radial-gradient(circle at top right, rgba(81, 129, 255, 0.08), transparent 40%);
+.home-login-gate-brand {
+    min-width: 0;
+    min-height: 46px;
+    display: flex;
+    align-items: center;
 }
 
-#moduleArea h2 {
-    margin-bottom: 20px;
-    font-size: clamp(1.4rem, 2vw, 1.9rem);
-    font-weight: 800;
-    letter-spacing: -0.04em;
-    text-align: left;
+.home-login-gate-brand-rotanota {
+    justify-content: flex-start;
 }
 
-#moduleArea button,
-.save-btn,
-.interval-btn,
-.row-btn {
-    border: 1px solid var(--line);
-    background: var(--surface-strong);
-    color: var(--ink);
-    box-shadow: var(--shadow-soft);
+.home-login-gate-brand-rotanota-logo {
+    display: block;
+    width: clamp(120px, 34vw, 164px);
+    height: auto;
+    filter: saturate(1.08);
 }
 
-#moduleArea input,
-#moduleArea select,
-.custom-row input {
-    border-radius: 14px;
-    border: 1px solid var(--line);
-    background: var(--surface-strong);
-    color: var(--ink);
-    padding: 10px 12px;
+.home-login-gate-brand-google {
+    justify-content: center;
 }
 
-body.dark #moduleArea button,
-body.dark .save-btn,
-body.dark .interval-btn,
-body.dark .row-btn,
-body.dark #moduleArea input,
-body.dark #moduleArea select,
-body.dark .custom-row input {
-    background: var(--surface-strong);
-    color: var(--ink);
-    border-color: var(--line);
+.home-login-gate-merge {
+    position: relative;
+    width: clamp(44px, 12vw, 68px);
+    height: 3px;
+    border-radius: 999px;
+    background: linear-gradient(
+        90deg,
+        rgba(31, 164, 255, 0.95) 0%,
+        rgba(104, 190, 255, 0.8) 38%,
+        rgba(251, 188, 5, 0.88) 58%,
+        rgba(52, 168, 83, 0.9) 78%,
+        rgba(234, 67, 53, 0.95) 100%
+    );
+    background-size: 180% 100%;
+    box-shadow: 0 0 0 1px rgba(208, 220, 236, 0.3);
 }
 
-#calendarModule,
-#alarmModule,
-#qtsModule,
-#questionsModule {
-    color: var(--ink);
+.home-login-gate-merge::after {
+    content: "";
+    position: absolute;
+    top: 50%;
+    left: 0;
+    width: 12px;
+    height: 12px;
+    border-radius: 999px;
+    background: radial-gradient(circle, rgba(255, 255, 255, 0.98) 0%, rgba(125, 201, 255, 0.95) 35%, rgba(86, 173, 255, 0) 72%);
+    transform: translate(-12%, -50%);
 }
 
-body[data-mode="qts"] {
-    --qts-detached-rail-width: clamp(86px, 8.8vw, 126px);
-    --qts-detached-rail-gap: clamp(1px, 0.2vw, 3px);
+.home-login-gate-google-mark {
+    width: 38px;
+    height: 38px;
+    flex: 0 0 38px;
+    display: inline-flex;
+    align-items: center;
+    justify-content: center;
 }
 
-#qtsModule {
-    overflow: visible;
-    --qts-content-max: min(100%, 1180px);
-    --qts-inline-gutter: clamp(1px, 0.35vw, 6px);
-    --qts-left-rail-space: var(--qts-detached-rail-width, 0px);
-    --qts-left-clearance: var(--qts-detached-rail-gap, 0px);
-    --qts-right-tools-space: 0px;
-    --qts-right-edge-gap: 1px;
+.home-login-gate-google-mark svg {
+    width: 100%;
+    height: 100%;
 }
 
-.calendar-header {
-    justify-content: space-between;
+.home-login-gate-route {
+    min-height: 0;
+    padding: 0;
+    border-radius: 0;
+    background: transparent;
+    border: 0;
+    color: #8a96a8;
+    display: flex;
     align-items: center;
-    margin-bottom: 24px;
-    font-size: 1.2rem;
+    justify-content: center;
+    text-align: center;
+    font-size: 0.84rem;
+    line-height: 1.4;
 }
 
-.calendar-weekdays,
-.calendar-grid {
-    gap: 10px;
+.home-login-gate-status {
+    min-height: 0;
+    padding: 10px 12px;
+    border-radius: 12px;
+    background: #fff5f5;
+    border: 1px solid rgba(244, 199, 199, 0.95);
+    color: #aa3f3f;
+    display: flex;
+    align-items: center;
+    font-size: 0.86rem;
+    line-height: 1.4;
 }
 
-.calendar-day,
-.weekday {
-    border-radius: 18px;
+.home-login-gate-status.is-soft {
+    background: #f7fbff;
+    border-color: rgba(193, 218, 245, 0.96);
+    color: #537196;
 }
 
-.weekday {
-    min-height: 26px;
-    display: grid;
-    place-items: center;
-    font-size: 0.84rem;
-    letter-spacing: 0.02em;
+.home-login-gate-actions {
+    display: flex;
+    justify-content: center;
+    align-items: center;
+    gap: 10px;
+    margin-top: 14px;
 }
 
-.calendar-day {
-    min-height: 92px;
-    padding: 16px;
-    background: rgba(255, 255, 255, 0.44);
-    border: 1px solid rgba(255, 255, 255, 0.4);
+.home-login-gate-google-control {
+    position: relative;
+    width: 260px;
+    min-width: 260px;
+    min-height: 40px;
+    flex: 0 0 260px;
 }
 
-body.dark .calendar-day {
-    background: rgba(255, 255, 255, 0.04);
-    border-color: rgba(255, 255, 255, 0.06);
+.home-login-gate-primary-visual {
+    width: 100%;
+    min-width: 0;
+    pointer-events: none;
+    position: relative;
+    z-index: 1;
 }
 
-.alarm-row {
-    padding: 18px;
-    border-radius: 22px;
-    background: rgba(255, 255, 255, 0.44);
-    border: 1px solid rgba(255, 255, 255, 0.38);
+.home-login-gate-google-button-slot {
+    position: absolute;
+    inset: 0;
+    width: 100%;
+    min-width: 0;
+    min-height: 40px;
+    display: flex;
+    align-items: center;
+    justify-content: center;
+    overflow: hidden;
+    border-radius: 999px;
+    z-index: 2;
+    opacity: 0;
+    background: transparent;
 }
 
-body.dark .alarm-row {
-    background: rgba(255, 255, 255, 0.04);
-    border-color: rgba(255, 255, 255, 0.06);
+.home-login-gate-google-button-slot > div,
+.home-login-gate-google-button-slot iframe {
+    max-width: 100%;
+    width: 100% !important;
+    min-width: 100%;
+    height: 40px !important;
+    min-height: 40px !important;
+    border-radius: 999px;
 }
 
-.planner-shell {
-    display: grid;
-    gap: 18px;
+.home-login-gate-primary,
+.home-login-gate-secondary {
+    min-height: 40px;
+    padding: 0 16px;
+    border: 1px solid rgba(210, 218, 229, 1);
+    border-radius: 999px;
+    font-weight: 700;
+    cursor: pointer;
 }
 
-.planner-head,
-.planner-reminders,
-.alarm-bridge {
-    padding: 20px 24px;
-    border-radius: 0;
-    background: rgba(255, 255, 255, 0.46);
-    border: 1px solid rgba(255, 255, 255, 0.44);
-    box-shadow: var(--shadow-soft);
-    clip-path: polygon(18px 0, calc(100% - 18px) 0, 100% 18px, 100% calc(100% - 18px), calc(100% - 18px) 100%, 18px 100%, 0 calc(100% - 18px), 0 18px);
+.home-login-gate-primary {
+    background: #ffffff;
+    color: #1f2937;
+    border-color: rgba(210, 218, 229, 1);
+    display: inline-flex;
+    align-items: center;
+    justify-content: center;
+    gap: 8px;
 }
 
-body.dark .planner-head,
-body.dark .planner-reminders,
-body.dark .alarm-bridge {
-    background: rgba(255, 255, 255, 0.04);
-    border-color: rgba(255, 255, 255, 0.06);
+.home-login-gate-secondary {
+    background: #ffffff;
+    color: #516073;
 }
 
-.planner-head,
-.planner-section-head,
-.alarm-bridge-actions,
-.planner-month-nav,
-.planner-head-actions {
-    display: flex;
+.home-login-gate-primary-mark {
+    width: 18px;
+    height: 18px;
+    display: inline-flex;
     align-items: center;
-    justify-content: space-between;
-    gap: 10px;
-    flex-wrap: wrap;
+    justify-content: center;
+    flex: 0 0 18px;
 }
 
-.planner-head-copy,
-.alarm-bridge-copy {
-    display: grid;
-    gap: 4px;
+.home-login-gate-primary-mark svg {
+    width: 100%;
+    height: 100%;
 }
 
-.planner-kicker {
-    font-size: 0.68rem;
-    font-weight: 800;
-    letter-spacing: 0.16em;
-    text-transform: uppercase;
-    color: var(--muted);
-}
+@keyframes home-login-gate-flow {
+    0%,
+    100% {
+        background-position: 0% 50%;
+        opacity: 0.88;
+    }
 
-.planner-head h2,
-.planner-section-head h3,
-.alarm-bridge h2 {
-    margin: 0;
-    font-size: clamp(1.4rem, 1.9vw, 1.82rem);
-    font-weight: 800;
-    letter-spacing: -0.04em;
+    50% {
+        background-position: 100% 50%;
+        opacity: 1;
+    }
 }
 
-.planner-head p,
-.planner-modal-header p,
-.alarm-bridge p,
-.planner-empty-text {
-    margin: 0;
-    color: var(--muted);
-}
+@keyframes home-login-gate-travel {
+    0%,
+    8% {
+        left: 0%;
+        opacity: 0;
+    }
 
-.planner-head-actions button,
-.planner-month-nav button,
-.planner-section-head button,
-.alarm-bridge-actions button,
-.planner-btn {
-    border: 1px solid var(--line);
-    background: var(--surface-strong);
-    color: var(--ink);
-    box-shadow: var(--shadow-soft);
-}
+    18% {
+        opacity: 0.9;
+    }
 
-.planner-head-actions button,
-.planner-section-head button,
-.alarm-bridge-actions button,
-.planner-btn {
-    min-height: 40px;
-    padding: 0 15px;
-    border-radius: 0;
-    font-weight: 700;
-    clip-path: polygon(12px 0, calc(100% - 12px) 0, 100% 12px, 100% calc(100% - 12px), calc(100% - 12px) 100%, 12px 100%, 0 calc(100% - 12px), 0 12px);
-}
+    55% {
+        left: calc(100% - 12px);
+        opacity: 1;
+    }
 
-.planner-month-nav {
-    padding: 0 4px;
-    min-height: 40px;
-}
+    76% {
+        opacity: 0.35;
+    }
 
-.planner-month-label {
-    font-family: "Space Grotesk", sans-serif;
-    font-size: 0.94rem;
-    font-weight: 700;
-    letter-spacing: -0.04em;
+    100% {
+        left: calc(100% - 12px);
+        opacity: 0;
+    }
 }
 
-.planner-month-nav button {
-    width: 40px;
-    height: 40px;
-    border-radius: 0;
-    font-size: 0.98rem;
-    font-weight: 800;
-    clip-path: polygon(12px 0, calc(100% - 12px) 0, 100% 12px, 100% calc(100% - 12px), calc(100% - 12px) 100%, 12px 100%, 0 calc(100% - 12px), 0 12px);
-}
+@keyframes home-login-gate-logo-breathe {
+    0%,
+    100% {
+        transform: translateY(0) scale(1);
+        opacity: 0.98;
+    }
 
-.calendar-grid {
-    align-items: stretch;
+    50% {
+        transform: translateY(-1px) scale(1.015);
+        opacity: 1;
+    }
 }
 
-.calendar-spacer {
-    min-height: 0;
-}
+@keyframes home-login-gate-google-float {
+    0%,
+    100% {
+        transform: translateY(0);
+    }
 
-.calendar-day {
-    appearance: none;
-    min-height: 104px;
-    display: grid;
-    align-content: space-between;
-    gap: 8px;
-    text-align: left;
-    overflow: hidden;
-    transition: transform 0.18s ease, border-color 0.18s ease, box-shadow 0.18s ease;
-    padding: 12px 14px;
-    border-radius: 0;
-    clip-path: polygon(14px 0, calc(100% - 14px) 0, 100% 14px, 100% calc(100% - 14px), calc(100% - 14px) 100%, 14px 100%, 0 calc(100% - 14px), 0 14px);
+    50% {
+        transform: translateY(-1px);
+    }
 }
 
-.calendar-day:hover {
-    transform: translateY(-3px);
-    box-shadow: 0 18px 32px rgba(33, 25, 40, 0.12);
+@media (prefers-reduced-motion: reduce) {
+    .home-login-gate-brand-rotanota-logo,
+    .home-login-gate-merge,
+    .home-login-gate-merge::after,
+    .home-login-gate-google-mark {
+        animation: none !important;
+    }
 }
 
-.calendar-day.has-event::after {
-    content: "";
-    position: absolute;
-    inset: 5px;
-    border: 2px solid rgba(243, 108, 79, 0.84);
-    border-radius: 46% 54% 50% 50% / 56% 44% 58% 42%;
-    pointer-events: none;
-}
+@media (max-width: 980px) {
+    .rotanota-home-band {
+        grid-template-columns: minmax(0, 1fr) minmax(220px, 280px);
+        min-height: 112px;
+        padding: 14px 16px;
+    }
 
-.calendar-day-number {
-    font-family: "Space Grotesk", sans-serif;
-    font-size: 1.02rem;
-    font-weight: 700;
-}
+    .home-stats-stage {
+        grid-template-columns: 1fr;
+    }
 
-.calendar-day-preview {
-    display: grid;
-    gap: 3px;
-    min-width: 0;
-}
+    .home-stats-nav {
+        grid-template-columns: repeat(4, minmax(0, 1fr));
+        align-items: stretch;
+    }
 
-.calendar-day-preview-time {
-    font-size: 0.68rem;
-    font-weight: 800;
-    letter-spacing: 0.12em;
-    text-transform: uppercase;
-    color: var(--muted);
+    .home-stats-content {
+        grid-template-columns: 1fr;
+    }
 }
 
-.calendar-day-preview-title,
-.calendar-day-empty {
-    font-size: 0.84rem;
-    line-height: 1.28;
-}
+@media (max-width: 720px) {
+    .rotanota-home-band {
+        grid-template-columns: 1fr;
+        gap: 14px;
+        min-height: 0;
+        padding: 14px;
+    }
 
-.calendar-day-preview-title {
-    font-weight: 700;
-    display: -webkit-box;
-    -webkit-line-clamp: 2;
-    -webkit-box-orient: vertical;
-    overflow: hidden;
-}
+    .home-stats-widget {
+        padding: 22px 18px;
+    }
 
-.calendar-day-empty {
-    color: var(--muted);
-    opacity: 0.76;
-}
+    .home-stats-head {
+        flex-direction: column;
+    }
 
-.planner-reminder-list,
-.alarm-bridge-list {
-    display: grid;
-    gap: 12px;
-    margin-top: 18px;
-}
+    .home-stats-metrics {
+        grid-template-columns: repeat(2, minmax(0, 1fr));
+    }
 
-.planner-reminder-card,
-.alarm-bridge-card {
-    display: grid;
-    grid-template-columns: auto 1fr auto;
-    gap: 14px;
-    align-items: center;
-    padding: 16px 18px;
-    border-radius: 22px;
-    background: rgba(255, 255, 255, 0.52);
-    border: 1px solid rgba(255, 255, 255, 0.46);
+    .home-stats-nav {
+        grid-template-columns: repeat(2, minmax(0, 1fr));
+        padding: 14px;
+    }
+
+    .rotanota-home-band-consistency {
+        gap: 6px;
+    }
+
+    .rotanota-home-band-widget {
+        padding: 12px 0 0;
+        border-left: 0;
+        border-top: 1px solid rgba(120, 214, 255, 0.14);
+    }
+
+    .rotanota-home-logo {
+        width: clamp(144px, 42vw, 198px);
+    }
+
+    .rotanota-home-band-login-points {
+        grid-template-columns: repeat(2, minmax(0, 1fr));
+    }
+
+    .home-login-gate-card {
+        padding: 18px 18px 16px;
+        border-radius: 18px;
+    }
 }
 
-.alarm-bridge-card {
-    grid-template-columns: auto 1fr;
+@media (max-width: 520px) {
+    .home-stats-metrics {
+        grid-template-columns: 1fr;
+    }
+
+    .home-login-gate-card {
+        width: min(430px, calc(100vw - 20px));
+        padding: 16px 16px 14px;
+    }
+
+    .home-login-gate-card h2 {
+        font-size: 1.04rem;
+        line-height: 1.14;
+        max-width: 15ch;
+    }
+
+    .home-login-gate-kicker {
+        font-size: 0.68rem;
+        letter-spacing: 0.14em;
+    }
+
+    .home-login-gate-actions {
+        flex-direction: column-reverse;
+        align-items: stretch;
+        gap: 8px;
+    }
+
+    .home-login-gate-google-control {
+        width: 100%;
+        min-width: 0;
+        flex-basis: auto;
+    }
+
+    .home-login-gate-google-button-slot {
+        width: 100%;
+        min-width: 0;
+    }
+
+    .home-login-gate-primary,
+    .home-login-gate-secondary {
+        width: 100%;
+    }
+
+    .home-login-gate-lockup {
+        grid-template-columns: minmax(0, 1fr) auto auto;
+        gap: 8px;
+        min-height: 68px;
+        padding: 10px 12px;
+    }
+
+    .home-login-gate-brand-rotanota,
+    .home-login-gate-brand-google {
+        justify-content: center;
+    }
+
+    .home-login-gate-merge {
+        width: clamp(34px, 11vw, 48px);
+    }
+
+    .home-login-gate-brand-rotanota-logo {
+        width: min(152px, 42vw);
+    }
+
+    .home-login-gate-google-mark {
+        width: 34px;
+        height: 34px;
+        flex-basis: 34px;
+    }
+
+    .rotanota-home-band-widget-head {
+        align-items: baseline;
+    }
+
+    .rotanota-home-band-consistency {
+        gap: 5px;
+    }
+
+    .rotanota-home-band-consistency .home-stats-consistency-cell {
+        min-height: 15px;
+    }
+
+    .rotanota-home-band-login-points {
+        grid-template-columns: 1fr;
+    }
 }
 
-body.dark .planner-reminder-card,
-body.dark .alarm-bridge-card {
-    background: rgba(255, 255, 255, 0.04);
-    border-color: rgba(255, 255, 255, 0.06);
+.home-shortcut-card-progress {
+    background:
+        linear-gradient(145deg, rgba(166, 226, 204, 0.48), rgba(255, 255, 255, 0.94)),
+        var(--surface-strong);
+    border-color: rgba(74, 167, 128, 0.24);
 }
 
-.planner-reminder-card.is-paused {
-    opacity: 0.62;
+.home-shortcut-card-questions {
+    background:
+        linear-gradient(145deg, rgba(230, 193, 182, 0.38), rgba(255, 255, 255, 0.94)),
+        var(--surface-strong);
+    border-color: rgba(216, 174, 160, 0.24);
 }
 
-.planner-reminder-time,
-.alarm-bridge-time {
-    min-width: 82px;
-    font-family: "Space Grotesk", sans-serif;
-    font-size: 1.2rem;
-    font-weight: 700;
-    letter-spacing: -0.05em;
+.home-shortcut-card-pdf {
+    background:
+        linear-gradient(145deg, rgba(183, 196, 255, 0.44), rgba(255, 255, 255, 0.94)),
+        var(--surface-strong);
+    border-color: rgba(117, 132, 255, 0.24);
 }
 
-.planner-reminder-title,
-.alarm-bridge-title,
-.planner-empty-title {
-    font-size: 1rem;
-    font-weight: 800;
+body.dark .home-shortcut-card-progress {
+    background:
+        linear-gradient(135deg, rgba(20, 50, 40, 0.94), rgba(11, 32, 26, 0.9)),
+        var(--surface-strong);
+    border-color: rgba(74, 167, 128, 0.2);
 }
 
-.planner-reminder-meta,
-.alarm-bridge-meta {
-    font-size: 0.84rem;
-    color: var(--muted);
+body.dark .home-shortcut-card-questions {
+    background:
+        linear-gradient(135deg, rgba(52, 35, 28, 0.94), rgba(35, 24, 20, 0.9)),
+        var(--surface-strong);
+    border-color: rgba(191, 143, 96, 0.18);
 }
 
-.planner-reminder-copy {
-    display: grid;
-    gap: 4px;
+body.dark .home-shortcut-card-pdf {
+    background:
+        radial-gradient(circle at top left, rgba(121, 213, 255, 0.1), transparent 38%),
+        linear-gradient(135deg, rgba(22, 30, 57, 0.96), rgba(14, 18, 32, 0.94));
+    border-color: rgba(121, 213, 255, 0.18);
 }
 
-.planner-reminder-edit {
-    min-height: 40px;
-    padding: 0 14px;
-    border-radius: 12px;
-    border: 1px solid var(--line);
-    background: var(--surface-strong);
-    color: var(--ink);
-    font-weight: 700;
+body.dark .home-shortcut-status {
+    color: rgba(234, 244, 255, 0.92);
 }
 
-.planner-empty-state {
-    padding: 22px;
-    border-radius: 22px;
-    background: rgba(255, 255, 255, 0.36);
-    border: 1px dashed rgba(42, 33, 53, 0.12);
+body.dark .home-shortcut-signal {
+    background: rgba(8, 12, 22, 0.34);
+    border-color: rgba(121, 213, 255, 0.16);
+    color: rgba(234, 244, 255, 0.92);
 }
 
-body.dark .planner-empty-state {
-    background: rgba(255, 255, 255, 0.03);
-    border-color: rgba(255, 255, 255, 0.08);
+.home-launchpad-main {
+    display: grid;
+    justify-items: center;
+    margin-bottom: 14px;
 }
 
-.planner-modal {
-    position: fixed;
-    inset: 0;
-    z-index: 1800;
+.launchpad-primary-grid {
+    display: grid;
+    grid-template-columns: repeat(4, minmax(0, 1fr));
+    gap: 14px;
+}
+
+.launchpad-card,
+.launchpad-tool {
+    position: relative;
+    overflow: hidden;
+    border: 1px solid var(--ui-clickable-border);
+    background: var(--ui-clickable-surface);
+    color: var(--ink);
+    box-shadow: var(--ui-clickable-shadow);
+    cursor: pointer;
+    transition: transform 0.18s ease, box-shadow 0.18s ease, border-color 0.18s ease, background 0.18s ease;
+    border-radius: 0;
+}
+
+.launchpad-card {
+    min-height: 78px;
+    padding: 18px 20px 16px;
+    text-align: center;
     display: flex;
     align-items: center;
     justify-content: center;
-    padding: 24px;
-}
-
-.planner-modal.hidden {
-    display: none;
+    gap: 10px;
+    clip-path: polygon(16px 0, calc(100% - 16px) 0, 100% 16px, 100% calc(100% - 16px), calc(100% - 16px) 100%, 16px 100%, 0 calc(100% - 16px), 0 16px);
 }
 
-.planner-modal-backdrop {
+.launchpad-card::before,
+.launchpad-tool::before,
+#ambientRoot .ambient-mini::after {
+    content: "";
     position: absolute;
-    inset: 0;
-    background: rgba(12, 18, 30, 0.4);
-    backdrop-filter: blur(8px);
+    left: 12px;
+    right: 12px;
+    top: 0;
+    height: 1px;
+    border-radius: 999px;
+    background: var(--ui-clickable-line);
 }
 
-.planner-modal-card {
-    position: relative;
-    width: min(620px, calc(100vw - 24px));
-    max-height: min(88vh, 920px);
-    overflow: auto;
-    padding: 28px;
-    border-radius: 30px;
-    background: var(--surface-strong);
-    border: 1px solid var(--line);
-    box-shadow: var(--shadow);
+.launchpad-card:hover,
+.launchpad-tool:hover {
+    transform: translateY(-3px);
+    box-shadow: 0 20px 34px rgba(33, 25, 40, 0.14);
+    border-color: rgba(37, 88, 240, 0.22);
 }
 
-.planner-modal-close {
-    position: absolute;
-    top: 18px;
-    right: 18px;
-    width: 38px;
-    height: 38px;
-    border-radius: 14px;
-    border: 1px solid var(--line);
-    background: var(--surface-strong);
-    color: var(--ink);
-    font-weight: 800;
+body.dark .launchpad-card:hover,
+body.dark .launchpad-tool:hover {
+    box-shadow: 0 20px 34px rgba(0, 0, 0, 0.34);
 }
 
-.planner-modal-header {
-    display: grid;
-    gap: 6px;
-    margin-bottom: 20px;
-    padding-right: 44px;
+.launchpad-card-eyebrow {
+    display: none;
 }
 
-.planner-modal-header h3 {
-    margin: 0;
-    font-size: clamp(1.45rem, 2vw, 1.95rem);
+.launchpad-card-title {
+    font-size: 1.06rem;
     font-weight: 800;
-    letter-spacing: -0.04em;
+    letter-spacing: -0.03em;
 }
 
-.planner-form {
-    display: grid;
-    gap: 14px;
+.launchpad-card-text {
+    display: none;
 }
 
-.planner-field,
-.planner-toggle {
+.home-launchpad-tools {
     display: grid;
     gap: 8px;
+    justify-items: center;
+    text-align: center;
 }
 
-.planner-field span,
-.planner-toggle span {
-    font-size: 0.72rem;
-    font-weight: 800;
-    letter-spacing: 0.14em;
-    text-transform: uppercase;
-    color: var(--muted);
+.launchpad-tool-row {
+    display: flex;
+    flex-wrap: wrap;
+    gap: 10px;
+    justify-content: center;
 }
 
-.planner-field input,
-.planner-field select,
-.planner-field textarea {
-    width: 100%;
-    padding: 12px 14px;
-    border-radius: 16px;
-    border: 1px solid var(--line);
-    background: var(--surface-strong);
-    color: var(--ink);
+.launchpad-tool {
+    min-height: 44px;
+    padding: 0 16px;
+    font-weight: 700;
+    text-align: center;
+    justify-content: center;
+    clip-path: polygon(12px 0, calc(100% - 12px) 0, 100% 12px, 100% calc(100% - 12px), calc(100% - 12px) 100%, 12px 100%, 0 calc(100% - 12px), 0 12px);
 }
 
-.planner-field textarea {
-    resize: vertical;
-    min-height: 96px;
+.launchpad-card[data-module="pomodoro"] {
+    background:
+        linear-gradient(145deg, rgba(185, 222, 207, 0.42), rgba(255, 255, 255, 0.92)),
+        var(--surface-strong);
+    border-color: rgba(159, 206, 185, 0.26);
 }
 
-.planner-field-row {
-    display: grid;
-    grid-template-columns: repeat(2, minmax(0, 1fr));
-    gap: 12px;
+.launchpad-card[data-module="qts"] {
+    background:
+        linear-gradient(145deg, rgba(183, 215, 240, 0.42), rgba(255, 255, 255, 0.92)),
+        var(--surface-strong);
+    border-color: rgba(159, 197, 230, 0.26);
 }
 
-.planner-permission-pill {
-    min-height: 46px;
-    display: inline-flex;
-    align-items: center;
-    padding: 0 14px;
-    border-radius: 16px;
-    border: 1px solid var(--line);
-    background: var(--surface-strong);
-    font-weight: 700;
+.launchpad-card[data-module="questions"] {
+    background:
+        linear-gradient(145deg, rgba(230, 193, 182, 0.38), rgba(255, 255, 255, 0.92)),
+        var(--surface-strong);
+    border-color: rgba(216, 174, 160, 0.24);
 }
 
-.planner-permission-pill.is-on {
-    background: rgba(30, 166, 114, 0.12);
-    color: #0d7b53;
+.launchpad-card[data-module="premium-study"] {
+    background:
+        linear-gradient(145deg, rgba(183, 196, 255, 0.44), rgba(255, 255, 255, 0.92)),
+        var(--surface-strong);
+    border-color: rgba(117, 132, 255, 0.24);
 }
 
-.planner-permission-pill.is-off {
-    background: rgba(193, 79, 86, 0.12);
-    color: #b14951;
+body.dark .launchpad-card[data-module="pomodoro"] {
+    background:
+        linear-gradient(135deg, rgba(26, 40, 67, 0.94), rgba(17, 28, 48, 0.9)),
+        var(--surface-strong);
+    border-color: rgba(89, 123, 207, 0.22);
 }
 
-.planner-chip-row {
-    display: flex;
-    flex-wrap: wrap;
-    gap: 8px;
+body.dark .launchpad-card[data-module="qts"] {
+    background:
+        linear-gradient(135deg, rgba(24, 45, 42, 0.94), rgba(15, 31, 29, 0.9)),
+        var(--surface-strong);
+    border-color: rgba(89, 163, 144, 0.18);
 }
 
-.planner-chip {
-    position: relative;
-    min-height: 42px;
-    display: inline-flex;
-    align-items: center;
-    justify-content: center;
-    padding: 0 14px;
-    border-radius: 999px;
-    border: 1px solid var(--line);
-    background: var(--surface-strong);
-    font-weight: 700;
-    cursor: pointer;
+body.dark .launchpad-card[data-module="questions"] {
+    background:
+        linear-gradient(135deg, rgba(52, 35, 28, 0.94), rgba(35, 24, 20, 0.9)),
+        var(--surface-strong);
+    border-color: rgba(191, 143, 96, 0.18);
 }
 
-.planner-chip input {
-    position: absolute;
-    opacity: 0;
-    pointer-events: none;
+body.dark .launchpad-card[data-module="premium-study"] {
+    background:
+        radial-gradient(circle at top left, rgba(121, 213, 255, 0.1), transparent 38%),
+        linear-gradient(135deg, rgba(22, 30, 57, 0.96), rgba(14, 18, 32, 0.94));
+    border-color: rgba(121, 213, 255, 0.18);
 }
 
-.planner-chip.is-active {
-    background: var(--theme-primary-gradient);
-    color: #fff;
-    border-color: transparent;
+.launchpad-tool[data-module="stopwatch"] {
+    border-color: rgba(79, 115, 195, 0.18);
 }
 
-.planner-toggle {
-    grid-template-columns: auto 1fr;
-    align-items: center;
+.launchpad-tool[data-module="timer"] {
+    border-color: rgba(75, 132, 123, 0.18);
 }
 
-.planner-toggle input {
-    width: 18px;
-    height: 18px;
+.launchpad-tool[data-module="calculator"] {
+    border-color: rgba(153, 111, 80, 0.18);
 }
 
-.planner-modal-actions {
-    display: flex;
-    justify-content: space-between;
-    gap: 10px;
-    flex-wrap: wrap;
-    margin-top: 4px;
+.launchpad-tool[data-module="calendar"] {
+    border-color: rgba(108, 96, 165, 0.18);
 }
 
-.planner-btn-ghost {
-    background: transparent;
+.launchpad-card.active-footer,
+.launchpad-tool.active-footer {
+    background:
+        linear-gradient(180deg, rgba(183, 215, 240, 0.88), rgba(159, 197, 230, 0.82));
+    color: #2f3137;
+    border-color: rgba(159, 197, 230, 0.32);
+    box-shadow: 0 16px 28px rgba(159, 197, 230, 0.18);
 }
 
-.planner-btn-primary {
-    background: var(--theme-primary-gradient);
-    color: #fff;
-    border-color: transparent;
-    box-shadow: 0 16px 30px var(--theme-primary-shadow);
+.launchpad-card.active-footer .launchpad-card-eyebrow,
+.launchpad-card.active-footer .launchpad-card-text {
+    color: rgba(255, 255, 255, 0.82);
 }
 
-.planner-toast-host {
+.icon-circle.utility-active,
+.launchpad-tool.utility-active {
+    background:
+        linear-gradient(180deg, rgba(183, 215, 240, 0.88), rgba(159, 197, 230, 0.82));
+    color: #2f3137;
+    border-color: rgba(159, 197, 230, 0.32);
+    box-shadow: 0 16px 28px rgba(159, 197, 230, 0.18);
+}
+
+.icon-circle.utility-live,
+.launchpad-tool.utility-live {
+    box-shadow:
+        0 0 0 4px var(--theme-success-ring),
+        0 18px 30px var(--theme-success-shadow);
+}
+
+#utilityRoot {
     position: fixed;
-    right: 18px;
-    bottom: 22px;
-    z-index: 1810;
-    display: grid;
-    gap: 10px;
+    inset: 0;
+    pointer-events: none;
+    z-index: 1280;
 }
 
-.planner-toast {
-    min-width: min(320px, calc(100vw - 32px));
-    max-width: 420px;
-    padding: 14px 16px;
-    border-radius: 18px;
-    background: var(--surface-strong);
+#utilityRoot .utility-window {
+    position: fixed;
+    top: 140px;
+    left: calc(100vw - 320px);
+    width: min(292px, calc(100vw - 24px));
+    padding: 11px;
+    border-radius: 24px;
+    background: var(--glass);
     border: 1px solid var(--line);
     box-shadow: var(--shadow);
+    backdrop-filter: blur(20px);
     color: var(--ink);
-    opacity: 0;
-    transform: translateY(10px);
-    transition: opacity 0.2s ease, transform 0.2s ease;
+    display: grid;
+    gap: 12px;
+    pointer-events: auto;
+    user-select: none;
 }
 
-.planner-toast.is-visible {
-    opacity: 1;
-    transform: translateY(0);
+#utilityRoot .utility-window.utility-window-calc {
+    width: min(308px, calc(100vw - 24px));
+    border-radius: 0;
+    clip-path: polygon(18px 0, calc(100% - 18px) 0, 100% 18px, 100% calc(100% - 18px), calc(100% - 18px) 100%, 18px 100%, 0 calc(100% - 18px), 0 18px);
 }
 
-@media (max-width: 760px) {
-    .style-panel {
-        top: 88px;
-        right: 12px;
-        left: 12px;
-        width: auto;
-    }
+#utilityRoot .utility-window.utility-window-calc.is-scientific {
+    width: min(384px, calc(100vw - 24px));
+}
 
-    .planner-head,
-    .planner-reminders,
-    .alarm-bridge {
-        padding: 18px;
-        border-radius: 24px;
-    }
+#utilityRoot .utility-window[data-tool="stopwatch"],
+#utilityRoot .utility-window[data-tool="timer"] {
+    width: min(336px, calc(100vw - 24px));
+    gap: 14px;
+}
 
-    .calendar-day {
-        min-height: 104px;
-        padding: 14px;
-    }
+#utilityRoot .utility-window.is-dragging {
+    cursor: grabbing;
+}
 
-    .planner-field-row {
-        grid-template-columns: 1fr;
-    }
+#utilityRoot .utility-window-header {
+    display: flex;
+    align-items: center;
+    justify-content: space-between;
+    gap: 12px;
+    cursor: grab;
+}
 
-    .planner-reminder-card,
-    .alarm-bridge-card {
-        grid-template-columns: 1fr;
-        align-items: start;
-    }
+#utilityRoot .utility-window-copy {
+    display: grid;
+    gap: 2px;
+}
 
-    .planner-reminder-edit {
-        width: 100%;
-    }
+#utilityRoot .utility-window-kicker {
+    font-size: 0.64rem;
+    font-weight: 800;
+    letter-spacing: 0.16em;
+    text-transform: uppercase;
+    color: var(--muted);
+}
 
-    .planner-modal {
-        padding: 12px;
-    }
+#utilityRoot .utility-window-title {
+    font-size: 1rem;
+    font-weight: 800;
+    letter-spacing: -0.04em;
+}
 
-    .planner-modal-card {
-        padding: 22px 18px;
-        border-radius: 24px;
-    }
+#utilityRoot .utility-window-actions {
+    display: inline-flex;
+    align-items: center;
+    gap: 6px;
+}
 
-    .planner-modal-actions {
-        flex-direction: column;
-    }
+#utilityRoot .utility-window-close,
+#utilityRoot .utility-window-expand,
+#utilityRoot .utility-actions button,
+#utilityRoot .utility-calc-grid button,
+#utilityRoot .utility-calc-science-grid button,
+#utilityRoot .utility-calc-mode-toggle {
+    border: 1px solid var(--line);
+    background: var(--surface-strong);
+    color: var(--ink);
+    box-shadow: var(--shadow-soft);
+    transition: transform 0.18s ease, background 0.18s ease, box-shadow 0.18s ease;
+}
 
-    .planner-btn {
-        width: 100%;
-    }
+#utilityRoot .utility-window-close,
+#utilityRoot .utility-window-expand {
+    width: 34px;
+    height: 34px;
+    border-radius: 12px;
+    font-size: 0.78rem;
+    font-weight: 800;
+}
 
-    .planner-toast-host {
-        left: 12px;
-        right: 12px;
-        bottom: 14px;
-    }
+#utilityRoot .utility-window-expand.is-active {
+    background: var(--theme-success-gradient);
+    color: #fff;
+    border-color: transparent;
+}
 
-    .planner-toast {
-        min-width: 0;
-        max-width: none;
-    }
+#utilityRoot .utility-window-body {
+    display: grid;
+    gap: 10px;
+}
 
-    .qts-week-rail {
-        grid-template-columns: 1fr;
-    }
+#utilityRoot .utility-window[data-tool="stopwatch"] .utility-window-body,
+#utilityRoot .utility-window[data-tool="timer"] .utility-window-body {
+    gap: 12px;
+}
 
-    .qts-stage-shell {
-        display: block;
-    }
+#utilityRoot .utility-calc-toolbar {
+    display: flex;
+    justify-content: flex-end;
+}
 
-    .qts-detached-index {
-        position: static;
-        width: 100%;
-        z-index: auto;
-        display: none !important;
-    }
+#utilityRoot .utility-display,
+#utilityRoot .utility-calc-display {
+    display: flex;
+    align-items: center;
+    justify-content: center;
+    min-height: 92px;
+    padding: 18px 16px;
+    border-radius: 22px;
+    background:
+        linear-gradient(135deg, rgba(255, 255, 255, 0.78), rgba(255, 247, 238, 0.58)),
+        radial-gradient(circle at top, rgba(37, 88, 240, 0.08), transparent 56%);
+    border: 1px solid rgba(255, 255, 255, 0.48);
+    box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.42);
+    font-family: "Space Grotesk", sans-serif;
+    font-size: clamp(2.1rem, 6vw, 2.9rem);
+    font-weight: 700;
+    letter-spacing: -0.08em;
+    text-align: center;
+}
 
-    body[data-mode="qts"] #qtsModule.module.active {
-        margin-left: 0;
-        width: 100%;
-    }
+#utilityRoot .utility-window.utility-is-live .utility-display {
+    box-shadow:
+        inset 0 1px 0 rgba(255, 255, 255, 0.42),
+        0 0 0 4px rgba(18, 162, 112, 0.08);
+}
 
-    .qts-week-context,
-    .qts-summary-card {
-        width: 100%;
-    }
+#utilityRoot .utility-window[data-tool="stopwatch"] .utility-display,
+#utilityRoot .utility-window[data-tool="timer"] .utility-display {
+    min-height: 106px;
+    padding: 18px 18px;
+    border-radius: 0;
+    clip-path: polygon(18px 0, calc(100% - 18px) 0, 100% 18px, 100% calc(100% - 18px), calc(100% - 18px) 100%, 18px 100%, 0 calc(100% - 18px), 0 18px);
+    font-size: clamp(2.4rem, 7vw, 3rem);
+    letter-spacing: -0.07em;
+}
 
-    .qts-summary-head {
-        flex-direction: column;
-    }
+#utilityRoot .utility-actions {
+    display: grid;
+    grid-template-columns: repeat(3, minmax(0, 1fr));
+    gap: 8px;
+}
 
-    .qts-summary-item {
-        grid-template-columns: 1fr;
-        gap: 8px;
-    }
+#utilityRoot .utility-window[data-tool="stopwatch"] .utility-actions,
+#utilityRoot .utility-window[data-tool="timer"] .utility-actions {
+    gap: 10px;
 }
 
-#qtsGrid {
-    max-width: 100%;
-    width: 100%;
-    margin-top: 0;
-    padding: 2px;
-    border-radius: 0;
-    background: var(--ui-informative-surface);
-    border: 1px solid transparent;
-    box-shadow: var(--ui-informative-shadow);
-    clip-path: polygon(var(--ui-cut-lg) 0, calc(100% - var(--ui-cut-lg)) 0, 100% var(--ui-cut-lg), 100% calc(100% - var(--ui-cut-lg)), calc(100% - var(--ui-cut-lg)) 100%, var(--ui-cut-lg) 100%, 0 calc(100% - var(--ui-cut-lg)), 0 var(--ui-cut-lg));
+#utilityRoot .utility-actions button,
+#utilityRoot .utility-calc-grid button {
+    min-height: 44px;
+    border-radius: 14px;
+    font-weight: 800;
 }
 
-.qts-stage-shell {
-    display: block;
+#utilityRoot .utility-window.utility-window-calc .utility-window-close,
+#utilityRoot .utility-window.utility-window-calc .utility-window-expand {
+    border-radius: 0;
+    clip-path: polygon(10px 0, calc(100% - 10px) 0, 100% 10px, 100% calc(100% - 10px), calc(100% - 10px) 100%, 10px 100%, 0 calc(100% - 10px), 0 10px);
 }
 
-.qts-stage-main {
-    min-width: 0;
-    width: 100%;
-    margin-left: 0;
-    padding-left: 0;
-    padding-right: 0;
-    display: grid;
-    justify-items: center;
-    --qts-safe-inline-size: min(
-        100%,
-        var(--qts-content-max)
-    );
+#utilityRoot .utility-window[data-tool="stopwatch"] .utility-actions button,
+#utilityRoot .utility-window[data-tool="timer"] .utility-actions button {
+    min-height: 42px;
+    border-radius: 0;
+    clip-path: polygon(12px 0, calc(100% - 12px) 0, 100% 12px, 100% calc(100% - 12px), calc(100% - 12px) 100%, 12px 100%, 0 calc(100% - 12px), 0 12px);
+    font-size: 0.94rem;
 }
 
-.qts-detached-index {
-    position: fixed;
-    top: 124px;
-    left: 18px;
-    width: var(--qts-detached-rail-width, 126px);
-    display: none;
-    gap: 8px;
-    align-content: start;
-    padding: 2px 0;
-    background: transparent;
-    border: 0;
-    box-shadow: none;
-    z-index: 20;
-    contain: layout paint;
+#utilityRoot .utility-calc-grid button,
+#utilityRoot .utility-calc-science-grid button {
+    min-height: 46px;
+    border-radius: 0;
+    background:
+        linear-gradient(180deg, rgba(255, 255, 255, 0.98), rgba(243, 243, 243, 0.94));
+    border: 1px solid rgba(214, 218, 224, 0.92);
+    color: var(--ink);
+    box-shadow: 0 10px 20px rgba(15, 23, 42, 0.04);
+    clip-path: polygon(12px 0, calc(100% - 12px) 0, 100% 12px, 100% calc(100% - 12px), calc(100% - 12px) 100%, 12px 100%, 0 calc(100% - 12px), 0 12px);
 }
 
-body[data-mode="qts"] .qts-detached-index {
-    display: grid;
+#utilityRoot .utility-actions button:hover,
+#utilityRoot .utility-window-close:hover,
+#utilityRoot .utility-window-expand:hover,
+#utilityRoot .utility-calc-grid button:hover {
+    transform: translateY(-2px);
+    background: rgba(255, 255, 255, 0.96);
 }
 
-.qts-detached-week-nav {
-    display: grid;
-    gap: 5px;
+#utilityRoot .utility-calc-science-grid button:hover,
+#utilityRoot .utility-calc-mode-toggle:hover {
+    transform: translateY(-2px);
+    background: rgba(255, 255, 255, 0.96);
 }
 
-.qts-detached-block {
-    --qts-rail-accent-rgb: 183, 215, 240;
+#utilityRoot .utility-timer-fields {
     display: grid;
-    gap: 6px;
-    padding: 2px 0;
-    border-radius: 0;
-    background: transparent;
-    border: 0;
-    box-shadow: none;
-    clip-path: none;
+    grid-template-columns: repeat(3, minmax(0, 1fr));
+    gap: 8px;
 }
 
-.qts-detached-block.is-week {
-    --qts-rail-accent-rgb: 183, 215, 240;
+#utilityRoot .utility-window[data-tool="timer"] .utility-timer-fields {
+    gap: 10px;
 }
 
-.qts-detached-block.is-toggle {
-    --qts-rail-accent-rgb: 215, 220, 228;
+#utilityRoot .utility-timer-field {
+    display: grid;
+    gap: 6px;
 }
 
-.qts-detached-block.is-summary {
-    --qts-rail-accent-rgb: 185, 222, 207;
+#utilityRoot .utility-window[data-tool="timer"] .utility-timer-field {
+    gap: 5px;
 }
 
-.qts-detached-block-label {
-    text-align: center;
-    font-size: 0.5rem;
+#utilityRoot .utility-timer-field span {
+    font-size: 0.64rem;
     font-weight: 800;
     letter-spacing: 0.16em;
     text-transform: uppercase;
     color: var(--muted);
 }
 
-.qts-detached-week-button,
-.qts-detached-toggle,
-.qts-detached-summary-button {
-    position: relative;
-    overflow: hidden;
+#utilityRoot .utility-timer-field input {
     width: 100%;
-    min-height: 34px;
-    padding: 6px 8px;
-    border-radius: 0;
-    border: 1px solid rgba(195, 185, 174, 0.28);
-    background:
-        linear-gradient(180deg, rgba(255, 255, 255, 0.95), rgba(246, 244, 240, 0.9));
+    min-height: 44px;
+    padding: 10px 12px;
+    border-radius: 14px;
+    border: 1px solid var(--line);
+    background: var(--surface-strong);
     color: var(--ink);
     text-align: center;
-    font-size: 0.62rem;
-    font-weight: 800;
-    line-height: 1.08;
-    letter-spacing: 0.07em;
-    text-transform: uppercase;
-    box-shadow: 0 6px 12px rgba(42, 33, 53, 0.04);
-    cursor: pointer;
-    transition:
-        border-color 0.18s ease,
-        color 0.18s ease,
-        background 0.18s ease,
-        box-shadow 0.18s ease,
-        opacity 0.18s ease;
-    clip-path:
-        polygon(var(--ui-cut-sm) 0, calc(100% - var(--ui-cut-sm)) 0, 100% var(--ui-cut-sm), 100% calc(100% - var(--ui-cut-sm)), calc(100% - var(--ui-cut-sm)) 100%, var(--ui-cut-sm) 100%, 0 calc(100% - var(--ui-cut-sm)), 0 var(--ui-cut-sm));
+    font-size: 1rem;
+    font-weight: 700;
+    user-select: text;
 }
 
-.qts-detached-icon {
-    display: none;
-    position: relative;
-    z-index: 1;
-    font-size: 1rem;
-    line-height: 1;
+#utilityRoot .utility-window[data-tool="timer"] .utility-timer-field input {
+    min-height: 42px;
+    border-radius: 0;
+    clip-path: polygon(12px 0, calc(100% - 12px) 0, 100% 12px, 100% calc(100% - 12px), calc(100% - 12px) 100%, 12px 100%, 0 calc(100% - 12px), 0 12px);
 }
 
-.qts-detached-text {
-    position: relative;
-    z-index: 1;
+#utilityRoot .utility-timer-field input:disabled {
+    opacity: 0.56;
 }
 
-.qts-detached-week-button::before,
-.qts-detached-toggle::before,
-.qts-detached-summary-button::before {
-    content: "";
-    position: absolute;
-    top: 0;
-    left: var(--ui-cut-sm);
-    right: var(--ui-cut-sm);
-    height: 1px;
+#utilityRoot .utility-calc-display {
+    justify-content: flex-end;
+    padding-right: 18px;
+    min-height: 96px;
+    font-size: clamp(2.1rem, 6vw, 2.7rem);
+    overflow: hidden;
+    text-overflow: ellipsis;
     background:
-        linear-gradient(90deg, rgba(var(--qts-rail-accent-rgb), 0.06), rgba(var(--qts-rail-accent-rgb), 0.7), rgba(var(--qts-rail-accent-rgb), 0.06));
-    pointer-events: none;
+        linear-gradient(180deg, rgba(255, 255, 255, 0.96), rgba(245, 245, 245, 0.9));
+    border-color: rgba(214, 218, 224, 0.8);
+    box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.72);
+    border-radius: 0;
+    clip-path: polygon(18px 0, calc(100% - 18px) 0, 100% 18px, 100% calc(100% - 18px), calc(100% - 18px) 100%, 18px 100%, 0 calc(100% - 18px), 0 18px);
 }
 
-.qts-detached-week-button::after,
-.qts-detached-toggle::after,
-.qts-detached-summary-button::after {
-    content: "";
-    position: absolute;
-    left: 12px;
-    right: 12px;
-    bottom: 0;
-    height: 1px;
-    background:
-        linear-gradient(90deg, rgba(var(--qts-rail-accent-rgb), 0), rgba(var(--qts-rail-accent-rgb), 0.36), rgba(var(--qts-rail-accent-rgb), 0));
-    opacity: 0.88;
-    pointer-events: none;
+#utilityRoot .utility-calc-grid {
+    display: grid;
+    grid-template-columns: repeat(4, minmax(0, 1fr));
+    gap: 8px;
 }
 
-.qts-detached-week-button:hover,
-.qts-detached-toggle:hover,
-.qts-detached-summary-button:hover {
-    border-color: rgba(195, 185, 174, 0.36);
-    background:
-        linear-gradient(180deg, rgba(255, 255, 255, 0.96), rgba(247, 245, 241, 0.92));
-    box-shadow: 0 8px 16px rgba(42, 33, 53, 0.05);
+#utilityRoot .utility-calc-science-grid {
+    display: grid;
+    grid-template-columns: repeat(5, minmax(0, 1fr));
+    gap: 8px;
 }
 
-.qts-detached-week-button.is-active,
-.qts-detached-toggle:has(input:checked),
-.qts-detached-summary-button.is-active {
-    color: #1f2a33;
-    border-color: rgba(var(--qts-rail-accent-rgb), 0.42);
-    background:
-        linear-gradient(180deg, rgba(var(--qts-rail-accent-rgb), 0.54), rgba(255, 255, 255, 0.94));
-    box-shadow:
-        inset 0 0 0 1px rgba(255, 255, 255, 0.28),
-        0 8px 16px rgba(var(--qts-rail-accent-rgb), 0.1);
+#utilityRoot .utility-calc-science-grid[hidden] {
+    display: none;
 }
 
-body.dark .qts-detached-week-button,
-body.dark .qts-detached-toggle,
-body.dark .qts-detached-summary-button {
-    border-color: rgba(255, 255, 255, 0.08);
-    background:
-        linear-gradient(180deg, rgba(26, 33, 47, 0.96), rgba(17, 23, 35, 0.94));
-    color: rgba(231, 236, 246, 0.92);
-    box-shadow: 0 10px 18px rgba(0, 0, 0, 0.22);
+#utilityRoot .utility-calc-mode-toggle {
+    min-height: 34px;
+    padding: 0 14px;
+    border-radius: 0;
+    font-size: 0.72rem;
+    font-weight: 800;
+    letter-spacing: 0.12em;
+    text-transform: uppercase;
+    clip-path: polygon(12px 0, calc(100% - 12px) 0, 100% 12px, 100% calc(100% - 12px), calc(100% - 12px) 100%, 12px 100%, 0 calc(100% - 12px), 0 12px);
 }
 
-body.dark .qts-detached-week-button:hover,
-body.dark .qts-detached-toggle:hover,
-body.dark .qts-detached-summary-button:hover {
-    border-color: rgba(255, 255, 255, 0.12);
-    background:
-        linear-gradient(180deg, rgba(31, 39, 56, 0.98), rgba(20, 27, 40, 0.96));
-    box-shadow: 0 12px 20px rgba(0, 0, 0, 0.26);
+#utilityRoot .utility-calc-equals {
+    background: var(--surface-strong) !important;
+    color: var(--ink) !important;
+    border-color: var(--line) !important;
+    box-shadow: var(--shadow-soft) !important;
 }
 
-body.dark .qts-detached-week-button.is-active,
-body.dark .qts-detached-toggle:has(input:checked),
-body.dark .qts-detached-summary-button.is-active {
-    color: #f3f7ff;
-    border-color: rgba(var(--qts-rail-accent-rgb), 0.26);
+#utilityRoot .utility-window:fullscreen {
+    inset: 0 !important;
+    width: 100vw !important;
+    max-width: none !important;
+    height: 100vh !important;
+    max-height: none !important;
+    padding: clamp(22px, 3vw, 36px);
+    border-radius: 0;
     background:
-        linear-gradient(180deg, rgba(var(--qts-rail-accent-rgb), 0.28), rgba(22, 30, 44, 0.98));
-    box-shadow:
-        inset 0 1px 0 rgba(255, 255, 255, 0.08),
-        0 12px 22px rgba(0, 0, 0, 0.24);
+        radial-gradient(circle at top left, rgba(255, 208, 174, 0.18), transparent 30%),
+        radial-gradient(circle at top right, rgba(153, 189, 255, 0.16), transparent 30%),
+        linear-gradient(180deg, rgba(255, 252, 247, 0.96), rgba(240, 245, 255, 0.96));
 }
 
-.qts-detached-toggle-group {
-    display: grid;
-    gap: 5px;
+#utilityRoot .utility-window:fullscreen::backdrop {
+    background: rgba(9, 14, 24, 0.72);
 }
 
-.qts-detached-toggle {
-    display: grid;
-    grid-template-columns: minmax(0, 1fr);
-    align-items: center;
-    gap: 0;
+#utilityRoot .utility-window:fullscreen .utility-window-body {
+    width: min(720px, 100%);
+    margin: auto;
+    gap: 14px;
 }
 
-.qts-detached-toggle input {
-    position: absolute;
-    opacity: 0;
-    pointer-events: none;
-    inline-size: 1px;
-    block-size: 1px;
-    margin: 0;
+#utilityRoot .utility-window[data-tool="stopwatch"]:fullscreen .utility-display,
+#utilityRoot .utility-window[data-tool="timer"]:fullscreen .utility-display {
+    min-height: clamp(220px, 34vh, 340px);
+    border-radius: 38px;
+    font-size: clamp(4.6rem, 15vw, 9rem);
 }
 
-.qts-detached-toggle span {
-    min-width: 0;
-    position: relative;
-    z-index: 1;
+#utilityRoot .utility-window.utility-window-calc:fullscreen .utility-window-body {
+    width: min(720px, 100%);
 }
 
-.qts-detached-toggle:has(input:disabled) {
-    opacity: 0.56;
-    cursor: default;
+#utilityRoot .utility-window[data-tool="stopwatch"]:fullscreen .utility-window-body,
+#utilityRoot .utility-window[data-tool="timer"]:fullscreen .utility-window-body {
+    width: min(620px, 100%);
 }
 
-.qts-detached-index-item {
-    text-align: center;
-    font-size: 0.76rem;
-    font-weight: 800;
-    letter-spacing: 0.14em;
-    text-transform: uppercase;
-    color: var(--muted);
-    line-height: 1.5;
-    padding: 4px 10px;
+#utilityRoot .utility-window.utility-window-calc:fullscreen .utility-calc-display {
+    min-height: 138px;
+    font-size: clamp(3rem, 8vw, 5rem);
 }
 
-.qts-detached-index-divider {
-    position: relative;
-    height: 4px;
-    margin: 0;
-    background: none;
+#utilityRoot .utility-window.utility-window-calc:fullscreen .utility-calc-science-grid {
+    gap: 12px;
 }
 
-.qts-detached-index-divider::before {
-    content: "";
-    position: absolute;
-    left: 10px;
-    right: 10px;
-    top: 50%;
-    height: 1px;
-    transform: translateY(-50%);
-    background:
-        linear-gradient(90deg, rgba(154, 160, 172, 0), rgba(154, 160, 172, 0.28) 22%, rgba(154, 160, 172, 0.42) 50%, rgba(154, 160, 172, 0.28) 78%, rgba(154, 160, 172, 0));
+#utilityRoot .utility-window.utility-window-calc:fullscreen .utility-calc-grid {
+    gap: 12px;
 }
 
-body[data-mode="qts"] #qtsModule.module.active {
-    box-sizing: border-box;
-    margin-left: calc(var(--qts-left-rail-space, 0px) + var(--qts-left-clearance, 0px));
-    margin-right: calc(var(--qts-right-tools-space, 0px) + var(--qts-right-edge-gap, 0px));
-    width: calc(100% - var(--qts-left-rail-space, 0px) - var(--qts-left-clearance, 0px) - var(--qts-right-tools-space, 0px) - var(--qts-right-edge-gap, 0px));
-    max-width: 100%;
-    padding-left: var(--qts-inline-gutter, 18px);
-    padding-right: var(--qts-inline-gutter, 18px);
+#utilityRoot .utility-window.utility-window-calc:fullscreen .utility-calc-grid button,
+#utilityRoot .utility-window.utility-window-calc:fullscreen .utility-calc-science-grid button {
+    min-height: 72px;
+    font-size: 1.15rem;
 }
 
-@media (max-width: 1480px) and (min-width: 761px) {
-    body[data-mode="qts"] #qtsModule {
-        --qts-inline-gutter: 4px;
-        --qts-right-tools-space: 60px;
-        --qts-content-max: 1120px;
-    }
-
-    body[data-mode="qts"] {
-        --qts-detached-rail-width: 112px;
-        --qts-detached-rail-gap: 2px;
-    }
+body.dark #utilityRoot .utility-window {
+    background: var(--glass);
+    border-color: var(--line);
+}
 
-    .qts-detached-index {
-        left: 10px;
-        top: 116px;
-        gap: 8px;
-    }
+body.dark #utilityRoot .utility-window:fullscreen {
+    background:
+        radial-gradient(circle at top left, rgba(65, 109, 255, 0.16), transparent 30%),
+        radial-gradient(circle at top right, rgba(76, 164, 255, 0.12), transparent 30%),
+        linear-gradient(180deg, rgba(9, 15, 27, 0.98), rgba(13, 20, 33, 0.96));
+}
 
-    .qts-detached-block {
-        gap: 6px;
-        padding: 6px 4px 4px;
-    }
+body.dark #utilityRoot .utility-window:fullscreen::backdrop {
+    background: rgba(2, 4, 10, 0.84);
+}
 
-    .qts-detached-block-label,
-    .qts-detached-index-divider {
-        display: block;
+body.dark #utilityRoot .utility-display,
+body.dark #utilityRoot .utility-calc-display {
+    background:
+        linear-gradient(135deg, rgba(20, 27, 41, 0.94), rgba(12, 18, 29, 0.86)),
+        radial-gradient(circle at top, rgba(81, 129, 255, 0.12), transparent 56%);
+    border-color: rgba(255, 255, 255, 0.08);
+    color: var(--ink);
+}
+
+body.dark #utilityRoot .utility-window-close,
+body.dark #utilityRoot .utility-window-expand,
+body.dark #utilityRoot .utility-actions button,
+body.dark #utilityRoot .utility-calc-grid button,
+body.dark #utilityRoot .utility-calc-science-grid button,
+body.dark #utilityRoot .utility-calc-mode-toggle,
+body.dark #utilityRoot .utility-timer-field input {
+    background: var(--surface-strong);
+    color: var(--ink);
+    border-color: var(--line);
+}
+
+.control-btn {
+    width: 62px;
+    height: 42px;
+    display: inline-flex;
+    align-items: center;
+    justify-content: center;
+    border-radius: 0;
+    border: 1px solid var(--ui-clickable-border);
+    background: var(--ui-clickable-surface);
+    color: var(--ink);
+    font-size: 1.02rem;
+    line-height: 1;
+    box-shadow: var(--ui-clickable-shadow);
+    position: relative;
+    z-index: 2;
+    overflow: hidden;
+    clip-path: polygon(var(--ui-cut-sm) 0, calc(100% - var(--ui-cut-sm)) 0, 100% var(--ui-cut-sm), 100% calc(100% - var(--ui-cut-sm)), calc(100% - var(--ui-cut-sm)) 100%, var(--ui-cut-sm) 100%, 0 calc(100% - var(--ui-cut-sm)), 0 var(--ui-cut-sm));
+    cursor: pointer;
+}
+
+.control-btn::before {
+    content: "";
+    position: absolute;
+    top: 0;
+    left: var(--ui-cut-sm);
+    right: var(--ui-cut-sm);
+    height: 1px;
+    background: var(--ui-clickable-line);
+    pointer-events: none;
+}
+
+.control-btn::after {
+    content: "";
+    position: absolute;
+    inset: 1px;
+    clip-path: polygon(calc(var(--ui-cut-sm) - 1px) 0, calc(100% - (var(--ui-cut-sm) - 1px)) 0, 100% calc(var(--ui-cut-sm) - 1px), 100% calc(100% - (var(--ui-cut-sm) - 1px)), calc(100% - (var(--ui-cut-sm) - 1px)) 100%, calc(var(--ui-cut-sm) - 1px) 100%, 0 calc(100% - (var(--ui-cut-sm) - 1px)), 0 calc(var(--ui-cut-sm) - 1px));
+    background: linear-gradient(180deg, rgba(255, 255, 255, 0.7), transparent 44%);
+    pointer-events: none;
+    opacity: 0.38;
+}
+
+.control-btn:hover {
+    transform: translateY(-2px);
+}
+
+.control-btn:disabled,
+.controls.is-locked .control-btn {
+    opacity: 0.42;
+    pointer-events: none;
+    transform: none;
+}
+
+body.dark .control-btn {
+    background: var(--ui-clickable-surface);
+    color: var(--ink);
+    border-color: var(--ui-clickable-border);
+}
+
+body.dark .control-btn:hover {
+    background: var(--ui-clickable-surface);
+}
+
+#moduleArea {
+    padding: 0;
+    margin-top: 16px;
+}
+
+.module.active {
+    display: block;
+    padding: 22px;
+    border-radius: var(--radius-xl);
+    background:
+        linear-gradient(180deg, rgba(255, 255, 255, 0.74), rgba(255, 255, 255, 0.54)),
+        radial-gradient(circle at top right, rgba(37, 88, 240, 0.07), transparent 38%);
+    backdrop-filter: blur(18px);
+    border: 1px solid var(--line);
+    box-shadow: var(--shadow);
+    min-height: 360px;
+}
+
+body.dark .module.active {
+    background:
+        linear-gradient(180deg, rgba(18, 24, 37, 0.92), rgba(13, 18, 28, 0.82)),
+        radial-gradient(circle at top right, rgba(81, 129, 255, 0.08), transparent 40%);
+}
+
+#moduleArea h2 {
+    margin-bottom: 20px;
+    font-size: clamp(1.4rem, 2vw, 1.9rem);
+    font-weight: 800;
+    letter-spacing: -0.04em;
+    text-align: left;
+}
+
+#moduleArea button,
+.save-btn,
+.interval-btn,
+.row-btn {
+    border: 1px solid var(--line);
+    background: var(--surface-strong);
+    color: var(--ink);
+    box-shadow: var(--shadow-soft);
+}
+
+#moduleArea input,
+#moduleArea select,
+.custom-row input {
+    border-radius: 14px;
+    border: 1px solid var(--line);
+    background: var(--surface-strong);
+    color: var(--ink);
+    padding: 10px 12px;
+}
+
+body.dark #moduleArea button,
+body.dark .save-btn,
+body.dark .interval-btn,
+body.dark .row-btn,
+body.dark #moduleArea input,
+body.dark #moduleArea select,
+body.dark .custom-row input {
+    background: var(--surface-strong);
+    color: var(--ink);
+    border-color: var(--line);
+}
+
+#calendarModule,
+#alarmModule,
+#qtsModule,
+#questionsModule {
+    color: var(--ink);
+}
+
+body[data-mode="qts"] {
+    --qts-detached-rail-width: clamp(86px, 8.8vw, 126px);
+    --qts-detached-rail-gap: clamp(1px, 0.2vw, 3px);
+}
+
+#qtsModule {
+    overflow: visible;
+    --qts-content-max: min(100%, 1180px);
+    --qts-inline-gutter: clamp(1px, 0.35vw, 6px);
+    --qts-left-rail-space: var(--qts-detached-rail-width, 0px);
+    --qts-left-clearance: var(--qts-detached-rail-gap, 0px);
+    --qts-right-tools-space: 0px;
+    --qts-right-edge-gap: 1px;
+}
+
+.calendar-header {
+    justify-content: space-between;
+    align-items: center;
+    margin-bottom: 24px;
+    font-size: 1.2rem;
+}
+
+.calendar-weekdays,
+.calendar-grid {
+    gap: 10px;
+}
+
+.calendar-day,
+.weekday {
+    border-radius: 18px;
+}
+
+.weekday {
+    min-height: 26px;
+    display: grid;
+    place-items: center;
+    font-size: 0.84rem;
+    letter-spacing: 0.02em;
+}
+
+.calendar-day {
+    min-height: 92px;
+    padding: 16px;
+    background: rgba(255, 255, 255, 0.44);
+    border: 1px solid rgba(255, 255, 255, 0.4);
+}
+
+body.dark .calendar-day {
+    background: rgba(255, 255, 255, 0.04);
+    border-color: rgba(255, 255, 255, 0.06);
+}
+
+.alarm-row {
+    padding: 18px;
+    border-radius: 22px;
+    background: rgba(255, 255, 255, 0.44);
+    border: 1px solid rgba(255, 255, 255, 0.38);
+}
+
+body.dark .alarm-row {
+    background: rgba(255, 255, 255, 0.04);
+    border-color: rgba(255, 255, 255, 0.06);
+}
+
+.planner-shell {
+    display: grid;
+    gap: 18px;
+}
+
+.planner-head,
+.planner-reminders,
+.alarm-bridge {
+    padding: 20px 24px;
+    border-radius: 0;
+    background: rgba(255, 255, 255, 0.46);
+    border: 1px solid rgba(255, 255, 255, 0.44);
+    box-shadow: var(--shadow-soft);
+    clip-path: polygon(18px 0, calc(100% - 18px) 0, 100% 18px, 100% calc(100% - 18px), calc(100% - 18px) 100%, 18px 100%, 0 calc(100% - 18px), 0 18px);
+}
+
+body.dark .planner-head,
+body.dark .planner-reminders,
+body.dark .alarm-bridge {
+    background: rgba(255, 255, 255, 0.04);
+    border-color: rgba(255, 255, 255, 0.06);
+}
+
+.planner-head,
+.planner-section-head,
+.alarm-bridge-actions,
+.planner-month-nav,
+.planner-head-actions {
+    display: flex;
+    align-items: center;
+    justify-content: space-between;
+    gap: 10px;
+    flex-wrap: wrap;
+}
+
+.planner-head-copy,
+.alarm-bridge-copy {
+    display: grid;
+    gap: 4px;
+}
+
+.planner-kicker {
+    font-size: 0.68rem;
+    font-weight: 800;
+    letter-spacing: 0.16em;
+    text-transform: uppercase;
+    color: var(--muted);
+}
+
+.planner-head h2,
+.planner-section-head h3,
+.alarm-bridge h2 {
+    margin: 0;
+    font-size: clamp(1.4rem, 1.9vw, 1.82rem);
+    font-weight: 800;
+    letter-spacing: -0.04em;
+}
+
+.planner-head p,
+.planner-modal-header p,
+.alarm-bridge p,
+.planner-empty-text {
+    margin: 0;
+    color: var(--muted);
+}
+
+.planner-head-actions button,
+.planner-month-nav button,
+.planner-section-head button,
+.alarm-bridge-actions button,
+.planner-btn {
+    border: 1px solid var(--line);
+    background: var(--surface-strong);
+    color: var(--ink);
+    box-shadow: var(--shadow-soft);
+}
+
+.planner-head-actions button,
+.planner-section-head button,
+.alarm-bridge-actions button,
+.planner-btn {
+    min-height: 40px;
+    padding: 0 15px;
+    border-radius: 0;
+    font-weight: 700;
+    clip-path: polygon(12px 0, calc(100% - 12px) 0, 100% 12px, 100% calc(100% - 12px), calc(100% - 12px) 100%, 12px 100%, 0 calc(100% - 12px), 0 12px);
+}
+
+.planner-month-nav {
+    padding: 0 4px;
+    min-height: 40px;
+}
+
+.planner-month-label {
+    font-family: "Space Grotesk", sans-serif;
+    font-size: 0.94rem;
+    font-weight: 700;
+    letter-spacing: -0.04em;
+}
+
+.planner-month-nav button {
+    width: 40px;
+    height: 40px;
+    border-radius: 0;
+    font-size: 0.98rem;
+    font-weight: 800;
+    clip-path: polygon(12px 0, calc(100% - 12px) 0, 100% 12px, 100% calc(100% - 12px), calc(100% - 12px) 100%, 12px 100%, 0 calc(100% - 12px), 0 12px);
+}
+
+.calendar-grid {
+    align-items: stretch;
+}
+
+.calendar-spacer {
+    min-height: 0;
+}
+
+.calendar-day {
+    appearance: none;
+    min-height: 104px;
+    display: grid;
+    align-content: space-between;
+    gap: 8px;
+    text-align: left;
+    overflow: hidden;
+    transition: transform 0.18s ease, border-color 0.18s ease, box-shadow 0.18s ease;
+    padding: 12px 14px;
+    border-radius: 0;
+    clip-path: polygon(14px 0, calc(100% - 14px) 0, 100% 14px, 100% calc(100% - 14px), calc(100% - 14px) 100%, 14px 100%, 0 calc(100% - 14px), 0 14px);
+}
+
+.calendar-day:hover {
+    transform: translateY(-3px);
+    box-shadow: 0 18px 32px rgba(33, 25, 40, 0.12);
+}
+
+.calendar-day.has-event::after {
+    content: "";
+    position: absolute;
+    inset: 5px;
+    border: 2px solid rgba(243, 108, 79, 0.84);
+    border-radius: 46% 54% 50% 50% / 56% 44% 58% 42%;
+    pointer-events: none;
+}
+
+.calendar-day-number {
+    font-family: "Space Grotesk", sans-serif;
+    font-size: 1.02rem;
+    font-weight: 700;
+}
+
+.calendar-day-preview {
+    display: grid;
+    gap: 3px;
+    min-width: 0;
+}
+
+.calendar-day-preview-time {
+    font-size: 0.68rem;
+    font-weight: 800;
+    letter-spacing: 0.12em;
+    text-transform: uppercase;
+    color: var(--muted);
+}
+
+.calendar-day-preview-title,
+.calendar-day-empty {
+    font-size: 0.84rem;
+    line-height: 1.28;
+}
+
+.calendar-day-preview-title {
+    font-weight: 700;
+    display: -webkit-box;
+    -webkit-line-clamp: 2;
+    -webkit-box-orient: vertical;
+    overflow: hidden;
+}
+
+.calendar-day-empty {
+    color: var(--muted);
+    opacity: 0.76;
+}
+
+.planner-reminder-list,
+.alarm-bridge-list {
+    display: grid;
+    gap: 12px;
+    margin-top: 18px;
+}
+
+.planner-reminder-card,
+.alarm-bridge-card {
+    display: grid;
+    grid-template-columns: auto 1fr auto;
+    gap: 14px;
+    align-items: center;
+    padding: 16px 18px;
+    border-radius: 22px;
+    background: rgba(255, 255, 255, 0.52);
+    border: 1px solid rgba(255, 255, 255, 0.46);
+}
+
+.alarm-bridge-card {
+    grid-template-columns: auto 1fr;
+}
+
+body.dark .planner-reminder-card,
+body.dark .alarm-bridge-card {
+    background: rgba(255, 255, 255, 0.04);
+    border-color: rgba(255, 255, 255, 0.06);
+}
+
+.planner-reminder-card.is-paused {
+    opacity: 0.62;
+}
+
+.planner-reminder-time,
+.alarm-bridge-time {
+    min-width: 82px;
+    font-family: "Space Grotesk", sans-serif;
+    font-size: 1.2rem;
+    font-weight: 700;
+    letter-spacing: -0.05em;
+}
+
+.planner-reminder-title,
+.alarm-bridge-title,
+.planner-empty-title {
+    font-size: 1rem;
+    font-weight: 800;
+}
+
+.planner-reminder-meta,
+.alarm-bridge-meta {
+    font-size: 0.84rem;
+    color: var(--muted);
+}
+
+.planner-reminder-copy {
+    display: grid;
+    gap: 4px;
+}
+
+.planner-reminder-edit {
+    min-height: 40px;
+    padding: 0 14px;
+    border-radius: 12px;
+    border: 1px solid var(--line);
+    background: var(--surface-strong);
+    color: var(--ink);
+    font-weight: 700;
+}
+
+.planner-empty-state {
+    padding: 22px;
+    border-radius: 22px;
+    background: rgba(255, 255, 255, 0.36);
+    border: 1px dashed rgba(42, 33, 53, 0.12);
+}
+
+body.dark .planner-empty-state {
+    background: rgba(255, 255, 255, 0.03);
+    border-color: rgba(255, 255, 255, 0.08);
+}
+
+.planner-modal {
+    position: fixed;
+    inset: 0;
+    z-index: 1800;
+    display: flex;
+    align-items: center;
+    justify-content: center;
+    padding: 24px;
+}
+
+.planner-modal.hidden {
+    display: none;
+}
+
+.planner-modal-backdrop {
+    position: absolute;
+    inset: 0;
+    background: rgba(12, 18, 30, 0.4);
+    backdrop-filter: blur(8px);
+}
+
+.planner-modal-card {
+    position: relative;
+    width: min(620px, calc(100vw - 24px));
+    max-height: min(88vh, 920px);
+    overflow: auto;
+    padding: 28px;
+    border-radius: 30px;
+    background: var(--surface-strong);
+    border: 1px solid var(--line);
+    box-shadow: var(--shadow);
+}
+
+.planner-modal-close {
+    position: absolute;
+    top: 18px;
+    right: 18px;
+    width: 38px;
+    height: 38px;
+    border-radius: 14px;
+    border: 1px solid var(--line);
+    background: var(--surface-strong);
+    color: var(--ink);
+    font-weight: 800;
+}
+
+.planner-modal-header {
+    display: grid;
+    gap: 6px;
+    margin-bottom: 20px;
+    padding-right: 44px;
+}
+
+.planner-modal-header h3 {
+    margin: 0;
+    font-size: clamp(1.45rem, 2vw, 1.95rem);
+    font-weight: 800;
+    letter-spacing: -0.04em;
+}
+
+.planner-form {
+    display: grid;
+    gap: 14px;
+}
+
+.planner-field,
+.planner-toggle {
+    display: grid;
+    gap: 8px;
+}
+
+.planner-field span,
+.planner-toggle span {
+    font-size: 0.72rem;
+    font-weight: 800;
+    letter-spacing: 0.14em;
+    text-transform: uppercase;
+    color: var(--muted);
+}
+
+.planner-field input,
+.planner-field select,
+.planner-field textarea {
+    width: 100%;
+    padding: 12px 14px;
+    border-radius: 16px;
+    border: 1px solid var(--line);
+    background: var(--surface-strong);
+    color: var(--ink);
+}
+
+.planner-field textarea {
+    resize: vertical;
+    min-height: 96px;
+}
+
+.planner-field-row {
+    display: grid;
+    grid-template-columns: repeat(2, minmax(0, 1fr));
+    gap: 12px;
+}
+
+.planner-permission-pill {
+    min-height: 46px;
+    display: inline-flex;
+    align-items: center;
+    padding: 0 14px;
+    border-radius: 16px;
+    border: 1px solid var(--line);
+    background: var(--surface-strong);
+    font-weight: 700;
+}
+
+.planner-permission-pill.is-on {
+    background: rgba(30, 166, 114, 0.12);
+    color: #0d7b53;
+}
+
+.planner-permission-pill.is-off {
+    background: rgba(193, 79, 86, 0.12);
+    color: #b14951;
+}
+
+.planner-chip-row {
+    display: flex;
+    flex-wrap: wrap;
+    gap: 8px;
+}
+
+.planner-chip {
+    position: relative;
+    min-height: 42px;
+    display: inline-flex;
+    align-items: center;
+    justify-content: center;
+    padding: 0 14px;
+    border-radius: 999px;
+    border: 1px solid var(--line);
+    background: var(--surface-strong);
+    font-weight: 700;
+    cursor: pointer;
+}
+
+.planner-chip input {
+    position: absolute;
+    opacity: 0;
+    pointer-events: none;
+}
+
+.planner-chip.is-active {
+    background: var(--theme-primary-gradient);
+    color: #fff;
+    border-color: transparent;
+}
+
+.planner-toggle {
+    grid-template-columns: auto 1fr;
+    align-items: center;
+}
+
+.planner-toggle input {
+    width: 18px;
+    height: 18px;
+}
+
+.planner-modal-actions {
+    display: flex;
+    justify-content: space-between;
+    gap: 10px;
+    flex-wrap: wrap;
+    margin-top: 4px;
+}
+
+.planner-btn-ghost {
+    background: transparent;
+}
+
+.planner-btn-primary {
+    background: var(--theme-primary-gradient);
+    color: #fff;
+    border-color: transparent;
+    box-shadow: 0 16px 30px var(--theme-primary-shadow);
+}
+
+.planner-toast-host {
+    position: fixed;
+    right: 18px;
+    bottom: 22px;
+    z-index: 1810;
+    display: grid;
+    gap: 10px;
+}
+
+.planner-toast {
+    min-width: min(320px, calc(100vw - 32px));
+    max-width: 420px;
+    padding: 14px 16px;
+    border-radius: 18px;
+    background: var(--surface-strong);
+    border: 1px solid var(--line);
+    box-shadow: var(--shadow);
+    color: var(--ink);
+    opacity: 0;
+    transform: translateY(10px);
+    transition: opacity 0.2s ease, transform 0.2s ease;
+}
+
+.planner-toast.is-visible {
+    opacity: 1;
+    transform: translateY(0);
+}
+
+@media (max-width: 760px) {
+    .style-panel {
+        top: 88px;
+        right: 12px;
+        left: 12px;
+        width: auto;
+    }
+
+    .planner-head,
+    .planner-reminders,
+    .alarm-bridge {
+        padding: 18px;
+        border-radius: 24px;
+    }
+
+    .calendar-day {
+        min-height: 104px;
+        padding: 14px;
+    }
+
+    .planner-field-row {
+        grid-template-columns: 1fr;
+    }
+
+    .planner-reminder-card,
+    .alarm-bridge-card {
+        grid-template-columns: 1fr;
+        align-items: start;
+    }
+
+    .planner-reminder-edit {
+        width: 100%;
+    }
+
+    .planner-modal {
+        padding: 12px;
+    }
+
+    .planner-modal-card {
+        padding: 22px 18px;
+        border-radius: 24px;
+    }
+
+    .planner-modal-actions {
+        flex-direction: column;
+    }
+
+    .planner-btn {
+        width: 100%;
+    }
+
+    .planner-toast-host {
+        left: 12px;
+        right: 12px;
+        bottom: 14px;
+    }
+
+    .planner-toast {
+        min-width: 0;
+        max-width: none;
+    }
+
+    .qts-week-rail {
+        grid-template-columns: 1fr;
+    }
+
+    .qts-stage-shell {
+        display: block;
+    }
+
+    .qts-detached-index {
+        position: static;
+        width: 100%;
+        z-index: auto;
+        display: none !important;
+    }
+
+    body[data-mode="qts"] #qtsModule.module.active {
+        margin-left: 0;
+        width: 100%;
+    }
+
+    .qts-week-context,
+    .qts-summary-card {
+        width: 100%;
+    }
+
+    .qts-summary-head {
+        flex-direction: column;
+    }
+
+    .qts-summary-item {
+        grid-template-columns: 1fr;
+        gap: 8px;
+    }
+}
+
+#qtsGrid {
+    max-width: 100%;
+    width: 100%;
+    margin-top: 0;
+    padding: 2px;
+    border-radius: 0;
+    background: var(--ui-informative-surface);
+    border: 1px solid transparent;
+    box-shadow: var(--ui-informative-shadow);
+    clip-path: polygon(var(--ui-cut-lg) 0, calc(100% - var(--ui-cut-lg)) 0, 100% var(--ui-cut-lg), 100% calc(100% - var(--ui-cut-lg)), calc(100% - var(--ui-cut-lg)) 100%, var(--ui-cut-lg) 100%, 0 calc(100% - var(--ui-cut-lg)), 0 var(--ui-cut-lg));
+}
+
+.qts-stage-shell {
+    display: block;
+}
+
+.qts-stage-main {
+    min-width: 0;
+    width: 100%;
+    margin-left: 0;
+    padding-left: 0;
+    padding-right: 0;
+    display: grid;
+    justify-items: center;
+    --qts-safe-inline-size: min(
+        100%,
+        var(--qts-content-max)
+    );
+}
+
+.qts-detached-index {
+    position: fixed;
+    top: 124px;
+    left: 18px;
+    width: var(--qts-detached-rail-width, 126px);
+    display: none;
+    gap: 8px;
+    align-content: start;
+    padding: 2px 0;
+    background: transparent;
+    border: 0;
+    box-shadow: none;
+    z-index: 20;
+    contain: layout paint;
+}
+
+body[data-mode="qts"] .qts-detached-index {
+    display: grid;
+}
+
+.qts-detached-week-nav {
+    display: grid;
+    gap: 5px;
+}
+
+.qts-detached-block {
+    --qts-rail-accent-rgb: 183, 215, 240;
+    display: grid;
+    gap: 6px;
+    padding: 2px 0;
+    border-radius: 0;
+    background: transparent;
+    border: 0;
+    box-shadow: none;
+    clip-path: none;
+}
+
+.qts-detached-block.is-week {
+    --qts-rail-accent-rgb: 183, 215, 240;
+}
+
+.qts-detached-block.is-toggle {
+    --qts-rail-accent-rgb: 215, 220, 228;
+}
+
+.qts-detached-block.is-summary {
+    --qts-rail-accent-rgb: 185, 222, 207;
+}
+
+.qts-detached-block-label {
+    text-align: center;
+    font-size: 0.5rem;
+    font-weight: 800;
+    letter-spacing: 0.16em;
+    text-transform: uppercase;
+    color: var(--muted);
+}
+
+.qts-detached-week-button,
+.qts-detached-toggle,
+.qts-detached-summary-button {
+    position: relative;
+    overflow: hidden;
+    width: 100%;
+    min-height: 34px;
+    padding: 6px 8px;
+    border-radius: 0;
+    border: 1px solid rgba(195, 185, 174, 0.28);
+    background:
+        linear-gradient(180deg, rgba(255, 255, 255, 0.95), rgba(246, 244, 240, 0.9));
+    color: var(--ink);
+    text-align: center;
+    font-size: 0.62rem;
+    font-weight: 800;
+    line-height: 1.08;
+    letter-spacing: 0.07em;
+    text-transform: uppercase;
+    box-shadow: 0 6px 12px rgba(42, 33, 53, 0.04);
+    cursor: pointer;
+    transition:
+        border-color 0.18s ease,
+        color 0.18s ease,
+        background 0.18s ease,
+        box-shadow 0.18s ease,
+        opacity 0.18s ease;
+    clip-path:
+        polygon(var(--ui-cut-sm) 0, calc(100% - var(--ui-cut-sm)) 0, 100% var(--ui-cut-sm), 100% calc(100% - var(--ui-cut-sm)), calc(100% - var(--ui-cut-sm)) 100%, var(--ui-cut-sm) 100%, 0 calc(100% - var(--ui-cut-sm)), 0 var(--ui-cut-sm));
+}
+
+.qts-detached-icon {
+    display: none;
+    position: relative;
+    z-index: 1;
+    font-size: 1rem;
+    line-height: 1;
+}
+
+.qts-detached-text {
+    position: relative;
+    z-index: 1;
+}
+
+.qts-detached-week-button::before,
+.qts-detached-toggle::before,
+.qts-detached-summary-button::before {
+    content: "";
+    position: absolute;
+    top: 0;
+    left: var(--ui-cut-sm);
+    right: var(--ui-cut-sm);
+    height: 1px;
+    background:
+        linear-gradient(90deg, rgba(var(--qts-rail-accent-rgb), 0.06), rgba(var(--qts-rail-accent-rgb), 0.7), rgba(var(--qts-rail-accent-rgb), 0.06));
+    pointer-events: none;
+}
+
+.qts-detached-week-button::after,
+.qts-detached-toggle::after,
+.qts-detached-summary-button::after {
+    content: "";
+    position: absolute;
+    left: 12px;
+    right: 12px;
+    bottom: 0;
+    height: 1px;
+    background:
+        linear-gradient(90deg, rgba(var(--qts-rail-accent-rgb), 0), rgba(var(--qts-rail-accent-rgb), 0.36), rgba(var(--qts-rail-accent-rgb), 0));
+    opacity: 0.88;
+    pointer-events: none;
+}
+
+.qts-detached-week-button:hover,
+.qts-detached-toggle:hover,
+.qts-detached-summary-button:hover {
+    border-color: rgba(195, 185, 174, 0.36);
+    background:
+        linear-gradient(180deg, rgba(255, 255, 255, 0.96), rgba(247, 245, 241, 0.92));
+    box-shadow: 0 8px 16px rgba(42, 33, 53, 0.05);
+}
+
+.qts-detached-week-button.is-active,
+.qts-detached-toggle:has(input:checked),
+.qts-detached-summary-button.is-active {
+    color: #1f2a33;
+    border-color: rgba(var(--qts-rail-accent-rgb), 0.42);
+    background:
+        linear-gradient(180deg, rgba(var(--qts-rail-accent-rgb), 0.54), rgba(255, 255, 255, 0.94));
+    box-shadow:
+        inset 0 0 0 1px rgba(255, 255, 255, 0.28),
+        0 8px 16px rgba(var(--qts-rail-accent-rgb), 0.1);
+}
+
+body.dark .qts-detached-week-button,
+body.dark .qts-detached-toggle,
+body.dark .qts-detached-summary-button {
+    border-color: rgba(255, 255, 255, 0.08);
+    background:
+        linear-gradient(180deg, rgba(26, 33, 47, 0.96), rgba(17, 23, 35, 0.94));
+    color: rgba(231, 236, 246, 0.92);
+    box-shadow: 0 10px 18px rgba(0, 0, 0, 0.22);
+}
+
+body.dark .qts-detached-week-button:hover,
+body.dark .qts-detached-toggle:hover,
+body.dark .qts-detached-summary-button:hover {
+    border-color: rgba(255, 255, 255, 0.12);
+    background:
+        linear-gradient(180deg, rgba(31, 39, 56, 0.98), rgba(20, 27, 40, 0.96));
+    box-shadow: 0 12px 20px rgba(0, 0, 0, 0.26);
+}
+
+body.dark .qts-detached-week-button.is-active,
+body.dark .qts-detached-toggle:has(input:checked),
+body.dark .qts-detached-summary-button.is-active {
+    color: #f3f7ff;
+    border-color: rgba(var(--qts-rail-accent-rgb), 0.26);
+    background:
+        linear-gradient(180deg, rgba(var(--qts-rail-accent-rgb), 0.28), rgba(22, 30, 44, 0.98));
+    box-shadow:
+        inset 0 1px 0 rgba(255, 255, 255, 0.08),
+        0 12px 22px rgba(0, 0, 0, 0.24);
+}
+
+.qts-detached-toggle-group {
+    display: grid;
+    gap: 5px;
+}
+
+.qts-detached-toggle {
+    display: grid;
+    grid-template-columns: minmax(0, 1fr);
+    align-items: center;
+    gap: 0;
+}
+
+.qts-detached-toggle input {
+    position: absolute;
+    opacity: 0;
+    pointer-events: none;
+    inline-size: 1px;
+    block-size: 1px;
+    margin: 0;
+}
+
+.qts-detached-toggle span {
+    min-width: 0;
+    position: relative;
+    z-index: 1;
+}
+
+.qts-detached-toggle:has(input:disabled) {
+    opacity: 0.56;
+    cursor: default;
+}
+
+.qts-detached-index-item {
+    text-align: center;
+    font-size: 0.76rem;
+    font-weight: 800;
+    letter-spacing: 0.14em;
+    text-transform: uppercase;
+    color: var(--muted);
+    line-height: 1.5;
+    padding: 4px 10px;
+}
+
+.qts-detached-index-divider {
+    position: relative;
+    height: 4px;
+    margin: 0;
+    background: none;
+}
+
+.qts-detached-index-divider::before {
+    content: "";
+    position: absolute;
+    left: 10px;
+    right: 10px;
+    top: 50%;
+    height: 1px;
+    transform: translateY(-50%);
+    background:
+        linear-gradient(90deg, rgba(154, 160, 172, 0), rgba(154, 160, 172, 0.28) 22%, rgba(154, 160, 172, 0.42) 50%, rgba(154, 160, 172, 0.28) 78%, rgba(154, 160, 172, 0));
+}
+
+body[data-mode="qts"] #qtsModule.module.active {
+    box-sizing: border-box;
+    margin-left: calc(var(--qts-left-rail-space, 0px) + var(--qts-left-clearance, 0px));
+    margin-right: calc(var(--qts-right-tools-space, 0px) + var(--qts-right-edge-gap, 0px));
+    width: calc(100% - var(--qts-left-rail-space, 0px) - var(--qts-left-clearance, 0px) - var(--qts-right-tools-space, 0px) - var(--qts-right-edge-gap, 0px));
+    max-width: 100%;
+    padding-left: var(--qts-inline-gutter, 18px);
+    padding-right: var(--qts-inline-gutter, 18px);
+}
+
+@media (max-width: 1480px) and (min-width: 761px) {
+    body[data-mode="qts"] #qtsModule {
+        --qts-inline-gutter: 4px;
+        --qts-right-tools-space: 60px;
+        --qts-content-max: 1120px;
+    }
+
+    body[data-mode="qts"] {
+        --qts-detached-rail-width: 112px;
+        --qts-detached-rail-gap: 2px;
+    }
+
+    .qts-detached-index {
+        left: 10px;
+        top: 116px;
+        gap: 8px;
+    }
+
+    .qts-detached-block {
+        gap: 6px;
+        padding: 6px 4px 4px;
+    }
+
+    .qts-detached-block-label,
+    .qts-detached-index-divider {
+        display: block;
+    }
+
+    .qts-detached-week-nav,
+    .qts-detached-toggle-group {
+        gap: 6px;
+    }
+
+    .qts-detached-week-button,
+    .qts-detached-toggle,
+    .qts-detached-summary-button {
+        width: 100%;
+        min-height: 32px;
+        padding: 5px 6px;
+        font-size: 0.58rem;
+    }
+
+    .qts-detached-icon {
+        display: none;
+    }
+
+    .qts-detached-week-button .qts-detached-text,
+    .qts-detached-toggle span:last-child,
+    .qts-detached-summary-button .qts-detached-text {
+        display: block;
+    }
+
+    body[data-mode="qts"] #qtsModule .qts-stage-main {
+        width: 100%;
+        justify-items: stretch;
+    }
+
+    body[data-mode="qts"] #qtsModule .qts-template-strip,
+    body[data-mode="qts"] #qtsModule .qts-title {
+        justify-self: center;
+    }
+
+    body[data-mode="qts"] #qtsModule .qts-week-rail,
+    body[data-mode="qts"] #qtsModule .qts-grid-shell,
+    body[data-mode="qts"] #qtsModule .qts-controls,
+    body[data-mode="qts"] #qtsModule .qts-subject-summary {
+        width: var(--qts-safe-inline-size);
+        max-width: var(--qts-safe-inline-size);
+        justify-self: center;
+    }
+
+    body[data-mode="qts"] #qtsModule.module.active {
+        margin-left: calc(var(--qts-left-rail-space, 0px) + var(--qts-left-clearance, 0px));
+        width: calc(100% - var(--qts-left-rail-space, 0px) - var(--qts-left-clearance, 0px));
+        padding-left: var(--qts-inline-gutter);
+        padding-right: var(--qts-inline-gutter);
+    }
+
+    .qts-grid-shell {
+        padding: 1px 0.8mm 0.8mm;
+    }
+
+    #qtsGrid {
+        padding: 1px;
+    }
+
+    #qtsGrid > div {
+        min-height: 30px;
+    }
+
+    #qtsGrid div[contenteditable="true"] {
+        padding: 3px 5px;
+        font-size: 0.78rem;
+        line-height: 1.02;
+    }
+
+    .qts-header {
+        min-height: 40px;
+        padding: 3px 5px 2px;
+    }
+
+    .qts-time {
+        min-height: 30px;
+    }
+
+    .qts-header-day {
+        font-size: 0.76rem;
+    }
+
+    .qts-header-date {
+        font-size: 0.56rem;
+    }
+
+    .qts-interval {
+        min-height: 13px !important;
+        font-size: 0.72rem;
+    }
+}
+
+@media (max-width: 1220px) and (min-width: 761px) {
+    body[data-mode="qts"] #qtsModule {
+        --qts-safe-inline-size: min(100%, calc(100vw - 176px));
+        --qts-inline-gutter: 3px;
+        --qts-right-tools-space: 58px;
+        --qts-content-max: none;
+    }
+
+    body[data-mode="qts"] {
+        --qts-detached-rail-width: 100px;
+        --qts-detached-rail-gap: 2px;
+    }
+
+    .qts-detached-index {
+        top: 112px;
+        left: 10px;
+        gap: 8px;
+    }
+
+    body[data-mode="qts"] #qtsModule.module.active {
+        margin-left: calc(var(--qts-left-rail-space, 0px) + var(--qts-left-clearance, 0px));
+        width: calc(100% - var(--qts-left-rail-space, 0px) - var(--qts-left-clearance, 0px));
+        padding-left: var(--qts-inline-gutter);
+        padding-right: var(--qts-inline-gutter);
+    }
+
+    .qts-grid-shell {
+        padding: 1px 0.7mm 0.7mm;
+    }
+
+    body[data-mode="qts"] #qtsModule .qts-week-rail,
+    body[data-mode="qts"] #qtsModule .qts-grid-shell,
+    body[data-mode="qts"] #qtsModule .qts-controls,
+    body[data-mode="qts"] #qtsModule .qts-subject-summary {
+        width: var(--qts-safe-inline-size);
+        max-width: var(--qts-safe-inline-size);
+        justify-self: center;
+    }
+
+    #qtsGrid {
+        padding: 0;
+    }
+
+    #qtsGrid > div {
+        min-height: 28px;
+    }
+
+    #qtsGrid div[contenteditable="true"] {
+        padding: 3px 4px;
+        font-size: 0.74rem;
+        line-height: 1;
+    }
+
+    .qts-header {
+        min-height: 38px;
+        padding: 3px 4px 2px;
+    }
+
+    .qts-time {
+        min-height: 28px;
+    }
+
+    .qts-header-day {
+        font-size: 0.72rem;
+    }
+
+    .qts-header-date {
+        font-size: 0.54rem;
+    }
+
+    .qts-interval {
+        min-height: 12px !important;
+        font-size: 0.68rem;
+    }
+}
+
+@media (max-width: 1080px) and (min-width: 761px) {
+    body[data-mode="qts"] #qtsModule {
+        --qts-inline-gutter: 2px;
+        --qts-right-tools-space: 56px;
+    }
+
+    body[data-mode="qts"] {
+        --qts-detached-rail-width: 92px;
+        --qts-detached-rail-gap: 1px;
+    }
+
+    body[data-mode="qts"] .side-modules {
+        right: 2px;
+        width: auto;
+        padding: 0;
+        background: transparent;
+        backdrop-filter: none;
+        border: 0;
+        box-shadow: none;
+        clip-path: none;
+        transform: translateY(-50%);
+        transition: opacity 0.18s ease, right 0.18s ease;
+    }
+
+    body[data-mode="qts"] .side-modules::after {
+        display: none;
+    }
+
+    body[data-mode="qts"] .side-group {
+        gap: 6px;
+    }
+
+    body[data-mode="qts"] .side-group-buttons {
+        gap: 6px;
+    }
+
+    body[data-mode="qts"] .side-group[data-nav-group="utility"] {
+        padding-top: 6px;
+        border-top: 0;
+    }
+
+    body[data-mode="qts"] .side-modules .icon-circle,
+    body[data-mode="qts"] .side-group[data-nav-group="primary"] .icon-circle,
+    body[data-mode="qts"] .side-group[data-nav-group="utility"] .icon-circle {
+        width: 40px;
+        height: 40px;
+    }
+
+    body[data-mode="qts"] #qtsModule .qts-stage-main {
+        justify-items: center;
+    }
+
+    body[data-mode="qts"] #qtsModule .qts-template-strip,
+    body[data-mode="qts"] #qtsModule .qts-title {
+        justify-self: center;
+        max-width: calc(100% - 12px);
+    }
+
+    body[data-mode="qts"] #qtsModule .qts-grid-shell,
+    body[data-mode="qts"] #qtsModule .qts-controls,
+    body[data-mode="qts"] #qtsModule .qts-subject-summary {
+        width: var(--qts-safe-inline-size);
+        max-width: var(--qts-safe-inline-size);
+        justify-self: center;
+    }
+
+    .qts-detached-block {
+        gap: 6px;
+        padding: 6px 4px 4px;
+    }
+
+    .qts-detached-block-label {
+        font-size: 0.5rem;
+        letter-spacing: 0.16em;
+    }
+
+    .qts-detached-week-button,
+    .qts-detached-toggle,
+    .qts-detached-summary-button {
+        min-height: 32px;
+        padding: 5px 6px;
+        font-size: 0.58rem;
+    }
+}
+
+@media (max-width: 980px) and (min-width: 761px) {
+    body[data-mode="qts"] #qtsModule {
+        --qts-inline-gutter: 1px;
+        --qts-right-tools-space: 54px;
+    }
+
+    body[data-mode="qts"] {
+        --qts-detached-rail-width: 86px;
+        --qts-detached-rail-gap: 1px;
+    }
+
+    .qts-template-strip {
+        gap: 8px;
+        margin-bottom: 12px;
+    }
+
+    .qts-template-strip::after {
+        width: min(100%, 440px);
+    }
+
+    .qts-template-label {
+        min-height: 32px;
+        padding: 0 14px;
+        font-size: 0.64rem;
+        letter-spacing: 0.12em;
+    }
+
+    .qts-templates {
+        gap: 6px;
+    }
+
+    .qts-templates button {
+        min-height: 32px;
+        padding: 0 10px;
+        font-size: 0.74rem;
+    }
+
+    .qts-title {
+        padding: 7px 16px;
+        font-size: clamp(1.16rem, 2vw, 1.42rem);
+    }
+
+    .qts-grid-shell {
+        padding: 2px 4px 5px;
+        background: linear-gradient(180deg, rgba(255, 255, 255, 0.97), rgba(248, 247, 243, 0.95));
+        box-shadow: 0 12px 24px rgba(15, 23, 42, 0.07);
+    }
+
+    #qtsGrid {
+        background: linear-gradient(180deg, rgba(255, 255, 255, 0.98), rgba(248, 247, 243, 0.96));
+    }
+
+    .qts-controls {
+        gap: 12px;
+    }
+
+    .row-control {
+        min-width: 132px;
+        padding: 5px 8px;
+        grid-template-columns: 26px 1fr 26px;
+        gap: 6px;
+    }
+
+    .row-btn {
+        width: 26px;
+        height: 22px;
+    }
+
+    .row-label {
+        font-size: 0.54rem;
+        letter-spacing: 0.13em;
+    }
+
+    .qts-detached-block {
+        gap: 5px;
+        padding: 6px 4px 4px;
+    }
+
+    .qts-detached-block-label {
+        font-size: 0.5rem;
+        letter-spacing: 0.16em;
+    }
+
+    .qts-detached-week-button,
+    .qts-detached-toggle,
+    .qts-detached-summary-button {
+        min-height: 30px;
+        padding: 4px 5px;
+        font-size: 0.56rem;
+    }
+
+    #qtsGrid > div {
+        color: #202632;
+    }
+
+    #qtsGrid div[contenteditable="true"] {
+        font-size: 0.7rem;
+    }
+
+    .qts-header {
+        background: linear-gradient(180deg, rgba(222, 229, 244, 0.98), rgba(214, 223, 241, 0.96));
+    }
+
+    .qts-time {
+        background: linear-gradient(180deg, rgba(236, 244, 255, 0.98), rgba(225, 237, 252, 0.96));
+    }
+
+    .qts-interval {
+        background: linear-gradient(90deg, rgba(218, 240, 230, 0.92), rgba(227, 245, 237, 0.94));
+    }
+}
+
+.qts-week-rail {
+    width: min(100%, 1180px);
+    margin: 0 0 10px;
+    display: grid;
+    grid-template-columns: repeat(3, minmax(0, 1fr));
+    gap: 10px;
+    align-items: stretch;
+}
+
+.qts-week-context {
+    width: 100%;
+    margin: 0;
+    padding: 10px 14px;
+    display: grid;
+    gap: 1px;
+    justify-items: center;
+    position: relative;
+    overflow: hidden;
+    border-radius: 0;
+    background: var(--ui-clickable-surface);
+    border: 1px solid var(--ui-clickable-border);
+    box-shadow: var(--ui-clickable-shadow);
+    text-align: center;
+    transition: transform 0.18s ease, box-shadow 0.18s ease, border-color 0.18s ease, background 0.18s ease;
+    clip-path: polygon(var(--ui-cut-md) 0, calc(100% - var(--ui-cut-md)) 0, 100% var(--ui-cut-md), 100% calc(100% - var(--ui-cut-md)), calc(100% - var(--ui-cut-md)) 100%, var(--ui-cut-md) 100%, 0 calc(100% - var(--ui-cut-md)), 0 var(--ui-cut-md));
+}
+
+.qts-week-context::before {
+    content: "";
+    position: absolute;
+    top: 0;
+    left: var(--ui-cut-md);
+    right: var(--ui-cut-md);
+    height: 1px;
+    background: var(--ui-clickable-line);
+    pointer-events: none;
+}
+
+button.qts-week-context {
+    appearance: none;
+    cursor: pointer;
+    font: inherit;
+    color: inherit;
+}
+
+.qts-week-context:hover {
+    transform: translateY(-1px);
+}
+
+.qts-week-context.is-active {
+    border-color: rgba(159, 197, 230, 0.44);
+    background: linear-gradient(180deg, rgba(183, 215, 240, 0.38), rgba(255, 255, 255, 0.94));
+    box-shadow:
+        0 20px 36px rgba(159, 197, 230, 0.16),
+        inset 0 0 0 1px rgba(159, 197, 230, 0.16);
+}
+
+.qts-week-context-future {
+    position: relative;
+    overflow: hidden;
+}
+
+.qts-week-context-future::after {
+    content: "";
+    position: absolute;
+    inset: auto -18% 16px;
+    height: 10px;
+    background: rgba(230, 193, 182, 0.28);
+    transform: rotate(-6deg);
+}
+
+.qts-week-context-future.is-active {
+    border-color: rgba(230, 193, 182, 0.44);
+    box-shadow:
+        0 20px 40px rgba(230, 193, 182, 0.18),
+        inset 0 0 0 1px rgba(230, 193, 182, 0.16);
+}
+
+.qts-week-kicker,
+.qts-summary-kicker {
+    font-size: 0.62rem;
+    font-weight: 800;
+    letter-spacing: 0.18em;
+    text-transform: uppercase;
+    color: var(--muted);
+}
+
+.qts-week-context strong {
+    font-size: 0.94rem;
+    font-weight: 800;
+}
+
+.qts-week-context span:last-child {
+    font-size: 0.72rem;
+    color: var(--muted);
+    text-transform: capitalize;
+}
+
+body.dark #qtsGrid {
+    background: var(--ui-informative-surface);
+    border-color: transparent;
+}
+
+body.dark .qts-week-context {
+    background: var(--ui-clickable-surface);
+    border-color: var(--ui-clickable-border);
+}
+
+.qts-grid-shell {
+    position: relative;
+    width: var(--qts-safe-inline-size);
+    max-width: var(--qts-safe-inline-size);
+    justify-self: center;
+    margin: 0;
+    padding: 1px 2px 3px;
+    border-radius: 0;
+    background: var(--ui-informative-surface);
+    border: 1px solid transparent;
+    box-shadow: var(--ui-informative-shadow);
+    clip-path: polygon(var(--ui-cut-lg) 0, calc(100% - var(--ui-cut-lg)) 0, 100% var(--ui-cut-lg), 100% calc(100% - var(--ui-cut-lg)), calc(100% - var(--ui-cut-lg)) 100%, var(--ui-cut-lg) 100%, 0 calc(100% - var(--ui-cut-lg)), 0 var(--ui-cut-lg));
+}
+
+.qts-week-overlay {
+    position: absolute;
+    z-index: 6;
+    pointer-events: none;
+    font-size: 0.86rem;
+    font-weight: 800;
+    letter-spacing: 0.22em;
+    text-transform: uppercase;
+}
+
+.qts-week-overlay-future {
+    top: 46%;
+    left: -6%;
+    right: -6%;
+    padding: 14px 0;
+    text-align: center;
+    color: rgba(134, 45, 16, 0.92);
+    background: rgba(243, 108, 79, 0.2);
+    border-top: 1px solid rgba(243, 108, 79, 0.3);
+    border-bottom: 1px solid rgba(243, 108, 79, 0.3);
+    transform: rotate(-8deg);
+}
+
+.qts-week-overlay-readonly {
+    top: 14px;
+    right: 18px;
+    padding: 8px 12px;
+    border-radius: 0;
+    color: rgba(73, 85, 105, 0.92);
+    background: var(--ui-informative-surface);
+    border: 1px solid transparent;
+    letter-spacing: 0.12em;
+    clip-path: polygon(var(--ui-cut-sm) 0, calc(100% - var(--ui-cut-sm)) 0, 100% var(--ui-cut-sm), 100% calc(100% - var(--ui-cut-sm)), calc(100% - var(--ui-cut-sm)) 100%, var(--ui-cut-sm) 100%, 0 calc(100% - var(--ui-cut-sm)), 0 var(--ui-cut-sm));
+}
+
+body.dark .qts-week-overlay-readonly {
+    color: rgba(226, 232, 240, 0.9);
+    background: rgba(15, 23, 42, 0.72);
+    border-color: rgba(255, 255, 255, 0.08);
+}
+
+body.dark .qts-week-overlay-future {
+    color: rgba(255, 237, 213, 0.94);
+    background: rgba(243, 108, 79, 0.22);
+    border-top-color: rgba(243, 108, 79, 0.28);
+    border-bottom-color: rgba(243, 108, 79, 0.28);
+}
+
+#qtsGrid > div {
+    min-height: 32px;
+    border-radius: 0;
+    overflow: hidden;
+}
+
+#qtsGrid div[contenteditable="true"] {
+    display: flex;
+    align-items: center;
+    justify-content: flex-start;
+    padding: 4px 6px;
+    text-align: left;
+    font-size: 0.84rem;
+    line-height: 1.08;
+    overflow-wrap: normal;
+    word-break: keep-all;
+    white-space: nowrap;
+    text-overflow: ellipsis;
+}
+
+.qts-header {
+    background: rgba(37, 88, 240, 0.1);
+    color: var(--ink);
+    border-bottom: none;
+    display: grid;
+    gap: 0;
+    align-content: center;
+    min-height: 46px;
+    padding: 4px 6px 3px;
+    text-align: center;
+}
+
+.qts-time {
+    background: rgba(243, 108, 79, 0.1);
+    min-height: 32px;
+}
+
+#qtsGrid .qts-time[contenteditable="true"] {
+    justify-content: center;
+    padding: 4px 5px;
+    font-size: 0.82rem;
+    white-space: nowrap;
+    overflow-wrap: normal;
+    word-break: normal;
+}
+
+.qts-interval {
+    border-radius: 0 !important;
+    background: linear-gradient(90deg, rgba(30, 166, 114, 0.12), rgba(105, 206, 168, 0.12));
+    color: var(--muted);
+    min-height: 15px !important;
+    padding-top: 1px;
+    padding-bottom: 1px;
+}
+
+.qts-controls,
+.qts-templates {
+    flex-wrap: wrap;
+    justify-content: center;
+    gap: 8px;
+}
+
+.qts-template-strip {
+    width: fit-content;
+    max-width: 100%;
+    margin: 0 auto 14px;
+    display: grid;
+    gap: 10px;
+    justify-items: center;
+    position: relative;
+}
+
+.qts-template-strip::after {
+    content: "";
+    width: min(100%, 520px);
+    height: 1px;
+    background: linear-gradient(90deg, rgba(154, 160, 172, 0), rgba(154, 160, 172, 0.22) 18%, rgba(154, 160, 172, 0.38) 50%, rgba(154, 160, 172, 0.22) 82%, rgba(154, 160, 172, 0));
+    opacity: 0.95;
+}
+
+.qts-template-label {
+    min-height: 34px;
+    padding: 0 16px;
+    display: inline-flex;
+    align-items: center;
+    justify-content: center;
+    border: 1px solid rgba(195, 185, 174, 0.3);
+    background: linear-gradient(180deg, rgba(255, 255, 255, 0.76), rgba(249, 246, 240, 0.62));
+    color: var(--muted);
+    font-size: 0.7rem;
+    font-weight: 800;
+    letter-spacing: 0.14em;
+    text-transform: uppercase;
+    box-shadow: var(--ui-informative-shadow);
+    clip-path: polygon(var(--ui-cut-sm) 0, calc(100% - var(--ui-cut-sm)) 0, 100% var(--ui-cut-sm), 100% calc(100% - var(--ui-cut-sm)), calc(100% - var(--ui-cut-sm)) 100%, var(--ui-cut-sm) 100%, 0 calc(100% - var(--ui-cut-sm)), 0 var(--ui-cut-sm));
+    pointer-events: none;
+    user-select: none;
+}
+
+.qts-templates button {
+    position: relative;
+    overflow: hidden;
+    border-radius: 0;
+    border: 1px solid var(--ui-clickable-border);
+    background: var(--ui-clickable-surface);
+    color: var(--ink);
+    font-weight: 700;
+    transition: transform 0.18s ease, box-shadow 0.18s ease, background 0.18s ease;
+    clip-path: polygon(var(--ui-cut-sm) 0, calc(100% - var(--ui-cut-sm)) 0, 100% var(--ui-cut-sm), 100% calc(100% - var(--ui-cut-sm)), calc(100% - var(--ui-cut-sm)) 100%, var(--ui-cut-sm) 100%, 0 calc(100% - var(--ui-cut-sm)), 0 var(--ui-cut-sm));
+    box-shadow: var(--ui-clickable-shadow);
+    cursor: pointer;
+    min-height: 34px;
+    padding: 0 14px;
+    font-size: 0.82rem;
+}
+
+.qts-templates button::before {
+    content: "";
+    position: absolute;
+    top: 0;
+    left: var(--ui-cut-sm);
+    right: var(--ui-cut-sm);
+    height: 1px;
+    background: var(--ui-clickable-line);
+    pointer-events: none;
+}
+
+#qtsModule .qts-templates button.is-active {
+    background: linear-gradient(180deg, rgba(183, 215, 240, 0.94), rgba(159, 197, 230, 0.88));
+    color: #1f2a33 !important;
+    border-color: rgba(159, 197, 230, 0.54) !important;
+    transform: translateY(-1px);
+    box-shadow:
+        0 16px 28px rgba(159, 197, 230, 0.18),
+        inset 0 0 0 1px rgba(255, 255, 255, 0.3) !important;
+}
+
+body.dark .qts-templates button {
+    background: var(--ui-clickable-surface);
+}
+
+body.dark #qtsModule .qts-templates button.is-active,
+#qtsModule .qts-templates button[aria-pressed="true"],
+#qtsModule .qts-templates button.is-active:hover,
+#qtsModule .qts-templates button[aria-pressed="true"]:hover {
+    background: linear-gradient(180deg, rgba(183, 215, 240, 0.94), rgba(159, 197, 230, 0.88));
+    color: #1f2a33 !important;
+    border-color: rgba(159, 197, 230, 0.54) !important;
+    box-shadow:
+        0 16px 28px rgba(159, 197, 230, 0.18),
+        inset 0 0 0 1px rgba(255, 255, 255, 0.3) !important;
+}
+
+.qts-title {
+    width: fit-content;
+    margin: 0 auto 2px;
+    padding: 7px 18px;
+    text-align: center;
+    font-size: clamp(1.24rem, 2.2vw, 1.6rem);
+    font-weight: 800;
+    letter-spacing: -0.04em;
+    border: 1px solid rgba(183, 215, 240, 0.4);
+    background: linear-gradient(180deg, rgba(255, 255, 255, 0.72), rgba(255, 255, 255, 0.42));
+    box-shadow: var(--ui-informative-shadow);
+    clip-path: polygon(var(--ui-cut-md) 0, calc(100% - var(--ui-cut-md)) 0, 100% var(--ui-cut-md), 100% calc(100% - var(--ui-cut-md)), calc(100% - var(--ui-cut-md)) 100%, var(--ui-cut-md) 100%, 0 calc(100% - var(--ui-cut-md)), 0 var(--ui-cut-md));
+}
+
+body.dark .qts-template-label {
+    background: linear-gradient(180deg, rgba(24, 31, 46, 0.92), rgba(18, 25, 38, 0.8));
+    color: rgba(226, 232, 240, 0.78);
+    border-color: rgba(255, 255, 255, 0.08);
+}
+
+.qts-toggle-bar {
+    display: flex;
+    flex-wrap: wrap;
+    justify-content: center;
+    gap: 8px;
+    margin-bottom: 8px;
+}
+
+.qts-toggle {
+    display: inline-flex;
+    align-items: center;
+    gap: 8px;
+    min-height: 32px;
+    padding: 0 12px;
+    position: relative;
+    overflow: hidden;
+    border-radius: 0;
+    border: 1px solid var(--ui-clickable-border);
+    background: var(--ui-clickable-surface);
+    box-shadow: var(--ui-clickable-shadow);
+    clip-path: polygon(var(--ui-cut-sm) 0, calc(100% - var(--ui-cut-sm)) 0, 100% var(--ui-cut-sm), 100% calc(100% - var(--ui-cut-sm)), calc(100% - var(--ui-cut-sm)) 100%, var(--ui-cut-sm) 100%, 0 calc(100% - var(--ui-cut-sm)), 0 var(--ui-cut-sm));
+    cursor: pointer;
+    font-size: 0.78rem;
+    font-weight: 700;
+}
+
+.qts-toggle::before {
+    content: "";
+    position: absolute;
+    top: 0;
+    left: var(--ui-cut-sm);
+    right: var(--ui-cut-sm);
+    height: 1px;
+    background: var(--ui-clickable-line);
+    pointer-events: none;
+}
+
+.qts-toggle input {
+    accent-color: var(--ui-pastel-blue-strong);
+}
+
+.qts-controls {
+    display: flex;
+    align-items: center;
+    justify-content: center;
+    width: var(--qts-safe-inline-size);
+    max-width: var(--qts-safe-inline-size);
+    justify-self: center;
+    margin-top: 10px;
+    gap: 16px;
+    flex-wrap: wrap;
+}
+
+.row-control {
+    min-width: 144px;
+    padding: 6px 10px;
+    display: grid;
+    grid-template-columns: 28px 1fr 28px;
+    align-items: center;
+    gap: 8px;
+    border-radius: 0;
+    background: var(--ui-informative-surface);
+    border: 1px solid transparent;
+    box-shadow: var(--ui-informative-shadow);
+    clip-path: polygon(var(--ui-cut-md) 0, calc(100% - var(--ui-cut-md)) 0, 100% var(--ui-cut-md), 100% calc(100% - var(--ui-cut-md)), calc(100% - var(--ui-cut-md)) 100%, var(--ui-cut-md) 100%, 0 calc(100% - var(--ui-cut-md)), 0 var(--ui-cut-md));
+}
+
+.row-label {
+    text-align: center;
+    font-size: 0.58rem;
+    font-weight: 800;
+    letter-spacing: 0.15em;
+    text-transform: uppercase;
+    color: var(--muted);
+}
+
+.row-btn {
+    position: relative;
+    overflow: hidden;
+    width: 28px;
+    height: 24px;
+    border-radius: 0;
+    border: 1px solid var(--ui-clickable-border);
+    background: var(--ui-clickable-surface);
+    box-shadow: var(--ui-clickable-shadow);
+    clip-path: polygon(var(--ui-cut-sm) 0, calc(100% - var(--ui-cut-sm)) 0, 100% var(--ui-cut-sm), 100% calc(100% - var(--ui-cut-sm)), calc(100% - var(--ui-cut-sm)) 100%, var(--ui-cut-sm) 100%, 0 calc(100% - var(--ui-cut-sm)), 0 var(--ui-cut-sm));
+    cursor: pointer;
+}
+
+.qts-summary-card {
+    position: relative;
+}
+
+.qts-summary-toggle {
+    width: 100%;
+    display: grid;
+    grid-template-columns: minmax(0, 1fr) auto;
+    align-items: center;
+    gap: 10px;
+    padding: 12px 14px;
+    border: 1px solid var(--ui-clickable-border);
+    background: var(--ui-clickable-surface);
+    box-shadow: var(--ui-clickable-shadow);
+    clip-path: polygon(var(--ui-cut-md) 0, calc(100% - var(--ui-cut-md)) 0, 100% var(--ui-cut-md), 100% calc(100% - var(--ui-cut-md)), calc(100% - var(--ui-cut-md)) 100%, var(--ui-cut-md) 100%, 0 calc(100% - var(--ui-cut-md)), 0 var(--ui-cut-md));
+    cursor: pointer;
+    text-align: left;
+}
+
+.qts-summary-toggle::before {
+    content: "";
+    position: absolute;
+    top: 0;
+    left: var(--ui-cut-md);
+    right: var(--ui-cut-md);
+    height: 1px;
+    background: var(--ui-clickable-line);
+    pointer-events: none;
+}
+
+.qts-summary-toggle:hover {
+    transform: translateY(-1px);
+}
+
+.qts-summary-toggle-copy {
+    display: grid;
+    gap: 3px;
+}
+
+.qts-summary-body {
+    margin-top: 12px;
+}
+
+.qts-summary-card:not(.is-expanded) .qts-summary-body {
+    display: none;
+}
+
+.row-btn::before {
+    content: "";
+    position: absolute;
+    top: 0;
+    left: var(--ui-cut-sm);
+    right: var(--ui-cut-sm);
+    height: 1px;
+    background: var(--ui-clickable-line);
+    pointer-events: none;
+}
+
+#qtsGrid.is-selecting .qts-selectable {
+    user-select: none;
+}
+
+#qtsGrid > div.qts-cell-selected,
+#qtsGrid div[contenteditable="true"].qts-cell-selected {
+    background: rgba(73, 120, 255, 0.14) !important;
+    box-shadow: inset 0 0 0 1px rgba(73, 120, 255, 0.18) !important;
+}
+
+#qtsGrid > div.qts-time.qts-cell-selected,
+#qtsGrid div.qts-time[contenteditable="true"].qts-cell-selected {
+    background: rgba(243, 108, 79, 0.14) !important;
+    box-shadow: inset 0 0 0 1px rgba(243, 108, 79, 0.22) !important;
+}
+
+#qtsGrid > div.qts-selection-anchor {
+    box-shadow:
+        inset 0 0 0 2px rgba(73, 120, 255, 0.9),
+        0 10px 18px rgba(73, 120, 255, 0.12) !important;
+}
+
+body.dark #qtsGrid > div.qts-cell-selected,
+body.dark #qtsGrid div[contenteditable="true"].qts-cell-selected {
+    background: rgba(109, 148, 255, 0.18) !important;
+    box-shadow: inset 0 0 0 1px rgba(109, 148, 255, 0.26) !important;
+}
+
+body.dark #qtsGrid > div.qts-time.qts-cell-selected,
+body.dark #qtsGrid div.qts-time[contenteditable="true"].qts-cell-selected {
+    background: rgba(243, 108, 79, 0.2) !important;
+    box-shadow: inset 0 0 0 1px rgba(243, 108, 79, 0.3) !important;
+}
+
+.qts-autocomplete {
+    position: absolute;
+    z-index: 30;
+    display: grid;
+    gap: 6px;
+    padding: 10px;
+    border-radius: 18px;
+    background: rgba(255, 255, 255, 0.92);
+    border: 1px solid rgba(255, 255, 255, 0.82);
+    box-shadow: 0 24px 60px rgba(17, 24, 39, 0.14);
+    backdrop-filter: blur(18px);
+}
+
+.qts-autocomplete[hidden] {
+    display: none;
+}
+
+.qts-autocomplete-item {
+    padding: 10px 12px;
+    border-radius: 12px;
+    border: 1px solid transparent;
+    background: rgba(73, 120, 255, 0.06);
+    color: var(--ink);
+    text-align: left;
+    font-weight: 700;
+    cursor: pointer;
+    transition: background 0.18s ease, transform 0.18s ease, border-color 0.18s ease;
+}
+
+.qts-autocomplete-item:hover,
+.qts-autocomplete-item.is-active {
+    background: rgba(73, 120, 255, 0.14);
+    border-color: rgba(73, 120, 255, 0.22);
+    transform: translateY(-1px);
+}
+
+body.dark .qts-autocomplete {
+    background: rgba(17, 24, 39, 0.92);
+    border-color: rgba(255, 255, 255, 0.08);
+    box-shadow: 0 24px 60px rgba(0, 0, 0, 0.28);
+}
+
+body.dark .qts-autocomplete-item {
+    background: rgba(255, 255, 255, 0.06);
+    color: #f4f7ff;
+}
+
+body.dark .qts-autocomplete-item:hover,
+body.dark .qts-autocomplete-item.is-active {
+    background: rgba(109, 148, 255, 0.18);
+    border-color: rgba(109, 148, 255, 0.28);
+}
+
+.qts-header-day {
+    font-size: 0.84rem;
+    font-weight: 700;
+    line-height: 1.05;
+}
+
+.qts-header-date {
+    display: block;
+    font-size: 0.62rem;
+    letter-spacing: 0.08em;
+    text-transform: uppercase;
+    color: var(--muted);
+    line-height: 1.02;
+    white-space: nowrap;
+}
+
+.qts-subject-summary {
+    width: min(100%, 1080px);
+    justify-self: start;
+    margin-top: 16px;
+}
+
+.qts-summary-card {
+    padding: 16px 18px;
+    border-radius: 0;
+    background: var(--ui-informative-surface);
+    border: 1px solid transparent;
+    box-shadow: var(--ui-informative-shadow);
+    clip-path: polygon(var(--ui-cut-lg) 0, calc(100% - var(--ui-cut-lg)) 0, 100% var(--ui-cut-lg), 100% calc(100% - var(--ui-cut-lg)), calc(100% - var(--ui-cut-lg)) 100%, var(--ui-cut-lg) 100%, 0 calc(100% - var(--ui-cut-lg)), 0 var(--ui-cut-lg));
+}
+
+body.dark .qts-week-context,
+body.dark .qts-summary-card {
+    background: var(--ui-informative-surface);
+    border-color: transparent;
+}
+
+.qts-summary-card.is-empty {
+    display: grid;
+    gap: 10px;
+}
+
+.qts-summary-card.is-empty p {
+    margin: 0;
+    color: var(--muted);
+}
+
+.qts-summary-head {
+    display: flex;
+    align-items: center;
+    justify-content: space-between;
+    gap: 10px;
+    flex-wrap: wrap;
+    margin-bottom: 10px;
+}
+
+.qts-summary-head > div:first-child {
+    min-width: 0;
+}
+
+.qts-summary-head strong {
+    display: block;
+    margin-top: 4px;
+    font-size: 0.96rem;
+    font-weight: 800;
+}
+
+.qts-summary-total {
+    min-width: fit-content;
+    padding: 5px 9px;
+    border-radius: 0;
+    background: linear-gradient(180deg, rgba(183, 215, 240, 0.3), rgba(183, 215, 240, 0.16));
+    font-size: 0.78rem;
+    font-weight: 800;
+    color: var(--ink);
+    clip-path: polygon(var(--ui-cut-sm) 0, calc(100% - var(--ui-cut-sm)) 0, 100% var(--ui-cut-sm), 100% calc(100% - var(--ui-cut-sm)), calc(100% - var(--ui-cut-sm)) 100%, var(--ui-cut-sm) 100%, 0 calc(100% - var(--ui-cut-sm)), 0 var(--ui-cut-sm));
+}
+
+.qts-summary-list {
+    display: grid;
+    gap: 12px;
+}
+
+.qts-summary-scopes {
+    display: flex;
+    flex-wrap: wrap;
+    gap: 8px;
+    margin-bottom: 14px;
+}
+
+.qts-summary-scope {
+    min-height: 34px;
+    padding: 0 12px;
+    border-radius: 999px;
+    border: 1px solid var(--line);
+    background: rgba(255, 255, 255, 0.52);
+    color: var(--ink);
+    font-size: 0.76rem;
+    font-weight: 800;
+    letter-spacing: 0.08em;
+    text-transform: uppercase;
+}
+
+.qts-summary-scope.is-active {
+    background: rgba(37, 88, 240, 0.12);
+    border-color: rgba(37, 88, 240, 0.18);
+    color: #2558f0;
+}
+
+body.dark .qts-summary-scope {
+    background: rgba(255, 255, 255, 0.04);
+}
+
+.qts-summary-item {
+    display: grid;
+    grid-template-columns: minmax(0, 1.1fr) minmax(136px, 1.15fr) auto;
+    gap: 10px;
+    align-items: center;
+}
+
+@media (max-width: 1480px) and (min-width: 761px) {
+    .qts-summary-item {
+        grid-template-columns: minmax(0, 1fr) minmax(156px, 1.2fr) auto;
+        gap: 12px;
     }
 
-    .qts-detached-week-nav,
-    .qts-detached-toggle-group {
-        gap: 6px;
-    }
+    .qts-summary-time {
+        min-width: 64px;
+        text-align: right;
+    }
+}
+
+.qts-summary-copy {
+    display: grid;
+    gap: 2px;
+}
+
+.qts-summary-copy strong {
+    font-size: 0.94rem;
+}
+
+.qts-summary-copy span,
+.qts-summary-time {
+    color: var(--muted);
+    font-size: 0.78rem;
+}
+
+.qts-summary-bar {
+    height: 10px;
+    border-radius: 999px;
+    background: rgba(22, 30, 51, 0.08);
+    overflow: hidden;
+}
+
+body.dark .qts-summary-bar {
+    background: rgba(255, 255, 255, 0.08);
+}
+
+.qts-summary-fill {
+    height: 100%;
+    border-radius: inherit;
+    background: linear-gradient(90deg, rgba(37, 88, 240, 0.94), rgba(71, 191, 162, 0.94));
+    box-shadow: 0 10px 24px rgba(37, 88, 240, 0.22);
+}
+
+.side-modules {
+    position: fixed;
+    top: 50%;
+    right: clamp(20px, calc((100vw - var(--content-width)) / 2 + 20px), 88px);
+    left: auto;
+    transform: translateY(-50%);
+    padding: 11px 10px;
+    border-radius: 0;
+    background: var(--ui-informative-surface);
+    backdrop-filter: blur(16px);
+    border: 1px solid transparent;
+    box-shadow: var(--ui-informative-shadow);
+    gap: 10px;
+    clip-path: polygon(var(--ui-cut-md) 0, calc(100% - var(--ui-cut-md)) 0, 100% var(--ui-cut-md), 100% calc(100% - var(--ui-cut-md)), calc(100% - var(--ui-cut-md)) 100%, var(--ui-cut-md) 100%, 0 calc(100% - var(--ui-cut-md)), 0 var(--ui-cut-md));
+}
+
+.side-modules .icon-circle {
+    width: 44px;
+    height: 44px;
+}
+
+.side-group {
+    display: grid;
+    gap: 8px;
+}
+
+.side-group-label {
+    font-size: 0.62rem;
+    font-weight: 800;
+    letter-spacing: 0.16em;
+    text-transform: uppercase;
+    color: var(--muted);
+    text-align: center;
+}
+
+.side-group-buttons {
+    display: grid;
+    gap: 8px;
+}
+
+.side-group[data-nav-group="primary"] .icon-circle {
+    width: 48px;
+    height: 48px;
+}
+
+.side-group[data-nav-group="utility"] .icon-circle {
+    width: 38px;
+    height: 38px;
+    font-size: 0.82rem;
+}
+
+.side-group[data-nav-group="utility"] {
+    padding-top: 10px;
+    border-top: 1px solid var(--line);
+}
+
+.side-group[data-nav-group="utility"] .side-group-label {
+    display: none;
+}
+
+body[data-mode="clock"] .side-modules {
+    opacity: 0;
+    pointer-events: none;
+    transform: translateY(-50%) translateX(16px);
+}
+
+body:not([data-mode="clock"]) .side-modules {
+    opacity: 1;
+    pointer-events: auto;
+    transition: opacity 0.18s ease, transform 0.18s ease;
+}
+
+body[data-mode="qts"] .side-modules {
+    right: clamp(8px, calc((100vw - var(--content-width)) / 2 + 8px), 36px);
+}
+
+.footer {
+    margin-top: calc(16px * var(--site-scale, 1));
+    padding: var(--site-footer-pad-top) var(--site-footer-pad-x) var(--site-footer-pad-bottom);
+    border-radius: 0;
+    background: var(--ui-informative-surface);
+    backdrop-filter: blur(16px);
+    border: 1px solid transparent;
+    box-shadow: var(--ui-informative-shadow);
+    clip-path: polygon(var(--ui-cut-lg) 0, calc(100% - var(--ui-cut-lg)) 0, 100% var(--ui-cut-lg), 100% calc(100% - var(--ui-cut-lg)), calc(100% - var(--ui-cut-lg)) 100%, var(--ui-cut-lg) 100%, 0 calc(100% - var(--ui-cut-lg)), 0 var(--ui-cut-lg));
+}
+
+.donate-area {
+    display: flex;
+    justify-content: space-between;
+    align-items: center;
+    gap: calc(18px * var(--site-scale, 1));
+    margin-bottom: calc(14px * var(--site-scale, 1));
+}
+
+.donate-area a {
+    font-size: 0.92rem;
+    font-weight: 800;
+    letter-spacing: 0.08em;
+    text-transform: uppercase;
+}
+
+.footer-divider {
+    margin: calc(12px * var(--site-scale, 1)) 0;
+    background: var(--line);
+}
+
+.world-carousel {
+    font-family: "Space Grotesk", sans-serif;
+    font-size: 0.92rem;
+    letter-spacing: -0.03em;
+}
+
+.custom-box {
+    width: min(440px, calc(100vw - 36px));
+    border-radius: 28px;
+    padding: 26px;
+    background: var(--surface-strong);
+    border: 1px solid var(--line);
+}
+
+#ambientRoot {
+    --ambient-panel-surface:
+        linear-gradient(180deg, rgba(255, 255, 255, 0.94), rgba(242, 246, 252, 0.9));
+    --ambient-card-surface:
+        linear-gradient(180deg, rgba(255, 255, 255, 0.88), rgba(244, 248, 252, 0.82));
+    --ambient-card-hover:
+        linear-gradient(180deg, rgba(255, 255, 255, 0.98), rgba(235, 242, 251, 0.94));
+    --ambient-card-active:
+        linear-gradient(135deg, rgba(221, 234, 253, 0.98), rgba(241, 247, 255, 0.96));
+    --ambient-control-surface:
+        linear-gradient(180deg, rgba(255, 255, 255, 0.98), rgba(237, 243, 251, 0.94));
+    --ambient-control-hover:
+        linear-gradient(180deg, rgba(255, 255, 255, 1), rgba(229, 238, 249, 0.96));
+    --ambient-control-border: rgba(132, 157, 191, 0.26);
+    --ambient-control-border-strong: rgba(97, 137, 199, 0.42);
+    --ambient-control-shadow: 0 12px 22px rgba(44, 58, 78, 0.12);
+    --ambient-card-shadow: 0 16px 26px rgba(44, 58, 78, 0.08);
+    --ambient-accent: #4f84e3;
+    --ambient-accent-strong: #2f69d8;
+    --ambient-accent-soft: rgba(79, 132, 227, 0.18);
+    --ambient-success: #2ea483;
+    --ambient-shell-radius: calc(var(--rn-radius-shell, 14px) + 8px);
+    --ambient-card-radius: calc(var(--rn-radius-card, 10px) + 6px);
+    --ambient-control-radius: calc(var(--rn-radius-control, 9px) + 4px);
+}
+
+body.dark #ambientRoot,
+body.focus-mode #ambientRoot {
+    --ambient-panel-surface:
+        linear-gradient(180deg, rgba(15, 24, 39, 0.96), rgba(10, 17, 29, 0.92));
+    --ambient-card-surface:
+        linear-gradient(180deg, rgba(28, 41, 61, 0.94), rgba(19, 30, 47, 0.9));
+    --ambient-card-hover:
+        linear-gradient(180deg, rgba(37, 55, 81, 0.98), rgba(25, 39, 60, 0.94));
+    --ambient-card-active:
+        linear-gradient(135deg, rgba(48, 74, 118, 0.98), rgba(30, 48, 82, 0.94));
+    --ambient-control-surface:
+        linear-gradient(180deg, rgba(31, 45, 68, 0.96), rgba(22, 34, 54, 0.92));
+    --ambient-control-hover:
+        linear-gradient(180deg, rgba(40, 60, 90, 1), rgba(28, 44, 68, 0.94));
+    --ambient-control-border: rgba(118, 151, 202, 0.24);
+    --ambient-control-border-strong: rgba(137, 177, 239, 0.42);
+    --ambient-control-shadow: 0 14px 26px rgba(0, 0, 0, 0.24);
+    --ambient-card-shadow: 0 18px 30px rgba(0, 0, 0, 0.24);
+    --ambient-accent: #7aaeff;
+    --ambient-accent-strong: #5a91f2;
+    --ambient-accent-soft: rgba(122, 174, 255, 0.2);
+    --ambient-success: #49c8a0;
+}
+
+#ambientRoot .ambient-mini,
+#ambientRoot .ambient-panel {
+    color: var(--ink) !important;
+    background: var(--ambient-panel-surface) !important;
+    border: 1px solid var(--ambient-control-border) !important;
+    box-shadow:
+        0 24px 40px rgba(34, 47, 65, 0.12),
+        inset 0 1px 0 rgba(255, 255, 255, 0.55) !important;
+    backdrop-filter: blur(20px);
+}
+
+#ambientRoot .ambient-peek {
+    position: fixed;
+    left: 22px;
+    bottom: 22px;
+    display: inline-flex;
+    align-items: center;
+    justify-content: center;
+    gap: 0;
+    width: 58px;
+    min-width: 58px;
+    height: 58px;
+    padding: 0;
+    border-radius: 999px;
+    border: 1px solid rgba(125, 214, 255, 0.26);
+    background:
+        radial-gradient(circle at 38% 30%, rgba(125, 214, 255, 0.32), transparent 46%),
+        linear-gradient(145deg, rgba(12, 23, 40, 0.92), rgba(8, 14, 25, 0.86));
+    color: #eef9ff;
+    box-shadow:
+        0 18px 38px rgba(0, 0, 0, 0.32),
+        inset 0 1px 0 rgba(255, 255, 255, 0.12);
+    backdrop-filter: blur(18px);
+    z-index: 1310;
+    opacity: 0;
+    cursor: pointer;
+    pointer-events: none;
+    transform: translateY(10px);
+    transition: opacity 0.18s ease, transform 0.18s ease, background 0.18s ease;
+    clip-path: none;
+}
+
+#ambientRoot .ambient-peek.is-visible {
+    opacity: 1;
+    pointer-events: auto;
+    transform: translateY(0);
+}
+
+#ambientRoot .ambient-peek:hover {
+    background:
+        radial-gradient(circle at 38% 30%, rgba(125, 214, 255, 0.42), transparent 48%),
+        linear-gradient(145deg, rgba(15, 29, 50, 0.96), rgba(9, 17, 30, 0.9));
+}
+
+#ambientRoot .ambient-peek-dot {
+    width: 16px;
+    height: 16px;
+    border-radius: 999px;
+    background: #79d5ff;
+    box-shadow:
+        0 0 0 8px rgba(121, 213, 255, 0.1),
+        0 0 28px rgba(121, 213, 255, 0.42);
+}
+
+#ambientRoot .ambient-peek.is-playing .ambient-peek-dot {
+    background: #4fe3b3;
+    box-shadow:
+        0 0 0 8px rgba(79, 227, 179, 0.1),
+        0 0 30px rgba(79, 227, 179, 0.5);
+}
+
+#ambientRoot .ambient-peek-label {
+    display: none;
+    font-size: 0.82rem;
+    font-weight: 800;
+    letter-spacing: 0.12em;
+    text-transform: uppercase;
+}
+
+#ambientRoot .ambient-mini {
+    position: fixed;
+    left: 20px;
+    bottom: 22px;
+    width: min(clamp(246px, 24vw, 360px), calc(100vw - 32px));
+    padding: 12px 12px 11px;
+    border-radius: var(--ambient-shell-radius);
+    z-index: 1300;
+    display: grid;
+    gap: 10px;
+    transition: width 0.18s ease, opacity 0.18s ease, transform 0.18s ease;
+    box-shadow:
+        inset 0 1px 0 rgba(255, 255, 255, 0.28),
+        0 20px 34px rgba(34, 47, 65, 0.14) !important;
+    clip-path: none;
+}
+
+#ambientRoot .ambient-mini::after {
+    left: 14px;
+    right: 14px;
+    top: 0;
+    height: 2px;
+    border-radius: 999px;
+    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.78), transparent);
+    opacity: 0.7;
+}
+
+body.dark #ambientRoot .ambient-mini::after,
+body.focus-mode #ambientRoot .ambient-mini::after {
+    background: linear-gradient(90deg, transparent, rgba(174, 196, 232, 0.5), transparent);
+    opacity: 0.86;
+}
 
-    .qts-detached-week-button,
-    .qts-detached-toggle,
-    .qts-detached-summary-button {
-        width: 100%;
-        min-height: 32px;
-        padding: 5px 6px;
-        font-size: 0.58rem;
-    }
+#ambientRoot .ambient-mini.is-hidden {
+    display: none;
+}
 
-    .qts-detached-icon {
-        display: none;
-    }
+#ambientRoot .ambient-mini-main {
+    display: grid;
+    grid-template-columns: auto 1fr;
+    grid-template-areas:
+        "icon actions"
+        "ticker ticker";
+    align-items: center;
+    gap: 8px 12px;
+}
 
-    .qts-detached-week-button .qts-detached-text,
-    .qts-detached-toggle span:last-child,
-    .qts-detached-summary-button .qts-detached-text {
-        display: block;
-    }
+#ambientRoot .ambient-mini-actions {
+    grid-area: actions;
+    display: inline-flex;
+    align-items: center;
+    justify-content: flex-end;
+    justify-self: end;
+    gap: 6px;
+}
 
-    body[data-mode="qts"] #qtsModule .qts-stage-main {
-        width: 100%;
-        justify-items: stretch;
+#ambientRoot .ambient-mini-meta {
+    display: grid;
+    gap: 8px;
+}
+
+#ambientRoot .ambient-mini-icon,
+#ambientRoot .ambient-primary-control,
+#ambientRoot .ambient-secondary-control,
+#ambientRoot .ambient-header-actions button {
+    display: inline-flex;
+    align-items: center;
+    justify-content: center;
+    padding: 0;
+    border: 1px solid var(--ambient-control-border);
+    background: var(--ambient-control-surface);
+    color: var(--ink);
+    box-shadow: var(--ambient-control-shadow);
+    cursor: pointer;
+    transition: transform 0.18s ease, background 0.18s ease, box-shadow 0.18s ease, border-color 0.18s ease, color 0.18s ease;
+    border-radius: var(--ambient-control-radius);
+    clip-path: none;
+}
+
+#ambientRoot .ambient-primary-control {
+    background: linear-gradient(180deg, var(--ambient-accent) 0%, var(--ambient-accent-strong) 100%);
+    color: #ffffff;
+    border-color: rgba(76, 122, 207, 0.34);
+    box-shadow:
+        0 12px 22px rgba(47, 105, 216, 0.28),
+        inset 0 1px 0 rgba(255, 255, 255, 0.18);
+}
+
+#ambientRoot .ambient-secondary-control,
+#ambientRoot .ambient-mini-icon {
+    background: var(--ambient-control-surface);
+    border-color: var(--ambient-control-border);
+    box-shadow: var(--ambient-control-shadow);
+}
+
+#ambientRoot .ambient-mini-icon,
+#ambientRoot .ambient-primary-control {
+    width: clamp(38px, 3vw, 42px);
+    height: clamp(38px, 3vw, 42px);
+}
+
+#ambientRoot .ambient-secondary-control,
+#ambientRoot .ambient-header-actions button {
+    width: clamp(34px, 2.9vw, 38px);
+    height: clamp(34px, 2.9vw, 38px);
+}
+
+#ambientRoot .ambient-mini-icon:hover,
+#ambientRoot .ambient-primary-control:hover,
+#ambientRoot .ambient-secondary-control:hover,
+#ambientRoot .ambient-header-actions button:hover {
+    transform: translateY(-1px);
+    background: var(--ambient-control-hover);
+    border-color: var(--ambient-control-border-strong);
+}
+
+#ambientRoot .ambient-primary-control:hover {
+    background: linear-gradient(180deg, #6698ef 0%, var(--ambient-accent-strong) 100%);
+}
+
+#ambientRoot .ambient-mini-icon.is-active,
+#ambientRoot .ambient-header-actions button.is-active {
+    background: var(--ambient-card-active);
+    border-color: var(--ambient-control-border-strong);
+    color: var(--ambient-accent-strong);
+}
+
+#ambientRoot .ambient-icon {
+    width: 16px;
+    height: 16px;
+    display: block;
+    pointer-events: none;
+    flex: 0 0 auto;
+}
+
+#ambientRoot .ambient-mini-icon .ambient-icon,
+#ambientRoot .ambient-primary-control .ambient-icon {
+    width: 18px;
+    height: 18px;
+}
+
+#ambientRoot .ambient-header-actions .ambient-icon,
+#ambientRoot .ambient-secondary-control .ambient-icon {
+    width: 16px;
+    height: 16px;
+}
+
+#ambientRoot .ambient-now {
+    grid-area: ticker;
+    min-width: 0;
+}
+
+#ambientRoot .ambient-panel-kicker,
+#ambientRoot .ambient-item-meta {
+    font-size: 0.66rem;
+    font-weight: 800;
+    letter-spacing: 0.14em;
+    text-transform: uppercase;
+    color: var(--muted);
+}
+
+#ambientRoot .ambient-mini-icon {
+    grid-area: icon;
+}
+
+#ambientRoot .ambient-ticker {
+    position: relative;
+    min-width: 0;
+    overflow: hidden;
+    padding-left: 2px;
+    mask-image: linear-gradient(90deg, transparent 0, #000 8%, #000 92%, transparent 100%);
+    -webkit-mask-image: linear-gradient(90deg, transparent 0, #000 8%, #000 92%, transparent 100%);
+}
+
+#ambientRoot .ambient-ticker-track {
+    display: flex;
+    align-items: center;
+    width: max-content;
+    gap: 20px;
+}
+
+#ambientRoot .ambient-ticker-copy {
+    white-space: nowrap;
+    font-size: 0.7rem;
+    font-weight: 800;
+    letter-spacing: 0.12em;
+    text-transform: uppercase;
+    color: var(--muted);
+}
+
+#ambientRoot .ambient-ticker:not(.is-scrolling) #ambientTickerClone {
+    display: none;
+}
+
+#ambientRoot .ambient-ticker.is-scrolling .ambient-ticker-track {
+    animation: ambientTicker 14s linear infinite;
+}
+
+#ambientRoot .ambient-progress {
+    display: grid;
+    grid-template-columns: 44px 1fr 44px;
+    align-items: center;
+    gap: 8px;
+    font-size: 0.7rem;
+    color: var(--muted);
+    padding: 0;
+    opacity: 1;
+}
+
+#ambientRoot #ambientSeek,
+#ambientRoot #ambientVolume {
+    width: 100%;
+    appearance: none;
+    height: 6px;
+    border-radius: 999px;
+    background: linear-gradient(90deg, rgba(104, 132, 168, 0.24), rgba(79, 132, 227, 0.36));
+    cursor: pointer;
+    outline: none;
+}
+
+#ambientRoot #ambientSeek::-webkit-slider-thumb,
+#ambientRoot #ambientVolume::-webkit-slider-thumb {
+    appearance: none;
+    width: 14px;
+    height: 14px;
+    border-radius: 999px;
+    border: 2px solid rgba(255, 255, 255, 0.94);
+    background: var(--ambient-accent-strong);
+    box-shadow: 0 4px 12px rgba(47, 105, 216, 0.26);
+}
+
+#ambientRoot #ambientSeek::-moz-range-thumb,
+#ambientRoot #ambientVolume::-moz-range-thumb {
+    width: 14px;
+    height: 14px;
+    border-radius: 999px;
+    border: 2px solid rgba(255, 255, 255, 0.94);
+    background: var(--ambient-accent-strong);
+    box-shadow: 0 4px 12px rgba(47, 105, 216, 0.26);
+}
+
+#ambientRoot #ambientSeek::-moz-range-track,
+#ambientRoot #ambientVolume::-moz-range-track {
+    height: 6px;
+    border-radius: 999px;
+    background: linear-gradient(90deg, rgba(104, 132, 168, 0.24), rgba(79, 132, 227, 0.36));
+}
+
+#ambientRoot .ambient-volume-wrap {
+    display: grid;
+    grid-template-columns: 26px 1fr;
+    align-items: center;
+    gap: 6px;
+}
+
+#ambientRoot .ambient-volume-label {
+    font-size: 0.68rem;
+    font-weight: 800;
+    letter-spacing: 0.1em;
+    color: var(--muted);
+}
+
+@keyframes ambientTicker {
+    from {
+        transform: translateX(0);
     }
 
-    body[data-mode="qts"] #qtsModule .qts-template-strip,
-    body[data-mode="qts"] #qtsModule .qts-title {
-        justify-self: center;
+    to {
+        transform: translateX(calc(-50% - 10px));
     }
+}
+
+#ambientRoot .ambient-panel {
+    position: fixed;
+    left: 20px;
+    bottom: 132px;
+    top: auto;
+    right: auto;
+    width: min(372px, calc(100vw - 40px));
+    height: min(520px, 64vh);
+    border-radius: var(--ambient-shell-radius) !important;
+    padding: 16px 14px 14px;
+    z-index: 1290;
+    display: flex;
+    flex-direction: column;
+    gap: 12px;
+    opacity: 0;
+    transform: translateY(14px) scale(0.98);
+    pointer-events: none;
+    transition: opacity 0.18s ease, transform 0.18s ease;
+    background: var(--ambient-panel-surface) !important;
+    clip-path: none;
+}
+
+#ambientRoot .ambient-panel.is-open {
+    opacity: 1;
+    transform: translateY(0) scale(1);
+    pointer-events: auto;
+}
+
+#ambientRoot .ambient-header {
+    display: grid;
+    grid-template-columns: minmax(0, 1fr) auto;
+    align-items: center;
+    gap: 14px;
+    padding: 0 2px 8px;
+    border-bottom: 1px solid rgba(140, 166, 201, 0.18);
+}
+
+#ambientRoot .ambient-header-copy {
+    min-width: 0;
+    display: grid;
+    gap: 4px;
+    color: var(--ink);
+}
+
+#ambientRoot .ambient-panel-title {
+    font-size: 1.05rem;
+    font-weight: 800;
+    letter-spacing: -0.04em;
+    color: var(--ink);
+}
 
-    body[data-mode="qts"] #qtsModule .qts-week-rail,
-    body[data-mode="qts"] #qtsModule .qts-grid-shell,
-    body[data-mode="qts"] #qtsModule .qts-controls,
-    body[data-mode="qts"] #qtsModule .qts-subject-summary {
-        width: var(--qts-safe-inline-size);
-        max-width: var(--qts-safe-inline-size);
-        justify-self: center;
-    }
+#ambientRoot .ambient-header-actions {
+    display: flex;
+    align-items: center;
+    gap: 6px;
+}
 
-    body[data-mode="qts"] #qtsModule.module.active {
-        margin-left: calc(var(--qts-left-rail-space, 0px) + var(--qts-left-clearance, 0px));
-        width: calc(100% - var(--qts-left-rail-space, 0px) - var(--qts-left-clearance, 0px));
-        padding-left: var(--qts-inline-gutter);
-        padding-right: var(--qts-inline-gutter);
-    }
+#ambientRoot .category-selector {
+    position: static;
+    inset: auto;
+    display: grid;
+    grid-template-columns: repeat(3, minmax(0, 1fr));
+    gap: 8px;
+    padding: 2px 0 0;
+    background: transparent;
+    backdrop-filter: none;
+    animation: none;
+}
 
-    .qts-grid-shell {
-        padding: 1px 0.8mm 0.8mm;
-    }
+#ambientRoot .category-selector.hidden {
+    display: none;
+}
 
-    #qtsGrid {
-        padding: 1px;
-    }
+#ambientRoot .cat-item {
+    position: static;
+    width: 100%;
+    height: auto;
+    left: auto;
+    top: auto;
+    margin: 0;
+    padding: 10px 12px;
+    border-radius: var(--ambient-control-radius);
+    transform: none !important;
+    font-size: 0.76rem;
+    font-weight: 700;
+    letter-spacing: 0.08em;
+    text-transform: uppercase;
+    box-shadow: var(--ambient-control-shadow);
+    transition: transform 0.18s ease, filter 0.18s ease, background 0.18s ease, border-color 0.18s ease;
+    border: 1px solid var(--ambient-control-border);
+    background: var(--ambient-control-surface);
+    color: var(--ink);
+    clip-path: none;
+}
 
-    #qtsGrid > div {
-        min-height: 30px;
-    }
+#ambientRoot .cat-item:hover {
+    width: 100%;
+    height: auto;
+    margin: 0;
+    color: inherit;
+    transform: translateY(-1px) !important;
+    background: var(--ambient-control-hover);
+    border-color: var(--ambient-control-border-strong);
+}
 
-    #qtsGrid div[contenteditable="true"] {
-        padding: 3px 5px;
-        font-size: 0.78rem;
-        line-height: 1.02;
-    }
+#ambientRoot .cat-item::after {
+    display: none;
+}
 
-    .qts-header {
-        min-height: 40px;
-        padding: 3px 5px 2px;
-    }
+#ambientRoot .ambient-list {
+    flex: 1;
+    overflow-y: auto;
+    padding-right: 6px;
+    overscroll-behavior: contain;
+    scrollbar-width: thin;
+    scrollbar-color: rgba(106, 136, 181, 0.54) transparent;
+}
 
-    .qts-time {
-        min-height: 30px;
-    }
+#ambientRoot .ambient-list::-webkit-scrollbar {
+    width: 8px;
+}
 
-    .qts-header-day {
-        font-size: 0.76rem;
-    }
+#ambientRoot .ambient-list::-webkit-scrollbar-track {
+    background: transparent;
+}
 
-    .qts-header-date {
-        font-size: 0.56rem;
-    }
+#ambientRoot .ambient-list::-webkit-scrollbar-thumb {
+    border-radius: 999px;
+    background: linear-gradient(180deg, rgba(137, 164, 205, 0.72), rgba(79, 132, 227, 0.68));
+}
 
-    .qts-interval {
-        min-height: 13px !important;
-        font-size: 0.72rem;
-    }
+#ambientRoot .ambient-item {
+    position: relative;
+    display: grid;
+    grid-template-columns: auto 80px minmax(0, 1fr);
+    gap: 12px;
+    align-items: center;
+    padding: 10px;
+    border-radius: var(--ambient-card-radius) !important;
+    background: var(--ambient-card-surface);
+    border: 1px solid var(--ambient-control-border);
+    box-shadow: var(--ambient-card-shadow);
+    margin-bottom: 8px;
+    transition: transform 0.18s ease, background 0.18s ease, border-color 0.18s ease, box-shadow 0.18s ease;
+    clip-path: none;
 }
 
-@media (max-width: 1220px) and (min-width: 761px) {
-    body[data-mode="qts"] #qtsModule {
-        --qts-safe-inline-size: min(100%, calc(100vw - 176px));
-        --qts-inline-gutter: 3px;
-        --qts-right-tools-space: 58px;
-        --qts-content-max: none;
-    }
+#ambientRoot .ambient-item:hover {
+    background: var(--ambient-card-hover);
+    transform: translateY(-1px);
+    border-color: var(--ambient-control-border-strong);
+    box-shadow: 0 18px 28px rgba(44, 58, 78, 0.12);
+}
 
-    body[data-mode="qts"] {
-        --qts-detached-rail-width: 100px;
-        --qts-detached-rail-gap: 2px;
-    }
+#ambientRoot .ambient-item.cursor {
+    border-color: var(--ambient-control-border-strong);
+    box-shadow:
+        0 0 0 1px rgba(97, 137, 199, 0.16),
+        var(--ambient-card-shadow);
+}
 
-    .qts-detached-index {
-        top: 112px;
-        left: 10px;
-        gap: 8px;
-    }
+#ambientRoot .ambient-item.playing {
+    background: var(--ambient-card-active);
+    border-color: rgba(87, 131, 201, 0.46);
+    box-shadow:
+        0 18px 30px rgba(79, 132, 227, 0.14),
+        inset 0 1px 0 rgba(255, 255, 255, 0.34);
+}
 
-    body[data-mode="qts"] #qtsModule.module.active {
-        margin-left: calc(var(--qts-left-rail-space, 0px) + var(--qts-left-clearance, 0px));
-        width: calc(100% - var(--qts-left-rail-space, 0px) - var(--qts-left-clearance, 0px));
-        padding-left: var(--qts-inline-gutter);
-        padding-right: var(--qts-inline-gutter);
-    }
+#ambientRoot .ambient-item.playing::after {
+    content: "";
+    width: 10px;
+    height: 10px;
+    border-radius: 999px;
+    background: var(--ambient-accent-strong);
+    position: absolute;
+    top: 12px;
+    right: 12px;
+    box-shadow: 0 0 0 5px var(--ambient-accent-soft);
+}
 
-    .qts-grid-shell {
-        padding: 1px 0.7mm 0.7mm;
-    }
+#ambientRoot .ambient-item img {
+    width: 80px;
+    height: 48px;
+    border-radius: calc(var(--ambient-card-radius) - 4px);
+    object-fit: cover;
+    clip-path: none;
+    box-shadow: 0 8px 16px rgba(18, 29, 46, 0.14);
+}
 
-    body[data-mode="qts"] #qtsModule .qts-week-rail,
-    body[data-mode="qts"] #qtsModule .qts-grid-shell,
-    body[data-mode="qts"] #qtsModule .qts-controls,
-    body[data-mode="qts"] #qtsModule .qts-subject-summary {
-        width: var(--qts-safe-inline-size);
-        max-width: var(--qts-safe-inline-size);
-        justify-self: center;
-    }
+#ambientRoot .ambient-item-copy {
+    min-width: 0;
+    display: grid;
+    gap: 3px;
+    color: var(--ink);
+}
 
-    #qtsGrid {
-        padding: 0;
-    }
+#ambientRoot .ambient-title {
+    font-size: 0.95rem;
+    font-weight: 700;
+    line-height: 1.24;
+    color: #243246;
+}
 
-    #qtsGrid > div {
-        min-height: 28px;
-    }
+#ambientRoot .ambient-item-meta {
+    color: #6880a3;
+}
 
-    #qtsGrid div[contenteditable="true"] {
-        padding: 3px 4px;
-        font-size: 0.74rem;
-        line-height: 1;
-    }
+#ambientRoot .ambient-item.playing .ambient-title {
+    color: var(--ambient-accent-strong);
+}
 
-    .qts-header {
-        min-height: 38px;
-        padding: 3px 4px 2px;
-    }
+#ambientRoot .ambient-item.playing .ambient-item-meta {
+    color: #496ea9;
+}
 
-    .qts-time {
-        min-height: 28px;
-    }
+#ambientRoot .fav-btn {
+    width: 34px;
+    height: 34px;
+    padding: 0;
+    border-radius: var(--ambient-control-radius);
+    border: 1px solid var(--ambient-control-border);
+    background: var(--ambient-control-surface);
+    color: var(--muted);
+    box-shadow: var(--ambient-control-shadow);
+    cursor: pointer;
+    clip-path: none;
+    transition: transform 0.18s ease, background 0.18s ease, border-color 0.18s ease, color 0.18s ease;
+}
 
-    .qts-header-day {
-        font-size: 0.72rem;
-    }
+#ambientRoot .fav-btn .ambient-icon {
+    width: 14px;
+    height: 14px;
+}
 
-    .qts-header-date {
-        font-size: 0.54rem;
-    }
+#ambientRoot .fav-btn:hover {
+    transform: translateY(-1px);
+    background: var(--ambient-control-hover);
+    border-color: var(--ambient-control-border-strong);
+    color: var(--ambient-accent-strong);
+}
 
-    .qts-interval {
-        min-height: 12px !important;
-        font-size: 0.68rem;
-    }
+#ambientRoot .fav-btn.active {
+    background: var(--ambient-card-active);
+    color: var(--ambient-accent-strong);
+    border-color: rgba(87, 131, 201, 0.44);
 }
 
-@media (max-width: 1080px) and (min-width: 761px) {
-    body[data-mode="qts"] #qtsModule {
-        --qts-inline-gutter: 2px;
-        --qts-right-tools-space: 56px;
-    }
+#ambientRoot .ambient-empty {
+    padding: 18px;
+    border-radius: var(--ambient-card-radius);
+    background: var(--ambient-card-surface);
+    border: 1px solid var(--ambient-control-border);
+    color: var(--muted);
+    text-align: center;
+    clip-path: none;
+}
 
-    body[data-mode="qts"] {
-        --qts-detached-rail-width: 92px;
-        --qts-detached-rail-gap: 1px;
-    }
+#ambientHelp,
+#ambientBlocked {
+    position: fixed;
+    inset: 0;
+    display: flex;
+    align-items: center;
+    justify-content: center;
+    padding: 20px;
+    background: rgba(19, 24, 34, 0.36);
+    backdrop-filter: blur(10px);
+    z-index: 2200;
+}
+
+.ambient-help-box,
+.ambient-blocked-box,
+#ambientRoot .ambient-help-box {
+    background: var(--ambient-panel-surface);
+    color: var(--ink);
+    border: 1px solid var(--ambient-control-border);
+    border-radius: var(--ambient-shell-radius);
+    box-shadow: 0 24px 38px rgba(18, 28, 44, 0.16);
+    width: min(460px, calc(100vw - 28px));
+    max-height: min(78vh, 640px);
+    overflow: auto;
+    position: relative;
+    padding: 24px 22px 22px;
+    clip-path: none;
+}
 
-    body[data-mode="qts"] .side-modules {
-        right: 2px;
-        width: auto;
-        padding: 0;
-        background: transparent;
-        backdrop-filter: none;
-        border: 0;
-        box-shadow: none;
-        clip-path: none;
-        transform: translateY(-50%);
-        transition: opacity 0.18s ease, right 0.18s ease;
-    }
+#ambientHelpClose,
+#ambientBlockedClose,
+#ambientRoot #ambientHelpClose {
+    color: var(--ink);
+    position: absolute;
+    top: 12px;
+    right: 12px;
+    width: 36px;
+    height: 36px;
+    padding: 0;
+    border-radius: var(--ambient-control-radius);
+    border: 1px solid var(--ambient-control-border);
+    background: var(--ambient-control-surface);
+    box-shadow: var(--ambient-control-shadow);
+    clip-path: none;
+    cursor: pointer;
+}
 
-    body[data-mode="qts"] .side-modules::after {
-        display: none;
-    }
+#ambientHelpClose .ambient-icon,
+#ambientBlockedClose .ambient-icon,
+#ambientRoot #ambientHelpClose .ambient-icon {
+    width: 16px;
+    height: 16px;
+}
 
-    body[data-mode="qts"] .side-group {
-        gap: 6px;
-    }
+#ambientHelpClose:hover,
+#ambientBlockedClose:hover,
+#ambientRoot #ambientHelpClose:hover {
+    transform: translateY(-1px);
+    background: var(--ambient-control-hover);
+    border-color: var(--ambient-control-border-strong);
+}
 
-    body[data-mode="qts"] .side-group-buttons {
-        gap: 6px;
-    }
+.ambient-help-box h3,
+.ambient-blocked-header h3 {
+    margin: 0 0 8px;
+    font-size: 1.1rem;
+    font-weight: 800;
+    letter-spacing: -0.03em;
+}
 
-    body[data-mode="qts"] .side-group[data-nav-group="utility"] {
-        padding-top: 6px;
-        border-top: 0;
-    }
+.ambient-help-box div,
+.ambient-blocked-header p {
+    color: var(--muted);
+}
 
-    body[data-mode="qts"] .side-modules .icon-circle,
-    body[data-mode="qts"] .side-group[data-nav-group="primary"] .icon-circle,
-    body[data-mode="qts"] .side-group[data-nav-group="utility"] .icon-circle {
-        width: 40px;
-        height: 40px;
-    }
+.ambient-blocked-kicker {
+    margin-bottom: 8px;
+    font-size: 0.72rem;
+    font-weight: 800;
+    letter-spacing: 0.14em;
+    text-transform: uppercase;
+    color: var(--muted);
+}
 
-    body[data-mode="qts"] #qtsModule .qts-stage-main {
-        justify-items: center;
-    }
+.ambient-blocked-header p {
+    margin: 0;
+    line-height: 1.55;
+}
 
-    body[data-mode="qts"] #qtsModule .qts-template-strip,
-    body[data-mode="qts"] #qtsModule .qts-title {
-        justify-self: center;
-        max-width: calc(100% - 12px);
-    }
+.ambient-blocked-list {
+    margin-top: 16px;
+    display: grid;
+    gap: 10px;
+}
 
-    body[data-mode="qts"] #qtsModule .qts-grid-shell,
-    body[data-mode="qts"] #qtsModule .qts-controls,
-    body[data-mode="qts"] #qtsModule .qts-subject-summary {
-        width: var(--qts-safe-inline-size);
-        max-width: var(--qts-safe-inline-size);
-        justify-self: center;
-    }
+.ambient-blocked-item,
+.ambient-blocked-empty {
+    padding: 14px 15px;
+    border-radius: var(--ambient-card-radius);
+    background: var(--ambient-card-surface);
+    border: 1px solid var(--ambient-control-border);
+}
 
-    .qts-detached-block {
-        gap: 6px;
-        padding: 6px 4px 4px;
-    }
+.ambient-blocked-meta {
+    margin-bottom: 4px;
+    font-size: 0.7rem;
+    font-weight: 800;
+    letter-spacing: 0.14em;
+    text-transform: uppercase;
+    color: var(--muted);
+}
 
-    .qts-detached-block-label {
-        font-size: 0.5rem;
-        letter-spacing: 0.16em;
-    }
+.ambient-blocked-title {
+    font-size: 0.98rem;
+    font-weight: 700;
+    line-height: 1.35;
+}
 
-    .qts-detached-week-button,
-    .qts-detached-toggle,
-    .qts-detached-summary-button {
-        min-height: 32px;
-        padding: 5px 6px;
-        font-size: 0.58rem;
-    }
+.ambient-blocked-id {
+    margin-top: 8px;
+    font-family: "Space Grotesk", sans-serif;
+    font-size: 0.8rem;
+    letter-spacing: 0.04em;
+    color: var(--muted);
+    word-break: break-all;
 }
 
-@media (max-width: 980px) and (min-width: 761px) {
-    body[data-mode="qts"] #qtsModule {
-        --qts-inline-gutter: 1px;
-        --qts-right-tools-space: 54px;
-    }
+body.dark .ambient-blocked-item,
+body.dark .ambient-blocked-empty {
+    background: var(--ambient-card-surface);
+    border-color: var(--ambient-control-border);
+}
 
-    body[data-mode="qts"] {
-        --qts-detached-rail-width: 86px;
-        --qts-detached-rail-gap: 1px;
-    }
+body.dark #ambientRoot .ambient-item,
+body.dark #ambientRoot .fav-btn {
+    background: var(--ambient-card-surface);
+    border-color: var(--ambient-control-border);
+}
 
-    .qts-template-strip {
-        gap: 8px;
-        margin-bottom: 12px;
-    }
+body.dark #ambientRoot .ambient-item:hover {
+    background: var(--ambient-card-hover);
+    border-color: var(--ambient-control-border-strong);
+}
 
-    .qts-template-strip::after {
-        width: min(100%, 440px);
-    }
+body.dark #ambientRoot .ambient-panel {
+    background: var(--ambient-panel-surface) !important;
+}
 
-    .qts-template-label {
-        min-height: 32px;
-        padding: 0 14px;
-        font-size: 0.64rem;
-        letter-spacing: 0.12em;
-    }
+body.dark #ambientRoot .ambient-title {
+    color: #f2f7ff;
+}
 
-    .qts-templates {
-        gap: 6px;
-    }
+body.dark #ambientRoot .ambient-item-meta {
+    color: rgba(210, 223, 247, 0.7);
+}
 
-    .qts-templates button {
-        min-height: 32px;
-        padding: 0 10px;
-        font-size: 0.74rem;
-    }
+body.dark #ambientRoot .ambient-item.playing {
+    background: var(--ambient-card-active);
+    border-color: var(--ambient-control-border-strong);
+    box-shadow:
+        0 20px 34px rgba(0, 0, 0, 0.26),
+        inset 0 1px 0 rgba(255, 255, 255, 0.08);
+}
 
-    .qts-title {
-        padding: 7px 16px;
-        font-size: clamp(1.16rem, 2vw, 1.42rem);
-    }
+body.dark #ambientRoot .ambient-item.playing .ambient-title {
+    color: #f8fbff;
+}
 
-    .qts-grid-shell {
-        padding: 2px 4px 5px;
-        background: linear-gradient(180deg, rgba(255, 255, 255, 0.97), rgba(248, 247, 243, 0.95));
-        box-shadow: 0 12px 24px rgba(15, 23, 42, 0.07);
-    }
+body.dark #ambientRoot .ambient-mini-icon,
+body.dark #ambientRoot .ambient-primary-control,
+body.dark #ambientRoot .ambient-secondary-control,
+body.dark #ambientRoot .ambient-header-actions button {
+    background: var(--ambient-control-surface);
+    color: var(--ink);
+    border-color: var(--ambient-control-border);
+}
 
-    #qtsGrid {
-        background: linear-gradient(180deg, rgba(255, 255, 255, 0.98), rgba(248, 247, 243, 0.96));
-    }
+body.dark #ambientRoot .ambient-peek {
+    background:
+        radial-gradient(circle at 38% 30%, rgba(125, 214, 255, 0.32), transparent 46%),
+        linear-gradient(145deg, rgba(12, 23, 40, 0.92), rgba(8, 14, 25, 0.86));
+    color: #eef9ff;
+}
 
-    .qts-controls {
-        gap: 12px;
-    }
+body.focus-mode #ambientRoot {
+    display: block !important;
+}
 
-    .row-control {
-        min-width: 132px;
-        padding: 5px 8px;
-        grid-template-columns: 26px 1fr 26px;
-        gap: 6px;
-    }
+body.focus-mode .topbar,
+body.focus-mode .footer,
+body.focus-mode .side-modules,
+body.focus-mode #moduleArea,
+body.focus-mode #utilityRoot,
+body.focus-mode #miniAnalogClock,
+body.focus-mode #customModal {
+    display: none !important;
+}
 
-    .row-btn {
-        width: 26px;
-        height: 22px;
-    }
+body.focus-mode #ambientRoot .ambient-peek {
+    left: 22px;
+    bottom: 22px;
+    width: 58px;
+    min-width: 58px;
+    height: 58px;
+    padding: 0;
+    border-radius: 999px;
+    background:
+        radial-gradient(circle at 38% 30%, rgba(125, 214, 255, 0.32), transparent 46%),
+        linear-gradient(145deg, rgba(12, 23, 40, 0.92), rgba(8, 14, 25, 0.86));
+    border-color: rgba(125, 214, 255, 0.26);
+    box-shadow:
+        0 18px 38px rgba(0, 0, 0, 0.32),
+        inset 0 1px 0 rgba(255, 255, 255, 0.12);
+    gap: 0;
+}
 
-    .row-label {
-        font-size: 0.54rem;
-        letter-spacing: 0.13em;
-    }
+body.focus-mode #ambientRoot .ambient-peek.is-visible {
+    transform: none;
+}
 
-    .qts-detached-block {
-        gap: 5px;
-        padding: 6px 4px 4px;
-    }
+body.focus-mode #ambientRoot .ambient-peek-label {
+    display: none;
+    font-size: 0.74rem;
+    letter-spacing: 0.14em;
+    color: rgba(243, 247, 255, 0.86);
+}
 
-    .qts-detached-block-label {
-        font-size: 0.5rem;
-        letter-spacing: 0.16em;
-    }
+body.focus-mode #ambientRoot .ambient-peek-dot {
+    width: 16px;
+    height: 16px;
+    background: #79d5ff;
+    box-shadow:
+        0 0 0 8px rgba(121, 213, 255, 0.1),
+        0 0 28px rgba(121, 213, 255, 0.42);
+}
 
-    .qts-detached-week-button,
-    .qts-detached-toggle,
-    .qts-detached-summary-button {
-        min-height: 30px;
-        padding: 4px 5px;
-        font-size: 0.56rem;
-    }
+body.focus-mode #ambientRoot .ambient-peek.is-playing .ambient-peek-dot {
+    background: #4fe3b3;
+    box-shadow:
+        0 0 0 8px rgba(79, 227, 179, 0.1),
+        0 0 30px rgba(79, 227, 179, 0.5);
+}
 
-    #qtsGrid > div {
-        color: #202632;
-    }
+body.focus-mode #ambientRoot .ambient-mini,
+body.focus-mode #ambientRoot .ambient-panel {
+    background:
+        linear-gradient(180deg, rgba(14, 21, 33, 0.84), rgba(9, 14, 23, 0.74)) !important;
+    border-color: rgba(122, 146, 189, 0.18) !important;
+    color: #f3f7ff !important;
+}
 
-    #qtsGrid div[contenteditable="true"] {
-        font-size: 0.7rem;
-    }
+body.focus-mode[data-period="morning"] #ambientRoot .ambient-mini,
+body.focus-mode[data-period="morning"] #ambientRoot .ambient-panel {
+    background:
+        linear-gradient(180deg, rgba(69, 56, 44, 0.54), rgba(41, 33, 26, 0.48)) !important;
+    border-color: rgba(241, 212, 179, 0.18) !important;
+}
 
-    .qts-header {
-        background: linear-gradient(180deg, rgba(222, 229, 244, 0.98), rgba(214, 223, 241, 0.96));
-    }
+body.focus-mode[data-period="afternoon"] #ambientRoot .ambient-mini,
+body.focus-mode[data-period="afternoon"] #ambientRoot .ambient-panel {
+    background:
+        linear-gradient(180deg, rgba(71, 46, 42, 0.56), rgba(40, 26, 25, 0.5)) !important;
+    border-color: rgba(235, 187, 173, 0.18) !important;
+}
 
-    .qts-time {
-        background: linear-gradient(180deg, rgba(236, 244, 255, 0.98), rgba(225, 237, 252, 0.96));
-    }
+body.focus-mode[data-period="morning"] #ambientRoot .ambient-peek {
+    background:
+        radial-gradient(circle at 38% 30%, rgba(125, 214, 255, 0.32), transparent 46%),
+        linear-gradient(145deg, rgba(12, 23, 40, 0.92), rgba(8, 14, 25, 0.86));
+    border-color: rgba(125, 214, 255, 0.26);
+}
 
-    .qts-interval {
-        background: linear-gradient(90deg, rgba(218, 240, 230, 0.92), rgba(227, 245, 237, 0.94));
-    }
+body.focus-mode[data-period="afternoon"] #ambientRoot .ambient-peek {
+    background:
+        radial-gradient(circle at 38% 30%, rgba(125, 214, 255, 0.32), transparent 46%),
+        linear-gradient(145deg, rgba(12, 23, 40, 0.92), rgba(8, 14, 25, 0.86));
+    border-color: rgba(125, 214, 255, 0.26);
 }
 
-.qts-week-rail {
-    width: min(100%, 1180px);
-    margin: 0 0 10px;
-    display: grid;
-    grid-template-columns: repeat(3, minmax(0, 1fr));
-    gap: 10px;
-    align-items: stretch;
+body.focus-mode[data-period="night"] #ambientRoot .ambient-peek {
+    background:
+        radial-gradient(circle at 38% 30%, rgba(125, 214, 255, 0.32), transparent 46%),
+        linear-gradient(145deg, rgba(12, 23, 40, 0.92), rgba(8, 14, 25, 0.86));
+    border-color: rgba(125, 214, 255, 0.26);
 }
 
-.qts-week-context {
-    width: 100%;
-    margin: 0;
-    padding: 10px 14px;
-    display: grid;
-    gap: 1px;
-    justify-items: center;
-    position: relative;
-    overflow: hidden;
-    border-radius: 0;
-    background: var(--ui-clickable-surface);
-    border: 1px solid var(--ui-clickable-border);
-    box-shadow: var(--ui-clickable-shadow);
-    text-align: center;
-    transition: transform 0.18s ease, box-shadow 0.18s ease, border-color 0.18s ease, background 0.18s ease;
-    clip-path: polygon(var(--ui-cut-md) 0, calc(100% - var(--ui-cut-md)) 0, 100% var(--ui-cut-md), 100% calc(100% - var(--ui-cut-md)), calc(100% - var(--ui-cut-md)) 100%, var(--ui-cut-md) 100%, 0 calc(100% - var(--ui-cut-md)), 0 var(--ui-cut-md));
+body.pomodoro-table-sync-on .topbar,
+body.pomodoro-table-sync-on .footer,
+body.pomodoro-table-sync-on .side-modules,
+body.pomodoro-table-sync-on #ambientRoot,
+body.pomodoro-table-sync-on #miniAnalogClock,
+body.pomodoro-table-sync-on #pomodoroPresets,
+body.pomodoro-table-sync-on #cycleProgress,
+body.pomodoro-table-sync-on #progressiveBars,
+body.pomodoro-table-sync-on .controls,
+body.pomodoro-table-sync-on .main-stats-row,
+body.pomodoro-table-sync-on #streakVisual,
+body.pomodoro-table-sync-on #dailyGoal,
+body.pomodoro-table-sync-on #weeklyChart,
+body.pomodoro-table-sync-on #sessionLog,
+body.pomodoro-table-sync-on #advancedStats,
+body.pomodoro-table-sync-on #achievements,
+body.pomodoro-table-sync-on #homeLaunchpad {
+    opacity: 0.26;
+    filter: saturate(0.72);
+    transition: opacity 0.2s ease, filter 0.2s ease;
 }
 
-.qts-week-context::before {
-    content: "";
-    position: absolute;
-    top: 0;
-    left: var(--ui-cut-md);
-    right: var(--ui-cut-md);
-    height: 1px;
-    background: var(--ui-clickable-line);
-    pointer-events: none;
+body.pomodoro-table-sync-on .date-display,
+body.pomodoro-table-sync-on .time-display,
+body.pomodoro-table-sync-on #todayFlow {
+    position: relative;
+    z-index: 3;
+    opacity: 1;
+    filter: none;
 }
 
-button.qts-week-context {
-    appearance: none;
-    cursor: pointer;
-    font: inherit;
-    color: inherit;
+body.pomodoro-table-sync-on .date-display {
+    margin-bottom: -2px;
 }
 
-.qts-week-context:hover {
-    transform: translateY(-1px);
+body.pomodoro-table-sync-on .time-display {
+    transform: scale(1.012);
+    box-shadow: 0 26px 54px rgba(183, 215, 240, 0.16);
+    border-color: rgba(183, 215, 240, 0.22);
 }
 
-.qts-week-context.is-active {
-    border-color: rgba(159, 197, 230, 0.44);
-    background: linear-gradient(180deg, rgba(183, 215, 240, 0.38), rgba(255, 255, 255, 0.94));
-    box-shadow:
-        0 20px 36px rgba(159, 197, 230, 0.16),
-        inset 0 0 0 1px rgba(159, 197, 230, 0.16);
+body.pomodoro-table-sync-on #todayFlow {
+    margin-top: -20px;
 }
 
-.qts-week-context-future {
+body.pomodoro-table-sync-on .today-flow-shell.is-linked {
     position: relative;
-    overflow: hidden;
+    padding: 18px 20px 20px;
+    border-radius: 0;
+    background: var(--ui-informative-surface);
+    border-color: transparent;
+    box-shadow: 0 22px 46px rgba(183, 215, 240, 0.12);
+    clip-path: polygon(var(--ui-cut-lg) 0, calc(100% - var(--ui-cut-lg)) 0, 100% var(--ui-cut-lg), 100% calc(100% - var(--ui-cut-lg)), calc(100% - var(--ui-cut-lg)) 100%, var(--ui-cut-lg) 100%, 0 calc(100% - var(--ui-cut-lg)), 0 var(--ui-cut-lg));
 }
 
-.qts-week-context-future::after {
+body.pomodoro-table-sync-on .today-flow-shell.is-linked::before {
     content: "";
     position: absolute;
-    inset: auto -18% 16px;
-    height: 10px;
-    background: rgba(230, 193, 182, 0.28);
-    transform: rotate(-6deg);
+    inset: 1px 1px auto;
+    height: 34%;
+    border-radius: 0;
+    background: linear-gradient(180deg, rgba(255, 255, 255, 0.24), rgba(255, 255, 255, 0));
+    pointer-events: none;
 }
 
-.qts-week-context-future.is-active {
-    border-color: rgba(230, 193, 182, 0.44);
-    box-shadow:
-        0 20px 40px rgba(230, 193, 182, 0.18),
-        inset 0 0 0 1px rgba(230, 193, 182, 0.16);
+body.pomodoro-table-sync-on .today-flow-shell.is-linked .today-flow-track.is-live .today-flow-slot-card.is-current {
+    box-shadow: 0 18px 34px rgba(183, 215, 240, 0.14);
 }
 
-.qts-week-kicker,
-.qts-summary-kicker {
-    font-size: 0.62rem;
-    font-weight: 800;
-    letter-spacing: 0.18em;
-    text-transform: uppercase;
-    color: var(--muted);
+body.dark.pomodoro-table-sync-on .time-display {
+    box-shadow: 0 28px 62px rgba(7, 14, 26, 0.42);
 }
 
-.qts-week-context strong {
-    font-size: 0.94rem;
-    font-weight: 800;
+body.dark.pomodoro-table-sync-on .today-flow-shell.is-linked {
+    background:
+        linear-gradient(180deg, rgba(16, 26, 40, 0.92), rgba(11, 18, 31, 0.88));
+    border-color: rgba(108, 146, 232, 0.18);
+    box-shadow: 0 26px 52px rgba(0, 0, 0, 0.34);
 }
 
-.qts-week-context span:last-child {
-    font-size: 0.72rem;
-    color: var(--muted);
-    text-transform: capitalize;
+body.dark[data-style="immersion"].pomodoro-table-sync-on .today-flow-shell.is-linked {
+    background:
+        linear-gradient(180deg, rgba(15, 31, 50, 0.94), rgba(9, 20, 35, 0.9));
+    border-color: rgba(106, 150, 245, 0.18);
 }
 
-body.dark #qtsGrid {
-    background: var(--ui-informative-surface);
-    border-color: transparent;
+body.dark[data-style="pulse"].pomodoro-table-sync-on .today-flow-shell.is-linked {
+    background:
+        linear-gradient(180deg, rgba(39, 23, 28, 0.94), rgba(24, 16, 20, 0.9));
+    border-color: rgba(213, 138, 112, 0.16);
 }
 
-body.dark .qts-week-context {
-    background: var(--ui-clickable-surface);
-    border-color: var(--ui-clickable-border);
+body.focus-mode #ambientRoot .ambient-mini-icon,
+body.focus-mode #ambientRoot .ambient-secondary-control,
+body.focus-mode #ambientRoot .ambient-header-actions button {
+    background: var(--ambient-control-surface);
+    border-color: var(--ambient-control-border);
+    color: #f3f7ff;
 }
 
-.qts-grid-shell {
-    position: relative;
-    width: var(--qts-safe-inline-size);
-    max-width: var(--qts-safe-inline-size);
-    justify-self: center;
-    margin: 0;
-    padding: 1px 2px 3px;
-    border-radius: 0;
-    background: var(--ui-informative-surface);
-    border: 1px solid transparent;
-    box-shadow: var(--ui-informative-shadow);
-    clip-path: polygon(var(--ui-cut-lg) 0, calc(100% - var(--ui-cut-lg)) 0, 100% var(--ui-cut-lg), 100% calc(100% - var(--ui-cut-lg)), calc(100% - var(--ui-cut-lg)) 100%, var(--ui-cut-lg) 100%, 0 calc(100% - var(--ui-cut-lg)), 0 var(--ui-cut-lg));
+body.focus-mode #ambientRoot .ambient-title,
+body.focus-mode #ambientRoot .ambient-panel-title,
+body.focus-mode #ambientRoot .ambient-header-copy,
+body.focus-mode #ambientRoot .ambient-item-copy,
+body.focus-mode #ambientRoot .ambient-item-meta {
+    color: #f3f7ff;
 }
 
-.qts-week-overlay {
-    position: absolute;
-    z-index: 6;
-    pointer-events: none;
-    font-size: 0.86rem;
-    font-weight: 800;
-    letter-spacing: 0.22em;
-    text-transform: uppercase;
+body.focus-mode #ambientRoot .ambient-item {
+    background: var(--ambient-card-surface);
+    border-color: var(--ambient-control-border);
 }
 
-.qts-week-overlay-future {
-    top: 46%;
-    left: -6%;
-    right: -6%;
-    padding: 14px 0;
-    text-align: center;
-    color: rgba(134, 45, 16, 0.92);
-    background: rgba(243, 108, 79, 0.2);
-    border-top: 1px solid rgba(243, 108, 79, 0.3);
-    border-bottom: 1px solid rgba(243, 108, 79, 0.3);
-    transform: rotate(-8deg);
+body.focus-mode #ambientRoot .ambient-item.playing {
+    background: var(--ambient-card-active);
+    border-color: var(--ambient-control-border-strong);
+}
+
+body[data-mode="clock"] #ambientRoot .ambient-mini {
+    width: min(clamp(232px, 21vw, 320px), calc(100vw - 32px));
+}
+
+#shortcutHint {
+    position: fixed;
+    right: 22px;
+    bottom: 24px;
+    z-index: 1600;
+    width: min(420px, calc(100vw - 28px));
+    padding: 12px 14px;
+    border-radius: 18px;
+    border: 1px solid var(--line);
+    background: var(--glass-strong);
+    color: var(--ink);
+    box-shadow: var(--shadow-soft);
+    backdrop-filter: blur(18px);
+    opacity: 0;
+    pointer-events: none;
+    transform: translateY(10px);
+    transition: opacity 0.22s ease, transform 0.22s ease;
 }
 
-.qts-week-overlay-readonly {
-    top: 14px;
-    right: 18px;
-    padding: 8px 12px;
-    border-radius: 0;
-    color: rgba(73, 85, 105, 0.92);
-    background: var(--ui-informative-surface);
-    border: 1px solid transparent;
-    letter-spacing: 0.12em;
-    clip-path: polygon(var(--ui-cut-sm) 0, calc(100% - var(--ui-cut-sm)) 0, 100% var(--ui-cut-sm), 100% calc(100% - var(--ui-cut-sm)), calc(100% - var(--ui-cut-sm)) 100%, var(--ui-cut-sm) 100%, 0 calc(100% - var(--ui-cut-sm)), 0 var(--ui-cut-sm));
+#shortcutHint.is-visible {
+    opacity: 1;
+    pointer-events: auto;
+    transform: translateY(0);
 }
 
-body.dark .qts-week-overlay-readonly {
-    color: rgba(226, 232, 240, 0.9);
-    background: rgba(15, 23, 42, 0.72);
-    border-color: rgba(255, 255, 255, 0.08);
+.shortcut-hint-eyebrow {
+    margin-bottom: 8px;
+    font-size: 0.68rem;
+    font-weight: 800;
+    letter-spacing: 0.14em;
+    text-transform: uppercase;
+    color: var(--muted);
 }
 
-body.dark .qts-week-overlay-future {
-    color: rgba(255, 237, 213, 0.94);
-    background: rgba(243, 108, 79, 0.22);
-    border-top-color: rgba(243, 108, 79, 0.28);
-    border-bottom-color: rgba(243, 108, 79, 0.28);
+.shortcut-hint-row {
+    display: flex;
+    flex-wrap: wrap;
+    gap: 8px;
 }
 
-#qtsGrid > div {
-    min-height: 32px;
-    border-radius: 0;
-    overflow: hidden;
+.shortcut-hint-item {
+    display: inline-flex;
+    align-items: center;
+    gap: 8px;
+    min-height: 38px;
+    padding: 0 12px;
+    border-radius: 999px;
+    background: rgba(255, 255, 255, 0.64);
+    border: 1px solid rgba(255, 255, 255, 0.46);
+    color: var(--ink);
 }
 
-#qtsGrid div[contenteditable="true"] {
-    display: flex;
-    align-items: center;
-    justify-content: flex-start;
-    padding: 4px 6px;
-    text-align: left;
-    font-size: 0.84rem;
-    line-height: 1.08;
-    overflow-wrap: normal;
-    word-break: keep-all;
-    white-space: nowrap;
-    text-overflow: ellipsis;
+.shortcut-hint-item span {
+    font-size: 0.82rem;
+    font-weight: 700;
 }
 
-.qts-header {
-    background: rgba(37, 88, 240, 0.1);
-    color: var(--ink);
-    border-bottom: none;
-    display: grid;
-    gap: 0;
-    align-content: center;
-    min-height: 46px;
-    padding: 4px 6px 3px;
+.shortcut-hint-item kbd {
+    min-width: 52px;
+    padding: 6px 8px;
+    border-radius: 10px;
+    border: 1px solid rgba(42, 33, 53, 0.12);
+    background: rgba(255, 255, 255, 0.92);
+    box-shadow: inset 0 -1px 0 rgba(42, 33, 53, 0.06);
+    font-family: "Space Grotesk", sans-serif;
+    font-size: 0.74rem;
+    font-weight: 700;
     text-align: center;
 }
 
-.qts-time {
-    background: rgba(243, 108, 79, 0.1);
-    min-height: 32px;
+body.dark #shortcutHint {
+    background: rgba(15, 22, 34, 0.88);
+    border-color: rgba(255, 255, 255, 0.08);
+    color: var(--ink);
 }
 
-#qtsGrid .qts-time[contenteditable="true"] {
-    justify-content: center;
-    padding: 4px 5px;
-    font-size: 0.82rem;
-    white-space: nowrap;
-    overflow-wrap: normal;
-    word-break: normal;
+body.dark .shortcut-hint-item {
+    background: rgba(255, 255, 255, 0.06);
+    border-color: rgba(255, 255, 255, 0.08);
 }
 
-.qts-interval {
-    border-radius: 0 !important;
-    background: linear-gradient(90deg, rgba(30, 166, 114, 0.12), rgba(105, 206, 168, 0.12));
-    color: var(--muted);
-    min-height: 15px !important;
-    padding-top: 1px;
-    padding-bottom: 1px;
+body.dark .shortcut-hint-item kbd {
+    background: rgba(255, 255, 255, 0.08);
+    border-color: rgba(255, 255, 255, 0.08);
+    box-shadow: inset 0 -1px 0 rgba(255, 255, 255, 0.04);
+    color: var(--ink);
 }
 
-.qts-controls,
-.qts-templates {
-    flex-wrap: wrap;
-    justify-content: center;
-    gap: 8px;
-}
+@keyframes clockBreath {
+    0% {
+        transform: scale(1);
+        box-shadow: 0 36px 92px rgba(15, 20, 30, 0.22);
+    }
 
-.qts-template-strip {
-    width: fit-content;
-    max-width: 100%;
-    margin: 0 auto 14px;
-    display: grid;
-    gap: 10px;
-    justify-items: center;
-    position: relative;
-}
+    50% {
+        transform: scale(1.018);
+        box-shadow: 0 44px 116px rgba(15, 20, 30, 0.28);
+    }
 
-.qts-template-strip::after {
-    content: "";
-    width: min(100%, 520px);
-    height: 1px;
-    background: linear-gradient(90deg, rgba(154, 160, 172, 0), rgba(154, 160, 172, 0.22) 18%, rgba(154, 160, 172, 0.38) 50%, rgba(154, 160, 172, 0.22) 82%, rgba(154, 160, 172, 0));
-    opacity: 0.95;
+    100% {
+        transform: scale(1);
+        box-shadow: 0 36px 92px rgba(15, 20, 30, 0.22);
+    }
 }
 
-.qts-template-label {
-    min-height: 34px;
-    padding: 0 16px;
-    display: inline-flex;
-    align-items: center;
-    justify-content: center;
-    border: 1px solid rgba(195, 185, 174, 0.3);
-    background: linear-gradient(180deg, rgba(255, 255, 255, 0.76), rgba(249, 246, 240, 0.62));
-    color: var(--muted);
-    font-size: 0.7rem;
-    font-weight: 800;
-    letter-spacing: 0.14em;
-    text-transform: uppercase;
-    box-shadow: var(--ui-informative-shadow);
-    clip-path: polygon(var(--ui-cut-sm) 0, calc(100% - var(--ui-cut-sm)) 0, 100% var(--ui-cut-sm), 100% calc(100% - var(--ui-cut-sm)), calc(100% - var(--ui-cut-sm)) 100%, var(--ui-cut-sm) 100%, 0 calc(100% - var(--ui-cut-sm)), 0 var(--ui-cut-sm));
-    pointer-events: none;
-    user-select: none;
+body.focus-mode {
+    padding: 0 !important;
+    min-height: 100vh;
+    overflow: hidden !important;
 }
 
-.qts-templates button {
-    position: relative;
-    overflow: hidden;
-    border-radius: 0;
-    border: 1px solid var(--ui-clickable-border);
-    background: var(--ui-clickable-surface);
-    color: var(--ink);
-    font-weight: 700;
-    transition: transform 0.18s ease, box-shadow 0.18s ease, background 0.18s ease;
-    clip-path: polygon(var(--ui-cut-sm) 0, calc(100% - var(--ui-cut-sm)) 0, 100% var(--ui-cut-sm), 100% calc(100% - var(--ui-cut-sm)), calc(100% - var(--ui-cut-sm)) 100%, var(--ui-cut-sm) 100%, 0 calc(100% - var(--ui-cut-sm)), 0 var(--ui-cut-sm));
-    box-shadow: var(--ui-clickable-shadow);
-    cursor: pointer;
-    min-height: 34px;
-    padding: 0 14px;
-    font-size: 0.82rem;
+body.focus-mode {
+    background: linear-gradient(180deg, #eff4ff 0%, #e9edf7 100%) !important;
 }
 
-.qts-templates button::before {
-    content: "";
-    position: absolute;
-    top: 0;
-    left: var(--ui-cut-sm);
-    right: var(--ui-cut-sm);
-    height: 1px;
-    background: var(--ui-clickable-line);
-    pointer-events: none;
+body.focus-mode[data-period="morning"] {
+    background:
+        radial-gradient(circle at 14% 18%, rgba(255, 186, 114, 0.24), transparent 28%),
+        radial-gradient(circle at 86% 22%, rgba(117, 165, 255, 0.22), transparent 28%),
+        linear-gradient(180deg, #fff6ee 0%, #eef2fb 100%) !important;
 }
 
-#qtsModule .qts-templates button.is-active {
-    background: linear-gradient(180deg, rgba(183, 215, 240, 0.94), rgba(159, 197, 230, 0.88));
-    color: #1f2a33 !important;
-    border-color: rgba(159, 197, 230, 0.54) !important;
-    transform: translateY(-1px);
-    box-shadow:
-        0 16px 28px rgba(159, 197, 230, 0.18),
-        inset 0 0 0 1px rgba(255, 255, 255, 0.3) !important;
+body.focus-mode[data-period="afternoon"] {
+    background:
+        radial-gradient(circle at 14% 18%, rgba(243, 108, 79, 0.22), transparent 28%),
+        radial-gradient(circle at 86% 22%, rgba(255, 208, 138, 0.18), transparent 28%),
+        linear-gradient(180deg, #fff2eb 0%, #f2edf7 100%) !important;
 }
 
-body.dark .qts-templates button {
-    background: var(--ui-clickable-surface);
+body.focus-mode[data-period="night"] {
+    background:
+        radial-gradient(circle at 14% 18%, rgba(77, 125, 255, 0.26), transparent 28%),
+        radial-gradient(circle at 86% 20%, rgba(32, 83, 193, 0.18), transparent 32%),
+        linear-gradient(180deg, #08111f 0%, #0b1525 100%) !important;
 }
 
-body.dark #qtsModule .qts-templates button.is-active,
-#qtsModule .qts-templates button[aria-pressed="true"],
-#qtsModule .qts-templates button.is-active:hover,
-#qtsModule .qts-templates button[aria-pressed="true"]:hover {
-    background: linear-gradient(180deg, rgba(183, 215, 240, 0.94), rgba(159, 197, 230, 0.88));
-    color: #1f2a33 !important;
-    border-color: rgba(159, 197, 230, 0.54) !important;
-    box-shadow:
-        0 16px 28px rgba(159, 197, 230, 0.18),
-        inset 0 0 0 1px rgba(255, 255, 255, 0.3) !important;
+body.focus-mode::before,
+body.focus-mode::after {
+    opacity: 0.62;
 }
 
-.qts-title {
-    width: fit-content;
-    margin: 0 auto 2px;
-    padding: 7px 18px;
-    text-align: center;
-    font-size: clamp(1.24rem, 2.2vw, 1.6rem);
-    font-weight: 800;
-    letter-spacing: -0.04em;
-    border: 1px solid rgba(183, 215, 240, 0.4);
-    background: linear-gradient(180deg, rgba(255, 255, 255, 0.72), rgba(255, 255, 255, 0.42));
-    box-shadow: var(--ui-informative-shadow);
-    clip-path: polygon(var(--ui-cut-md) 0, calc(100% - var(--ui-cut-md)) 0, 100% var(--ui-cut-md), 100% calc(100% - var(--ui-cut-md)), calc(100% - var(--ui-cut-md)) 100%, var(--ui-cut-md) 100%, 0 calc(100% - var(--ui-cut-md)), 0 var(--ui-cut-md));
+body.focus-mode #hero {
+    position: fixed !important;
+    inset: 0 !important;
+    width: 100vw !important;
+    max-width: none !important;
+    height: 100vh !important;
+    min-height: 100vh !important;
+    margin: 0 !important;
+    padding: clamp(24px, 4vw, 42px) !important;
+    border-radius: 0 !important;
+    background: transparent !important;
+    border: none !important;
+    box-shadow: none !important;
+    display: grid !important;
+    grid-template-columns: minmax(0, 1fr) !important;
+    place-items: center !important;
+    justify-items: center !important;
+    align-items: center !important;
+    overflow: hidden !important;
 }
 
-body.dark .qts-template-label {
-    background: linear-gradient(180deg, rgba(24, 31, 46, 0.92), rgba(18, 25, 38, 0.8));
-    color: rgba(226, 232, 240, 0.78);
-    border-color: rgba(255, 255, 255, 0.08);
+body.focus-mode #hero::before,
+body.focus-mode #hero::after {
+    display: none !important;
 }
 
-.qts-toggle-bar {
-    display: flex;
-    flex-wrap: wrap;
-    justify-content: center;
-    gap: 8px;
-    margin-bottom: 8px;
+body.focus-mode #hero > :not(.time-display) {
+    display: none !important;
 }
 
-.qts-toggle {
-    display: inline-flex;
-    align-items: center;
-    gap: 8px;
-    min-height: 32px;
-    padding: 0 12px;
-    position: relative;
-    overflow: hidden;
-    border-radius: 0;
-    border: 1px solid var(--ui-clickable-border);
-    background: var(--ui-clickable-surface);
-    box-shadow: var(--ui-clickable-shadow);
-    clip-path: polygon(var(--ui-cut-sm) 0, calc(100% - var(--ui-cut-sm)) 0, 100% var(--ui-cut-sm), 100% calc(100% - var(--ui-cut-sm)), calc(100% - var(--ui-cut-sm)) 100%, var(--ui-cut-sm) 100%, 0 calc(100% - var(--ui-cut-sm)), 0 var(--ui-cut-sm));
-    cursor: pointer;
-    font-size: 0.78rem;
-    font-weight: 700;
+body.focus-mode .time-display {
+    grid-column: 1 / -1 !important;
+    grid-row: 1 !important;
+    justify-self: center !important;
+    align-self: center !important;
+    display: flex !important;
+    align-items: center !important;
+    justify-content: center !important;
+    width: min(82vw, 1040px) !important;
+    max-width: calc(100vw - 72px) !important;
+    min-height: clamp(220px, 34vh, 380px) !important;
+    margin: 0 auto !important;
+    padding: clamp(24px, 4.6vw, 52px) !important;
+    border-radius: 40px !important;
+    font-size: clamp(5rem, 14vw, 11.2rem) !important;
+    line-height: 0.9 !important;
+    letter-spacing: -0.1em !important;
+    text-align: center !important;
+    transform-origin: center !important;
+    overflow: hidden !important;
+    border-width: 1px !important;
+    animation: clockBreath 7s ease-in-out infinite !important;
+    box-shadow: 0 36px 92px rgba(15, 20, 30, 0.22) !important;
 }
 
-.qts-toggle::before {
-    content: "";
-    position: absolute;
-    top: 0;
-    left: var(--ui-cut-sm);
-    right: var(--ui-cut-sm);
-    height: 1px;
-    background: var(--ui-clickable-line);
-    pointer-events: none;
+body.focus-mode .time-display::before {
+    inset: -78px !important;
 }
 
-.qts-toggle input {
-    accent-color: var(--ui-pastel-blue-strong);
+body.focus-mode[data-period="morning"] .time-display {
+    background:
+        linear-gradient(135deg, rgba(245, 236, 223, 0.92), rgba(224, 208, 188, 0.78)),
+        radial-gradient(circle at 12% 10%, rgba(213, 164, 107, 0.24), transparent 34%) !important;
+    color: #1d1712 !important;
+    border-color: rgba(255, 243, 225, 0.62) !important;
 }
 
-.qts-controls {
-    display: flex;
-    align-items: center;
-    justify-content: center;
-    width: var(--qts-safe-inline-size);
-    max-width: var(--qts-safe-inline-size);
-    justify-self: center;
-    margin-top: 10px;
-    gap: 16px;
-    flex-wrap: wrap;
+body.focus-mode[data-period="afternoon"] .time-display {
+    background:
+        linear-gradient(135deg, rgba(243, 231, 226, 0.92), rgba(223, 202, 195, 0.8)),
+        radial-gradient(circle at 12% 10%, rgba(196, 114, 89, 0.24), transparent 34%) !important;
+    color: #211716 !important;
+    border-color: rgba(246, 227, 222, 0.62) !important;
 }
 
-.row-control {
-    min-width: 144px;
-    padding: 6px 10px;
-    display: grid;
-    grid-template-columns: 28px 1fr 28px;
-    align-items: center;
-    gap: 8px;
-    border-radius: 0;
-    background: var(--ui-informative-surface);
-    border: 1px solid transparent;
-    box-shadow: var(--ui-informative-shadow);
-    clip-path: polygon(var(--ui-cut-md) 0, calc(100% - var(--ui-cut-md)) 0, 100% var(--ui-cut-md), 100% calc(100% - var(--ui-cut-md)), calc(100% - var(--ui-cut-md)) 100%, var(--ui-cut-md) 100%, 0 calc(100% - var(--ui-cut-md)), 0 var(--ui-cut-md));
+body.focus-mode[data-period="night"] .time-display {
+    background:
+        linear-gradient(135deg, rgba(16, 25, 39, 0.97), rgba(8, 13, 23, 0.92)),
+        radial-gradient(circle at 12% 10%, rgba(88, 131, 255, 0.24), transparent 34%) !important;
+    color: #f7f9ff !important;
+    border-color: rgba(114, 149, 255, 0.24) !important;
 }
 
-.row-label {
-    text-align: center;
-    font-size: 0.58rem;
-    font-weight: 800;
-    letter-spacing: 0.15em;
-    text-transform: uppercase;
-    color: var(--muted);
+body.focus-mode[data-period="morning"] .time-display::before {
+    background: radial-gradient(circle, rgba(255, 183, 112, 0.28) 0%, rgba(255, 183, 112, 0.06) 48%, transparent 76%) !important;
 }
 
-.row-btn {
-    position: relative;
-    overflow: hidden;
-    width: 28px;
-    height: 24px;
-    border-radius: 0;
-    border: 1px solid var(--ui-clickable-border);
-    background: var(--ui-clickable-surface);
-    box-shadow: var(--ui-clickable-shadow);
-    clip-path: polygon(var(--ui-cut-sm) 0, calc(100% - var(--ui-cut-sm)) 0, 100% var(--ui-cut-sm), 100% calc(100% - var(--ui-cut-sm)), calc(100% - var(--ui-cut-sm)) 100%, var(--ui-cut-sm) 100%, 0 calc(100% - var(--ui-cut-sm)), 0 var(--ui-cut-sm));
-    cursor: pointer;
+body.focus-mode[data-period="afternoon"] .time-display::before {
+    background: radial-gradient(circle, rgba(243, 108, 79, 0.26) 0%, rgba(243, 108, 79, 0.06) 48%, transparent 76%) !important;
 }
 
-.qts-summary-card {
-    position: relative;
+body.focus-mode[data-period="night"] .time-display::before {
+    background: radial-gradient(circle, rgba(88, 131, 255, 0.28) 0%, rgba(88, 131, 255, 0.06) 48%, transparent 76%) !important;
 }
 
-.qts-summary-toggle {
-    width: 100%;
-    display: grid;
-    grid-template-columns: minmax(0, 1fr) auto;
-    align-items: center;
-    gap: 10px;
-    padding: 12px 14px;
-    border: 1px solid var(--ui-clickable-border);
-    background: var(--ui-clickable-surface);
-    box-shadow: var(--ui-clickable-shadow);
-    clip-path: polygon(var(--ui-cut-md) 0, calc(100% - var(--ui-cut-md)) 0, 100% var(--ui-cut-md), 100% calc(100% - var(--ui-cut-md)), calc(100% - var(--ui-cut-md)) 100%, var(--ui-cut-md) 100%, 0 calc(100% - var(--ui-cut-md)), 0 var(--ui-cut-md));
-    cursor: pointer;
-    text-align: left;
+body.focus-mode.clock-immersion-mode,
+body.focus-mode.clock-immersion-mode[data-period="morning"],
+body.focus-mode.clock-immersion-mode[data-period="afternoon"],
+body.focus-mode.clock-immersion-mode[data-period="night"] {
+    background:
+        radial-gradient(circle at 18% 14%, rgba(70, 155, 255, 0.18), transparent 30%),
+        radial-gradient(circle at 86% 18%, rgba(45, 97, 180, 0.14), transparent 32%),
+        linear-gradient(180deg, #07101d 0%, #091421 48%, #050912 100%) !important;
+    color: #f7f9ff !important;
 }
 
-.qts-summary-toggle::before {
-    content: "";
-    position: absolute;
-    top: 0;
-    left: var(--ui-cut-md);
-    right: var(--ui-cut-md);
-    height: 1px;
-    background: var(--ui-clickable-line);
-    pointer-events: none;
+body.focus-mode.clock-immersion-mode::before,
+body.focus-mode.clock-immersion-mode::after {
+    opacity: 0.18 !important;
 }
 
-.qts-summary-toggle:hover {
-    transform: translateY(-1px);
+body.focus-mode.clock-immersion-mode .time-display,
+body.focus-mode.clock-immersion-mode[data-period="morning"] .time-display,
+body.focus-mode.clock-immersion-mode[data-period="afternoon"] .time-display,
+body.focus-mode.clock-immersion-mode[data-period="night"] .time-display {
+    background:
+        linear-gradient(135deg, rgba(15, 25, 40, 0.98), rgba(6, 12, 22, 0.94)),
+        radial-gradient(circle at 14% 12%, rgba(87, 166, 255, 0.24), transparent 34%) !important;
+    color: #f7f9ff !important;
+    border-color: rgba(125, 214, 255, 0.2) !important;
+    box-shadow:
+        0 38px 96px rgba(0, 0, 0, 0.46),
+        inset 0 1px 0 rgba(255, 255, 255, 0.06) !important;
 }
 
-.qts-summary-toggle-copy {
-    display: grid;
-    gap: 3px;
+body.focus-mode.clock-immersion-mode .time-display::before,
+body.focus-mode.clock-immersion-mode[data-period="morning"] .time-display::before,
+body.focus-mode.clock-immersion-mode[data-period="afternoon"] .time-display::before,
+body.focus-mode.clock-immersion-mode[data-period="night"] .time-display::before {
+    background: radial-gradient(circle, rgba(88, 151, 255, 0.24) 0%, rgba(88, 151, 255, 0.05) 48%, transparent 76%) !important;
 }
 
-.qts-summary-body {
-    margin-top: 12px;
+body.focus-mode #shortcutHint {
+    left: 50%;
+    right: auto;
+    bottom: 22px;
+    width: auto;
+    max-width: calc(100vw - 24px);
+    transform: translate(-50%, 10px);
 }
 
-.qts-summary-card:not(.is-expanded) .qts-summary-body {
-    display: none;
+body.focus-mode #shortcutHint.is-visible {
+    transform: translate(-50%, 0);
 }
 
-.row-btn::before {
-    content: "";
-    position: absolute;
-    top: 0;
-    left: var(--ui-cut-sm);
-    right: var(--ui-cut-sm);
-    height: 1px;
-    background: var(--ui-clickable-line);
-    pointer-events: none;
-}
+@media (max-width: 1100px) {
+    body:not([data-mode="clock"]) .topbar,
+    body:not([data-mode="clock"]) .hero,
+    body:not([data-mode="clock"]) #moduleArea,
+    body:not([data-mode="clock"]) .footer {
+        width: calc(var(--content-width) - 104px);
+        margin-left: auto;
+        margin-right: 108px;
+    }
 
-#qtsGrid.is-selecting .qts-selectable {
-    user-select: none;
-}
+    .hero {
+        grid-template-columns: repeat(6, minmax(0, 1fr));
+        padding: 22px;
+    }
 
-#qtsGrid > div.qts-cell-selected,
-#qtsGrid div[contenteditable="true"].qts-cell-selected {
-    background: rgba(73, 120, 255, 0.14) !important;
-    box-shadow: inset 0 0 0 1px rgba(73, 120, 255, 0.18) !important;
-}
+    #pomodoroPresets,
+    .date-display,
+    .time-display,
+#todayFlow,
+    #cycleProgress,
+    .main-stats-row,
+    #streakVisual,
+    #dailyGoal,
+    #weeklyChart,
+    #sessionLog,
+    #advancedStats,
+    #achievements,
+    #progressiveBars,
+    .controls,
+    #homeLaunchpad {
+        grid-column: 1 / -1;
+    }
 
-#qtsGrid > div.qts-time.qts-cell-selected,
-#qtsGrid div.qts-time[contenteditable="true"].qts-cell-selected {
-    background: rgba(243, 108, 79, 0.14) !important;
-    box-shadow: inset 0 0 0 1px rgba(243, 108, 79, 0.22) !important;
-}
+    .launchpad-primary-grid {
+        grid-template-columns: repeat(3, minmax(0, 1fr));
+    }
+
+    .main-stats-row {
+        grid-template-columns: 1fr;
+    }
+
+    .today-flow-head,
+    .today-flow-grid {
+        grid-template-columns: 1fr;
+    }
+
+    .today-flow-head {
+        display: grid;
+    }
 
-#qtsGrid > div.qts-selection-anchor {
-    box-shadow:
-        inset 0 0 0 2px rgba(73, 120, 255, 0.9),
-        0 10px 18px rgba(73, 120, 255, 0.12) !important;
-}
+    .today-flow-center {
+        grid-template-columns: 1fr;
+    }
 
-body.dark #qtsGrid > div.qts-cell-selected,
-body.dark #qtsGrid div[contenteditable="true"].qts-cell-selected {
-    background: rgba(109, 148, 255, 0.18) !important;
-    box-shadow: inset 0 0 0 1px rgba(109, 148, 255, 0.26) !important;
-}
+    .today-flow-track {
+        grid-template-columns: repeat(3, minmax(0, 1fr));
+    }
 
-body.dark #qtsGrid > div.qts-time.qts-cell-selected,
-body.dark #qtsGrid div.qts-time[contenteditable="true"].qts-cell-selected {
-    background: rgba(243, 108, 79, 0.2) !important;
-    box-shadow: inset 0 0 0 1px rgba(243, 108, 79, 0.3) !important;
-}
+    #streakVisual,
+    #dailyGoal,
+    #weeklyChart,
+    #sessionLog,
+    #advancedStats,
+    #achievements {
+        grid-column: 1 / -1;
+    }
 
-.qts-autocomplete {
-    position: absolute;
-    z-index: 30;
-    display: grid;
-    gap: 6px;
-    padding: 10px;
-    border-radius: 18px;
-    background: rgba(255, 255, 255, 0.92);
-    border: 1px solid rgba(255, 255, 255, 0.82);
-    box-shadow: 0 24px 60px rgba(17, 24, 39, 0.14);
-    backdrop-filter: blur(18px);
-}
+    .side-modules {
+        top: 50%;
+        bottom: auto;
+        right: 20px;
+        left: auto;
+        transform: translateY(-50%);
+        flex-direction: column;
+        z-index: 1200;
+    }
 
-.qts-autocomplete[hidden] {
-    display: none;
-}
+    body[data-mode="clock"] .side-modules {
+        opacity: 0;
+        pointer-events: none;
+        transform: translateY(-50%) translateX(16px);
+    }
 
-.qts-autocomplete-item {
-    padding: 10px 12px;
-    border-radius: 12px;
-    border: 1px solid transparent;
-    background: rgba(73, 120, 255, 0.06);
-    color: var(--ink);
-    text-align: left;
-    font-weight: 700;
-    cursor: pointer;
-    transition: background 0.18s ease, transform 0.18s ease, border-color 0.18s ease;
-}
+    body[data-mode="qts"] .side-modules {
+        right: 2px;
+    }
 
-.qts-autocomplete-item:hover,
-.qts-autocomplete-item.is-active {
-    background: rgba(73, 120, 255, 0.14);
-    border-color: rgba(73, 120, 255, 0.22);
-    transform: translateY(-1px);
-}
+    body[data-mode="qts"] #qtsModule.module.active {
+        width: calc(100% - var(--qts-left-rail-space, 0px) - var(--qts-left-clearance, 0px) - var(--qts-right-tools-space, 0px) - var(--qts-right-edge-gap, 0px));
+        margin-left: calc(var(--qts-left-rail-space, 0px) + var(--qts-left-clearance, 0px));
+        margin-right: calc(var(--qts-right-tools-space, 0px) + var(--qts-right-edge-gap, 0px));
+    }
 
-body.dark .qts-autocomplete {
-    background: rgba(17, 24, 39, 0.92);
-    border-color: rgba(255, 255, 255, 0.08);
-    box-shadow: 0 24px 60px rgba(0, 0, 0, 0.28);
-}
+    .side-group {
+        gap: 8px;
+    }
 
-body.dark .qts-autocomplete-item {
-    background: rgba(255, 255, 255, 0.06);
-    color: #f4f7ff;
-}
+    .side-group-label {
+        display: none;
+    }
 
-body.dark .qts-autocomplete-item:hover,
-body.dark .qts-autocomplete-item.is-active {
-    background: rgba(109, 148, 255, 0.18);
-    border-color: rgba(109, 148, 255, 0.28);
-}
+    .side-group-buttons {
+        display: grid;
+        gap: 8px;
+    }
 
-.qts-header-day {
-    font-size: 0.84rem;
-    font-weight: 700;
-    line-height: 1.05;
-}
+    #ambientRoot .ambient-mini {
+        left: 20px;
+        bottom: 110px;
+        width: min(clamp(238px, 28vw, 332px), calc(100vw - 40px));
+    }
 
-.qts-header-date {
-    display: block;
-    font-size: 0.62rem;
-    letter-spacing: 0.08em;
-    text-transform: uppercase;
-    color: var(--muted);
-    line-height: 1.02;
-    white-space: nowrap;
-}
+    #ambientRoot .ambient-peek {
+        left: 20px;
+        bottom: 110px;
+    }
 
-.qts-subject-summary {
-    width: min(100%, 1080px);
-    justify-self: start;
-    margin-top: 16px;
-}
+    #ambientRoot .ambient-panel {
+        left: 20px;
+        bottom: 220px;
+    }
 
-.qts-summary-card {
-    padding: 16px 18px;
-    border-radius: 0;
-    background: var(--ui-informative-surface);
-    border: 1px solid transparent;
-    box-shadow: var(--ui-informative-shadow);
-    clip-path: polygon(var(--ui-cut-lg) 0, calc(100% - var(--ui-cut-lg)) 0, 100% var(--ui-cut-lg), 100% calc(100% - var(--ui-cut-lg)), calc(100% - var(--ui-cut-lg)) 100%, var(--ui-cut-lg) 100%, 0 calc(100% - var(--ui-cut-lg)), 0 var(--ui-cut-lg));
+    #ambientRoot .ambient-mini-meta {
+        gap: 6px;
+    }
 }
 
-body.dark .qts-week-context,
-body.dark .qts-summary-card {
-    background: var(--ui-informative-surface);
-    border-color: transparent;
+@media (max-width: 1100px) {
+    body[data-mode="qts"] .topbar,
+    body[data-mode="qts"] #moduleArea,
+    body[data-mode="qts"] .footer {
+        width: var(--content-width);
+        margin-left: auto;
+        margin-right: auto;
+    }
 }
 
-.qts-summary-card.is-empty {
-    display: grid;
-    gap: 10px;
-}
+@media (max-width: 760px) {
+    .ambient-help-box,
+    .ambient-blocked-box {
+        width: calc(100vw - 20px);
+        padding: 22px 18px 18px;
+        border-radius: 20px;
+    }
 
-.qts-summary-card.is-empty p {
-    margin: 0;
-    color: var(--muted);
-}
+    :root {
+        --content-width: min(100vw - 24px, 100%);
+    }
 
-.qts-summary-head {
-    display: flex;
-    align-items: center;
-    justify-content: space-between;
-    gap: 10px;
-    flex-wrap: wrap;
-    margin-bottom: 10px;
-}
+    body {
+        padding: 12px 12px 140px;
+    }
 
-.qts-summary-head > div:first-child {
-    min-width: 0;
-}
+    .topbar {
+        flex-direction: column;
+        align-items: stretch;
+        gap: 12px;
+        padding: 14px 52px 14px 14px;
+    }
 
-.qts-summary-head strong {
-    display: block;
-    margin-top: 4px;
-    font-size: 0.96rem;
-    font-weight: 800;
-}
+    .topbar-collapse-btn {
+        top: 14px;
+        right: 14px;
+        transform: none;
+    }
 
-.qts-summary-total {
-    min-width: fit-content;
-    padding: 5px 9px;
-    border-radius: 0;
-    background: linear-gradient(180deg, rgba(183, 215, 240, 0.3), rgba(183, 215, 240, 0.16));
-    font-size: 0.78rem;
-    font-weight: 800;
-    color: var(--ink);
-    clip-path: polygon(var(--ui-cut-sm) 0, calc(100% - var(--ui-cut-sm)) 0, 100% var(--ui-cut-sm), 100% calc(100% - var(--ui-cut-sm)), calc(100% - var(--ui-cut-sm)) 100%, var(--ui-cut-sm) 100%, 0 calc(100% - var(--ui-cut-sm)), 0 var(--ui-cut-sm));
-}
+    body.topbar-collapsed .topbar {
+        width: 100px;
+        margin-left: 12px;
+    }
 
-.qts-summary-list {
-    display: grid;
-    gap: 12px;
-}
+    .topbar-left,
+    .topbar-right {
+        justify-content: space-between;
+        flex-wrap: wrap;
+    }
 
-.qts-summary-scopes {
-    display: flex;
-    flex-wrap: wrap;
-    gap: 8px;
-    margin-bottom: 14px;
-}
+    .timezone-topbar {
+        width: 100%;
+        min-width: 0;
+        margin-left: 0;
+    }
 
-.qts-summary-scope {
-    min-height: 34px;
-    padding: 0 12px;
-    border-radius: 999px;
-    border: 1px solid var(--line);
-    background: rgba(255, 255, 255, 0.52);
-    color: var(--ink);
-    font-size: 0.76rem;
-    font-weight: 800;
-    letter-spacing: 0.08em;
-    text-transform: uppercase;
-}
+    .hero,
+    .module.active,
+    .footer {
+        border-radius: 0;
+        padding: 18px;
+    }
 
-.qts-summary-scope.is-active {
-    background: rgba(37, 88, 240, 0.12);
-    border-color: rgba(37, 88, 240, 0.18);
-    color: #2558f0;
-}
+    body[data-mode="qts"] #qtsModule {
+        --qts-inline-gutter: 0px;
+        --qts-left-rail-space: 0px;
+        --qts-right-tools-space: 0px;
+        --qts-content-max: 100%;
+    }
 
-body.dark .qts-summary-scope {
-    background: rgba(255, 255, 255, 0.04);
-}
+    body[data-mode="qts"] #qtsModule.module.active {
+        padding-left: 0;
+        padding-right: 0;
+    }
 
-.qts-summary-item {
-    display: grid;
-    grid-template-columns: minmax(0, 1.1fr) minmax(136px, 1.15fr) auto;
-    gap: 10px;
-    align-items: center;
-}
+    #qtsModule .qts-stage-main {
+        width: 100%;
+        margin-left: 0;
+        padding-left: 0;
+        padding-right: 0;
+        justify-items: stretch;
+    }
 
-@media (max-width: 1480px) and (min-width: 761px) {
-    .qts-summary-item {
-        grid-template-columns: minmax(0, 1fr) minmax(156px, 1.2fr) auto;
-        gap: 12px;
+    #qtsModule .qts-grid-shell,
+    #qtsModule .qts-controls,
+    #qtsModule .qts-subject-summary {
+        width: 100%;
+        max-width: 100%;
+        justify-self: stretch;
     }
 
-    .qts-summary-time {
-        min-width: 64px;
-        text-align: right;
+    .qts-template-strip {
+        gap: 8px;
+        margin-bottom: 12px;
+        width: min(100%, 100%);
+        justify-self: center;
     }
-}
 
-.qts-summary-copy {
-    display: grid;
-    gap: 2px;
-}
+    .qts-template-strip::after {
+        width: min(100%, 360px);
+    }
 
-.qts-summary-copy strong {
-    font-size: 0.94rem;
-}
+    .qts-template-label {
+        min-height: 30px;
+        padding: 0 12px;
+        font-size: 0.6rem;
+        letter-spacing: 0.1em;
+    }
 
-.qts-summary-copy span,
-.qts-summary-time {
-    color: var(--muted);
-    font-size: 0.78rem;
-}
+    .qts-templates {
+        gap: 6px;
+    }
 
-.qts-summary-bar {
-    height: 10px;
-    border-radius: 999px;
-    background: rgba(22, 30, 51, 0.08);
-    overflow: hidden;
-}
+    .qts-templates button {
+        min-height: 30px;
+        padding: 0 9px;
+        font-size: 0.68rem;
+    }
 
-body.dark .qts-summary-bar {
-    background: rgba(255, 255, 255, 0.08);
-}
+    .qts-title {
+        width: fit-content;
+        max-width: calc(100% - 10px);
+        justify-self: center;
+        margin-bottom: 4px;
+        padding: 7px 14px;
+        font-size: clamp(1.02rem, 4.8vw, 1.34rem);
+        text-align: center;
+    }
 
-.qts-summary-fill {
-    height: 100%;
-    border-radius: inherit;
-    background: linear-gradient(90deg, rgba(37, 88, 240, 0.94), rgba(71, 191, 162, 0.94));
-    box-shadow: 0 10px 24px rgba(37, 88, 240, 0.22);
-}
+    .qts-grid-shell {
+        padding: 2px 3px 4px;
+        background: linear-gradient(180deg, rgba(255, 255, 255, 0.985), rgba(248, 247, 243, 0.97));
+        box-shadow: 0 10px 20px rgba(15, 23, 42, 0.06);
+    }
 
-.side-modules {
-    position: fixed;
-    top: 50%;
-    right: clamp(20px, calc((100vw - var(--content-width)) / 2 + 20px), 88px);
-    left: auto;
-    transform: translateY(-50%);
-    padding: 11px 10px;
-    border-radius: 0;
-    background: var(--ui-informative-surface);
-    backdrop-filter: blur(16px);
-    border: 1px solid transparent;
-    box-shadow: var(--ui-informative-shadow);
-    gap: 10px;
-    clip-path: polygon(var(--ui-cut-md) 0, calc(100% - var(--ui-cut-md)) 0, 100% var(--ui-cut-md), 100% calc(100% - var(--ui-cut-md)), calc(100% - var(--ui-cut-md)) 100%, var(--ui-cut-md) 100%, 0 calc(100% - var(--ui-cut-md)), 0 var(--ui-cut-md));
-}
+    #qtsGrid {
+        padding: 1px;
+        background: linear-gradient(180deg, rgba(255, 255, 255, 0.99), rgba(248, 247, 243, 0.975));
+    }
 
-.side-modules .icon-circle {
-    width: 44px;
-    height: 44px;
-}
+    #qtsGrid > div {
+        min-height: 26px;
+        color: #1d2430;
+    }
 
-.side-group {
-    display: grid;
-    gap: 8px;
-}
+    #qtsGrid div[contenteditable="true"] {
+        padding: 3px 4px;
+        font-size: 0.64rem;
+        line-height: 1;
+    }
 
-.side-group-label {
-    font-size: 0.62rem;
-    font-weight: 800;
-    letter-spacing: 0.16em;
-    text-transform: uppercase;
-    color: var(--muted);
-    text-align: center;
-}
+    .qts-header {
+        min-height: 34px;
+        padding: 3px 3px 2px;
+        background: linear-gradient(180deg, rgba(223, 230, 245, 0.99), rgba(214, 223, 241, 0.97));
+    }
 
-.side-group-buttons {
-    display: grid;
-    gap: 8px;
-}
+    .qts-header-day {
+        font-size: 0.64rem;
+    }
 
-.side-group[data-nav-group="primary"] .icon-circle {
-    width: 48px;
-    height: 48px;
-}
+    .qts-header-date {
+        font-size: 0.48rem;
+    }
 
-.side-group[data-nav-group="utility"] .icon-circle {
-    width: 38px;
-    height: 38px;
-    font-size: 0.82rem;
-}
+    .qts-time {
+        min-height: 26px;
+        background: linear-gradient(180deg, rgba(236, 244, 255, 0.99), rgba(226, 237, 252, 0.97));
+    }
 
-.side-group[data-nav-group="utility"] {
-    padding-top: 10px;
-    border-top: 1px solid var(--line);
-}
+    #qtsGrid .qts-time[contenteditable="true"] {
+        padding: 3px 3px;
+        font-size: 0.7rem;
+    }
 
-.side-group[data-nav-group="utility"] .side-group-label {
-    display: none;
-}
+    .qts-interval {
+        min-height: 11px !important;
+        font-size: 0.56rem;
+        background: linear-gradient(90deg, rgba(220, 241, 232, 0.94), rgba(228, 245, 237, 0.96));
+    }
 
-body[data-mode="clock"] .side-modules {
-    opacity: 0;
-    pointer-events: none;
-    transform: translateY(-50%) translateX(16px);
-}
+    .qts-controls {
+        margin-top: 8px;
+        gap: 8px;
+    }
 
-body:not([data-mode="clock"]) .side-modules {
-    opacity: 1;
-    pointer-events: auto;
-    transition: opacity 0.18s ease, transform 0.18s ease;
-}
+    .row-control {
+        flex: 1 1 180px;
+        min-width: min(180px, calc(50% - 4px));
+        padding: 5px 8px;
+        grid-template-columns: 24px 1fr 24px;
+        gap: 6px;
+    }
 
-body[data-mode="qts"] .side-modules {
-    right: clamp(8px, calc((100vw - var(--content-width)) / 2 + 8px), 36px);
-}
+    .row-label {
+        font-size: 0.52rem;
+        letter-spacing: 0.12em;
+    }
 
-.footer {
-    margin-top: calc(16px * var(--site-scale, 1));
-    padding: var(--site-footer-pad-top) var(--site-footer-pad-x) var(--site-footer-pad-bottom);
-    border-radius: 0;
-    background: var(--ui-informative-surface);
-    backdrop-filter: blur(16px);
-    border: 1px solid transparent;
-    box-shadow: var(--ui-informative-shadow);
-    clip-path: polygon(var(--ui-cut-lg) 0, calc(100% - var(--ui-cut-lg)) 0, 100% var(--ui-cut-lg), 100% calc(100% - var(--ui-cut-lg)), calc(100% - var(--ui-cut-lg)) 100%, var(--ui-cut-lg) 100%, 0 calc(100% - var(--ui-cut-lg)), 0 var(--ui-cut-lg));
-}
+    .row-btn {
+        width: 24px;
+        height: 22px;
+    }
 
-.donate-area {
-    display: flex;
-    justify-content: space-between;
-    align-items: center;
-    gap: calc(18px * var(--site-scale, 1));
-    margin-bottom: calc(14px * var(--site-scale, 1));
-}
+    .time-display {
+        font-size: clamp(3.5rem, 18vw, 5.6rem);
+        padding: 22px 18px;
+        border-radius: 24px;
+    }
 
-.donate-area a {
-    font-size: 0.92rem;
-    font-weight: 800;
-    letter-spacing: 0.08em;
-    text-transform: uppercase;
-}
+    .today-flow-shell,
+    .today-flow-card {
+        padding: 18px;
+        border-radius: 22px;
+    }
 
-.footer-divider {
-    margin: calc(12px * var(--site-scale, 1)) 0;
-    background: var(--line);
-}
+    .today-flow-head {
+        gap: 12px;
+    }
 
-.world-carousel {
-    font-family: "Space Grotesk", sans-serif;
-    font-size: 0.92rem;
-    letter-spacing: -0.03em;
-}
+    .today-flow-progress {
+        width: 100%;
+    }
 
-.custom-box {
-    width: min(440px, calc(100vw - 36px));
-    border-radius: 28px;
-    padding: 26px;
-    background: var(--surface-strong);
-    border: 1px solid var(--line);
-}
+    .today-flow-card-top,
+    .today-flow-queue-item {
+        align-items: flex-start;
+    }
 
-#ambientRoot .ambient-mini,
-#ambientRoot .ambient-panel {
-    color: var(--ink) !important;
-    background: var(--glass) !important;
-    border: 1px solid var(--line) !important;
-    box-shadow: var(--shadow) !important;
-    backdrop-filter: blur(20px);
-}
+    .today-flow-card-top {
+        flex-direction: column;
+    }
 
-#ambientRoot .ambient-peek {
-    position: fixed;
-    left: 20px;
-    bottom: 22px;
-    display: inline-flex;
-    align-items: center;
-    gap: 10px;
-    height: 42px;
-    padding: 0 16px;
-    border-radius: 0;
-    border: 1px solid var(--ui-clickable-border);
-    background: var(--ui-clickable-surface);
-    color: var(--ink);
-    box-shadow: var(--ui-clickable-shadow);
-    backdrop-filter: blur(18px);
-    z-index: 1310;
-    opacity: 0;
-    pointer-events: none;
-    transform: translateY(10px);
-    transition: opacity 0.18s ease, transform 0.18s ease, background 0.18s ease;
-    clip-path: polygon(12px 0, calc(100% - 12px) 0, 100% 12px, 100% calc(100% - 12px), calc(100% - 12px) 100%, 12px 100%, 0 calc(100% - 12px), 0 12px);
-}
+    .today-flow-queue-item {
+        display: grid;
+        grid-template-columns: 1fr;
+    }
 
-#ambientRoot .ambient-peek.is-visible {
-    opacity: 1;
-    pointer-events: auto;
-    transform: translateY(0);
-}
+    .today-flow-queue-time {
+        min-width: 0;
+    }
 
-#ambientRoot .ambient-peek:hover {
-    background: var(--ui-preset-clickable-hover-surface);
-}
+    .today-flow-actions {
+        display: grid;
+        grid-template-columns: 1fr;
+    }
 
-#ambientRoot .ambient-peek-dot {
-    width: 9px;
-    height: 9px;
-    border-radius: 999px;
-    background: rgba(24, 20, 27, 0.18);
-}
+    .today-flow-track {
+        grid-template-columns: 1fr;
+    }
 
-#ambientRoot .ambient-peek.is-playing .ambient-peek-dot {
-    background: var(--accent);
-    box-shadow: 0 0 0 6px rgba(37, 88, 240, 0.12);
-}
+    .today-flow-center {
+        grid-template-columns: 1fr;
+        justify-items: stretch;
+    }
 
-#ambientRoot .ambient-peek-label {
-    font-size: 0.82rem;
-    font-weight: 800;
-    letter-spacing: 0.12em;
-    text-transform: uppercase;
-}
+    .today-flow-mode-wrap {
+        justify-items: flex-start;
+        text-align: left;
+    }
 
-#ambientRoot .ambient-mini {
-    position: fixed;
-    left: 20px;
-    bottom: 22px;
-    width: min(clamp(246px, 24vw, 360px), calc(100vw - 32px));
-    padding: 10px 12px 11px;
-    border-radius: 0;
-    z-index: 1300;
-    display: grid;
-    gap: 8px;
-    transition: width 0.18s ease, opacity 0.18s ease, transform 0.18s ease;
-    box-shadow:
-        inset 0 1px 0 rgba(255, 255, 255, 0.22),
-        var(--shadow) !important;
-    clip-path: polygon(16px 0, calc(100% - 16px) 0, 100% 16px, 100% calc(100% - 16px), calc(100% - 16px) 100%, 16px 100%, 0 calc(100% - 16px), 0 16px);
-}
+    #homeLaunchpad {
+        gap: 14px;
+    }
 
-#ambientRoot .ambient-mini::after {
-    left: 14px;
-    right: 14px;
-    top: 1px;
-    height: 2px;
-    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.9), transparent);
-    opacity: 0.92;
-}
+    .home-launchpad-main,
+    .home-launchpad-tools {
+        padding: 18px;
+        border-radius: 24px;
+    }
 
-body.dark #ambientRoot .ambient-mini::after,
-body.focus-mode #ambientRoot .ambient-mini::after {
-    background: linear-gradient(90deg, transparent, rgba(174, 196, 232, 0.5), transparent);
-    opacity: 0.86;
-}
+    .rotanota-home-brand {
+        grid-template-columns: 1fr;
+        justify-items: center;
+        text-align: center;
+        padding: 20px 18px;
+    }
 
-#ambientRoot .ambient-mini.is-hidden {
-    display: none;
-}
+    .rotanota-home-brand p {
+        margin-inline: auto;
+    }
 
-#ambientRoot .ambient-mini-main {
-    display: grid;
-    grid-template-columns: auto 1fr;
-    grid-template-areas:
-        "icon actions"
-        "ticker ticker";
-    align-items: center;
-    gap: 6px 10px;
-}
+    .rotanota-topbar-brand small {
+        display: none;
+    }
 
-#ambientRoot .ambient-mini-actions {
-    grid-area: actions;
-    display: inline-flex;
-    align-items: center;
-    justify-content: flex-end;
-    justify-self: end;
-    gap: 4px;
-}
+    .launchpad-card {
+        min-height: auto;
+    }
 
-#ambientRoot .ambient-mini-meta {
-    display: grid;
-    gap: 6px;
-}
+    .launchpad-tool-row {
+        display: grid;
+        grid-template-columns: 1fr 1fr;
+        width: 100%;
+    }
 
-#ambientRoot .ambient-mini-icon,
-#ambientRoot .ambient-primary-control,
-#ambientRoot .ambient-secondary-control,
-#ambientRoot .ambient-header-actions button {
-    display: inline-flex;
-    align-items: center;
-    justify-content: center;
-    border: 1px solid var(--line);
-    background: var(--surface-strong);
-    color: var(--ink);
-    box-shadow: var(--shadow-soft);
-    cursor: pointer;
-    transition: transform 0.18s ease, background 0.18s ease, box-shadow 0.18s ease;
-    border-radius: 0;
-}
+    .launchpad-primary-grid {
+        grid-template-columns: 1fr;
+    }
 
-#ambientRoot .ambient-primary-control {
-    background: var(--ui-preset-active-surface);
-    color: var(--ui-preset-active-text);
-    border-color: var(--ui-preset-active-border);
-    box-shadow: var(--ui-preset-active-shadow);
-}
+    .study-stats {
+        grid-template-columns: 1fr;
+    }
 
-#ambientRoot .ambient-secondary-control,
-#ambientRoot .ambient-mini-icon {
-    background: var(--ui-clickable-surface);
-    border-color: var(--ui-clickable-border);
-    box-shadow: var(--ui-clickable-shadow);
-}
+    .heat-cell {
+        min-width: 20px;
+        height: 44px;
+    }
 
-#ambientRoot .ambient-mini-icon,
-#ambientRoot .ambient-primary-control {
-    width: clamp(36px, 3vw, 40px);
-    height: clamp(36px, 3vw, 40px);
-    font-size: 0.95rem;
-    font-weight: 800;
-    clip-path: polygon(12px 0, calc(100% - 12px) 0, 100% 12px, 100% calc(100% - 12px), calc(100% - 12px) 100%, 12px 100%, 0 calc(100% - 12px), 0 12px);
-}
+    .donate-area {
+        flex-direction: column;
+        align-items: flex-start;
+    }
 
-#ambientRoot .ambient-secondary-control,
-#ambientRoot .ambient-header-actions button {
-    width: clamp(30px, 2.7vw, 34px);
-    height: clamp(30px, 2.7vw, 34px);
-    font-size: 0.8rem;
-    font-weight: 800;
-    clip-path: polygon(10px 0, calc(100% - 10px) 0, 100% 10px, 100% calc(100% - 10px), calc(100% - 10px) 100%, 10px 100%, 0 calc(100% - 10px), 0 10px);
-}
+    .side-modules {
+        top: 50%;
+        right: 10px;
+        left: auto;
+        width: 60px;
+        max-width: 60px;
+        justify-content: flex-start;
+        align-items: center;
+        padding: 8px 6px;
+        border-radius: 0;
+        gap: 6px;
+        transform: translateY(-50%);
+    }
 
-#ambientRoot .ambient-mini-icon:hover,
-#ambientRoot .ambient-primary-control:hover,
-#ambientRoot .ambient-secondary-control:hover,
-#ambientRoot .ambient-header-actions button:hover {
-    transform: translateY(-2px);
-    background: var(--ui-preset-clickable-hover-surface);
-}
+    .side-group,
+    .side-group-buttons {
+        justify-items: center;
+        gap: 6px;
+    }
 
-#ambientRoot .ambient-now {
-    grid-area: ticker;
-    min-width: 0;
-}
+    .side-modules .icon-circle {
+        width: 40px;
+        height: 40px;
+    }
 
-#ambientRoot .ambient-panel-kicker,
-#ambientRoot .ambient-item-meta {
-    font-size: 0.66rem;
-    font-weight: 800;
-    letter-spacing: 0.14em;
-    text-transform: uppercase;
-    color: var(--muted);
-}
+    .side-group[data-nav-group="primary"] .icon-circle,
+    .side-group[data-nav-group="utility"] .icon-circle {
+        width: 40px;
+        height: 40px;
+    }
 
-#ambientRoot .ambient-mini-icon {
-    grid-area: icon;
-}
+    #ambientRoot .ambient-mini {
+        left: 12px;
+        bottom: 94px;
+        width: min(clamp(238px, 62vw, 312px), calc(100vw - 24px));
+    }
 
-#ambientRoot .ambient-ticker {
-    position: relative;
-    min-width: 0;
-    overflow: hidden;
-    padding-left: 2px;
-    mask-image: linear-gradient(90deg, transparent 0, #000 8%, #000 92%, transparent 100%);
-    -webkit-mask-image: linear-gradient(90deg, transparent 0, #000 8%, #000 92%, transparent 100%);
-}
+    #ambientRoot .ambient-peek {
+        left: 12px;
+        bottom: 94px;
+    }
 
-#ambientRoot .ambient-ticker-track {
-    display: flex;
-    align-items: center;
-    width: max-content;
-    gap: 20px;
-}
+    #ambientRoot .ambient-mini-meta {
+        gap: 6px;
+    }
 
-#ambientRoot .ambient-ticker-copy {
-    white-space: nowrap;
-    font-size: 0.68rem;
-    font-weight: 800;
-    letter-spacing: 0.14em;
-    text-transform: uppercase;
-    color: var(--muted);
-}
+    #ambientRoot .ambient-mini-main {
+        gap: 6px 8px;
+    }
 
-#ambientRoot .ambient-ticker:not(.is-scrolling) #ambientTickerClone {
-    display: none;
-}
+    #ambientRoot .ambient-mini-actions {
+        gap: 4px;
+    }
 
-#ambientRoot .ambient-ticker.is-scrolling .ambient-ticker-track {
-    animation: ambientTicker 14s linear infinite;
-}
+    #ambientRoot .ambient-progress {
+        grid-template-columns: 40px 1fr 40px;
+    }
 
-#ambientRoot .ambient-progress {
-    display: grid;
-    grid-template-columns: 44px 1fr 44px;
-    align-items: center;
-    gap: 6px;
-    font-size: 0.68rem;
-    color: var(--muted);
-    padding: 0;
-    opacity: 1;
-}
+    #ambientRoot .ambient-volume-wrap {
+        grid-template-columns: 28px 1fr;
+    }
 
-#ambientRoot #ambientSeek,
-#ambientRoot #ambientVolume {
-    width: 100%;
-    accent-color: var(--accent);
-    cursor: pointer;
-}
+    #ambientRoot .ambient-panel {
+        left: 12px;
+        bottom: 188px;
+        width: calc(100vw - 24px);
+        height: min(56vh, 460px);
+        padding: 12px;
+    }
 
-#ambientRoot .ambient-volume-wrap {
-    display: grid;
-    grid-template-columns: 26px 1fr;
-    align-items: center;
-    gap: 6px;
-}
+    #ambientRoot .category-selector {
+        grid-template-columns: repeat(2, minmax(0, 1fr));
+    }
 
-#ambientRoot .ambient-volume-label {
-    font-size: 0.68rem;
-    font-weight: 800;
-    letter-spacing: 0.1em;
-    color: var(--muted);
-}
+    #ambientRoot .ambient-item {
+        grid-template-columns: 32px 72px minmax(0, 1fr);
+        gap: 10px;
+        padding: 9px;
+    }
 
-@keyframes ambientTicker {
-    from {
-        transform: translateX(0);
+    #ambientRoot .ambient-item img {
+        width: 72px;
+        height: 46px;
     }
 
-    to {
-        transform: translateX(calc(-50% - 10px));
+    #ambientRoot .fav-btn {
+        width: 32px;
+        height: 32px;
     }
-}
 
-#ambientRoot .ambient-panel {
-    position: fixed;
-    left: 20px;
-    bottom: 132px;
-    top: auto;
-    right: auto;
-    width: min(372px, calc(100vw - 40px));
-    height: min(520px, 64vh);
-    border-radius: 0 !important;
-    padding: 12px;
-    z-index: 1290;
-    display: flex;
-    flex-direction: column;
-    gap: 10px;
-    opacity: 0;
-    transform: translateY(14px) scale(0.98);
-    pointer-events: none;
-    transition: opacity 0.18s ease, transform 0.18s ease;
-    background: var(--glass) !important;
-    clip-path: polygon(18px 0, calc(100% - 18px) 0, 100% 18px, 100% calc(100% - 18px), calc(100% - 18px) 100%, 18px 100%, 0 calc(100% - 18px), 0 18px);
+    .mini-analog-clock {
+        right: 16px;
+        bottom: 160px;
+        width: 92px;
+        height: 92px;
+    }
 }
 
-#ambientRoot .ambient-panel.is-open {
-    opacity: 1;
-    transform: translateY(0) scale(1);
-    pointer-events: auto;
-}
+@media (max-width: 520px) {
+    .qts-template-strip {
+        max-width: 100%;
+    }
 
-#ambientRoot .ambient-header {
-    display: grid;
-    grid-template-columns: minmax(0, 1fr) auto;
-    align-items: center;
-    gap: 14px;
-    padding: 0;
-    border-bottom: none;
-}
+    .qts-templates {
+        gap: 5px;
+    }
 
-#ambientRoot .ambient-header-copy {
-    min-width: 0;
-    display: grid;
-    gap: 4px;
-    color: var(--ink);
-}
+    .qts-templates button {
+        min-height: 28px;
+        padding: 0 8px;
+        font-size: 0.62rem;
+    }
 
-#ambientRoot .ambient-panel-title {
-    font-size: 1rem;
-    font-weight: 800;
-    letter-spacing: -0.04em;
-    color: var(--ink);
-}
+    .qts-template-label {
+        min-height: 28px;
+        padding: 0 10px;
+        font-size: 0.56rem;
+    }
 
-#ambientRoot .ambient-header-actions {
-    display: flex;
-    align-items: center;
-    gap: 6px;
-}
+    .qts-title {
+        width: fit-content;
+        max-width: calc(100% - 8px);
+        justify-self: center;
+        font-size: clamp(0.94rem, 4.4vw, 1.14rem);
+        padding: 6px 12px;
+    }
 
-#ambientRoot .category-selector {
-    position: static;
-    inset: auto;
-    display: grid;
-    grid-template-columns: repeat(3, minmax(0, 1fr));
-    gap: 8px;
-    padding: 0;
-    background: transparent;
-    backdrop-filter: none;
-    animation: none;
-}
+    #qtsGrid > div {
+        min-height: 24px;
+    }
 
-#ambientRoot .category-selector.hidden {
-    display: none;
-}
+    #qtsGrid div[contenteditable="true"] {
+        font-size: 0.56rem;
+        padding: 2px 3px;
+    }
 
-#ambientRoot .cat-item {
-    position: static;
-    width: 100%;
-    height: auto;
-    left: auto;
-    top: auto;
-    margin: 0;
-    padding: 10px 12px;
-    border-radius: 0;
-    transform: none !important;
-    font-size: 0.78rem;
-    box-shadow: var(--ui-clickable-shadow);
-    transition: transform 0.18s ease, filter 0.18s ease;
-    border: 1px solid var(--ui-clickable-border);
-    background: var(--ui-clickable-surface);
-    color: var(--ink);
-    clip-path: polygon(12px 0, calc(100% - 12px) 0, 100% 12px, 100% calc(100% - 12px), calc(100% - 12px) 100%, 12px 100%, 0 calc(100% - 12px), 0 12px);
-}
+    .qts-header {
+        min-height: 31px;
+        padding: 2px 2px 1px;
+    }
+
+    .qts-header-day {
+        font-size: 0.52rem;
+    }
+
+    .qts-header-date {
+        font-size: 0.38rem;
+    }
 
-#ambientRoot .cat-item:hover {
-    width: 100%;
-    height: auto;
-    margin: 0;
-    color: inherit;
-    transform: translateY(-1px) !important;
-}
+    #qtsGrid .qts-time[contenteditable="true"] {
+        font-size: 0.56rem;
+    }
 
-#ambientRoot .cat-item::after {
-    display: none;
-}
+    .qts-interval {
+        font-size: 0.5rem;
+    }
 
-#ambientRoot .ambient-list {
-    flex: 1;
-    overflow-y: auto;
-    padding-right: 4px;
-    overscroll-behavior: contain;
+    .row-control {
+        min-width: 100%;
+    }
 }
 
-#ambientRoot .ambient-item {
-    display: grid;
-    grid-template-columns: auto 72px minmax(0, 1fr);
-    gap: 8px;
-    align-items: center;
-    padding: 8px;
-    border-radius: 0 !important;
-    background: var(--ui-display-surface);
-    border: 1px solid var(--ui-clickable-border);
-    box-shadow: var(--ui-informative-shadow);
-    margin-bottom: 6px;
-    transition: transform 0.18s ease, background 0.18s ease, border-color 0.18s ease;
-    clip-path: polygon(14px 0, calc(100% - 14px) 0, 100% 14px, 100% calc(100% - 14px), calc(100% - 14px) 100%, 14px 100%, 0 calc(100% - 14px), 0 14px);
+/* =====================================================
+   RotaNota refinement - home, Pomodoro and light radii
+===================================================== */
+
+:root {
+    --rn-radius-shell: 14px;
+    --rn-radius-card: 10px;
+    --rn-radius-control: 9px;
+    --rn-home-width: min(1040px, calc(100vw - 64px));
 }
 
-#ambientRoot .ambient-item:hover {
-    background: var(--ui-preset-clickable-hover-surface);
-    transform: translateY(-1px);
-    border-color: var(--ui-preset-clickable-hover-border);
+body.dark {
+    --bg: #0a101b;
+    --bg-deep: #111f34;
+    --ink: #f7f2f1;
+    --muted: #a8b1c2;
+    --surface: rgba(13, 22, 38, 0.82);
+    --surface-strong: rgba(15, 25, 42, 0.95);
+    --ui-clickable-surface:
+        linear-gradient(180deg, rgba(29, 40, 60, 0.97), rgba(16, 26, 43, 0.96));
+    --ui-clickable-border: rgba(125, 174, 255, 0.14);
+    --ui-informative-surface:
+        linear-gradient(180deg, rgba(255, 255, 255, 0.025), rgba(255, 255, 255, 0.055));
+    background:
+        radial-gradient(circle at 16% 0%, rgba(83, 127, 218, 0.2), transparent 28%),
+        radial-gradient(circle at 90% 12%, rgba(208, 72, 76, 0.12), transparent 26%),
+        linear-gradient(180deg, #050b14 0%, #09121f 48%, #111a2b 100%);
 }
 
-#ambientRoot .ambient-item.cursor {
-    border-color: var(--ui-preset-active-border);
+.topbar,
+.hero,
+.module.active,
+.footer,
+.home-launchpad-main,
+.home-launchpad-tools,
+.rotanota-home-brand,
+.launchpad-card,
+.launchpad-tool,
+.icon-circle,
+.timezone-topbar,
+.style-menu,
+.style-option,
+.preset-chip,
+.pomodoro-presets,
+.today-flow-shell,
+.today-flow-card,
+#ambientRoot .ambient-mini,
+#ambientRoot .ambient-panel,
+.ambient-player,
+.ambient-panel-new {
+    clip-path: none !important;
 }
 
-#ambientRoot .ambient-item.playing {
-    background: var(--ui-preset-active-surface);
-    border-color: var(--ui-preset-active-border);
-    box-shadow: var(--ui-preset-active-shadow);
+.topbar,
+.hero,
+.module.active,
+.footer,
+.home-launchpad-main,
+.home-launchpad-tools,
+.rotanota-home-brand,
+.style-menu,
+.pomodoro-presets,
+.today-flow-shell,
+#ambientRoot .ambient-panel,
+.ambient-panel-new {
+    border-radius: var(--rn-radius-shell) !important;
 }
 
-#ambientRoot .ambient-item.playing::after {
-    content: "";
-    width: 10px;
-    height: 10px;
-    border-radius: 999px;
-    background: var(--accent);
-    justify-self: end;
+.launchpad-card,
+.launchpad-tool,
+.style-option,
+.preset-chip,
+.today-flow-card,
+.timezone-topbar,
+#ambientRoot .ambient-mini,
+.ambient-player,
+.ambient-sound,
+.youtube-controls {
+    border-radius: var(--rn-radius-card) !important;
 }
 
-#ambientRoot .ambient-item img {
-    width: 72px;
-    height: 44px;
-    border-radius: 0;
-    object-fit: cover;
-    clip-path: polygon(10px 0, calc(100% - 10px) 0, 100% 10px, 100% calc(100% - 10px), calc(100% - 10px) 100%, 10px 100%, 0 calc(100% - 10px), 0 10px);
+.icon-circle,
+.topbar-collapse-btn,
+.control-btn,
+.ambient-play,
+.ambient-fav,
+.yt-fav {
+    border-radius: var(--rn-radius-control) !important;
 }
 
-#ambientRoot .ambient-item-copy {
-    min-width: 0;
-    display: grid;
-    gap: 4px;
-    color: var(--ink);
+body[data-mode="clock"] .hero {
+    width: var(--rn-home-width);
+    min-height: min(720px, calc(100vh - 118px));
+    display: flex;
+    align-items: center;
+    justify-content: center;
+    padding: clamp(22px, 4vw, 42px);
+    overflow: visible;
 }
 
-#ambientRoot .ambient-title {
-    font-size: 0.86rem;
-    font-weight: 700;
-    line-height: 1.28;
-    color: #1c1720;
+body[data-mode="clock"] #pomodoroPresets,
+body[data-mode="clock"] .date-display,
+body[data-mode="clock"] .time-display,
+body[data-mode="clock"] #cycleProgress,
+body[data-mode="clock"] #todayFlow,
+body[data-mode="clock"] .controls,
+body[data-mode="clock"] .main-stats-row,
+body[data-mode="clock"] #streakVisual,
+body[data-mode="clock"] #dailyGoal,
+body[data-mode="clock"] #weeklyChart,
+body[data-mode="clock"] #sessionLog,
+body[data-mode="clock"] #advancedStats,
+body[data-mode="clock"] #achievements,
+body[data-mode="clock"] #progressiveBars {
+    display: none !important;
 }
 
-#ambientRoot .ambient-item-meta {
-    color: #716979;
+#homeLaunchpad {
+    width: 100%;
+    max-width: 980px;
+    margin: 0 auto;
 }
 
-#ambientRoot .ambient-item.playing .ambient-title {
-    color: #12305f;
+body:not([data-mode="clock"]) #homeLaunchpad {
+    display: none !important;
 }
 
-#ambientRoot .fav-btn {
-    width: 28px;
-    height: 28px;
-    border-radius: 0;
-    border: 1px solid var(--ui-clickable-border);
-    background: var(--ui-clickable-surface);
-    color: var(--muted);
-    clip-path: polygon(8px 0, calc(100% - 8px) 0, 100% 8px, 100% calc(100% - 8px), calc(100% - 8px) 100%, 8px 100%, 0 calc(100% - 8px), 0 8px);
+.rotanota-home-brand {
+    width: 100%;
+    margin: 0 auto 12px;
+    padding: clamp(18px, 2.6vw, 28px);
+    grid-template-columns: auto minmax(0, 1fr);
+    gap: clamp(14px, 2vw, 22px);
 }
 
-#ambientRoot .fav-btn.active {
-    background: var(--ui-preset-active-surface);
-    color: var(--ui-preset-active-text);
-    border-color: var(--ui-preset-active-border);
+.rotanota-home-brand img {
+    width: clamp(54px, 6vw, 74px);
+    height: clamp(54px, 6vw, 74px);
 }
 
-#ambientRoot .ambient-empty {
-    padding: 18px;
-    border-radius: 0;
-    background: var(--ui-display-surface);
-    color: var(--muted);
-    text-align: center;
-    clip-path: polygon(14px 0, calc(100% - 14px) 0, 100% 14px, 100% calc(100% - 14px), calc(100% - 14px) 100%, 14px 100%, 0 calc(100% - 14px), 0 14px);
+.rotanota-home-brand span {
+    font-size: clamp(1.9rem, 3.2vw, 3.35rem);
 }
 
-#ambientHelp,
-#ambientBlocked {
-    position: fixed;
-    inset: 0;
-    display: flex;
-    align-items: center;
-    justify-content: center;
-    padding: 20px;
-    background: rgba(19, 24, 34, 0.36);
-    backdrop-filter: blur(10px);
-    z-index: 2200;
+.rotanota-home-brand strong {
+    font-size: clamp(0.96rem, 1.2vw, 1.16rem);
 }
 
-.ambient-help-box,
-.ambient-blocked-box,
-#ambientRoot .ambient-help-box {
-    background: var(--surface-strong);
-    color: var(--ink);
-    border: 1px solid var(--line);
-    border-radius: 0;
-    box-shadow: var(--shadow);
-    width: min(460px, calc(100vw - 28px));
-    max-height: min(78vh, 640px);
-    overflow: auto;
-    position: relative;
-    padding: 24px 22px 22px;
-    clip-path: polygon(16px 0, calc(100% - 16px) 0, 100% 16px, 100% calc(100% - 16px), calc(100% - 16px) 100%, 16px 100%, 0 calc(100% - 16px), 0 16px);
+.rotanota-home-brand p {
+    font-size: 0.88rem;
 }
 
-#ambientHelpClose,
-#ambientBlockedClose,
-#ambientRoot #ambientHelpClose {
-    color: var(--ink);
-    position: absolute;
-    top: 12px;
-    right: 12px;
-    width: 36px;
-    height: 36px;
-    border-radius: 0;
-    border: 1px solid var(--line);
-    background: var(--surface-strong);
-    box-shadow: var(--shadow-soft);
-    clip-path: polygon(10px 0, calc(100% - 10px) 0, 100% 10px, 100% calc(100% - 10px), calc(100% - 10px) 100%, 10px 100%, 0 calc(100% - 10px), 0 10px);
+.home-launchpad-main,
+.home-launchpad-tools {
+    width: 100%;
+    padding: clamp(14px, 2vw, 20px);
 }
 
-.ambient-help-box h3,
-.ambient-blocked-header h3 {
-    margin: 0 0 8px;
-    font-size: 1.1rem;
-    font-weight: 800;
-    letter-spacing: -0.03em;
+.launchpad-primary-grid {
+    width: 100%;
+    grid-template-columns: repeat(4, minmax(128px, 1fr));
+    gap: 10px;
 }
 
-.ambient-help-box div,
-.ambient-blocked-header p {
-    color: var(--muted);
+.launchpad-card {
+    min-height: 74px;
+    padding: 16px 14px;
 }
 
-.ambient-blocked-kicker {
-    margin-bottom: 8px;
-    font-size: 0.72rem;
-    font-weight: 800;
-    letter-spacing: 0.14em;
-    text-transform: uppercase;
-    color: var(--muted);
+.launchpad-card-title {
+    font-size: clamp(0.92rem, 1vw, 1rem);
 }
 
-.ambient-blocked-header p {
-    margin: 0;
-    line-height: 1.55;
+.home-launchpad-tools {
+    margin-top: 10px;
 }
 
-.ambient-blocked-list {
-    margin-top: 16px;
+.launchpad-tool-row {
+    width: 100%;
     display: grid;
+    grid-template-columns: repeat(4, minmax(112px, 1fr));
     gap: 10px;
 }
 
-.ambient-blocked-item,
-.ambient-blocked-empty {
-    padding: 14px 15px;
-    border-radius: 18px;
-    background: rgba(255, 255, 255, 0.46);
-    border: 1px solid rgba(255, 255, 255, 0.38);
+.launchpad-tool {
+    min-height: 38px;
+    padding: 0 12px;
+    font-size: 0.82rem;
 }
 
-.ambient-blocked-meta {
-    margin-bottom: 4px;
-    font-size: 0.7rem;
-    font-weight: 800;
-    letter-spacing: 0.14em;
-    text-transform: uppercase;
-    color: var(--muted);
+body[data-mode="pomodoro"] .hero {
+    width: min(var(--content-width), calc(1160px * var(--site-scale, 1)));
+    min-height: min(760px, calc(100vh - 122px));
+    grid-template-columns: repeat(12, minmax(0, 1fr));
+    align-content: start;
+    row-gap: 14px;
+    padding: clamp(22px, 3vw, 34px);
+    margin-left: auto;
+    margin-right: auto;
 }
 
-.ambient-blocked-title {
-    font-size: 0.98rem;
-    font-weight: 700;
-    line-height: 1.35;
+body[data-mode="pomodoro"] #pomodoroPresets,
+body[data-mode="pomodoro"] .date-display,
+body[data-mode="pomodoro"] .time-display,
+body[data-mode="pomodoro"] #cycleProgress,
+body[data-mode="pomodoro"] #todayFlow,
+body[data-mode="pomodoro"] .controls,
+body[data-mode="pomodoro"] .main-stats-row,
+body[data-mode="pomodoro"] #streakVisual,
+body[data-mode="pomodoro"] #dailyGoal,
+body[data-mode="pomodoro"] #weeklyChart,
+body[data-mode="pomodoro"] #sessionLog,
+body[data-mode="pomodoro"] #advancedStats,
+body[data-mode="pomodoro"] #achievements,
+body[data-mode="pomodoro"] #progressiveBars {
+    grid-column: 1 / -1;
 }
 
-.ambient-blocked-id {
-    margin-top: 8px;
-    font-family: "Space Grotesk", sans-serif;
-    font-size: 0.8rem;
-    letter-spacing: 0.04em;
-    color: var(--muted);
-    word-break: break-all;
+body[data-mode="pomodoro"] .time-display {
+    display: block;
+    max-width: 960px;
+    margin-inline: auto;
 }
 
-body.dark .ambient-blocked-item,
-body.dark .ambient-blocked-empty {
-    background: rgba(255, 255, 255, 0.05);
-    border-color: rgba(255, 255, 255, 0.08);
+body[data-mode="pomodoro"] .controls {
+    display: flex;
 }
 
-body.dark #ambientRoot .ambient-item,
-body.dark #ambientRoot .fav-btn {
-    background: var(--ui-display-surface);
-    border-color: var(--ui-clickable-border);
+body[data-mode="pomodoro"] .main-stats-row {
+    width: min(100%, 900px);
+    margin-inline: auto;
 }
 
-body.dark #ambientRoot .ambient-item:hover {
-    background: var(--ui-preset-clickable-hover-surface);
-    border-color: var(--ui-preset-clickable-hover-border);
+.pomodoro-presets-title span {
+    letter-spacing: 0.12em;
 }
 
-body.dark #ambientRoot .ambient-panel {
-    background:
-        linear-gradient(180deg, rgba(18, 24, 37, 0.98), rgba(14, 19, 30, 0.92)) !important;
+.topbar {
+    overflow: visible;
 }
 
-body.dark #ambientRoot .ambient-title {
-    color: #f3f6fb;
+.rotanota-topbar-brand {
+    border-radius: var(--rn-radius-card);
+    clip-path: none;
 }
 
-body.dark #ambientRoot .ambient-item-meta {
-    color: #96a3b6;
-}
+@media (max-width: 980px) {
+    body[data-mode="clock"] .hero {
+        width: min(100%, calc(100vw - 28px));
+        min-height: auto;
+        align-items: flex-start;
+    }
 
-body.dark #ambientRoot .ambient-item.playing {
-    background: var(--ui-preset-active-surface);
-    border-color: var(--ui-preset-active-border);
-    box-shadow: var(--ui-preset-active-shadow);
-}
+    .launchpad-primary-grid {
+        grid-template-columns: repeat(2, minmax(0, 1fr));
+    }
 
-body.dark #ambientRoot .ambient-item.playing .ambient-title {
-    color: var(--ui-preset-active-text);
-}
+    .home-shortcuts-grid {
+        grid-template-columns: repeat(2, minmax(0, 1fr));
+    }
 
-body.dark #ambientRoot .ambient-mini-icon,
-body.dark #ambientRoot .ambient-primary-control,
-body.dark #ambientRoot .ambient-secondary-control,
-body.dark #ambientRoot .ambient-header-actions button {
-    background: var(--surface-strong);
-    color: var(--ink);
-}
+    .launchpad-tool-row {
+        grid-template-columns: repeat(2, minmax(0, 1fr));
+    }
 
-body.dark #ambientRoot .ambient-peek {
-    background: var(--glass);
-    color: var(--ink);
+    body[data-mode="pomodoro"] #pomodoroPresets,
+    body[data-mode="pomodoro"] .date-display,
+    body[data-mode="pomodoro"] .time-display,
+    body[data-mode="pomodoro"] #cycleProgress,
+    body[data-mode="pomodoro"] #todayFlow,
+    body[data-mode="pomodoro"] .controls,
+    body[data-mode="pomodoro"] .main-stats-row,
+    body[data-mode="pomodoro"] #streakVisual,
+    body[data-mode="pomodoro"] #dailyGoal,
+    body[data-mode="pomodoro"] #weeklyChart,
+    body[data-mode="pomodoro"] #sessionLog,
+    body[data-mode="pomodoro"] #advancedStats,
+    body[data-mode="pomodoro"] #achievements,
+    body[data-mode="pomodoro"] #progressiveBars {
+        grid-column: 1 / -1;
+    }
 }
 
-body.focus-mode #ambientRoot {
-    display: block !important;
-}
+@media (max-width: 620px) {
+    :root {
+        --rn-radius-shell: 12px;
+        --rn-radius-card: 9px;
+    }
 
-body.focus-mode .topbar,
-body.focus-mode .footer,
-body.focus-mode .side-modules,
-body.focus-mode #moduleArea,
-body.focus-mode #utilityRoot,
-body.focus-mode #miniAnalogClock,
-body.focus-mode #customModal {
-    display: none !important;
-}
+    .rotanota-home-brand {
+        grid-template-columns: 1fr;
+        justify-items: start;
+        text-align: left;
+    }
 
-body.focus-mode #ambientRoot .ambient-peek {
-    left: 18px;
-    bottom: 18px;
-    min-width: 72px;
-    height: 40px;
-    padding: 0 14px;
-    border-radius: 999px;
-    background: rgba(20, 27, 40, 0.42);
-    border-color: rgba(255, 255, 255, 0.22);
-    box-shadow: 0 12px 28px rgba(5, 10, 18, 0.24);
-    gap: 10px;
-}
+    .launchpad-primary-grid,
+    .launchpad-tool-row {
+        grid-template-columns: 1fr;
+    }
 
-body.focus-mode #ambientRoot .ambient-peek.is-visible {
-    transform: none;
-}
+    .home-shortcuts-grid {
+        grid-template-columns: 1fr;
+    }
 
-body.focus-mode #ambientRoot .ambient-peek-label {
-    display: inline;
-    font-size: 0.74rem;
-    letter-spacing: 0.14em;
-    color: rgba(243, 247, 255, 0.86);
+    .launchpad-card {
+        min-height: 58px;
+    }
 }
 
-body.focus-mode #ambientRoot .ambient-peek-dot {
-    width: 10px;
-    height: 10px;
-    background: rgba(255, 255, 255, 0.74);
+/* Topbar responsive correction: keep logo and controls from squeezing. */
+.topbar-left {
+    flex: 1 1 auto;
+    min-width: 0;
 }
 
-body.focus-mode #ambientRoot .ambient-peek.is-playing .ambient-peek-dot {
-    background: #f4f7ff;
-    box-shadow: 0 0 0 4px rgba(255, 255, 255, 0.12);
+.topbar-right {
+    flex: 0 0 auto;
+    flex-wrap: nowrap;
 }
 
-body.focus-mode #ambientRoot .ambient-mini,
-body.focus-mode #ambientRoot .ambient-panel {
-    background:
-        linear-gradient(180deg, rgba(14, 21, 33, 0.84), rgba(9, 14, 23, 0.74)) !important;
-    border-color: rgba(122, 146, 189, 0.18) !important;
-    color: #f3f7ff !important;
+.rotanota-topbar-brand {
+    flex: 0 1 220px;
+    max-width: min(220px, 26vw);
 }
 
-body.focus-mode[data-period="morning"] #ambientRoot .ambient-mini,
-body.focus-mode[data-period="morning"] #ambientRoot .ambient-panel {
-    background:
-        linear-gradient(180deg, rgba(69, 56, 44, 0.54), rgba(41, 33, 26, 0.48)) !important;
-    border-color: rgba(241, 212, 179, 0.18) !important;
-}
+@media (max-width: 980px) {
+    .topbar {
+        gap: 8px;
+        padding-left: 12px;
+    }
 
-body.focus-mode[data-period="afternoon"] #ambientRoot .ambient-mini,
-body.focus-mode[data-period="afternoon"] #ambientRoot .ambient-panel {
-    background:
-        linear-gradient(180deg, rgba(71, 46, 42, 0.56), rgba(40, 26, 25, 0.5)) !important;
-    border-color: rgba(235, 187, 173, 0.18) !important;
+    .topbar-left,
+    .topbar-right {
+        gap: 8px;
+    }
+
+    .rotanota-topbar-brand {
+        max-width: 162px;
+        padding: 5px 9px 5px 5px;
+    }
+
+    .timezone-topbar {
+        width: clamp(142px, 24vw, 190px);
+        min-width: 0;
+        margin-left: 0;
+    }
+
+    .icon-circle {
+        width: 38px;
+        height: 38px;
+    }
 }
 
-body.focus-mode[data-period="morning"] #ambientRoot .ambient-peek {
-    background: rgba(88, 68, 48, 0.48);
-    border-color: rgba(245, 219, 188, 0.22);
-}
+@media (max-width: 760px) {
+    .topbar {
+        display: flex !important;
+        flex-direction: row !important;
+        align-items: center !important;
+        min-height: 58px;
+        padding: 10px 46px 10px 10px !important;
+    }
+
+    .topbar-left {
+        display: grid;
+        grid-template-columns: auto;
+        align-items: center;
+        gap: 8px;
+        flex: 1 1 auto;
+    }
+
+    .topbar-right {
+        display: flex;
+        gap: 6px;
+        flex: 0 0 auto;
+    }
+
+    .rotanota-topbar-brand {
+        width: 42px;
+        max-width: 42px;
+        height: 38px;
+        padding: 4px;
+        justify-content: center;
+    }
+
+    .timezone-topbar {
+        width: 100%;
+        min-width: 118px;
+        max-width: 190px;
+    }
 
-body.focus-mode[data-period="afternoon"] #ambientRoot .ambient-peek {
-    background: rgba(89, 55, 47, 0.5);
-    border-color: rgba(238, 190, 176, 0.22);
-}
+    .icon-circle {
+        width: 34px;
+        height: 34px;
+        font-size: 0.88rem;
+    }
 
-body.focus-mode[data-period="night"] #ambientRoot .ambient-peek {
-    background: rgba(18, 29, 45, 0.58);
-    border-color: rgba(122, 146, 189, 0.2);
+    .topbar-collapse-btn {
+        top: 50%;
+        right: 10px;
+        width: 30px;
+        height: 36px;
+        transform: translateY(-50%);
+    }
 }
 
-body.pomodoro-table-sync-on .topbar,
-body.pomodoro-table-sync-on .footer,
-body.pomodoro-table-sync-on .side-modules,
-body.pomodoro-table-sync-on #ambientRoot,
-body.pomodoro-table-sync-on #miniAnalogClock,
-body.pomodoro-table-sync-on #pomodoroPresets,
-body.pomodoro-table-sync-on #cycleProgress,
-body.pomodoro-table-sync-on #progressiveBars,
-body.pomodoro-table-sync-on .controls,
-body.pomodoro-table-sync-on .main-stats-row,
-body.pomodoro-table-sync-on #streakVisual,
-body.pomodoro-table-sync-on #dailyGoal,
-body.pomodoro-table-sync-on #weeklyChart,
-body.pomodoro-table-sync-on #sessionLog,
-body.pomodoro-table-sync-on #advancedStats,
-body.pomodoro-table-sync-on #achievements,
-body.pomodoro-table-sync-on #homeLaunchpad {
-    opacity: 0.26;
-    filter: saturate(0.72);
-    transition: opacity 0.2s ease, filter 0.2s ease;
-}
+@media (max-width: 560px) {
+    .topbar {
+        min-height: 54px;
+        padding-right: 42px !important;
+    }
 
-body.pomodoro-table-sync-on .date-display,
-body.pomodoro-table-sync-on .time-display,
-body.pomodoro-table-sync-on #todayFlow {
-    position: relative;
-    z-index: 3;
-    opacity: 1;
-    filter: none;
-}
+    .topbar-left {
+        grid-template-columns: auto;
+        gap: 6px;
+    }
 
-body.pomodoro-table-sync-on .date-display {
-    margin-bottom: -2px;
-}
+    .topbar-right {
+        gap: 5px;
+    }
 
-body.pomodoro-table-sync-on .time-display {
-    transform: scale(1.012);
-    box-shadow: 0 26px 54px rgba(183, 215, 240, 0.16);
-    border-color: rgba(183, 215, 240, 0.22);
-}
+    .timezone-topbar {
+        min-width: 92px;
+        padding-inline: 8px;
+        font-size: 0.72rem;
+    }
 
-body.pomodoro-table-sync-on #todayFlow {
-    margin-top: -20px;
+    .icon-circle {
+        width: 31px;
+        height: 31px;
+    }
 }
 
-body.pomodoro-table-sync-on .today-flow-shell.is-linked {
-    position: relative;
-    padding: 18px 20px 20px;
-    border-radius: 0;
-    background: var(--ui-informative-surface);
-    border-color: transparent;
-    box-shadow: 0 22px 46px rgba(183, 215, 240, 0.12);
-    clip-path: polygon(var(--ui-cut-lg) 0, calc(100% - var(--ui-cut-lg)) 0, 100% var(--ui-cut-lg), 100% calc(100% - var(--ui-cut-lg)), calc(100% - var(--ui-cut-lg)) 100%, var(--ui-cut-lg) 100%, 0 calc(100% - var(--ui-cut-lg)), 0 var(--ui-cut-lg));
-}
+/* =====================================================
+   RotaNota brand family - icon + lockups
+===================================================== */
 
-body.pomodoro-table-sync-on .today-flow-shell.is-linked::before {
-    content: "";
-    position: absolute;
-    inset: 1px 1px auto;
-    height: 34%;
-    border-radius: 0;
-    background: linear-gradient(180deg, rgba(255, 255, 255, 0.24), rgba(255, 255, 255, 0));
-    pointer-events: none;
+.rotanota-topbar-brand {
+    appearance: none;
+    display: inline-flex;
+    align-items: center;
+    justify-content: center;
+    gap: 0;
+    min-width: 0;
+    padding: 6px 10px;
+    border: 1px solid rgba(56, 175, 255, 0.18);
+    background:
+        linear-gradient(180deg, rgba(255, 255, 255, 0.84), rgba(246, 249, 255, 0.72)),
+        radial-gradient(circle at top left, rgba(68, 180, 255, 0.14), transparent 54%);
+    box-shadow:
+        inset 0 1px 0 rgba(255, 255, 255, 0.78),
+        0 10px 24px rgba(17, 34, 66, 0.08);
+    color: inherit;
+    cursor: pointer;
+    font: inherit;
+    text-decoration: none;
+    transition: transform 0.18s ease, border-color 0.18s ease, box-shadow 0.18s ease, max-width 0.18s ease;
 }
 
-body.pomodoro-table-sync-on .today-flow-shell.is-linked .today-flow-track.is-live .today-flow-slot-card.is-current {
-    box-shadow: 0 18px 34px rgba(183, 215, 240, 0.14);
+.rotanota-topbar-brand:hover {
+    transform: translateY(-1px);
+    border-color: rgba(56, 175, 255, 0.3);
 }
 
-body.dark.pomodoro-table-sync-on .time-display {
-    box-shadow: 0 28px 62px rgba(7, 14, 26, 0.42);
+.rotanota-topbar-brand:focus-visible {
+    outline: 2px solid rgba(37, 88, 240, 0.38);
+    outline-offset: 3px;
 }
 
-body.dark.pomodoro-table-sync-on .today-flow-shell.is-linked {
+body.dark .rotanota-topbar-brand {
+    border-color: rgba(77, 184, 255, 0.16);
     background:
-        linear-gradient(180deg, rgba(16, 26, 40, 0.92), rgba(11, 18, 31, 0.88));
-    border-color: rgba(108, 146, 232, 0.18);
-    box-shadow: 0 26px 52px rgba(0, 0, 0, 0.34);
+        linear-gradient(180deg, rgba(12, 19, 32, 0.96), rgba(8, 14, 26, 0.92)),
+        radial-gradient(circle at top left, rgba(52, 144, 255, 0.18), transparent 52%);
+    box-shadow:
+        inset 0 1px 0 rgba(255, 255, 255, 0.04),
+        0 12px 26px rgba(0, 0, 0, 0.24);
 }
 
-body.dark[data-style="immersion"].pomodoro-table-sync-on .today-flow-shell.is-linked {
-    background:
-        linear-gradient(180deg, rgba(15, 31, 50, 0.94), rgba(9, 20, 35, 0.9));
-    border-color: rgba(106, 150, 245, 0.18);
+.rotanota-topbar-mark,
+.rotanota-topbar-logo {
+    display: block;
 }
 
-body.dark[data-style="pulse"].pomodoro-table-sync-on .today-flow-shell.is-linked {
-    background:
-        linear-gradient(180deg, rgba(39, 23, 28, 0.94), rgba(24, 16, 20, 0.9));
-    border-color: rgba(213, 138, 112, 0.16);
+.rotanota-topbar-mark {
+    display: none;
+    width: 28px;
+    height: 28px;
+    flex: 0 0 auto;
 }
 
-body.focus-mode #ambientRoot .ambient-mini-icon,
-body.focus-mode #ambientRoot .ambient-secondary-control,
-body.focus-mode #ambientRoot .ambient-header-actions button {
-    background: rgba(255, 255, 255, 0.08);
-    border-color: rgba(255, 255, 255, 0.1);
-    color: #f3f7ff;
+.rotanota-topbar-logo {
+    width: auto;
+    height: 30px;
+    max-width: min(176px, 22vw);
 }
 
-body.focus-mode #ambientRoot .ambient-title,
-body.focus-mode #ambientRoot .ambient-panel-title,
-body.focus-mode #ambientRoot .ambient-header-copy,
-body.focus-mode #ambientRoot .ambient-item-copy {
-    color: #f3f7ff;
+.rotanota-topbar-logo-dark {
+    display: none;
 }
 
-body.focus-mode #ambientRoot .ambient-item {
-    background: linear-gradient(135deg, rgba(28, 37, 54, 0.92), rgba(16, 23, 35, 0.88));
-    border-color: rgba(255, 255, 255, 0.08);
+body.dark .rotanota-topbar-logo-light {
+    display: none;
 }
 
-body.focus-mode #ambientRoot .ambient-item.playing {
-    background: linear-gradient(135deg, rgba(34, 56, 96, 0.92), rgba(20, 31, 53, 0.96));
+body.dark .rotanota-topbar-logo-dark {
+    display: block;
 }
 
-body[data-mode="clock"] #ambientRoot .ambient-mini {
-    width: min(clamp(232px, 21vw, 320px), calc(100vw - 32px));
+.rotanota-home-brand {
+    width: 100%;
+    margin: 0 auto 12px;
+    padding: clamp(20px, 2.8vw, 30px);
+    grid-template-columns: minmax(0, 1fr);
+    gap: 12px;
+    background:
+        radial-gradient(circle at 8% 10%, rgba(49, 150, 255, 0.18), transparent 24%),
+        linear-gradient(180deg, rgba(11, 18, 30, 0.98), rgba(8, 14, 24, 0.96));
+    border: 1px solid rgba(70, 177, 255, 0.14);
+    box-shadow: 0 22px 42px rgba(4, 9, 18, 0.28);
 }
 
-#shortcutHint {
-    position: fixed;
-    right: 22px;
-    bottom: 24px;
-    z-index: 1600;
-    width: min(420px, calc(100vw - 28px));
-    padding: 12px 14px;
-    border-radius: 18px;
-    border: 1px solid var(--line);
-    background: var(--glass-strong);
-    color: var(--ink);
-    box-shadow: var(--shadow-soft);
-    backdrop-filter: blur(18px);
-    opacity: 0;
-    pointer-events: none;
-    transform: translateY(10px);
-    transition: opacity 0.22s ease, transform 0.22s ease;
+.rotanota-home-lockup {
+    display: flex;
+    align-items: center;
+    justify-content: flex-start;
 }
 
-#shortcutHint.is-visible {
-    opacity: 1;
-    pointer-events: auto;
-    transform: translateY(0);
+.rotanota-home-brand .rotanota-home-logo {
+    width: clamp(232px, 32vw, 338px);
+    height: auto;
+    filter: drop-shadow(0 12px 24px rgba(14, 121, 255, 0.16));
 }
 
-.shortcut-hint-eyebrow {
-    margin-bottom: 8px;
-    font-size: 0.68rem;
-    font-weight: 800;
-    letter-spacing: 0.14em;
-    text-transform: uppercase;
-    color: var(--muted);
+.rotanota-home-brand div {
+    display: grid;
+    gap: 6px;
 }
 
-.shortcut-hint-row {
-    display: flex;
-    flex-wrap: wrap;
-    gap: 8px;
+.rotanota-home-brand strong {
+    max-width: 620px;
+    color: rgba(243, 247, 252, 0.96);
+    font-size: clamp(0.98rem, 1.26vw, 1.18rem);
+    line-height: 1.28;
+    letter-spacing: -0.02em;
 }
 
-.shortcut-hint-item {
-    display: inline-flex;
-    align-items: center;
-    gap: 8px;
-    min-height: 38px;
-    padding: 0 12px;
-    border-radius: 999px;
-    background: rgba(255, 255, 255, 0.64);
-    border: 1px solid rgba(255, 255, 255, 0.46);
-    color: var(--ink);
+.rotanota-home-brand p {
+    max-width: 560px;
+    margin: 0;
+    color: rgba(194, 207, 226, 0.8);
+    font-size: 0.9rem;
+    line-height: 1.48;
 }
 
-.shortcut-hint-item span {
-    font-size: 0.82rem;
-    font-weight: 700;
+body:not(.dark) .rotanota-home-brand {
+    color: #f1f5fa;
 }
 
-.shortcut-hint-item kbd {
-    min-width: 52px;
-    padding: 6px 8px;
-    border-radius: 10px;
-    border: 1px solid rgba(42, 33, 53, 0.12);
-    background: rgba(255, 255, 255, 0.92);
-    box-shadow: inset 0 -1px 0 rgba(42, 33, 53, 0.06);
-    font-family: "Space Grotesk", sans-serif;
-    font-size: 0.74rem;
-    font-weight: 700;
-    text-align: center;
-}
+@media (max-width: 980px) {
+    .rotanota-topbar-brand {
+        padding: 5px 8px;
+    }
 
-body.dark #shortcutHint {
-    background: rgba(15, 22, 34, 0.88);
-    border-color: rgba(255, 255, 255, 0.08);
-    color: var(--ink);
+    .rotanota-topbar-logo {
+        height: 28px;
+        max-width: 148px;
+    }
 }
 
-body.dark .shortcut-hint-item {
-    background: rgba(255, 255, 255, 0.06);
-    border-color: rgba(255, 255, 255, 0.08);
-}
+@media (max-width: 760px) {
+    .rotanota-topbar-brand {
+        width: 40px;
+        max-width: 40px;
+        height: 38px;
+        padding: 4px;
+        border-radius: var(--rn-radius-control);
+    }
 
-body.dark .shortcut-hint-item kbd {
-    background: rgba(255, 255, 255, 0.08);
-    border-color: rgba(255, 255, 255, 0.08);
-    box-shadow: inset 0 -1px 0 rgba(255, 255, 255, 0.04);
-    color: var(--ink);
-}
+    .rotanota-topbar-logo {
+        display: none !important;
+    }
 
-@keyframes clockBreath {
-    0% {
-        transform: scale(1);
-        box-shadow: 0 36px 92px rgba(15, 20, 30, 0.22);
+    .rotanota-topbar-mark {
+        display: block;
+        width: 26px;
+        height: 26px;
     }
 
-    50% {
-        transform: scale(1.018);
-        box-shadow: 0 44px 116px rgba(15, 20, 30, 0.28);
+    .rotanota-home-brand {
+        padding: 18px;
+    }
+
+    .rotanota-home-brand .rotanota-home-logo {
+        width: min(270px, 82vw);
     }
+}
+
+/* =====================================================
+   Compact agenda - quicker calendar, less vertical weight
+===================================================== */
 
-    100% {
-        transform: scale(1);
-        box-shadow: 0 36px 92px rgba(15, 20, 30, 0.22);
-    }
+#calendarModule.module.active {
+    padding: clamp(18px, 2vw, 24px);
 }
 
-body.focus-mode {
-    padding: 0 !important;
-    min-height: 100vh;
-    overflow: hidden !important;
+#calendarModule .planner-shell {
+    gap: 10px;
 }
 
-body.focus-mode {
-    background: linear-gradient(180deg, #eff4ff 0%, #e9edf7 100%) !important;
+#calendarModule .planner-head {
+    padding: 14px 18px;
+    min-height: auto;
 }
 
-body.focus-mode[data-period="morning"] {
-    background:
-        radial-gradient(circle at 14% 18%, rgba(255, 186, 114, 0.24), transparent 28%),
-        radial-gradient(circle at 86% 22%, rgba(117, 165, 255, 0.22), transparent 28%),
-        linear-gradient(180deg, #fff6ee 0%, #eef2fb 100%) !important;
+#calendarModule .planner-head-copy {
+    gap: 2px;
 }
 
-body.focus-mode[data-period="afternoon"] {
-    background:
-        radial-gradient(circle at 14% 18%, rgba(243, 108, 79, 0.22), transparent 28%),
-        radial-gradient(circle at 86% 22%, rgba(255, 208, 138, 0.18), transparent 28%),
-        linear-gradient(180deg, #fff2eb 0%, #f2edf7 100%) !important;
+#calendarModule .planner-kicker {
+    font-size: 0.62rem;
+    letter-spacing: 0.14em;
 }
 
-body.focus-mode[data-period="night"] {
-    background:
-        radial-gradient(circle at 14% 18%, rgba(77, 125, 255, 0.26), transparent 28%),
-        radial-gradient(circle at 86% 20%, rgba(32, 83, 193, 0.18), transparent 32%),
-        linear-gradient(180deg, #08111f 0%, #0b1525 100%) !important;
+#calendarModule .planner-head h2 {
+    font-size: clamp(1.16rem, 1.5vw, 1.45rem);
 }
 
-body.focus-mode::before,
-body.focus-mode::after {
-    opacity: 0.62;
+#calendarModule .planner-head p {
+    font-size: 0.86rem;
 }
 
-body.focus-mode #hero {
-    position: fixed !important;
-    inset: 0 !important;
-    width: 100vw !important;
-    max-width: none !important;
-    height: 100vh !important;
-    min-height: 100vh !important;
-    margin: 0 !important;
-    padding: clamp(24px, 4vw, 42px) !important;
-    border-radius: 0 !important;
-    background: transparent !important;
-    border: none !important;
-    box-shadow: none !important;
-    display: grid !important;
-    grid-template-columns: minmax(0, 1fr) !important;
-    place-items: center !important;
-    justify-items: center !important;
-    align-items: center !important;
-    overflow: hidden !important;
+#calendarModule .planner-head-actions {
+    gap: 8px;
 }
 
-body.focus-mode #hero::before,
-body.focus-mode #hero::after {
-    display: none !important;
+#calendarModule .planner-head-actions button,
+#calendarModule .planner-section-head button {
+    min-height: 32px;
+    padding: 0 12px;
+    font-size: 0.78rem;
 }
 
-body.focus-mode #hero > :not(.time-display) {
-    display: none !important;
+#calendarModule .planner-month-nav {
+    min-height: 32px;
+    padding: 0 2px;
 }
 
-body.focus-mode .time-display {
-    grid-column: 1 / -1 !important;
-    grid-row: 1 !important;
-    justify-self: center !important;
-    align-self: center !important;
-    display: flex !important;
-    align-items: center !important;
-    justify-content: center !important;
-    width: min(82vw, 1040px) !important;
-    max-width: calc(100vw - 72px) !important;
-    min-height: clamp(220px, 34vh, 380px) !important;
-    margin: 0 auto !important;
-    padding: clamp(24px, 4.6vw, 52px) !important;
-    border-radius: 40px !important;
-    font-size: clamp(5rem, 14vw, 11.2rem) !important;
-    line-height: 0.9 !important;
-    letter-spacing: -0.1em !important;
-    text-align: center !important;
-    transform-origin: center !important;
-    overflow: hidden !important;
-    border-width: 1px !important;
-    animation: clockBreath 7s ease-in-out infinite !important;
-    box-shadow: 0 36px 92px rgba(15, 20, 30, 0.22) !important;
+#calendarModule .planner-month-label {
+    font-size: 0.86rem;
 }
 
-body.focus-mode .time-display::before {
-    inset: -78px !important;
+#calendarModule .planner-month-nav button {
+    width: 32px;
+    height: 32px;
+    font-size: 0.86rem;
 }
 
-body.focus-mode[data-period="morning"] .time-display {
-    background:
-        linear-gradient(135deg, rgba(245, 236, 223, 0.92), rgba(224, 208, 188, 0.78)),
-        radial-gradient(circle at 12% 10%, rgba(213, 164, 107, 0.24), transparent 34%) !important;
-    color: #1d1712 !important;
-    border-color: rgba(255, 243, 225, 0.62) !important;
+#calendarModule .calendar-weekdays,
+#calendarModule .calendar-grid {
+    gap: 5px;
 }
 
-body.focus-mode[data-period="afternoon"] .time-display {
-    background:
-        linear-gradient(135deg, rgba(243, 231, 226, 0.92), rgba(223, 202, 195, 0.8)),
-        radial-gradient(circle at 12% 10%, rgba(196, 114, 89, 0.24), transparent 34%) !important;
-    color: #211716 !important;
-    border-color: rgba(246, 227, 222, 0.62) !important;
+#calendarModule .weekday {
+    min-height: 20px;
+    font-size: 0.72rem;
 }
 
-body.focus-mode[data-period="night"] .time-display {
-    background:
-        linear-gradient(135deg, rgba(16, 25, 39, 0.97), rgba(8, 13, 23, 0.92)),
-        radial-gradient(circle at 12% 10%, rgba(88, 131, 255, 0.24), transparent 34%) !important;
-    color: #f7f9ff !important;
-    border-color: rgba(114, 149, 255, 0.24) !important;
+#calendarModule .calendar-day {
+    min-height: clamp(50px, 7vh, 62px);
+    padding: 7px 9px;
+    gap: 3px;
+    clip-path: polygon(9px 0, calc(100% - 9px) 0, 100% 9px, 100% calc(100% - 9px), calc(100% - 9px) 100%, 9px 100%, 0 calc(100% - 9px), 0 9px);
 }
 
-body.focus-mode[data-period="morning"] .time-display::before {
-    background: radial-gradient(circle, rgba(255, 183, 112, 0.28) 0%, rgba(255, 183, 112, 0.06) 48%, transparent 76%) !important;
+#calendarModule .calendar-day:hover {
+    transform: translateY(-1px);
+    box-shadow: 0 10px 20px rgba(33, 25, 40, 0.08);
 }
 
-body.focus-mode[data-period="afternoon"] .time-display::before {
-    background: radial-gradient(circle, rgba(243, 108, 79, 0.26) 0%, rgba(243, 108, 79, 0.06) 48%, transparent 76%) !important;
+#calendarModule .calendar-day-number {
+    font-size: 0.9rem;
 }
 
-body.focus-mode[data-period="night"] .time-display::before {
-    background: radial-gradient(circle, rgba(88, 131, 255, 0.28) 0%, rgba(88, 131, 255, 0.06) 48%, transparent 76%) !important;
+#calendarModule .calendar-day-preview {
+    gap: 1px;
 }
 
-body.focus-mode #shortcutHint {
-    left: 50%;
-    right: auto;
-    bottom: 22px;
-    width: auto;
-    max-width: calc(100vw - 24px);
-    transform: translate(-50%, 10px);
+#calendarModule .calendar-day-preview-time {
+    font-size: 0.56rem;
+    letter-spacing: 0.08em;
 }
 
-body.focus-mode #shortcutHint.is-visible {
-    transform: translate(-50%, 0);
+#calendarModule .calendar-day-preview-title,
+#calendarModule .calendar-day-empty {
+    font-size: 0.68rem;
+    line-height: 1.12;
 }
 
-@media (max-width: 1100px) {
-    body:not([data-mode="clock"]) .topbar,
-    body:not([data-mode="clock"]) .hero,
-    body:not([data-mode="clock"]) #moduleArea,
-    body:not([data-mode="clock"]) .footer {
-        width: calc(var(--content-width) - 104px);
-        margin-left: auto;
-        margin-right: 108px;
-    }
+#calendarModule .calendar-day-preview-title {
+    -webkit-line-clamp: 1;
+}
 
-    .hero {
-        grid-template-columns: repeat(6, minmax(0, 1fr));
-        padding: 22px;
-    }
+#calendarModule .planner-reminders {
+    padding: 14px 18px;
+}
 
-    #pomodoroPresets,
-    .date-display,
-    .time-display,
-#todayFlow,
-    #cycleProgress,
-    .main-stats-row,
-    #streakVisual,
-    #dailyGoal,
-    #weeklyChart,
-    #sessionLog,
-    #advancedStats,
-    #achievements,
-    #progressiveBars,
-    .controls,
-    #homeLaunchpad {
-        grid-column: 1 / -1;
-    }
+#calendarModule .planner-reminder-list {
+    gap: 8px;
+    margin-top: 10px;
+}
 
-    .launchpad-primary-grid {
-        grid-template-columns: repeat(3, minmax(0, 1fr));
-    }
+#calendarModule .planner-reminder-card {
+    min-height: 48px;
+    padding: 10px 12px;
+}
 
-    .main-stats-row {
-        grid-template-columns: 1fr;
+@media (max-width: 760px) {
+    #calendarModule.module.active {
+        padding: 14px;
     }
 
-    .today-flow-head,
-    .today-flow-grid {
-        grid-template-columns: 1fr;
+    #calendarModule .planner-head,
+    #calendarModule .planner-reminders {
+        padding: 12px;
+        border-radius: 12px;
     }
 
-    .today-flow-head {
-        display: grid;
+    #calendarModule .planner-head p {
+        font-size: 0.78rem;
     }
 
-    .today-flow-center {
-        grid-template-columns: 1fr;
+    #calendarModule .calendar-day {
+        min-height: 46px;
+        padding: 6px 7px;
     }
 
-    .today-flow-track {
-        grid-template-columns: repeat(3, minmax(0, 1fr));
+    #calendarModule .calendar-day-empty {
+        font-size: 0.62rem;
     }
+}
 
-    #streakVisual,
-    #dailyGoal,
-    #weeklyChart,
-    #sessionLog,
-    #advancedStats,
-    #achievements {
-        grid-column: 1 / -1;
-    }
+/* =====================================================
+   Home clock entry and official immersion mode
+===================================================== */
 
-    .side-modules {
-        top: 50%;
-        bottom: auto;
-        right: 20px;
-        left: auto;
-        transform: translateY(-50%);
-        flex-direction: column;
-        z-index: 1200;
-    }
+.home-immersion-entry {
+    width: min(100%, 980px);
+    margin: 0 auto 10px;
+    padding: clamp(12px, 1.5vw, 16px) clamp(14px, 2vw, 20px);
+    display: flex;
+    align-items: center;
+    justify-content: space-between;
+    gap: 14px;
+    border: 1px solid rgba(125, 214, 255, 0.14);
+    border-radius: var(--rn-radius-shell);
+    background:
+        radial-gradient(circle at 8% 20%, rgba(0, 212, 255, 0.13), transparent 26%),
+        linear-gradient(135deg, rgba(11, 22, 39, 0.88), rgba(12, 18, 31, 0.72));
+    box-shadow: 0 18px 34px rgba(8, 19, 36, 0.14);
+    color: #eef6ff;
+}
 
-    body[data-mode="clock"] .side-modules {
-        opacity: 0;
-        pointer-events: none;
-        transform: translateY(-50%) translateX(16px);
-    }
+.home-immersion-copy {
+    min-width: 0;
+    display: grid;
+    gap: 2px;
+}
 
-    body[data-mode="qts"] .side-modules {
-        right: 2px;
-    }
+.home-immersion-copy span {
+    color: #79d5ff;
+    font-size: 0.68rem;
+    font-weight: 900;
+    letter-spacing: 0.18em;
+    text-transform: uppercase;
+}
 
-    body[data-mode="qts"] #qtsModule.module.active {
-        width: calc(100% - var(--qts-left-rail-space, 0px) - var(--qts-left-clearance, 0px) - var(--qts-right-tools-space, 0px) - var(--qts-right-edge-gap, 0px));
-        margin-left: calc(var(--qts-left-rail-space, 0px) + var(--qts-left-clearance, 0px));
-        margin-right: calc(var(--qts-right-tools-space, 0px) + var(--qts-right-edge-gap, 0px));
-    }
+.home-immersion-copy strong {
+    color: #f7fbff;
+    font-family: "Space Grotesk", "Manrope", sans-serif;
+    font-size: clamp(1.75rem, 3.5vw, 3rem);
+    font-weight: 800;
+    letter-spacing: -0.07em;
+    line-height: 0.92;
+}
 
-    .side-group {
-        gap: 8px;
-    }
+.home-immersion-copy p {
+    margin: 0;
+    color: rgba(231, 240, 252, 0.7);
+    font-size: clamp(0.76rem, 1vw, 0.88rem);
+    line-height: 1.35;
+}
+
+.home-immersion-btn {
+    flex: 0 0 auto;
+    min-height: 42px;
+    padding: 0 16px;
+    border: 1px solid rgba(125, 214, 255, 0.26);
+    border-radius: var(--rn-radius-control);
+    background:
+        linear-gradient(135deg, rgba(0, 212, 255, 0.18), rgba(37, 88, 240, 0.18)),
+        rgba(255, 255, 255, 0.06);
+    color: #eef9ff;
+    box-shadow: 0 12px 26px rgba(0, 0, 0, 0.18);
+    cursor: pointer;
+    font-size: 0.78rem;
+    font-weight: 900;
+    letter-spacing: 0.08em;
+    text-transform: uppercase;
+    transition: transform 0.18s ease, border-color 0.18s ease, background 0.18s ease;
+}
+
+.home-immersion-btn:hover {
+    transform: translateY(-1px);
+    border-color: rgba(125, 214, 255, 0.44);
+    background:
+        linear-gradient(135deg, rgba(0, 212, 255, 0.26), rgba(37, 88, 240, 0.24)),
+        rgba(255, 255, 255, 0.08);
+}
 
-    .side-group-label {
-        display: none;
-    }
+body.focus-mode[data-mode="clock"] .time-display,
+body.clock-immersion-mode[data-mode="clock"] .time-display {
+    display: flex !important;
+}
 
-    .side-group-buttons {
-        display: grid;
-        gap: 8px;
-    }
+body.clock-immersion-mode #ambientRoot {
+    display: block !important;
+}
 
-    #ambientRoot .ambient-mini {
-        left: 20px;
-        bottom: 110px;
-        width: min(clamp(238px, 28vw, 332px), calc(100vw - 40px));
-    }
+body.clock-immersion-mode #shortcutHint {
+    display: none !important;
+}
 
-    #ambientRoot .ambient-peek {
-        left: 20px;
-        bottom: 110px;
-    }
+body.clock-immersion-mode #ambientRoot .ambient-mini,
+body.clock-immersion-mode #ambientRoot .ambient-panel {
+    display: none !important;
+}
 
-    #ambientRoot .ambient-panel {
-        left: 20px;
-        bottom: 220px;
-    }
+body.clock-immersion-mode #ambientRoot .ambient-peek {
+    left: 22px !important;
+    bottom: 22px !important;
+    width: 58px !important;
+    min-width: 58px !important;
+    height: 58px !important;
+    padding: 0 !important;
+    display: inline-flex !important;
+    align-items: center !important;
+    justify-content: center !important;
+    gap: 0 !important;
+    border-radius: 999px !important;
+    clip-path: none !important;
+    background:
+        radial-gradient(circle at 38% 30%, rgba(125, 214, 255, 0.32), transparent 46%),
+        linear-gradient(145deg, rgba(12, 23, 40, 0.92), rgba(8, 14, 25, 0.86)) !important;
+    border-color: rgba(125, 214, 255, 0.26) !important;
+    box-shadow:
+        0 18px 38px rgba(0, 0, 0, 0.32),
+        inset 0 1px 0 rgba(255, 255, 255, 0.12) !important;
+    opacity: 1 !important;
+    pointer-events: auto !important;
+    transform: none !important;
+}
 
-    #ambientRoot .ambient-mini-meta {
-        gap: 6px;
-    }
+body.clock-immersion-mode #ambientRoot .ambient-peek-label {
+    display: none !important;
 }
 
-@media (max-width: 1100px) {
-    body[data-mode="qts"] .topbar,
-    body[data-mode="qts"] #moduleArea,
-    body[data-mode="qts"] .footer {
-        width: var(--content-width);
-        margin-left: auto;
-        margin-right: auto;
-    }
+body.clock-immersion-mode #ambientRoot .ambient-peek-dot {
+    width: 16px !important;
+    height: 16px !important;
+    background: #79d5ff !important;
+    box-shadow:
+        0 0 0 8px rgba(121, 213, 255, 0.1),
+        0 0 28px rgba(121, 213, 255, 0.42) !important;
 }
 
-@media (max-width: 760px) {
-    .ambient-help-box,
-    .ambient-blocked-box {
-        width: calc(100vw - 20px);
-        padding: 22px 18px 18px;
-        border-radius: 20px;
-    }
+body.clock-immersion-mode #ambientRoot .ambient-peek.is-playing .ambient-peek-dot {
+    background: #4fe3b3 !important;
+    box-shadow:
+        0 0 0 8px rgba(79, 227, 179, 0.1),
+        0 0 30px rgba(79, 227, 179, 0.5) !important;
+}
 
-    :root {
-        --content-width: min(100vw - 24px, 100%);
-    }
+/* =====================================================
+   Routine Table V2 - clean planner, legacy QTS preserved
+===================================================== */
 
-    body {
-        padding: 12px 12px 140px;
-    }
+body[data-mode="routine"] #moduleArea {
+    width: min(100%, calc(100vw - 24px));
+    max-width: calc(100vw - 24px);
+    margin-top: 14px;
+    margin-inline: auto;
+    overflow: visible;
+}
 
-    .topbar {
-        flex-direction: column;
-        align-items: stretch;
-        gap: 12px;
-        padding: 14px 52px 14px 14px;
-    }
+body[data-mode="routine"] #routineModule.module.active {
+    box-sizing: border-box;
+    width: min(100%, 1180px, calc(100vw - 24px));
+    max-width: calc(100vw - 24px);
+    margin-inline: auto;
+    padding: clamp(12px, 1.9vw, 24px);
+    overflow: visible;
+    border-radius: 12px;
+    background:
+        radial-gradient(circle at 18% 4%, rgba(125, 214, 255, 0.12), transparent 34%),
+        linear-gradient(180deg, rgba(16, 24, 38, 0.96), rgba(9, 15, 25, 0.94));
+    border-color: rgba(125, 214, 255, 0.12);
+    color: #f7f9ff;
+    box-shadow:
+        0 28px 58px rgba(4, 8, 16, 0.22),
+        inset 0 1px 0 rgba(255, 255, 255, 0.06);
+}
 
-    .topbar-collapse-btn {
-        top: 14px;
-        right: 14px;
-        transform: none;
-    }
+.routine-v2-shell {
+    display: grid;
+    gap: clamp(10px, 1.4vw, 16px);
+    min-height: 0;
+}
 
-    body.topbar-collapsed .topbar {
-        width: 100px;
-        margin-left: 12px;
-    }
+.routine-v2-hero,
+.routine-v2-setup,
+.routine-v2-board {
+    border-radius: var(--rn-radius-card, 10px);
+    border: 1px solid rgba(125, 214, 255, 0.12);
+    background:
+        linear-gradient(180deg, rgba(255, 255, 255, 0.055), rgba(255, 255, 255, 0.025)),
+        rgba(12, 19, 31, 0.72);
+    box-shadow:
+        0 18px 40px rgba(0, 0, 0, 0.18),
+        inset 0 1px 0 rgba(255, 255, 255, 0.055);
+}
 
-    .topbar-left,
-    .topbar-right {
-        justify-content: space-between;
-        flex-wrap: wrap;
-    }
+.routine-v2-hero {
+    position: relative;
+    display: flex;
+    align-items: end;
+    justify-content: space-between;
+    gap: 18px;
+    padding: clamp(14px, 1.7vw, 22px);
+}
 
-    .timezone-topbar {
-        width: 100%;
-        min-width: 0;
-        margin-left: 0;
-    }
+.routine-v2-kicker,
+.routine-control-card > span,
+.routine-board-toolbar span {
+    color: #80d9ff;
+    font-size: 0.66rem;
+    font-weight: 800;
+    letter-spacing: 0.15em;
+    text-transform: uppercase;
+}
 
-    .hero,
-    .module.active,
-    .footer {
-        border-radius: 0;
-        padding: 18px;
-    }
+.routine-v2-hero h2 {
+    max-width: 680px;
+    margin: 8px 0 6px;
+    padding-right: clamp(0px, 24vw, 272px);
+    color: #f7f9ff;
+    font-size: clamp(1.45rem, 3.2vw, 2.7rem);
+    line-height: 1.02;
+    letter-spacing: -0.045em;
+}
 
-    body[data-mode="qts"] #qtsModule {
-        --qts-inline-gutter: 0px;
-        --qts-left-rail-space: 0px;
-        --qts-right-tools-space: 0px;
-        --qts-content-max: 100%;
-    }
+.routine-v2-hero p {
+    max-width: 560px;
+    margin: 0;
+    color: rgba(235, 240, 249, 0.7);
+    font-size: clamp(0.92rem, 1.15vw, 1.05rem);
+}
 
-    body[data-mode="qts"] #qtsModule.module.active {
-        padding-left: 0;
-        padding-right: 0;
-    }
+.routine-v2-actions {
+    position: absolute;
+    top: clamp(12px, 1.4vw, 18px);
+    right: clamp(12px, 1.4vw, 18px);
+    display: flex;
+    flex-wrap: nowrap;
+    justify-content: flex-end;
+    gap: 10px;
+}
 
-    #qtsModule .qts-stage-main {
-        width: 100%;
-        margin-left: 0;
-        padding-left: 0;
-        padding-right: 0;
-        justify-items: stretch;
-    }
+#moduleArea .routine-v2-primary,
+#moduleArea .routine-v2-secondary,
+#moduleArea .routine-board-toolbar button,
+#moduleArea .routine-method,
+#moduleArea .routine-day-toggle,
+#moduleArea .routine-mobile-day,
+#moduleArea .routine-row-actions button {
+    min-height: 36px;
+    border-radius: 8px;
+    border: 1px solid rgba(125, 214, 255, 0.18);
+    background: rgba(255, 255, 255, 0.075);
+    color: #f7f9ff;
+    font-weight: 800;
+    letter-spacing: 0.02em;
+    box-shadow: none;
+}
 
-    #qtsModule .qts-grid-shell,
-    #qtsModule .qts-controls,
-    #qtsModule .qts-subject-summary {
-        width: 100%;
-        max-width: 100%;
-        justify-self: stretch;
-    }
+#moduleArea .routine-v2-primary,
+#moduleArea .routine-method.is-active,
+#moduleArea .routine-day-toggle.is-active,
+#moduleArea .routine-mobile-day.is-active {
+    background:
+        linear-gradient(135deg, rgba(0, 212, 255, 0.28), rgba(37, 88, 240, 0.34)),
+        rgba(255, 255, 255, 0.08);
+    border-color: rgba(125, 214, 255, 0.42);
+    color: #ffffff;
+}
 
-    .qts-template-strip {
-        gap: 8px;
-        margin-bottom: 12px;
-        width: min(100%, 100%);
-        justify-self: center;
-    }
+.routine-v2-setup {
+    display: grid;
+    grid-template-columns: minmax(340px, 1.65fr) minmax(90px, 0.42fr) minmax(90px, 0.42fr) minmax(270px, 0.9fr) minmax(160px, 0.62fr);
+    align-items: start;
+    gap: 7px;
+    padding: 8px;
+}
 
-    .qts-template-strip::after {
-        width: min(100%, 360px);
-    }
+.routine-control-card {
+    min-width: 0;
+    display: grid;
+    gap: 5px;
+    align-content: start;
+    padding: 7px 9px;
+    border-radius: 7px;
+    background: rgba(255, 255, 255, 0.055);
+    border: 1px solid rgba(255, 255, 255, 0.07);
+}
 
-    .qts-template-label {
-        min-height: 30px;
-        padding: 0 12px;
-        font-size: 0.6rem;
-        letter-spacing: 0.1em;
-    }
+.routine-control-card.is-time-control {
+    padding-inline: 8px;
+}
 
-    .qts-templates {
-        gap: 6px;
-    }
+.routine-control-card.is-generate {
+    align-content: stretch;
+}
 
-    .qts-templates button {
-        min-height: 30px;
-        padding: 0 9px;
-        font-size: 0.68rem;
-    }
+.routine-control-card.is-generate .routine-v2-primary {
+    width: 100%;
+    min-height: 34px;
+    align-self: stretch;
+    padding-inline: 10px;
+}
 
-    .qts-title {
-        width: fit-content;
-        max-width: calc(100% - 10px);
-        justify-self: center;
-        margin-bottom: 4px;
-        padding: 7px 14px;
-        font-size: clamp(1.02rem, 4.8vw, 1.34rem);
-        text-align: center;
-    }
+.routine-control-card.is-wide {
+    min-width: 0;
+}
 
-    .qts-grid-shell {
-        padding: 2px 3px 4px;
-        background: linear-gradient(180deg, rgba(255, 255, 255, 0.985), rgba(248, 247, 243, 0.97));
-        box-shadow: 0 10px 20px rgba(15, 23, 42, 0.06);
-    }
+.routine-control-card.is-wide .routine-method-row {
+    display: grid;
+    grid-template-columns: repeat(5, minmax(0, 1fr));
+}
 
-    #qtsGrid {
-        padding: 1px;
-        background: linear-gradient(180deg, rgba(255, 255, 255, 0.99), rgba(248, 247, 243, 0.975));
-    }
+.routine-control-card input {
+    width: 100%;
+}
 
-    #qtsGrid > div {
-        min-height: 26px;
-        color: #1d2430;
-    }
+#moduleArea .routine-control-card input {
+    border-radius: 7px;
+    border-color: rgba(125, 214, 255, 0.18);
+    background: rgba(255, 255, 255, 0.085);
+    color: #f7f9ff;
+    box-shadow: none;
+    min-height: 32px;
+    padding: 5px 8px;
+    font-size: 0.76rem;
+}
 
-    #qtsGrid div[contenteditable="true"] {
-        padding: 3px 4px;
-        font-size: 0.64rem;
-        line-height: 1;
-    }
+.routine-day-toggle-row,
+.routine-method-row,
+.routine-mobile-days {
+    display: flex;
+    flex-wrap: nowrap;
+    gap: 5px;
+}
 
-    .qts-header {
-        min-height: 34px;
-        padding: 3px 3px 2px;
-        background: linear-gradient(180deg, rgba(223, 230, 245, 0.99), rgba(214, 223, 241, 0.97));
-    }
+#moduleArea .routine-day-toggle,
+#moduleArea .routine-method,
+#moduleArea .routine-mobile-day {
+    min-height: 27px;
+    padding: 0 7px;
+    font-size: 0.72rem;
+}
 
-    .qts-header-day {
-        font-size: 0.64rem;
-    }
+#moduleArea .routine-control-card.is-wide .routine-method {
+    padding-inline: 4px;
+    font-size: clamp(0.62rem, 0.72vw, 0.72rem);
+}
 
-    .qts-header-date {
-        font-size: 0.48rem;
-    }
+.routine-mobile-days {
+    display: none;
+}
 
-    .qts-time {
-        min-height: 26px;
-        background: linear-gradient(180deg, rgba(236, 244, 255, 0.99), rgba(226, 237, 252, 0.97));
-    }
+.routine-v2-board {
+    min-width: 0;
+    min-height: 0;
+    overflow-x: visible;
+    overflow-y: visible;
+    padding: clamp(9px, 1.2vw, 12px);
+}
 
-    #qtsGrid .qts-time[contenteditable="true"] {
-        padding: 3px 3px;
-        font-size: 0.7rem;
-    }
+.routine-board-toolbar {
+    min-width: 0;
+    display: flex;
+    align-items: center;
+    gap: 7px;
+    flex-wrap: wrap;
+    margin-bottom: 10px;
+}
 
-    .qts-interval {
-        min-height: 11px !important;
-        font-size: 0.56rem;
-        background: linear-gradient(90deg, rgba(220, 241, 232, 0.94), rgba(228, 245, 237, 0.96));
-    }
+.routine-board-toolbar span {
+    margin-left: auto;
+    color: rgba(235, 240, 249, 0.48);
+    letter-spacing: 0.08em;
+    text-transform: none;
+}
 
-    .qts-controls {
-        margin-top: 8px;
-        gap: 8px;
-    }
+.routine-grid-scroll {
+    min-width: 0;
+    overflow-x: auto;
+    overflow-y: hidden;
+    scrollbar-gutter: stable;
+    padding-bottom: 2px;
+}
 
-    .row-control {
-        flex: 1 1 180px;
-        min-width: min(180px, calc(50% - 4px));
-        padding: 5px 8px;
-        grid-template-columns: 24px 1fr 24px;
-        gap: 6px;
-    }
+.routine-grid {
+    display: grid;
+    grid-template-columns:
+        clamp(56px, 6.5vw, 88px)
+        repeat(var(--routine-visible-days, 5), minmax(clamp(76px, 7.2vw, 112px), 1fr))
+        clamp(28px, 3vw, 38px);
+    gap: clamp(3px, 0.55vw, 6px);
+    width: max(100%, var(--routine-grid-min, 710px));
+    min-width: var(--routine-grid-min, 710px);
+}
+
+.routine-head,
+.routine-time-cell,
+.routine-cell-wrap,
+.routine-row-actions {
+    min-width: 0;
+    border-radius: 7px;
+}
 
-    .row-label {
-        font-size: 0.52rem;
-        letter-spacing: 0.12em;
-    }
+.routine-head {
+    min-height: clamp(30px, 3.6vw, 34px);
+    display: flex;
+    align-items: center;
+    justify-content: center;
+    padding: 0 clamp(4px, 0.7vw, 10px);
+    color: rgba(235, 240, 249, 0.68);
+    background: rgba(255, 255, 255, 0.045);
+    border: 1px solid rgba(255, 255, 255, 0.065);
+    font-size: clamp(0.58rem, 0.9vw, 0.74rem);
+    font-weight: 900;
+    letter-spacing: clamp(0.05em, 0.75vw, 0.12em);
+    text-transform: uppercase;
+    overflow: hidden;
+    text-overflow: ellipsis;
+    white-space: nowrap;
+}
 
-    .row-btn {
-        width: 24px;
-        height: 22px;
-    }
+.routine-time-cell {
+    display: grid;
+    grid-template-columns: 1fr auto 1fr;
+    align-items: center;
+    gap: 3px;
+    padding: clamp(2px, 0.35vw, 4px);
+    background: rgba(255, 255, 255, 0.052);
+    border: 1px solid rgba(255, 255, 255, 0.065);
+}
 
-    .time-display {
-        font-size: clamp(3.5rem, 18vw, 5.6rem);
-        padding: 22px 18px;
-        border-radius: 24px;
-    }
+.routine-time-cell span {
+    width: 5px;
+    height: 1px;
+    background: rgba(235, 240, 249, 0.34);
+}
 
-    .today-flow-shell,
-    .today-flow-card {
-        padding: 18px;
-        border-radius: 22px;
-    }
+#moduleArea .routine-time-cell input {
+    min-width: 0;
+    padding: 3px 1px;
+    border-radius: 5px;
+    border-color: transparent;
+    background: rgba(255, 255, 255, 0.06);
+    color: #f7f9ff;
+    font-size: clamp(0.52rem, 0.68vw, 0.62rem);
+    line-height: 1.1;
+    text-align: center;
+}
 
-    .today-flow-head {
-        gap: 12px;
-    }
+.routine-cell-wrap {
+    padding: clamp(2px, 0.38vw, 4px);
+    background: rgba(243, 246, 250, 0.96);
+    border: 1px solid rgba(255, 255, 255, 0.68);
+}
 
-    .today-flow-progress {
-        width: 100%;
-    }
+.routine-cell-wrap.is-interval,
+.routine-time-cell.is-interval {
+    background:
+        linear-gradient(135deg, rgba(240, 203, 122, 0.2), rgba(125, 214, 255, 0.08)),
+        rgba(255, 255, 255, 0.06);
+}
 
-    .today-flow-card-top,
-    .today-flow-queue-item {
-        align-items: flex-start;
-    }
+.routine-cell {
+    width: 100%;
+    box-sizing: border-box;
+    min-height: clamp(26px, 3.2vw, 34px);
+    border: 0;
+    resize: vertical;
+    overflow: hidden;
+    padding: clamp(3px, 0.42vw, 5px) clamp(5px, 0.7vw, 7px);
+    border-radius: 5px;
+    outline: none;
+    color: #172033;
+    font-family: inherit;
+    font-size: clamp(0.68rem, 0.78vw, 0.8rem);
+    font-weight: 650;
+    line-height: 1.18;
+    white-space: pre-wrap;
+    overflow-wrap: anywhere;
+    background: rgba(255, 255, 255, 0.34);
+    caret-color: #0a7cff;
+    cursor: text;
+    user-select: text;
+    -webkit-user-select: text;
+    transition: background 0.15s ease, box-shadow 0.15s ease;
+}
 
-    .today-flow-card-top {
-        flex-direction: column;
-    }
+.routine-cell:focus {
+    background: #ffffff;
+    box-shadow:
+        inset 0 0 0 2px rgba(0, 173, 255, 0.36),
+        0 8px 18px rgba(0, 0, 0, 0.08);
+}
 
-    .today-flow-queue-item {
-        display: grid;
-        grid-template-columns: 1fr;
-    }
+.routine-cell-wrap.is-interval .routine-cell {
+    color: #614c18;
+    background: rgba(255, 245, 219, 0.72);
+}
 
-    .today-flow-queue-time {
-        min-width: 0;
-    }
+.routine-row-actions {
+    display: grid;
+    place-items: center;
+    background: rgba(255, 255, 255, 0.035);
+    border: 1px solid rgba(255, 255, 255, 0.05);
+}
 
-    .today-flow-actions {
-        display: grid;
-        grid-template-columns: 1fr;
-    }
+#moduleArea .routine-row-actions button {
+    width: clamp(26px, 3vw, 30px);
+    min-height: clamp(26px, 3vw, 30px);
+    padding: 0;
+    border-radius: 7px;
+    color: rgba(247, 249, 255, 0.68);
+}
 
-    .today-flow-track {
-        grid-template-columns: 1fr;
-    }
+.launchpad-card[data-module="routine"] {
+    background:
+        linear-gradient(135deg, rgba(242, 249, 255, 0.96), rgba(229, 243, 255, 0.88)),
+        radial-gradient(circle at 18% 18%, rgba(125, 214, 255, 0.34), transparent 32%);
+}
 
-    .today-flow-center {
-        grid-template-columns: 1fr;
-        justify-items: stretch;
-    }
+body.dark .launchpad-card[data-module="routine"] {
+    background:
+        linear-gradient(135deg, rgba(20, 32, 50, 0.94), rgba(13, 21, 35, 0.9)),
+        radial-gradient(circle at 18% 18%, rgba(125, 214, 255, 0.18), transparent 34%);
+}
 
-    .today-flow-mode-wrap {
-        justify-items: flex-start;
-        text-align: left;
+@media (max-width: 1060px) {
+    .routine-v2-setup {
+        grid-template-columns: minmax(0, 1.65fr) repeat(2, minmax(92px, 0.62fr));
     }
 
-    #homeLaunchpad {
-        gap: 14px;
+    .routine-day-toggle-row {
+        flex-wrap: wrap;
     }
 
-    .home-launchpad-main,
-    .home-launchpad-tools {
-        padding: 18px;
-        border-radius: 24px;
+    .routine-control-card.is-wide {
+        grid-column: 1 / 3;
     }
 
-    .rotanota-home-brand {
-        grid-template-columns: 1fr;
-        justify-items: center;
-        text-align: center;
-        padding: 20px 18px;
+    .routine-control-card.is-generate {
+        grid-column: 3;
     }
 
-    .rotanota-home-brand p {
-        margin-inline: auto;
+    .routine-v2-hero {
+        align-items: start;
+        flex-direction: row;
     }
 
-    .rotanota-topbar-brand small {
-        display: none;
+    .routine-v2-actions {
+        justify-content: flex-start;
     }
+}
 
-    .launchpad-card {
-        min-height: auto;
+@media (max-width: 760px) {
+    body[data-mode="routine"] #routineModule.module.active {
+        width: min(100%, calc(100vw - 18px));
+        padding: 10px;
     }
 
-    .launchpad-tool-row {
-        display: grid;
-        grid-template-columns: 1fr 1fr;
-        width: 100%;
+    .routine-v2-hero h2 {
+        font-size: clamp(1.4rem, 7.2vw, 2rem);
+        padding-right: 250px;
     }
 
-    .launchpad-primary-grid {
-        grid-template-columns: 1fr;
+    .routine-v2-setup {
+        grid-template-columns: minmax(0, 1.6fr) repeat(2, minmax(86px, 0.62fr));
     }
 
-    .study-stats {
-        grid-template-columns: 1fr;
+    .routine-control-card.is-wide {
+        grid-column: 1 / 3;
     }
 
-    .heat-cell {
-        min-width: 20px;
-        height: 44px;
+    .routine-control-card.is-generate {
+        grid-column: 3;
     }
 
-    .donate-area {
-        flex-direction: column;
-        align-items: flex-start;
+    .routine-board-toolbar span {
+        width: 100%;
+        margin-left: 0;
     }
 
-    .side-modules {
-        top: 50%;
-        right: 10px;
-        left: auto;
-        width: 60px;
-        max-width: 60px;
-        justify-content: flex-start;
-        align-items: center;
-        padding: 8px 6px;
-        border-radius: 0;
-        gap: 6px;
-        transform: translateY(-50%);
+    .routine-v2-board {
+        overflow-x: visible;
+        overflow-y: visible;
     }
 
-    .side-group,
-    .side-group-buttons {
-        justify-items: center;
-        gap: 6px;
+    .routine-grid {
+        grid-template-columns:
+            clamp(44px, 12vw, 64px)
+            repeat(var(--routine-visible-days, 5), minmax(clamp(32px, 10vw, 62px), 1fr))
+            28px;
+        gap: 4px;
+        width: max(100%, var(--routine-grid-min, 710px));
+        min-width: var(--routine-grid-min, 710px);
     }
 
-    .side-modules .icon-circle {
-        width: 40px;
-        height: 40px;
+    .routine-time-cell {
+        grid-template-columns: minmax(0, 1fr) 4px minmax(0, 1fr);
+        gap: 2px;
+        padding: 3px;
     }
 
-    .side-group[data-nav-group="primary"] .icon-circle,
-    .side-group[data-nav-group="utility"] .icon-circle {
-        width: 40px;
-        height: 40px;
+    .routine-time-cell span {
+        display: block;
+        width: 4px;
     }
 
-    #ambientRoot .ambient-mini {
-        left: 12px;
-        bottom: 94px;
-        width: min(clamp(238px, 62vw, 312px), calc(100vw - 24px));
+    #moduleArea .routine-time-cell input {
+        padding: 3px 1px;
+        font-size: clamp(0.5rem, 1.6vw, 0.58rem);
+        line-height: 1.1;
     }
 
-    #ambientRoot .ambient-peek {
-        left: 12px;
-        bottom: 94px;
+    .routine-cell {
+        min-height: 30px;
+        padding: 3px 5px;
+        font-size: clamp(0.6rem, 2.1vw, 0.7rem);
     }
 
-    #ambientRoot .ambient-mini-meta {
-        gap: 6px;
+    .routine-head {
+        padding-inline: 2px;
     }
 
-    #ambientRoot .ambient-mini-main {
-        gap: 6px 8px;
+    .routine-day-col {
+        font-size: 0;
+        letter-spacing: 0;
     }
 
-    #ambientRoot .ambient-mini-actions {
-        gap: 4px;
+    .routine-day-col::after {
+        content: attr(data-day-short);
+        font-size: 0.64rem;
+        letter-spacing: 0.08em;
     }
 
-    #ambientRoot .ambient-progress {
-        grid-template-columns: 40px 1fr 40px;
+    .routine-row-actions {
+        min-width: 0;
     }
 
-    #ambientRoot .ambient-volume-wrap {
-        grid-template-columns: 28px 1fr;
+    #moduleArea .routine-row-actions button {
+        width: 24px;
+        min-height: 24px;
     }
+}
 
-    #ambientRoot .ambient-panel {
-        left: 12px;
-        bottom: 188px;
-        width: calc(100vw - 24px);
-        height: min(56vh, 460px);
-        padding: 12px;
+@media (max-width: 520px) {
+    .routine-v2-hero {
+        padding: 58px 12px 12px;
     }
 
-    #ambientRoot .category-selector {
-        grid-template-columns: repeat(2, minmax(0, 1fr));
+    .routine-v2-hero h2 {
+        padding-right: 0;
     }
 
-    #ambientRoot .ambient-item {
-        grid-template-columns: auto 72px minmax(0, 1fr);
+    .routine-v2-setup {
+        grid-template-columns: minmax(0, 1.45fr) repeat(2, minmax(72px, 0.7fr));
     }
 
-    #ambientRoot .ambient-item img {
-        width: 72px;
-        height: 46px;
+    .routine-v2-setup .routine-control-card:first-child {
+        grid-column: 1;
     }
 
-    .mini-analog-clock {
-        right: 16px;
-        bottom: 160px;
-        width: 92px;
-        height: 92px;
+    .routine-control-card.is-wide {
+        grid-column: 1 / 3;
     }
-}
 
-@media (max-width: 520px) {
-    .qts-template-strip {
-        max-width: 100%;
+    .routine-control-card.is-generate {
+        grid-column: 3;
     }
 
-    .qts-templates {
-        gap: 5px;
+    .routine-control-card.is-generate .routine-v2-primary {
+        padding-inline: 6px;
+        font-size: 0.72rem;
     }
 
-    .qts-templates button {
-        min-height: 28px;
-        padding: 0 8px;
-        font-size: 0.62rem;
+    .routine-board-toolbar {
+        display: grid;
+        grid-template-columns: repeat(2, minmax(0, 1fr));
     }
 
-    .qts-template-label {
-        min-height: 28px;
-        padding: 0 10px;
-        font-size: 0.56rem;
+    .routine-board-toolbar span {
+        grid-column: 1 / -1;
     }
 
-    .qts-title {
-        width: fit-content;
-        max-width: calc(100% - 8px);
-        justify-self: center;
-        font-size: clamp(0.94rem, 4.4vw, 1.14rem);
-        padding: 6px 12px;
+    .routine-grid {
+        grid-template-columns:
+            clamp(48px, 15vw, 58px)
+            repeat(var(--routine-visible-days, 5), minmax(0, 1fr));
+        gap: 3px;
+        width: max(100%, calc(var(--routine-grid-min, 710px) - 36px));
+        min-width: calc(var(--routine-grid-min, 710px) - 36px);
     }
 
-    #qtsGrid > div {
-        min-height: 24px;
+    .routine-head.is-action,
+    .routine-row-actions {
+        display: none;
     }
 
-    #qtsGrid div[contenteditable="true"] {
-        font-size: 0.56rem;
-        padding: 2px 3px;
+    .routine-cell-wrap {
+        padding: 3px;
     }
 
-    .qts-header {
-        min-height: 31px;
-        padding: 2px 2px 1px;
+    .routine-cell {
+        min-height: 28px;
+        font-size: 0.64rem;
     }
+}
 
-    .qts-header-day {
-        font-size: 0.52rem;
+@media (max-width: 760px) {
+    .home-immersion-entry {
+        align-items: stretch;
+        flex-direction: column;
     }
 
-    .qts-header-date {
-        font-size: 0.38rem;
+    .home-immersion-btn {
+        width: 100%;
     }
 
-    #qtsGrid .qts-time[contenteditable="true"] {
-        font-size: 0.56rem;
+    body.clock-immersion-mode #ambientRoot .ambient-peek {
+        left: 16px !important;
+        bottom: 16px !important;
+        width: 52px !important;
+        min-width: 52px !important;
+        height: 52px !important;
     }
+}
 
-    .qts-interval {
-        font-size: 0.5rem;
+body[data-mode="routine"] {
+    padding-bottom: 78px;
+}
+
+body[data-mode="routine"] .side-modules {
+    position: fixed;
+    top: auto;
+    right: auto;
+    bottom: 10px;
+    left: 50%;
+    width: min(520px, calc(100vw - 24px));
+    max-width: calc(100vw - 24px);
+    display: flex;
+    flex-direction: row;
+    justify-content: center;
+    align-items: center;
+    gap: 8px;
+    padding: 8px 10px;
+    margin: 0;
+    transform: translateX(-50%);
+    z-index: 1300;
+}
+
+body[data-mode="routine"] .side-group {
+    display: flex;
+    align-items: center;
+    gap: 8px;
+    padding: 0;
+    border: 0;
+}
+
+body[data-mode="routine"] .side-group-buttons {
+    display: flex;
+    align-items: center;
+    gap: 8px;
+}
+
+body[data-mode="routine"] .side-modules .icon-circle,
+body[data-mode="routine"] .side-group[data-nav-group="primary"] .icon-circle,
+body[data-mode="routine"] .side-group[data-nav-group="utility"] .icon-circle {
+    width: 38px;
+    height: 38px;
+    min-width: 38px;
+}
+
+body[data-mode="routine"] .side-group[data-nav-group="utility"] {
+    padding-left: 8px;
+    border-left: 1px solid var(--line);
+}
+
+@media (max-width: 520px) {
+    body[data-mode="routine"] .side-modules {
+        justify-content: flex-start;
+        overflow-x: auto;
+        scrollbar-width: none;
     }
 
-    .row-control {
-        min-width: 100%;
+    body[data-mode="routine"] .side-modules::-webkit-scrollbar {
+        display: none;
     }
 }
 
 /* =====================================================
-   RotaNota refinement - home, Pomodoro and light radii
+   QTS classic table polish - lighter radius, less noise
 ===================================================== */
 
-:root {
-    --rn-radius-shell: 14px;
-    --rn-radius-card: 10px;
-    --rn-radius-control: 9px;
-    --rn-home-width: min(1040px, calc(100vw - 64px));
+body[data-mode="qts"] #qtsModule.module.active {
+    border-radius: 12px;
 }
 
-body.dark {
-    --bg: #0b111b;
-    --bg-deep: #101b2a;
-    --ink: #f4f7fb;
-    --muted: #9aa8ba;
-    --surface: rgba(16, 23, 35, 0.82);
-    --surface-strong: rgba(17, 25, 38, 0.95);
-    --ui-clickable-surface:
-        linear-gradient(180deg, rgba(31, 39, 53, 0.97), rgba(21, 28, 41, 0.96));
-    --ui-clickable-border: rgba(125, 214, 255, 0.12);
-    --ui-informative-surface:
-        linear-gradient(180deg, rgba(255, 255, 255, 0.025), rgba(255, 255, 255, 0.055));
-    background:
-        radial-gradient(circle at 16% 0%, rgba(0, 212, 255, 0.12), transparent 28%),
-        radial-gradient(circle at 90% 12%, rgba(37, 88, 240, 0.12), transparent 26%),
-        linear-gradient(180deg, #070b12 0%, #0b111b 48%, #111a25 100%);
+body[data-mode="qts"] .qts-title,
+body[data-mode="qts"] .qts-grid-shell,
+body[data-mode="qts"] #qtsGrid,
+body[data-mode="qts"] .qts-controls,
+body[data-mode="qts"] .row-control,
+body[data-mode="qts"] .row-btn,
+body[data-mode="qts"] .qts-week-context,
+body[data-mode="qts"] .qts-detached-week-button,
+body[data-mode="qts"] .qts-detached-toggle,
+body[data-mode="qts"] .qts-detached-summary-button {
+    clip-path: none;
 }
 
-.topbar,
-.hero,
-.module.active,
-.footer,
-.home-launchpad-main,
-.home-launchpad-tools,
-.rotanota-home-brand,
-.launchpad-card,
-.launchpad-tool,
-.icon-circle,
-.timezone-topbar,
-.style-menu,
-.style-option,
-.preset-chip,
-.pomodoro-presets,
-.today-flow-shell,
-.today-flow-card,
-#ambientRoot .ambient-mini,
-#ambientRoot .ambient-panel,
-.ambient-player,
-.ambient-panel-new {
-    clip-path: none !important;
+body[data-mode="qts"] .qts-title {
+    border-radius: 8px;
 }
 
-.topbar,
-.hero,
-.module.active,
-.footer,
-.home-launchpad-main,
-.home-launchpad-tools,
-.rotanota-home-brand,
-.style-menu,
-.pomodoro-presets,
-.today-flow-shell,
-#ambientRoot .ambient-panel,
-.ambient-panel-new {
-    border-radius: var(--rn-radius-shell) !important;
+body[data-mode="qts"] .qts-grid-shell,
+body[data-mode="qts"] #qtsGrid,
+body[data-mode="qts"] .qts-controls,
+body[data-mode="qts"] .row-control,
+body[data-mode="qts"] .qts-week-context {
+    border-radius: 9px;
 }
 
-.launchpad-card,
-.launchpad-tool,
-.style-option,
-.preset-chip,
-.today-flow-card,
-.timezone-topbar,
-#ambientRoot .ambient-mini,
-.ambient-player,
-.ambient-sound,
-.youtube-controls {
-    border-radius: var(--rn-radius-card) !important;
+body[data-mode="qts"] #qtsGrid > div {
+    border-radius: 5px;
 }
 
-.icon-circle,
-.topbar-collapse-btn,
-.control-btn,
-.ambient-play,
-.ambient-fav,
-.yt-fav {
-    border-radius: var(--rn-radius-control) !important;
+body[data-mode="qts"] .qts-interval {
+    border-radius: 4px !important;
 }
 
-body[data-mode="clock"] .hero {
-    width: var(--rn-home-width);
-    min-height: min(720px, calc(100vh - 118px));
-    display: flex;
-    align-items: center;
-    justify-content: center;
-    padding: clamp(22px, 4vw, 42px);
-    overflow: visible;
+body[data-mode="qts"] .row-btn,
+body[data-mode="qts"] .qts-detached-week-button,
+body[data-mode="qts"] .qts-detached-toggle,
+body[data-mode="qts"] .qts-detached-summary-button {
+    border-radius: 7px;
 }
 
-body[data-mode="clock"] #pomodoroPresets,
-body[data-mode="clock"] .date-display,
-body[data-mode="clock"] .time-display,
-body[data-mode="clock"] #cycleProgress,
-body[data-mode="clock"] #todayFlow,
-body[data-mode="clock"] .controls,
-body[data-mode="clock"] .main-stats-row,
-body[data-mode="clock"] #streakVisual,
-body[data-mode="clock"] #dailyGoal,
-body[data-mode="clock"] #weeklyChart,
-body[data-mode="clock"] #sessionLog,
-body[data-mode="clock"] #advancedStats,
-body[data-mode="clock"] #achievements,
-body[data-mode="clock"] #progressiveBars {
-    display: none !important;
+@media (max-width: 1480px) and (min-width: 761px) {
+    body[data-mode="qts"] #qtsModule {
+        --qts-inline-gutter: clamp(2px, 0.6vw, 10px);
+        --qts-left-clearance: 0px;
+        --qts-right-edge-gap: clamp(2px, 0.5vw, 8px);
+    }
+
+    body[data-mode="qts"] #qtsModule.module.active {
+        padding-left: clamp(6px, 1vw, 14px);
+        padding-right: clamp(6px, 1vw, 14px);
+    }
 }
 
-#homeLaunchpad {
-    width: 100%;
-    max-width: 980px;
-    margin: 0 auto;
+@media (max-width: 1220px) and (min-width: 761px) {
+    body[data-mode="qts"] #qtsModule {
+        --qts-inline-gutter: clamp(1px, 0.45vw, 7px);
+        --qts-right-tools-space: 48px;
+        --qts-right-edge-gap: 2px;
+    }
+
+    body[data-mode="qts"] #qtsModule .qts-stage-main {
+        --qts-safe-inline-size: 100%;
+    }
+
+    body[data-mode="qts"] #qtsModule .qts-grid-shell,
+    body[data-mode="qts"] #qtsModule .qts-controls,
+    body[data-mode="qts"] #qtsModule .qts-subject-summary {
+        width: 100%;
+        max-width: 100%;
+        justify-self: stretch;
+    }
+
+    body[data-mode="qts"] .qts-detached-index {
+        width: clamp(78px, 8.5vw, 94px);
+        left: 6px;
+    }
+
+    body[data-mode="qts"] #qtsModule.module.active {
+        padding-left: clamp(4px, 0.7vw, 10px);
+        padding-right: clamp(4px, 0.7vw, 10px);
+    }
 }
 
-body:not([data-mode="clock"]) #homeLaunchpad {
-    display: none !important;
+@media (max-width: 920px) and (min-width: 761px) {
+    body[data-mode="qts"] #qtsModule {
+        --qts-left-rail-space: 72px;
+        --qts-right-tools-space: 42px;
+        --qts-right-edge-gap: 0px;
+    }
+
+    body[data-mode="qts"] .qts-detached-index {
+        width: 66px;
+    }
+
+    body[data-mode="qts"] .qts-detached-block-label,
+    body[data-mode="qts"] .qts-detached-index-divider,
+    body[data-mode="qts"] .qts-detached-week-button .qts-detached-text,
+    body[data-mode="qts"] .qts-detached-toggle span:last-child,
+    body[data-mode="qts"] .qts-detached-summary-button .qts-detached-text {
+        display: none;
+    }
+
+    body[data-mode="qts"] .qts-detached-icon {
+        display: block;
+    }
 }
 
-.rotanota-home-brand {
-    width: 100%;
-    margin: 0 auto 12px;
-    padding: clamp(18px, 2.6vw, 28px);
-    grid-template-columns: auto minmax(0, 1fr);
-    gap: clamp(14px, 2vw, 22px);
+@media (max-width: 900px) {
+    body:not([data-mode="clock"]) {
+        padding-bottom: 78px;
+    }
+
+    body:not(.topbar-collapsed):not([data-mode="clock"]) .topbar,
+    body:not([data-mode="clock"]) .hero,
+    body:not([data-mode="clock"]) #moduleArea,
+    body:not([data-mode="clock"]) .footer {
+        width: var(--content-width);
+        margin-left: auto;
+        margin-right: auto;
+    }
+
+    body.topbar-collapsed:not([data-mode="clock"]) .topbar {
+        width: 108px;
+        margin-left: 14px;
+        margin-right: auto;
+    }
+
+    body:not([data-mode="clock"]) .side-modules {
+        position: fixed;
+        top: auto;
+        right: auto;
+        bottom: 10px;
+        left: 50%;
+        width: min(520px, calc(100vw - 24px));
+        max-width: calc(100vw - 24px);
+        display: flex;
+        flex-direction: row;
+        justify-content: center;
+        align-items: center;
+        gap: 8px;
+        padding: 8px 10px;
+        margin: 0;
+        transform: translateX(-50%);
+        z-index: 1300;
+    }
+
+    body:not([data-mode="clock"]) .side-group {
+        display: flex;
+        align-items: center;
+        gap: 8px;
+        padding: 0;
+        border: 0;
+    }
+
+    body:not([data-mode="clock"]) .side-group-buttons {
+        display: flex;
+        align-items: center;
+        gap: 8px;
+    }
+
+    body:not([data-mode="clock"]) .side-modules .icon-circle,
+    body:not([data-mode="clock"]) .side-group[data-nav-group="primary"] .icon-circle,
+    body:not([data-mode="clock"]) .side-group[data-nav-group="utility"] .icon-circle {
+        width: 38px;
+        height: 38px;
+        min-width: 38px;
+    }
+
+    body:not([data-mode="clock"]) .side-group[data-nav-group="utility"] {
+        padding-top: 0;
+        padding-left: 8px;
+        border-top: 0;
+        border-left: 1px solid var(--line);
+    }
 }
 
-.rotanota-home-brand img {
-    width: clamp(54px, 6vw, 74px);
-    height: clamp(54px, 6vw, 74px);
+@media (max-width: 520px) {
+    body:not([data-mode="clock"]) .side-modules {
+        justify-content: flex-start;
+        overflow-x: auto;
+        scrollbar-width: none;
+    }
+
+    body:not([data-mode="clock"]) .side-modules::-webkit-scrollbar {
+        display: none;
+    }
 }
 
-.rotanota-home-brand span {
-    font-size: clamp(1.9rem, 3.2vw, 3.35rem);
+@media (max-width: 760px) {
+    body.topbar-collapsed:not([data-mode="clock"]) .topbar {
+        width: 100px;
+        margin-left: 12px;
+    }
 }
 
-.rotanota-home-brand strong {
-    font-size: clamp(0.96rem, 1.2vw, 1.16rem);
+/* Keep the compact sound button anchored after responsive panel shifts. */
+#ambientRoot .ambient-peek,
+body[data-mode="clock"] #ambientRoot .ambient-peek,
+body:not([data-mode="clock"]) #ambientRoot .ambient-peek {
+    left: 22px !important;
+    bottom: 22px !important;
 }
 
-.rotanota-home-brand p {
-    font-size: 0.88rem;
+@media (max-width: 760px) {
+    #ambientRoot .ambient-peek,
+    body[data-mode="clock"] #ambientRoot .ambient-peek,
+    body:not([data-mode="clock"]) #ambientRoot .ambient-peek {
+        left: 16px !important;
+        bottom: 16px !important;
+    }
 }
 
-.home-launchpad-main,
-.home-launchpad-tools {
-    width: 100%;
-    padding: clamp(14px, 2vw, 20px);
-}
+/* =====================================================
+   Unified rounded finish for day/night layouts
+===================================================== */
 
-.launchpad-primary-grid {
-    width: 100%;
-    grid-template-columns: repeat(4, minmax(128px, 1fr));
-    gap: 10px;
+body .ui-cut-shape,
+body .ui-clickable-cut,
+body .ui-informative-fade,
+body .module.active,
+body .planner-head,
+body .planner-reminders,
+body .alarm-bridge,
+body .calendar-day,
+body .launcher-primary,
+body .planner-head-actions button,
+body .planner-section-head button,
+body .planner-month-nav button,
+body .alarm-bridge-actions button,
+body .planner-btn {
+    clip-path: none !important;
 }
 
-.launchpad-card {
-    min-height: 74px;
-    padding: 16px 14px;
+body .ui-cut-shape,
+body .ui-clickable-cut,
+body .ui-informative-fade,
+body .planner-head,
+body .planner-reminders,
+body .alarm-bridge {
+    border-radius: var(--rn-radius-card, 10px) !important;
 }
 
-.launchpad-card-title {
-    font-size: clamp(0.92rem, 1vw, 1rem);
+body .module.active {
+    border-radius: var(--rn-radius-shell, 14px) !important;
 }
 
-.home-launchpad-tools {
-    margin-top: 10px;
+body .calendar-day,
+body .launcher-primary,
+body .planner-head-actions button,
+body .planner-section-head button,
+body .planner-month-nav button,
+body .alarm-bridge-actions button,
+body .planner-btn {
+    border-radius: var(--rn-radius-control, 9px) !important;
 }
 
-.launchpad-tool-row {
-    width: 100%;
-    display: grid;
-    grid-template-columns: repeat(4, minmax(112px, 1fr));
-    gap: 10px;
+body .ui-clickable-cut::after,
+body .ui-informative-fade::before {
+    clip-path: none !important;
+    border-radius: inherit !important;
 }
 
-.launchpad-tool {
-    min-height: 38px;
-    padding: 0 12px;
-    font-size: 0.82rem;
-}
+/* =====================================================
+   Mobile browser sweep
+   Target touch phones without disturbing narrow desktop windows.
+===================================================== */
 
-body[data-mode="pomodoro"] .hero {
-    min-height: min(760px, calc(100vh - 122px));
-    grid-template-columns: repeat(12, minmax(0, 1fr));
-    align-content: start;
-    row-gap: 14px;
-    padding: clamp(22px, 3vw, 34px);
-}
+@media (max-width: 760px) and (pointer: coarse) {
+    body {
+        padding-bottom: calc(34px + env(safe-area-inset-bottom, 0px));
+    }
 
-body[data-mode="pomodoro"] #pomodoroPresets,
-body[data-mode="pomodoro"] .date-display,
-body[data-mode="pomodoro"] .time-display,
-body[data-mode="pomodoro"] #cycleProgress,
-body[data-mode="pomodoro"] #todayFlow,
-body[data-mode="pomodoro"] .controls,
-body[data-mode="pomodoro"] .main-stats-row,
-body[data-mode="pomodoro"] #streakVisual,
-body[data-mode="pomodoro"] #dailyGoal,
-body[data-mode="pomodoro"] #weeklyChart,
-body[data-mode="pomodoro"] #sessionLog,
-body[data-mode="pomodoro"] #advancedStats,
-body[data-mode="pomodoro"] #achievements,
-body[data-mode="pomodoro"] #progressiveBars {
-    grid-column: 2 / 12;
-}
+    body:not([data-mode="clock"]) .hero,
+    body:not([data-mode="clock"]) .module.active {
+        padding-bottom: calc(24px + env(safe-area-inset-bottom, 0px));
+    }
 
-body[data-mode="pomodoro"] .time-display {
-    display: block;
-    max-width: 920px;
-    margin-inline: auto;
-}
+    body:not([data-mode="clock"]) .side-modules {
+        position: sticky;
+        top: calc(96px + env(safe-area-inset-top, 0px));
+        right: auto;
+        bottom: auto;
+        left: auto;
+        width: min(520px, calc(100vw - 24px));
+        max-width: calc(100vw - 24px);
+        min-height: 44px;
+        padding: 6px 8px;
+        margin: 8px auto 14px;
+        transform: none;
+        display: flex;
+        align-items: center;
+        justify-content: center;
+        gap: 6px;
+        overflow-x: auto;
+        overflow-y: hidden;
+        z-index: 1220;
+    }
 
-body[data-mode="pomodoro"] .controls {
-    display: flex;
-}
+    body:not([data-mode="clock"]) .side-modules::-webkit-scrollbar {
+        display: none;
+    }
 
-.pomodoro-presets-title span {
-    letter-spacing: 0.12em;
-}
+    body:not([data-mode="clock"]) .side-group {
+        flex: 0 0 auto;
+        display: flex;
+        align-items: center;
+        gap: 6px;
+        min-width: 0;
+    }
 
-.topbar {
-    overflow: visible;
-}
+    body:not([data-mode="clock"]) .side-group-label {
+        display: none;
+    }
 
-.rotanota-topbar-brand {
-    border-radius: var(--rn-radius-card);
-    clip-path: none;
-}
+    body:not([data-mode="clock"]) .side-group-buttons {
+        display: flex;
+        align-items: center;
+        gap: 6px;
+    }
 
-.rotanota-topbar-brand img {
-    width: 30px;
-    height: 30px;
-}
+    body:not([data-mode="clock"]) .side-group[data-nav-group="utility"] {
+        padding-top: 0;
+        padding-left: 6px;
+        border-top: 0;
+        border-left: 1px solid var(--line);
+    }
 
-.rotanota-topbar-brand small {
-    letter-spacing: 0.08em;
+    body:not([data-mode="clock"]) .side-modules .icon-circle,
+    body:not([data-mode="clock"]) .side-group[data-nav-group="primary"] .icon-circle {
+        width: 34px;
+        height: 34px;
+        min-width: 34px;
+    }
+
+    body:not([data-mode="clock"]) .side-group[data-nav-group="utility"] .icon-circle {
+        width: 30px;
+        height: 30px;
+        min-width: 30px;
+        font-size: 0.76rem;
+    }
+
+    #ambientRoot .ambient-mini,
+    body[data-mode="clock"] #ambientRoot .ambient-mini,
+    body:not([data-mode="clock"]) #ambientRoot .ambient-mini {
+        bottom: calc(18px + env(safe-area-inset-bottom, 0px)) !important;
+    }
+
+    #ambientRoot .ambient-peek,
+    body[data-mode="clock"] #ambientRoot .ambient-peek,
+    body:not([data-mode="clock"]) #ambientRoot .ambient-peek {
+        bottom: calc(18px + env(safe-area-inset-bottom, 0px)) !important;
+    }
+
+    body[data-mode="routine"] #routineModule.module.active {
+        padding-bottom: calc(24px + env(safe-area-inset-bottom, 0px));
+    }
+
+    body[data-mode="routine"] .routine-mobile-days {
+        display: flex;
+        flex-wrap: nowrap;
+        overflow-x: auto;
+        padding-bottom: 2px;
+        margin-top: -2px;
+    }
+
+    body[data-mode="routine"] .routine-mobile-days::-webkit-scrollbar {
+        display: none;
+    }
+
+    body[data-mode="routine"] .routine-grid-scroll {
+        overflow-x: hidden;
+    }
 }
 
-@media (max-width: 980px) {
-    body[data-mode="clock"] .hero {
-        width: min(100%, calc(100vw - 28px));
-        min-height: auto;
-        align-items: flex-start;
+@media (max-width: 520px) and (pointer: coarse) {
+    body:not([data-mode="clock"]) .side-modules {
+        width: calc(100vw - 20px);
+        max-width: calc(100vw - 20px);
+        min-height: 42px;
+        margin-bottom: 12px;
+        justify-content: flex-start;
+        padding: 6px 7px;
     }
 
-    .launchpad-primary-grid {
+    body[data-mode="routine"] .routine-v2-setup {
         grid-template-columns: repeat(2, minmax(0, 1fr));
     }
 
-    .launchpad-tool-row {
-        grid-template-columns: repeat(2, minmax(0, 1fr));
+    body[data-mode="routine"] .routine-control-card,
+    body[data-mode="routine"] .routine-control-card.is-wide,
+    body[data-mode="routine"] .routine-control-card.is-generate,
+    body[data-mode="routine"] .routine-v2-setup .routine-control-card:first-child {
+        grid-column: auto;
     }
 
-    body[data-mode="pomodoro"] #pomodoroPresets,
-    body[data-mode="pomodoro"] .date-display,
-    body[data-mode="pomodoro"] .time-display,
-    body[data-mode="pomodoro"] #cycleProgress,
-    body[data-mode="pomodoro"] #todayFlow,
-    body[data-mode="pomodoro"] .controls,
-    body[data-mode="pomodoro"] .main-stats-row,
-    body[data-mode="pomodoro"] #streakVisual,
-    body[data-mode="pomodoro"] #dailyGoal,
-    body[data-mode="pomodoro"] #weeklyChart,
-    body[data-mode="pomodoro"] #sessionLog,
-    body[data-mode="pomodoro"] #advancedStats,
-    body[data-mode="pomodoro"] #achievements,
-    body[data-mode="pomodoro"] #progressiveBars {
-        grid-column: 1 / -1;
+    body[data-mode="routine"] .routine-control-card.is-generate .routine-v2-primary {
+        min-height: 32px;
+    }
+
+    body[data-mode="routine"] .routine-v2-board {
+        padding: 10px;
+    }
+
+    body[data-mode="routine"] .routine-grid {
+        grid-template-columns: clamp(48px, 16vw, 58px) minmax(0, 1fr);
+        width: 100%;
+        min-width: 100%;
+    }
+
+    body[data-mode="routine"] .routine-head.is-action,
+    body[data-mode="routine"] .routine-row-actions {
+        display: none;
+    }
+
+    body[data-mode="routine"] .routine-v2-board[data-active-day="mon"] .routine-day-col:not([data-day="mon"]),
+    body[data-mode="routine"] .routine-v2-board[data-active-day="tue"] .routine-day-col:not([data-day="tue"]),
+    body[data-mode="routine"] .routine-v2-board[data-active-day="wed"] .routine-day-col:not([data-day="wed"]),
+    body[data-mode="routine"] .routine-v2-board[data-active-day="thu"] .routine-day-col:not([data-day="thu"]),
+    body[data-mode="routine"] .routine-v2-board[data-active-day="fri"] .routine-day-col:not([data-day="fri"]),
+    body[data-mode="routine"] .routine-v2-board[data-active-day="sat"] .routine-day-col:not([data-day="sat"]),
+    body[data-mode="routine"] .routine-v2-board[data-active-day="sun"] .routine-day-col:not([data-day="sun"]),
+    body[data-mode="routine"] .routine-v2-board[data-active-day="mon"] .routine-day-cell:not([data-day="mon"]),
+    body[data-mode="routine"] .routine-v2-board[data-active-day="tue"] .routine-day-cell:not([data-day="tue"]),
+    body[data-mode="routine"] .routine-v2-board[data-active-day="wed"] .routine-day-cell:not([data-day="wed"]),
+    body[data-mode="routine"] .routine-v2-board[data-active-day="thu"] .routine-day-cell:not([data-day="thu"]),
+    body[data-mode="routine"] .routine-v2-board[data-active-day="fri"] .routine-day-cell:not([data-day="fri"]),
+    body[data-mode="routine"] .routine-v2-board[data-active-day="sat"] .routine-day-cell:not([data-day="sat"]),
+    body[data-mode="routine"] .routine-v2-board[data-active-day="sun"] .routine-day-cell:not([data-day="sun"]) {
+        display: none;
+    }
+
+    body[data-mode="questions"] #questionsModule .questions-card.questions-entry-card {
+        padding: 14px;
+    }
+
+    body[data-mode="questions"] #questionsModule .questions-head {
+        gap: 10px;
+        margin-bottom: 12px;
+    }
+
+    body[data-mode="questions"] #questionsModule .questions-head h2 {
+        margin: 6px 0;
+        font-size: clamp(1.8rem, 8.4vw, 2.15rem);
+    }
+
+    body[data-mode="questions"] #questionsModule .questions-head p {
+        font-size: 0.9rem;
+        line-height: 1.28;
+    }
+
+    body[data-mode="questions"] #questionsModule .questions-entry-card .questions-entry-grid {
+        gap: 12px;
+    }
+
+    body[data-mode="questions"] #questionsModule .questions-entry-option {
+        min-height: 156px;
+        padding: 16px 14px;
+        gap: 12px;
+    }
+
+    body[data-mode="questions"] #questionsModule .questions-entry-grid > .questions-entry-option .questions-entry-copy {
+        gap: 8px;
+    }
+
+    body[data-mode="questions"] #questionsModule .questions-entry-grid > .questions-entry-option .questions-entry-copy p {
+        max-width: 24ch;
+        font-size: 0.84rem;
+        line-height: 1.26;
+    }
+
+    body[data-mode="questions"] #questionsModule .questions-entry-grid > .questions-entry-option .questions-primary-btn,
+    body[data-mode="questions"] #questionsModule .questions-entry-grid > .questions-entry-option .questions-secondary-btn {
+        min-height: 40px;
     }
 }
 
-@media (max-width: 620px) {
-    :root {
-        --rn-radius-shell: 12px;
-        --rn-radius-card: 9px;
+@media (max-width: 900px) and (max-height: 520px) and (orientation: landscape) and (pointer: coarse) {
+    body {
+        padding-bottom: calc(78px + env(safe-area-inset-bottom, 0px));
     }
 
-    .rotanota-home-brand {
-        grid-template-columns: 1fr;
-        justify-items: start;
-        text-align: left;
+    body:not([data-mode="clock"]) .hero,
+    body:not([data-mode="clock"]) .module.active {
+        padding-bottom: calc(62px + env(safe-area-inset-bottom, 0px));
     }
 
-    .launchpad-primary-grid,
-    .launchpad-tool-row {
-        grid-template-columns: 1fr;
+    body:not([data-mode="clock"]) .side-modules {
+        position: fixed;
+        top: auto;
+        right: 12px;
+        bottom: calc(8px + env(safe-area-inset-bottom, 0px));
+        left: 12px;
+        width: auto;
+        max-width: none;
+        min-height: 44px;
+        margin: 0;
+        transform: none;
+        justify-content: center;
+        overflow-x: auto;
+        padding: 5px 7px;
     }
 
-    .launchpad-card {
-        min-height: 58px;
+    body:not([data-mode="clock"]) .side-group {
+        flex: 0 0 auto;
+    }
+
+    #ambientRoot .ambient-mini,
+    body[data-mode="clock"] #ambientRoot .ambient-mini,
+    body:not([data-mode="clock"]) #ambientRoot .ambient-mini {
+        bottom: calc(12px + env(safe-area-inset-bottom, 0px)) !important;
+    }
+
+    #ambientRoot .ambient-peek,
+    body[data-mode="clock"] #ambientRoot .ambient-peek,
+    body:not([data-mode="clock"]) #ambientRoot .ambient-peek {
+        bottom: calc(12px + env(safe-area-inset-bottom, 0px)) !important;
+    }
+
+    body[data-mode="routine"] #routineModule.module.active {
+        padding-bottom: calc(62px + env(safe-area-inset-bottom, 0px));
+    }
+}
+
+@media (max-width: 760px) and (pointer: coarse) {
+    #utilityRoot .utility-window[data-tool="timer"].utility-window-compact-mobile {
+        width: min(244px, calc(100vw - 24px));
+        gap: 8px;
+        padding: 9px 10px;
+        border-radius: 18px;
+    }
+
+    #utilityRoot .utility-window[data-tool="timer"].utility-window-compact-mobile .utility-window-header {
+        align-items: center;
+        gap: 8px;
+        cursor: default;
+    }
+
+    #utilityRoot .utility-window[data-tool="timer"].utility-window-compact-mobile .utility-window-copy {
+        min-width: 0;
+        flex: 1 1 auto;
+    }
+
+    #utilityRoot .utility-window[data-tool="timer"].utility-window-compact-mobile .utility-window-kicker,
+    #utilityRoot .utility-window[data-tool="timer"].utility-window-compact-mobile .utility-window-title,
+    #utilityRoot .utility-window[data-tool="timer"].utility-window-compact-mobile .utility-window-expand,
+    #utilityRoot .utility-window[data-tool="timer"].utility-window-compact-mobile .utility-timer-fields,
+    #utilityRoot .utility-window[data-tool="timer"].utility-window-compact-mobile .utility-actions button[data-action="play"],
+    #utilityRoot .utility-window[data-tool="timer"].utility-window-compact-mobile .utility-actions button[data-action="reset"] {
+        display: none;
+    }
+
+    #utilityRoot .utility-window[data-tool="timer"].utility-window-compact-mobile .utility-window-actions,
+    #utilityRoot .utility-window[data-tool="timer"].utility-window-compact-mobile .utility-actions {
+        display: inline-flex;
+        align-items: center;
+        gap: 6px;
+    }
+
+    #utilityRoot .utility-window[data-tool="timer"].utility-window-compact-mobile .utility-window-body {
+        grid-template-columns: minmax(0, 1fr) auto;
+        align-items: center;
+        gap: 8px;
+    }
+
+    #utilityRoot .utility-window[data-tool="timer"].utility-window-compact-mobile .utility-display {
+        min-height: 52px;
+        padding: 8px 10px;
+        justify-content: flex-start;
+        font-size: clamp(1.55rem, 8vw, 2rem);
+        line-height: 1;
+        background:
+            linear-gradient(135deg, rgba(255, 255, 255, 0.82), rgba(255, 247, 238, 0.62)),
+            radial-gradient(circle at top, rgba(37, 88, 240, 0.08), transparent 56%);
+    }
+
+    #utilityRoot .utility-window[data-tool="timer"].utility-window-compact-mobile .utility-window-close,
+    #utilityRoot .utility-window[data-tool="timer"].utility-window-compact-mobile .utility-actions button[data-action="pause"] {
+        width: 36px;
+        min-width: 36px;
+        height: 36px;
+        min-height: 36px;
+        padding: 0;
+        border-radius: 12px;
+        clip-path: none;
     }
 }
diff --git a/docs/premium_study_operacao_ai_pagamentos.md b/docs/premium_study_operacao_ai_pagamentos.md
index 5f665f7..1e79130 100644
--- a/docs/premium_study_operacao_ai_pagamentos.md
+++ b/docs/premium_study_operacao_ai_pagamentos.md
@@ -9,6 +9,12 @@ Status:
 
 Atualizado em 2026-04-14.
 
+Documentos complementares desta fase:
+- `docs/premium_study_ops_console.md`
+- `docs/premium_study_ops_runbook.md`
+- `docs/premium_study_ops_copilot.md`
+- `docs/premium_study_ops_promotions.md`
+
 ---
 
 ## 1. Missão deste documento
@@ -667,7 +673,7 @@ No painel de desenvolvedor:
 Quando houver backend/serverless, usar:
 
 ```txt
-https://dominio-do-site.com/api/mercado-pago/webhook
+https://dominio-do-rotanota.com/api/mercado-pago/webhook
 ```
 
 Em desenvolvimento local, `127.0.0.1` nao serve para webhook do Mercado Pago.
@@ -738,7 +744,7 @@ Recomendadas:
 ```txt
 MERCADO_PAGO_PUBLIC_KEY
 MERCADO_PAGO_CURRENCY
-STUDY_OS_BASE_URL
+ROTANOTA_BASE_URL
 MERCADO_PAGO_SUCCESS_URL
 MERCADO_PAGO_FAILURE_URL
 MERCADO_PAGO_PENDING_URL
@@ -948,3 +954,72 @@ Este passo ja impede que o navegador se auto-promova a premium, mas ainda e uma
 - `/api/premium/status?customerId=...` responde mesmo sem Supabase, mantendo gratis
 - com Supabase configurado, pagamento aprovado passa a criar registro em `premium_entitlements`
 - recursos premium passam a consultar `accessTier` vindo do backend
+
+---
+
+## 24. Estado operacional em 2026-04-20
+
+Atualizado em 2026-04-20.
+
+Este bloco consolida o estado real da operacao depois da integracao com Supabase, Gemini, Mercado Pago e retaguarda ops.
+
+### O que esta configurado
+
+- schema do Supabase aplicado
+- Gemini configurado por env
+- checkout Mercado Pago criando preferencia
+- `/api/premium/status` funcionando
+- retaguarda local funcionando em `/ops/`
+
+### O que foi validado localmente
+
+- login da retaguarda
+- overview operacional
+- copiloto Gemini
+- growth
+- promocoes
+- relatorio semanal
+- checkout Mercado Pago
+
+### O que esta pendente
+
+A pendencia principal nao e mais infraestrutura local. O ponto aberto em 2026-04-20 e a publicacao correta da versao nova da retaguarda.
+
+Estado atual:
+
+- localhost esta saudavel
+- `https://rota-nota.vercel.app` esta publicado
+- a producao ja recebeu o deploy corretivo da retaguarda em 2026-04-20
+
+Historico do incidente resolvido:
+
+- `POST /api/ops/login` funciona
+- `GET /api/ops/overview` funciona
+- antes do deploy corretivo, `GET /api/ops/growth/overview` retornava `404`
+- antes do deploy corretivo, `GET /api/ops/reports/weekly` retornava `404`
+- a faixa `Falha na requisicao` antes do login era efeito desse frontend/API antigo publicado
+
+Estado apos a correcao:
+
+- `GET /api/ops/growth/overview` funciona
+- `GET /api/ops/reports/weekly` funciona
+- `ops/app.js` publicado contem a mensagem nova antes da autenticacao
+
+### Diagnostico fechado
+
+O problema atual da retaguarda publicada nao e:
+
+- senha
+- Gemini
+
+O problema atual e:
+
+- deploy publicado com frontend/API de uma versao intermediaria da retaguarda
+
+### Proximo passo objetivo
+
+Com a retaguarda publicada corrigida, o proximo passo volta a ser operacional:
+
+- configurar `MERCADO_PAGO_WEBHOOK_SECRET`
+- validar o webhook publico do Mercado Pago
+- continuar os testes funcionais do `/ops` em producao
diff --git a/docs/questions_v2_indexeddb_schema.md b/docs/questions_v2_indexeddb_schema.md
index f72d7a9..ccf1362 100644
--- a/docs/questions_v2_indexeddb_schema.md
+++ b/docs/questions_v2_indexeddb_schema.md
@@ -21,7 +21,7 @@ Referencia em codigo:
 
 Nome:
 
-- `study_os_questions_v2`
+- `rotanota_questions_v3`
 
 Versao inicial:
 
diff --git a/docs/supabase_premium_schema.sql b/docs/supabase_premium_schema.sql
index 7fd876e..a1527ae 100644
--- a/docs/supabase_premium_schema.sql
+++ b/docs/supabase_premium_schema.sql
@@ -1,6 +1,7 @@
 create table if not exists public.premium_checkout_sessions (
   id uuid primary key default gen_random_uuid(),
   customer_id text not null,
+  user_id text,
   plan_id text not null,
   preference_id text,
   external_reference text,
@@ -21,6 +22,7 @@ create index if not exists premium_checkout_sessions_preference_idx
 
 create table if not exists public.premium_entitlements (
   customer_id text primary key,
+  user_id text,
   access_tier text not null default 'premium',
   status text not null default 'active',
   plan_id text not null,
@@ -37,6 +39,48 @@ create table if not exists public.premium_entitlements (
 create index if not exists premium_entitlements_status_idx
   on public.premium_entitlements (status);
 
+alter table public.premium_checkout_sessions
+  add column if not exists user_id text;
+
+alter table public.premium_entitlements
+  add column if not exists user_id text;
+
+create index if not exists premium_checkout_sessions_user_idx
+  on public.premium_checkout_sessions (user_id);
+
+create index if not exists premium_entitlements_user_idx
+  on public.premium_entitlements (user_id);
+
+create table if not exists public.rotanota_user_accounts (
+  user_id text primary key,
+  provider text not null default 'google',
+  provider_user_id text not null unique,
+  email text not null default '',
+  full_name text not null default '',
+  avatar_url text not null default '',
+  created_at timestamptz not null default now(),
+  updated_at timestamptz not null default now(),
+  last_seen_at timestamptz not null default now()
+);
+
+create index if not exists rotanota_user_accounts_email_idx
+  on public.rotanota_user_accounts (email);
+
+create table if not exists public.rotanota_user_customer_links (
+  user_id text not null references public.rotanota_user_accounts (user_id) on delete cascade,
+  customer_id text not null unique,
+  is_primary boolean not null default false,
+  created_at timestamptz not null default now(),
+  updated_at timestamptz not null default now(),
+  primary key (user_id, customer_id)
+);
+
+create index if not exists rotanota_user_customer_links_user_idx
+  on public.rotanota_user_customer_links (user_id, is_primary desc, created_at asc);
+
+create index if not exists rotanota_user_customer_links_customer_idx
+  on public.rotanota_user_customer_links (customer_id);
+
 create or replace function public.set_updated_at()
 returns trigger
 language plpgsql
@@ -60,3 +104,332 @@ drop trigger if exists premium_entitlements_updated_at
 create trigger premium_entitlements_updated_at
 before update on public.premium_entitlements
 for each row execute function public.set_updated_at();
+
+drop trigger if exists rotanota_user_accounts_updated_at
+  on public.rotanota_user_accounts;
+
+create trigger rotanota_user_accounts_updated_at
+before update on public.rotanota_user_accounts
+for each row execute function public.set_updated_at();
+
+drop trigger if exists rotanota_user_customer_links_updated_at
+  on public.rotanota_user_customer_links;
+
+create trigger rotanota_user_customer_links_updated_at
+before update on public.rotanota_user_customer_links
+for each row execute function public.set_updated_at();
+
+create table if not exists public.premium_study_growth_events (
+  id uuid primary key default gen_random_uuid(),
+  customer_id text,
+  event_type text not null,
+  material_hash text not null default '',
+  channel text not null default 'internal_site',
+  utm_source text not null default '',
+  utm_medium text not null default '',
+  utm_campaign text not null default '',
+  utm_content text not null default '',
+  referrer text not null default '',
+  landing_path text not null default '',
+  metadata jsonb not null default '{}'::jsonb,
+  created_at timestamptz not null default now()
+);
+
+create index if not exists premium_study_growth_events_created_idx
+  on public.premium_study_growth_events (created_at desc);
+
+create index if not exists premium_study_growth_events_customer_idx
+  on public.premium_study_growth_events (customer_id);
+
+create index if not exists premium_study_growth_events_event_idx
+  on public.premium_study_growth_events (event_type);
+
+create table if not exists public.premium_study_channel_spend (
+  id uuid primary key default gen_random_uuid(),
+  period_start date not null,
+  period_end date not null,
+  channel text not null default 'internal_site',
+  campaign text not null default '(manual)',
+  external_platform text not null default '',
+  external_account_id text not null default '',
+  amount numeric(12,2) not null default 0,
+  currency text not null default 'BRL',
+  notes text not null default '',
+  metadata jsonb not null default '{}'::jsonb,
+  created_at timestamptz not null default now(),
+  updated_at timestamptz not null default now()
+);
+
+create index if not exists premium_study_channel_spend_period_idx
+  on public.premium_study_channel_spend (period_start desc, period_end desc);
+
+create index if not exists premium_study_channel_spend_channel_idx
+  on public.premium_study_channel_spend (channel, campaign);
+
+drop trigger if exists premium_study_channel_spend_updated_at
+  on public.premium_study_channel_spend;
+
+create trigger premium_study_channel_spend_updated_at
+before update on public.premium_study_channel_spend
+for each row execute function public.set_updated_at();
+
+create table if not exists public.premium_study_ops_alerts (
+  id uuid primary key default gen_random_uuid(),
+  event_type text not null,
+  severity text not null default 'info',
+  provider text not null default '',
+  message text not null default '',
+  payload jsonb not null default '{}'::jsonb,
+  acknowledged_at timestamptz,
+  resolved_at timestamptz,
+  created_at timestamptz not null default now(),
+  updated_at timestamptz not null default now()
+);
+
+create index if not exists premium_study_ops_alerts_created_idx
+  on public.premium_study_ops_alerts (created_at desc);
+
+create index if not exists premium_study_ops_alerts_event_idx
+  on public.premium_study_ops_alerts (event_type, severity);
+
+drop trigger if exists premium_study_ops_alerts_updated_at
+  on public.premium_study_ops_alerts;
+
+create trigger premium_study_ops_alerts_updated_at
+before update on public.premium_study_ops_alerts
+for each row execute function public.set_updated_at();
+
+create table if not exists public.premium_study_ops_state (
+  state_key text primary key,
+  state_value jsonb not null default '{}'::jsonb,
+  created_at timestamptz not null default now(),
+  updated_at timestamptz not null default now()
+);
+
+drop trigger if exists premium_study_ops_state_updated_at
+  on public.premium_study_ops_state;
+
+create trigger premium_study_ops_state_updated_at
+before update on public.premium_study_ops_state
+for each row execute function public.set_updated_at();
+
+create table if not exists public.premium_study_promotion_campaigns (
+  id uuid primary key default gen_random_uuid(),
+  surface text not null default 'premium_checkout',
+  feature text not null default '',
+  channel text not null default 'internal_site',
+  status text not null default 'draft',
+  mode text not null default 'suggest',
+  headline text not null default '',
+  lead text not null default '',
+  benefits jsonb not null default '[]'::jsonb,
+  cta text not null default '',
+  recommended_plan_id text not null default '',
+  targeting jsonb not null default '{}'::jsonb,
+  external_platform text not null default '',
+  campaign_id text not null default '',
+  adset_id text not null default '',
+  ad_id text not null default '',
+  creative_id text not null default '',
+  created_at timestamptz not null default now(),
+  updated_at timestamptz not null default now()
+);
+
+create index if not exists premium_study_promotion_campaigns_status_idx
+  on public.premium_study_promotion_campaigns (status, surface, channel);
+
+create index if not exists premium_study_promotion_campaigns_feature_idx
+  on public.premium_study_promotion_campaigns (feature, updated_at desc);
+
+drop trigger if exists premium_study_promotion_campaigns_updated_at
+  on public.premium_study_promotion_campaigns;
+
+create trigger premium_study_promotion_campaigns_updated_at
+before update on public.premium_study_promotion_campaigns
+for each row execute function public.set_updated_at();
+
+create table if not exists public.premium_study_promotion_actions (
+  id uuid primary key default gen_random_uuid(),
+  campaign_id uuid references public.premium_study_promotion_campaigns (id) on delete cascade,
+  action_type text not null,
+  status text not null default 'completed',
+  mode text not null default 'suggest',
+  reason text not null default '',
+  payload jsonb not null default '{}'::jsonb,
+  created_at timestamptz not null default now()
+);
+
+create index if not exists premium_study_promotion_actions_campaign_idx
+  on public.premium_study_promotion_actions (campaign_id, created_at desc);
+
+create table if not exists public.premium_study_promotion_rules (
+  id uuid primary key default gen_random_uuid(),
+  name text not null,
+  channel text not null default 'internal_site',
+  surface text not null default 'premium_checkout',
+  feature text not null default '',
+  status text not null default 'draft',
+  approved boolean not null default false,
+  priority integer not null default 100,
+  rule_type text not null default 'conversion_guardrail',
+  conditions jsonb not null default '{}'::jsonb,
+  actions jsonb not null default '{}'::jsonb,
+  notes text not null default '',
+  external_platform text not null default '',
+  created_at timestamptz not null default now(),
+  updated_at timestamptz not null default now()
+);
+
+create index if not exists premium_study_promotion_rules_status_idx
+  on public.premium_study_promotion_rules (status, approved, priority);
+
+drop trigger if exists premium_study_promotion_rules_updated_at
+  on public.premium_study_promotion_rules;
+
+create trigger premium_study_promotion_rules_updated_at
+before update on public.premium_study_promotion_rules
+for each row execute function public.set_updated_at();
+
+create table if not exists public.northstar_app_work_items (
+  id uuid primary key default gen_random_uuid(),
+  app_key text not null,
+  item_type text not null default 'improvement',
+  title text not null default '',
+  summary text not null default '',
+  status text not null default 'open',
+  priority integer not null default 100,
+  owner text not null default '',
+  metadata jsonb not null default '{}'::jsonb,
+  created_at timestamptz not null default now(),
+  updated_at timestamptz not null default now()
+);
+
+create index if not exists northstar_app_work_items_app_idx
+  on public.northstar_app_work_items (app_key, status, priority);
+
+drop trigger if exists northstar_app_work_items_updated_at
+  on public.northstar_app_work_items;
+
+create trigger northstar_app_work_items_updated_at
+before update on public.northstar_app_work_items
+for each row execute function public.set_updated_at();
+
+create table if not exists public.northstar_app_bug_reports (
+  id uuid primary key default gen_random_uuid(),
+  app_key text not null,
+  title text not null default '',
+  description text not null default '',
+  severity text not null default 'medium',
+  status text not null default 'open',
+  source_channel text not null default 'ops_console',
+  reporter_email text not null default '',
+  metadata jsonb not null default '{}'::jsonb,
+  created_at timestamptz not null default now(),
+  updated_at timestamptz not null default now()
+);
+
+create index if not exists northstar_app_bug_reports_app_idx
+  on public.northstar_app_bug_reports (app_key, status, severity);
+
+drop trigger if exists northstar_app_bug_reports_updated_at
+  on public.northstar_app_bug_reports;
+
+create trigger northstar_app_bug_reports_updated_at
+before update on public.northstar_app_bug_reports
+for each row execute function public.set_updated_at();
+
+create table if not exists public.northstar_app_finance_snapshots (
+  id uuid primary key default gen_random_uuid(),
+  app_key text not null,
+  period_start date not null,
+  period_end date not null,
+  revenue_amount numeric(12,2) not null default 0,
+  expense_amount numeric(12,2) not null default 0,
+  currency text not null default 'BRL',
+  source_channel text not null default 'manual',
+  notes text not null default '',
+  metadata jsonb not null default '{}'::jsonb,
+  created_at timestamptz not null default now(),
+  updated_at timestamptz not null default now()
+);
+
+create index if not exists northstar_app_finance_snapshots_app_idx
+  on public.northstar_app_finance_snapshots (app_key, period_start desc, period_end desc);
+
+drop trigger if exists northstar_app_finance_snapshots_updated_at
+  on public.northstar_app_finance_snapshots;
+
+create trigger northstar_app_finance_snapshots_updated_at
+before update on public.northstar_app_finance_snapshots
+for each row execute function public.set_updated_at();
+
+create table if not exists public.northstar_change_requests (
+  id uuid primary key default gen_random_uuid(),
+  review_run_id uuid,
+  target_system text not null,
+  action_type text not null,
+  payload jsonb not null default '{}'::jsonb,
+  prepared_by text not null default 'northstar',
+  status text not null default 'pending',
+  approval_notes text not null default '',
+  approved_by text not null default '',
+  rejected_by text not null default '',
+  executed_at timestamptz,
+  result_summary text not null default '',
+  created_at timestamptz not null default now(),
+  updated_at timestamptz not null default now()
+);
+
+create index if not exists northstar_change_requests_status_idx
+  on public.northstar_change_requests (status, target_system, created_at desc);
+
+drop trigger if exists northstar_change_requests_updated_at
+  on public.northstar_change_requests;
+
+create trigger northstar_change_requests_updated_at
+before update on public.northstar_change_requests
+for each row execute function public.set_updated_at();
+
+create table if not exists public.northstar_review_runs (
+  id uuid primary key default gen_random_uuid(),
+  run_type text not null default 'three_day_growth_review',
+  provider text not null default 'fallback',
+  status text not null default 'completed',
+  summary text not null default '',
+  recommendations jsonb not null default '{}'::jsonb,
+  confidence text not null default '',
+  missing_data jsonb not null default '[]'::jsonb,
+  generated_change_requests integer not null default 0,
+  started_at timestamptz not null default now(),
+  completed_at timestamptz,
+  created_at timestamptz not null default now(),
+  updated_at timestamptz not null default now()
+);
+
+create index if not exists northstar_review_runs_type_idx
+  on public.northstar_review_runs (run_type, created_at desc);
+
+drop trigger if exists northstar_review_runs_updated_at
+  on public.northstar_review_runs;
+
+create trigger northstar_review_runs_updated_at
+before update on public.northstar_review_runs
+for each row execute function public.set_updated_at();
+
+create table if not exists public.northstar_audit_log (
+  id uuid primary key default gen_random_uuid(),
+  event_type text not null,
+  actor text not null default 'northstar',
+  target_system text not null default '',
+  entity_type text not null default '',
+  entity_id text not null default '',
+  status text not null default 'info',
+  metadata jsonb not null default '{}'::jsonb,
+  created_at timestamptz not null default now()
+);
+
+create index if not exists northstar_audit_log_created_idx
+  on public.northstar_audit_log (created_at desc);
+
+create index if not exists northstar_audit_log_target_idx
+  on public.northstar_audit_log (target_system, event_type, created_at desc);
diff --git a/index.html b/index.html
index 1e72165..fd14252 100644
--- a/index.html
+++ b/index.html
@@ -32,6 +32,19 @@
   </div>
 </div>
 
+<div id="homeStatsLoginGate" class="home-login-gate hidden" aria-hidden="true">
+  <div class="home-login-gate-backdrop" data-home-login-close="true"></div>
+  <div class="home-login-gate-card" role="dialog" aria-modal="true" aria-labelledby="homeStatsLoginGateTitle">
+    <span class="home-login-gate-kicker">Estatísticas</span>
+    <h2 id="homeStatsLoginGateTitle">Entre com Google para abrir o painel completo</h2>
+    <p>A prévia já aparece na home. O painel completo destrava histórico detalhado, evolução por frente e continuidade entre dispositivos.</p>
+    <div class="home-login-gate-actions">
+      <button class="home-login-gate-secondary" type="button" data-home-login-close="true">Agora não</button>
+      <button class="home-login-gate-primary" type="button" id="homeStatsLoginGatePrimary">Continuar</button>
+    </div>
+  </div>
+</div>
+
 <header class="topbar">
     <div class="topbar-left">
         <button id="homeBtn" class="icon-circle" title="Início" aria-label="Início">
@@ -40,11 +53,11 @@
                 <path d="M6 10.5V20h12v-9.5" stroke-linecap="round" stroke-linejoin="round"/>
             </svg>
         </button>
-        <div class="rotanota-topbar-brand" aria-label="RotaNota">
-            <img src="assets/rotanota-mark.svg" alt="" width="34" height="34">
-            <span><strong>Rota<span>Nota</span></strong><small>Sua rota. Sua melhor nota.</small></span>
-        </div>
-        <select id="timezoneSelector" class="timezone-topbar"></select>
+        <button id="brandHomeBtn" class="rotanota-topbar-brand" type="button" title="Ir para inicio" aria-label="Ir para inicio">
+            <img class="rotanota-topbar-mark" src="assets/rotanota-mark.svg" alt="" width="34" height="34">
+            <img class="rotanota-topbar-logo rotanota-topbar-logo-light" src="assets/rotanota-logo-light.svg" alt="" width="162" height="42">
+            <img class="rotanota-topbar-logo rotanota-topbar-logo-dark" src="assets/rotanota-logo-dark.svg" alt="" width="162" height="42">
+        </button>
     </div>
 
     <div class="topbar-right">
@@ -55,7 +68,7 @@
                 <path d="M7 14h10v6H7z" stroke-linecap="round" stroke-linejoin="round"/>
             </svg>
         </button>
-        <button id="focusModeBtn" class="icon-circle" title="Modo foco (Shift + D)" aria-label="Modo foco (Shift + D)">
+        <button id="focusModeBtn" class="icon-circle" title="Modo imers&atilde;o (Shift + D)" aria-label="Modo imers&atilde;o (Shift + D)">
             <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="1.8">
                 <circle cx="12" cy="12" r="6"/>
                 <path d="M12 2v3M12 19v3M2 12h3M19 12h3" stroke-linecap="round"/>
@@ -66,14 +79,6 @@
                 <path d="M8 3H3v5M16 3h5v5M3 16v5h5M21 16v5h-5" stroke-linecap="round" stroke-linejoin="round"/>
             </svg>
         </button>
-        <button id="styleToggle" class="icon-circle" title="Estilo visual" aria-label="Estilo visual">
-            <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="1.8">
-                <path d="M12 3a9 9 0 1 0 9 9c0-.9-.7-1.5-1.5-1.5H17a2 2 0 0 1-2-2V6.5C15 5.7 14.3 5 13.5 5H12z" stroke-linecap="round" stroke-linejoin="round"/>
-                <circle cx="7.5" cy="10" r="1"/>
-                <circle cx="9.5" cy="15" r="1"/>
-                <circle cx="14.5" cy="15" r="1"/>
-            </svg>
-        </button>
         <button id="darkToggle" class="icon-circle" title="Modo escuro" aria-label="Modo escuro">
             <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="1.8">
                 <path d="M20 14.5A8.5 8.5 0 1 1 9.5 4 7 7 0 0 0 20 14.5z" stroke-linecap="round" stroke-linejoin="round"/>
@@ -132,12 +137,102 @@
     <div id="progressiveBars" class="progressive-bars"></div>
 
     <div id="homeLaunchpad" class="home-launchpad">
-        <section class="rotanota-home-brand" aria-label="RotaNota">
-            <img src="assets/rotanota-mark.svg" alt="" width="82" height="82">
-            <div>
-                <span>Rota<span>Nota</span></span>
-                <strong>Seu material vira uma rota direta para a prova.</strong>
-                <p>Carregue, organize, pratique e avance com foco no resultado.</p>
+        <section class="rotanota-home-band" aria-label="RotaNota e consistencia">
+            <div class="rotanota-home-band-copy">
+                <div class="rotanota-home-lockup">
+                    <img class="rotanota-home-logo" src="assets/rotanota-logo-light.svg" alt="RotaNota" width="300" height="76">
+                </div>
+                <div class="rotanota-home-band-text">
+                    <strong>sua Rota. sua Nota</strong>
+                </div>
+            </div>
+
+            <button class="rotanota-home-band-widget" type="button" data-home-gated="stats" aria-label="Abrir estatisticas com login">
+                <div class="rotanota-home-band-widget-head">
+                    <span id="homeBandPanelLabel">Consist&ecirc;ncia</span>
+                    <strong id="homeBandPanelValue">0 dias</strong>
+                </div>
+                <div id="homeBandPanelBody" class="rotanota-home-band-panel"></div>
+            </button>
+        </section>
+
+        <section class="home-immersion-entry" aria-label="Horario de imersao">
+            <div class="home-immersion-copy">
+                <span>Agora</span>
+                <strong id="homeCompactTime">00:00</strong>
+                <p id="homeCompactDate">Organize o ritmo antes de estudar.</p>
+            </div>
+            <button id="homeImmersionBtn" class="home-immersion-btn" type="button">
+                Modo imers&atilde;o
+            </button>
+        </section>
+
+        <section class="home-stats-shell" aria-label="Prévia das estatísticas">
+            <div class="home-stats-stage">
+                <div class="home-stats-nav">
+                    <button class="home-stats-nav-btn is-active" type="button" data-home-gated="stats">Resumo</button>
+                    <button class="home-stats-nav-btn" type="button" data-home-gated="stats">Onde melhorar</button>
+                    <button class="home-stats-nav-btn" type="button" data-home-gated="stats">Evolu&ccedil;&atilde;o</button>
+                    <button class="home-stats-nav-btn" type="button" data-home-gated="stats">Consist&ecirc;ncia</button>
+                </div>
+
+                <button class="home-stats-widget" type="button" data-home-gated="stats">
+                    <div class="home-stats-head">
+                        <div>
+                            <span class="home-stats-kicker">Estatísticas</span>
+                            <strong id="homeStatsHeadline">Seu progresso começa a ganhar forma.</strong>
+                            <p id="homeStatsSubline">A prévia aparece aqui antes do login completo.</p>
+                        </div>
+                        <span class="home-stats-gate-chip">Entrar com Google</span>
+                    </div>
+
+                    <div class="home-stats-metrics">
+                        <article class="home-stats-metric">
+                            <span>Acurácia</span>
+                            <strong id="homeStatsAccuracy">0%</strong>
+                            <small id="homeStatsAccuracyNote">Sem base ainda</small>
+                        </article>
+                        <article class="home-stats-metric">
+                            <span>Sessões</span>
+                            <strong id="homeStatsSessions">0</strong>
+                            <small id="homeStatsSessionsNote">Nenhuma registrada</small>
+                        </article>
+                        <article class="home-stats-metric">
+                            <span>Retomadas</span>
+                            <strong id="homeStatsPausedRuns">0</strong>
+                            <small id="homeStatsPausedRunsNote">Nada pausado</small>
+                        </article>
+                        <article class="home-stats-metric">
+                            <span>Guardados</span>
+                            <strong id="homeStatsSavedBlocks">0</strong>
+                            <small id="homeStatsSavedBlocksNote">Sem blocos salvos</small>
+                        </article>
+                    </div>
+
+                    <div class="home-stats-content">
+                        <section class="home-stats-panel">
+                            <div class="home-stats-panel-head">
+                                <span>Onde melhorar</span>
+                            </div>
+                            <div id="homeStatsWeakTopics" class="home-stats-tags"></div>
+                        </section>
+
+                        <section class="home-stats-panel">
+                            <div class="home-stats-panel-head">
+                                <span>Evolução</span>
+                            </div>
+                            <div id="homeStatsTrend" class="home-stats-trend"></div>
+                        </section>
+
+                        <section class="home-stats-panel home-stats-panel-consistency">
+                            <div class="home-stats-panel-head">
+                                <span>Consistência</span>
+                                <strong id="homeStatsStreakLegacy">0 dias</strong>
+                            </div>
+                            <div id="homeStatsConsistencyLegacy" class="home-stats-consistency"></div>
+                        </section>
+                    </div>
+                </button>
             </div>
         </section>
 
@@ -147,8 +242,8 @@
                     <span class="launchpad-card-title">Pomodoro</span>
                 </button>
 
-                <button data-module="qts" class="launchpad-card" type="button">
-                    <span class="launchpad-card-title">Quadro semanal</span>
+                <button data-module="routine" class="launchpad-card" type="button">
+                    <span class="launchpad-card-title">Rotina semanal</span>
                 </button>
 
                 <button data-module="premium-study" class="launchpad-card" type="button">
@@ -177,6 +272,7 @@
     <section id="alarmModule" class="module"></section>
     <section id="calendarModule" class="module"></section>
     <section id="pomodoroModule" class="module"></section>
+    <section id="routineModule" class="module"></section>
     <section id="qtsModule" class="module"></section>
     <section id="premium-studyModule" class="module"></section>
     <section id="questionsModule" class="module">
@@ -222,7 +318,7 @@
 </svg>
 </button>
 
-<button data-module="qts" data-tier="primary" class="icon-circle footer-icon" data-label="Quadro semanal">
+<button data-module="routine" data-tier="primary" class="icon-circle footer-icon" data-label="Rotina semanal">
 <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
 <rect x="3" y="3" width="7" height="7" />
 <rect x="14" y="3" width="7" height="7" />
@@ -261,6 +357,7 @@
 <script src="js/alarm.js"></script>
 <script src="js/calendar.js"></script>
 <script src="js/pomodoro.js"></script>
+<script src="js/routineTable.js"></script>
 <script src="js/qts.js"></script>
 <script src="js/stats.js"></script>
 
diff --git a/js/app.js b/js/app.js
index e706b68..d91c72b 100644
--- a/js/app.js
+++ b/js/app.js
@@ -21,22 +21,23 @@ const Core = {
     shortcutHideTimer: null,
     topbar: null,
     topbarCollapseBtn: null,
-    stylePanel: null,
-    styleToggle: null,
-    styleStorageKey: "study_os_style_theme",
-    topbarStorageKey: "study_os_topbar_collapsed",
+    topbarStorageKey: "rotanota_topbar_collapsed",
     ambientFocusPanelMode: null,
-    supportedStyles: [
-        "clarity",
-        "immersion",
-        "pulse"
-    ],
     premiumStudyLoadPromise: null,
+    homeStatsRefreshToken: 0,
+    homeBandPanelTimer: null,
+    homeBandPanelIndex: 0,
+    pendingGoogleGateIntent: null,
+    historyReady: false,
+    lastHistoryTarget: "home",
 
     /* ================= INIT ================= */
 
     init() {
+        window.RotaNotaCore = this;
         this.cacheDOM();
+        this.ensureAuthBridge();
+        this.upgradeHomeLoginGate();
         this.initStyleTheme();
         this.registerModules();
         if (typeof UtilityWindows !== "undefined") {
@@ -47,9 +48,259 @@ const Core = {
         this.initFullscreen();
         this.initDarkMode();
         this.initTopbarCollapse();
-        this.goHome();
-        this.initShortcutHint();
         this.handlePremiumPaymentReturn();
+        const initialHistoryTarget =
+            this.initBrowserHistory();
+
+        if (this.state.mode === "clock") {
+            if (initialHistoryTarget === "home") {
+                this.goHome({
+                    replaceHistory: true
+                });
+            } else {
+                this.navigate(
+                    initialHistoryTarget,
+                    {
+                        replaceHistory: true
+                    }
+                );
+            }
+        } else {
+            this.syncBrowserHistory(
+                this.state.mode,
+                {
+                    replace: true,
+                    force: true
+                }
+            );
+        }
+
+        this.initShortcutHint();
+        this.schedulePremiumStudyPreload();
+        this.refreshHomeStatsWidget();
+    },
+
+    ensureAuthBridge() {
+        if (window.RotaNotaAuth) {
+            return;
+        }
+
+        const existing = document.querySelector(
+            'script[data-rotanota-auth="true"]'
+        );
+
+        if (existing) {
+            return;
+        }
+
+        const script = document.createElement("script");
+        script.src = "js/auth.js";
+        script.defer = true;
+        script.setAttribute("data-rotanota-auth", "true");
+        document.body.appendChild(script);
+    },
+
+    upgradeHomeLoginGate() {
+        const gateCard =
+            document.querySelector(
+                "#homeStatsLoginGate .home-login-gate-card"
+            );
+
+        if (!gateCard) {
+            return;
+        }
+
+        gateCard.innerHTML = `
+            <div class="home-login-gate-shell">
+                <span id="homeLoginGateKicker" class="home-login-gate-kicker">Conta Google</span>
+                <div class="home-login-gate-lockup" aria-hidden="true">
+                    <div class="home-login-gate-brand home-login-gate-brand-rotanota">
+                        <img src="assets/rotanota-logo-dark.svg" alt="" class="home-login-gate-brand-rotanota-logo">
+                    </div>
+                    <span class="home-login-gate-merge" aria-hidden="true"></span>
+                    <div class="home-login-gate-brand home-login-gate-brand-google">
+                        <span class="home-login-gate-google-mark">
+                            <svg viewBox="0 0 48 48" focusable="false" aria-hidden="true">
+                                <path fill="#EA4335" d="M24 9.5c3.15 0 6 1.09 8.24 3.22l6.15-6.15C34.63 3.11 29.72 1 24 1 14.63 1 6.54 6.38 2.56 14.22l7.58 5.89C11.96 13.81 17.47 9.5 24 9.5z"></path>
+                                <path fill="#4285F4" d="M46.98 24.55c0-1.57-.14-3.08-.4-4.55H24v9.04h12.9c-.56 3-2.25 5.54-4.8 7.25l7.4 5.74c4.32-3.98 6.48-9.85 6.48-17.48z"></path>
+                                <path fill="#FBBC05" d="M10.14 28.11a14.51 14.51 0 0 1 0-8.22l-7.58-5.89a23.02 23.02 0 0 0 0 20l7.58-5.89z"></path>
+                                <path fill="#34A853" d="M24 48c6.48 0 11.92-2.13 15.89-5.8l-7.4-5.74c-2.06 1.38-4.7 2.19-8.49 2.19-6.53 0-12.04-4.31-14.02-10.61l-7.58 5.89C6.54 41.62 14.63 48 24 48z"></path>
+                            </svg>
+                        </span>
+                    </div>
+                </div>
+                <h2 id="homeStatsLoginGateTitle">Entre com Google para continuar</h2>
+                <p id="homeLoginGateDescription">Sua conta segura painel, premium e continuidade entre dispositivos.</p>
+                <div id="homeLoginGateRoute" class="home-login-gate-route">O acesso continua so depois do login.</div>
+                <div id="homeLoginGateStatus" class="home-login-gate-status hidden" aria-live="polite"></div>
+            </div>
+            <div class="home-login-gate-actions">
+                <button class="home-login-gate-secondary" type="button" data-home-login-close="true">Agora nao</button>
+                <div class="home-login-gate-google-control">
+                    <div class="home-login-gate-primary home-login-gate-primary-visual" aria-hidden="true">
+                        <span class="home-login-gate-primary-mark" aria-hidden="true">
+                            <svg viewBox="0 0 48 48" focusable="false" aria-hidden="true">
+                                <path fill="#EA4335" d="M24 9.5c3.15 0 6 1.09 8.24 3.22l6.15-6.15C34.63 3.11 29.72 1 24 1 14.63 1 6.54 6.38 2.56 14.22l7.58 5.89C11.96 13.81 17.47 9.5 24 9.5z"></path>
+                                <path fill="#4285F4" d="M46.98 24.55c0-1.57-.14-3.08-.4-4.55H24v9.04h12.9c-.56 3-2.25 5.54-4.8 7.25l7.4 5.74c4.32-3.98 6.48-9.85 6.48-17.48z"></path>
+                                <path fill="#FBBC05" d="M10.14 28.11a14.51 14.51 0 0 1 0-8.22l-7.58-5.89a23.02 23.02 0 0 0 0 20l7.58-5.89z"></path>
+                                <path fill="#34A853" d="M24 48c6.48 0 11.92-2.13 15.89-5.8l-7.4-5.74c-2.06 1.38-4.7 2.19-8.49 2.19-6.53 0-12.04-4.31-14.02-10.61l-7.58 5.89C6.54 41.62 14.63 48 24 48z"></path>
+                            </svg>
+                        </span>
+                        <span>Continuar com o Google</span>
+                    </div>
+                    <div class="home-login-gate-google-button-slot" id="homeStatsLoginGateButtonSlot" aria-live="polite"></div>
+                </div>
+            </div>
+        `;
+    },
+
+    getGoogleGateIntent(intent = "stats") {
+        if (typeof intent === "string") {
+            return {
+                kind: intent === "stats"
+                    ? "stats"
+                    : intent,
+                source: intent
+            };
+        }
+
+        return {
+            kind: String(intent.kind || intent.source || "stats").trim() || "stats",
+            source: String(intent.source || intent.kind || "stats").trim() || "stats",
+            planId: String(intent.planId || "").trim(),
+            planLabel: String(intent.planLabel || "").trim()
+        };
+    },
+
+    getGoogleGateCopy(intent = {}) {
+        const normalized =
+            this.getGoogleGateIntent(intent);
+
+        if (normalized.kind === "premium_checkout") {
+            const planLabel =
+                normalized.planLabel ||
+                (normalized.planId === "premium_annual"
+                    ? "Premium anual"
+                    : "Premium mensal");
+
+            return {
+                kicker: "Premium",
+                title: `Entre com Google para continuar com ${planLabel}`,
+                description: "Uma conta unica para premium e continuidade.",
+                route: `Depois do login, voce segue para ${planLabel}.`
+            };
+        }
+
+        if (normalized.kind === "account") {
+            return {
+                kicker: "Conta Google",
+                title: "Entre com Google para usar sua conta",
+                description: "",
+                route: ""
+            };
+        }
+
+        return {
+            kicker: "Estatisticas",
+            title: "Entre com Google para abrir seu painel completo",
+            description: "O painel completo fica salvo na sua conta.",
+            route: "Depois do login, abrimos seu painel completo."
+        };
+    },
+
+    updateGoogleGateContent(intent = "stats") {
+        const copy =
+            this.getGoogleGateCopy(intent);
+        const kicker =
+            document.getElementById(
+                "homeLoginGateKicker"
+            );
+        const title =
+            document.getElementById(
+                "homeStatsLoginGateTitle"
+            );
+        const description =
+            document.getElementById(
+                "homeLoginGateDescription"
+            );
+        const route =
+            document.getElementById(
+                "homeLoginGateRoute"
+            );
+
+        if (kicker) {
+            kicker.textContent =
+                copy.kicker;
+        }
+
+        if (title) {
+            title.textContent =
+                copy.title;
+        }
+
+        if (description) {
+            description.textContent =
+                copy.description;
+            description.classList.toggle(
+                "hidden",
+                !copy.description
+            );
+        }
+
+        if (route) {
+            route.textContent =
+                copy.route;
+            route.classList.toggle(
+                "hidden",
+                !copy.route
+            );
+        }
+
+        this.setGoogleGateStatus("");
+    },
+
+    setGoogleGateStatus(message = "", options = {}) {
+        const status =
+            document.getElementById(
+                "homeLoginGateStatus"
+            );
+
+        if (!status) {
+            return;
+        }
+
+        if (message) {
+            status.textContent = message;
+            status.classList.toggle(
+                "is-soft",
+                options.level === "soft"
+            );
+            status.classList.remove("hidden");
+            return;
+        }
+
+        status.textContent = "";
+        status.classList.remove("is-soft");
+        status.classList.add("hidden");
+    },
+
+    isGoogleAuthenticated() {
+        if (
+            window.RotaNotaAuth &&
+            typeof window.RotaNotaAuth
+                .isAuthenticated ===
+                "function"
+        ) {
+            try {
+                return Boolean(
+                    window.RotaNotaAuth.isAuthenticated()
+                );
+            } catch (_error) {
+                return false;
+            }
+        }
+
+        return false;
     },
 
     getPremiumPaymentReturn() {
@@ -99,7 +350,7 @@ const Core = {
 
         if (typeof Pomodoro !== "undefined")
             this.modules.pomodoro = Pomodoro;
-        
+
     },
 
        getActiveModule() {
@@ -131,11 +382,6 @@ const Core = {
         this.topbarCollapseBtn =
             document.getElementById("topbarCollapseBtn") || null;
 
-        this.stylePanel =
-            document.getElementById("stylePanel") || null;
-
-        this.styleToggle =
-            document.getElementById("styleToggle") || null;
     },
 
     bindUI() {
@@ -149,6 +395,41 @@ const Core = {
             });
         }
 
+        const homeImmersionBtn =
+            document.getElementById("homeImmersionBtn");
+
+        if (homeImmersionBtn) {
+            homeImmersionBtn.addEventListener("click", () => {
+                this.enterImmersionMode();
+            });
+        }
+
+        document
+            .querySelectorAll("[data-home-gated]")
+            .forEach((button) => {
+                button.addEventListener("click", () => {
+                    const intent = {
+                        kind: button.dataset.homeGated ||
+                            "stats",
+                        source: button.dataset.homeGated ||
+                            "stats"
+                    };
+                    const gate =
+                        this.requireGoogleLogin(
+                            intent
+                        );
+
+                    if (
+                        gate &&
+                        gate.allowed === true
+                    ) {
+                        this.handleGoogleLoginSuccess(
+                            { intent }
+                        );
+                    }
+                });
+            });
+
         this.footerButtons.forEach(btn => {
 
             btn.addEventListener("click", () => {
@@ -158,24 +439,30 @@ const Core = {
             });
         });
 
-        const homeBtn =
-            document.getElementById("homeBtn");
+        const handleHomeClick = () => {
+            if (
+                this.state.mode ===
+                    "questions" &&
+                window.QuestionsPage &&
+                typeof QuestionsPage
+                    .prepareHomeExit ===
+                    "function"
+            ) {
+                QuestionsPage.prepareHomeExit();
+            }
+            this.goHome();
+        };
 
-        if (homeBtn) {
-            homeBtn.addEventListener("click", () => {
-                if (
-                    this.state.mode ===
-                        "questions" &&
-                    window.QuestionsPage &&
-                    typeof QuestionsPage
-                        .prepareHomeExit ===
-                        "function"
-                ) {
-                    QuestionsPage.prepareHomeExit();
-                }
-                this.goHome();
-            });
-        }
+        [
+            document.getElementById("homeBtn"),
+            document.getElementById("brandHomeBtn")
+        ].forEach((homeBtn) => {
+            if (!homeBtn) {
+                return;
+            }
+
+            homeBtn.addEventListener("click", handleHomeClick);
+        });
 
         if (this.topbarCollapseBtn) {
             this.topbarCollapseBtn.addEventListener("click", () => {
@@ -183,11 +470,81 @@ const Core = {
             });
         }
 
-        if (this.styleToggle) {
-            this.styleToggle.addEventListener("click", () => {
-                this.cycleStyleTheme();
+        window.addEventListener("focus", () => {
+            this.refreshHomeStatsWidget();
+        });
+
+        document
+            .querySelectorAll(
+                "[data-home-login-close]"
+            )
+            .forEach((button) => {
+                button.addEventListener(
+                    "click",
+                    () => {
+                        this.closeHomeStatsLoginGate();
+                    }
+                );
             });
-        }
+
+        document.addEventListener(
+            "keydown",
+            (event) => {
+                if (event.key === "Escape") {
+                    this.closeHomeStatsLoginGate();
+                }
+            }
+        );
+
+        window.addEventListener(
+            "rotanota:google-login-success",
+            (event) => {
+                this.handleGoogleLoginSuccess(
+                    event.detail || {}
+                );
+            }
+        );
+
+        window.addEventListener(
+            "rotanota:google-login-complete",
+            (event) => {
+                this.handleGoogleLoginSuccess(
+                    event.detail || {}
+                );
+            }
+        );
+
+        window.addEventListener(
+            "rotanota:google-login-error",
+            (event) => {
+                this.setGoogleGateStatus(
+                    event.detail &&
+                    event.detail.message
+                        ? event.detail.message
+                        : "Nao foi possivel concluir o login Google agora.",
+                    {
+                        level: event.detail && event.detail.level
+                            ? event.detail.level
+                            : "error"
+                    }
+                );
+            }
+        );
+
+        window.addEventListener(
+            "rotanota:auth-changed",
+            () => {
+                this.refreshHomeStatsWidget();
+
+                if (
+                    window.PremiumStudyAccount &&
+                    typeof window.PremiumStudyAccount.refreshAndApply ===
+                        "function"
+                ) {
+                    window.PremiumStudyAccount.refreshAndApply();
+                }
+            }
+        );
 
         const playBtn =
             document.getElementById("playBtn");
@@ -276,6 +633,146 @@ const Core = {
         });
     },
 
+    normalizeHistoryTarget(target = "") {
+        const safeTarget = String(
+            target || ""
+        ).trim();
+
+        if (!safeTarget || safeTarget === "clock") {
+            return "home";
+        }
+
+        if (safeTarget === "alarm") {
+            return "calendar";
+        }
+
+        return safeTarget;
+    },
+
+    getHistoryTargetFromLocation() {
+        const hashTarget = String(
+            window.location.hash || ""
+        )
+            .replace(/^#/, "")
+            .trim();
+
+        return this.normalizeHistoryTarget(
+            hashTarget || "home"
+        );
+    },
+
+    getHistoryUrl(target = "") {
+        const safeTarget =
+            this.normalizeHistoryTarget(target);
+        const hash =
+            safeTarget === "home"
+                ? ""
+                : `#${safeTarget}`;
+
+        return `${window.location.pathname}${hash}`;
+    },
+
+    syncBrowserHistory(
+        target = "",
+        options = {}
+    ) {
+        if (
+            !this.historyReady ||
+            !window.history
+        ) {
+            return;
+        }
+
+        const safeTarget =
+            this.normalizeHistoryTarget(target);
+        const replace = Boolean(
+            options.replace
+        );
+        const force = Boolean(options.force);
+
+        if (
+            !force &&
+            safeTarget ===
+                this.lastHistoryTarget
+        ) {
+            return;
+        }
+
+        const nextState = {
+            rotanota: true,
+            target: safeTarget
+        };
+        const nextUrl =
+            this.getHistoryUrl(safeTarget);
+
+        if (
+            replace &&
+            typeof window.history
+                .replaceState === "function"
+        ) {
+            window.history.replaceState(
+                nextState,
+                document.title,
+                nextUrl
+            );
+        } else if (
+            typeof window.history
+                .pushState === "function"
+        ) {
+            window.history.pushState(
+                nextState,
+                document.title,
+                nextUrl
+            );
+        }
+
+        this.lastHistoryTarget =
+            safeTarget;
+    },
+
+    initBrowserHistory() {
+        const initialTarget =
+            this.getHistoryTargetFromLocation();
+
+        window.addEventListener(
+            "popstate",
+            (event) => {
+                const popTarget =
+                    this.normalizeHistoryTarget(
+                        event.state?.target ||
+                            this.getHistoryTargetFromLocation()
+                    );
+
+                this.lastHistoryTarget =
+                    popTarget;
+
+                if (popTarget === "home") {
+                    this.goHome({
+                        skipHistory: true
+                    });
+                    return;
+                }
+
+                this.navigate(popTarget, {
+                    skipHistory: true
+                });
+            }
+        );
+
+        this.historyReady = true;
+        this.lastHistoryTarget =
+            initialTarget;
+        this.syncBrowserHistory(
+            initialTarget,
+            {
+                replace: true,
+                force: true
+            }
+        );
+
+        return initialTarget;
+    },
+
     activateExternalModule(target) {
         this.stopAll();
         this.clearVisualResidues(target);
@@ -313,9 +810,54 @@ const Core = {
         return this.premiumStudyLoadPromise;
     },
 
+    schedulePremiumStudyPreload() {
+        const preload = () => {
+            this.loadPremiumStudyModule()
+                .then(() => {
+                    if (
+                        window.PremiumStudyBootstrap &&
+                        typeof window.PremiumStudyBootstrap.preload === "function"
+                    ) {
+                        return window.PremiumStudyBootstrap.preload();
+                    }
+                    return null;
+                })
+                .catch((error) => {
+                    console.warn("Preload do PDF Focado falhou", error);
+                });
+        };
+
+        if ("requestIdleCallback" in window) {
+            window.requestIdleCallback(preload, { timeout: 3500 });
+            return;
+        }
+
+        window.setTimeout(preload, 1200);
+    },
+
+    showPremiumStudyLoading() {
+        const moduleEl = document.getElementById("premium-studyModule");
+
+        if (!moduleEl || moduleEl.dataset.ready === "true") {
+            return;
+        }
+
+        moduleEl.innerHTML = `
+<section style="min-height:72vh;display:grid;place-items:center;padding:32px;color:#eaf4ff;background:radial-gradient(circle at top left, rgba(121,213,255,.2), transparent 34%), #0f1724;">
+  <article style="width:min(520px,92vw);border:1px solid rgba(121,213,255,.22);border-radius:28px;padding:28px;background:rgba(18,29,45,.86);box-shadow:0 28px 80px rgba(0,0,0,.32);">
+    <span style="display:inline-block;margin-bottom:14px;color:#79d5ff;font-size:12px;font-weight:800;letter-spacing:.18em;text-transform:uppercase;">PDF Focado</span>
+    <h2 style="margin:0 0 10px;font-size:clamp(24px,6vw,38px);line-height:1.03;">Abrindo seu workspace premium.</h2>
+    <p style="margin:0;color:rgba(234,244,255,.72);line-height:1.55;">Carregando a mesa de estudo agora. Se a rede estiver lenta, a tela ainda aparece sem parecer travada.</p>
+  </article>
+</section>`;
+    },
+
     /* ================= NAVIGATION ================= */
 
-    navigate(target) {
+    navigate(target, options = {}) {
+
+   const skipHistory = Boolean(options.skipHistory)
+   const replaceHistory = Boolean(options.replaceHistory)
 
    if (target === "alarm") {
 target = "calendar"
@@ -345,6 +887,46 @@ return
 
    if (target === "questions") {
 
+const applyRequestedLauncherView = () => {
+const requestedView = String(
+window.RotaNotaQuestionsLauncherTarget || ""
+).trim()
+
+if (
+!requestedView ||
+!window.QuestionsPage ||
+typeof QuestionsPage.openLauncher !== "function"
+) {
+return false
+}
+
+window.RotaNotaQuestionsLauncherTarget = ""
+QuestionsPage.openLauncher(requestedView)
+return true
+}
+
+const applyPendingQuestionsNotice = () => {
+const notice = String(
+window.RotaNotaQuestionsRuntimeNotice || ""
+).trim()
+
+if (
+!notice ||
+!window.QuestionsPage
+) {
+return false
+}
+
+QuestionsPage.runtimeNotice = notice
+window.RotaNotaQuestionsRuntimeNotice = ""
+
+if (typeof QuestionsPage.render === "function") {
+QuestionsPage.render()
+}
+
+return true
+}
+
 this.stopAll()
 this.clearVisualResidues(target)
 this.hideHero()
@@ -356,6 +938,11 @@ if (moduleEl) moduleEl.classList.add("active")
 this.state.mode = "questions"
 document.body.setAttribute("data-mode", "questions")
 this.setActiveNav("questions")
+if (!skipHistory) {
+this.syncBrowserHistory("questions", {
+replace: replaceHistory
+})
+}
 
 if (!window.questionsLoaded) {
 
@@ -410,7 +997,11 @@ mainScript.onload = () => {
 window.questionsLoaded = true
 
 if (window.QuestionsPage) {
-QuestionsPage.init()
+Promise.resolve(QuestionsPage.init())
+.then(() => {
+applyRequestedLauncherView()
+applyPendingQuestionsNotice()
+})
 }
 
 }
@@ -451,6 +1042,9 @@ QuestionsPage.render()
 } else {
 QuestionsPage.render()
 }
+
+applyRequestedLauncherView()
+applyPendingQuestionsNotice()
 }
 
 }
@@ -461,6 +1055,12 @@ QuestionsPage.render()
    if (target === "premium-study") {
 
 this.activateExternalModule("premium-study")
+this.showPremiumStudyLoading()
+if (!skipHistory) {
+this.syncBrowserHistory("premium-study", {
+replace: replaceHistory
+})
+}
 
 this.loadPremiumStudyModule()
 .then(() => {
@@ -469,6 +1069,16 @@ if (window.PremiumStudyBootstrap) {
 window.PremiumStudyBootstrap.init({
 root: document.getElementById("premium-studyModule")
 })
+
+if (
+window.RotaNotaPremiumHomeAction &&
+window.PremiumStudyApp &&
+typeof PremiumStudyApp.runHomeAction === "function"
+) {
+PremiumStudyApp.runHomeAction(
+window.RotaNotaPremiumHomeAction
+)
+}
 }
 
 })
@@ -476,11 +1086,37 @@ root: document.getElementById("premium-studyModule")
 console.error(error)
 })
 
+return
+}
+
+   if (target === "routine") {
+
+this.activateExternalModule("routine")
+if (!skipHistory) {
+this.syncBrowserHistory("routine", {
+replace: replaceHistory
+})
+}
+
+const routineRenderer =
+window.RoutineTable ||
+(typeof RoutineTable !== "undefined" ? RoutineTable : null)
+
+if (
+routineRenderer &&
+typeof routineRenderer.render === "function"
+) {
+routineRenderer.render()
+}
+
 return
 }
 
         if (this.modules[target]) {
-            this.changeMode(target);
+            this.changeMode(target, {
+                skipHistory,
+                replaceHistory
+            });
             return;
         }
 
@@ -509,15 +1145,27 @@ return
             );
         }
         this.setActiveNav(target);
+        if (!skipHistory) {
+            this.syncBrowserHistory(target, {
+                replace: replaceHistory
+            });
+        }
     },
 
-    changeMode(mode) {
+    changeMode(mode, options = {}) {
 
     const previousMode = this.state.mode;
+    const skipHistory = Boolean(options.skipHistory);
+    const replaceHistory = Boolean(options.replaceHistory);
 
     this.stopAll();
     this.clearVisualResidues(mode);
     this.setMode(mode);
+    if (!skipHistory) {
+        this.syncBrowserHistory(mode, {
+            replace: replaceHistory
+        });
+    }
 
     if (window.AmbientEngine && AmbientEngine.onModeChange) {
         AmbientEngine.onModeChange(previousMode, mode);
@@ -557,7 +1205,7 @@ return
             module.render();
     },
 
-    goHome() {
+    goHome(options = {}) {
         if (
             window.PremiumStudyApp &&
             typeof PremiumStudyApp.clearAnalysisTimers ===
@@ -566,22 +1214,1395 @@ return
             PremiumStudyApp.clearAnalysisTimers();
         }
         document.body.removeAttribute("data-premium-step");
-        this.changeMode("clock");
+        this.changeMode("clock", options);
+        this.refreshHomeStatsWidget();
     },
 
-    /* ================= CONTROL ================= */
-
-    control(action) {
-
-    const module = this.getActiveModule();
+    openHomeStatsLoginGate(intent = "stats") {
+        const gate =
+            document.getElementById(
+                "homeStatsLoginGate"
+            );
 
-    if (!module) return;
+        if (!gate) {
+            return;
+        }
 
-    if (typeof module[action] === "function") {
-        module[action]();
-    }
+        this.pendingGoogleGateIntent =
+            this.getGoogleGateIntent(
+                intent
+            );
+        this.updateGoogleGateContent(
+            this.pendingGoogleGateIntent
+        );
 
-},
+        gate.classList.remove("hidden");
+        gate.setAttribute(
+            "aria-hidden",
+            "false"
+        );
+
+        if (
+            window.RotaNotaAuth &&
+            typeof window.RotaNotaAuth.prepareGoogleGateButton ===
+                "function"
+        ) {
+            window.RotaNotaAuth.prepareGoogleGateButton({
+                source: "home_stats",
+                intent: this.pendingGoogleGateIntent
+            });
+        }
+    },
+
+    closeHomeStatsLoginGate(
+        options = {}
+    ) {
+        const gate =
+            document.getElementById(
+                "homeStatsLoginGate"
+            );
+
+        if (!gate) {
+            return;
+        }
+
+        gate.classList.add("hidden");
+        gate.setAttribute(
+            "aria-hidden",
+            "true"
+        );
+        this.setGoogleGateStatus("");
+
+        if (
+            options.preserveIntent !==
+            true
+        ) {
+            this.pendingGoogleGateIntent =
+                null;
+        }
+    },
+
+    requireGoogleLogin(intent = "stats") {
+        if (this.isGoogleAuthenticated()) {
+            return { allowed: true };
+        }
+
+        this.openHomeStatsLoginGate(intent);
+        return { allowed: false };
+    },
+
+    requestGoogleLogin(source = "home") {
+        const pendingIntent =
+            this.pendingGoogleGateIntent ||
+            this.getGoogleGateIntent(source);
+
+        if (
+            window.RotaNotaAuth &&
+            typeof window.RotaNotaAuth
+                .requestGoogleLogin ===
+                "function"
+        ) {
+            window.RotaNotaAuth.requestGoogleLogin(
+                {
+                    source,
+                    intent: pendingIntent
+                }
+            );
+        } else {
+            window.dispatchEvent(
+                new CustomEvent(
+                    "rotanota:google-login-requested",
+                    {
+                        detail: {
+                            source,
+                            intent: pendingIntent
+                        }
+                    }
+                )
+            );
+            this.setGoogleGateStatus(
+                "O login Google ainda nao esta conectado neste build."
+            );
+            return;
+        }
+    },
+
+    async handleGoogleLoginSuccess(
+        detail = {}
+    ) {
+        const intent =
+            this.pendingGoogleGateIntent ||
+            this.getGoogleGateIntent(
+                detail.intent ||
+                    detail.source ||
+                    "stats"
+            );
+
+        this.pendingGoogleGateIntent =
+            null;
+        this.closeHomeStatsLoginGate(
+            { preserveIntent: true }
+        );
+
+        if (intent.kind === "premium_checkout") {
+            if (
+                this.state.mode !==
+                "premium-study"
+            ) {
+                this.navigate("premium-study");
+            }
+
+            const planId =
+                intent.planId ||
+                "premium_monthly";
+            const premiumState =
+                window.PremiumStudyStore &&
+                typeof window.PremiumStudyStore.getState ===
+                    "function"
+                    ? window.PremiumStudyStore.getState()
+                    : null;
+
+            if (
+                window.PremiumStudyBilling &&
+                typeof window.PremiumStudyBilling.startCheckout ===
+                    "function"
+            ) {
+                const checkout =
+                    await window.PremiumStudyBilling.startCheckout(
+                        planId,
+                        {
+                            feature:
+                                premiumState &&
+                                premiumState.premiumOffer
+                                    ? premiumState.premiumOffer.feature
+                                    : "",
+                            sourceStep:
+                                premiumState &&
+                                premiumState.returnStep
+                                    ? premiumState.returnStep
+                                    : "premium-checkout"
+                        }
+                    );
+
+                if (
+                    !checkout?.ok &&
+                    window.PremiumStudyStore &&
+                    typeof window.PremiumStudyStore.setSessionNote ===
+                        "function"
+                ) {
+                    window.PremiumStudyStore.setSessionNote(
+                        {
+                            step:
+                                premiumState &&
+                                premiumState.step
+                                    ? premiumState.step
+                                    : "premium-checkout",
+                            tone: "premium",
+                            title: "Nao foi possivel abrir o checkout",
+                            message:
+                                checkout?.message ||
+                                "O checkout nao respondeu corretamente agora. Tente de novo em instantes."
+                        }
+                    );
+                }
+            }
+
+            return;
+        }
+
+        if (intent.kind === "account") {
+            return;
+        }
+
+        window.RotaNotaQuestionsLauncherTarget =
+            "progress";
+        this.navigate("questions");
+    },
+
+    updateHomeBandPanels(panels = []) {
+        const label =
+            document.getElementById(
+                "homeBandPanelLabel"
+            );
+        const value =
+            document.getElementById(
+                "homeBandPanelValue"
+            );
+        const body =
+            document.getElementById(
+                "homeBandPanelBody"
+            );
+
+        if (!label || !value || !body) {
+            return;
+        }
+
+        const cleanPanels = Array.isArray(panels)
+            ? panels.filter(Boolean)
+            : [];
+
+        if (this.homeBandPanelTimer) {
+            clearInterval(
+                this.homeBandPanelTimer
+            );
+            this.homeBandPanelTimer = null;
+        }
+
+        const applyPanel = (panel) => {
+            label.textContent = String(
+                panel?.label ||
+                    "Consistencia"
+            );
+            value.textContent = String(
+                panel?.value || "0 dias"
+            );
+            body.innerHTML = String(
+                panel?.html || ""
+            );
+        };
+
+        if (!cleanPanels.length) {
+            applyPanel({
+                label: "Consistencia",
+                value: "0 dias",
+                html: ""
+            });
+            return;
+        }
+
+        this.homeBandPanelIndex = 0;
+        applyPanel(cleanPanels[0]);
+
+        if (cleanPanels.length === 1) {
+            return;
+        }
+
+        this.homeBandPanelTimer =
+            window.setInterval(() => {
+                this.homeBandPanelIndex =
+                    (this.homeBandPanelIndex + 1) %
+                    cleanPanels.length;
+                applyPanel(
+                    cleanPanels[
+                        this.homeBandPanelIndex
+                    ]
+                );
+            }, 11200);
+    },
+
+    parseStoredJson(key, fallbackValue) {
+        if (typeof localStorage === "undefined") {
+            return fallbackValue;
+        }
+
+        try {
+            const raw = localStorage.getItem(key);
+            if (!raw) {
+                return fallbackValue;
+            }
+
+            return JSON.parse(raw);
+        } catch (_error) {
+            return fallbackValue;
+        }
+    },
+
+    formatShortcutCount(value, singular, plural = `${singular}s`) {
+        const amount = Math.max(Number(value) || 0, 0);
+        return `${amount} ${amount === 1 ? singular : plural}`;
+    },
+
+    formatShortcutPercent(value) {
+        const amount = Math.round(Number(value) || 0);
+        return `${amount}%`;
+    },
+
+    clamp(value, min, max) {
+        return Math.min(
+            Math.max(
+                Number(value) || 0,
+                min
+            ),
+            max
+        );
+    },
+
+    formatQuestionAverageTime(value) {
+        const totalMs = Math.max(
+            Number(value) || 0,
+            0
+        );
+
+        if (!totalMs) {
+            return "0s";
+        }
+
+        const totalSeconds = Math.round(
+            totalMs / 1000
+        );
+
+        if (totalSeconds < 60) {
+            return `${totalSeconds}s`;
+        }
+
+        const minutes = Math.floor(
+            totalSeconds / 60
+        );
+        const seconds = totalSeconds % 60;
+
+        if (!seconds) {
+            return `${minutes}min`;
+        }
+
+        return `${minutes}min ${String(
+            seconds
+        ).padStart(2, "0")}s`;
+    },
+
+    getQuestionsLevelProfile(snapshot = {}) {
+        const attempts = Math.max(
+            Number(snapshot.attempts) || 0,
+            0
+        );
+        const sessions = Math.max(
+            Number(snapshot.totalSessions) || 0,
+            0
+        );
+
+        if (
+            attempts >= 220 ||
+            sessions >= 24
+        ) {
+            return {
+                key: "consolidado",
+                label: "consolidado",
+                benchmarkAccuracy: 0.78,
+                benchmarkTimeMs: 70000,
+                benchmarkVolume: 220
+            };
+        }
+
+        if (
+            attempts >= 90 ||
+            sessions >= 10
+        ) {
+            return {
+                key: "em_progresso",
+                label: "em progresso",
+                benchmarkAccuracy: 0.68,
+                benchmarkTimeMs: 85000,
+                benchmarkVolume: 120
+            };
+        }
+
+        return {
+            key: "inicial",
+            label: "inicial",
+            benchmarkAccuracy: 0.58,
+            benchmarkTimeMs: 105000,
+            benchmarkVolume: 45
+        };
+    },
+
+    estimateQuestionsStanding(snapshot = {}) {
+        const attempts = Math.max(
+            Number(snapshot.attempts) || 0,
+            0
+        );
+
+        if (!attempts) {
+            return {
+                levelLabel: "inicial",
+                percentile: null,
+                topPercent: null,
+                note: "A posicao aparece quando voce cria uma base minima de questoes."
+            };
+        }
+
+        const accuracy = this.clamp(
+            (Number(snapshot.accuracy) || 0) /
+                100,
+            0,
+            1
+        );
+        const avgTimeMs = Math.max(
+            Number(snapshot.avgTimeMs) || 0,
+            0
+        );
+        const level =
+            this.getQuestionsLevelProfile(
+                snapshot
+            );
+        const accuracyScore =
+            this.clamp(
+                0.5 +
+                    (accuracy -
+                        level.benchmarkAccuracy) /
+                        0.28,
+                0,
+                1
+            );
+        const timeScore =
+            avgTimeMs > 0
+                ? this.clamp(
+                    (
+                        level.benchmarkTimeMs *
+                            1.35 -
+                        avgTimeMs
+                    ) /
+                        (level.benchmarkTimeMs *
+                            0.9),
+                    0,
+                    1
+                )
+                : 0.5;
+        const volumeScore =
+            this.clamp(
+                attempts /
+                    level.benchmarkVolume,
+                0,
+                1
+            );
+        const score =
+            accuracyScore * 0.6 +
+            timeScore * 0.22 +
+            volumeScore * 0.18;
+        const percentile = Math.round(
+            18 + score * 76
+        );
+        const topPercent = Math.max(
+            6,
+            100 - percentile
+        );
+
+        return {
+            levelLabel: level.label,
+            percentile,
+            topPercent,
+            note: `Estimativa local entre alunos do nivel ${level.label}, combinando acuracia, ritmo e volume recente.`
+        };
+    },
+
+    toCalendarDayKey(value) {
+        const date = new Date(value);
+
+        if (Number.isNaN(date.getTime())) {
+            return "";
+        }
+
+        const year = date.getFullYear();
+        const month = String(
+            date.getMonth() + 1
+        ).padStart(2, "0");
+        const day = String(
+            date.getDate()
+        ).padStart(2, "0");
+
+        return `${year}-${month}-${day}`;
+    },
+
+    buildConsistencyPreview(
+        recentSessions = []
+    ) {
+        const totalDays = 28;
+        const activeDays = new Set(
+            recentSessions
+                .map((session) =>
+                    this.toCalendarDayKey(
+                        session?.createdAt
+                    )
+                )
+                .filter(Boolean)
+        );
+        const cells = [];
+        const today = new Date();
+        let streak = 0;
+
+        for (
+            let offset = totalDays - 1;
+            offset >= 0;
+            offset -= 1
+        ) {
+            const day = new Date(
+                today.getFullYear(),
+                today.getMonth(),
+                today.getDate() - offset
+            );
+            const dayKey =
+                this.toCalendarDayKey(day);
+            cells.push({
+                key: dayKey,
+                active: activeDays.has(dayKey)
+            });
+        }
+
+        for (
+            let offset = 0;
+            offset < totalDays;
+            offset += 1
+        ) {
+            const day = new Date(
+                today.getFullYear(),
+                today.getMonth(),
+                today.getDate() - offset
+            );
+            const dayKey =
+                this.toCalendarDayKey(day);
+
+            if (!activeDays.has(dayKey)) {
+                break;
+            }
+
+            streak += 1;
+        }
+
+        return {
+            cells,
+            streak
+        };
+    },
+
+    getQuestionsHomeSnapshot() {
+        try {
+            if (
+                window.QuestionsStore &&
+                typeof window.QuestionsStore.load === "function"
+            ) {
+                window.QuestionsStore.load();
+                const dashboard =
+                    typeof window.QuestionsStore.getDashboard === "function"
+                        ? window.QuestionsStore.getDashboard()
+                        : null;
+                const pausedRuns =
+                    typeof window.QuestionsStore.getRuns === "function"
+                        ? window.QuestionsStore.getRuns({
+                              status: "in_progress"
+                          })
+                        : [];
+                const savedBlocks =
+                    typeof window.QuestionsStore.getSavedBlocks === "function"
+                        ? window.QuestionsStore.getSavedBlocks()
+                        : [];
+                const weakTopic =
+                    typeof window.QuestionsStore.getWeakTopics === "function"
+                        ? window.QuestionsStore.getWeakTopics({
+                              minAttempts: 1,
+                              minErrors: 1
+                          })
+                        : null;
+                const recentSessions =
+                    typeof window.QuestionsStore.getRecentSessions === "function"
+                        ? window.QuestionsStore.getRecentSessions()
+                        : [];
+
+                return {
+                    pausedRunsCount: pausedRuns.length,
+                    pausedRunTitle:
+                        pausedRuns[0]?.title || "",
+                    savedBlocksCount:
+                        savedBlocks.length,
+                    weakTopicLabel:
+                        weakTopic?.[0]?.topicLabel ||
+                        "",
+                    weakTopics: Array.isArray(
+                        weakTopic
+                    )
+                        ? weakTopic.slice(0, 3)
+                        : [],
+                    attempts:
+                        Number(dashboard?.attempts) || 0,
+                    hits:
+                        Number(dashboard?.hits) || 0,
+                    errors:
+                        Number(dashboard?.errors) || 0,
+                    avgTimeMs:
+                        Number(
+                            dashboard?.avgTimeMs
+                        ) || 0,
+                    totalSessions:
+                        Number(
+                            dashboard?.totalSessions
+                        ) || 0,
+                    accuracy:
+                        Number.isFinite(
+                            dashboard?.accuracy
+                        )
+                            ? Math.round(
+                                  dashboard.accuracy * 100
+                              )
+                            : null,
+                    mostTrainedTopic:
+                        dashboard?.mostTrainedTopic
+                            ?.topicLabel || "",
+                    recentSessions: Array.isArray(
+                        recentSessions
+                    )
+                        ? recentSessions
+                              .slice(0, 8)
+                              .map((session) => ({
+                                  createdAt:
+                                      session?.createdAt ||
+                                      0,
+                                  accuracy:
+                                      Number(
+                                          session?.accuracy
+                                      ) || 0,
+                                  mode:
+                                      session?.mode ||
+                                      "Treino"
+                              }))
+                        : []
+                };
+            }
+        } catch (error) {
+            console.warn(
+                "Leitura rapida de Questions falhou",
+                error
+            );
+        }
+
+        const profile =
+            this.parseStoredJson(
+                "questions_profile_v4",
+                {}
+            ) || {};
+        const runs = Array.isArray(
+            this.parseStoredJson(
+                "questions_runs_v2",
+                []
+            )
+        )
+            ? this.parseStoredJson(
+                  "questions_runs_v2",
+                  []
+              )
+            : [];
+        const topics = Object.values(
+            profile.topics || {}
+        );
+        const sessions = Array.isArray(
+            profile.sessions
+        )
+            ? profile.sessions
+            : [];
+        const savedBlocks = Array.isArray(
+            profile.savedBlocks
+        )
+            ? profile.savedBlocks
+            : [];
+        const pausedRuns = runs
+            .filter(
+                (run) =>
+                    run &&
+                    run.status ===
+                        "in_progress"
+            )
+            .sort(
+                (left, right) =>
+                    (right.updatedAt || 0) -
+                    (left.updatedAt || 0)
+            );
+        const totals = topics.reduce(
+            (acc, entry) => {
+                acc.attempts +=
+                    Number(entry?.attempts) || 0;
+                acc.hits +=
+                    Number(entry?.hits) || 0;
+                return acc;
+            },
+            { attempts: 0, hits: 0 }
+        );
+        const weakTopic = [...topics]
+            .filter((entry) => {
+                const attempts =
+                    Number(entry?.attempts) || 0;
+                const errors =
+                    Number(entry?.errors) || 0;
+                return attempts > 0 && errors > 0;
+            })
+            .map((entry) => ({
+                ...entry,
+                accuracy:
+                    Number(entry?.attempts) > 0
+                        ? Number(entry?.hits || 0) /
+                          Number(entry.attempts)
+                        : 0
+            }))
+            .sort(
+                (left, right) =>
+                    (Number(right.errors) || 0) -
+                        (Number(left.errors) ||
+                            0) ||
+                    left.accuracy -
+                        right.accuracy ||
+                    (Number(right.attempts) ||
+                        0) -
+                        (Number(left.attempts) ||
+                            0)
+            )[0];
+        const weakTopics = [...topics]
+            .filter((entry) => {
+                const attempts =
+                    Number(entry?.attempts) || 0;
+                const errors =
+                    Number(entry?.errors) || 0;
+                return attempts > 0 && errors > 0;
+            })
+            .map((entry) => ({
+                ...entry,
+                accuracy:
+                    Number(entry?.attempts) > 0
+                        ? Number(entry?.hits || 0) /
+                          Number(entry.attempts)
+                        : 0
+            }))
+            .sort(
+                (left, right) =>
+                    (Number(right.errors) || 0) -
+                        (Number(left.errors) ||
+                            0) ||
+                    left.accuracy -
+                        right.accuracy
+            )
+            .slice(0, 3);
+        const mostTrainedTopic = [...topics].sort(
+            (left, right) =>
+                (Number(right?.attempts) || 0) -
+                (Number(left?.attempts) || 0)
+        )[0];
+
+        return {
+            pausedRunsCount: pausedRuns.length,
+            pausedRunTitle:
+                pausedRuns[0]?.title || "",
+            savedBlocksCount: savedBlocks.length,
+            weakTopicLabel:
+                weakTopic?.topicLabel || "",
+            weakTopics,
+            attempts: totals.attempts,
+            hits: totals.hits,
+            errors: Math.max(
+                totals.attempts - totals.hits,
+                0
+            ),
+            avgTimeMs:
+                totals.attempts > 0
+                    ? topics.reduce(
+                        (acc, entry) =>
+                            acc +
+                            (
+                                Number(
+                                    entry?.avgTime
+                                ) || 0
+                            ) *
+                                (
+                                    Number(
+                                        entry?.attempts
+                                    ) || 0
+                                ),
+                        0
+                    ) / totals.attempts
+                    : 0,
+            totalSessions: sessions.length,
+            accuracy:
+                totals.attempts > 0
+                    ? Math.round(
+                          (totals.hits /
+                              totals.attempts) *
+                              100
+                      )
+                    : null,
+            mostTrainedTopic:
+                mostTrainedTopic?.topicLabel || "",
+            recentSessions: sessions
+                .slice()
+                .sort(
+                    (left, right) =>
+                        (right.createdAt || 0) -
+                        (left.createdAt || 0)
+                )
+                .slice(0, 8)
+                .map((session) => ({
+                    createdAt:
+                        session?.createdAt || 0,
+                    accuracy:
+                        Number(
+                            session?.accuracy
+                        ) || 0,
+                    mode:
+                        session?.mode ||
+                        "Treino"
+                }))
+        };
+    },
+
+    renderHomeStatsConsistency(
+        cells = []
+    ) {
+        return cells
+            .map(
+                (cell) => `
+                <span class="home-stats-consistency-cell ${cell.active ? "is-active" : ""}"></span>
+            `
+            )
+            .join("");
+    },
+
+    renderHomeStatsTrend(
+        recentSessions = []
+    ) {
+        const sessions = Array.isArray(
+            recentSessions
+        )
+            ? recentSessions.slice(0, 5)
+            : [];
+
+        if (!sessions.length) {
+            return `
+                <div class="home-stats-trend-empty">As barras aparecem conforme voce pratica.</div>
+            `;
+        }
+
+        return `
+            <div class="home-stats-trend-bars">
+                ${sessions
+                    .slice()
+                    .reverse()
+                    .map((session) => {
+                        const accuracy = Math.max(
+                            10,
+                            Math.min(
+                                100,
+                                Math.round(
+                                    Number(
+                                        session?.accuracy
+                                    ) || 0
+                                )
+                            )
+                        );
+                        return `
+                            <span class="home-stats-trend-bar-wrap">
+                                <span class="home-stats-trend-bar" style="height:${accuracy}%"></span>
+                            </span>
+                        `;
+                    })
+                    .join("")}
+            </div>
+        `;
+    },
+
+    renderHomeStatsWeakTopics(
+        weakTopics = [],
+        fallbackLabel = ""
+    ) {
+        const topics = Array.isArray(
+            weakTopics
+        )
+            ? weakTopics.slice(0, 3)
+            : [];
+
+        if (!topics.length) {
+            return `
+                <span class="home-stats-tag is-muted">${fallbackLabel || "Seu mapa de reforco aparece depois das primeiras respostas."}</span>
+            `;
+        }
+
+        return topics
+            .map(
+                (topic) => `
+                <span class="home-stats-tag">${String(topic?.topicLabel || "Reforco").trim()}</span>
+            `
+            )
+            .join("");
+    },
+
+    renderHomeBandConsistencyPanel(
+        cells = []
+    ) {
+        return `
+            <div class="rotanota-home-band-consistency">
+                ${this.renderHomeStatsConsistency(
+                    cells
+                )}
+            </div>
+        `;
+    },
+
+    renderHomeBandTrendPanel(
+        recentSessions = []
+    ) {
+        const sessionItems = Array.isArray(
+            recentSessions
+        )
+            ? recentSessions.slice(0, 6)
+            : [];
+        const sessions = Array.from(
+            { length: 6 },
+            (_item, index) =>
+                sessionItems[
+                    sessionItems.length - 6 + index
+                ] || {
+                    accuracy: 0
+                }
+        );
+
+        return `
+            <div class="rotanota-home-band-trend">
+                ${sessions
+                    .map((session) => {
+                        const rawAccuracy =
+                            Math.round(
+                                Number(
+                                    session?.accuracy
+                                ) || 0
+                            );
+                        const accuracy = Math.max(
+                            8,
+                            Math.min(
+                                100,
+                                rawAccuracy
+                            )
+                        );
+                        return `
+                            <span class="rotanota-home-band-trend-bar-wrap ${rawAccuracy <= 0 ? "is-zero" : ""}">
+                                <span class="rotanota-home-band-trend-bar" style="height:${accuracy}%"></span>
+                            </span>
+                        `;
+                    })
+                    .join("")}
+            </div>
+        `;
+    },
+
+    renderHomeBandLoginPanel() {
+        return `
+            <div class="rotanota-home-band-login">
+                <div class="rotanota-home-band-login-badge">
+                    <span class="rotanota-home-band-login-g" aria-hidden="true">
+                        <span class="rotanota-home-band-login-g-blue"></span>
+                        <span class="rotanota-home-band-login-g-red"></span>
+                        <span class="rotanota-home-band-login-g-yellow"></span>
+                        <span class="rotanota-home-band-login-g-green"></span>
+                    </span>
+                    <div class="rotanota-home-band-login-copy">
+                        <strong>Entre com Google</strong>
+                        <span>salve progresso e sincronize em qualquer dispositivo</span>
+                    </div>
+                </div>
+                <div class="rotanota-home-band-login-points">
+                    <span>Historico salvo</span>
+                    <span>Painel completo</span>
+                    <span>Premium na conta</span>
+                </div>
+            </div>
+        `;
+    },
+
+    renderHomeBandRhythmPanel(
+        snapshot = {}
+    ) {
+        const sessions =
+            Math.max(
+                Number(snapshot.totalSessions) || 0,
+                0
+            );
+        const paused =
+            Math.max(
+                Number(snapshot.pausedRunsCount) || 0,
+                0
+            );
+        const saved =
+            Math.max(
+                Number(snapshot.savedBlocksCount) || 0,
+                0
+            );
+        const maxValue = Math.max(
+            sessions,
+            paused,
+            saved,
+            1
+        );
+        const rows = [
+            {
+                label: "Sessoes",
+                value: sessions
+            },
+            {
+                label: "Retomadas",
+                value: paused
+            },
+            {
+                label: "Guardados",
+                value: saved
+            }
+        ];
+
+        return `
+            <div class="rotanota-home-band-rhythm">
+                ${rows
+                    .map(
+                        (row) => `
+                        <div class="rotanota-home-band-rhythm-row">
+                            <div class="rotanota-home-band-rhythm-meta">
+                                <span>${row.label}</span>
+                                <strong>${row.value}</strong>
+                            </div>
+                            <div class="rotanota-home-band-rhythm-track">
+                                <span class="rotanota-home-band-rhythm-fill" style="width:${Math.max(8, Math.round((row.value / maxValue) * 100))}%"></span>
+                            </div>
+                        </div>
+                    `
+                    )
+                    .join("")}
+            </div>
+        `;
+    },
+
+    renderHomeBandComparisonPanel(
+        snapshot = {}
+    ) {
+        const standing =
+            this.estimateQuestionsStanding(
+                snapshot
+            );
+        const rows = [
+            {
+                label: "Acertos",
+                value: Math.max(
+                    Number(snapshot.hits) || 0,
+                    0
+                )
+            },
+            {
+                label: "Erros",
+                value: Math.max(
+                    Number(snapshot.errors) || 0,
+                    0
+                )
+            },
+            {
+                label: "Tempo/questao",
+                value:
+                    this.formatQuestionAverageTime(
+                        snapshot.avgTimeMs
+                    )
+            }
+        ];
+
+        return `
+            <div class="rotanota-home-band-comparison">
+                <div class="rotanota-home-band-comparison-grid">
+                    ${rows
+                        .map(
+                            (row) => `
+                                <div class="rotanota-home-band-comparison-card">
+                                    <span>${row.label}</span>
+                                    <strong>${row.value}</strong>
+                                </div>
+                            `
+                        )
+                        .join("")}
+                </div>
+                <div class="rotanota-home-band-comparison-note">
+                    ${
+                        standing.percentile
+                            ? `Top ${standing.topPercent}% estimado`
+                            : "Posicao estimada em aquecimento"
+                    }
+                </div>
+            </div>
+        `;
+    },
+
+    syncLegacyHomeStatsWidget(
+        snapshot = {}
+    ) {
+        const setText = (
+            id,
+            value
+        ) => {
+            const node =
+                document.getElementById(id);
+
+            if (node) {
+                node.textContent =
+                    String(value);
+            }
+        };
+        const metricLabelNodes =
+            Array.from(
+                document.querySelectorAll(
+                    ".home-stats-metric > span"
+                )
+            );
+        const consistency =
+            this.buildConsistencyPreview(
+                snapshot.recentSessions
+            );
+        const standing =
+            this.estimateQuestionsStanding(
+                snapshot
+            );
+        const weakTopicsHtml =
+            this.renderHomeStatsWeakTopics(
+                snapshot.weakTopics,
+                snapshot.weakTopicLabel
+            );
+        const trendHtml =
+            this.renderHomeStatsTrend(
+                snapshot.recentSessions
+            );
+        const consistencyHtml =
+            this.renderHomeStatsConsistency(
+                consistency.cells
+            );
+        const weakTopicsNode =
+            document.getElementById(
+                "homeStatsWeakTopics"
+            );
+        const trendNode =
+            document.getElementById(
+                "homeStatsTrend"
+            );
+        const consistencyNode =
+            document.getElementById(
+                "homeStatsConsistencyLegacy"
+            );
+
+        if (metricLabelNodes[0]) {
+            metricLabelNodes[0].textContent =
+                "Acuracia";
+        }
+        if (metricLabelNodes[1]) {
+            metricLabelNodes[1].textContent =
+                "Acertos";
+        }
+        if (metricLabelNodes[2]) {
+            metricLabelNodes[2].textContent =
+                "Erros";
+        }
+        if (metricLabelNodes[3]) {
+            metricLabelNodes[3].textContent =
+                "Tempo/questao";
+        }
+        setText(
+            "homeStatsHeadline",
+            standing.percentile
+                ? `Top ${standing.topPercent}% estimado no nivel ${standing.levelLabel}.`
+                : "Seu progresso comeca a ganhar forma."
+        );
+        setText(
+            "homeStatsSubline",
+            standing.note
+        );
+        setText(
+            "homeStatsAccuracy",
+            Number.isFinite(snapshot.accuracy)
+                ? this.formatShortcutPercent(
+                    snapshot.accuracy
+                )
+                : "0%"
+        );
+        setText(
+            "homeStatsAccuracyNote",
+            snapshot.attempts
+                ? `${Math.max(Number(snapshot.attempts) || 0, 0)} questoes na base`
+                : "Sem base ainda"
+        );
+        setText(
+            "homeStatsSessions",
+            Math.max(
+                Number(snapshot.hits) || 0,
+                0
+            )
+        );
+        setText(
+            "homeStatsSessionsNote",
+            snapshot.totalSessions
+                ? `${Math.max(Number(snapshot.totalSessions) || 0, 0)} sessoes registradas`
+                : "Nenhuma registrada"
+        );
+        setText(
+            "homeStatsPausedRuns",
+            Math.max(
+                Number(snapshot.errors) || 0,
+                0
+            )
+        );
+        setText(
+            "homeStatsPausedRunsNote",
+            snapshot.errors
+                ? "Erros para revisar"
+                : "Sem erros na base"
+        );
+        setText(
+            "homeStatsSavedBlocks",
+            this.formatQuestionAverageTime(
+                snapshot.avgTimeMs
+            )
+        );
+        setText(
+            "homeStatsSavedBlocksNote",
+            standing.percentile
+                ? `Top ${standing.topPercent}% estimado`
+                : "Ritmo aparece depois das primeiras respostas"
+        );
+        setText(
+            "homeStatsStreakLegacy",
+            `${consistency.streak} dia${
+                consistency.streak === 1
+                    ? ""
+                    : "s"
+            }`
+        );
+
+        if (weakTopicsNode) {
+            weakTopicsNode.innerHTML =
+                weakTopicsHtml;
+        }
+
+        if (trendNode) {
+            trendNode.innerHTML = trendHtml;
+        }
+
+        if (consistencyNode) {
+            consistencyNode.innerHTML =
+                consistencyHtml;
+        }
+    },
+
+    async refreshHomeStatsWidget() {
+        const refreshToken =
+            ++this.homeStatsRefreshToken;
+        const snapshot =
+            await Promise.resolve(
+                this.getQuestionsHomeSnapshot()
+            ).catch((error) => {
+                console.warn(
+                    "Atualizacao do widget da home falhou",
+                    error
+                );
+                return null;
+            });
+
+        if (
+            refreshToken !==
+                this.homeStatsRefreshToken ||
+            !snapshot
+        ) {
+            return;
+        }
+
+        const consistency =
+            this.buildConsistencyPreview(
+                snapshot.recentSessions
+            );
+        const accuracyText =
+            Number.isFinite(snapshot.accuracy)
+                ? this.formatShortcutPercent(
+                      snapshot.accuracy
+                  )
+                : "0%";
+        const standing =
+            this.estimateQuestionsStanding(
+                snapshot
+            );
+
+        this.updateHomeBandPanels([
+            {
+                label: "Consistencia",
+                value: `${consistency.streak} dia${consistency.streak === 1 ? "" : "s"}`,
+                html: this.renderHomeBandConsistencyPanel(
+                    consistency.cells
+                )
+            },
+            {
+                label: "Evolucao",
+                value: accuracyText,
+                html: this.renderHomeBandTrendPanel(
+                    snapshot.recentSessions
+                )
+            },
+            {
+                label: "Ritmo",
+                value: `${Math.max(Number(snapshot.pausedRunsCount) || 0, 0) + Math.max(Number(snapshot.savedBlocksCount) || 0, 0)} sinais`,
+                html: this.renderHomeBandRhythmPanel(
+                    snapshot
+                )
+            },
+            {
+                label: "Mesmo nivel",
+                value: standing.percentile
+                    ? `Top ${standing.topPercent}%`
+                    : "Estimando",
+                html: this.renderHomeBandComparisonPanel(
+                    snapshot
+                )
+            },
+            {
+                label: "Conta",
+                value: "Google",
+                html: this.renderHomeBandLoginPanel()
+            }
+        ]);
+
+        this.syncLegacyHomeStatsWidget(
+            snapshot
+        );
+    },
+
+    handleHomeAction(action = "") {
+        const safeAction =
+            String(action || "").trim();
+
+        if (!safeAction) {
+            return;
+        }
+
+        if (
+            safeAction ===
+            "questions-progress"
+        ) {
+            window.RotaNotaQuestionsLauncherTarget =
+                "progress";
+            this.navigate("questions");
+            return;
+        }
+
+        if (
+            safeAction ===
+            "questions-quick"
+        ) {
+            window.RotaNotaQuestionsLauncherTarget =
+                "quick";
+            this.navigate("questions");
+            return;
+        }
+
+        if (
+            safeAction === "pdf-upload" ||
+            safeAction === "pdf-resume" ||
+            safeAction === "pdf-library"
+        ) {
+            window.RotaNotaPremiumHomeAction =
+                safeAction;
+            this.navigate("premium-study");
+        }
+    },
+
+    /* ================= CONTROL ================= */
+
+    control(action) {
+
+    const module = this.getActiveModule();
+
+    if (!module) return;
+
+    if (typeof module[action] === "function") {
+        module[action]();
+    }
+
+},
 
     stopAll() {
 
@@ -665,6 +2686,24 @@ return
 
         this.prepareAmbientForFocus();
         document.body.classList.add("focus-mode");
+        document.body.classList.add("clock-immersion-mode");
+        this.refreshShortcutHint(2400);
+
+        if (!document.fullscreenElement) {
+            document.documentElement
+                .requestFullscreen()
+                .catch(() => {});
+        }
+    },
+
+    enterImmersionMode() {
+        if (this.state.mode !== "clock") {
+            this.goHome();
+        }
+
+        this.prepareAmbientForFocus();
+        document.body.classList.add("focus-mode");
+        document.body.classList.add("clock-immersion-mode");
         this.refreshShortcutHint(2400);
 
         if (!document.fullscreenElement) {
@@ -677,12 +2716,14 @@ return
     exitViewportModes() {
         document.body.classList.remove("focus-mode");
         document.body.classList.remove("immersive-mode");
+        document.body.classList.remove("clock-immersion-mode");
         this.restoreAmbientAfterFocus();
         this.refreshShortcutHint(5000);
     },
 
     exitFocusMode() {
         document.body.classList.remove("focus-mode");
+        document.body.classList.remove("clock-immersion-mode");
         this.restoreAmbientAfterFocus();
         this.refreshShortcutHint(5000);
 
@@ -695,6 +2736,7 @@ return
         if (!document.fullscreenElement) {
             document.body.classList.remove("immersive-mode");
             document.body.classList.remove("focus-mode");
+            document.body.classList.remove("clock-immersion-mode");
             this.restoreAmbientAfterFocus();
             this.refreshShortcutHint(5000);
             return;
@@ -829,15 +2871,6 @@ return
                 e.target.tagName === "TEXTAREA"
             ) return;
 
-            if (
-                e.key === "Escape" &&
-                this.stylePanel &&
-                !this.stylePanel.classList.contains("hidden")
-            ) {
-                this.closeStylePanel();
-                return;
-            }
-
             if (e.key === "Escape") {
 
                 this.exitViewportModes();
@@ -896,6 +2929,7 @@ return
     toggleFullscreenCurrentView() {
         if (document.body.classList.contains("focus-mode")) {
             document.body.classList.remove("focus-mode");
+            document.body.classList.remove("clock-immersion-mode");
             this.restoreAmbientAfterFocus();
             this.refreshShortcutHint(2400);
             return;
@@ -1011,104 +3045,16 @@ return
     },
 
     initStyleTheme() {
-        const savedStyle =
-            localStorage.getItem(this.styleStorageKey) ||
-            "clarity";
-
-        this.applyStyleTheme(savedStyle, false);
+        localStorage.removeItem("rotanota_style_theme");
+        this.applyStyleTheme("default", false);
     },
 
-    applyStyleTheme(style = "clarity", persist = true) {
-        const nextStyle =
-            this.supportedStyles.includes(style)
-                ? style
-                : "clarity";
-
-        document.body.dataset.style = nextStyle;
+    applyStyleTheme(style = "default", persist = true) {
+        document.body.dataset.style = "default";
 
         if (persist) {
-            localStorage.setItem(
-                this.styleStorageKey,
-                nextStyle
-            );
+            localStorage.removeItem("rotanota_style_theme");
         }
-
-        this.syncStyleThemeUI(nextStyle);
-    },
-
-    syncStyleThemeUI(activeStyle) {
-        if (!this.styleToggle) return;
-
-        const labels = {
-            clarity: "Clareza",
-            immersion: "Imersao",
-            pulse: "Pulso"
-        };
-
-        const nextStyle =
-            this.supportedStyles[
-                (this.supportedStyles.indexOf(activeStyle) + 1) %
-                this.supportedStyles.length
-            ] || "clarity";
-
-        const nextLabel =
-            labels[nextStyle] || "Clareza";
-
-        this.styleToggle.innerHTML =
-            this.getStyleToggleIcon(activeStyle);
-
-        this.styleToggle.setAttribute(
-            "aria-label",
-            `Estilo visual: ${labels[activeStyle] || "Clareza"}. Clique para mudar para ${nextLabel}`
-        );
-
-        this.styleToggle.setAttribute(
-            "title",
-            `Estilo visual: ${labels[activeStyle] || "Clareza"}`
-        );
-
-        this.styleToggle.setAttribute(
-            "aria-expanded",
-            "false"
-        );
-    },
-
-    cycleStyleTheme() {
-        const currentStyle =
-            document.body.dataset.style || "clarity";
-
-        const currentIndex =
-            this.supportedStyles.indexOf(currentStyle);
-
-        const nextStyle =
-            this.supportedStyles[
-                currentIndex >= 0
-                    ? (currentIndex + 1) % this.supportedStyles.length
-                    : 0
-            ];
-
-        this.applyStyleTheme(nextStyle);
-    },
-
-    getStyleToggleIcon(style = "clarity") {
-        const icons = {
-            clarity: `
-<svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="1.8" aria-hidden="true">
-    <circle cx="12" cy="12" r="5"/>
-    <path d="M12 2v3M12 19v3M2 12h3M19 12h3M5 5l2.2 2.2M16.8 16.8L19 19M19 5l-2.2 2.2M5 19l2.2-2.2" stroke-linecap="round"/>
-</svg>`,
-            immersion: `
-<svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="1.8" aria-hidden="true">
-    <path d="M12 3l7 4v10l-7 4-7-4V7l7-4z" stroke-linejoin="round"/>
-    <path d="M12 7v10M7.7 9.4 16.3 14.6" stroke-linecap="round"/>
-</svg>`,
-            pulse: `
-<svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="1.8" aria-hidden="true">
-    <path d="M3 12h4l2-4 3 8 2-4h7" stroke-linecap="round" stroke-linejoin="round"/>
-</svg>`
-        };
-
-        return icons[style] || icons.clarity;
     }
 
 };
diff --git a/js/calendar.js b/js/calendar.js
index 9405554..b04c4db 100644
--- a/js/calendar.js
+++ b/js/calendar.js
@@ -289,7 +289,7 @@ const Calendar = {
 <div class="calendar-day-preview-title">${eventData.title}</div>
 `
                 : `
-<div class="calendar-day-empty">Clique para lembrar</div>
+<div class="calendar-day-empty">Lembrar</div>
 `;
 
             cell.innerHTML = `
diff --git a/js/clock.js b/js/clock.js
index c13497e..20ecc91 100644
--- a/js/clock.js
+++ b/js/clock.js
@@ -42,14 +42,42 @@ const Clock = {
             year: "numeric"
         }).format(now);
 
-        document.getElementById("timeDisplay").textContent = time;
-        document.getElementById("dateDisplay").textContent =
-            this.capitalize(date);
+        const formattedDate = this.capitalize(date);
+        const timeDisplay =
+            document.getElementById("timeDisplay");
+        const dateDisplay =
+            document.getElementById("dateDisplay");
+        const compactTime =
+            document.getElementById("homeCompactTime");
+        const compactDate =
+            document.getElementById("homeCompactDate");
+
+        if (timeDisplay) {
+            timeDisplay.textContent = time;
+        }
+
+        if (dateDisplay) {
+            dateDisplay.textContent = formattedDate;
+        }
+
+        if (compactTime) {
+            compactTime.textContent = time.slice(0, 5);
+        }
+
+        if (compactDate) {
+            compactDate.textContent = formattedDate;
+        }
     },
 
     buildTimeZoneSelector() {
 
     const select = document.getElementById("timezoneSelector");
+    this.currentTimeZone = "America/Sao_Paulo";
+
+    if (!select) {
+        return;
+    }
+
     select.innerHTML = "";
 
     const zones = [
@@ -79,8 +107,6 @@ const Clock = {
 
     // padrão Brasil oficial
     select.value = "America/Sao_Paulo";
-    this.currentTimeZone = "America/Sao_Paulo";
-
     select.addEventListener("change", (e) => {
         this.currentTimeZone = e.target.value;
         this.update();
@@ -262,4 +288,4 @@ const MiniAnalogClock = {
 
 document.addEventListener("DOMContentLoaded", () => {
     MiniAnalogClock.init();
-});
\ No newline at end of file
+});
diff --git a/js/pomodoro.js b/js/pomodoro.js
index 7db3cbe..7ea8c9f 100644
--- a/js/pomodoro.js
+++ b/js/pomodoro.js
@@ -191,12 +191,15 @@ const Pomodoro = {
                 </h2>
             </div>
             <div class="pomodoro-presets-grid"></div>
+            <div class="pomodoro-presets-custom"></div>
         `;
 
         const grid =
             container.querySelector(".pomodoro-presets-grid");
+        const customGrid =
+            container.querySelector(".pomodoro-presets-custom");
 
-        if (!grid) return;
+        if (!grid || !customGrid) return;
 
         Object.keys(this.presets).forEach((key) => {
             const preset = this.presets[key];
@@ -230,7 +233,11 @@ const Pomodoro = {
                 this.renderPresets();
             };
 
-            grid.appendChild(chip);
+            if (key === "custom") {
+                customGrid.appendChild(chip);
+            } else {
+                grid.appendChild(chip);
+            }
         });
     },
 
@@ -875,30 +882,24 @@ const Pomodoro = {
                 document.getElementById(id);
 
             if (button) {
-                button.disabled = this.syncEnabled;
+                button.disabled = false;
             }
         });
 
         if (controls) {
-            controls.classList.toggle(
-                "is-locked",
-                this.syncEnabled
-            );
+            controls.classList.remove("is-locked");
         }
     },
 
     loadSyncPreference() {
-        this.syncEnabled =
-            localStorage.getItem(this.syncStorageKey) === "1";
-        this.todayFlowExpanded =
-            this.syncEnabled;
+        this.syncEnabled = false;
+        this.todayFlowExpanded = false;
+        localStorage.removeItem(this.syncStorageKey);
+        document.body.classList.remove("pomodoro-table-sync-on");
     },
 
     saveSyncPreference() {
-        localStorage.setItem(
-            this.syncStorageKey,
-            this.syncEnabled ? "1" : "0"
-        );
+        localStorage.removeItem(this.syncStorageKey);
     },
 
     readTodayProgress(date = new Date()) {
@@ -1460,7 +1461,6 @@ const Pomodoro = {
                         <div class="today-flow-status">${status.title}</div>
                         <div class="today-flow-detail">${status.detail}</div>
                         <div class="today-flow-actions">
-                            <button id="todayFlowSyncBtn" class="today-flow-btn is-primary" type="button"${currentBlock ? "" : " disabled"}>${this.syncEnabled ? "Sincronizado" : "Sincronizar com tabela"}</button>
                             <button id="todayFlowDoneBtn" class="today-flow-btn" type="button"${currentBlock ? "" : " disabled"}>Concluir bloco</button>
                             <button id="todayFlowOpenQtsBtn" class="today-flow-btn" type="button">Abrir quadro</button>
                         </div>
@@ -1495,22 +1495,11 @@ const Pomodoro = {
     },
 
     bindTodayFlowActions() {
-        const syncBtn =
-            document.getElementById("todayFlowSyncBtn");
         const doneBtn =
             document.getElementById("todayFlowDoneBtn");
         const openQtsBtn =
             document.getElementById("todayFlowOpenQtsBtn");
 
-        if (syncBtn) {
-            syncBtn.onclick = () => {
-                this.syncTodayFlowToPlan();
-                this.updateDisplay();
-                this.renderPresets();
-                this.renderTodayFlow();
-            };
-        }
-
         if (doneBtn) {
             doneBtn.onclick = () => {
                 this.completeTodayFlowBlock();
@@ -1527,19 +1516,10 @@ const Pomodoro = {
     },
 
     toggleTodaySync() {
-        if (!this.syncEnabled) {
-            this.todayFlowExpanded = true;
-        }
-
-        this.syncEnabled = !this.syncEnabled;
+        this.syncEnabled = false;
+        this.todayFlowExpanded = false;
         this.saveSyncPreference();
 
-        if (this.syncEnabled) {
-            this.syncTodayFlowToPlan({ autostart: true });
-            return;
-        }
-
-        this.todayFlowExpanded = false;
         this.pause();
         this.detachLinkedSchedule();
         this.applyDefaultState();
@@ -1604,6 +1584,13 @@ const Pomodoro = {
         const container = document.getElementById("todayFlow");
         if (!container) return;
 
+        this.syncEnabled = false;
+        this.todayFlowExpanded = false;
+        document.body.classList.remove("pomodoro-table-sync-on");
+        container.innerHTML = "";
+        container.classList.add("hidden");
+        return;
+
         const state =
             this.getTodayFlowState(new Date());
 
@@ -1673,9 +1660,6 @@ const Pomodoro = {
         container.innerHTML = `
             <div class="today-flow-shell ${shellState}${isCollapsed ? " is-collapsed" : ""}">
                 <div class="today-flow-center">
-                    <button id="todayFlowSyncBtn" class="today-flow-btn is-primary${this.syncEnabled ? " is-on" : ""}" type="button" aria-pressed="${this.syncEnabled ? "true" : "false"}"${referenceBlock ? "" : " disabled"}>
-                        ${this.syncEnabled ? "Sincronizado" : "Sincronizar com tabela"}
-                    </button>
                     ${isCollapsed ? "" : `
                         <div class="today-flow-mode-wrap">
                             <span class="today-flow-mode-label">Ritmo da tabela</span>
@@ -1697,17 +1681,9 @@ const Pomodoro = {
     },
 
     bindTodayFlowActions() {
-        const syncBtn =
-            document.getElementById("todayFlowSyncBtn");
         const openQtsBtn =
             document.getElementById("todayFlowOpenQtsBtn");
 
-        if (syncBtn) {
-            syncBtn.onclick = () => {
-                this.toggleTodaySync();
-            };
-        }
-
         if (openQtsBtn) {
             openQtsBtn.onclick = () => {
                 if (typeof Core !== "undefined") {
diff --git a/js/qts-lab-hooks.js b/js/qts-lab-hooks.js
index 51026ac..fe02dcb 100644
--- a/js/qts-lab-hooks.js
+++ b/js/qts-lab-hooks.js
@@ -2,15 +2,14 @@
     const markerSpecs = [
         { n: 1, label: "Topo", selector: ".topbar", corner: "tl" },
         { n: 2, label: "Semanas", selector: "#qtsDetachedIndex .qts-detached-block.is-week", corner: "tl" },
-        { n: 3, label: "Dias e horário", selector: "#qtsDetachedIndex .qts-detached-block.is-toggle", corner: "tl" },
+        { n: 3, label: "Dias e horario", selector: "#qtsDetachedIndex .qts-detached-block.is-toggle", corner: "tl" },
         { n: 4, label: "Planejamento lateral", selector: "#qtsDetachedIndex .qts-detached-block.is-summary", corner: "tl" },
-        { n: 5, label: "Método Pomodoro", selector: ".qts-template-strip", corner: "tl" },
-        { n: 6, label: "Título", selector: ".qts-title", corner: "tr" },
-        { n: 7, label: "Tabela", selector: ".qts-grid-shell", corner: "tl" },
-        { n: 8, label: "Controles inferiores", selector: ".qts-controls", corner: "bl" },
-        { n: 9, label: "Planejamento por matéria", selector: "#qtsSubjectSummary", corner: "tl", optional: true },
-        { n: 10, label: "Trilho direito", selector: ".side-modules", corner: "tr" },
-        { n: 11, label: "Rodapé", selector: ".footer", corner: "tl" }
+        { n: 5, label: "Titulo", selector: ".qts-title", corner: "tr" },
+        { n: 6, label: "Tabela", selector: ".qts-grid-shell", corner: "tl" },
+        { n: 7, label: "Controles inferiores", selector: ".qts-controls", corner: "bl" },
+        { n: 8, label: "Planejamento por materia", selector: "#qtsSubjectSummary", corner: "tl", optional: true },
+        { n: 9, label: "Trilho direito", selector: ".side-modules", corner: "tr" },
+        { n: 10, label: "Rodape", selector: ".footer", corner: "tl" }
     ];
 
     function clearMarkers() {
diff --git a/js/qts.js b/js/qts.js
index f69e310..5c28958 100644
--- a/js/qts.js
+++ b/js/qts.js
@@ -1298,87 +1298,6 @@
         return structure;
     },
 
-    buildRepeatingStructure(studyMinutes, breakMinutes) {
-        const structure = [];
-        let rowCount = 0;
-        let intervalCount = 0;
-
-        while (
-            structure.length < this.maxTotalRows &&
-            rowCount < this.defaultInitialRows
-        ) {
-            structure.push({
-                type: "row",
-                duration: studyMinutes
-            });
-            rowCount += 1;
-
-            if (
-                rowCount >=
-                    this.defaultInitialRows ||
-                intervalCount >=
-                    this.defaultInitialIntervals ||
-                structure.length + 1 >
-                    this.maxTotalRows
-            ) {
-                break;
-            }
-
-            structure.push({
-                type: "interval",
-                duration: `Pausa ${breakMinutes}min`
-            });
-            intervalCount += 1;
-        }
-
-        return structure;
-    },
-
-    getTemplateDefinition(type) {
-        const map = {
-            pomo25: {
-                activeTemplate: "pomo25",
-                studyDuration: 25,
-                structure: this.buildRepeatingStructure(25, 5)
-            },
-            deep50: {
-                activeTemplate: "deep50",
-                studyDuration: 50,
-                structure: this.buildRepeatingStructure(50, 15)
-            },
-            flow52: {
-                activeTemplate: "flow52",
-                studyDuration: 52,
-                structure: this.buildRepeatingStructure(52, 17)
-            },
-            ultra90: {
-                activeTemplate: "ultra90",
-                studyDuration: 90,
-                structure: this.buildRepeatingStructure(90, 30)
-            },
-            sprint15: {
-                activeTemplate: "sprint15",
-                studyDuration: 15,
-                structure: this.buildRepeatingStructure(15, 5)
-            },
-            progressivo: {
-                activeTemplate: "progressivo",
-                studyDuration: 15,
-                structure: [
-                    { type: "row", duration: 15 },
-                    { type: "interval", duration: "Pausa 5min" },
-                    { type: "row", duration: 25 },
-                    { type: "interval", duration: "Pausa 10min" },
-                    { type: "row", duration: 35 },
-                    { type: "interval", duration: "Pausa 15min" },
-                    { type: "row", duration: 45 }
-                ]
-            }
-        };
-
-        return map[type] || null;
-    },
-
     render() {
 
         const container =
@@ -1395,20 +1314,7 @@
         container.innerHTML = `
             <div class="qts-stage-shell">
                 <div class="qts-stage-main">
-                    <div class="qts-template-strip">
-                        <div class="qts-template-label" aria-hidden="true">Método pomodoro</div>
-                        <div class="qts-templates">
-                            <button type="button" ${readOnly ? "disabled" : ""} aria-pressed="${this.data.activeTemplate === "pomo25"}" class="${this.data.activeTemplate === "pomo25" ? "is-active" : ""}" data-template="pomo25">25/5</button>
-                            <button type="button" ${readOnly ? "disabled" : ""} aria-pressed="${this.data.activeTemplate === "deep50"}" class="${this.data.activeTemplate === "deep50" ? "is-active" : ""}" data-template="deep50">50/15</button>
-                            <button type="button" ${readOnly ? "disabled" : ""} aria-pressed="${this.data.activeTemplate === "flow52"}" class="${this.data.activeTemplate === "flow52" ? "is-active" : ""}" data-template="flow52">52/17</button>
-                            <button type="button" ${readOnly ? "disabled" : ""} aria-pressed="${this.data.activeTemplate === "ultra90"}" class="${this.data.activeTemplate === "ultra90" ? "is-active" : ""}" data-template="ultra90">90/30</button>
-                            <button type="button" ${readOnly ? "disabled" : ""} aria-pressed="${this.data.activeTemplate === "sprint15"}" class="${this.data.activeTemplate === "sprint15" ? "is-active" : ""}" data-template="sprint15">15/5</button>
-                            <button type="button" ${readOnly ? "disabled" : ""} aria-pressed="${this.data.activeTemplate === "progressivo"}" class="${this.data.activeTemplate === "progressivo" ? "is-active" : ""}" data-template="progressivo">Progressivo</button>
-                            <button type="button" ${readOnly ? "disabled" : ""} aria-pressed="${this.data.activeTemplate === "manual"}" class="${this.data.activeTemplate === "manual" ? "is-active" : ""}" data-template="manual">Manual</button>
-                        </div>
-                    </div>
-
-                    <h2 class="qts-title">Quadro semanal</h2>
+                    <h2 class="qts-title">Tabela classica</h2>
 
                     <div class="qts-grid-shell${readOnly ? " is-readonly" : ""}${futureWeek ? " is-future-week" : ""}">
                         ${futureWeek ? '<div class="qts-week-overlay qts-week-overlay-future">PR\u00d3XIMA SEMANA</div>' : ""}
@@ -1439,20 +1345,6 @@
         this.buildGrid();
         this.renderSubjectSummary();
 
-        document
-            .querySelectorAll(".qts-templates button")
-            .forEach((btn) => {
-                if (btn.disabled) {
-                    return;
-                }
-                btn.onpointerdown = () => {
-                    this.commitPendingEdits();
-                };
-                btn.onclick = () => {
-                    this.applyTemplate(btn.dataset.template);
-                };
-            });
-
     },
     renderDetachedIndexRail() {
         let rail =
@@ -2102,7 +1994,11 @@ visibleDays.forEach((day, colIndex) =>
         }
     },
 
-    startTableSelection(cell, extendExisting = false) {
+    startTableSelection(
+        cell,
+        extendExisting = false,
+        preserveNativeSelection = false
+    ) {
         const coord =
             this.getCellCoord(cell);
 
@@ -2120,8 +2016,10 @@ visibleDays.forEach((day, colIndex) =>
         this.selection.focus = coord;
         this.selection.pointerDown = true;
         this.selection.dragging = false;
-        window.getSelection()
-            ?.removeAllRanges();
+        if (!preserveNativeSelection) {
+            window.getSelection()
+                ?.removeAllRanges();
+        }
         this.applyTableSelection();
     },
 
@@ -2178,7 +2076,7 @@ visibleDays.forEach((day, colIndex) =>
         }
     },
 
-    selectSingleCell(cell) {
+    selectSingleCell(cell, clearNativeSelection = true) {
         const coord =
             this.getCellCoord(cell);
 
@@ -2189,8 +2087,11 @@ visibleDays.forEach((day, colIndex) =>
         this.selection.anchor = coord;
         this.selection.focus = coord;
         this.applyTableSelection();
-        window.getSelection()
-            ?.removeAllRanges();
+
+        if (clearNativeSelection) {
+            window.getSelection()
+                ?.removeAllRanges();
+        }
     },
 
     enterCellEditMode(cell) {
@@ -2271,15 +2172,25 @@ visibleDays.forEach((day, colIndex) =>
                 return;
             }
 
-            e.preventDefault();
-            cell._qtsEditing = false;
+            const keepNativeCaret =
+                cell.isContentEditable &&
+                !e.shiftKey;
+
+            if (!keepNativeCaret) {
+                e.preventDefault();
+            }
+
+            cell._qtsEditing = keepNativeCaret;
 
             this.startTableSelection(
                 cell,
-                e.shiftKey
+                e.shiftKey,
+                keepNativeCaret
             );
 
-            cell.focus({ preventScroll: true });
+            if (!keepNativeCaret) {
+                cell.focus({ preventScroll: true });
+            }
         });
 
         cell.addEventListener("pointerenter", (e) => {
@@ -3600,42 +3511,6 @@ commitPendingEdits() {
     }
 },
 
-applyTemplate(type){
-
-this.commitPendingEdits()
-if(this.isReadonlyWeekView()) return
-
-if(type === "manual"){
-this.data.activeTemplate = "manual"
-this.save()
-this.render()
-return
-}
-
-const template =
-    this.getTemplateDefinition(type)
-
-if(!template) return
-
-const snapshots =
-    this.getRowSnapshots()
-const startTime =
-    this.getSeedStartTime()
-
-this.data.activeTemplate =
-    template.activeTemplate
-this.data.studyDuration =
-    template.studyDuration
-this.data.structure =
-    template.structure.map((item) => ({ ...item }))
-
-this.generateTimes(startTime)
-this.restoreRowSnapshots(snapshots)
-this.refreshIntervalLabels()
-this.save()
-this.render()
-},
-
     save() {
         this.syncViewedWeekPlan();
         localStorage.setItem(
diff --git a/js/utilityWindows.js b/js/utilityWindows.js
index 6363c07..512e091 100644
--- a/js/utilityWindows.js
+++ b/js/utilityWindows.js
@@ -19,6 +19,7 @@ const UtilityWindows = {
             total: 0,
             remaining: 0,
             interval: null,
+            compactMode: false,
             fields: {
                 hours: "00",
                 minutes: "00",
@@ -619,6 +620,8 @@ const UtilityWindows = {
         tool.interval = null;
         tool.total = safeTotal;
         tool.remaining = safeTotal;
+        tool.compactMode =
+            options.simuladoCompact === true;
         this.setTimerFieldsFromSeconds(
             safeTotal
         );
@@ -691,6 +694,7 @@ const UtilityWindows = {
             tool.interval = null;
             tool.total = this.getTimerFieldsInSeconds();
             tool.remaining = tool.total;
+            tool.compactMode = false;
         }
 
         this.updateTimerView();
@@ -701,6 +705,14 @@ const UtilityWindows = {
         const tool = this.tools.timer;
         const display = document.getElementById("utilityTimerDisplay");
         const panel = this.getPanel("timer");
+        const compactMobile =
+            Boolean(tool?.compactMode) &&
+            typeof window !== "undefined" &&
+            typeof window.matchMedia ===
+                "function" &&
+            window.matchMedia(
+                "(max-width: 760px) and (pointer: coarse)"
+            ).matches;
 
         if (display) {
             display.textContent = this.formatClock(tool.remaining);
@@ -716,6 +728,10 @@ const UtilityWindows = {
 
         if (panel) {
             panel.classList.toggle("utility-is-live", Boolean(tool.interval));
+            panel.classList.toggle(
+                "utility-window-compact-mobile",
+                compactMobile
+            );
         }
     },
 
diff --git a/premium-study/app/index.js b/premium-study/app/index.js
index 1ff74f7..5da3c88 100644
--- a/premium-study/app/index.js
+++ b/premium-study/app/index.js
@@ -3,6 +3,8 @@
         return;
     }
 
+    const CHECKOUT_CONTEXT_KEY = "rotanota-premium-checkout-context";
+
     window.PremiumStudyApp = {
         root: null,
         analysisTimers: [],
@@ -12,6 +14,74 @@
         persistTimer: null,
         persistPromise: null,
         premiumStatusTimers: [],
+        initialLoadPromise: null,
+        lastKnownPremiumActive: false,
+        activeMaterialFile: null,
+        materialExtractionPromise: null,
+
+        getGrowthService() {
+            return window.PremiumStudyGrowth || null;
+        },
+
+        trackGrowth(eventType, data = {}, options = {}) {
+            const growth = this.getGrowthService();
+            if (!growth || typeof growth.track !== "function") {
+                return Promise.resolve(null);
+            }
+
+            const store = window.PremiumStudyStore;
+            const state = store && typeof store.getState === "function"
+                ? store.getState()
+                : {};
+            const metadata = data.metadata && typeof data.metadata === "object"
+                ? data.metadata
+                : {};
+
+            return growth.track(eventType, {
+                customerId: data.customerId || state.customerId || "",
+                materialHash: data.materialHash || state.materialHash || "",
+                metadata: {
+                    step: data.step || state.step || "",
+                    feature: data.feature || "",
+                    surface: data.surface || "",
+                    sourceStep: data.sourceStep || "",
+                    planId: data.planId || "",
+                    reason: data.reason || "",
+                    pageCount: Number.isFinite(data.pageCount) ? data.pageCount : (state.materialPageCount || 0),
+                    ...metadata
+                }
+            }, options);
+        },
+
+        trackGrowthOnce(key, eventType, data = {}, options = {}) {
+            const growth = this.getGrowthService();
+            if (!growth || typeof growth.trackOnce !== "function") {
+                return Promise.resolve(null);
+            }
+
+            const store = window.PremiumStudyStore;
+            const state = store && typeof store.getState === "function"
+                ? store.getState()
+                : {};
+            const metadata = data.metadata && typeof data.metadata === "object"
+                ? data.metadata
+                : {};
+
+            return growth.trackOnce(key, eventType, {
+                customerId: data.customerId || state.customerId || "",
+                materialHash: data.materialHash || state.materialHash || "",
+                metadata: {
+                    step: data.step || state.step || "",
+                    feature: data.feature || "",
+                    surface: data.surface || "",
+                    sourceStep: data.sourceStep || "",
+                    planId: data.planId || "",
+                    reason: data.reason || "",
+                    pageCount: Number.isFinite(data.pageCount) ? data.pageCount : (state.materialPageCount || 0),
+                    ...metadata
+                }
+            }, options);
+        },
 
         async init(options = {}) {
             this.root = options.root || document.getElementById("premium-studyModule");
@@ -21,27 +91,133 @@
             }
 
             this.root.classList.add("premium-study-host");
+            this.root.dataset.ready = "true";
             this.bindRoot();
-            await this.hydrateFromStorage();
-            await this.refreshPremiumAccess();
-            this.consumePaymentReturn();
+            if (window.PremiumStudyGrowth && typeof window.PremiumStudyGrowth.captureAcquisitionContext === "function") {
+                window.PremiumStudyGrowth.captureAcquisitionContext();
+            }
             this.render();
+
+            if (!this.initialLoadPromise) {
+                this.initialLoadPromise = this.finishInitialLoad();
+            }
+        },
+
+        async finishInitialLoad() {
+            try {
+                await this.hydrateFromStorage();
+                this.render();
+
+                const paymentReturn = window.RotaNotaPremiumPaymentReturn;
+                if (paymentReturn && !paymentReturn.consumed) {
+                    await this.consumePaymentReturn();
+                } else {
+                    await this.refreshPremiumAccess();
+                }
+                this.render();
+
+                if (window.PremiumStudyPromotions && typeof window.PremiumStudyPromotions.refresh === "function") {
+                    await window.PremiumStudyPromotions.refresh("premium_checkout", "");
+                    if (window.PremiumStudyStore.getState().step === "premium-checkout") {
+                        this.render();
+                    }
+                }
+
+                await this.runHomeAction(window.RotaNotaPremiumHomeAction || "");
+
+                this.trackGrowthOnce("premium-module-entry", "premium_module_entry", {
+                    surface: "premium_entry"
+                });
+            } catch (error) {
+                console.warn("Inicializacao em segundo plano do PDF Focado falhou", error);
+            } finally {
+                this.initialLoadPromise = null;
+            }
         },
 
-        async refreshPremiumAccess() {
+        getCheckoutReturnContext() {
+            if (!window.sessionStorage) {
+                return null;
+            }
+
+            try {
+                const raw = window.sessionStorage.getItem(CHECKOUT_CONTEXT_KEY);
+                return raw ? JSON.parse(raw) : null;
+            } catch (error) {
+                return null;
+            }
+        },
+
+        clearCheckoutReturnContext() {
+            if (!window.sessionStorage) {
+                return;
+            }
+
+            try {
+                window.sessionStorage.removeItem(CHECKOUT_CONTEXT_KEY);
+            } catch (error) {
+                // Ignora bloqueios de storage do navegador.
+            }
+        },
+
+        async refreshPremiumAccess(options = {}) {
             if (!window.PremiumStudyAccount || typeof window.PremiumStudyAccount.refreshAndApply !== "function") {
                 return null;
             }
 
-            const status = await window.PremiumStudyAccount.refreshAndApply();
+            const status = await window.PremiumStudyAccount.refreshAndApply(options);
+            const isPremiumActive = Boolean(status && status.premiumActive);
 
-            if (status && status.premiumActive) {
+            if (isPremiumActive) {
                 window.PremiumStudyStore.clearSessionNote();
             }
 
+            if (!this.lastKnownPremiumActive && isPremiumActive) {
+                this.trackGrowthOnce(`premium-active-${status.customerId || "guest"}`, "premium_active_client_seen", {
+                    metadata: {
+                        subscriptionStatus: status.subscriptionStatus || "premium_active"
+                    }
+                });
+            }
+
+            this.lastKnownPremiumActive = isPremiumActive;
+
             return status;
         },
 
+        async runHomeAction(action = "") {
+            const requestedAction = String(action || "").trim();
+
+            if (!requestedAction) {
+                return false;
+            }
+
+            window.RotaNotaPremiumHomeAction = "";
+
+            if (!this.root) {
+                return false;
+            }
+
+            if (requestedAction === "pdf-upload") {
+                await this.handleAction("open-file-picker", {});
+                return true;
+            }
+
+            if (requestedAction === "pdf-resume") {
+                await this.handleAction("resume-latest-study", {});
+                this.render();
+                return true;
+            }
+
+            if (requestedAction === "pdf-library") {
+                await this.handleAction("open-premium-library", {});
+                this.render();
+                return true;
+            }
+
+            return false;
+        },
+
         consumePaymentReturn() {
             const paymentReturn = window.RotaNotaPremiumPaymentReturn;
 
@@ -125,6 +301,149 @@
             });
         },
 
+        buildPaymentReturnNote(paymentStatus, isPremiumActive) {
+            if (paymentStatus === "failure") {
+                return {
+                    tone: "premium",
+                    title: "Pagamento nao concluido",
+                    message: "O pagamento foi cancelado ou nao foi aprovado. Quando quiser, voce pode tentar novamente sem perder sua trilha."
+                };
+            }
+
+            if (isPremiumActive) {
+                return {
+                    tone: "success",
+                    title: "Premium liberado. Sua trilha continua daqui.",
+                    message: "Pagamento confirmado com sucesso. Biblioteca completa, continuidade e extras premium ja ficaram disponiveis neste estudo."
+                };
+            }
+
+            if (paymentStatus === "success") {
+                return {
+                    tone: "info",
+                    title: "Pagamento recebido. Estamos finalizando a liberacao.",
+                    message: "Voce ja voltou para a sua trilha. Enquanto isso, validamos a confirmacao do servidor em segundo plano para ativar o premium sem te tirar do estudo."
+                };
+            }
+
+            return {
+                tone: "premium",
+                title: "Pagamento em analise",
+                message: "O Mercado Pago marcou o pagamento como pendente. Sua trilha continua normal enquanto buscamos a confirmacao final."
+            };
+        },
+
+        async consumePaymentReturn() {
+            const paymentReturn = window.RotaNotaPremiumPaymentReturn;
+
+            if (!paymentReturn || paymentReturn.consumed) {
+                return;
+            }
+
+            const store = window.PremiumStudyStore;
+            const router = window.PremiumStudyRouter;
+            const checkoutContext = this.getCheckoutReturnContext();
+            const snapshot = checkoutContext && checkoutContext.snapshot && checkoutContext.snapshot.materialName
+                ? checkoutContext.snapshot
+                : null;
+            const targetStep = snapshot
+                ? this.getResumeStep(snapshot)
+                : "entry";
+            let status = null;
+
+            if (
+                (paymentReturn.status === "success" || paymentReturn.status === "pending") &&
+                paymentReturn.paymentId
+            ) {
+                status = await this.refreshPremiumAccess({
+                    paymentId: paymentReturn.paymentId
+                });
+            }
+
+            const note = this.buildPaymentReturnNote(
+                paymentReturn.status,
+                Boolean(status && status.premiumActive)
+            );
+
+            paymentReturn.consumed = true;
+
+            if (snapshot && paymentReturn.status !== "failure") {
+                store.restoreFromSnapshot({
+                    ...snapshot,
+                    step: targetStep,
+                    sessionNote: null
+                });
+                router.goTo(targetStep);
+                store.setReturnStep(snapshot.returnStep || targetStep);
+                store.setPremiumOffer(null);
+                store.setSessionNote({
+                    step: targetStep,
+                    ...note
+                });
+            } else {
+                store.setPremiumOffer({
+                    eyebrow: "RotaNota Premium",
+                    title: paymentReturn.status === "success"
+                        ? "Pagamento recebido. Estamos voltando para sua trilha."
+                        : "Finalize seu acesso premium com seguranca.",
+                    lead: "O checkout segue conectado ao Mercado Pago e o servidor continua como fonte de verdade da liberacao premium.",
+                    benefits: [
+                        "Checkout seguro funcionando",
+                        "Confirmacao premium monitorada pelo servidor",
+                        "Acesso premium liberado sem reiniciar sua trilha"
+                    ],
+                    cta: "Voltar ao estudo",
+                    sourceStep: checkoutContext && checkoutContext.sourceStep
+                        ? checkoutContext.sourceStep
+                        : "entry"
+                });
+                store.setReturnStep(checkoutContext && checkoutContext.sourceStep
+                    ? checkoutContext.sourceStep
+                    : "entry");
+                store.setSessionNote({
+                    step: "premium-checkout",
+                    ...note
+                });
+                router.goTo("premium-checkout");
+            }
+
+            this.clearCheckoutReturnContext();
+
+            if (
+                (paymentReturn.status === "success" || paymentReturn.status === "pending") &&
+                !(status && status.premiumActive)
+            ) {
+                this.schedulePremiumStatusRefresh(paymentReturn, snapshot ? targetStep : "premium-checkout");
+            }
+        },
+
+        schedulePremiumStatusRefresh(paymentReturn = null, targetStep = "premium-checkout") {
+            this.clearPremiumStatusTimers();
+
+            [2500, 7000, 15000, 30000].forEach((delay) => {
+                const timerId = window.setTimeout(async () => {
+                    const status = await this.refreshPremiumAccess({
+                        paymentId: paymentReturn && paymentReturn.paymentId
+                            ? paymentReturn.paymentId
+                            : ""
+                    });
+
+                    if (status && status.premiumActive) {
+                        window.PremiumStudyStore.setSessionNote({
+                            step: targetStep,
+                            tone: "success",
+                            title: "Premium liberado",
+                            message: "Pagamento confirmado. Seus recursos premium ja estao ativos e sua trilha continua do ponto onde voce parou."
+                        });
+                        this.clearPremiumStatusTimers();
+                        this.render();
+                    }
+                }, delay);
+
+                this.premiumStatusTimers.push(timerId);
+            });
+        },
+
         bindRoot() {
             if (this.root.dataset.bound === "true") {
                 return;
@@ -144,6 +463,17 @@
                 if (state.step === "block" && state.blockFullScreen && !isActive) {
                     store.setBlockFullScreen(false);
                     this.render();
+                    return;
+                }
+
+                if (
+                    state.step === "highlight-preview" &&
+                    state.highlightEditorOpen &&
+                    state.highlightEditorFullScreen &&
+                    !isActive
+                ) {
+                    store.setHighlightEditorFullScreen(false);
+                    this.render();
                 }
             });
 
@@ -162,7 +492,22 @@
                 const itemValue = actionTarget.dataset.itemValue || "";
                 const practiceType = actionTarget.dataset.practiceType || "";
                 const slotIndex = actionTarget.dataset.slotIndex || "";
-                this.handleAction(action, { blockId, tabId, dateValue, answerIndex, itemIndex, itemValue, practiceType, slotIndex });
+                const sectionIndex = actionTarget.dataset.sectionIndex || "";
+                const paragraphIndex = actionTarget.dataset.paragraphIndex || "";
+                const partIndex = actionTarget.dataset.partIndex || "";
+                this.handleAction(action, {
+                    blockId,
+                    tabId,
+                    dateValue,
+                    answerIndex,
+                    itemIndex,
+                    itemValue,
+                    practiceType,
+                    slotIndex,
+                    sectionIndex,
+                    paragraphIndex,
+                    partIndex
+                });
             });
 
             this.root.addEventListener("change", (event) => {
@@ -207,6 +552,78 @@
 
             this.root.addEventListener("pointerup", clearPointer);
             this.root.addEventListener("pointercancel", clearPointer);
+            this.root.addEventListener("mouseup", () => {
+                this.captureHighlightSelection();
+            });
+            this.root.addEventListener("keyup", () => {
+                this.captureHighlightSelection();
+            });
+        },
+
+        getHighlightSelectionOffset(root, container, offset) {
+            if (!root || !container) {
+                return 0;
+            }
+
+            const range = document.createRange();
+
+            try {
+                range.selectNodeContents(root);
+                range.setEnd(container, Number(offset) || 0);
+            } catch (error) {
+                return 0;
+            }
+
+            return range.toString().length;
+        },
+
+        captureHighlightSelection() {
+            if (!this.root) {
+                return;
+            }
+
+            const store = window.PremiumStudyStore;
+            const state = store.getState();
+
+            if (
+                state.step !== "highlight-preview" ||
+                !state.highlightEditorOpen
+            ) {
+                return;
+            }
+
+            const selection = window.getSelection ? window.getSelection() : null;
+
+            if (!selection || selection.rangeCount === 0 || selection.isCollapsed) {
+                return;
+            }
+
+            const range = selection.getRangeAt(0);
+            const startElement = range.startContainer?.parentElement
+                ? range.startContainer.parentElement.closest("[data-premium-highlight-paragraph=\"true\"]")
+                : null;
+            const endElement = range.endContainer?.parentElement
+                ? range.endContainer.parentElement.closest("[data-premium-highlight-paragraph=\"true\"]")
+                : null;
+
+            if (!startElement || startElement !== endElement) {
+                return;
+            }
+
+            const sectionIndex = Number(startElement.dataset.sectionIndex);
+            const paragraphIndex = Number(startElement.dataset.paragraphIndex);
+            const startOffset = this.getHighlightSelectionOffset(startElement, range.startContainer, range.startOffset);
+            const endOffset = this.getHighlightSelectionOffset(startElement, range.endContainer, range.endOffset);
+
+            if (!Number.isFinite(sectionIndex) || !Number.isFinite(paragraphIndex) || startOffset === endOffset) {
+                return;
+            }
+
+            store.setHighlightTextSelection(sectionIndex, paragraphIndex, startOffset, endOffset);
+            store.setHighlightInspectorMode("");
+            selection.removeAllRanges();
+            this.render();
+            this.schedulePersist(120);
         },
 
         isNativeFullScreenActive() {
@@ -240,8 +657,12 @@
         async syncNativeFullScreen(preferEnter = false) {
             const state = window.PremiumStudyStore.getState();
             const shouldBeNativeFullScreen =
-                state.step === "block" &&
-                state.blockFullScreen;
+                (state.step === "block" && state.blockFullScreen) ||
+                (
+                    state.step === "highlight-preview" &&
+                    state.highlightEditorOpen &&
+                    state.highlightEditorFullScreen
+                );
 
             if (!shouldBeNativeFullScreen) {
                 await this.exitNativeFullScreen();
@@ -284,7 +705,7 @@
             const feature = access && access.FEATURES
                 ? access.FEATURES[featureName] || featureName
                 : featureName;
-            const offer = access
+            const baseOffer = access
                 ? access.buildOffer(feature)
                 : {
                     feature,
@@ -294,13 +715,43 @@
                     benefits: ["Mais continuidade", "Mais treino", "Mais controle"],
                     cta: "Conhecer premium"
                 };
+            const source = sourceStep || store.getState().step || "entry";
 
-            store.setPremiumOffer({
-                ...offer,
-                sourceStep: sourceStep || store.getState().step
-            });
-            store.setReturnStep(sourceStep || store.getState().step || "entry");
+            const applyOffer = (offer) => {
+                store.setPremiumOffer({
+                    ...offer,
+                    sourceStep: source
+                });
+            };
+
+            applyOffer(baseOffer);
+            store.setReturnStep(source);
             router.goTo("premium-checkout");
+            this.trackGrowth("paywall_viewed", {
+                feature,
+                sourceStep: source,
+                surface: "premium_checkout",
+                metadata: {
+                    offerTitle: baseOffer.title || ""
+                }
+            });
+
+            if (window.PremiumStudyPromotions && typeof window.PremiumStudyPromotions.refresh === "function") {
+                window.PremiumStudyPromotions.refresh("premium_checkout", feature)
+                    .then(() => {
+                        const enhanced = window.PremiumStudyPromotions.enhanceOffer(baseOffer, {
+                            feature,
+                            surface: "premium_checkout"
+                        });
+                        applyOffer(enhanced);
+                        if (window.PremiumStudyStore.getState().step === "premium-checkout") {
+                            this.render();
+                        }
+                    })
+                    .catch(() => {
+                        // Se a promocao nao responder, mantemos a oferta base.
+                    });
+            }
         },
 
         async handleSelectedFile(file, input) {
@@ -316,6 +767,14 @@
             }
 
             if (!validation.ok) {
+                this.trackGrowth("pdf_upload_blocked", {
+                    reason: validation.reason || "invalid_pdf",
+                    pageCount: Number(validation.pageCount || 0),
+                    metadata: {
+                        fileName: file && file.name ? file.name : "",
+                        message: validation.message || ""
+                    }
+                });
                 if (validation.reason === "page_limit") {
                     this.openPremiumOffer("LARGE_PDF_UPLOAD", "entry");
                     store.setSessionNote({
@@ -337,15 +796,156 @@
                 return;
             }
 
+            this.activeMaterialFile = file;
+            let materialHash = "";
+
+            if (window.PremiumStudyGrowth && typeof window.PremiumStudyGrowth.buildMaterialHash === "function") {
+                materialHash = await window.PremiumStudyGrowth.buildMaterialHash({
+                    name: file.name,
+                    size: file.size,
+                    type: file.type,
+                    pageCount: validation.pageCount
+                });
+            }
+
+            const materialFileKey = this.buildMaterialFileKey(file, materialHash);
+
             store.setMaterial({
                 name: file.name,
                 size: file.size,
                 type: file.type,
-                pageCount: validation.pageCount
+                pageCount: validation.pageCount,
+                materialHash,
+                materialFileKey
+            });
+
+            if (window.PremiumStudyStorage && typeof window.PremiumStudyStorage.saveMaterialFile === "function") {
+                await window.PremiumStudyStorage.saveMaterialFile(materialFileKey, file, {
+                    name: file.name,
+                    type: file.type,
+                    size: file.size,
+                    lastModified: file.lastModified
+                });
+            }
+
+            store.patch({
+                progressLabel: "PDF salvo localmente. Agora estamos preparando o texto integral para os modos de leitura e marcacao."
+            });
+            this.render();
+
+            this.trackGrowth("pdf_upload_success", {
+                materialHash,
+                pageCount: Number(validation.pageCount || 0),
+                metadata: {
+                    fileName: file.name || ""
+                }
             });
             router.goTo("exam-date");
             this.render();
             this.schedulePersist(80);
+            this.ensureMaterialTextExtracted().catch(() => {
+                // Seguimos o fluxo e deixamos a extracao integral trabalhar em segundo plano.
+            });
+        },
+
+        buildMaterialFileKey(file, materialHash = "") {
+            const stableHash = String(materialHash || "").trim();
+            if (stableHash) {
+                return `pdf-${stableHash}`;
+            }
+
+            const name = String(file && file.name ? file.name : "material.pdf")
+                .trim()
+                .toLowerCase()
+                .replace(/[^a-z0-9]+/g, "-")
+                .replace(/^-+|-+$/g, "");
+            const size = Number(file && file.size ? file.size : 0);
+            const lastModified = Number(file && file.lastModified ? file.lastModified : 0);
+
+            return `pdf-${name || "material"}-${size}-${lastModified}`;
+        },
+
+        async restoreActiveMaterialFile(preferredKey = "") {
+            const store = window.PremiumStudyStore;
+            const state = store.getState();
+            const targetKey =
+                String(preferredKey || state.materialFileKey || state.materialHash || "").trim();
+
+            if (
+                this.activeMaterialFile &&
+                targetKey &&
+                targetKey === this.buildMaterialFileKey(this.activeMaterialFile, state.materialHash)
+            ) {
+                return this.activeMaterialFile;
+            }
+
+            if (
+                !targetKey ||
+                !window.PremiumStudyStorage ||
+                typeof window.PremiumStudyStorage.getMaterialFile !== "function"
+            ) {
+                return null;
+            }
+
+            const restoredFile = await window.PremiumStudyStorage.getMaterialFile(targetKey);
+            if (restoredFile) {
+                this.activeMaterialFile = restoredFile;
+            }
+
+            return restoredFile || null;
+        },
+
+        async ensureMaterialTextExtracted() {
+            const store = window.PremiumStudyStore;
+            const state = store.getState();
+
+            if (String(state.materialExtractedText || "").trim()) {
+                return state.materialExtractedText;
+            }
+
+            const materialFile =
+                this.activeMaterialFile ||
+                await this.restoreActiveMaterialFile(state.materialFileKey || state.materialHash || "");
+
+            if (!materialFile || !window.PremiumStudyPdfTextExtractor) {
+                return "";
+            }
+
+            if (!this.materialExtractionPromise) {
+                this.materialExtractionPromise = (async () => {
+                    const extraction = await window.PremiumStudyPdfTextExtractor.extractText(materialFile, {
+                        maxChars: 180000,
+                        maxPages: state.accessTier === "premium" ? 80 : 24
+                    });
+
+                    store.setMaterialExtraction(extraction);
+                    this.schedulePersist(80);
+
+                    if (!extraction || !String(extraction.text || "").trim()) {
+                        const extractionStatus = String(
+                            extraction && extraction.status ? extraction.status : "desconhecido"
+                        );
+                        const lowQuality = extractionStatus.includes("low_quality");
+                        store.setSessionNote({
+                            step: "highlight-preview",
+                            tone: "info",
+                            title: "Nao consegui abrir o texto integral ainda",
+                            message: lowQuality
+                                ? "O PDF abriu, mas o texto extraido ficou com baixa qualidade. Isso costuma acontecer em arquivo escaneado, protegido ou com camada de texto ruim."
+                                : `Este navegador nao conseguiu extrair o texto completo do PDF agora. Status tecnico: ${extractionStatus}.`
+                        });
+                        return "";
+                    }
+
+                    return extraction.text;
+                })();
+            }
+
+            try {
+                return await this.materialExtractionPromise;
+            } finally {
+                this.materialExtractionPromise = null;
+            }
         },
 
         async persistCurrentState() {
@@ -487,7 +1087,12 @@ li{margin:0 0 10px}
     <h2>${window.PremiumStudyUI.escapeHtml(section.title)}</h2>
     ${(section.paragraphs || []).map((paragraph) => `
         <p>${paragraph.map((part) => part.highlight
-        ? `<mark>${window.PremiumStudyUI.escapeHtml(part.text)}</mark>`
+        ? `<mark style="background:${({
+            gold: "rgba(255, 203, 109, 0.52)",
+            mint: "rgba(88, 227, 183, 0.42)",
+            blue: "rgba(121, 213, 255, 0.42)",
+            rose: "rgba(255, 151, 188, 0.44)"
+        })[part.colorKey] || "rgba(255, 203, 109, 0.52)"}">${window.PremiumStudyUI.escapeHtml(part.text)}</mark>`
         : window.PremiumStudyUI.escapeHtml(part.text)).join("")}</p>
     `).join("")}
 </div>`).join("");
@@ -506,7 +1111,7 @@ ${sections}`
             this.analysisTimers = [];
         },
 
-        startAnalysisSequence() {
+        legacyStartAnalysisSequence() {
             this.clearAnalysisTimers();
             window.PremiumStudyStore.setAnalysisProgress(10, "running");
 
@@ -533,6 +1138,114 @@ ${sections}`
             });
         },
 
+        buildBundleSummary() {
+            const state = window.PremiumStudyStore.getState();
+            return state.blocks.map((block) => {
+                const learn = block.learn || {};
+                return [
+                    block.title,
+                    block.subtitle,
+                    learn.summary,
+                    ...(Array.isArray(block.topics) ? block.topics : []),
+                    ...(Array.isArray(learn.keyConcepts) ? learn.keyConcepts : []),
+                    ...(Array.isArray(learn.hotPoints) ? learn.hotPoints : [])
+                ].filter(Boolean).join("\n");
+            }).join("\n\n");
+        },
+
+        async startAnalysisSequence() {
+            this.clearAnalysisTimers();
+            const store = window.PremiumStudyStore;
+            const state = store.getState();
+            store.setAnalysisProgress(12, "running");
+            store.patch({
+                progressLabel: "Lendo o PDF e preparando uma rota unica de estudo."
+            });
+            this.render();
+
+            try {
+                let extractedText = state.materialExtractedText || "";
+
+                if (!extractedText) {
+                    store.setAnalysisProgress(28, "running");
+                    this.render();
+                    extractedText = await this.ensureMaterialTextExtracted();
+                }
+
+                store.setAnalysisProgress(52, "running");
+                store.patch({
+                    progressLabel: "A IA esta montando blocos, praticas e mini provas a partir do material."
+                });
+                this.render();
+
+                const current = store.getState();
+                const dailyMinutes = (Number(current.studyHours) || 0) * 60
+                    + (Number(current.studyMinutes) || 0);
+                const ai = window.PremiumStudyAI;
+                const result = ai && typeof ai.request === "function"
+                    ? await ai.request(ai.TASKS.FREE_BUNDLE_FROM_MATERIAL, {
+                        customerId: current.customerId || "",
+                        materialHash: current.materialHash || "",
+                        materialName: current.materialName || "",
+                        pageCount: current.materialPageCount || 0,
+                        extractedText,
+                        examDate: current.examDate || "",
+                        targetScore: current.targetScore || "",
+                        dailyMinutes
+                    })
+                    : { ok: false, status: "ai_unavailable" };
+
+                store.setAnalysisProgress(82, "running");
+                if (result && result.ok && result.bundle) {
+                    store.applyGeneratedBundle(result);
+                    this.trackGrowth("ai_bundle_generated", {
+                        materialHash: store.getState().materialHash,
+                        metadata: {
+                            provider: result.provider || "",
+                            model: result.model || "",
+                            blockCount: result.bundle && result.bundle.blocks ? result.bundle.blocks.length : 0
+                        }
+                    });
+                } else {
+                    store.patch({
+                        aiGeneration: {
+                            status: result && result.status ? result.status : "fallback",
+                            provider: result && result.provider ? result.provider : "",
+                            model: result && result.model ? result.model : "",
+                            promptVersion: result && result.promptVersion ? result.promptVersion : "",
+                            generatedAt: new Date().toISOString()
+                        },
+                        progressLabel: "Nao consegui acionar a IA agora. Mantive uma rota local para voce continuar estudando."
+                    });
+                    store.setSessionNote({
+                        step: "mode-select",
+                        tone: "info",
+                        title: "IA em modo fallback",
+                        message: "O PDF abriu com o pacote local. Tente novamente mais tarde para gerar conteudo totalmente baseado no texto."
+                    });
+                }
+
+                store.setAnalysisProgress(100, "done");
+                window.PremiumStudyRouter.goTo("mode-select");
+                this.render();
+                this.schedulePersist(120);
+            } catch (error) {
+                store.setAnalysisProgress(100, "done");
+                store.patch({
+                    progressLabel: "A rota local esta pronta. A IA nao respondeu desta vez."
+                });
+                store.setSessionNote({
+                    step: "mode-select",
+                    tone: "info",
+                    title: "IA indisponivel agora",
+                    message: "Mantive uma rota base para voce nao perder o fluxo. Quando a IA responder, ela gera o pacote completo do PDF."
+                });
+                window.PremiumStudyRouter.goTo("mode-select");
+                this.render();
+                this.schedulePersist(120);
+            }
+        },
+
         updateRingFromPointer(event, ring) {
             const control = ring.dataset.ringControl || "";
             const rect = ring.getBoundingClientRect();
@@ -599,6 +1312,13 @@ ${sections}`
                 };
             }
 
+            await this.restoreActiveMaterialFile(
+                store.getState().materialFileKey ||
+                snapshot.materialFileKey ||
+                snapshot.materialHash ||
+                ""
+            );
+
             router.goTo(nextStep);
 
             return {
@@ -703,18 +1423,21 @@ ${sections}`
             case "resume-latest-study": {
                 const latestDraft = await window.PremiumStudyStorage.getLatestDraft();
                 if (latestDraft && latestDraft.snapshot) {
-                    store.restoreFromSnapshot({
+                    const resumeState = await this.resumeSnapshot({
                         ...latestDraft.snapshot,
                         savedAt: latestDraft.savedAt,
                         step: "mode-select",
                         sessionNote: null
                     });
-                    if (store.getState().materialName) {
-                        router.goTo("mode-select");
-                        shouldSyncNativeFullScreen = true;
-                        preferEnterNativeFullScreen = false;
-                    }
+                    shouldSyncNativeFullScreen = resumeState.shouldSyncNativeFullScreen;
+                    preferEnterNativeFullScreen = resumeState.preferEnterNativeFullScreen;
                     shouldPersist = true;
+                    this.trackGrowth("resume_latest_study", {
+                        materialHash: store.getState().materialHash,
+                        metadata: {
+                            source: "latest_draft"
+                        }
+                    });
                 }
                 break;
             }
@@ -740,6 +1463,16 @@ ${sections}`
                 store.setAnalysisProgress(10, "pending");
                 router.goTo("analysis");
                 shouldPersist = true;
+                this.trackGrowth("trial_started", {
+                    materialHash: store.getState().materialHash,
+                    pageCount: Number(store.getState().materialPageCount || 0),
+                    metadata: {
+                        examDate: store.getState().examDate || "",
+                        targetScore: store.getState().targetScore || 0,
+                        studyHours: store.getState().studyHours || 0,
+                        studyMinutes: store.getState().studyMinutes || 0
+                    }
+                });
                 break;
             case "choose-mode-learn":
                 store.setBlockTab("aprender");
@@ -747,6 +1480,7 @@ ${sections}`
                 shouldPersist = true;
                 break;
             case "choose-mode-highlight":
+                await this.ensureMaterialTextExtracted();
                 store.openHighlightDocument();
                 router.goTo("highlight-preview");
                 shouldPersist = true;
@@ -759,9 +1493,13 @@ ${sections}`
                 shouldSyncNativeFullScreen = true;
                 break;
             case "choose-mode-exam":
+                if (!canUseFeature("LEVEL_EXAM")) {
+                    openPremiumOffer("LEVEL_EXAM");
+                    shouldPersist = true;
+                    break;
+                }
                 store.setReturnStep("mode-select");
-                store.resetActiveSession("miniExam");
-                router.goTo("mini-exam");
+                router.goTo("level-exam");
                 shouldPersist = true;
                 shouldSyncNativeFullScreen = true;
                 break;
@@ -820,6 +1558,144 @@ ${sections}`
                 this.downloadHighlightedPdf("full");
                 shouldPersist = true;
                 break;
+            case "open-highlight-editor":
+                await this.ensureMaterialTextExtracted();
+                store.openHighlightDocument();
+                store.setHighlightEditorOpen(true);
+                store.setHighlightEditorFullScreen(true);
+                shouldPersist = true;
+                shouldSyncNativeFullScreen = true;
+                preferEnterNativeFullScreen = true;
+                break;
+            case "close-highlight-editor":
+                store.setHighlightEditorFullScreen(false);
+                store.setHighlightEditorOpen(false);
+                shouldPersist = true;
+                shouldSyncNativeFullScreen = true;
+                break;
+            case "expand-highlight-editor":
+                store.setHighlightEditorOpen(true);
+                store.setHighlightEditorFullScreen(true);
+                shouldPersist = true;
+                shouldSyncNativeFullScreen = true;
+                preferEnterNativeFullScreen = true;
+                break;
+            case "collapse-highlight-editor":
+                store.setHighlightEditorFullScreen(false);
+                shouldPersist = true;
+                shouldSyncNativeFullScreen = true;
+                break;
+            case "select-highlight-part":
+                store.setHighlightSelection(
+                    Number(payload.sectionIndex),
+                    Number(payload.paragraphIndex),
+                    Number(payload.partIndex)
+                );
+                store.setHighlightInspectorMode("");
+                shouldPersist = true;
+                break;
+            case "open-highlight-edit":
+                store.setHighlightInspectorMode("edit");
+                shouldPersist = true;
+                break;
+            case "open-highlight-note":
+                store.setHighlightInspectorMode("note");
+                shouldPersist = true;
+                break;
+            case "close-highlight-inspector":
+                store.setHighlightInspectorMode("");
+                shouldPersist = true;
+                break;
+            case "set-highlight-color":
+                store.setHighlightColor(payload.itemValue);
+                shouldPersist = true;
+                break;
+            case "mark-highlight-selection":
+                store.setSelectedHighlightStyleMode("highlight");
+                shouldPersist = true;
+                break;
+            case "unmark-highlight-selection":
+                store.toggleSelectedHighlight(false);
+                shouldPersist = true;
+                break;
+            case "set-highlight-style-box":
+                store.setSelectedHighlightStyleMode("box");
+                shouldPersist = true;
+                break;
+            case "toggle-highlight-selection":
+                store.toggleSelectedHighlight();
+                shouldPersist = true;
+                break;
+            case "clear-all-highlights":
+                store.clearAllHighlights();
+                shouldPersist = true;
+                break;
+            case "restore-highlight-document":
+                store.restoreOriginalHighlightedDocument();
+                shouldPersist = true;
+                break;
+            case "save-highlight-text": {
+                const editor = this.root.querySelector("#premiumHighlightEditor");
+                store.updateSelectedHighlightText(editor ? editor.value : "");
+                store.setHighlightInspectorMode("");
+                shouldPersist = true;
+                break;
+            }
+            case "save-highlight-note": {
+                const editor = this.root.querySelector("#premiumHighlightNoteEditor");
+                store.updateSelectedHighlightNote(editor ? editor.value : "");
+                store.setHighlightInspectorMode("");
+                shouldPersist = true;
+                break;
+            }
+            case "delete-highlight-text":
+                store.deleteSelectedHighlightText();
+                store.setHighlightInspectorMode("");
+                shouldPersist = true;
+                break;
+            case "copy-highlight-text": {
+                const documentData = store.getState().highlightedDocument;
+                const selectedPartId = documentData && documentData.selectedPartId
+                    ? documentData.selectedPartId
+                    : "";
+                let selectedText = "";
+
+                if (selectedPartId) {
+                    (documentData.sections || []).some((section) =>
+                        (section.paragraphs || []).some((paragraph) =>
+                            (paragraph || []).some((part) => {
+                                if (part.id !== selectedPartId) {
+                                    return false;
+                                }
+
+                                selectedText = String(part.text || "");
+                                return true;
+                            })
+                        )
+                    );
+                }
+
+                if (selectedText && navigator.clipboard && typeof navigator.clipboard.writeText === "function") {
+                    try {
+                        await navigator.clipboard.writeText(selectedText);
+                        store.setSessionNote({
+                            step: "highlight-preview",
+                            tone: "info",
+                            title: "Trecho copiado",
+                            message: "O texto selecionado foi enviado para a area de transferencia."
+                        });
+                    } catch (error) {
+                        store.setSessionNote({
+                            step: "highlight-preview",
+                            tone: "premium",
+                            title: "Nao consegui copiar agora",
+                            message: "O navegador bloqueou a copia automatica deste trecho."
+                        });
+                    }
+                }
+                shouldPersist = true;
+                break;
+            }
             case "set-tab":
                 store.setBlockTab(payload.tabId);
                 shouldPersist = true;
@@ -830,26 +1706,59 @@ ${sections}`
                 shouldSyncNativeFullScreen = true;
                 break;
             case "start-premium-checkout": {
-                store.setSessionNote({
-                    step: store.getState().step,
-                    tone: "info",
-                    title: "Abrindo checkout seguro",
-                    message: "Estamos criando sua rota de pagamento no Mercado Pago."
-                });
-                this.render();
+                const plans = window.PremiumStudyBilling
+                    && typeof window.PremiumStudyBilling.getPlans === "function"
+                    ? window.PremiumStudyBilling.getPlans()
+                    : [];
+                const selectedPlanId = payload.itemValue || "premium_monthly";
+                const selectedPlan = plans.find((plan) => plan.id === selectedPlanId) || null;
 
+                if (
+                    window.RotaNotaCore
+                    && typeof window.RotaNotaCore.requireGoogleLogin === "function"
+                ) {
+                    const gate = window.RotaNotaCore.requireGoogleLogin({
+                        kind: "premium_checkout",
+                        source: "premium_checkout",
+                        planId: selectedPlanId,
+                        planLabel: selectedPlan && selectedPlan.label
+                            ? selectedPlan.label
+                            : ""
+                    });
+
+                    if (!gate || gate.allowed !== true) {
+                        shouldPersist = true;
+                        break;
+                    }
+                }
+
+                this.trackGrowth("checkout_click", {
+                    feature: store.getState().premiumOffer && store.getState().premiumOffer.feature,
+                    sourceStep: store.getState().returnStep,
+                    surface: "premium_checkout",
+                    planId: selectedPlanId,
+                    metadata: {
+                        promotionCampaignId: store.getState().premiumOffer && store.getState().premiumOffer.promotionCampaignId
+                            ? store.getState().premiumOffer.promotionCampaignId
+                            : ""
+                    }
+                });
                 const checkout = window.PremiumStudyBilling
-                    ? await window.PremiumStudyBilling.startCheckout(payload.itemValue || "premium_monthly", {
+                    ? await window.PremiumStudyBilling.startCheckout(selectedPlanId, {
                         feature: store.getState().premiumOffer && store.getState().premiumOffer.feature,
                         sourceStep: store.getState().returnStep
                     })
                     : { status: "not_configured", message: "Checkout real ainda nao foi conectado." };
 
+                if (checkout && checkout.ok && checkout.checkoutUrl) {
+                    return;
+                }
+
                 store.setSessionNote({
                     step: store.getState().step,
-                    tone: checkout.ok ? "info" : "premium",
-                    title: checkout.ok ? "Checkout iniciado" : "Pagamento ainda precisa ser conectado",
-                    message: checkout.message || "A proxima etapa liga este botao ao provedor real."
+                    tone: "premium",
+                    title: "Nao foi possivel abrir o checkout",
+                    message: checkout.message || "O checkout nao respondeu corretamente agora. Tente de novo em instantes."
                 });
                 shouldPersist = true;
                 break;
@@ -879,6 +1788,7 @@ ${sections}`
                 break;
             case "highlight-block":
                 store.selectBlock(payload.blockId);
+                await this.ensureMaterialTextExtracted();
                 store.openHighlightDocument(payload.blockId);
                 router.goTo("highlight-preview");
                 shouldPersist = true;
@@ -971,7 +1881,108 @@ ${sections}`
                 shouldPersist = true;
                 break;
             case "request-extra-mini-exam":
-                openPremiumOffer("MINI_EXAM_EXTRA");
+                if (!canUseFeature("MINI_EXAM_EXTRA")) {
+                    openPremiumOffer("MINI_EXAM_EXTRA");
+                    shouldPersist = true;
+                    break;
+                }
+                store.setSessionNote({
+                    step: "mini-exam",
+                    tone: "info",
+                    title: "Gerando mais 5 questoes",
+                    message: "A IA esta criando uma nova rodada premium para este bloco."
+                });
+                this.render();
+                {
+                    const block = store.getActiveBlock();
+                    const ai = window.PremiumStudyAI;
+                    const result = ai && typeof ai.request === "function"
+                        ? await ai.request(ai.TASKS.EXTRA_MINI_EXAM, {
+                            customerId: store.getState().customerId || "",
+                            materialHash: store.getState().materialHash || "",
+                            blockId: block.id,
+                            blockTitle: block.title,
+                            blockSummary: block.learn && block.learn.summary ? block.learn.summary : "",
+                            topics: block.topics || [],
+                            count: 5
+                        })
+                        : { ok: false, status: "ai_unavailable" };
+
+                    if (result && result.ok && result.questions) {
+                        store.appendMiniExamQuestions(block.id, result.questions);
+                        router.goTo("mini-exam");
+                    } else {
+                        store.setSessionNote({
+                            step: "mini-exam",
+                            tone: "premium",
+                            title: "Nao foi possivel gerar agora",
+                            message: "A IA nao retornou novas questoes. Tente novamente em alguns instantes."
+                        });
+                    }
+                }
+                shouldPersist = true;
+                break;
+            case "select-level-exam-count":
+                store.setLevelExamQuestionCount(Number(payload.itemValue));
+                shouldPersist = true;
+                break;
+            case "generate-level-exam":
+                if (!canUseFeature("LEVEL_EXAM")) {
+                    openPremiumOffer("LEVEL_EXAM");
+                    shouldPersist = true;
+                    break;
+                }
+                store.setSessionNote({
+                    step: "level-exam",
+                    tone: "info",
+                    title: "Gerando prova premium",
+                    message: "A IA esta montando uma prova de nivel com base na sua rota."
+                });
+                this.render();
+                {
+                    const levelExam = store.getState().levelExam || {};
+                    const ai = window.PremiumStudyAI;
+                    const result = ai && typeof ai.request === "function"
+                        ? await ai.request(ai.TASKS.PREMIUM_LEVEL_EXAM, {
+                            customerId: store.getState().customerId || "",
+                            materialHash: store.getState().materialHash || "",
+                            questionCount: levelExam.questionCount || 10,
+                            bundleSummary: this.buildBundleSummary()
+                        })
+                        : { ok: false, status: "ai_unavailable" };
+
+                    if (result && result.ok && result.questions) {
+                        store.setLevelExamQuestions({
+                            title: result.title,
+                            questions: result.questions
+                        });
+                        store.clearSessionNote();
+                    } else {
+                        store.setSessionNote({
+                            step: "level-exam",
+                            tone: "premium",
+                            title: "Nao foi possivel gerar a prova",
+                            message: "A IA nao retornou uma prova valida. Tente novamente em instantes."
+                        });
+                    }
+                }
+                router.goTo("level-exam");
+                shouldPersist = true;
+                break;
+            case "start-level-exam":
+                store.startLevelExam();
+                shouldPersist = true;
+                break;
+            case "answer-level-exam":
+                store.setLevelExamAnswer(Number(payload.answerIndex));
+                shouldPersist = true;
+                break;
+            case "continue-level-exam":
+                store.advanceLevelExam();
+                shouldPersist = true;
+                break;
+            case "finish-level-exam":
+                store.advanceLevelExam();
                 shouldPersist = true;
                 break;
             case "request-extra-quiz": {
@@ -1151,7 +2162,16 @@ ${sections}`
                 this.clearAnalysisTimers();
             }
 
-            if (step !== "block" || !window.PremiumStudyStore.getState().blockFullScreen) {
+            const state = window.PremiumStudyStore.getState();
+            const keepNativeFullScreen =
+                (step === "block" && state.blockFullScreen) ||
+                (
+                    step === "highlight-preview" &&
+                    state.highlightEditorOpen &&
+                    state.highlightEditorFullScreen
+                );
+
+            if (!keepNativeFullScreen) {
                 this.exitNativeFullScreen();
             }
         },
@@ -1163,7 +2183,15 @@ ${sections}`
 
             const state = window.PremiumStudyStore.getState();
             const step = state.step;
-            const meta = window.PremiumStudyRouter.getMeta(step);
+            const meta = {
+                ...window.PremiumStudyRouter.getMeta(step)
+            };
+
+            if (step === "highlight-preview" && state.highlightEditorOpen) {
+                meta.hideHeading = true;
+                meta.showKicker = false;
+            }
+
             const summary = meta.showSummary
                 ? window.PremiumStudyUI.summaryPanel(state, step === "mode-select" ? "compact" : "default")
                 : "";
diff --git a/premium-study/bootstrap/index.js b/premium-study/bootstrap/index.js
index 2eca760..016d541 100644
--- a/premium-study/bootstrap/index.js
+++ b/premium-study/bootstrap/index.js
@@ -10,6 +10,9 @@
         "premium-study/services/access-control.js",
         "premium-study/services/identity.js",
         "premium-study/services/pdf-validator.js",
+        "premium-study/services/pdf-text-extractor.js",
+        "premium-study/services/promotions.js",
+        "premium-study/services/growth.js",
         "premium-study/services/billing.js",
         "premium-study/services/account.js",
         "premium-study/services/ai.js",
@@ -19,6 +22,7 @@
         "premium-study/ui/views/index.js",
         "premium-study/app/index.js"
     ];
+    let dependenciesPromise = null;
 
     function ensureStyle() {
         if (document.getElementById(STYLE_ID)) {
@@ -59,13 +63,29 @@
         });
     }
 
-    window.PremiumStudyBootstrap = {
-        async init(options = {}) {
+    async function loadDependencies() {
+        if (dependenciesPromise) {
+            return dependenciesPromise;
+        }
+
+        dependenciesPromise = (async () => {
             ensureStyle();
 
             for (const dependency of dependencies) {
                 await loadScript(dependency);
             }
+        })();
+
+        return dependenciesPromise;
+    }
+
+    window.PremiumStudyBootstrap = {
+        async preload() {
+            return loadDependencies();
+        },
+
+        async init(options = {}) {
+            await loadDependencies();
 
             if (window.PremiumStudyApp && typeof window.PremiumStudyApp.init === "function") {
                 window.PremiumStudyApp.init(options);
diff --git a/premium-study/router/index.js b/premium-study/router/index.js
index f2ad811..4bf8aa5 100644
--- a/premium-study/router/index.js
+++ b/premium-study/router/index.js
@@ -19,6 +19,7 @@
         "flashcards",
         "mini-exam",
         "exam-result",
+        "level-exam",
         "premium-checkout",
         "premium-library",
         "trail"
@@ -26,9 +27,10 @@
 
     const STEP_META = {
         entry: {
-            title: "RotaNota transforma seu PDF em uma rota para a prova.",
-            subtitle: "Tudo começa no seu material. Depois disso, cada etapa ajusta a trilha ao seu prazo e à sua meta.",
-            label: "Entrada RotaNota",
+            title: "Seu PDF vira uma rota clara atÃ© a prova.",
+            subtitle: "Carregue o material e comece uma trilha montada para o seu objetivo.",
+            label: "Entrada premium",
+            showKicker: false,
             backVisible: false,
             progressVisible: false,
             showSummary: false
@@ -43,7 +45,7 @@
             showSummary: false
         },
         "target-score": {
-            title: "Qual nota você quer tirar?",
+            title: "Qual nota vocÃª quer tirar?",
             subtitle: "Defina a meta para que o plano ajuste foco, ritmo e profundidade.",
             label: "Meta de nota",
             backVisible: true,
@@ -52,16 +54,16 @@
             showSummary: false
         },
         "study-time": {
-            title: "Quanto tempo por dia você vai ter para estudar?",
+            title: "Quanto tempo por dia vocÃª vai ter para estudar?",
             subtitle: "Ajuste horas e minutos para que o plano respeite sua rotina real.",
-            label: "Tempo diário",
+            label: "Tempo diÃ¡rio",
             backVisible: true,
             progressVisible: true,
             progress: 78,
             showSummary: false
         },
         analysis: {
-            title: "RotaNota está montando o melhor caminho para você.",
+            title: "RotaNota estÃ¡ montando o melhor caminho para vocÃª.",
             subtitle: "Agora o sistema transforma suas escolhas em uma trilha mais objetiva e focada na sua meta.",
             label: "Processamento",
             backVisible: false,
@@ -69,8 +71,8 @@
             showSummary: false
         },
         "mode-select": {
-            title: "Como você quer começar agora?",
-            subtitle: "Escolha o jeito mais natural para entrar no conteúdo neste momento.",
+            title: "Como vocÃª quer comeÃ§ar agora?",
+            subtitle: "Escolha o jeito mais natural para entrar no conteÃºdo neste momento.",
             label: "Modo inicial",
             backVisible: true,
             progressVisible: false,
@@ -86,8 +88,8 @@
             scrollable: true
         },
         "learn-map": {
-            title: "Escolha o assunto por onde quer começar.",
-            subtitle: "Cada assunto abre um resumo focado no resultado e você pode avançar para o próximo direto de lá.",
+            title: "Escolha o assunto por onde quer comeÃ§ar.",
+            subtitle: "Cada assunto abre um resumo focado no resultado e vocÃª pode avanÃ§ar para o prÃ³ximo direto de lÃ¡.",
             label: "Aprender",
             backVisible: true,
             progressVisible: false,
@@ -105,18 +107,18 @@
             hideHeading: true
         },
         practice: {
-            title: "Como você quer praticar este bloco?",
+            title: "Como vocÃª quer praticar este bloco?",
             subtitle: "Escolha um formato curto para consolidar o que acabou de estudar.",
-            label: "Prática",
+            label: "PrÃ¡tica",
             backVisible: true,
             progressVisible: false,
             showSummary: false,
             scrollable: true
         },
         quiz: {
-            title: "Questionário orientado",
-            subtitle: "Uma questão por vez, com leitura limpa e correção objetiva.",
-            label: "Questionário",
+            title: "QuestionÃ¡rio orientado",
+            subtitle: "Uma questÃ£o por vez, com leitura limpa e correÃ§Ã£o objetiva.",
+            label: "QuestionÃ¡rio",
             backVisible: true,
             progressVisible: false,
             showSummary: false,
@@ -125,7 +127,7 @@
         },
         "true-false": {
             title: "Verdadeiro ou falso",
-            subtitle: "Teste formulações parecidas e veja onde a banca pode confundir você.",
+            subtitle: "Teste formulaÃ§Ãµes parecidas e veja onde a banca pode confundir vocÃª.",
             label: "V ou F",
             backVisible: true,
             progressVisible: false,
@@ -135,7 +137,7 @@
         },
         flashcards: {
             title: "Flashcards do bloco",
-            subtitle: "Revise termos e relações em ciclos curtos de memorização.",
+            subtitle: "Revise termos e relaÃ§Ãµes em ciclos curtos de memorizaÃ§Ã£o.",
             label: "Flashcards",
             backVisible: true,
             progressVisible: false,
@@ -171,6 +173,16 @@
             showSummary: false,
             scrollable: true
         },
+        "level-exam": {
+            title: "Prova de nivel premium",
+            subtitle: "Escolha a quantidade de questoes e meca sua prontidao geral no material.",
+            label: "Prova premium",
+            backVisible: true,
+            progressVisible: false,
+            showSummary: true,
+            scrollable: true,
+            hideHeading: true
+        },
         "premium-checkout": {
             title: "Plano premium",
             subtitle: "Libere continuidade, biblioteca e treinos extras sem poluir sua rotina de estudo.",
@@ -181,7 +193,7 @@
             scrollable: true
         },
         trail: {
-            title: "Sua trilha está pronta para continuar.",
+            title: "Sua trilha estÃ¡ pronta para continuar.",
             subtitle: "Veja os blocos, retome seu ponto atual e avance com clareza.",
             label: "Sua trilha",
             backVisible: true,
@@ -246,6 +258,10 @@
                 return this.goTo("mini-exam");
             }
 
+            if (step === "level-exam") {
+                return this.goTo("mode-select");
+            }
+
             if (step === "premium-checkout") {
                 return this.goTo(state.returnStep || "entry");
             }
diff --git a/premium-study/services/access-control.js b/premium-study/services/access-control.js
index 6df4781..430954d 100644
--- a/premium-study/services/access-control.js
+++ b/premium-study/services/access-control.js
@@ -20,6 +20,7 @@
         PREMIUM_LIBRARY: "premium_library",
         PRACTICE_EXTRA_SERIES: "practice_extra_series",
         MINI_EXAM_EXTRA: "mini_exam_extra",
+        LEVEL_EXAM: "level_exam",
         HIGHLIGHT_EXPORT: "highlight_export",
         STATS: "stats",
         CLOUD_SYNC: "cloud_sync"
@@ -32,7 +33,7 @@
             pdfPageLimit: 12,
             latestStudyResume: true,
             practiceSeriesLimit: 3,
-            miniExamBaseQuestions: 10,
+            miniExamBaseQuestions: 5,
             premiumLibrary: false,
             stats: false,
             cloudSync: false
@@ -43,7 +44,7 @@
             pdfPageLimit: Infinity,
             latestStudyResume: true,
             practiceSeriesLimit: Infinity,
-            miniExamBaseQuestions: 10,
+            miniExamBaseQuestions: 5,
             premiumLibrary: true,
             stats: true,
             cloudSync: true
@@ -67,6 +68,10 @@
             title: "Mini provas extras no premium",
             message: "A mini prova base continua grátis. No premium, você gera novas questões e variações para o mesmo assunto."
         },
+        [FEATURES.LEVEL_EXAM]: {
+            title: "Prova de nivel premium",
+            message: "A mini prova do bloco fica gratis. A prova de nivel com seletor de quantidade entra no premium."
+        },
         [FEATURES.HIGHLIGHT_EXPORT]: {
             title: "Exportação premium",
             message: "O marcador pode ser visualizado. Baixar PDF com destaques fica no premium."
@@ -89,6 +94,7 @@
         "V/F extras para pegar pegadinhas",
         "Flashcards extras com mnemônicos",
         "Mini provas extras por assunto",
+        "Prova de nivel com quantidade flexivel",
         "Exportação dos marcadores em PDF"
     ];
 
@@ -120,6 +126,15 @@
             ],
             cta: "Liberar mini provas extras"
         },
+        [FEATURES.LEVEL_EXAM]: {
+            eyebrow: "Prova premium",
+            title: "Meça seu nivel com mais profundidade.",
+            lead: "Escolha 10, 20 ou 30 questoes para validar se o material ja esta pronto para prova.",
+            benefits: [
+                "Teste de nivel com seletor"
+            ],
+            cta: "Liberar prova premium"
+        },
         [FEATURES.HIGHLIGHT_EXPORT]: {
             eyebrow: "Marcador premium",
             title: "Leve seus destaques com você.",
@@ -192,6 +207,7 @@
         case FEATURES.PREMIUM_LIBRARY:
         case FEATURES.PRACTICE_EXTRA_SERIES:
         case FEATURES.MINI_EXAM_EXTRA:
+        case FEATURES.LEVEL_EXAM:
         case FEATURES.HIGHLIGHT_EXPORT:
         case FEATURES.STATS:
         case FEATURES.CLOUD_SYNC:
diff --git a/premium-study/services/account.js b/premium-study/services/account.js
index 02ec1a9..007050e 100644
--- a/premium-study/services/account.js
+++ b/premium-study/services/account.js
@@ -5,13 +5,21 @@
 
     const STATUS_ENDPOINT = "/api/premium/status";
 
-    async function refreshStatus() {
+    async function refreshStatus(options = {}) {
         const identity = window.PremiumStudyIdentity;
+        const auth = window.RotaNotaAuth;
+        const session = auth && typeof auth.getSession === "function"
+            ? auth.getSession()
+            : null;
         const customerId = identity && typeof identity.getCustomerId === "function"
             ? identity.getCustomerId()
             : "";
+        const paymentId = String(options.paymentId || "").trim();
+        const userId = session && session.userId
+            ? session.userId
+            : "";
 
-        if (!customerId) {
+        if (!customerId && !paymentId && !userId) {
             return {
                 ok: false,
                 accessTier: "free",
@@ -24,12 +32,27 @@
         let payload;
 
         try {
-            response = await fetch(`${STATUS_ENDPOINT}?customerId=${encodeURIComponent(customerId)}`);
+            const params = new URLSearchParams();
+
+            if (customerId) {
+                params.set("customerId", customerId);
+            }
+
+            if (userId) {
+                params.set("userId", userId);
+            }
+
+            if (paymentId) {
+                params.set("paymentId", paymentId);
+            }
+
+            response = await fetch(`${STATUS_ENDPOINT}?${params.toString()}`);
             payload = await response.json();
         } catch (error) {
             return {
                 ok: false,
                 customerId,
+                userId,
                 accessTier: "free",
                 subscriptionStatus: "status_endpoint_unavailable",
                 premiumActive: false
@@ -40,6 +63,7 @@
             return {
                 ok: false,
                 customerId,
+                userId,
                 accessTier: "free",
                 subscriptionStatus: payload && payload.subscriptionStatus
                     ? payload.subscriptionStatus
@@ -53,6 +77,10 @@
 
     function applyStatusToStore(status) {
         const store = window.PremiumStudyStore;
+        const auth = window.RotaNotaAuth;
+        const session = auth && typeof auth.getSession === "function"
+            ? auth.getSession()
+            : null;
 
         if (!store || !status) {
             return status;
@@ -60,17 +88,34 @@
 
         store.patch({
             customerId: status.customerId || "",
+            accountUser: status.user || (session
+                ? {
+                    userId: session.userId,
+                    email: session.email || "",
+                    name: session.name || "",
+                    picture: session.picture || ""
+                }
+                : null),
+            accountAuthenticated: Boolean(status.authenticated || status.userId),
             accessTier: status.accessTier === "premium" ? "premium" : "free",
             subscriptionStatus: status.subscriptionStatus || "registered_free",
             premiumEntitlement: status.entitlement || null,
-            premiumStatusConfigured: Boolean(status.configured)
+            premiumStatusConfigured: Boolean(status.configured),
+            generationPaused: Boolean(status.generationPaused),
+            opsLanes: status.lanes && typeof status.lanes === "object"
+                ? status.lanes
+                : store.getState().opsLanes,
+            opsThresholds: status.opsThresholds && typeof status.opsThresholds === "object"
+                ? status.opsThresholds
+                : store.getState().opsThresholds,
+            trialState: status.trialState || null
         });
 
         return status;
     }
 
-    async function refreshAndApply() {
-        const status = await refreshStatus();
+    async function refreshAndApply(options = {}) {
+        const status = await refreshStatus(options);
         applyStatusToStore(status);
         return status;
     }
diff --git a/premium-study/services/ai.js b/premium-study/services/ai.js
index 6fc06ac..81d426d 100644
--- a/premium-study/services/ai.js
+++ b/premium-study/services/ai.js
@@ -3,19 +3,30 @@
         return;
     }
 
-    const PROMPT_VERSION = "premium-study-ai-v1";
+    const PROMPT_VERSION = "rotanota-pdf-focused-ai-v1";
+    const ENDPOINT = "/api/premium/ai-generate";
+    const state = {
+        configured: null,
+        model: ""
+    };
 
     const TASKS = {
+        FREE_BUNDLE_FROM_MATERIAL: "free_bundle_from_material",
         PLAN_FROM_MATERIAL: "plan_from_material",
         EXPLAIN_BLOCK: "explain_block",
         QUICK_REVIEW: "quick_review",
         EXTRA_QUIZ: "extra_quiz",
         EXTRA_TRUE_FALSE: "extra_true_false",
         EXTRA_FLASHCARDS: "extra_flashcards",
-        EXTRA_MINI_EXAM: "extra_mini_exam"
+        EXTRA_MINI_EXAM: "extra_mini_exam",
+        PREMIUM_LEVEL_EXAM: "premium_level_exam"
     };
 
     const TASK_CONTRACTS = {
+        [TASKS.FREE_BUNDLE_FROM_MATERIAL]: {
+            prompt: "api/premium/ai-generate.js",
+            cacheKeyParts: ["materialHash", "examDate", "targetScore", "dailyMinutes"]
+        },
         [TASKS.PLAN_FROM_MATERIAL]: {
             prompt: "premium-study/services/ai/prompts/plan.md",
             cacheKeyParts: ["materialHash", "examDate", "targetScore", "dailyMinutes"]
@@ -43,11 +54,15 @@
         [TASKS.EXTRA_MINI_EXAM]: {
             prompt: "premium-study/services/ai/prompts/mini_exam.md",
             cacheKeyParts: ["materialHash", "blockId", "count"]
+        },
+        [TASKS.PREMIUM_LEVEL_EXAM]: {
+            prompt: "api/premium/ai-generate.js",
+            cacheKeyParts: ["materialHash", "questionCount"]
         }
     };
 
     function isConfigured() {
-        return false;
+        return state.configured !== false;
     }
 
     function getTaskContract(task) {
@@ -68,16 +83,65 @@
 
     async function request(task, payload = {}) {
         const contract = getTaskContract(task);
+        const normalizedTask = task === TASKS.PLAN_FROM_MATERIAL
+            ? TASKS.FREE_BUNDLE_FROM_MATERIAL
+            : task;
+
+        try {
+            const response = await fetch(ENDPOINT, {
+                method: "POST",
+                headers: {
+                    "Content-Type": "application/json"
+                },
+                body: JSON.stringify({
+                    ...payload,
+                    task: normalizedTask
+                })
+            });
+            const data = await response.json().catch(() => null);
+
+            return {
+                ok: Boolean(response.ok && data && data.ok),
+                status: data && data.status ? data.status : response.ok ? "ok" : "request_failed",
+                task: normalizedTask,
+                promptVersion: data && data.promptVersion ? data.promptVersion : PROMPT_VERSION,
+                cacheKey: buildCacheKey(normalizedTask, payload),
+                contract,
+                ...data
+            };
+        } catch (error) {
+            state.configured = false;
+            return {
+                ok: false,
+                status: "network_error",
+                task: normalizedTask,
+                promptVersion: PROMPT_VERSION,
+                cacheKey: buildCacheKey(normalizedTask, payload),
+                contract,
+                message: "Nao consegui acessar a IA agora. Vou manter o pacote base local para voce seguir estudando."
+            };
+        }
+    }
+
+    function syncConfiguration(data = {}) {
+        if (typeof data.aiAvailable === "boolean") {
+            state.configured = data.aiAvailable;
+        } else if (typeof data.ok === "boolean") {
+            state.configured = data.ok;
+        }
+
+        if (data.model) {
+            state.model = data.model;
+        } else if (data.aiModel) {
+            state.model = data.aiModel;
+        }
+    }
 
-        return {
-            ok: false,
-            status: "not_configured",
-            task,
-            promptVersion: PROMPT_VERSION,
-            cacheKey: buildCacheKey(task, payload),
-            contract,
-            message: "Cliente de IA real ainda nao foi conectado. Use este contrato para ligar backend, cache e prompts versionados."
-        };
+    const originalRequest = request;
+    async function trackedRequest(task, payload = {}) {
+        const result = await originalRequest(task, payload);
+        syncConfiguration(result);
+        return result;
     }
 
     window.PremiumStudyAI = {
@@ -87,6 +151,6 @@
         isConfigured,
         getTaskContract,
         buildCacheKey,
-        request
+        request: trackedRequest
     };
 })();
diff --git a/premium-study/services/billing.js b/premium-study/services/billing.js
index a387e65..5c3a826 100644
--- a/premium-study/services/billing.js
+++ b/premium-study/services/billing.js
@@ -11,26 +11,27 @@
     };
 
     const CHECKOUT_ENDPOINT = "/api/mercado-pago/checkout";
+    const CHECKOUT_CONTEXT_KEY = "rotanota-premium-checkout-context";
 
     const PLANS = [
         {
             id: "premium_monthly",
             tier: "premium",
             label: "Premium mensal",
-            priceLabel: "Mensal",
+            priceLabel: "R$ 19,90",
             interval: "month",
-            description: "Continuidade, biblioteca e extras com renovacao mensal.",
-            recommended: true,
+            description: "Entrada mais leve para liberar biblioteca, continuidade e extras.",
+            recommended: false,
             checkoutProvider: "mercado_pago"
         },
         {
             id: "premium_annual",
             tier: "premium",
             label: "Premium anual",
-            priceLabel: "Anual",
+            priceLabel: "R$ 149,90",
             interval: "year",
-            description: "Melhor para concurso, materiais longos e rotina de meses.",
-            recommended: false,
+            description: "Melhor escolha para rotina longa, apostilas grandes e preparo continuo.",
+            recommended: true,
             checkoutProvider: "mercado_pago"
         }
     ];
@@ -43,13 +44,64 @@
         return getPlans().find((plan) => plan.recommended) || getPlans()[0];
     }
 
+    function buildCheckoutReturnSnapshot(checkoutContext = {}) {
+        const store = window.PremiumStudyStore;
+
+        if (!store || typeof store.exportSnapshot !== "function") {
+            return null;
+        }
+
+        const snapshot = store.exportSnapshot();
+
+        if (!snapshot || !snapshot.materialName) {
+            return null;
+        }
+
+        const resumeStep = snapshot.step === "premium-checkout" || snapshot.step === "premium-library"
+            ? (snapshot.returnStep || checkoutContext.sourceStep || "mode-select")
+            : snapshot.step;
+
+        return {
+            ...snapshot,
+            step: resumeStep || "mode-select",
+            returnStep: snapshot.returnStep || checkoutContext.sourceStep || "mode-select",
+            premiumOffer: null
+        };
+    }
+
+    function persistCheckoutReturnContext(plan, checkoutContext = {}) {
+        if (!window.sessionStorage) {
+            return;
+        }
+
+        try {
+            window.sessionStorage.setItem(CHECKOUT_CONTEXT_KEY, JSON.stringify({
+                storedAt: new Date().toISOString(),
+                planId: plan && plan.id ? plan.id : "",
+                sourceStep: checkoutContext.sourceStep || "",
+                feature: checkoutContext.feature || "",
+                snapshot: buildCheckoutReturnSnapshot(checkoutContext)
+            }));
+        } catch (error) {
+            // Se o navegador bloquear sessionStorage, o fluxo segue com fallback para a tela base.
+        }
+    }
+
     async function startCheckout(planId, context = {}) {
         const plan = PLANS.find((item) => item.id === planId) || getRecommendedPlan();
         const identityContext = window.PremiumStudyIdentity && typeof window.PremiumStudyIdentity.getCheckoutContext === "function"
             ? window.PremiumStudyIdentity.getCheckoutContext()
             : {};
+        const promotionContext = window.PremiumStudyPromotions && typeof window.PremiumStudyPromotions.getCheckoutContext === "function"
+            ? window.PremiumStudyPromotions.getCheckoutContext(context.feature || "", context.surface || "premium_checkout")
+            : {};
+        const acquisitionContext = window.PremiumStudyGrowth && typeof window.PremiumStudyGrowth.getAcquisitionContext === "function"
+            ? window.PremiumStudyGrowth.getAcquisitionContext()
+            : {};
         const checkoutContext = {
             ...context,
+            ...acquisitionContext,
+            ...promotionContext,
             ...identityContext
         };
 
@@ -97,6 +149,7 @@
         }
 
         if (payload.checkoutUrl) {
+            persistCheckoutReturnContext(plan, checkoutContext);
             window.location.assign(payload.checkoutUrl);
         }
 
diff --git a/premium-study/services/identity.js b/premium-study/services/identity.js
index 0828e43..cb52f64 100644
--- a/premium-study/services/identity.js
+++ b/premium-study/services/identity.js
@@ -29,8 +29,17 @@
     }
 
     function getCheckoutContext() {
+        const auth = window.RotaNotaAuth;
+        const session = auth && typeof auth.getSession === "function"
+            ? auth.getSession()
+            : null;
+
         return {
-            customerId: getCustomerId()
+            customerId: getCustomerId(),
+            userId: session && session.userId
+                ? session.userId
+                : "",
+            authenticated: Boolean(session && session.userId)
         };
     }
 
diff --git a/premium-study/state/store.js b/premium-study/state/store.js
index b16e884..192c6c9 100644
--- a/premium-study/state/store.js
+++ b/premium-study/state/store.js
@@ -7,6 +7,8 @@
         return JSON.parse(JSON.stringify(value));
     }
 
+    const DEFAULT_HIGHLIGHT_COLOR = "#fde68a";
+
     function buildStudyTitle(materialName) {
         if (!materialName) {
             return "Estudo personalizado";
@@ -27,6 +29,21 @@
         return { front, back, tip };
     }
 
+    function createLevelExamState(overrides = {}) {
+        return {
+            questionCount: 10,
+            title: "Prova de nivel RotaNota",
+            questions: [],
+            started: false,
+            index: 0,
+            answers: [],
+            isComplete: false,
+            result: null,
+            status: "idle",
+            ...overrides
+        };
+    }
+
     function buildExamPack(material, descriptors) {
         const [first, second, third, fourth] = descriptors;
 
@@ -36,33 +53,33 @@
                 [
                     "Pular as definicoes centrais",
                     `Mapear ${first} antes de decorar detalhes`,
-                    "Começar apenas por exemplos isolados",
+                    "ComeÃ§ar apenas por exemplos isolados",
                     "Ir direto para excecoes"
                 ],
                 1,
                 `O ponto de partida mais forte e entender ${first} antes de aprofundar o resto.`
             ),
             buildQuestion(
-                `O que mais ajuda a não confundir ${first} com aplicação prática?`,
+                `O que mais ajuda a nÃ£o confundir ${first} com aplicaÃ§Ã£o prÃ¡tica?`,
                 [
-                    "Separar conceito, critério e exemplo",
+                    "Separar conceito, critÃ©rio e exemplo",
                     "Ler so a conclusao",
                     "Decorar uma frase solta",
                     "Ignorar a linguagem da banca"
                 ],
                 0,
-                "Separar conceito, critério e exemplo evita trocas típicas de prova."
+                "Separar conceito, critÃ©rio e exemplo evita trocas tÃ­picas de prova."
             ),
             buildQuestion(
-                `Quando o enunciado cobra ${second}, o que você deve procurar primeiro?`,
+                `Quando o enunciado cobra ${second}, o que vocÃª deve procurar primeiro?`,
                 [
-                    "A palavra que delimita o critério",
+                    "A palavra que delimita o critÃ©rio",
                     "O exemplo mais bonito",
                     "A alternativa mais longa",
                     "A resposta que parece familiar"
                 ],
                 0,
-                `Em questões de ${second}, a palavra que limita o critério costuma decidir a alternativa.`
+                `Em questÃµes de ${second}, a palavra que limita o critÃ©rio costuma decidir a alternativa.`
             ),
             buildQuestion(
                 `Qual erro mais comum ao revisar ${third}?`,
@@ -84,18 +101,18 @@
                     "Decorando so o titulo"
                 ],
                 1,
-                "Uma relação mnemônica curta ajuda a recuperar o conteúdo sob pressão."
+                "Uma relaÃ§Ã£o mnemÃ´nica curta ajuda a recuperar o conteÃºdo sob pressÃ£o."
             ),
             buildQuestion(
                 "Qual sequencia de revisao reduz mais a dispersao?",
                 [
-                    "Resumo, critério, prática",
+                    "Resumo, critÃ©rio, prÃ¡tica",
                     "Pratica, titulo, acaso",
                     "Excecao, detalhe, exemplo",
                     "Mini prova, sem resumo"
                 ],
                 0,
-                "Resumo, critério e prática formam a trilha mais limpa para consolidar o bloco."
+                "Resumo, critÃ©rio e prÃ¡tica formam a trilha mais limpa para consolidar o bloco."
             ),
             buildQuestion(
                 "Em alternativas muito parecidas, o que mais protege seu resultado?",
@@ -114,32 +131,32 @@
                     "Quando o erro mostra duvida de base",
                     "So quando faltar tempo",
                     "Depois de decorar tudo",
-                    "Nunca, porque prática basta"
+                    "Nunca, porque prÃ¡tica basta"
                 ],
                 0,
-                "Se o erro foi de base, o resumo focado recompõe o mapa mental do assunto."
+                "Se o erro foi de base, o resumo focado recompÃµe o mapa mental do assunto."
             ),
             buildQuestion(
-                "Qual sinal indica que você entendeu o assunto, e não só decorou?",
+                "Qual sinal indica que vocÃª entendeu o assunto, e nÃ£o sÃ³ decorou?",
                 [
-                    "Consegue explicar o critério sem depender do exemplo",
+                    "Consegue explicar o critÃ©rio sem depender do exemplo",
                     "Reconhece a fonte do PDF",
                     "Lembra a cor do card",
                     "Responde so pelo tema geral"
                 ],
                 0,
-                "Entendimento real aparece quando o critério se sustenta sem depender do exemplo."
+                "Entendimento real aparece quando o critÃ©rio se sustenta sem depender do exemplo."
             ),
             buildQuestion(
                 "Qual proximo passo faz mais sentido depois de consolidar este assunto?",
                 [
-                    "Ir para o próximo assunto ou para a prática",
+                    "Ir para o prÃ³ximo assunto ou para a prÃ¡tica",
                     "Reiniciar o onboarding",
                     "Apagar o progresso",
                     "Fechar sem salvar"
                 ],
                 0,
-                "Depois de consolidar um assunto, faz sentido praticar ou seguir para o próximo bloco."
+                "Depois de consolidar um assunto, faz sentido praticar ou seguir para o prÃ³ximo bloco."
             )
         ];
     }
@@ -154,10 +171,10 @@
                 subtitle: "Comece pelo que mais organiza o entendimento e reduz dispersao.",
                 duration: "34 min",
                 status: "recommended",
-                excerpt: "Termos centrais, definições e relações que abrem o restante do conteúdo.",
+                excerpt: "Termos centrais, definiÃ§Ãµes e relaÃ§Ãµes que abrem o restante do conteÃºdo.",
                 topics: [
                     "conceitos centrais do material",
-                    "critérios e definições chave",
+                    "critÃ©rios e definiÃ§Ãµes chave",
                     "linguagem que mais aparece na prova"
                 ],
                 progress: {
@@ -166,7 +183,7 @@
                     exam: false
                 },
                 learn: {
-                    summary: `Este bloco isola o núcleo de ${material}, priorizando termos, definições e relações que ajudam você a entrar no conteúdo sem desperdiçar tempo.`,
+                    summary: `Este bloco isola o nÃºcleo de ${material}, priorizando termos, definiÃ§Ãµes e relaÃ§Ãµes que ajudam vocÃª a entrar no conteÃºdo sem desperdiÃ§ar tempo.`,
                     hotPoints: [
                         "Entender o vocabulario principal antes de memorizar detalhes.",
                         "Separar o que e regra, o que e excecao e o que e exemplo.",
@@ -174,14 +191,14 @@
                     ],
                     keyConcepts: [
                         "conceito central",
-                        "critério de classificação",
+                        "critÃ©rio de classificaÃ§Ã£o",
                         "estrutura basica",
                         "aplicacao mais cobrada"
                     ],
                     pitfalls: [
                         "Trocar definicao por exemplo pratico.",
                         "Memorizar nomes sem entender relacao entre eles.",
-                        "Ignorar o critério que diferencia itens parecidos."
+                        "Ignorar o critÃ©rio que diferencia itens parecidos."
                     ]
                 },
                 practice: {
@@ -200,7 +217,7 @@
                                 "Pular os termos principais"
                             ],
                             1,
-                            "O primeiro ganho real vem de organizar linguagem, critério e definições do bloco."
+                            "O primeiro ganho real vem de organizar linguagem, critÃ©rio e definiÃ§Ãµes do bloco."
                         ),
                         buildQuestion(
                             "Ao revisar um conceito central, o que mais ajuda a evitar erro de prova?",
@@ -222,7 +239,7 @@
                                 "Pular o resumo principal"
                             ],
                             1,
-                            "Marcar relações entre conceitos transforma o conteúdo em estrutura, não em lista solta."
+                            "Marcar relaÃ§Ãµes entre conceitos transforma o conteÃºdo em estrutura, nÃ£o em lista solta."
                         )
                     ],
                     quizSeries: [
@@ -236,7 +253,7 @@
                                     "Pular os termos principais"
                                 ],
                                 1,
-                                "O primeiro ganho real vem de organizar linguagem, critério e definições do bloco."
+                                "O primeiro ganho real vem de organizar linguagem, critÃ©rio e definiÃ§Ãµes do bloco."
                             ),
                             buildQuestion(
                                 "Ao revisar um conceito central, o que mais ajuda a evitar erro de prova?",
@@ -258,20 +275,20 @@
                                     "Pular o resumo principal"
                                 ],
                                 1,
-                                "Marcar relações entre conceitos transforma o conteúdo em estrutura, não em lista solta."
+                                "Marcar relaÃ§Ãµes entre conceitos transforma o conteÃºdo em estrutura, nÃ£o em lista solta."
                             )
                         ],
                         [
                             buildQuestion(
-                                "Qual ganho aparece quando você entende o critério principal do bloco?",
+                                "Qual ganho aparece quando vocÃª entende o critÃ©rio principal do bloco?",
                                 [
                                     "A leitura fica mais organizada",
                                     "Os exemplos deixam de importar",
-                                    "A prática pode ser ignorada",
+                                    "A prÃ¡tica pode ser ignorada",
                                     "Toda excecao vira regra"
                                 ],
                                 0,
-                                "Com critério claro, o conteúdo deixa de parecer uma lista solta e passa a ter eixo."
+                                "Com critÃ©rio claro, o conteÃºdo deixa de parecer uma lista solta e passa a ter eixo."
                             ),
                             buildQuestion(
                                 "O que mais protege contra erro por semelhanca superficial?",
@@ -293,20 +310,20 @@
                                     "Nunca, porque questao basta"
                                 ],
                                 0,
-                                "Se a duvida e estrutural, o resumo base recompõe o mapa do assunto."
+                                "Se a duvida e estrutural, o resumo base recompÃµe o mapa do assunto."
                             )
                         ],
                         [
                             buildQuestion(
                                 "Qual sequencia consolida melhor o bloco?",
                                 [
-                                    "Resumo, critério, prática",
+                                    "Resumo, critÃ©rio, prÃ¡tica",
                                     "Titulo, exemplo, chute",
                                     "Excecao, detalhe, pressa",
                                     "Mini prova sem revisao"
                                 ],
                                 0,
-                                "Resumo, critério e prática formam a trilha mais limpa para o assunto."
+                                "Resumo, critÃ©rio e prÃ¡tica formam a trilha mais limpa para o assunto."
                             ),
                             buildQuestion(
                                 "O que mostra que o conceito foi realmente entendido?",
@@ -325,10 +342,10 @@
                                     "Responder pelo tema geral da alternativa",
                                     "Separar regra e excecao",
                                     "Comparar item por item",
-                                    "Voltar ao critério"
+                                    "Voltar ao critÃ©rio"
                                 ],
                                 0,
-                                "Quem leu superficialmente tende a marcar pela sensação geral, não pelo critério."
+                                "Quem leu superficialmente tende a marcar pela sensaÃ§Ã£o geral, nÃ£o pelo critÃ©rio."
                             )
                         ]
                     ],
@@ -339,14 +356,14 @@
                             "Separar conceito de exemplo evita trocas comuns em prova."
                         ),
                         buildTrueFalse(
-                            "A linguagem principal do material deve ser revisada antes da prática intensa.",
+                            "A linguagem principal do material deve ser revisada antes da prÃ¡tica intensa.",
                             true,
                             "Entender a linguagem do bloco acelera o restante da trilha."
                         ),
                         buildTrueFalse(
-                            "Ignorar critérios de classificação reduz o risco de erro em alternativas parecidas.",
+                            "Ignorar critÃ©rios de classificaÃ§Ã£o reduz o risco de erro em alternativas parecidas.",
                             false,
-                            "É justamente o critério que diferencia opções muito próximas."
+                            "Ã‰ justamente o critÃ©rio que diferencia opÃ§Ãµes muito prÃ³ximas."
                         )
                     ],
                     trueFalseSeries: [
@@ -357,19 +374,19 @@
                                 "Separar conceito de exemplo evita trocas comuns em prova."
                             ),
                             buildTrueFalse(
-                                "A linguagem principal do material deve ser revisada antes da prática intensa.",
+                                "A linguagem principal do material deve ser revisada antes da prÃ¡tica intensa.",
                                 true,
                                 "Entender a linguagem do bloco acelera o restante da trilha."
                             ),
                             buildTrueFalse(
-                                "Ignorar critérios de classificação reduz o risco de erro em alternativas parecidas.",
+                                "Ignorar critÃ©rios de classificaÃ§Ã£o reduz o risco de erro em alternativas parecidas.",
                                 false,
-                                "É justamente o critério que diferencia opções muito próximas."
+                                "Ã‰ justamente o critÃ©rio que diferencia opÃ§Ãµes muito prÃ³ximas."
                             )
                         ],
                         [
                             buildTrueFalse(
-                                "O vocabulário principal do tema pode ficar para depois, desde que você memorize os nomes.",
+                                "O vocabulÃ¡rio principal do tema pode ficar para depois, desde que vocÃª memorize os nomes.",
                                 false,
                                 "Sem entender a linguagem principal, a leitura perde eixo e a memorizacao quebra rapido."
                             ),
@@ -379,9 +396,9 @@
                                 "Essa separacao evita que elementos parecidos sejam tratados como equivalentes."
                             ),
                             buildTrueFalse(
-                                "Quando dois itens parecem próximos, o critério de classificação deixa de ser importante.",
+                                "Quando dois itens parecem prÃ³ximos, o critÃ©rio de classificaÃ§Ã£o deixa de ser importante.",
                                 false,
-                                "É justamente o critério que mostra onde os itens se afastam."
+                                "Ã‰ justamente o critÃ©rio que mostra onde os itens se afastam."
                             )
                         ],
                         [
@@ -393,12 +410,12 @@
                             buildTrueFalse(
                                 "Se um caso aparece muito no material, ele automaticamente vira a regra principal.",
                                 false,
-                                "Frequência não substitui critério; um caso recorrente ainda pode ser só exemplo."
+                                "FrequÃªncia nÃ£o substitui critÃ©rio; um caso recorrente ainda pode ser sÃ³ exemplo."
                             ),
                             buildTrueFalse(
                                 "Revisar a estrutura basica do tema ajuda a reduzir resposta por semelhanca superficial.",
                                 true,
-                                "Estrutura clara faz o aluno decidir por critério, não por impressão."
+                                "Estrutura clara faz o aluno decidir por critÃ©rio, nÃ£o por impressÃ£o."
                             )
                         ]
                     ],
@@ -411,7 +428,7 @@
                         buildFlashcard(
                             "Pergunta gatilho",
                             "O que define este conceito?",
-                            "Se a resposta virar um exemplo, você ainda não fixou o conceito."
+                            "Se a resposta virar um exemplo, vocÃª ainda nÃ£o fixou o conceito."
                         ),
                         buildFlashcard(
                             "Qual leitura gera mais resultado?",
@@ -430,7 +447,7 @@
                             buildFlashcard(
                                 "Pergunta gatilho",
                                 "O que define este conceito?",
-                                "Se a resposta virar um exemplo, você ainda não fixou o conceito."
+                                "Se a resposta virar um exemplo, vocÃª ainda nÃ£o fixou o conceito."
                             ),
                             buildFlashcard(
                                 "Qual leitura gera mais resultado?",
@@ -441,8 +458,8 @@
                         [
                             buildFlashcard(
                                 "Base antes do detalhe",
-                                "Primeiro critério, depois exemplo.",
-                                "Sem critério, o detalhe parece importante demais."
+                                "Primeiro critÃ©rio, depois exemplo.",
+                                "Sem critÃ©rio, o detalhe parece importante demais."
                             ),
                             buildFlashcard(
                                 "Erro classico",
@@ -464,7 +481,7 @@
                             buildFlashcard(
                                 "Foco da banca",
                                 "Criterio decide mais que tema.",
-                                "Muitas alternativas parecem certas até o critério entrar."
+                                "Muitas alternativas parecem certas atÃ© o critÃ©rio entrar."
                             ),
                             buildFlashcard(
                                 "Pergunta final",
@@ -475,19 +492,19 @@
                     ]
                 },
                 exam: {
-                    baseCount: 10,
+                    baseCount: 5,
                     questions: buildExamPack(material, [
                         "a linguagem central",
-                        "o critério principal",
+                        "o critÃ©rio principal",
                         "relacoes entre conceitos",
                         "a memorizacao do bloco"
-                    ])
+                    ]).slice(0, 5)
                 }
             },
             {
                 id: "block-2",
                 title: "Consolidacao objetiva",
-                subtitle: "Aperte a retenção do que mais diferencia desempenho em questões.",
+                subtitle: "Aperte a retenÃ§Ã£o do que mais diferencia desempenho em questÃµes.",
                 duration: "22 min",
                 status: "ready",
                 excerpt: "Comparacoes, excecoes e palavras-chave que costumam separar acerto de erro.",
@@ -531,12 +548,12 @@
                             "O que mais ajuda a acertar itens muito parecidos?",
                             [
                                 "Responder rapido",
-                                "Comparar palavra-chave e critério",
+                                "Comparar palavra-chave e critÃ©rio",
                                 "Ignorar excecoes",
                                 "Confiar apenas no instinto"
                             ],
                             1,
-                            "Itens parecidos se resolvem por palavra-chave e critério, não por impressão geral."
+                            "Itens parecidos se resolvem por palavra-chave e critÃ©rio, nÃ£o por impressÃ£o geral."
                         ),
                         buildQuestion(
                             "Como a banca costuma montar pegadinhas?",
@@ -567,12 +584,12 @@
                                 "O que mais ajuda a acertar itens muito parecidos?",
                                 [
                                     "Responder rapido",
-                                    "Comparar palavra-chave e critério",
+                                    "Comparar palavra-chave e critÃ©rio",
                                     "Ignorar excecoes",
                                     "Confiar apenas no instinto"
                                 ],
                                 1,
-                                "Itens parecidos se resolvem por palavra-chave e critério, não por impressão geral."
+                                "Itens parecidos se resolvem por palavra-chave e critÃ©rio, nÃ£o por impressÃ£o geral."
                             ),
                             buildQuestion(
                                 "Como a banca costuma montar pegadinhas?",
@@ -638,7 +655,7 @@
                                 [
                                     "Saber o tema geral",
                                     "Justificar por que a quase correta ainda esta errada",
-                                    "Lembrar a ordem das páginas",
+                                    "Lembrar a ordem das pÃ¡ginas",
                                     "Responder sempre mais rapido"
                                 ],
                                 1,
@@ -661,7 +678,7 @@
                                     "Passar o olho so nos titulos",
                                     "Voltar aos contrastes e excecoes principais",
                                     "Ignorar palavras-chave",
-                                    "Trocar critério por exemplo"
+                                    "Trocar critÃ©rio por exemplo"
                                 ],
                                 1,
                                 "Antes da mini prova, revisar contraste e excecao limpa o ponto mais sensivel do bloco."
@@ -717,7 +734,7 @@
                             buildTrueFalse(
                                 "Separar regra e excecao visualmente reduz resposta por automatismo.",
                                 true,
-                                "Essa divisão deixa o critério mais limpo no momento da prova."
+                                "Essa divisÃ£o deixa o critÃ©rio mais limpo no momento da prova."
                             )
                         ],
                         [
@@ -727,12 +744,12 @@
                                 "Quando ficam grudadas, a tendencia e embaralhar as duas."
                             ),
                             buildTrueFalse(
-                                "O erro por semelhança superficial costuma cair quando você compara item por item.",
+                                "O erro por semelhanÃ§a superficial costuma cair quando vocÃª compara item por item.",
                                 true,
                                 "Comparacao deliberada enfraquece o impulso de responder pela primeira impressao."
                             ),
                             buildTrueFalse(
-                                "Pegadinha boa troca o critério em um detalhe pequeno, não necessariamente o tema inteiro.",
+                                "Pegadinha boa troca o critÃ©rio em um detalhe pequeno, nÃ£o necessariamente o tema inteiro.",
                                 true,
                                 "A banca costuma deslocar o sentido por uma palavra, limite ou excecao."
                             )
@@ -782,13 +799,13 @@
                             ),
                             buildFlashcard(
                                 "Erro fino",
-                                "Tema certo, critério errado.",
+                                "Tema certo, critÃ©rio errado.",
                                 "Muita alternativa quase correta cai nessa armadilha."
                             ),
                             buildFlashcard(
                                 "Excecao segura",
                                 "So vale com a condicao junto.",
-                                "Sem a condição, você provavelmente voltou para a regra."
+                                "Sem a condiÃ§Ã£o, vocÃª provavelmente voltou para a regra."
                             )
                         ],
                         [
@@ -811,31 +828,419 @@
                     ]
                 },
                 exam: {
-                    baseCount: 10,
+                    baseCount: 5,
                     questions: buildExamPack(material, [
                         "as comparacoes do tema",
                         "a palavra-chave de contraste",
                         "as excecoes do assunto",
                         "a memorizacao das pegadinhas"
-                    ])
+                    ]).slice(0, 5)
                 }
             }
         ];
     }
 
-    function buildHighlightedParts(text, highlights) {
+    const HIGHLIGHT_COLOR_OPTIONS = [
+        {
+            key: "gold",
+            label: "Amarelo IA",
+            value: "rgba(255, 203, 109, 0.42)"
+        },
+        {
+            key: "mint",
+            label: "Verde agua",
+            value: "rgba(88, 227, 183, 0.34)"
+        },
+        {
+            key: "blue",
+            label: "Azul",
+            value: "rgba(121, 213, 255, 0.32)"
+        },
+        {
+            key: "rose",
+            label: "Rosa",
+            value: "rgba(255, 151, 188, 0.34)"
+        }
+    ];
+
+    function getHighlightColorOptions() {
+        return HIGHLIGHT_COLOR_OPTIONS.map((option) => ({ ...option }));
+    }
+
+    function resolveHighlightColorKey(colorKey) {
+        const normalized = String(colorKey || "").trim().toLowerCase();
+        return HIGHLIGHT_COLOR_OPTIONS.some((option) => option.key === normalized)
+            ? normalized
+            : "gold";
+    }
+
+    function resolveHighlightStyleMode(styleMode) {
+        return String(styleMode || "").trim().toLowerCase() === "box"
+            ? "box"
+            : "highlight";
+    }
+
+    function normalizeHighlightMatchText(text) {
+        return String(text || "")
+            .normalize("NFD")
+            .replace(/[\u0300-\u036f]/g, "")
+            .toLowerCase()
+            .replace(/\s+/g, " ")
+            .trim();
+    }
+
+    function createHighlightPart(partId, text, highlight, colorKey, styleMode = "highlight", note = "") {
+        return {
+            id: partId,
+            text: String(text || ""),
+            highlight: Boolean(highlight),
+            colorKey: highlight
+                ? resolveHighlightColorKey(colorKey)
+                : "",
+            styleMode: resolveHighlightStyleMode(styleMode),
+            note: String(note || "")
+        };
+    }
+
+    function createHighlightPartId(scope = "highlight-edit") {
+        return `${String(scope || "highlight-edit")}-${Date.now()}-${Math.random().toString(36).slice(2, 8)}`;
+    }
+
+    function normalizeHighlightParagraph(paragraph = []) {
+        const normalized = [];
+
+        (Array.isArray(paragraph) ? paragraph : []).forEach((part) => {
+            const text = String(part?.text || "");
+
+            if (!text) {
+                return;
+            }
+
+            const nextPart = createHighlightPart(
+                String(part?.id || createHighlightPartId("highlight-part")),
+                text,
+                Boolean(part?.highlight),
+                part?.colorKey || "",
+                part?.styleMode || "highlight",
+                part?.note || ""
+            );
+            const previous = normalized[normalized.length - 1];
+
+            if (
+                previous &&
+                previous.highlight === nextPart.highlight &&
+                previous.colorKey === nextPart.colorKey &&
+                previous.styleMode === nextPart.styleMode &&
+                !previous.note &&
+                !nextPart.note
+            ) {
+                previous.text += nextPart.text;
+                return;
+            }
+
+            normalized.push(nextPart);
+        });
+
+        return normalized;
+    }
+
+    function selectHighlightRangeFromParagraph(
+        paragraph = [],
+        startOffset = 0,
+        endOffset = 0,
+        activeColorKey = "gold"
+    ) {
+        const parts = Array.isArray(paragraph)
+            ? paragraph
+            : [];
+        const totalLength = parts.reduce(
+            (acc, part) =>
+                acc + String(part?.text || "").length,
+            0
+        );
+        const start = Math.max(
+            0,
+            Math.min(Number(startOffset) || 0, totalLength)
+        );
+        const end = Math.max(
+            start,
+            Math.min(Number(endOffset) || 0, totalLength)
+        );
+
+        if (start === end) {
+            return {
+                paragraph: normalizeHighlightParagraph(parts),
+                selectedPartId: ""
+            };
+        }
+
+        const selectedPartId = createHighlightPartId("highlight-selection");
+        const nextParagraph = [];
+        let selectedText = "";
+        let selectedHighlight = false;
+        let selectedColorKey = resolveHighlightColorKey(activeColorKey);
+        let selectedStyleMode = "highlight";
+        let selectedNote = "";
+        let selectionInserted = false;
+        let cursor = 0;
+
+        parts.forEach((part) => {
+            const text = String(part?.text || "");
+            const partStart = cursor;
+            const partEnd = cursor + text.length;
+            const clonedPart = () =>
+                createHighlightPart(
+                    String(part?.id || createHighlightPartId("highlight-piece")),
+                    "",
+                    Boolean(part?.highlight),
+                    part?.colorKey || "",
+                    part?.styleMode || "highlight",
+                    part?.note || ""
+                );
+
+            if (!text) {
+                cursor = partEnd;
+                return;
+            }
+
+            if (partEnd <= start || partStart >= end) {
+                if (!selectionInserted && partStart >= end && selectedText) {
+                    nextParagraph.push(
+                        createHighlightPart(
+                            selectedPartId,
+                            selectedText,
+                            selectedHighlight,
+                            selectedColorKey,
+                            selectedStyleMode,
+                            selectedNote
+                        )
+                    );
+                    selectionInserted = true;
+                }
+
+                const passthrough = clonedPart();
+                passthrough.text = text;
+                nextParagraph.push(passthrough);
+                cursor = partEnd;
+                return;
+            }
+
+            const overlapStart = Math.max(start, partStart);
+            const overlapEnd = Math.min(end, partEnd);
+            const beforeText = text.slice(0, Math.max(0, overlapStart - partStart));
+            const selectedSlice = text.slice(
+                Math.max(0, overlapStart - partStart),
+                Math.max(0, overlapEnd - partStart)
+            );
+            const afterText = text.slice(Math.max(0, overlapEnd - partStart));
+
+            if (beforeText) {
+                const beforePart = clonedPart();
+                beforePart.text = beforeText;
+                nextParagraph.push(beforePart);
+            }
+
+            if (selectedSlice) {
+                selectedText += selectedSlice;
+
+                if (part?.highlight && !selectedHighlight) {
+                    selectedHighlight = true;
+                    selectedColorKey = resolveHighlightColorKey(
+                        part?.colorKey || activeColorKey
+                    );
+                    selectedStyleMode = resolveHighlightStyleMode(part?.styleMode || "highlight");
+                }
+
+                if (part?.note && !selectedNote) {
+                    selectedNote = String(part.note || "");
+                }
+            }
+
+            if (!selectionInserted && partEnd >= end && selectedText) {
+                nextParagraph.push(
+                    createHighlightPart(
+                        selectedPartId,
+                        selectedText,
+                        selectedHighlight,
+                        selectedColorKey,
+                        selectedStyleMode,
+                        selectedNote
+                    )
+                );
+                selectionInserted = true;
+            }
+
+            if (afterText) {
+                const afterPart = clonedPart();
+                afterPart.text = afterText;
+                nextParagraph.push(afterPart);
+            }
+
+            cursor = partEnd;
+        });
+
+        if (!selectionInserted && selectedText) {
+            nextParagraph.push(
+                createHighlightPart(
+                    selectedPartId,
+                    selectedText,
+                    selectedHighlight,
+                    selectedColorKey,
+                    selectedStyleMode,
+                    selectedNote
+                )
+            );
+        }
+
+        return {
+            paragraph: normalizeHighlightParagraph(nextParagraph),
+            selectedPartId
+        };
+    }
+
+    function splitTextIntoHighlightFragments(text) {
+        const matches = String(text || "").match(/[^,.;:!?]+(?:[,.;:!?]+\s*|\s*$)/g);
+        return matches && matches.length
+            ? matches
+            : [String(text || "")];
+    }
+
+    function scoreHighlightFragment(fragment, normalizedHighlights = []) {
+        const raw = String(fragment || "");
+        const clean = raw.trim();
+        const normalized = normalizeHighlightMatchText(clean);
+
+        if (!normalized) {
+            return 0;
+        }
+
+        const cueWords = [
+            "principal",
+            "importante",
+            "essencial",
+            "nucleo",
+            "centrais",
+            "definicoes",
+            "criterio",
+            "regra",
+            "excecao",
+            "resultado",
+            "processo",
+            "causa",
+            "efeito",
+            "passo",
+            "base",
+            "estrutura",
+            "comparar",
+            "diferenca",
+            "cobrada",
+            "prova",
+            "deve",
+            "precisa"
+        ];
+        let score = 0;
+
+        if (normalizedHighlights.some((term) => term && normalized.includes(term))) {
+            score += 4;
+        }
+
+        score += cueWords.reduce((total, cue) => (
+            normalized.includes(cue) ? total + 1 : total
+        ), 0);
+
+        if (/\d/.test(clean)) {
+            score += 1;
+        }
+
+        if (clean.length >= 24 && clean.length <= 180) {
+            score += 1;
+        }
+
+        return score;
+    }
+
+    function buildSmartHighlightedFragments(content, highlights, options = {}) {
+        const fragments = splitTextIntoHighlightFragments(content);
+        const normalizedHighlights = (Array.isArray(highlights) ? highlights : [])
+            .map((item) => normalizeHighlightMatchText(item))
+            .filter(Boolean);
+        const highlightColorKey =
+            resolveHighlightColorKey(options.highlightColorKey);
+        const maxHighlights = Math.max(1, Math.min(3, Math.ceil(fragments.length / 3)));
+        const scored = fragments.map((fragment, index) => ({
+            index,
+            fragment,
+            score: scoreHighlightFragment(fragment, normalizedHighlights)
+        }));
+        const ranked = scored
+            .filter((item) => item.score >= 2)
+            .sort((left, right) => right.score - left.score || left.index - right.index);
+        const selectedIndexes = new Set();
+        let highlightedChars = 0;
+        const maxHighlightedChars = Math.max(80, Math.floor(content.length * 0.58));
+
+        ranked.forEach((item) => {
+            if (selectedIndexes.size >= maxHighlights) {
+                return;
+            }
+
+            const length = item.fragment.trim().length;
+            if (!length || highlightedChars + length > maxHighlightedChars) {
+                return;
+            }
+
+            selectedIndexes.add(item.index);
+            highlightedChars += length;
+        });
+
+        return fragments.map((fragment, index) =>
+            createHighlightPart(
+                `${String(options.baseId || "highlight")}-part-${index}`,
+                fragment,
+                selectedIndexes.has(index),
+                highlightColorKey,
+                "highlight",
+                ""
+            )
+        );
+    }
+
+    function buildHighlightedParts(text, highlights, options = {}) {
         const content = String(text || "");
         const terms = Array.isArray(highlights)
             ? highlights.filter(Boolean)
             : [];
+        const baseId = String(options.baseId || "highlight");
+        const highlightColorKey =
+            resolveHighlightColorKey(
+                options.highlightColorKey
+            );
 
         if (!content || terms.length === 0) {
             return [
+                createHighlightPart(
+                    `${baseId}-part-0`,
+                    content,
+                    false,
+                    "",
+                    "highlight",
+                    ""
+                )
+            ];
+        }
+
+        const smartParts =
+            buildSmartHighlightedFragments(
+                content,
+                terms,
                 {
-                    text: content,
-                    highlight: false
+                    baseId,
+                    highlightColorKey
                 }
-            ];
+            );
+
+        if (smartParts.some((part) => part.highlight)) {
+            return smartParts;
         }
 
         const escapedTerms = terms
@@ -844,10 +1249,14 @@
 
         if (escapedTerms.length === 0) {
             return [
-                {
-                    text: content,
-                    highlight: false
-                }
+                createHighlightPart(
+                    `${baseId}-part-0`,
+                    content,
+                    false,
+                    "",
+                    "highlight",
+                    ""
+                )
             ];
         }
 
@@ -858,85 +1267,578 @@
 
         while ((match = regex.exec(content))) {
             if (match.index > lastIndex) {
-                parts.push({
-                    text: content.slice(lastIndex, match.index),
-                    highlight: false
-                });
+                parts.push(
+                    createHighlightPart(
+                        `${baseId}-part-${parts.length}`,
+                        content.slice(
+                            lastIndex,
+                            match.index
+                        ),
+                        false,
+                        "",
+                        "highlight",
+                        ""
+                    )
+                );
             }
 
-            parts.push({
-                text: match[0],
-                highlight: true
-            });
+            parts.push(
+                createHighlightPart(
+                    `${baseId}-part-${parts.length}`,
+                    match[0],
+                    true,
+                    highlightColorKey,
+                    "highlight",
+                    ""
+                )
+            );
             lastIndex = match.index + match[0].length;
         }
 
         if (lastIndex < content.length) {
-            parts.push({
-                text: content.slice(lastIndex),
-                highlight: false
-            });
+            parts.push(
+                createHighlightPart(
+                    `${baseId}-part-${parts.length}`,
+                    content.slice(lastIndex),
+                    false,
+                    "",
+                    "highlight",
+                    ""
+                )
+            );
         }
 
         return parts.length > 0
             ? parts
-            : [
-                {
-                    text: content,
-                    highlight: false
-                }
-            ];
+            : [createHighlightPart(`${baseId}-part-0`, content, false, "", "highlight", "")];
+    }
+
+    function splitExtractedTextIntoParagraphs(text) {
+        return String(text || "")
+            .replace(/\r/g, "")
+            .split(/\n{2,}/)
+            .map((paragraph) =>
+                paragraph
+                    .replace(/\n+/g, " ")
+                    .replace(/\s+/g, " ")
+                    .trim()
+            )
+            .filter(Boolean);
+    }
+
+    function looksLikeExtractedHeading(paragraph) {
+        const clean = String(paragraph || "").trim();
+        if (!clean) {
+            return false;
+        }
+
+        const wordCount = clean.split(/\s+/).filter(Boolean).length;
+        return clean.length <= 78
+            && wordCount <= 8
+            && !/[.!?;:]$/.test(clean);
+    }
+
+    function buildHighlightSectionsFromExtractedText(
+        text,
+        options = {}
+    ) {
+        const title =
+            String(
+                options.primaryTitle ||
+                "Material completo"
+            ).trim() ||
+            "Material completo";
+        const paragraphs =
+            splitExtractedTextIntoParagraphs(text);
+
+        if (!paragraphs.length) {
+            return [];
+        }
+
+        return [
+            {
+                label: "Documento integral",
+                title,
+                paragraphs: paragraphs.map((paragraph, paragraphIndex) => [
+                    createHighlightPart(
+                        `highlight-section-0-paragraph-${paragraphIndex}-part-0`,
+                        paragraph,
+                        false,
+                        "",
+                        "highlight",
+                        ""
+                    )
+                ])
+            }
+        ];
+    }
+
+    function refreshHighlightedDocumentState(
+        documentData,
+        state,
+        sourceBlock
+    ) {
+        const block =
+            sourceBlock ||
+            state.blocks.find(
+                (item) =>
+                    item.id ===
+                    documentData.sourceBlockId
+            ) ||
+            state.blocks.find(
+                (item) =>
+                    item.id ===
+                    state.activeBlockId
+            ) ||
+            state.blocks[0];
+        const materialLabel =
+            state.studyTitle ||
+            state.materialName ||
+            "Documento";
+        const sections = Array.isArray(
+            documentData.sections
+        )
+            ? documentData.sections
+            : [];
+        const selected =
+            findSelectedHighlightPart(
+                documentData
+            );
+        const nextSelectedId = selected
+            ? selected.part.id
+            : "";
+
+        return {
+            ...documentData,
+            sourceBlockId:
+                block?.id ||
+                documentData.sourceBlockId ||
+                "",
+            title:
+                documentData.title ||
+                `${materialLabel} - texto com marcador`,
+            subtitle:
+                documentData.subtitle ||
+                "Texto integral extraido do PDF, sem resumo automatico e pronto para selecao manual.",
+            ctaLabel:
+                documentData.ctaLabel ||
+                "Extrair resumo para documento novo",
+            sections,
+            originalSections: Array.isArray(documentData.originalSections)
+                ? documentData.originalSections
+                : clone(sections),
+            selectedPartId: nextSelectedId,
+            activeColorKey:
+                resolveHighlightColorKey(
+                    documentData.activeColorKey ||
+                    "gold"
+                ),
+            colorOptions:
+                getHighlightColorOptions(),
+            extractedSummary: {
+                title:
+                    `${block?.title || "Resumo"} - resumo extraido`,
+                lead: "",
+                bullets:
+                    [],
+                sourceTitle:
+                    materialLabel,
+                blockTitle:
+                    block?.title || ""
+            }
+        };
     }
 
     function buildHighlightedDocument(state, block) {
         const sourceBlock = block || state.blocks[0];
         const materialLabel = state.studyTitle || state.materialName || "Documento";
-        const emphasisTerms = [
-            sourceBlock.title,
-            ...(sourceBlock.topics || []).slice(0, 3),
-            ...(sourceBlock.learn.keyConcepts || []).slice(0, 3)
-        ].filter(Boolean);
         const learnSections = Array.isArray(sourceBlock.learn.sections)
             ? sourceBlock.learn.sections
             : [];
-        const sections = [
-            {
-                label: "Visao geral",
-                title: sourceBlock.title,
-                paragraphs: [
-                    buildHighlightedParts(sourceBlock.learn.summary, emphasisTerms),
-                    buildHighlightedParts(sourceBlock.learn.intro || sourceBlock.subtitle || "", emphasisTerms)
-                ].filter((parts) => parts.some((part) => part.text))
-            },
-            ...learnSections.map((section) => ({
-                label: section.label,
-                title: section.title,
-                paragraphs: section.paragraphs.map((paragraph) =>
-                    buildHighlightedParts(paragraph, emphasisTerms)
+        const extractedTextSections =
+            state.materialExtractedText
+                ? buildHighlightSectionsFromExtractedText(
+                    state.materialExtractedText,
+                    {
+                        primaryTitle: materialLabel
+                    }
                 )
-            }))
-        ];
+                : [];
+        const sections = extractedTextSections.length
+            ? extractedTextSections
+            : [
+                {
+                    label: "Documento integral",
+                    title: materialLabel,
+                    paragraphs: [
+                        [
+                            createHighlightPart(
+                                "highlight-summary-part-0",
+                                "O texto integral deste PDF ainda nao foi extraido neste navegador. Reabra o PDF original aqui para carregar o documento completo antes de marcar trechos.",
+                                false,
+                                "",
+                                "highlight",
+                                ""
+                            )
+                        ]
+                    ].filter((parts) => parts.some((part) => part.text))
+                },
+            ];
 
-        return {
+        return refreshHighlightedDocumentState({
             id: `highlight-${sourceBlock.id}`,
             sourceBlockId: sourceBlock.id,
             title: `${materialLabel} - texto com marcador`,
-            subtitle: "Documento original preservado, com destaque nas partes mais importantes.",
+            subtitle: extractedTextSections.length
+                ? "Texto integral extraido do PDF, sem resumo automatico e sem marcacoes iniciais."
+                : "Texto integral carregado para selecao manual, sem marcacoes iniciais.",
             ctaLabel: "Extrair resumo para documento novo",
             sections,
+            originalSections: clone(sections),
+            selectedPartId: "",
+            activeColorKey: "gold",
+            colorOptions: getHighlightColorOptions(),
             extractedSummary: {
                 title: `${sourceBlock.title} - resumo extraido`,
-                lead: sourceBlock.learn.summary,
-                bullets: [
-                    ...(sourceBlock.learn.keyConcepts || []).slice(0, 4),
-                    ...(sourceBlock.learn.hotPoints || []).slice(0, 2)
-                ].filter(Boolean),
+                lead: "",
+                bullets: [],
                 sourceTitle: materialLabel,
                 blockTitle: sourceBlock.title
             }
+        }, state, sourceBlock);
+    }
+
+    function buildPracticeCuePool(block) {
+        return [
+            block.title,
+            block.subtitle,
+            ...(Array.isArray(block.topics)
+                ? block.topics
+                : []),
+            ...(Array.isArray(
+                block.learn?.keyConcepts
+            )
+                ? block.learn.keyConcepts
+                : []),
+            ...(Array.isArray(
+                block.learn?.hotPoints
+            )
+                ? block.learn.hotPoints
+                : [])
+        ]
+            .filter(Boolean)
+            .map((item) => String(item).trim())
+            .filter(Boolean);
+    }
+
+    function buildFallbackQuizItem(
+        block,
+        absoluteIndex
+    ) {
+        const cues =
+            buildPracticeCuePool(block);
+        const cue =
+            cues[
+                absoluteIndex %
+                Math.max(cues.length, 1)
+            ] || block.title;
+
+        return buildQuestion(
+            `No material completo de ${block.title}, qual leitura ajuda mais a dominar ${cue}?`,
+            [
+                "Responder so pelo tema geral",
+                "Comparar criterio, funcao e limite antes de decidir",
+                "Ignorar relacoes entre conceitos",
+                "Memorizar uma frase isolada"
+            ],
+            1,
+            "A leitura mais forte nasce quando voce compara criterio, funcao e limite, em vez de decidir por semelhanca."
+        );
+    }
+
+    function buildFallbackTrueFalseItem(
+        block,
+        absoluteIndex
+    ) {
+        const cues =
+            buildPracticeCuePool(block);
+        const cue =
+            cues[
+                absoluteIndex %
+                Math.max(cues.length, 1)
+            ] || block.title;
+
+        return buildTrueFalse(
+            `No estudo completo de ${block.title}, ${cue} pode ser decidido sem comparar criterio e limite.`,
+            false,
+            "Quando criterio e limite somem da leitura, o erro por semelhanca superficial cresce."
+        );
+    }
+
+    function buildFallbackFlashcardItem(
+        block,
+        absoluteIndex
+    ) {
+        const cues =
+            buildPracticeCuePool(block);
+        const cue =
+            cues[
+                absoluteIndex %
+                Math.max(cues.length, 1)
+            ] || block.title;
+
+        return buildFlashcard(
+            `Ponto-chave ${absoluteIndex + 1}`,
+            cue,
+            "Use esse lembrete para puxar o criterio central do material inteiro antes de responder."
+        );
+    }
+
+    function normalizeQuizItem(
+        item,
+        block,
+        absoluteIndex
+    ) {
+        if (
+            !item ||
+            !String(item.prompt || "").trim()
+        ) {
+            return buildFallbackQuizItem(
+                block,
+                absoluteIndex
+            );
+        }
+
+        const options = Array.isArray(item.options)
+            ? item.options
+                .map((option) =>
+                    String(option || "").trim()
+                )
+                .filter(Boolean)
+            : [];
+
+        while (options.length < 4) {
+            options.push(
+                `Opcao ${String.fromCharCode(65 + options.length)}`
+            );
+        }
+
+        const correctIndex =
+            Number.isFinite(item.correctIndex)
+                ? Math.max(
+                    0,
+                    Math.min(
+                        options.length - 1,
+                        Number(item.correctIndex)
+                    )
+                )
+                : 0;
+
+        return buildQuestion(
+            String(item.prompt).trim(),
+            options.slice(0, 4),
+            correctIndex,
+            String(item.rationale || "").trim() ||
+                "Compare criterio, funcao e limite para validar a resposta correta."
+        );
+    }
+
+    function normalizeTrueFalseItem(
+        item,
+        block,
+        absoluteIndex
+    ) {
+        if (
+            !item ||
+            !String(item.statement || "").trim()
+        ) {
+            return buildFallbackTrueFalseItem(
+                block,
+                absoluteIndex
+            );
+        }
+
+        return buildTrueFalse(
+            String(item.statement).trim(),
+            Boolean(item.answer),
+            String(item.rationale || "").trim() ||
+                "Volte ao criterio do texto para separar o que e regra do que e excecao."
+        );
+    }
+
+    function normalizeFlashcardItem(
+        item,
+        block,
+        absoluteIndex
+    ) {
+        if (
+            !item ||
+            !String(item.front || "").trim() ||
+            !String(item.back || "").trim()
+        ) {
+            return buildFallbackFlashcardItem(
+                block,
+                absoluteIndex
+            );
+        }
+
+        return buildFlashcard(
+            String(item.front).trim(),
+            String(item.back).trim(),
+            String(item.tip || "").trim() ||
+                "Use esse card para puxar o criterio principal do material."
+        );
+    }
+
+    function normalizePracticeSeriesByType(
+        block,
+        practice,
+        type
+    ) {
+        const config = {
+            quiz: {
+                baseKey: "quiz",
+                seriesKey: "quizSeries",
+                itemCount: 3,
+                normalizer: normalizeQuizItem
+            },
+            trueFalse: {
+                baseKey: "trueFalse",
+                seriesKey: "trueFalseSeries",
+                itemCount: 3,
+                normalizer: normalizeTrueFalseItem
+            },
+            flashcards: {
+                baseKey: "flashcards",
+                seriesKey: "flashcardSeries",
+                itemCount: 3,
+                normalizer: normalizeFlashcardItem
+            }
+        }[type];
+
+        const rawBase = Array.isArray(
+            practice[config.baseKey]
+        )
+            ? practice[config.baseKey]
+            : [];
+        const rawSeries = Array.isArray(
+            practice[config.seriesKey]
+        )
+            ? practice[config.seriesKey]
+            : [];
+        const pool = [
+            ...rawBase,
+            ...rawSeries.flatMap((series) =>
+                Array.isArray(series)
+                    ? series
+                    : []
+            )
+        ].map((item, index) =>
+            config.normalizer(
+                item,
+                block,
+                index
+            )
+        );
+        const safePool = pool.length
+            ? pool
+            : Array.from(
+                { length: config.itemCount * 3 },
+                (_, index) =>
+                    config.normalizer(
+                        null,
+                        block,
+                        index
+                    )
+            );
+        const series = Array.from(
+            { length: 3 },
+            (_, seriesIndex) =>
+                Array.from(
+                    { length: config.itemCount },
+                    (_, itemIndex) => {
+                        const absoluteIndex =
+                            seriesIndex *
+                                config.itemCount +
+                            itemIndex;
+                        const source =
+                            safePool[
+                                absoluteIndex %
+                                    safePool.length
+                            ];
+
+                        return clone(source);
+                    }
+                )
+        );
+
+        return {
+            base: series[0],
+            series
         };
     }
 
+    function findSelectedHighlightPart(
+        documentData
+    ) {
+        if (
+            !documentData ||
+            !documentData.selectedPartId
+        ) {
+            return null;
+        }
+
+        for (
+            let sectionIndex = 0;
+            sectionIndex <
+            (documentData.sections || [])
+                .length;
+            sectionIndex += 1
+        ) {
+            const section =
+                documentData.sections[
+                    sectionIndex
+                ];
+
+            for (
+                let paragraphIndex = 0;
+                paragraphIndex <
+                (section.paragraphs || [])
+                    .length;
+                paragraphIndex += 1
+            ) {
+                const paragraph =
+                    section.paragraphs[
+                        paragraphIndex
+                    ];
+
+                for (
+                    let partIndex = 0;
+                    partIndex <
+                    (paragraph || []).length;
+                    partIndex += 1
+                ) {
+                    const part =
+                        paragraph[partIndex];
+
+                    if (
+                        part.id ===
+                        documentData.selectedPartId
+                    ) {
+                        return {
+                            sectionIndex,
+                            paragraphIndex,
+                            partIndex,
+                            part
+                        };
+                    }
+                }
+            }
+        }
+
+        return null;
+    }
+
     function createSavedSummaryRecord(documentData, state) {
         const summary = documentData.extractedSummary || {};
         return {
@@ -998,30 +1900,37 @@
     }
 
     function enrichLearnContent(block, materialLabel) {
+        if (
+            block.generatedByAi ||
+            (block.learn && Array.isArray(block.learn.documentSections) && block.learn.documentSections.length > 0)
+        ) {
+            return block;
+        }
+
         if (block.id === "block-1") {
             return {
                 ...block,
                 learn: {
                     ...block.learn,
-                    summary: `Este bloco abre o estudo pelo núcleo de ${materialLabel}, organizando os termos centrais, as definições mais importantes e as relações que sustentam o restante do conteúdo.`,
+                    summary: `Este bloco abre o estudo pelo nÃºcleo de ${materialLabel}, organizando os termos centrais, as definiÃ§Ãµes mais importantes e as relaÃ§Ãµes que sustentam o restante do conteÃºdo.`,
                     intro: "A proposta aqui e construir entendimento antes de acelerar. Quando o nucleo do assunto fica claro, o material deixa de parecer uma lista de informacoes soltas e passa a ter ordem, hierarquia e direcao.",
                     documentSections: buildDocumentSections({
                         summaryTitle: "O mapa que organiza a leitura inteira",
                         summaryParagraphs: [
-                            `O núcleo principal de ${materialLabel} precisa ser lido como a espinha dorsal do tema. A função deste assunto não é despejar informação, mas criar ordem: primeiro você entende o que define o tema, depois enxerga como cada parte se conecta e, só então, diferencia exemplo, aplicação e exceção.`,
-                            "Quando esse mapa fica claro, a leitura acelera naturalmente. O estudante para de decorar frases soltas e passa a reconhecer critério, estrutura e linguagem recorrente. Isso deixa o conteúdo mais leve, reduz a dispersão e melhora muito a entrada nas questões.",
-                            "Em prova, esse bloco vale porque ele sustenta todos os demais. Se a base estiver firme, você identifica com mais segurança o que o enunciado realmente quer cobrar e evita respostas por semelhança superficial."
+                            `O nÃºcleo principal de ${materialLabel} precisa ser lido como a espinha dorsal do tema. A funÃ§Ã£o deste assunto nÃ£o Ã© despejar informaÃ§Ã£o, mas criar ordem: primeiro vocÃª entende o que define o tema, depois enxerga como cada parte se conecta e, sÃ³ entÃ£o, diferencia exemplo, aplicaÃ§Ã£o e exceÃ§Ã£o.`,
+                            "Quando esse mapa fica claro, a leitura acelera naturalmente. O estudante para de decorar frases soltas e passa a reconhecer critÃ©rio, estrutura e linguagem recorrente. Isso deixa o conteÃºdo mais leve, reduz a dispersÃ£o e melhora muito a entrada nas questÃµes.",
+                            "Em prova, esse bloco vale porque ele sustenta todos os demais. Se a base estiver firme, vocÃª identifica com mais seguranÃ§a o que o enunciado realmente quer cobrar e evita respostas por semelhanÃ§a superficial."
                         ],
                         conceptsTitle: "As ideias que sustentam o restante do material",
                         conceptsItems: [
                             "Definir o conceito central antes de aceitar exemplos como se fossem a propria definicao.",
-                            "Reconhecer qual critério de classificação organiza o assunto e separa as partes principais.",
+                            "Reconhecer qual critÃ©rio de classificaÃ§Ã£o organiza o assunto e separa as partes principais.",
                             "Entender a estrutura basica do tema para nao tratar elementos relacionados como se fossem equivalentes.",
                             "Ligar cada termo importante a sua funcao, e nao apenas ao nome que aparece no PDF."
                         ],
                         rulesTitle: "O que funciona como regra e onde o assunto costuma abrir excecao",
                         rulesItems: [
-                            "A regra aqui é partir da definição e do critério antes de avançar para detalhe ou aplicação.",
+                            "A regra aqui Ã© partir da definiÃ§Ã£o e do critÃ©rio antes de avanÃ§ar para detalhe ou aplicaÃ§Ã£o.",
                             "Excecoes pontuais aparecem quando um caso parece pertencer ao conceito, mas altera sua funcao ou limite.",
                             "Sempre que a leitura fugir da base e entrar num caso especifico, vale verificar se aquilo e exemplo ou excecao real.",
                             "Se a excecao depende de condicao, essa condicao precisa ficar colada a ela na revisao."
@@ -1029,15 +1938,15 @@
                         pitfallsTitle: "Onde a banca pode confundir quem leu sem estruturar",
                         pitfallsItems: [
                             "Trocar definicao por exemplo pratico porque os dois parecem falar da mesma coisa.",
-                            "Responder pelo tema geral da alternativa e ignorar a palavra que muda o critério.",
+                            "Responder pelo tema geral da alternativa e ignorar a palavra que muda o critÃ©rio.",
                             "Memorizar nomes isolados sem entender qual relacao existe entre eles.",
                             "Assumir que todo caso recorrente representa a regra principal."
                         ],
-                        comparisonsTitle: "Comparações que deixam o critério mais nítido",
+                        comparisonsTitle: "ComparaÃ§Ãµes que deixam o critÃ©rio mais nÃ­tido",
                         comparisonsItems: [
                             "Conceito central versus aplicacao: um explica o que o tema e, o outro mostra como ele aparece.",
                             "Regra versus excecao: a regra organiza, a excecao limita ou desvia esse alcance.",
-                            "Critério versus exemplo: critério serve para decidir; exemplo serve para ilustrar.",
+                            "CritÃ©rio versus exemplo: critÃ©rio serve para decidir; exemplo serve para ilustrar.",
                             "Leitura estrutural versus decoracao: a primeira sustenta acerto, a segunda quebra quando o enunciado muda."
                         ],
                         criteriaTitle: "O que precisa virar referencia rapida na revisao",
@@ -1051,16 +1960,16 @@
                     explainBetter: {
                         title: "Explicacao mais didatica deste assunto",
                         paragraphs: [
-                            `Pense este assunto como a fundação de ${materialLabel}. Se a fundação estiver clara, o resto do conteúdo deixa de parecer um monte de blocos separados e passa a funcionar como uma construção coerente.`,
-                            "O jeito mais seguro de estudar aqui é perguntar o tempo todo: isso é definição, critério, exemplo ou exceção? Essa pergunta simples evita quase todos os erros de leitura superficial.",
-                            "Quando você consegue responder com suas próprias palavras qual é a ideia central e por que ela organiza o tema, o assunto já saiu do campo da decoração e entrou no campo do entendimento."
+                            `Pense este assunto como a fundaÃ§Ã£o de ${materialLabel}. Se a fundaÃ§Ã£o estiver clara, o resto do conteÃºdo deixa de parecer um monte de blocos separados e passa a funcionar como uma construÃ§Ã£o coerente.`,
+                            "O jeito mais seguro de estudar aqui Ã© perguntar o tempo todo: isso Ã© definiÃ§Ã£o, critÃ©rio, exemplo ou exceÃ§Ã£o? Essa pergunta simples evita quase todos os erros de leitura superficial.",
+                            "Quando vocÃª consegue responder com suas prÃ³prias palavras qual Ã© a ideia central e por que ela organiza o tema, o assunto jÃ¡ saiu do campo da decoraÃ§Ã£o e entrou no campo do entendimento."
                         ]
                     },
                     reviewInFivePoints: [
                         "Descubra primeiro qual conceito organiza o assunto inteiro.",
-                        "Separe definição, critério, exemplo e exceção antes de revisar detalhes.",
+                        "Separe definiÃ§Ã£o, critÃ©rio, exemplo e exceÃ§Ã£o antes de revisar detalhes.",
                         "Nao aceite duas ideias parecidas como equivalentes sem comparar a funcao de cada uma.",
-                        "Volte ao critério sempre que uma alternativa parecer familiar demais.",
+                        "Volte ao critÃ©rio sempre que uma alternativa parecer familiar demais.",
                         "Considere o bloco dominado quando conseguir explicar a base sem depender do PDF."
                     ],
                     sections: [
@@ -1076,16 +1985,16 @@
                             label: "Leitura guiada",
                             title: "Como transformar resumo em entendimento real",
                             paragraphs: [
-                                "Leia procurando relações, não frases soltas. Sempre que surgir um termo importante, pergunte qual critério ele representa, com o que ele se conecta e que erro pode acontecer se ele for confundido com exemplo ou aplicação.",
+                                "Leia procurando relaÃ§Ãµes, nÃ£o frases soltas. Sempre que surgir um termo importante, pergunte qual critÃ©rio ele representa, com o que ele se conecta e que erro pode acontecer se ele for confundido com exemplo ou aplicaÃ§Ã£o.",
                                 "Essa postura melhora a retencao porque o cerebro guarda estrutura. Em prova, isso vale muito mais do que decorar definicoes isoladas sem entender por que elas importam."
                             ]
                         },
                         {
                             label: "Resultado",
-                            title: "O que este assunto precisa entregar para você",
+                            title: "O que este assunto precisa entregar para vocÃª",
                             paragraphs: [
-                                "O ganho principal aqui é criar segurança conceitual. Quando a base está firme, você reconhece o que o enunciado realmente está cobrando e evita cair em alternativas que parecem familiares, mas trocam critério por exemplo.",
-                                "Se ao final deste bloco você conseguir explicar o assunto com suas próprias palavras e mostrar a diferença entre regra, exceção e aplicação, o aprendizado já saiu do nível superficial."
+                                "O ganho principal aqui Ã© criar seguranÃ§a conceitual. Quando a base estÃ¡ firme, vocÃª reconhece o que o enunciado realmente estÃ¡ cobrando e evita cair em alternativas que parecem familiares, mas trocam critÃ©rio por exemplo.",
+                                "Se ao final deste bloco vocÃª conseguir explicar o assunto com suas prÃ³prias palavras e mostrar a diferenÃ§a entre regra, exceÃ§Ã£o e aplicaÃ§Ã£o, o aprendizado jÃ¡ saiu do nÃ­vel superficial."
                             ]
                         }
                     ]
@@ -1099,17 +2008,17 @@
                 learn: {
                     ...block.learn,
                     summary: `Este bloco concentra o que mais costuma gerar erro em ${materialLabel}: comparacoes, excecoes e formulacoes parecidas que separam leitura superficial de acerto consistente.`,
-                    intro: "Depois que a base fica entendida, o que mais faz diferença no desempenho é o refinamento do critério. Este bloco existe para treinar o olhar nas pequenas mudanças de sentido que a banca usa para separar domínio de reconhecimento superficial.",
+                    intro: "Depois que a base fica entendida, o que mais faz diferenÃ§a no desempenho Ã© o refinamento do critÃ©rio. Este bloco existe para treinar o olhar nas pequenas mudanÃ§as de sentido que a banca usa para separar domÃ­nio de reconhecimento superficial.",
                     documentSections: buildDocumentSections({
                         summaryTitle: "O ajuste fino que transforma leitura em acerto",
                         summaryParagraphs: [
-                            `Depois de entender a base de ${materialLabel}, o maior salto de resultado vem do refinamento. Este assunto existe para treinar o olhar nas comparações, nas palavras de contraste e nas exceções que a banca usa para separar quem domina o critério de quem apenas reconhece o tema.`,
+                            `Depois de entender a base de ${materialLabel}, o maior salto de resultado vem do refinamento. Este assunto existe para treinar o olhar nas comparaÃ§Ãµes, nas palavras de contraste e nas exceÃ§Ãµes que a banca usa para separar quem domina o critÃ©rio de quem apenas reconhece o tema.`,
                             "Aqui o estudo precisa ficar mais preciso. A leitura deixa de perguntar apenas do que o texto esta falando e passa a perguntar o que mudou, qual limite foi inserido e qual palavra alterou o sentido do enunciado.",
-                            "Quando esse bloco fica bem resolvido, o aluno passa a errar menos por impulso e ganha mais controle em alternativas muito parecidas, especialmente nas questões em que a diferença cabe em um detalhe."
+                            "Quando esse bloco fica bem resolvido, o aluno passa a errar menos por impulso e ganha mais controle em alternativas muito parecidas, especialmente nas questÃµes em que a diferenÃ§a cabe em um detalhe."
                         ],
                         conceptsTitle: "As ideias que precisam virar ferramenta de prova",
                         conceptsItems: [
-                            "Comparação direta entre itens próximos para enxergar onde o critério muda.",
+                            "ComparaÃ§Ã£o direta entre itens prÃ³ximos para enxergar onde o critÃ©rio muda.",
                             "Leitura da palavra-chave que limita, confirma ou invalida o sentido do enunciado.",
                             "Separacao limpa entre regra principal e excecao recorrente.",
                             "Reconhecimento das formulacoes que parecem equivalentes, mas nao produzem o mesmo efeito."
@@ -1121,7 +2030,7 @@
                             "Sempre que houver limite, condicao ou ressalva, esse elemento precisa ser lido como parte da resposta, nao como detalhe lateral.",
                             "A excecao nunca deve ser memorizada colada na regra; ela precisa aparecer como desvio controlado."
                         ],
-                        pitfallsTitle: "Erros típicos de quem conhece o tema, mas não o critério",
+                        pitfallsTitle: "Erros tÃ­picos de quem conhece o tema, mas nÃ£o o critÃ©rio",
                         pitfallsItems: [
                             "Confiar na memoria visual do PDF e ignorar a mudanca de uma palavra decisiva.",
                             "Marcar a alternativa pelo assunto geral, sem checar o limite da afirmacao.",
@@ -1132,10 +2041,10 @@
                         comparisonsItems: [
                             "Regra versus excecao: uma organiza o caso comum, a outra limita o alcance.",
                             "Palavra neutra versus palavra de contraste: a segunda costuma decidir a questao.",
-                            "Enunciado correto versus quase correto: a diferença geralmente está no critério, não no tema.",
+                            "Enunciado correto versus quase correto: a diferenÃ§a geralmente estÃ¡ no critÃ©rio, nÃ£o no tema.",
                             "Reconhecimento superficial versus leitura analitica: o primeiro acelera erro, a segunda sustenta acerto."
                         ],
-                        criteriaTitle: "Definições curtas que protegem você da pegadinha",
+                        criteriaTitle: "DefiniÃ§Ãµes curtas que protegem vocÃª da pegadinha",
                         criteriaItems: [
                             "Palavra-chave: termo que confirma, restringe ou desloca o sentido da alternativa.",
                             "Criterio de contraste: ponto exato que separa duas formulacoes parecidas.",
@@ -1148,7 +2057,7 @@
                         paragraphs: [
                             "Este bloco nao pede mais volume de leitura, e sim mais precisao. A pergunta principal deixa de ser 'eu ja vi isso?' e vira 'o que exatamente mudou aqui?'.",
                             "Quase sempre a banca esconde a diferenca em um termo pequeno: uma restricao, uma ressalva, um conectivo ou uma palavra que parece inofensiva. Por isso a comparacao precisa ser ativa e deliberada.",
-                            "Quando você passa a ler procurando limite, contraste e exceção, o assunto deixa de ser escorregadio e vira um bloco tecnicamente controlável."
+                            "Quando vocÃª passa a ler procurando limite, contraste e exceÃ§Ã£o, o assunto deixa de ser escorregadio e vira um bloco tecnicamente controlÃ¡vel."
                         ]
                     },
                     reviewInFivePoints: [
@@ -1156,15 +2065,15 @@
                         "Procure a palavra que limita, inverte ou condiciona o enunciado.",
                         "Nao memorize excecao junto da regra: destaque o limite que a torna especial.",
                         "Desconfie de respostas que parecem certas apenas pelo tema geral.",
-                        "Considere o bloco dominado quando você consegue explicar por que a quase correta ainda está errada."
+                        "Considere o bloco dominado quando vocÃª consegue explicar por que a quase correta ainda estÃ¡ errada."
                     ],
                     sections: [
                         {
                             label: "Comparacao",
                             title: "Onde a maioria dos erros nasce",
                             paragraphs: [
-                                "Grande parte dos erros nesta fase não vem de desconhecer o assunto, mas de ler duas formulações parecidas como se fossem equivalentes. É justamente aqui que a prova separa quem só reconhece o tema de quem domina o critério.",
-                                "Por isso, a comparação precisa ser ativa. Em vez de perguntar apenas se você já viu aquilo, o caminho melhor é perguntar o que muda, qual palavra limita o sentido e onde está a diferença que altera a resposta."
+                                "Grande parte dos erros nesta fase nÃ£o vem de desconhecer o assunto, mas de ler duas formulaÃ§Ãµes parecidas como se fossem equivalentes. Ã‰ justamente aqui que a prova separa quem sÃ³ reconhece o tema de quem domina o critÃ©rio.",
+                                "Por isso, a comparaÃ§Ã£o precisa ser ativa. Em vez de perguntar apenas se vocÃª jÃ¡ viu aquilo, o caminho melhor Ã© perguntar o que muda, qual palavra limita o sentido e onde estÃ¡ a diferenÃ§a que altera a resposta."
                             ]
                         },
                         {
@@ -1180,7 +2089,7 @@
                             title: "Como transformar este bloco em acerto de prova",
                             paragraphs: [
                                 "A estrategia mais forte aqui e revisar por contraste: regra versus excecao, definicao versus exemplo, termo central versus palavra que desvia o sentido. Isso deixa a leitura mais afiada para a linguagem da banca.",
-                                "Se você conseguir localizar rapidamente a palavra que confirma, limita ou invalida o enunciado, este bloco já cumpriu sua função."
+                                "Se vocÃª conseguir localizar rapidamente a palavra que confirma, limita ou invalida o enunciado, este bloco jÃ¡ cumpriu sua funÃ§Ã£o."
                             ]
                         }
                     ]
@@ -1429,13 +2338,30 @@
             accessTier: "free",
             subscriptionStatus: "registered_free",
             customerId: "",
+            accountUser: null,
+            accountAuthenticated: false,
             premiumEntitlement: null,
             premiumStatusConfigured: false,
+            generationPaused: false,
+            opsLanes: {
+                freeLanePaused: false,
+                premiumLanePaused: false
+            },
+            opsThresholds: {
+                dailyWarnThreshold: 500,
+                dailyCriticalThreshold: 600,
+                dailyHardStopThreshold: 650
+            },
+            trialState: null,
             studyLibraryId: createStudyLibraryId(),
             studyTitle: "",
             materialName: "",
+            materialHash: "",
+            materialFileKey: "",
             materialSizeLabel: "",
             materialPageCount: null,
+            materialExtractedText: "",
+            materialExtractionStatus: "pending",
             examDate: "",
             calendarMonth: today.getMonth(),
             calendarYear: today.getFullYear(),
@@ -1444,12 +2370,17 @@
             studyMinutes: 30,
             analysisProgress: 8,
             analysisStatus: "pending",
+            aiGeneration: null,
             blocks,
             sessions: buildSessions(blocks),
             activeBlockId: blocks[0].id,
             blockTab: "aprender",
             blockFullScreen: true,
             blockAssistMode: "",
+            highlightEditorOpen: false,
+            highlightEditorFullScreen: false,
+            highlightInspectorMode: "",
+            levelExam: createLevelExamState(),
             highlightedDocument: null,
             savedSummaries: [],
             activeSavedSummaryId: "",
@@ -1549,14 +2480,20 @@
                 ...this.state,
                 studyLibraryId: createStudyLibraryId(),
                 materialName: fileLike.name || "material.pdf",
+                materialHash: fileLike.materialHash || fileLike.hash || "",
+                materialFileKey: fileLike.materialFileKey || fileLike.fileKey || "",
                 materialSizeLabel: sizeLabel,
                 materialPageCount: pageCount,
+                materialExtractedText: "",
+                materialExtractionStatus: "pending",
                 studyTitle,
                 blocks,
                 sessions: buildSessions(blocks),
                 activeBlockId: blocks[0].id,
                 blockFullScreen: true,
                 blockAssistMode: "",
+                aiGeneration: null,
+                levelExam: createLevelExamState(),
                 sessionNote: null,
                 progressLabel: "Material recebido. Agora vamos ajustar tudo ao seu prazo e a sua meta."
             };
@@ -1657,6 +2594,284 @@
             return this.state;
         },
 
+        setMaterialExtraction(result = {}) {
+            const nextExtractedText =
+                result.text || this.state.materialExtractedText || "";
+            const hadExtractedText = Boolean(
+                String(this.state.materialExtractedText || "").trim()
+            );
+
+            let nextState = {
+                ...this.state,
+                materialExtractedText: nextExtractedText,
+                materialExtractionStatus: result.status || (result.text ? "extracted" : "empty_text"),
+                materialPageCount: Number.isFinite(result.pageCount)
+                    ? result.pageCount
+                    : this.state.materialPageCount
+            };
+
+            if (!hadExtractedText && String(nextExtractedText).trim()) {
+                const block =
+                    nextState.blocks.find((item) => item.id === nextState.activeBlockId) ||
+                    nextState.blocks[0];
+
+                nextState = {
+                    ...nextState,
+                    highlightedDocument: refreshHighlightedDocumentState(
+                        buildHighlightedDocument(nextState, block),
+                        nextState,
+                        block
+                    )
+                };
+            }
+
+            this.state = nextState;
+
+            return this.state;
+        },
+
+        applyGeneratedBundle(payload = {}) {
+            const bundle = payload.bundle || payload;
+            const sourceBlocks = Array.isArray(bundle.blocks) ? bundle.blocks : [];
+            if (!sourceBlocks.length) {
+                return this.state;
+            }
+
+            const blocks = sourceBlocks.map((block, index) => {
+                const practice = block.practice || {};
+                const normalizedBlock = {
+                    ...block,
+                    id: block.id || `block-${index + 1}`
+                };
+                const normalizedQuiz =
+                    normalizePracticeSeriesByType(
+                        normalizedBlock,
+                        practice,
+                        "quiz"
+                    );
+                const normalizedTrueFalse =
+                    normalizePracticeSeriesByType(
+                        normalizedBlock,
+                        practice,
+                        "trueFalse"
+                    );
+                const normalizedFlashcards =
+                    normalizePracticeSeriesByType(
+                        normalizedBlock,
+                        practice,
+                        "flashcards"
+                    );
+                const questions = Array.isArray(block.exam && block.exam.questions)
+                    ? block.exam.questions.slice(0, 5)
+                    : [];
+
+                return {
+                    ...block,
+                    id: block.id || `block-${index + 1}`,
+                    generatedByAi: true,
+                    status: index === 0 ? "recommended" : (block.status || "ready"),
+                    progress: {
+                        learn: false,
+                        practice: false,
+                        exam: false,
+                        ...(block.progress || {})
+                    },
+                    learn: {
+                        summary: "",
+                        intro: "",
+                        hotPoints: [],
+                        keyConcepts: [],
+                        pitfalls: [],
+                        documentSections: [],
+                        explainBetter: null,
+                        reviewInFivePoints: [],
+                        ...(block.learn || {})
+                    },
+                    practice: {
+                        targets: {
+                            quiz: 3,
+                            trueFalse: 3,
+                            flashcards: 3
+                        },
+                        ...practice,
+                        quiz: normalizedQuiz.base,
+                        quizSeries: normalizedQuiz.series,
+                        trueFalse: normalizedTrueFalse.base,
+                        trueFalseSeries: normalizedTrueFalse.series,
+                        flashcards: normalizedFlashcards.base,
+                        flashcardSeries: normalizedFlashcards.series
+                    },
+                    exam: {
+                        ...(block.exam || {}),
+                        baseCount: 5,
+                        questions
+                    }
+                };
+            });
+
+            this.state = {
+                ...this.state,
+                studyTitle: bundle.title || this.state.studyTitle,
+                blocks,
+                sessions: buildSessions(blocks),
+                activeBlockId: bundle.recommendedBlockId && blocks.some((block) => block.id === bundle.recommendedBlockId)
+                    ? bundle.recommendedBlockId
+                    : blocks[0].id,
+                blockAssistMode: "",
+                aiGeneration: {
+                    status: payload.status || "generated",
+                    provider: payload.provider || "",
+                    model: payload.model || "",
+                    promptVersion: payload.promptVersion || "",
+                    warnings: Array.isArray(bundle.warnings) ? bundle.warnings : [],
+                    generatedAt: new Date().toISOString()
+                },
+                progressLabel: "Sua rota foi gerada com IA a partir do PDF. Agora escolha como quer estudar."
+            };
+
+            return this.state;
+        },
+
+        appendMiniExamQuestions(blockId, questions = []) {
+            const targetId = blockId || this.state.activeBlockId;
+            const nextQuestions = Array.isArray(questions) ? questions : [];
+            if (!targetId || !nextQuestions.length) {
+                return this.state;
+            }
+
+            const blocks = this.state.blocks.map((block) => {
+                if (block.id !== targetId) {
+                    return block;
+                }
+
+                return {
+                    ...block,
+                    exam: {
+                        ...block.exam,
+                        baseCount: Number(block.exam && block.exam.baseCount) || 5,
+                        questions: [
+                            ...((block.exam && block.exam.questions) || []),
+                            ...nextQuestions
+                        ]
+                    }
+                };
+            });
+            const sessions = normalizeSessions(blocks, this.state.sessions);
+            sessions[targetId].miniExam = createMiniExamSession(
+                (blocks.find((block) => block.id === targetId).exam || {}).questions || []
+            );
+
+            this.state = {
+                ...this.state,
+                blocks,
+                sessions,
+                progressLabel: "Mais 5 questoes premium foram adicionadas a mini prova deste bloco."
+            };
+
+            return this.state;
+        },
+
+        setLevelExamQuestionCount(count) {
+            const allowed = [10, 20, 30];
+            const questionCount = allowed.includes(Number(count)) ? Number(count) : 10;
+            this.state = {
+                ...this.state,
+                levelExam: createLevelExamState({
+                    ...this.state.levelExam,
+                    questionCount,
+                    questions: [],
+                    started: false,
+                    answers: [],
+                    index: 0,
+                    isComplete: false,
+                    result: null,
+                    status: "idle"
+                })
+            };
+
+            return this.state;
+        },
+
+        setLevelExamQuestions(payload = {}) {
+            const questions = Array.isArray(payload.questions) ? payload.questions : [];
+            this.state = {
+                ...this.state,
+                levelExam: createLevelExamState({
+                    ...this.state.levelExam,
+                    title: payload.title || "Prova de nivel RotaNota",
+                    questions,
+                    questionCount: questions.length || this.state.levelExam.questionCount,
+                    started: false,
+                    index: 0,
+                    answers: [],
+                    isComplete: false,
+                    result: null,
+                    status: questions.length ? "ready" : "idle"
+                })
+            };
+
+            return this.state;
+        },
+
+        startLevelExam() {
+            this.state = {
+                ...this.state,
+                levelExam: createLevelExamState({
+                    ...this.state.levelExam,
+                    started: true,
+                    index: 0,
+                    answers: [],
+                    isComplete: false,
+                    result: null,
+                    status: "running"
+                })
+            };
+
+            return this.state;
+        },
+
+        setLevelExamAnswer(answerIndex) {
+            const levelExam = clone(this.state.levelExam);
+            levelExam.answers[levelExam.index] = answerIndex;
+            this.state = {
+                ...this.state,
+                levelExam
+            };
+
+            return this.state;
+        },
+
+        advanceLevelExam() {
+            const levelExam = clone(this.state.levelExam);
+            const questions = Array.isArray(levelExam.questions) ? levelExam.questions : [];
+
+            if (levelExam.index >= questions.length - 1) {
+                let correct = 0;
+                questions.forEach((question, index) => {
+                    if (levelExam.answers[index] === question.correctIndex) {
+                        correct += 1;
+                    }
+                });
+                levelExam.isComplete = true;
+                levelExam.started = false;
+                levelExam.status = "complete";
+                levelExam.result = {
+                    correct,
+                    total: questions.length,
+                    ratio: questions.length ? Math.round((correct / questions.length) * 100) : 0
+                };
+            } else {
+                levelExam.index += 1;
+            }
+
+            this.state = {
+                ...this.state,
+                levelExam
+            };
+
+            return this.state;
+        },
+
         setBlockTab(tab) {
             this.state = {
                 ...this.state,
@@ -1732,18 +2947,310 @@
                 ? this.getBlockById(blockId)
                 : this.getActiveBlock();
             const highlightedDocument =
-                buildHighlightedDocument(this.state, block);
+                refreshHighlightedDocumentState(
+                    buildHighlightedDocument(
+                        this.state,
+                        block
+                    ),
+                    this.state,
+                    block
+                );
 
             this.state = {
                 ...this.state,
                 highlightedDocument,
+                highlightEditorOpen: false,
+                highlightEditorFullScreen: false,
+                highlightInspectorMode: "",
                 activeBlockId: block ? block.id : this.state.activeBlockId,
-                progressLabel: "Documento marcado preparado. Agora você pode exportar o texto grifado ou só os destaques em PDF."
+                progressLabel: "Documento marcado preparado. Agora vocÃª pode exportar o texto grifado ou sÃ³ os destaques em PDF."
+            };
+
+            return this.state;
+        },
+
+        setHighlightEditorOpen(value) {
+            this.state = {
+                ...this.state,
+                highlightEditorOpen: Boolean(value)
+            };
+
+            return this.state;
+        },
+
+        setHighlightEditorFullScreen(value) {
+            this.state = {
+                ...this.state,
+                highlightEditorFullScreen: Boolean(value)
+            };
+
+            return this.state;
+        },
+
+        setHighlightInspectorMode(value) {
+            this.state = {
+                ...this.state,
+                highlightInspectorMode: String(value || "")
             };
 
             return this.state;
         },
 
+        updateHighlightedDocument(updater) {
+            const current =
+                this.state.highlightedDocument ||
+                buildHighlightedDocument(
+                    this.state,
+                    this.getActiveBlock()
+                );
+            const draft = clone(current);
+            const next =
+                typeof updater === "function"
+                    ? updater(draft) || draft
+                    : draft;
+            const normalized =
+                refreshHighlightedDocumentState(
+                    next,
+                    this.state
+                );
+
+            this.state = {
+                ...this.state,
+                highlightedDocument:
+                    normalized
+            };
+
+            return this.state;
+        },
+
+        setHighlightSelection(sectionIndex, paragraphIndex, partIndex) {
+            return this.updateHighlightedDocument((documentData) => {
+                const target =
+                    documentData.sections?.[Number(sectionIndex)]
+                        ?.paragraphs?.[Number(paragraphIndex)]
+                        ?.[Number(partIndex)];
+
+                documentData.selectedPartId =
+                    target?.id || "";
+                return documentData;
+            });
+        },
+
+        setHighlightTextSelection(sectionIndex, paragraphIndex, startOffset, endOffset) {
+            return this.updateHighlightedDocument((documentData) => {
+                const normalizedSectionIndex = Number(sectionIndex);
+                const normalizedParagraphIndex = Number(paragraphIndex);
+                const paragraph =
+                    documentData.sections?.[normalizedSectionIndex]
+                        ?.paragraphs?.[normalizedParagraphIndex];
+
+                if (!Array.isArray(paragraph)) {
+                    return documentData;
+                }
+
+                const selection =
+                    selectHighlightRangeFromParagraph(
+                        paragraph,
+                        startOffset,
+                        endOffset,
+                        documentData.activeColorKey
+                    );
+
+                documentData.sections[normalizedSectionIndex].paragraphs[normalizedParagraphIndex] =
+                    selection.paragraph;
+                documentData.selectedPartId =
+                    selection.selectedPartId || "";
+                return documentData;
+            });
+        },
+
+        updateSelectedHighlightText(text) {
+            return this.updateHighlightedDocument((documentData) => {
+                const selected =
+                    findSelectedHighlightPart(
+                        documentData
+                    );
+
+                if (!selected) {
+                    return documentData;
+                }
+
+                const nextText =
+                    String(text || "");
+
+                selected.part.text =
+                    nextText;
+                const paragraph =
+                    documentData.sections?.[selected.sectionIndex]
+                        ?.paragraphs?.[selected.paragraphIndex];
+
+                if (Array.isArray(paragraph)) {
+                    documentData.sections[selected.sectionIndex].paragraphs[selected.paragraphIndex] =
+                        normalizeHighlightParagraph(paragraph);
+                }
+
+                if (!nextText) {
+                    documentData.selectedPartId = "";
+                }
+
+                return documentData;
+            });
+        },
+
+        toggleSelectedHighlight(forceValue) {
+            return this.updateHighlightedDocument((documentData) => {
+                const selected =
+                    findSelectedHighlightPart(
+                        documentData
+                    );
+
+                if (!selected) {
+                    return documentData;
+                }
+
+                const nextValue =
+                    typeof forceValue === "boolean"
+                        ? forceValue
+                        : !selected.part.highlight;
+
+                selected.part.highlight =
+                    nextValue;
+                selected.part.colorKey =
+                    nextValue
+                        ? resolveHighlightColorKey(
+                            selected.part.colorKey ||
+                            documentData.activeColorKey
+                        )
+                        : "";
+                selected.part.styleMode =
+                    nextValue
+                        ? resolveHighlightStyleMode(
+                            selected.part.styleMode || "highlight"
+                        )
+                        : "highlight";
+
+                return documentData;
+            });
+        },
+
+        setSelectedHighlightStyleMode(styleMode) {
+            return this.updateHighlightedDocument((documentData) => {
+                const selected =
+                    findSelectedHighlightPart(
+                        documentData
+                    );
+
+                if (!selected) {
+                    return documentData;
+                }
+
+                selected.part.highlight = true;
+                selected.part.styleMode =
+                    resolveHighlightStyleMode(styleMode);
+                selected.part.colorKey =
+                    resolveHighlightColorKey(
+                        selected.part.colorKey ||
+                        documentData.activeColorKey
+                    );
+                return documentData;
+            });
+        },
+
+        setHighlightColor(colorKey) {
+            const normalizedColor =
+                resolveHighlightColorKey(
+                    colorKey
+                );
+
+            return this.updateHighlightedDocument((documentData) => {
+                documentData.activeColorKey =
+                    normalizedColor;
+                const selected =
+                    findSelectedHighlightPart(
+                        documentData
+                    );
+
+                if (selected) {
+                    selected.part.highlight = true;
+                    selected.part.colorKey =
+                        normalizedColor;
+                }
+
+                return documentData;
+            });
+        },
+
+        clearAllHighlights() {
+            return this.updateHighlightedDocument((documentData) => {
+                (documentData.sections || []).forEach((section) => {
+                    (section.paragraphs || []).forEach((paragraph) => {
+                        (paragraph || []).forEach((part) => {
+                            part.highlight = false;
+                            part.colorKey = "";
+                            part.styleMode = "highlight";
+                        });
+                    });
+                });
+
+                return documentData;
+            });
+        },
+
+        updateSelectedHighlightNote(note) {
+            return this.updateHighlightedDocument((documentData) => {
+                const selected =
+                    findSelectedHighlightPart(
+                        documentData
+                    );
+
+                if (!selected) {
+                    return documentData;
+                }
+
+                selected.part.note =
+                    String(note || "").trim();
+                return documentData;
+            });
+        },
+
+        deleteSelectedHighlightText() {
+            return this.updateHighlightedDocument((documentData) => {
+                const selected =
+                    findSelectedHighlightPart(
+                        documentData
+                    );
+
+                if (!selected) {
+                    return documentData;
+                }
+
+                const paragraph =
+                    documentData.sections?.[selected.sectionIndex]
+                        ?.paragraphs?.[selected.paragraphIndex];
+
+                if (!Array.isArray(paragraph)) {
+                    return documentData;
+                }
+
+                const nextParagraph =
+                    paragraph.filter((part) => part.id !== selected.part.id);
+
+                documentData.sections[selected.sectionIndex].paragraphs[selected.paragraphIndex] =
+                    normalizeHighlightParagraph(nextParagraph);
+                documentData.selectedPartId = "";
+                return documentData;
+            });
+        },
+
+        restoreOriginalHighlightedDocument() {
+            return this.updateHighlightedDocument((documentData) => {
+                documentData.sections =
+                    clone(documentData.originalSections || []);
+                documentData.selectedPartId = "";
+                return documentData;
+            });
+        },
+
         setSavedSummaries(items) {
             const summaries = Array.isArray(items)
                 ? items
@@ -1914,11 +3421,10 @@
                 seeds.quiz.freeSeriesUsed = options.allSeries ? 1 : current?.freeSeriesUsed || 1;
                 seeds.quiz.completedSeries = options.allSeries
                     ? []
-                    : normalizeCompletedSeries("quiz", current).filter((index) => index !== seeds.quiz.currentSeriesIndex);
+                    : normalizeCompletedSeries("quiz", current);
                 seeds.quiz.seriesSnapshots = options.allSeries
                     ? {}
                     : { ...(current?.seriesSnapshots || {}) };
-                delete seeds.quiz.seriesSnapshots[seeds.quiz.currentSeriesIndex];
             }
             if (type === "trueFalse") {
                 const current = this.getActiveSession("trueFalse");
@@ -1926,11 +3432,10 @@
                 seeds.trueFalse.freeSeriesUsed = options.allSeries ? 1 : current?.freeSeriesUsed || 1;
                 seeds.trueFalse.completedSeries = options.allSeries
                     ? []
-                    : normalizeCompletedSeries("trueFalse", current).filter((index) => index !== seeds.trueFalse.currentSeriesIndex);
+                    : normalizeCompletedSeries("trueFalse", current);
                 seeds.trueFalse.seriesSnapshots = options.allSeries
                     ? {}
                     : { ...(current?.seriesSnapshots || {}) };
-                delete seeds.trueFalse.seriesSnapshots[seeds.trueFalse.currentSeriesIndex];
             }
             if (type === "flashcards") {
                 const current = this.getActiveSession("flashcards");
@@ -1938,11 +3443,10 @@
                 seeds.flashcards.freeSeriesUsed = options.allSeries ? 1 : current?.freeSeriesUsed || 1;
                 seeds.flashcards.completedSeries = options.allSeries
                     ? []
-                    : normalizeCompletedSeries("flashcards", current).filter((index) => index !== seeds.flashcards.currentSeriesIndex);
+                    : normalizeCompletedSeries("flashcards", current);
                 seeds.flashcards.seriesSnapshots = options.allSeries
                     ? {}
                     : { ...(current?.seriesSnapshots || {}) };
-                delete seeds.flashcards.seriesSnapshots[seeds.flashcards.currentSeriesIndex];
             }
             return this.updateActiveSession(type, seeds[type]);
         },
@@ -2210,12 +3714,18 @@
                 accessTier: this.state.accessTier,
                 subscriptionStatus: this.state.subscriptionStatus,
                 customerId: this.state.customerId,
+                accountUser: this.state.accountUser,
+                accountAuthenticated: this.state.accountAuthenticated,
                 premiumEntitlement: this.state.premiumEntitlement,
                 studyTitle: this.state.studyTitle,
                 studyLibraryId: this.state.studyLibraryId,
                 materialName: this.state.materialName,
+                materialHash: this.state.materialHash,
+                materialFileKey: this.state.materialFileKey,
                 materialSizeLabel: this.state.materialSizeLabel,
                 materialPageCount: this.state.materialPageCount,
+                materialExtractedText: this.state.materialExtractedText,
+                materialExtractionStatus: this.state.materialExtractionStatus,
                 examDate: this.state.examDate,
                 targetScore: this.state.targetScore,
                 studyHours: this.state.studyHours,
@@ -2225,7 +3735,15 @@
                 activeBlockId: this.state.activeBlockId,
                 blockTab: this.state.blockTab,
                 blockFullScreen: this.state.blockFullScreen,
+                highlightEditorOpen: this.state.highlightEditorOpen,
+                highlightEditorFullScreen: this.state.highlightEditorFullScreen,
+                highlightInspectorMode: this.state.highlightInspectorMode,
                 blockAssistMode: this.state.blockAssistMode,
+                aiGeneration: this.state.aiGeneration,
+                levelExam: clone(this.state.levelExam),
+                highlightedDocument: clone(this.state.highlightedDocument),
+                savedSummaries: clone(this.state.savedSummaries),
+                activeSavedSummaryId: this.state.activeSavedSummaryId,
                 savedDraftId: this.state.savedDraftId,
                 savedAt: this.state.savedAt,
                 premiumOffer: this.state.premiumOffer,
@@ -2243,8 +3761,14 @@
                 accessTier: this.state.accessTier,
                 subscriptionStatus: this.state.subscriptionStatus,
                 customerId: this.state.customerId,
+                accountUser: this.state.accountUser,
+                accountAuthenticated: this.state.accountAuthenticated,
                 premiumEntitlement: this.state.premiumEntitlement,
-                premiumStatusConfigured: this.state.premiumStatusConfigured
+                premiumStatusConfigured: this.state.premiumStatusConfigured,
+                generationPaused: this.state.generationPaused,
+                opsLanes: this.state.opsLanes,
+                opsThresholds: this.state.opsThresholds,
+                trialState: this.state.trialState
             };
             const studyTitle = snapshot.studyTitle || buildStudyTitle(snapshot.materialName);
             const materialLabel = studyTitle || "seu material";
@@ -2263,14 +3787,43 @@
                 step: normalizedStep,
                 studyTitle,
                 studyLibraryId: snapshot.studyLibraryId || defaults.studyLibraryId,
+                materialHash: snapshot.materialHash || defaults.materialHash,
+                materialFileKey: snapshot.materialFileKey || defaults.materialFileKey,
                 blocks,
                 sessions,
                 activeBlockId: snapshot.activeBlockId || blocks[0].id,
                 blockFullScreen: typeof snapshot.blockFullScreen === "boolean"
                     ? snapshot.blockFullScreen
                     : defaults.blockFullScreen,
+                highlightEditorOpen: typeof snapshot.highlightEditorOpen === "boolean"
+                    ? snapshot.highlightEditorOpen
+                    : defaults.highlightEditorOpen,
+                highlightEditorFullScreen: typeof snapshot.highlightEditorFullScreen === "boolean"
+                    ? snapshot.highlightEditorFullScreen
+                    : defaults.highlightEditorFullScreen,
+                highlightInspectorMode: snapshot.highlightInspectorMode || defaults.highlightInspectorMode,
                 blockAssistMode: snapshot.blockAssistMode || defaults.blockAssistMode,
-                highlightedDocument: snapshot.highlightedDocument || null,
+                aiGeneration: snapshot.aiGeneration || defaults.aiGeneration,
+                levelExam: createLevelExamState(snapshot.levelExam || {}),
+                materialExtractionStatus: snapshot.materialExtractionStatus || defaults.materialExtractionStatus,
+                materialExtractedText: snapshot.materialExtractedText || "",
+                highlightedDocument: snapshot.highlightedDocument
+                    ? refreshHighlightedDocumentState(
+                        clone(snapshot.highlightedDocument),
+                        {
+                            ...defaults,
+                            ...snapshot,
+                            studyTitle,
+                            blocks,
+                            activeBlockId:
+                                snapshot.activeBlockId ||
+                                blocks[0].id,
+                            materialExtractedText:
+                                snapshot.materialExtractedText ||
+                                ""
+                        }
+                    )
+                    : null,
                 savedSummaries: Array.isArray(snapshot.savedSummaries) ? snapshot.savedSummaries : defaults.savedSummaries,
                 activeSavedSummaryId: snapshot.activeSavedSummaryId || defaults.activeSavedSummaryId,
                 studyLibrary: this.state.studyLibrary,
diff --git a/premium-study/storage/indexeddb.js b/premium-study/storage/indexeddb.js
index 3811055..92d5b40 100644
--- a/premium-study/storage/indexeddb.js
+++ b/premium-study/storage/indexeddb.js
@@ -9,8 +9,13 @@
     const LATEST_KEY = "latest";
     const SAVED_SUMMARIES_KEY = "saved-summaries";
     const STUDY_LIBRARY_KEY = "study-library";
+    const MATERIAL_FILE_PREFIX = "material-file:";
     let openPromise = null;
 
+    function buildMaterialFileRecordKey(fileKey) {
+        return `${MATERIAL_FILE_PREFIX}${String(fileKey || "").trim()}`;
+    }
+
     function requestToPromise(request) {
         return new Promise((resolve, reject) => {
             request.addEventListener("success", () => resolve(request.result), { once: true });
@@ -99,8 +104,12 @@
             studyTitle: snapshot.studyTitle || "",
             studyLibraryId: snapshot.studyLibraryId || "",
             materialName: snapshot.materialName || "",
+            materialHash: snapshot.materialHash || "",
+            materialFileKey: snapshot.materialFileKey || "",
             materialSizeLabel: snapshot.materialSizeLabel || "",
             materialPageCount: snapshot.materialPageCount || 0,
+            materialExtractedText: snapshot.materialExtractedText || "",
+            materialExtractionStatus: snapshot.materialExtractionStatus || "pending",
             examDate: snapshot.examDate || "",
             targetScore: snapshot.targetScore || 7,
             studyHours: snapshot.studyHours || 1,
@@ -112,10 +121,29 @@
             activeBlockId: snapshot.activeBlockId || "",
             blockTab: snapshot.blockTab || "aprender",
             blockFullScreen: Boolean(snapshot.blockFullScreen),
+            highlightEditorOpen: Boolean(snapshot.highlightEditorOpen),
+            highlightEditorFullScreen: Boolean(snapshot.highlightEditorFullScreen),
+            highlightInspectorMode: snapshot.highlightInspectorMode || "",
             blockAssistMode: snapshot.blockAssistMode || "",
+            levelExam: snapshot.levelExam && typeof snapshot.levelExam === "object"
+                ? snapshot.levelExam
+                : null,
+            highlightedDocument: snapshot.highlightedDocument && typeof snapshot.highlightedDocument === "object"
+                ? snapshot.highlightedDocument
+                : null,
+            savedSummaries: Array.isArray(snapshot.savedSummaries)
+                ? snapshot.savedSummaries
+                : [],
+            activeSavedSummaryId: snapshot.activeSavedSummaryId || "",
             savedDraftId: snapshot.savedDraftId || "",
             savedAt: snapshot.savedAt || "",
-            progressLabel: snapshot.progressLabel || ""
+            progressLabel: snapshot.progressLabel || "",
+            sessionNote: snapshot.sessionNote && typeof snapshot.sessionNote === "object"
+                ? snapshot.sessionNote
+                : null,
+            premiumOffer: snapshot.premiumOffer && typeof snapshot.premiumOffer === "object"
+                ? snapshot.premiumOffer
+                : null
         };
     }
 
@@ -208,6 +236,69 @@
             }
         },
 
+        async saveMaterialFile(fileKey, file, metadata = {}) {
+            const normalizedKey = String(fileKey || "").trim();
+            if (!normalizedKey || !file) {
+                return null;
+            }
+
+            const record = {
+                id: buildMaterialFileRecordKey(normalizedKey),
+                fileKey: normalizedKey,
+                savedAt: new Date().toISOString(),
+                name: metadata.name || file.name || "material.pdf",
+                type: metadata.type || file.type || "application/pdf",
+                size: Number(metadata.size ?? file.size ?? 0),
+                lastModified: Number(metadata.lastModified ?? file.lastModified ?? Date.now()),
+                file
+            };
+
+            try {
+                await withStore("readwrite", (store) => requestToPromise(store.put(record)));
+                return record;
+            } catch (error) {
+                console.error(error);
+                return null;
+            }
+        },
+
+        async getMaterialFile(fileKey) {
+            const normalizedKey = String(fileKey || "").trim();
+            if (!normalizedKey) {
+                return null;
+            }
+
+            try {
+                const record = await withStore(
+                    "readonly",
+                    (store) => requestToPromise(store.get(buildMaterialFileRecordKey(normalizedKey)))
+                );
+                if (!record || !record.file) {
+                    return null;
+                }
+
+                if (record.file instanceof File) {
+                    return record.file;
+                }
+
+                if (record.file instanceof Blob) {
+                    return new File(
+                        [record.file],
+                        record.name || "material.pdf",
+                        {
+                            type: record.type || record.file.type || "application/pdf",
+                            lastModified: Number(record.lastModified || Date.now())
+                        }
+                    );
+                }
+
+                return null;
+            } catch (error) {
+                console.error(error);
+                return null;
+            }
+        },
+
         async getStudyLibrary() {
             try {
                 const record = await withStore("readonly", (store) => requestToPromise(store.get(STUDY_LIBRARY_KEY)));
diff --git a/premium-study/styles/premium-study.css b/premium-study/styles/premium-study.css
index f89482f..3acd568 100644
--- a/premium-study/styles/premium-study.css
+++ b/premium-study/styles/premium-study.css
@@ -133,10 +133,10 @@
     display: inline-flex;
     align-items: center;
     min-height: 46px;
-    color: var(--premium-accent-bright);
-    font-size: 0.82rem;
+    color: rgba(121, 213, 255, 0.82);
+    font-size: 0.74rem;
     font-weight: 700;
-    letter-spacing: 0.08em;
+    letter-spacing: 0.11em;
     text-transform: uppercase;
 }
 
@@ -201,8 +201,8 @@
 }
 
 .premium-stage-heading {
-    max-width: 700px;
-    margin-bottom: 14px;
+    max-width: 760px;
+    margin-bottom: 16px;
 }
 
 .premium-shell-scrollable .premium-study-shell-body {
@@ -239,20 +239,21 @@
     display: inline-flex;
     align-items: center;
     gap: 8px;
-    margin-bottom: 12px;
-    color: var(--premium-accent-bright);
-    font-size: 0.82rem;
+    margin-bottom: 10px;
+    color: rgba(121, 213, 255, 0.84);
+    font-size: 0.72rem;
     font-weight: 700;
-    letter-spacing: 0.08em;
+    letter-spacing: 0.12em;
     text-transform: uppercase;
 }
 
 .premium-stage-heading h1,
 .premium-block-hero h2 {
-    margin: 0;
+    margin: 0 0 8px;
     font-family: "Space Grotesk", "Manrope", sans-serif;
-    font-size: clamp(1.8rem, 3.2vw, 2.6rem);
-    line-height: 1.02;
+    max-width: 14ch;
+    font-size: clamp(1.68rem, 2.7vw, 2.32rem);
+    line-height: 1.03;
     letter-spacing: -0.04em;
 }
 
@@ -264,12 +265,16 @@
 .premium-mode-card p,
 .premium-block-tool p,
 .premium-tab-panel p,
-.premium-block-card p,
-.premium-entry-note span {
+.premium-block-card p {
     margin: 0;
     color: var(--premium-muted);
     font-size: 0.92rem;
-    line-height: 1.42;
+    line-height: 1.46;
+}
+
+.premium-stage-heading p {
+    max-width: 52ch;
+    color: rgba(228, 231, 247, 0.8);
 }
 
 .premium-entry-grid,
@@ -284,7 +289,7 @@
 
 .premium-entry-stage {
     display: grid;
-    gap: 18px;
+    gap: 16px;
 }
 
 .premium-entry-hero {
@@ -343,7 +348,7 @@
 
 .premium-entry-card {
     display: grid;
-    gap: 8px;
+    gap: 10px;
     align-content: start;
     min-height: 156px;
     color: var(--premium-ink);
@@ -362,52 +367,55 @@
 
 .premium-entry-card-primary {
     background:
-        radial-gradient(circle at top left, rgba(121, 213, 255, 0.12), transparent 34%),
-        linear-gradient(180deg, rgba(95, 114, 255, 0.18), rgba(255, 255, 255, 0.04));
-    border-color: rgba(121, 213, 255, 0.26);
+        linear-gradient(135deg, rgba(255, 255, 255, 0.38) 0 12%, transparent 12% 100%),
+        radial-gradient(circle at top left, rgba(64, 168, 255, 0.08), transparent 26%),
+        linear-gradient(180deg, #f3efea 0%, #e5ded7 100%);
+    border-color: rgba(194, 184, 174, 0.7);
+    color: #241e24;
+    box-shadow:
+        inset 0 1px 0 rgba(255, 255, 255, 0.62),
+        inset 0 -20px 34px rgba(102, 84, 64, 0.035),
+        0 16px 30px rgba(5, 12, 24, 0.12);
 }
 
 .premium-entry-card-secondary {
-    background: var(--premium-surface-soft);
+    background:
+        linear-gradient(135deg, rgba(255, 255, 255, 0.34) 0 12%, transparent 12% 100%),
+        linear-gradient(180deg, #f0ebe6 0%, #e1dad3 100%);
+    border-color: rgba(190, 180, 170, 0.58);
+    color: #241e24;
+    box-shadow:
+        inset 0 1px 0 rgba(255, 255, 255, 0.56),
+        inset 0 -14px 26px rgba(88, 74, 57, 0.03),
+        0 12px 22px rgba(6, 12, 22, 0.08);
 }
 
 .premium-entry-card-resume {
     background:
-        radial-gradient(circle at top left, rgba(93, 182, 255, 0.12), transparent 30%),
-        linear-gradient(180deg, rgba(93, 182, 255, 0.1), rgba(255, 255, 255, 0.04));
-    border-color: rgba(93, 182, 255, 0.26);
+        radial-gradient(circle at top left, rgba(93, 182, 255, 0.08), transparent 26%),
+        linear-gradient(180deg, #f3eee7 0%, #e7e0d8 100%);
+    border-color: rgba(168, 198, 227, 0.54);
 }
 
 .premium-entry-card-premium {
     background:
-        radial-gradient(circle at top right, rgba(255, 194, 76, 0.18), transparent 34%),
-        linear-gradient(180deg, rgba(255, 194, 76, 0.12), rgba(255, 255, 255, 0.04));
-    border-color: rgba(255, 194, 76, 0.32);
+        radial-gradient(circle at top right, rgba(217, 167, 75, 0.14), transparent 30%),
+        linear-gradient(135deg, rgba(255, 255, 255, 0.28) 0 11%, transparent 11% 100%),
+        linear-gradient(180deg, #eee5d9 0%, #ddd1c2 100%);
+    border-color: rgba(214, 173, 97, 0.46);
+    color: #251f20;
+    box-shadow:
+        inset 0 1px 0 rgba(255, 247, 230, 0.56),
+        inset 0 -16px 28px rgba(148, 105, 43, 0.035),
+        0 14px 28px rgba(8, 12, 20, 0.12);
 }
 
 .premium-entry-card.is-locked {
-    border-style: dashed;
-    opacity: 0.88;
+    opacity: 0.96;
 }
 
 .premium-entry-card.is-locked::after {
-    content: "Premium";
-    position: absolute;
-    top: 18px;
-    right: 18px;
-    display: inline-flex;
-    align-items: center;
-    min-height: 28px;
-    padding: 0 12px;
-    border-radius: var(--premium-radius-chip);
-    border: 1px solid rgba(255, 203, 109, 0.24);
-    background: rgba(255, 203, 109, 0.12);
-    color: rgba(255, 224, 157, 0.92);
-    font-size: 0.72rem;
-    font-weight: 700;
-    letter-spacing: 0.08em;
-    text-transform: uppercase;
-    box-shadow: 0 10px 20px rgba(255, 203, 109, 0.16);
+    content: none;
 }
 
 .premium-entry-card-premium.is-locked::after {
@@ -445,117 +453,126 @@
 }
 
 .premium-entry-card-featured {
-    width: min(100%, 560px);
-    min-height: 210px;
+    width: min(100%, 620px);
+    min-height: 168px;
     justify-items: start;
-    align-content: center;
-    padding: clamp(22px, 2.8vw, 30px);
+    align-content: start;
+    padding: clamp(20px, 2.4vw, 26px);
 }
 
 .premium-entry-card-featured strong {
-    font-size: clamp(1.74rem, 3.5vw, 2.4rem);
+    font-size: clamp(1.72rem, 3vw, 2.28rem);
+    line-height: 1.02;
 }
 
 .premium-entry-card-featured p {
-    max-width: 30ch;
-    font-size: 0.92rem;
-    line-height: 1.42;
+    max-width: 34ch;
+    color: rgba(36, 30, 36, 0.72);
+    font-size: 0.96rem;
+    line-height: 1.48;
 }
 
 .premium-entry-card-support {
-    min-height: 104px;
-    max-height: 104px;
-    align-content: center;
-    padding: 14px 16px;
-    gap: 6px;
+    grid-template-rows: auto auto minmax(0, 1fr) auto;
+    min-height: 118px;
+    max-height: none;
+    align-content: start;
+    padding: 16px 18px;
+    gap: 8px;
 }
 
 .premium-entry-card-support strong {
-    font-size: clamp(1rem, 1.5vw, 1.24rem);
+    font-size: clamp(1.02rem, 1.4vw, 1.18rem);
+    line-height: 1.16;
+}
+
+.premium-entry-topline {
+    display: flex;
+    align-items: center;
+    justify-content: space-between;
+    gap: 10px;
+    flex-wrap: wrap;
 }
 
 .premium-entry-inline-badge {
     display: inline-flex;
     align-items: center;
     min-height: 24px;
-    padding: 0 10px;
+    padding: 0 9px;
     border-radius: var(--premium-radius-chip);
-    font-size: 0.66rem;
+    font-size: 0.62rem;
     font-weight: 800;
-    letter-spacing: 0.08em;
+    letter-spacing: 0.1em;
     text-transform: uppercase;
     white-space: nowrap;
 }
 
 .premium-entry-inline-badge-free {
-    border: 1px solid rgba(93, 182, 255, 0.28);
-    background: rgba(93, 182, 255, 0.16);
-    color: #4dafff;
+    border: 1px solid rgba(96, 167, 219, 0.24);
+    background: rgba(96, 167, 219, 0.08);
+    color: #4a9ddf;
 }
 
 .premium-entry-inline-badge-premium {
-    border: 1px solid rgba(255, 194, 76, 0.34);
-    background: rgba(255, 194, 76, 0.18);
-    color: #d5961a;
-    box-shadow: 0 10px 20px rgba(255, 194, 76, 0.16);
+    border: 1px solid rgba(214, 173, 97, 0.28);
+    background: rgba(214, 173, 97, 0.14);
+    color: #dfb46b;
+    box-shadow: none;
+}
+
+.premium-entry-inline-badge-active {
+    border: 1px solid rgba(49, 215, 163, 0.26);
+    background: rgba(49, 215, 163, 0.12);
+    color: #2dbb8e;
+    box-shadow: none;
 }
 
 .premium-entry-card p {
-    max-width: 26ch;
+    max-width: 34ch;
 }
 
 .premium-entry-card-support p {
-    overflow: hidden;
-    display: -webkit-box;
-    max-width: 32ch;
-    -webkit-line-clamp: 1;
-    -webkit-box-orient: vertical;
-    font-size: 0.82rem;
-    line-height: 1.25;
+    max-width: 34ch;
+    color: rgba(36, 30, 36, 0.74);
+    font-size: 0.84rem;
+    line-height: 1.32;
 }
 
 .premium-entry-card-support .premium-entry-kicker {
-    margin-bottom: 4px;
+    margin-bottom: 0;
 }
 
 .premium-entry-card-support small {
-    align-self: end;
+    align-self: start;
 }
 
 .premium-entry-card .premium-entry-kicker {
-    color: #64c3ff;
+    margin: 0;
+    color: #3896d7;
+    font-size: 0.74rem;
+    letter-spacing: 0.12em;
 }
 
 .premium-entry-card-premium .premium-entry-kicker {
-    color: #dca02f;
+    color: #ddb66f;
+}
+
+.premium-entry-card-premium strong {
+    color: #241e24;
 }
 
 .premium-entry-card small {
-    color: #3dafff;
-    font-size: 0.8rem;
+    color: #2e76bb;
+    font-size: 0.78rem;
     font-weight: 700;
 }
 
 .premium-entry-card-premium small {
-    color: #dca02f;
-}
-
-.premium-entry-note {
-    display: flex;
-    flex-wrap: wrap;
-    gap: 8px;
-    margin-top: 12px;
+    color: #bd8424;
 }
 
-.premium-entry-note span {
-    display: inline-flex;
-    align-items: center;
-    min-height: 34px;
-    padding: 0 12px;
-    border: 1px solid rgba(255, 255, 255, 0.08);
-    border-radius: var(--premium-radius-chip);
-    background: rgba(255, 255, 255, 0.03);
-    font-size: 0.92rem;
+.premium-entry-card-premium p {
+    color: rgba(36, 30, 36, 0.74);
 }
 
 .premium-hidden-input {
@@ -1774,6 +1791,227 @@
     gap: 14px;
 }
 
+.premium-highlight-shell-preview {
+    max-width: 880px;
+}
+
+.premium-highlight-preview-card {
+    gap: 14px;
+}
+
+.premium-highlight-preview-actions {
+    display: grid;
+    grid-template-columns: repeat(3, minmax(0, 1fr));
+    gap: 12px;
+}
+
+.premium-highlight-reader {
+    position: relative;
+    width: 100%;
+}
+
+.premium-highlight-reader.is-fullscreen {
+    position: fixed;
+    inset: 0;
+    z-index: 80;
+    display: grid;
+    place-items: center;
+    padding: 34px;
+}
+
+.premium-highlight-reader-scrim {
+    position: absolute;
+    inset: 0;
+    background: rgba(4, 7, 14, 0.78);
+    backdrop-filter: blur(8px);
+}
+
+.premium-highlight-reader-panel {
+    position: relative;
+    z-index: 1;
+    display: grid;
+    gap: 18px;
+}
+
+.premium-highlight-reader.is-fullscreen .premium-highlight-reader-panel {
+    width: min(100%, 1380px);
+    max-height: calc(100vh - 68px);
+    padding: 26px 28px 24px;
+    border: 1px solid rgba(255, 255, 255, 0.08);
+    border-radius: var(--premium-radius-shell);
+    background:
+        radial-gradient(circle at top center, rgba(95, 114, 255, 0.12), transparent 28%),
+        linear-gradient(180deg, rgba(18, 22, 34, 0.98), rgba(9, 12, 20, 0.98));
+    box-shadow: 0 40px 100px rgba(0, 0, 0, 0.46);
+}
+
+.premium-study-host:fullscreen .premium-highlight-reader.is-fullscreen {
+    position: relative;
+    inset: auto;
+    display: block;
+    padding: 0;
+}
+
+.premium-study-host:fullscreen .premium-highlight-reader-scrim {
+    display: none;
+}
+
+.premium-study-host:fullscreen .premium-highlight-reader.is-fullscreen .premium-highlight-reader-panel {
+    width: 100%;
+    max-width: none;
+    max-height: none;
+    min-height: calc(100vh - 132px);
+    padding: 0;
+    border: 0;
+    border-radius: 0;
+    background: transparent;
+    box-shadow: none;
+}
+
+.premium-highlight-reader-head {
+    display: grid;
+    gap: 8px;
+    justify-items: center;
+    text-align: center;
+}
+
+.premium-highlight-reader-copy {
+    display: grid;
+    gap: 8px;
+    justify-items: center;
+}
+
+.premium-highlight-reader-copy h2 {
+    margin: 0;
+    font-size: clamp(1.8rem, 3vw, 2.4rem);
+    line-height: 0.98;
+}
+
+.premium-highlight-document {
+    display: grid;
+    grid-template-columns: 240px minmax(0, 1fr);
+    gap: 18px;
+    align-items: start;
+}
+
+.premium-highlight-reader.is-fullscreen .premium-highlight-document {
+    min-height: min(78vh, 980px);
+    max-height: min(78vh, 980px);
+    overflow: hidden;
+}
+
+.premium-study-host:fullscreen .premium-highlight-reader.is-fullscreen .premium-highlight-document {
+    min-height: calc(100vh - 210px);
+    max-height: calc(100vh - 210px);
+}
+
+.premium-highlight-toolbar {
+    position: sticky;
+    top: 0;
+    z-index: 4;
+    display: grid;
+    gap: 10px;
+    padding: 14px;
+    border: 1px solid rgba(121, 213, 255, 0.14);
+    border-radius: 12px;
+    background:
+        linear-gradient(180deg, rgba(12, 16, 27, 0.96), rgba(12, 16, 27, 0.9)),
+        rgba(9, 12, 19, 0.92);
+    backdrop-filter: blur(18px);
+}
+
+.premium-highlight-sidebar {
+    position: relative;
+}
+
+.premium-highlight-return {
+    margin-bottom: 12px;
+    width: 100%;
+    border-radius: 12px;
+}
+
+.premium-highlight-toolbar-copy {
+    display: grid;
+    gap: 6px;
+}
+
+.premium-highlight-toolbar-copy strong,
+.premium-highlight-toolbar-copy p {
+    margin: 0;
+}
+
+.premium-highlight-toolbar-copy p {
+    color: var(--premium-muted);
+    font-size: 0.86rem;
+    line-height: 1.42;
+}
+
+.premium-highlight-toolrail {
+    display: grid;
+    gap: 8px;
+}
+
+.premium-highlight-toolrail .premium-action {
+    width: 100%;
+    justify-content: center;
+    min-height: 40px;
+    padding: 0 10px;
+    border-radius: 10px;
+    font-size: 0.9rem;
+}
+
+.premium-highlight-inline-actions {
+    display: grid;
+    gap: 8px;
+}
+
+.premium-highlight-canvas {
+    min-width: 0;
+    min-height: 0;
+    display: grid;
+    overflow: auto;
+    padding-right: 8px;
+    scrollbar-width: thin;
+    scrollbar-color: rgba(121, 213, 255, 0.3) transparent;
+}
+
+.premium-highlight-canvas::-webkit-scrollbar {
+    width: 8px;
+}
+
+.premium-highlight-canvas::-webkit-scrollbar-track {
+    background: transparent;
+}
+
+.premium-highlight-canvas::-webkit-scrollbar-thumb {
+    border-radius: var(--premium-radius-pill);
+    background: rgba(121, 213, 255, 0.24);
+}
+
+.premium-highlight-paper {
+    width: min(100%, 101ch);
+    min-height: 100%;
+    margin: 0 auto;
+    padding: 34px 38px 42px;
+    border-radius: 12px;
+    border: 1px solid rgba(22, 32, 45, 0.12);
+    background: #ffffff;
+    box-shadow: 0 24px 64px rgba(0, 0, 0, 0.22);
+}
+
+.premium-highlight-paper-head {
+    display: grid;
+    gap: 6px;
+    padding-bottom: 18px;
+    border-bottom: 1px solid rgba(22, 32, 45, 0.08);
+}
+
+.premium-highlight-paper-head strong {
+    color: #182434;
+    font-size: 1.12rem;
+    line-height: 1.24;
+}
+
 .premium-highlight-note {
     display: grid;
     gap: 6px;
@@ -1807,6 +2045,10 @@
         rgba(255, 255, 255, 0.035);
 }
 
+.premium-highlight-tools {
+    gap: 12px;
+}
+
 .premium-highlight-export strong {
     margin: 0;
     font-size: 0.98rem;
@@ -1845,13 +2087,205 @@
     color: var(--premium-ink);
     font-size: 0.92rem;
     line-height: 1.58;
+    user-select: text;
 }
 
-.premium-highlight-mark {
-    padding: 0 0.16em;
-    border-radius: 0.22em;
+.premium-highlight-piece {
+    border-radius: 0.14em;
+    transition: background 0.18s ease, box-shadow 0.18s ease;
+}
+
+.premium-highlight-piece.is-selected {
+    box-shadow: 0 0 0 1px rgba(121, 213, 255, 0.34);
+    background: rgba(121, 213, 255, 0.08);
+}
+
+.premium-highlight-piece.is-boxed {
+    border-radius: 0.14em;
+}
+
+.premium-highlight-mark {
+    padding: 0 0.16em;
+    border-radius: 0.14em;
+    background: rgba(255, 203, 109, 0.42);
+    color: var(--premium-ink);
+}
+
+.premium-highlight-mark-box {
+    border: 1px solid currentColor;
+    background: transparent;
+    box-shadow: inset 0 0 0 1px rgba(0, 0, 0, 0.04);
+}
+
+.premium-highlight-mark-gold {
     background: rgba(255, 203, 109, 0.42);
+}
+
+.premium-highlight-mark-mint {
+    background: rgba(88, 227, 183, 0.34);
+}
+
+.premium-highlight-mark-blue {
+    background: rgba(121, 213, 255, 0.32);
+}
+
+.premium-highlight-mark-rose {
+    background: rgba(255, 151, 188, 0.34);
+}
+
+.premium-highlight-editor-label {
+    color: var(--premium-muted);
+    font-size: 0.76rem;
+    font-weight: 700;
+    letter-spacing: 0.06em;
+    text-transform: uppercase;
+}
+
+.premium-highlight-editor-field {
+    width: 100%;
+    min-height: 112px;
+    padding: 12px 14px;
+    border: 1px solid rgba(121, 213, 255, 0.14);
+    border-radius: 10px;
+    background: rgba(9, 12, 19, 0.54);
     color: var(--premium-ink);
+    font: inherit;
+    line-height: 1.5;
+    resize: vertical;
+}
+
+.premium-highlight-swatch-row {
+    display: flex;
+    flex-wrap: wrap;
+    gap: 10px;
+}
+
+.premium-highlight-swatch {
+    width: 36px;
+    height: 36px;
+    padding: 0;
+    display: inline-flex;
+    align-items: center;
+    justify-content: center;
+    border: 1px solid rgba(255, 255, 255, 0.12);
+    border-radius: 10px;
+    background: rgba(255, 255, 255, 0.04);
+    cursor: pointer;
+}
+
+.premium-highlight-swatch.is-active {
+    border-color: rgba(121, 213, 255, 0.44);
+    box-shadow: 0 0 0 2px rgba(121, 213, 255, 0.12);
+}
+
+.premium-highlight-swatch-fill {
+    width: 18px;
+    height: 18px;
+    border-radius: 999px;
+}
+
+.premium-highlight-swatch-fill-gold {
+    background: rgba(255, 203, 109, 0.88);
+}
+
+.premium-highlight-swatch-fill-mint {
+    background: rgba(88, 227, 183, 0.88);
+}
+
+.premium-highlight-swatch-fill-blue {
+    background: rgba(121, 213, 255, 0.88);
+}
+
+.premium-highlight-swatch-fill-rose {
+    background: rgba(255, 151, 188, 0.88);
+}
+
+.premium-highlight-reader.is-page .premium-highlight-document {
+    max-width: 1040px;
+}
+
+.premium-highlight-paper .premium-highlight-doc {
+    gap: 10px;
+    padding: 0;
+    border: 0;
+    border-radius: 0;
+    background: transparent;
+    box-shadow: none;
+    overflow: visible;
+}
+
+.premium-highlight-paper .premium-highlight-section {
+    padding: 22px 0 0;
+    border-top: 1px solid rgba(22, 32, 45, 0.08);
+}
+
+.premium-highlight-paper .premium-highlight-section:first-of-type {
+    padding-top: 22px;
+}
+
+.premium-highlight-paper .premium-detail-label {
+    color: #5d7894;
+}
+
+.premium-highlight-paper .premium-highlight-section h3,
+.premium-highlight-paper .premium-highlight-paragraph {
+    color: #223246;
+}
+
+.premium-highlight-paper .premium-highlight-piece.is-selected {
+    box-shadow: 0 0 0 1px rgba(39, 127, 255, 0.28);
+    background: rgba(39, 127, 255, 0.08);
+}
+
+.premium-highlight-paper .premium-highlight-mark {
+    color: #1b2735;
+}
+
+.premium-highlight-note-badge {
+    display: inline-flex;
+    align-items: center;
+    justify-content: center;
+    width: 1.1rem;
+    height: 1.1rem;
+    margin-left: 0.28rem;
+    border-radius: 999px;
+    background: rgba(39, 127, 255, 0.12);
+    color: #2a6db2;
+    font-size: 0.68rem;
+    font-weight: 700;
+    vertical-align: text-top;
+}
+
+@media (max-width: 900px) {
+    .premium-highlight-preview-actions {
+        grid-template-columns: 1fr;
+    }
+
+    .premium-highlight-reader.is-fullscreen {
+        padding: 16px;
+    }
+
+    .premium-highlight-reader-head {
+        justify-items: start;
+        text-align: left;
+    }
+
+    .premium-highlight-reader.is-fullscreen .premium-highlight-reader-panel {
+        padding: 18px;
+    }
+
+    .premium-highlight-reader-copy {
+        justify-items: start;
+    }
+
+    .premium-highlight-document {
+        grid-template-columns: 1fr;
+    }
+
+    .premium-highlight-paper {
+        width: 100%;
+        padding: 24px 22px 28px;
+    }
 }
 
 .premium-saved-grid {
@@ -1925,10 +2359,13 @@
 .premium-paywall-shell {
     max-width: 920px;
     margin: 0 auto;
+    display: grid;
+    gap: 14px;
     align-content: start;
 }
 
 .premium-paywall-hero,
+.premium-comparison-card,
 .premium-paywall-status,
 .premium-plan-card {
     border: 1px solid var(--premium-line);
@@ -1939,7 +2376,7 @@
 
 .premium-paywall-hero {
     display: grid;
-    gap: 12px;
+    gap: 14px;
     padding: 26px;
     border-radius: var(--premium-radius-panel);
     background:
@@ -1948,189 +2385,121 @@
         var(--premium-surface);
 }
 
+.premium-paywall-hero-active {
+    background:
+        radial-gradient(circle at top right, rgba(49, 215, 163, 0.14), transparent 34%),
+        radial-gradient(circle at top left, rgba(121, 213, 255, 0.1), transparent 30%),
+        var(--premium-surface);
+}
+
 .premium-paywall-hero h2 {
-    max-width: 720px;
+    max-width: 640px;
     margin: 0;
-    font-size: clamp(1.9rem, 4vw, 3.3rem);
-    line-height: 0.98;
+    font-size: clamp(1.72rem, 3.4vw, 2.75rem);
+    line-height: 1.01;
     letter-spacing: -0.05em;
 }
 
 .premium-paywall-hero p,
+.premium-comparison-card p,
 .premium-paywall-status p {
-    max-width: 680px;
+    max-width: 640px;
     margin: 0;
     color: var(--premium-muted);
-    font-size: 0.96rem;
+    font-size: 0.92rem;
     line-height: 1.45;
 }
 
-.premium-paywall-benefits {
+.premium-paywall-benefit-groups {
     display: grid;
-    grid-template-columns: repeat(2, minmax(0, 1fr));
-    gap: 7px;
-    max-width: 760px;
+    grid-template-columns: repeat(3, minmax(0, 1fr));
+    gap: 10px;
+    max-width: 820px;
     margin-top: 6px;
-    padding: 0;
-    list-style: none;
 }
 
-.premium-paywall-benefits li {
+.premium-paywall-benefit-card {
     display: grid;
-    grid-template-columns: 28px minmax(0, 1fr);
-    align-items: center;
     gap: 10px;
-    min-height: 42px;
-    padding: 8px 10px 8px 8px;
-    border-left: 2px solid rgba(121, 213, 255, 0.38);
-    border-radius: 4px var(--premium-radius-tight) var(--premium-radius-tight) 4px;
-    background:
-        linear-gradient(90deg, rgba(121, 213, 255, 0.12), rgba(255, 255, 255, 0.025));
+    min-height: 144px;
+    padding: 14px;
+    border: 1px solid rgba(255, 255, 255, 0.08);
+    border-radius: var(--premium-radius-card);
+    background: rgba(255, 255, 255, 0.03);
 }
 
-.premium-paywall-benefits li:nth-child(2) {
-    border-left-color: rgba(95, 114, 255, 0.52);
+.premium-paywall-benefit-card-continuity {
     background:
-        linear-gradient(90deg, rgba(95, 114, 255, 0.13), rgba(255, 255, 255, 0.025));
+        linear-gradient(180deg, rgba(78, 166, 219, 0.14), rgba(255, 255, 255, 0.025));
+    border-color: rgba(96, 167, 219, 0.2);
 }
 
-.premium-paywall-benefits li:nth-child(3) {
-    border-left-color: rgba(255, 203, 109, 0.62);
+.premium-paywall-benefit-card-depth {
     background:
-        linear-gradient(90deg, rgba(255, 203, 109, 0.13), rgba(255, 255, 255, 0.025));
+        linear-gradient(180deg, rgba(95, 114, 255, 0.14), rgba(255, 255, 255, 0.025));
+    border-color: rgba(95, 114, 255, 0.2);
 }
 
-.premium-paywall-benefits li:nth-child(4) {
-    border-left-color: rgba(49, 215, 163, 0.52);
+.premium-paywall-benefit-card-performance {
     background:
-        linear-gradient(90deg, rgba(49, 215, 163, 0.12), rgba(255, 255, 255, 0.025));
+        linear-gradient(180deg, rgba(255, 203, 109, 0.14), rgba(255, 255, 255, 0.025));
+    border-color: rgba(255, 203, 109, 0.2);
 }
 
-.premium-paywall-benefits li:nth-child(5) {
-    border-left-color: rgba(255, 128, 171, 0.52);
-    background:
-        linear-gradient(90deg, rgba(255, 128, 171, 0.12), rgba(255, 255, 255, 0.025));
+.premium-paywall-benefit-label {
+    display: inline-flex;
+    width: fit-content;
+    min-height: 24px;
+    align-items: center;
+    padding: 0 8px;
+    border-radius: var(--premium-radius-chip);
+    font-size: 0.64rem;
+    font-weight: 800;
+    letter-spacing: 0.11em;
+    text-transform: uppercase;
 }
 
-.premium-paywall-benefits li:nth-child(6) {
-    border-left-color: rgba(180, 139, 255, 0.56);
-    background:
-        linear-gradient(90deg, rgba(180, 139, 255, 0.12), rgba(255, 255, 255, 0.025));
+.premium-paywall-benefit-card-continuity .premium-paywall-benefit-label {
+    background: rgba(96, 167, 219, 0.12);
+    color: #79c6ff;
 }
 
-.premium-paywall-benefits li:nth-child(7) {
-    border-left-color: rgba(255, 157, 107, 0.56);
-    background:
-        linear-gradient(90deg, rgba(255, 157, 107, 0.12), rgba(255, 255, 255, 0.025));
+.premium-paywall-benefit-card-depth .premium-paywall-benefit-label {
+    background: rgba(95, 114, 255, 0.12);
+    color: #9baaff;
 }
 
-.premium-paywall-benefits li:nth-child(8) {
-    border-left-color: rgba(121, 213, 255, 0.44);
-    background:
-        linear-gradient(90deg, rgba(121, 213, 255, 0.11), rgba(255, 255, 255, 0.025));
+.premium-paywall-benefit-card-performance .premium-paywall-benefit-label {
+    background: rgba(255, 203, 109, 0.12);
+    color: #f0c56a;
 }
 
-.premium-paywall-benefits li:nth-child(n + 9) {
-    border-left-color: rgba(121, 213, 255, 0.28);
-    background:
-        linear-gradient(90deg, rgba(255, 255, 255, 0.07), rgba(255, 255, 255, 0.02));
+.premium-paywall-benefit-card ul {
+    display: grid;
+    gap: 8px;
+    margin: 0;
+    padding: 0;
+    list-style: none;
 }
 
-.premium-paywall-benefits li > span {
+.premium-paywall-benefit-card li {
     position: relative;
-    display: grid;
-    place-items: center;
-    width: 28px;
-    height: 28px;
-    border-radius: 50%;
-    background: rgba(121, 213, 255, 0.1);
-    box-shadow: inset 0 0 0 1px rgba(121, 213, 255, 0.22);
+    padding-left: 14px;
+    color: var(--premium-ink);
+    font-size: 0.84rem;
+    line-height: 1.3;
+    font-weight: 700;
 }
 
-.premium-paywall-benefits li > span::before {
+.premium-paywall-benefit-card li::before {
     content: "";
-    width: 9px;
-    height: 9px;
+    position: absolute;
+    left: 0;
+    top: 0.5em;
+    width: 6px;
+    height: 6px;
     border-radius: 50%;
-    background: var(--premium-accent-bright);
-    box-shadow: 0 0 16px rgba(121, 213, 255, 0.58);
-}
-
-.premium-paywall-benefits li:nth-child(2) > span {
-    background: rgba(95, 114, 255, 0.12);
-    box-shadow: inset 0 0 0 1px rgba(95, 114, 255, 0.24);
-}
-
-.premium-paywall-benefits li:nth-child(2) > span::before {
-    background: var(--premium-accent);
-    box-shadow: 0 0 16px rgba(95, 114, 255, 0.58);
-}
-
-.premium-paywall-benefits li:nth-child(3) > span {
-    background: rgba(255, 203, 109, 0.12);
-    box-shadow: inset 0 0 0 1px rgba(255, 203, 109, 0.24);
-}
-
-.premium-paywall-benefits li:nth-child(3) > span::before {
-    background: var(--premium-warm);
-    box-shadow: 0 0 16px rgba(255, 203, 109, 0.52);
-}
-
-.premium-paywall-benefits li:nth-child(4) > span {
-    background: rgba(49, 215, 163, 0.12);
-    box-shadow: inset 0 0 0 1px rgba(49, 215, 163, 0.24);
-}
-
-.premium-paywall-benefits li:nth-child(4) > span::before {
-    background: var(--premium-success);
-    box-shadow: 0 0 16px rgba(49, 215, 163, 0.52);
-}
-
-.premium-paywall-benefits li:nth-child(5) > span {
-    background: rgba(255, 128, 171, 0.12);
-    box-shadow: inset 0 0 0 1px rgba(255, 128, 171, 0.24);
-}
-
-.premium-paywall-benefits li:nth-child(5) > span::before {
-    background: #ff80ab;
-    box-shadow: 0 0 16px rgba(255, 128, 171, 0.46);
-}
-
-.premium-paywall-benefits li:nth-child(6) > span {
-    background: rgba(180, 139, 255, 0.12);
-    box-shadow: inset 0 0 0 1px rgba(180, 139, 255, 0.24);
-}
-
-.premium-paywall-benefits li:nth-child(6) > span::before {
-    background: #b48bff;
-    box-shadow: 0 0 16px rgba(180, 139, 255, 0.46);
-}
-
-.premium-paywall-benefits li:nth-child(7) > span {
-    background: rgba(255, 157, 107, 0.12);
-    box-shadow: inset 0 0 0 1px rgba(255, 157, 107, 0.24);
-}
-
-.premium-paywall-benefits li:nth-child(7) > span::before {
-    background: #ff9d6b;
-    box-shadow: 0 0 16px rgba(255, 157, 107, 0.46);
-}
-
-.premium-paywall-benefits li:nth-child(8) > span {
-    background: rgba(121, 213, 255, 0.1);
-    box-shadow: inset 0 0 0 1px rgba(121, 213, 255, 0.22);
-}
-
-.premium-paywall-benefits li:nth-child(8) > span::before {
-    background: var(--premium-accent-bright);
-    box-shadow: 0 0 16px rgba(121, 213, 255, 0.5);
-}
-
-.premium-paywall-benefits strong {
-    color: var(--premium-ink);
-    font-size: 0.88rem;
-    font-weight: 800;
-    line-height: 1.22;
+    background: currentColor;
 }
 
 .premium-plan-grid {
@@ -2142,9 +2511,18 @@
 .premium-plan-card {
     display: grid;
     gap: 8px;
-    min-height: 160px;
-    padding: 20px;
+    min-height: 124px;
+    padding: 20px 22px;
     border-radius: var(--premium-radius-card);
+    background:
+        linear-gradient(135deg, rgba(255, 255, 255, 0.2) 0 12%, transparent 12% 100%),
+        linear-gradient(180deg, #e3dbd1 0%, #d4cbc0 100%);
+    border-color: rgba(184, 172, 158, 0.54);
+    color: #201f23;
+    box-shadow:
+        inset 0 1px 0 rgba(255, 255, 255, 0.34),
+        inset 0 -18px 34px rgba(76, 64, 49, 0.028),
+        0 14px 28px rgba(6, 12, 22, 0.12);
     text-align: left;
     cursor: pointer;
     transition: transform 0.18s ease, border-color 0.18s ease, background 0.18s ease;
@@ -2156,18 +2534,23 @@
 }
 
 .premium-plan-card.is-recommended {
-    border-color: rgba(255, 203, 109, 0.32);
+    border-color: rgba(210, 170, 90, 0.42);
     background:
+        linear-gradient(135deg, rgba(255, 248, 225, 0.18) 0 12%, transparent 12% 100%),
         radial-gradient(circle at top right, rgba(255, 203, 109, 0.16), transparent 34%),
-        var(--premium-surface);
+        linear-gradient(180deg, #ddd1b7 0%, #cdbb98 100%);
+    box-shadow:
+        inset 0 1px 0 rgba(255, 245, 220, 0.3),
+        inset 0 -16px 30px rgba(112, 86, 37, 0.04),
+        0 18px 36px rgba(0, 0, 0, 0.16);
 }
 
 .premium-plan-card span {
     width: fit-content;
     padding: 5px 9px;
     border-radius: var(--premium-radius-chip);
-    background: rgba(255, 203, 109, 0.12);
-    color: var(--premium-warm);
+    background: rgba(227, 183, 92, 0.14);
+    color: #c89431;
     font-size: 0.66rem;
     font-weight: 800;
     letter-spacing: 0.08em;
@@ -2175,21 +2558,121 @@
 }
 
 .premium-plan-card strong {
-    font-size: 1.1rem;
-    line-height: 1.1;
+    color: #2a2321;
+    font-size: 1.2rem;
+    line-height: 1.06;
+    letter-spacing: -0.03em;
 }
 
 .premium-plan-card em {
-    color: var(--premium-accent-bright);
+    color: #49a7e6;
     font-style: normal;
-    font-size: 0.86rem;
+    font-size: clamp(1.42rem, 2.5vw, 2rem);
+    line-height: 1;
     font-weight: 800;
+    letter-spacing: -0.04em;
+}
+
+.premium-plan-card p {
+    margin: 0;
+    color: rgba(42, 35, 33, 0.78);
+    font-size: 0.84rem;
+    line-height: 1.35;
+    font-weight: 700;
 }
 
 .premium-plan-card small {
+    display: block;
+    color: rgba(42, 35, 33, 0.78);
+    font-size: 0.82rem;
+    line-height: 1.28;
+    font-weight: 700;
+}
+
+.premium-plan-card b {
+    display: none;
+}
+
+.premium-comparison-card {
+    display: grid;
+    gap: 12px;
+    padding: 18px 20px;
+    border-radius: var(--premium-radius-card);
+    background:
+        radial-gradient(circle at top left, rgba(121, 213, 255, 0.08), transparent 34%),
+        rgba(255, 255, 255, 0.025);
+}
+
+.premium-comparison-head {
+    display: grid;
+    gap: 4px;
+}
+
+.premium-comparison-head strong {
+    font-size: 1rem;
+    line-height: 1.2;
+}
+
+.premium-comparison-table {
+    display: grid;
+    gap: 8px;
+}
+
+.premium-comparison-row {
+    display: grid;
+    grid-template-columns: minmax(0, 1.7fr) minmax(72px, 0.62fr) minmax(72px, 0.72fr);
+    align-items: center;
+    gap: 8px;
+    min-height: 42px;
+    padding: 8px 10px;
+    border: 1px solid rgba(255, 255, 255, 0.06);
+    border-radius: var(--premium-radius-tight);
+    background: rgba(255, 255, 255, 0.03);
+}
+
+.premium-comparison-row-head {
+    min-height: 34px;
+    padding: 0 10px;
+    border: 0;
+    background: transparent;
+}
+
+.premium-comparison-row-head span {
     color: var(--premium-muted);
+    font-size: 0.68rem;
+    font-weight: 800;
+    letter-spacing: 0.1em;
+    text-transform: uppercase;
+}
+
+.premium-comparison-row strong {
     font-size: 0.82rem;
-    line-height: 1.35;
+    line-height: 1.24;
+}
+
+.premium-comparison-row span[role="cell"] {
+    display: inline-flex;
+    align-items: center;
+    justify-content: center;
+    min-height: 28px;
+    padding: 0 8px;
+    border-radius: var(--premium-radius-chip);
+    font-size: 0.72rem;
+    font-weight: 800;
+    text-align: center;
+    line-height: 1.1;
+}
+
+.premium-comparison-row .is-free {
+    border: 1px solid rgba(121, 213, 255, 0.16);
+    background: rgba(121, 213, 255, 0.08);
+    color: #8ed7ff;
+}
+
+.premium-comparison-row .is-premium {
+    border: 1px solid rgba(255, 203, 109, 0.18);
+    background: rgba(255, 203, 109, 0.1);
+    color: #f3cb74;
 }
 
 .premium-paywall-status {
@@ -2205,6 +2688,13 @@
     font-size: 0.92rem;
 }
 
+.premium-paywall-status-active {
+    background:
+        radial-gradient(circle at top right, rgba(49, 215, 163, 0.12), transparent 34%),
+        rgba(49, 215, 163, 0.08);
+    border-color: rgba(49, 215, 163, 0.22);
+}
+
 .premium-practice-progress {
     margin-top: auto;
     display: flex;
@@ -2243,23 +2733,24 @@
 
 .premium-session-note {
     display: grid;
-    gap: 6px;
+    gap: 8px;
     margin-top: 14px;
-    padding: 12px 14px;
+    padding: 14px 16px;
     border: 1px solid var(--premium-line);
     border-radius: var(--premium-radius-card);
     background: rgba(255, 255, 255, 0.04);
+    box-shadow: var(--premium-shadow-soft);
 }
 
 .premium-session-note strong {
     margin: 0;
-    font-size: 0.88rem;
+    font-size: 0.96rem;
 }
 
 .premium-session-note p {
     margin: 0;
     color: var(--premium-muted);
-    font-size: 0.86rem;
+    font-size: 0.9rem;
     line-height: 1.4;
 }
 
@@ -2273,6 +2764,13 @@
     background: rgba(95, 114, 255, 0.1);
 }
 
+.premium-session-note-success {
+    border-color: rgba(49, 215, 163, 0.3);
+    background:
+        radial-gradient(circle at top right, rgba(49, 215, 163, 0.16), transparent 36%),
+        rgba(49, 215, 163, 0.1);
+}
+
 .premium-question-meta {
     display: flex;
     align-items: center;
@@ -2717,7 +3215,6 @@ body.dark .launchpad-card[data-module="premium-study"] {
 
     .premium-detail-grid,
     .premium-practice-grid,
-    .premium-plan-grid,
     .premium-saved-grid {
         grid-template-columns: 1fr;
     }
@@ -2725,6 +3222,21 @@ body.dark .launchpad-card[data-module="premium-study"] {
     .premium-detail-card-wide {
         grid-column: auto;
     }
+
+    .premium-paywall-benefit-groups {
+        grid-template-columns: repeat(3, minmax(0, 1fr));
+        gap: 8px;
+    }
+
+    .premium-paywall-benefit-card {
+        min-height: 132px;
+        padding: 12px;
+    }
+
+    .premium-paywall-benefit-card li {
+        font-size: 0.76rem;
+        line-height: 1.24;
+    }
 }
 
 @media (max-width: 780px) {
@@ -2910,8 +3422,12 @@ body.dark .launchpad-card[data-module="premium-study"] {
     }
 
     .premium-entry-card-support {
-        min-height: 104px;
-        max-height: 104px;
+        min-height: 112px;
+        max-height: none;
+    }
+
+    .premium-entry-topline {
+        align-items: flex-start;
     }
 
     .premium-time-grid {
@@ -2975,10 +3491,67 @@ body.dark .launchpad-card[data-module="premium-study"] {
         font-size: clamp(1.5rem, 7vw, 2rem);
     }
 
+    .premium-paywall-benefit-groups {
+        grid-template-columns: repeat(3, minmax(0, 1fr));
+        gap: 7px;
+    }
+
+    .premium-paywall-benefit-card {
+        min-height: 122px;
+        padding: 10px;
+        gap: 8px;
+    }
+
+    .premium-paywall-benefit-label {
+        min-height: 22px;
+        padding: 0 6px;
+        font-size: 0.54rem;
+        letter-spacing: 0.08em;
+    }
+
+    .premium-paywall-benefit-card ul {
+        gap: 6px;
+    }
+
+    .premium-paywall-benefit-card li {
+        padding-left: 10px;
+        font-size: 0.66rem;
+        line-height: 1.2;
+    }
+
+    .premium-paywall-benefit-card li::before {
+        width: 5px;
+        height: 5px;
+    }
+
     .premium-plan-card {
         min-height: 122px;
     }
 
+    .premium-comparison-card {
+        padding: 16px;
+    }
+
+    .premium-comparison-row {
+        grid-template-columns: minmax(0, 1.5fr) minmax(64px, 0.66fr) minmax(64px, 0.74fr);
+        gap: 6px;
+        padding: 7px 8px;
+    }
+
+    .premium-comparison-row strong {
+        font-size: 0.74rem;
+    }
+
+    .premium-comparison-row span[role="cell"] {
+        min-height: 24px;
+        padding: 0 6px;
+        font-size: 0.62rem;
+    }
+
+    .premium-comparison-row-head span {
+        font-size: 0.56rem;
+    }
+
     .premium-question-meta,
     .premium-summary-compact-head {
         align-items: start;
@@ -3208,13 +3781,6 @@ body.dark .launchpad-card[data-module="premium-study"] {
         justify-content: center;
     }
 
-    .premium-entry-note {
-        display: grid;
-    }
-
-    .premium-paywall-benefits {
-        grid-template-columns: 1fr;
-    }
 }
 
 @media (min-width: 781px) and (max-width: 1100px) {
@@ -3405,3 +3971,288 @@ body.dark .launchpad-card[data-module="premium-study"] {
 .premium-study-shell::before {
     background: linear-gradient(180deg, rgba(255, 255, 255, 0.05), transparent 13%);
 }
+
+@media (max-width: 900px) and (max-height: 520px) and (orientation: landscape) and (pointer: coarse) {
+    #premium-studyModule.active {
+        padding-bottom: 12px;
+    }
+
+    .premium-study-host {
+        width: 100%;
+    }
+
+    .premium-study-shell {
+        grid-template-rows: auto minmax(0, 1fr);
+        height: min(560px, calc(100vh - 150px));
+        min-height: min(560px, calc(100vh - 150px));
+        max-height: calc(100vh - 150px);
+        overflow: hidden;
+        padding: 14px;
+    }
+
+    .premium-study-shell-header {
+        margin-bottom: 10px;
+    }
+
+    .premium-shell-nav {
+        grid-template-columns: auto minmax(0, 1fr) auto;
+        gap: 8px;
+    }
+
+    .premium-study-shell-body {
+        grid-template-columns: 1fr;
+        gap: 10px;
+        align-items: start;
+        min-height: 0;
+        height: 100%;
+    }
+
+    .premium-study-stage,
+    .premium-shell-scrollable .premium-study-stage,
+    .premium-study-shell[data-premium-step="learn-map"] .premium-study-stage {
+        display: grid;
+        grid-template-rows: auto minmax(0, 1fr);
+        min-height: 0;
+        overflow: hidden;
+        padding-right: 0;
+        padding-bottom: 0;
+    }
+
+    .premium-stage-heading {
+        margin-bottom: 10px;
+    }
+
+    .premium-stage-heading h1 {
+        max-width: 18ch;
+        font-size: clamp(1.16rem, 4vw, 1.78rem);
+    }
+
+    .premium-stage-heading p {
+        font-size: 0.78rem;
+        line-height: 1.28;
+    }
+
+    .premium-study-summary {
+        order: 2;
+    }
+
+    .premium-entry-stage {
+        display: grid;
+        gap: 10px;
+        min-height: 0;
+        overflow-y: auto;
+        padding-right: 4px;
+        padding-bottom: 4px;
+        scrollbar-width: thin;
+    }
+
+    .premium-entry-grid,
+    .premium-entry-grid-secondary,
+    .premium-entry-grid-secondary.is-single {
+        grid-template-columns: 1fr;
+    }
+
+    .premium-entry-card-featured,
+    .premium-entry-card-support {
+        min-height: 0;
+        max-height: none;
+    }
+
+    .premium-entry-card-featured {
+        min-height: 124px;
+    }
+
+    .premium-entry-card-support {
+        min-height: 98px;
+    }
+}
+
+@media (max-width: 760px) and (pointer: coarse) {
+    .premium-study-shell {
+        padding-bottom: calc(18px + env(safe-area-inset-bottom, 0px));
+    }
+
+    .premium-study-shell[data-premium-step="practice"] .premium-study-stage,
+    .premium-study-shell[data-premium-step="learn"] .premium-study-stage,
+    .premium-study-shell[data-premium-step="highlight"] .premium-study-stage {
+        overflow-y: auto;
+        padding-right: 4px;
+        padding-bottom: calc(88px + env(safe-area-inset-bottom, 0px));
+    }
+
+    .premium-mode-grid,
+    .premium-step-stage,
+    .premium-time-stage,
+    .premium-quiz-shell,
+    .premium-vf-shell,
+    .premium-highlight-shell,
+    .premium-saved-shell {
+        padding-bottom: calc(88px + env(safe-area-inset-bottom, 0px));
+    }
+
+    .premium-quiz-shell .premium-action-bar {
+        position: static;
+        padding-top: 6px;
+        padding-bottom: 0;
+        background: none;
+    }
+
+    .premium-quiz-shell .premium-action {
+        width: 100%;
+    }
+
+    .premium-question-meta {
+        flex-direction: column;
+        align-items: flex-start;
+        gap: 6px;
+    }
+
+    .premium-feedback-card {
+        gap: 8px;
+        padding: 12px 12px 14px;
+    }
+
+    .premium-practice-card {
+        overflow: visible;
+    }
+
+    .premium-practice-card > p {
+        max-width: none;
+    }
+
+    .premium-practice-slots {
+        overflow: visible;
+    }
+
+    .premium-practice-slot-row {
+        grid-template-columns: repeat(3, minmax(0, 1fr));
+    }
+
+    .premium-practice-slot {
+        min-height: 54px;
+        padding-bottom: 8px;
+    }
+
+    .premium-practice-slot strong,
+    .premium-practice-slot-lock {
+        font-size: 0.78rem;
+    }
+
+    .premium-highlight-export .premium-action {
+        width: 100%;
+        justify-content: center;
+    }
+
+    .premium-learn-reader,
+    .premium-learn-reader-panel,
+    .premium-learn-focus,
+    .premium-learn-article {
+        width: 100%;
+        max-width: 100%;
+        min-width: 0;
+    }
+
+    .premium-learn-reader-panel {
+        overflow: hidden;
+    }
+
+    .premium-learn-article {
+        overflow-x: hidden;
+        overflow-y: auto;
+        padding-right: 2px;
+    }
+
+    .premium-learn-reader-head {
+        margin-bottom: 12px;
+    }
+
+    .premium-learn-article h2 {
+        max-width: 100%;
+        font-size: clamp(1.52rem, 6.6vw, 2rem);
+    }
+
+    .premium-learn-lead {
+        max-width: 100%;
+        font-size: 0.88rem;
+        line-height: 1.42;
+    }
+
+    .premium-learn-section-rich {
+        padding: 14px 14px 0;
+    }
+
+    .premium-learn-section-rich h3 {
+        max-width: 100%;
+        font-size: 0.98rem;
+    }
+
+    .premium-learn-section-rich p,
+    .premium-learn-section-document .premium-bullet-list {
+        font-size: 0.88rem;
+        line-height: 1.46;
+        overflow-wrap: anywhere;
+    }
+
+    .premium-step-stage,
+    .premium-time-stage {
+        padding-left: 48px;
+        padding-right: 48px;
+    }
+
+    .premium-step-advance,
+    .premium-time-advance {
+        width: 44px;
+        height: 44px;
+        font-size: 1.4rem;
+    }
+
+    .premium-study-shell[data-premium-step="exam-date"] .premium-calendar-panel {
+        width: min(100%, 440px);
+        padding: 10px 10px 12px;
+    }
+
+    .premium-time-grid {
+        gap: 8px;
+    }
+
+    .premium-ring-card-time {
+        padding: 10px 8px;
+    }
+
+    .premium-ring-visual-time {
+        width: min(100%, 146px);
+    }
+
+    .premium-ring-card-time .premium-ring-center strong {
+        font-size: clamp(1.5rem, 7vw, 2rem);
+    }
+
+    .premium-ring-card-time .premium-ring-center span {
+        font-size: 0.58rem;
+        letter-spacing: 0.04em;
+    }
+}
+
+@media (max-width: 900px) and (max-height: 520px) and (orientation: landscape) and (pointer: coarse) {
+    .premium-learn-reader.is-fullscreen .premium-learn-reader-panel {
+        width: 100%;
+        max-width: none;
+        max-height: calc(100vh - 132px);
+        padding: 16px 16px 14px;
+    }
+
+    .premium-learn-reader.is-fullscreen .premium-learn-article {
+        overflow-y: auto;
+        padding-right: 4px;
+    }
+
+    .premium-learn-article h2 {
+        font-size: clamp(1.3rem, 4.6vw, 1.7rem);
+    }
+
+    .premium-learn-section-rich p,
+    .premium-bullet-list {
+        font-size: 0.82rem;
+        line-height: 1.4;
+    }
+}
diff --git a/premium-study/ui/components/index.js b/premium-study/ui/components/index.js
index 0928224..69012cc 100644
--- a/premium-study/ui/components/index.js
+++ b/premium-study/ui/components/index.js
@@ -118,6 +118,7 @@
     }) {
         const showProgress = meta.progressVisible !== false;
         const showHeading = meta.hideHeading !== true;
+        const showKicker = meta.showKicker !== false;
         const inlineSummary = step === "mode-select" && summary;
         const asideSummary = summary && !inlineSummary;
         const scrollableClass = meta.scrollable ? "premium-shell-scrollable" : "";
@@ -141,7 +142,7 @@
     <div class="premium-study-shell-body">
         <section class="premium-study-stage">
             ${showHeading ? `<div class="premium-stage-heading">
-                <span class="premium-stage-kicker">${escapeHtml(meta.label)}</span>
+                ${showKicker ? `<span class="premium-stage-kicker">${escapeHtml(meta.label)}</span>` : ""}
                 <h1>${escapeHtml(meta.title)}</h1>
                 <p>${escapeHtml(meta.subtitle)}</p>
             </div>` : ""}
diff --git a/premium-study/ui/views/index.js b/premium-study/ui/views/index.js
index efa8c8b..fb20a69 100644
--- a/premium-study/ui/views/index.js
+++ b/premium-study/ui/views/index.js
@@ -8,7 +8,7 @@
     const Access = () => window.PremiumStudyAccessControl;
 
     const MONTHS = [
-        "Janeiro", "Fevereiro", "Março", "Abril", "Maio", "Junho",
+        "Janeiro", "Fevereiro", "MarÃƒÂ§o", "Abril", "Maio", "Junho",
         "Julho", "Agosto", "Setembro", "Outubro", "Novembro", "Dezembro"
     ];
 
@@ -22,6 +22,7 @@
     }
 
     function entry(state) {
+        const access = Access();
         const resume = state.latestLocalStudy;
         const studyLibrary = Array.isArray(state.studyLibrary)
             ? state.studyLibrary
@@ -31,40 +32,86 @@
             ? Access().FEATURES.PREMIUM_LIBRARY
             : "premium_library";
         const premiumLibraryEnabled = canAccess(premiumLibraryFeature, state);
+        const premiumActive = access && typeof access.isPremiumLike === "function"
+            ? access.isPremiumLike(state)
+            : state.accessTier === "premium";
+
+        if (premiumActive) {
+            return `
+<section class="premium-entry-stage">
+    <div class="premium-entry-hero">
+        <button type="button" class="premium-entry-card premium-entry-card-primary premium-entry-card-featured" data-premium-action="open-file-picker">
+            <div class="premium-entry-topline">
+                <span class="premium-entry-kicker">Workspace premium</span>
+                <span class="premium-entry-inline-badge premium-entry-inline-badge-active">PDFs ilimitados</span>
+            </div>
+            <strong>Carregar novo PDF</strong>
+            <p>Adicionar outro material e manter tudo organizado na sua biblioteca premium.</p>
+        </button>
+    </div>
+    <div class="premium-entry-grid premium-entry-grid-secondary ${resume ? "" : "is-single"}">
+        ${resume ? `
+        <button type="button" class="premium-entry-card premium-entry-card-secondary premium-entry-card-support premium-entry-card-resume" data-premium-action="resume-latest-study">
+            <div class="premium-entry-topline">
+                <span class="premium-entry-kicker">Continuidade</span>
+                <span class="premium-entry-inline-badge premium-entry-inline-badge-active">Continuidade completa</span>
+            </div>
+            <strong>Retomar ultimo estudo</strong>
+            <p>${UI().escapeHtml(resume.title)}</p>
+            <small>Prova em ${UI().escapeHtml(resume.examDateLabel)}</small>
+        </button>` : ""}
+        <button type="button" class="premium-entry-card premium-entry-card-secondary premium-entry-card-support premium-entry-card-premium" data-premium-action="open-premium-library">
+            <div class="premium-entry-topline">
+                <span class="premium-entry-kicker">Biblioteca ativa</span>
+                <span class="premium-entry-inline-badge premium-entry-inline-badge-active">Tudo liberado</span>
+            </div>
+            <strong>Abrir biblioteca</strong>
+            <p>${additionalStudiesCount > 0 ? `${additionalStudiesCount} estudo(s) extra(s) ja estao guardados para retomada imediata.` : "Seu historico completo fica pronto para receber novos materiais e retomadas."}</p>
+            <small>Entrar no acervo completo</small>
+        </button>
+    </div>
+    <input id="premiumStudyFileInput" class="premium-hidden-input" type="file" accept=".pdf,application/pdf" />
+</section>
+${renderSessionNote(state, "entry")}`;
+        }
 
         return `
 <section class="premium-entry-stage">
     <div class="premium-entry-hero">
         <button type="button" class="premium-entry-card premium-entry-card-primary premium-entry-card-featured" data-premium-action="open-file-picker">
-            <span class="premium-entry-kicker">Novo estudo <span class="premium-entry-inline-badge premium-entry-inline-badge-free">Até 12 páginas grátis</span></span>
+            <div class="premium-entry-topline">
+                <span class="premium-entry-kicker">Novo estudo</span>
+                <span class="premium-entry-inline-badge premium-entry-inline-badge-free">Gratis ate 12 paginas</span>
+            </div>
             <strong>Carregar PDF</strong>
-            <p>Envie seu material e receba um caminho focado na sua necessidade.</p>
+            <p>Enviar material para montar a trilha.</p>
         </button>
     </div>
     <div class="premium-entry-grid premium-entry-grid-secondary ${resume ? "" : "is-single"}">
         ${resume ? `
         <button type="button" class="premium-entry-card premium-entry-card-secondary premium-entry-card-support premium-entry-card-resume" data-premium-action="resume-latest-study">
-            <span class="premium-entry-kicker">Último estudo salvo <span class="premium-entry-inline-badge premium-entry-inline-badge-free">Grátis</span></span>
-            <strong>Retomar último estudo</strong>
+            <div class="premium-entry-topline">
+                <span class="premium-entry-kicker">Continuidade</span>
+                <span class="premium-entry-inline-badge premium-entry-inline-badge-free">Ultimo estudo livre</span>
+            </div>
+            <strong>Retomar ultimo estudo</strong>
             <p>${UI().escapeHtml(resume.title)}</p>
             <small>Prova em ${UI().escapeHtml(resume.examDateLabel)}</small>
         </button>` : ""}
         <button type="button" class="premium-entry-card premium-entry-card-secondary premium-entry-card-support premium-entry-card-premium ${premiumLibraryEnabled ? "" : "is-locked"}" data-premium-action="open-premium-library" ${premiumLibraryEnabled ? "" : "aria-disabled=\"true\""}>
-            <span class="premium-entry-kicker">Biblioteca premium <span class="premium-entry-inline-badge premium-entry-inline-badge-premium">Premium</span></span>
-            <strong>Biblioteca premium</strong>
-            <p>${additionalStudiesCount > 0 ? `${additionalStudiesCount} estudo(s) extras já esperam por você aqui.` : "Retome outros estudos salvos e mantenha seu histórico organizado."}</p>
-            <small>${premiumLibraryEnabled ? "Abrir histórico premium" : "Estude mais com premium"}</small>
+            <div class="premium-entry-topline">
+                <span class="premium-entry-kicker">Camada premium</span>
+                <span class="premium-entry-inline-badge premium-entry-inline-badge-premium">Biblioteca completa</span>
+            </div>
+            <strong>${premiumLibraryEnabled ? "Abrir biblioteca" : "Biblioteca premium"}</strong>
+            <p>${additionalStudiesCount > 0 ? `${additionalStudiesCount} estudo(s) extras ja estao prontos para retomada.` : "Guarde outros materiais e mantenha seu historico completo no mesmo lugar."}</p>
+            <small>${premiumLibraryEnabled ? "Ver historico premium" : "Expanda seus estudos com premium"}</small>
         </button>
     </div>
     <input id="premiumStudyFileInput" class="premium-hidden-input" type="file" accept=".pdf,application/pdf" />
 </section>
-<div class="premium-entry-note">
-    <span>Grátis para sempre em PDFs de até 12 páginas.</span>
-    <span>Retomar o último estudo continua grátis. Os outros estudos e o histórico completo ficam no premium.</span>
-</div>
 ${renderSessionNote(state, "entry")}`;
     }
-
     function buildCalendar(state) {
         const year = Number.isFinite(state.calendarYear) ? state.calendarYear : new Date().getFullYear();
         const month = Number.isFinite(state.calendarMonth) ? state.calendarMonth : new Date().getMonth();
@@ -105,9 +152,9 @@ ${renderSessionNote(state, "entry")}`;
     <div class="premium-calendar-panel">
         <div class="premium-calendar-head">
             <div class="premium-calendar-title-wrap">
-                <button type="button" class="premium-calendar-nav" data-premium-action="calendar-prev" aria-label="Mês anterior">&larr;</button>
+                <button type="button" class="premium-calendar-nav" data-premium-action="calendar-prev" aria-label="MÃƒÂªs anterior">&larr;</button>
                 <strong class="premium-calendar-title">${MONTHS[month]} ${year}</strong>
-                <button type="button" class="premium-calendar-nav" data-premium-action="calendar-next" aria-label="Mês seguinte">&rarr;</button>
+                <button type="button" class="premium-calendar-nav" data-premium-action="calendar-next" aria-label="MÃƒÂªs seguinte">&rarr;</button>
             </div>
             <span class="premium-calendar-selection">${state.examDate ? `Selecionado: ${UI().formatDateLabel(state.examDate)}` : "Escolha um dia"}</span>
         </div>
@@ -192,11 +239,11 @@ ${renderSessionNote(state, "entry")}`;
         return `
 <section class="premium-loading-stage">
     <div class="premium-loading-orb" aria-hidden="true"></div>
-    <strong>Extraindo o melhor conteúdo para você buscar nota ${targetScore} no dia ${UI().escapeHtml(examDateLabel)}.</strong>
-    <p>Estamos montando uma trilha mais objetiva para o seu prazo e para o tempo diário que você informou.</p>
+    <strong>Extraindo o melhor conteÃƒÂºdo para vocÃƒÂª buscar nota ${targetScore} no dia ${UI().escapeHtml(examDateLabel)}.</strong>
+    <p>Estamos montando uma trilha mais objetiva para o seu prazo e para o tempo diÃƒÂ¡rio que vocÃƒÂª informou.</p>
     <div class="premium-loading-checks">
         <span>Lendo PDF</span>
-        <span>Separando tópicos</span>
+        <span>Separando tÃƒÂ³picos</span>
         <span>Priorizando trilha</span>
         <span>Montando plano</span>
     </div>
@@ -216,13 +263,13 @@ ${renderSessionNote(state, "entry")}`;
     </button>
     <button type="button" class="premium-mode-card" data-premium-action="choose-mode-practice">
         <span class="premium-mode-word">Praticar</span>
-        <strong>Vá direto para questões</strong>
+        <strong>VÃƒÂ¡ direto para questÃƒÂµes</strong>
         <p>Treino objetivo para consolidar o assunto sem desviar do foco.</p>
     </button>
     <button type="button" class="premium-mode-card" data-premium-action="choose-mode-exam">
         <span class="premium-mode-word">Prova</span>
-        <strong>Teste seu nível agora</strong>
-        <p>Mini prova do bloco para medir critério, segurança e prontidão.</p>
+        <strong>Teste seu nivel agora</strong>
+        <p>Prova premium com seletor de quantidade para medir prontidao geral.</p>
     </button>
     <button type="button" class="premium-mode-card premium-mode-card-highlight" data-premium-action="choose-mode-highlight">
         <span class="premium-mode-word">Marcar</span>
@@ -345,7 +392,7 @@ ${renderSessionNote(state, "entry")}`;
             <span class="premium-practice-slot-lock">+</span>
         </button>`).join("")}
     </div>
-    <small>3 grátis agora. Depois, potes extras e infinitos no premium.</small>
+    <small>3 grÃƒÂ¡tis agora. Depois, potes extras e infinitos no premium.</small>
 </div>`;
     }
 
@@ -420,16 +467,50 @@ ${renderSessionNote(state, "entry")}`;
             { action: "ai-explain-better", label: "Explicar melhor", variant: state.blockAssistMode === "explain" ? "secondary" : "ghost" },
             { action: "ai-quick-review", label: "Revisar em 5 pontos", variant: state.blockAssistMode === "review" ? "secondary" : "ghost" },
             { action: "open-mini-exam", label: "Mini prova", variant: "primary" },
-            ...(nextBlockId ? [{ action: "open-next-block", label: "Próximo assunto", variant: "ghost" }] : [])
+            ...(nextBlockId ? [{ action: "open-next-block", label: "PrÃƒÂ³ximo assunto", variant: "ghost" }] : [])
         ])}
     </div>
 </section>`;
     }
 
-    function renderHighlightParagraph(parts = []) {
-        return `<p class="premium-highlight-paragraph">${parts.map((part) => part.highlight
-            ? `<mark class="premium-highlight-mark">${UI().escapeHtml(part.text)}</mark>`
-            : UI().escapeHtml(part.text)).join("")}</p>`;
+    function getSelectedHighlightPart(documentData) {
+        const selectedPartId = documentData && documentData.selectedPartId
+            ? documentData.selectedPartId
+            : "";
+
+        if (!selectedPartId) {
+            return null;
+        }
+
+        for (const section of documentData.sections || []) {
+            for (const paragraph of section.paragraphs || []) {
+                for (const part of paragraph || []) {
+                    if (part.id === selectedPartId) {
+                        return part;
+                    }
+                }
+            }
+        }
+
+        return null;
+    }
+
+    function renderHighlightParagraph(parts = [], sectionIndex, paragraphIndex, selectedPartId) {
+        return `<p class="premium-highlight-paragraph" data-premium-highlight-paragraph="true" data-section-index="${sectionIndex}" data-paragraph-index="${paragraphIndex}">${parts.map((part, partIndex) => `
+            <span
+                class="premium-highlight-piece${part.highlight ? " is-highlighted" : ""}${part.highlight && part.styleMode === "box" ? " is-boxed" : ""}${selectedPartId === part.id ? " is-selected" : ""}${part.note ? " has-note" : ""}"
+                ${part.highlight ? `style="--premium-highlight-color:${UI().escapeHtml(part.colorKey || "gold")};"` : ""}
+                data-premium-action="select-highlight-part"
+                data-section-index="${sectionIndex}"
+                data-paragraph-index="${paragraphIndex}"
+                data-part-index="${partIndex}"
+                role="button"
+                tabindex="0"
+                aria-pressed="${selectedPartId === part.id ? "true" : "false"}"
+            >${part.highlight
+                ? `<mark class="premium-highlight-mark premium-highlight-mark-${UI().escapeHtml(part.colorKey || "gold")}${part.styleMode === "box" ? " premium-highlight-mark-box" : ""}">${UI().escapeHtml(part.text)}</mark>`
+                : UI().escapeHtml(part.text)}${part.note ? `<span class="premium-highlight-note-badge" aria-label="Trecho com nota">N</span>` : ""}</span>
+        `).join("")}</p>`;
     }
 
     function highlightPreview(state) {
@@ -438,38 +519,125 @@ ${renderSessionNote(state, "entry")}`;
             ? Access().FEATURES.HIGHLIGHT_EXPORT
             : "highlight_export";
         const premiumLibraryEnabled = canAccess(highlightExportFeature, state);
+        const selectedPart = getSelectedHighlightPart(documentData);
+        const colorOptions = Array.isArray(documentData.colorOptions)
+            ? documentData.colorOptions
+            : [];
+        const selectedText = selectedPart ? selectedPart.text : "";
+        const inspectorMode = state.highlightInspectorMode || "";
+        const hasSelection = Boolean(selectedPart);
+        const noteText = selectedPart && selectedPart.note ? selectedPart.note : "";
+        const showEditField = hasSelection && inspectorMode === "edit";
+        const showNoteField = hasSelection && inspectorMode === "note";
 
-        return `
-<section class="premium-highlight-shell">
-    <article class="premium-highlight-doc">
-        <span class="premium-panel-kicker">Documento com marcador</span>
-        <h2>${UI().escapeHtml(documentData.title)}</h2>
-        <p class="premium-learn-lead">${UI().escapeHtml(documentData.subtitle)}</p>
-        <div class="premium-highlight-note">
-            <strong>Documento integral preservado.</strong>
-            <p>Nada e removido daqui. O sistema so grifa os trechos mais importantes para guiar a leitura e preparar a exportacao em PDF no final.</p>
+        if (!state.highlightEditorOpen) {
+            return `
+<section class="premium-highlight-shell premium-highlight-shell-preview">
+    <article class="premium-highlight-export premium-highlight-preview-card">
+        ${renderSessionNote(state, "highlight-preview")}
+        <span class="premium-panel-kicker">Texto integral</span>
+        <strong>${UI().escapeHtml(documentData.title)}</strong>
+        <p>O documento abre primeiro sem resumo reescrito e sem marcacoes iniciais. A partir dali voce seleciona exatamente o trecho que quiser para editar, marcar ou desmarcar.</p>
+        <div class="premium-highlight-preview-actions">
+            <button type="button" class="premium-action premium-action-primary" data-premium-action="open-highlight-editor">Abrir texto integral</button>
+            <button type="button" class="premium-action premium-action-secondary" data-premium-action="download-highlight-summary">Baixar PDF so com marcacoes</button>
+            <button type="button" class="premium-action premium-action-secondary" data-premium-action="download-highlighted-full">Baixar PDF completo com as marcacoes</button>
         </div>
-        ${documentData.sections.map((section) => `
-            <section class="premium-highlight-section">
-                <span class="premium-detail-label">${UI().escapeHtml(section.label)}</span>
-                <h3>${UI().escapeHtml(section.title)}</h3>
-                ${section.paragraphs.map((paragraph) => renderHighlightParagraph(paragraph)).join("")}
-            </section>
-        `).join("")}
+        <small>${premiumLibraryEnabled ? "Os downloads abrem a versao pronta para salvar em PDF." : "Os downloads continuam liberados no premium."}</small>
     </article>
-    <section class="premium-highlight-export">
-        <span class="premium-panel-kicker">Resumo destacado</span>
-        <strong>Baixe em PDF so os destaques principais ou o documento inteiro com os grifos.</strong>
-        <p>O texto marcado nao fica salvo para consulta dentro do app. Aqui ele so pode ser exportado em PDF.</p>
-        <div class="premium-inline-actions premium-inline-actions-contextual">
-            <button type="button" class="premium-action premium-action-primary" data-premium-action="download-highlight-summary">Baixar PDF so com destaques</button>
-            <button type="button" class="premium-action premium-action-secondary" data-premium-action="download-highlighted-full">Baixar PDF com texto todo destacado</button>
+</section>`;
+        }
+
+        return `
+<section class="premium-highlight-reader is-fullscreen">
+    <div class="premium-highlight-reader-scrim" aria-hidden="true"></div>
+    <div class="premium-highlight-focus premium-highlight-reader-panel">
+        <div class="premium-highlight-reader-head">
+            <div class="premium-highlight-reader-copy">
+                ${renderSessionNote(state, "highlight-preview")}
+                <span class="premium-panel-kicker">Texto integral</span>
+                <h2>${UI().escapeHtml(documentData.title)}</h2>
+                <p class="premium-learn-lead">${UI().escapeHtml(documentData.subtitle)}</p>
+            </div>
         </div>
-        <small>${premiumLibraryEnabled ? "O navegador vai abrir a saida em formato de impressao para salvar em PDF." : "Os dois downloads ficam liberados no premium."}</small>
-    </section>
-    ${UI().actionBar([
-        { action: "back-to-mode-select", label: "Voltar para modos", variant: "secondary" }
-    ])}
+        <article class="premium-highlight-document">
+            <aside class="premium-highlight-sidebar">
+                <button type="button" class="premium-tab premium-highlight-return" data-premium-action="close-highlight-editor">Voltar aos downloads</button>
+                <div class="premium-highlight-toolbar">
+                    <div class="premium-highlight-toolbar-copy">
+                        <span class="premium-detail-label">Ferramentas</span>
+                        <strong>${selectedPart ? "Selecao pronta para acao" : "Selecione um trecho do texto"}</strong>
+                        <p>${selectedPart
+            ? "O fluxo agora e direto: selecione e escolha editar, marcar, desmarcar, anotar ou enquadrar."
+            : "O documento abre limpo. Selecione qualquer palavra, frase ou trecho para ajustar do seu jeito."}</p>
+                    </div>
+                    <div class="premium-highlight-swatch-row">
+                        ${colorOptions.map((option) => `
+                            <button
+                                type="button"
+                                class="premium-highlight-swatch${documentData.activeColorKey === option.key ? " is-active" : ""}"
+                                data-premium-action="set-highlight-color"
+                                data-item-value="${UI().escapeHtml(option.key)}"
+                                aria-label="${UI().escapeHtml(option.label)}"
+                                title="${UI().escapeHtml(option.label)}"
+                            >
+                                <span class="premium-highlight-swatch-fill premium-highlight-swatch-fill-${UI().escapeHtml(option.key)}"></span>
+                            </button>
+                        `).join("")}
+                    </div>
+                    <div class="premium-highlight-toolrail">
+                        <button type="button" class="premium-action premium-action-primary" data-premium-action="open-highlight-edit" ${selectedPart ? "" : "disabled"}>Editar</button>
+                        <button type="button" class="premium-action premium-action-secondary" data-premium-action="mark-highlight-selection" ${selectedPart ? "" : "disabled"}>Marcar</button>
+                        <button type="button" class="premium-action premium-action-secondary" data-premium-action="unmark-highlight-selection" ${selectedPart ? "" : "disabled"}>Desmarcar</button>
+                        <button type="button" class="premium-action premium-action-secondary" data-premium-action="set-highlight-style-box" ${selectedPart ? "" : "disabled"}>Caixa</button>
+                        <button type="button" class="premium-action premium-action-secondary" data-premium-action="open-highlight-note" ${selectedPart ? "" : "disabled"}>Nota</button>
+                        <button type="button" class="premium-action premium-action-secondary" data-premium-action="copy-highlight-text" ${selectedPart ? "" : "disabled"}>Copiar trecho</button>
+                        <button type="button" class="premium-action premium-action-secondary" data-premium-action="delete-highlight-text" ${selectedPart ? "" : "disabled"}>Apagar trecho</button>
+                        <button type="button" class="premium-action premium-action-ghost" data-premium-action="restore-highlight-document">Voltar ao original da IA</button>
+                        <button type="button" class="premium-action premium-action-ghost" data-premium-action="clear-all-highlights">Limpar marcacoes</button>
+                    </div>
+                    ${showEditField ? `
+                    <label class="premium-highlight-editor-label" for="premiumHighlightEditor">Editar selecao</label>
+                    <textarea id="premiumHighlightEditor" class="premium-highlight-editor-field" rows="6" placeholder="Reescreva o trecho selecionado.">${UI().escapeHtml(selectedText)}</textarea>
+                    <div class="premium-highlight-inline-actions">
+                        <button type="button" class="premium-action premium-action-primary" data-premium-action="save-highlight-text">Salvar edicao</button>
+                        <button type="button" class="premium-action premium-action-ghost" data-premium-action="close-highlight-inspector">Fechar</button>
+                    </div>` : ""}
+                    ${showNoteField ? `
+                    <label class="premium-highlight-editor-label" for="premiumHighlightNoteEditor">Nota do trecho</label>
+                    <textarea id="premiumHighlightNoteEditor" class="premium-highlight-editor-field" rows="5" placeholder="Escreva uma nota curta para este trecho.">${UI().escapeHtml(noteText)}</textarea>
+                    <div class="premium-highlight-inline-actions">
+                        <button type="button" class="premium-action premium-action-primary" data-premium-action="save-highlight-note">Salvar nota</button>
+                        <button type="button" class="premium-action premium-action-ghost" data-premium-action="close-highlight-inspector">Fechar</button>
+                    </div>` : ""}
+                </div>
+            </aside>
+            <div class="premium-highlight-canvas">
+                <div class="premium-highlight-paper">
+                    <div class="premium-highlight-paper-head">
+                        <span class="premium-panel-kicker">Documento integral</span>
+                        <strong>${UI().escapeHtml(documentData.title)}</strong>
+                    </div>
+                    <div class="premium-highlight-doc">
+                        ${documentData.sections.map((section, sectionIndex) => `
+                            <section class="premium-highlight-section">
+                                <span class="premium-detail-label">${UI().escapeHtml(section.label)}</span>
+                                <h3>${UI().escapeHtml(section.title)}</h3>
+                                ${section.paragraphs.map((paragraph, paragraphIndex) =>
+                renderHighlightParagraph(
+                    paragraph,
+                    sectionIndex,
+                    paragraphIndex,
+                    documentData.selectedPartId
+                )
+            ).join("")}
+                            </section>
+                        `).join("")}
+                    </div>
+                </div>
+            </div>
+        </article>
+    </div>
 </section>`;
     }
 
@@ -504,7 +672,7 @@ ${renderSessionNote(state, "entry")}`;
             `).join("") : `
                 <article class="premium-empty-library">
                     <strong>Nenhum material salvo ainda.</strong>
-                    <p>Assim que você carregar PDFs e avançar no fluxo, eles passam a aparecer aqui.</p>
+                    <p>Assim que vocÃƒÂª carregar PDFs e avanÃƒÂ§ar no fluxo, eles passam a aparecer aqui.</p>
                 </article>
             `}
         </aside>
@@ -528,7 +696,7 @@ ${renderSessionNote(state, "entry")}`;
             ` : `
                 <div class="premium-empty-library">
                     <strong>Sua biblioteca premium fica aqui.</strong>
-                    <p>Quando você carregar materiais, eles passam a ficar guardados nesta área para retomada futura.</p>
+                    <p>Quando vocÃƒÂª carregar materiais, eles passam a ficar guardados nesta ÃƒÂ¡rea para retomada futura.</p>
                 </div>
             `}
         </article>
@@ -539,6 +707,9 @@ ${renderSessionNote(state, "entry")}`;
     function premiumCheckout(state) {
         const access = Access();
         const billing = window.PremiumStudyBilling;
+        const premiumActive = access && typeof access.isPremiumLike === "function"
+            ? access.isPremiumLike(state)
+            : state.accessTier === "premium";
         const feature = state.premiumOffer && state.premiumOffer.feature
             ? state.premiumOffer.feature
             : premiumLibraryFeatureFallback();
@@ -547,40 +718,152 @@ ${renderSessionNote(state, "entry")}`;
             : {
                 eyebrow: "Premium",
                 title: "Libere continuidade e profundidade.",
-                lead: "O grátis entrega a base. O premium entra quando você quer mais materiais, mais treino e mais continuidade.",
+                lead: "O grÃƒÂ¡tis entrega a base. O premium entra quando vocÃƒÂª quer mais materiais, mais treino e mais continuidade.",
                 benefits: ["Historico completo", "Treinos extras", "Recursos avancados"],
                 cta: "Conhecer premium"
             });
         const plans = billing && typeof billing.getPlans === "function"
             ? billing.getPlans()
             : [];
+        const recommendedPlanId = offer.recommendedPlanId
+            || (plans.find((plan) => plan.recommended) || {}).id
+            || "";
+        const orderedPlans = [...plans].sort((left, right) => {
+            const leftScore = left.id === recommendedPlanId ? 2 : (left.recommended ? 1 : 0);
+            const rightScore = right.id === recommendedPlanId ? 2 : (right.recommended ? 1 : 0);
+            return rightScore - leftScore;
+        });
         const providerStatus = billing && typeof billing.getProviderStatus === "function"
             ? billing.getProviderStatus()
             : { checkoutReady: false, message: "Checkout real ainda nao foi conectado." };
+        const primaryBenefits = Array.isArray(offer.benefits)
+            ? offer.benefits.slice(0, 3)
+            : [];
+        const benefitGroups = [
+            {
+                tone: "continuity",
+                title: "Continuidade",
+                items: [
+                    "Retome qualquer PDF salvo",
+                    "Biblioteca completa por objetivo",
+                    "PDFs longos com divisao inteligente"
+                ]
+            },
+            {
+                tone: "depth",
+                title: "Mais profundidade",
+                items: [
+                    "Questionarios extras por assunto",
+                    "V/F extras para pegar pegadinhas",
+                    "Flashcards extras com mnemÃ´nicos"
+                ]
+            },
+            {
+                tone: "performance",
+                title: "Mais desempenho",
+                items: [
+                    "Mini provas extras por assunto",
+                    "Estatisticas de evolucao e pontos fracos",
+                    "Exportacao dos marcadores em PDF"
+                ]
+            }
+        ];
+        const comparisonRows = [
+            { label: "Ultimo estudo salvo", free: "Sim", premium: "Sim" },
+            { label: "Outros estudos guardados", free: "Nao", premium: "Sim" },
+            { label: "PDFs longos", free: "Ate 12 paginas", premium: "Ilimitado" },
+            { label: "Questionarios, V/F e flashcards", free: "3 rodadas", premium: "Novas rodadas" },
+            { label: "Mini prova do assunto", free: "Base", premium: "Extras" },
+            { label: "Estatisticas e pontos fracos", free: "Nao", premium: "Sim" }
+        ];
+
+        if (premiumActive) {
+            return `
+<section class="premium-paywall-shell">
+    <article class="premium-paywall-hero premium-paywall-hero-active">
+        <span class="premium-panel-kicker">Premium ativo</span>
+        <h2>Seu workspace completo ja esta liberado.</h2>
+        <p>Agora a experiencia precisa servir o estudo, nao vender acesso. Use a biblioteca, envie materiais longos e retome sua trilha com tudo disponivel.</p>
+        <div class="premium-paywall-benefit-groups" aria-label="Recursos premium ativos">
+            ${benefitGroups.map((group) => `
+            <article class="premium-paywall-benefit-card premium-paywall-benefit-card-${group.tone}">
+                <span class="premium-paywall-benefit-label">${group.title}</span>
+                <ul>
+                    ${group.items.map((item) => `<li>${item}</li>`).join("")}
+                </ul>
+            </article>`).join("")}
+        </div>
+    </article>
+    <article class="premium-paywall-status premium-paywall-status-active">
+        <strong>Premium confirmado neste workspace</strong>
+        <p>Biblioteca, PDFs extensos, extras de pratica e recursos avancados ja fazem parte da sua rotina daqui para frente.</p>
+    </article>
+    ${renderSessionNote(state, "premium-checkout")}
+    ${UI().actionBar([
+        { action: "open-premium-library", label: "Abrir biblioteca", variant: "primary" },
+        { action: "back", label: "Voltar ao estudo", variant: "secondary" }
+    ])}
+</section>`;
+        }
 
         return `
 <section class="premium-paywall-shell">
     <article class="premium-paywall-hero">
         <span class="premium-panel-kicker">${UI().escapeHtml(offer.eyebrow || "Premium")}</span>
-        <h2>${UI().escapeHtml(offer.title || "Libere o premium.")}</h2>
-        <p>${UI().escapeHtml(offer.lead || "Recursos avancados ficam no plano premium.")}</p>
-        <ul class="premium-paywall-benefits" aria-label="Beneficios do premium">
-            ${(offer.benefits || []).map((benefit) => `
-            <li>
-                <span aria-hidden="true"></span>
-                <strong>${UI().escapeHtml(benefit)}</strong>
-            </li>`).join("")}
-        </ul>
+        <h2>${UI().escapeHtml(offer.title || "Seu historico, seus materiais e seus treinos em um so lugar.")}</h2>
+        <p>${UI().escapeHtml(offer.lead || "Continue qualquer estudo, libere extras por assunto e acompanhe sua evolucao sem perder o fio da trilha.")}</p>
+        ${primaryBenefits.length ? `
+        <article class="premium-paywall-benefit-card premium-paywall-benefit-card-continuity">
+            <span class="premium-paywall-benefit-label">Foco desta oferta</span>
+            <ul>
+                ${primaryBenefits.map((item) => `<li>${UI().escapeHtml(item)}</li>`).join("")}
+            </ul>
+        </article>` : ""}
+        <div class="premium-paywall-benefit-groups" aria-label="Beneficios do premium">
+            ${benefitGroups.map((group) => `
+            <article class="premium-paywall-benefit-card premium-paywall-benefit-card-${group.tone}">
+                <span class="premium-paywall-benefit-label">${group.title}</span>
+                <ul>
+                    ${group.items.map((item) => `<li>${item}</li>`).join("")}
+                </ul>
+            </article>`).join("")}
+        </div>
     </article>
     <div class="premium-plan-grid">
-        ${plans.map((plan) => `
-        <button type="button" class="premium-plan-card ${plan.recommended ? "is-recommended" : ""}" data-premium-action="start-premium-checkout" data-item-value="${UI().escapeHtml(plan.id)}">
-            <span>${plan.recommended ? "Recomendado" : "Opcional"}</span>
+        ${orderedPlans.map((plan) => {
+            const isRecommended = plan.id === recommendedPlanId || (!recommendedPlanId && plan.recommended);
+
+            return `
+        <button type="button" class="premium-plan-card ${isRecommended ? "is-recommended" : ""}" data-premium-action="start-premium-checkout" data-item-value="${UI().escapeHtml(plan.id)}">
+            <span>${isRecommended ? "Mais indicado agora" : "Flexivel"}</span>
             <strong>${UI().escapeHtml(plan.label)}</strong>
             <em>${UI().escapeHtml(plan.priceLabel)}</em>
-            <small>${UI().escapeHtml(plan.description)}</small>
-        </button>`).join("")}
+            <p>${UI().escapeHtml(plan.description || "")}</p>
+            <small>${UI().escapeHtml(isRecommended
+                ? (offer.cta || "Oferta destacada agora.")
+                : (plan.interval === "year" ? "Cobranca anual." : "Cobranca mensal."))}</small>
+        </button>`;
+        }).join("")}
     </div>
+    <article class="premium-comparison-card">
+        <div class="premium-comparison-head">
+            <span class="premium-panel-kicker">Comparativo rapido</span>
+            <strong>O que muda do gratis para o premium</strong>
+        </div>
+        <div class="premium-comparison-table" role="table" aria-label="Comparativo gratis e premium">
+            <div class="premium-comparison-row premium-comparison-row-head" role="row">
+                <span role="columnheader">Recurso</span>
+                <span role="columnheader">Gratis</span>
+                <span role="columnheader">Premium</span>
+            </div>
+            ${comparisonRows.map((row) => `
+            <div class="premium-comparison-row" role="row">
+                <strong role="cell">${row.label}</strong>
+                <span role="cell" class="is-free">${row.free}</span>
+                <span role="cell" class="is-premium">${row.premium}</span>
+            </div>`).join("")}
+        </div>
+    </article>
     <article class="premium-paywall-status">
         <strong>${providerStatus.checkoutReady ? "Checkout pronto" : "Checkout em preparacao"}</strong>
         <p>${UI().escapeHtml(providerStatus.message || "O provedor real sera conectado na proxima etapa.")}</p>
@@ -611,9 +894,9 @@ ${renderSessionNote(state, "entry")}`;
     ${renderPracticeCard({
         action: "open-quiz",
         type: "quiz",
-        label: "Questionário",
+        label: "QuestionÃƒÂ¡rio",
         title: "Multipla escolha",
-        description: "Treino direto para critério, leitura e decisão.",
+        description: "Treino direto para critÃƒÂ©rio, leitura e decisÃƒÂ£o.",
         slots: quizSlots,
         seriesMeta: quizSeriesMeta,
         primary: true
@@ -631,8 +914,8 @@ ${renderSessionNote(state, "entry")}`;
         action: "open-flashcards",
         type: "flashcards",
         label: "Flashcards",
-        title: "Memorização ativa",
-        description: "Mnemônicos, gatilhos e fixação rápida do bloco.",
+        title: "MemorizaÃƒÂ§ÃƒÂ£o ativa",
+        description: "MnemÃƒÂ´nicos, gatilhos e fixaÃƒÂ§ÃƒÂ£o rÃƒÂ¡pida do bloco.",
         slots: flashcardSlots,
         seriesMeta: flashcardSeriesMeta
     })}
@@ -653,16 +936,16 @@ ${renderSessionNote(state, "entry")}`;
 
         if (session.isComplete) {
             const restartAction = seriesMeta.isAllComplete ? "restart-quiz" : "reset-quiz";
-            const restartLabel = seriesMeta.isAllComplete ? "Refazer questionário" : "Refazer esta rodada";
+            const restartLabel = seriesMeta.isAllComplete ? "Refazer questionÃƒÂ¡rio" : "Refazer esta rodada";
             return `
 <section class="premium-result-shell">
     <article class="premium-result-hero premium-result-hero-compact">
-        <span class="premium-detail-label">Questionário concluído</span>
+        <span class="premium-detail-label">QuestionÃƒÂ¡rio concluÃƒÂ­do</span>
         <strong>${seriesMeta.completedCount}/${seriesMeta.freeSeriesLimit}</strong>
-        <p>${seriesMeta.isAllComplete ? "Você concluiu as 3 rodadas grátis deste questionário." : "Você terminou esta rodada. Pode seguir para a próxima grátis."}</p>
+        <p>${seriesMeta.isAllComplete ? "VocÃƒÂª concluiu as 3 rodadas grÃƒÂ¡tis deste questionÃƒÂ¡rio." : "VocÃƒÂª terminou esta rodada. Pode seguir para a prÃƒÂ³xima grÃƒÂ¡tis."}</p>
     </article>
     ${UI().actionBar([
-        { action: "open-practice", label: "Voltar para prática", variant: "secondary" },
+        { action: "open-practice", label: "Voltar para prÃƒÂ¡tica", variant: "secondary" },
         { action: restartAction, label: restartLabel, variant: "ghost" },
         { action: "request-extra-quiz", label: extraButtonLabel, variant: "primary" }
     ])}
@@ -677,8 +960,8 @@ ${renderSessionNote(state, "entry")}`;
         return `
 <section class="premium-quiz-shell">
     <div class="premium-question-meta">
-        <span>Questão ${session.index + 1} de ${items.length}</span>
-        <strong>Série ${seriesMeta.currentSeries}/${seriesMeta.freeSeriesLimit}</strong>
+        <span>QuestÃƒÂ£o ${session.index + 1} de ${items.length}</span>
+        <strong>SÃƒÂ©rie ${seriesMeta.currentSeries}/${seriesMeta.freeSeriesLimit}</strong>
     </div>
     <article class="premium-question-card">
         <h2>${UI().escapeHtml(question.prompt)}</h2>
@@ -704,7 +987,7 @@ ${renderSessionNote(state, "entry")}`;
             <p>${UI().escapeHtml(question.rationale)}</p>
         </div>
         ${UI().actionBar([
-            { action: session.index >= items.length - 1 ? "finish-quiz" : "continue-quiz", label: session.index >= items.length - 1 ? "Ver resultado" : "Próxima", variant: "primary" }
+            { action: session.index >= items.length - 1 ? "finish-quiz" : "continue-quiz", label: session.index >= items.length - 1 ? "Ver resultado" : "PrÃƒÂ³xima", variant: "primary" }
         ])}
         ${renderSessionNote(state, "quiz")}` : ""}
     </article>
@@ -723,7 +1006,7 @@ ${renderSessionNote(state, "entry")}`;
         return `
 <section class="premium-vf-shell">
     <div class="premium-question-meta">
-        <span>Série curta ${seriesMeta.currentSeries}/${seriesMeta.freeSeriesLimit}</span>
+        <span>SÃƒÂ©rie curta ${seriesMeta.currentSeries}/${seriesMeta.freeSeriesLimit}</span>
         <strong>${UI().escapeHtml(block.title)}</strong>
     </div>
     <div class="premium-vf-list">
@@ -756,7 +1039,7 @@ ${renderSessionNote(state, "entry")}`;
     </div>
     ${session.submitted
         ? UI().actionBar([
-            { action: "open-practice", label: "Voltar para prática", variant: "secondary" },
+            { action: "open-practice", label: "Voltar para prÃƒÂ¡tica", variant: "secondary" },
             { action: seriesMeta.isAllComplete ? "restart-true-false" : "reset-true-false", label: seriesMeta.isAllComplete ? "Refazer V ou F" : "Refazer esta rodada", variant: "ghost" },
             { action: "request-extra-true-false", label: extraButtonLabel, variant: "primary" }
         ])
@@ -783,12 +1066,12 @@ ${renderSessionNote(state, "entry")}`;
             return `
 <section class="premium-result-shell">
     <article class="premium-result-hero premium-result-hero-compact">
-        <span class="premium-detail-label">Flashcards concluídos</span>
+        <span class="premium-detail-label">Flashcards concluÃƒÂ­dos</span>
         <strong>${seriesMeta.completedCount}/${seriesMeta.freeSeriesLimit}</strong>
-        <p>${seriesMeta.isAllComplete ? "Você concluiu as 3 rodadas grátis de flashcards." : `Você marcou ${isDone} cards como entendidos nesta rodada.`}</p>
+        <p>${seriesMeta.isAllComplete ? "VocÃƒÂª concluiu as 3 rodadas grÃƒÂ¡tis de flashcards." : `VocÃƒÂª marcou ${isDone} cards como entendidos nesta rodada.`}</p>
     </article>
     ${UI().actionBar([
-        { action: "open-practice", label: "Voltar para prática", variant: "secondary" },
+        { action: "open-practice", label: "Voltar para prÃƒÂ¡tica", variant: "secondary" },
         { action: restartAction, label: restartLabel, variant: "ghost" },
         { action: "request-extra-flashcards", label: extraButtonLabel, variant: "primary" }
     ])}
@@ -825,12 +1108,12 @@ ${renderSessionNote(state, "entry")}`;
 <section class="premium-result-shell">
     <article class="premium-result-hero premium-result-hero-compact">
         <span class="premium-detail-label">Mini prova do assunto</span>
-        <strong>${block.exam.baseCount || block.exam.questions.length} questões</strong>
-        <p>${hasHistory ? "Refaça a mesma mini prova deste assunto. Para novas questões e variações, o premium libera extras." : "Gere o pacote base deste assunto agora. Para um volume maior, o premium libera extras."}</p>
+        <strong>${block.exam.questions.length || block.exam.baseCount || 5} questoes</strong>
+        <p>${hasHistory ? "RefaÃƒÂ§a a mesma mini prova deste assunto. Para novas questÃƒÂµes e variaÃƒÂ§ÃƒÂµes, o premium libera extras." : "Gere o pacote base deste assunto agora. Para um volume maior, o premium libera extras."}</p>
     </article>
     ${UI().actionBar([
-        { action: hasHistory ? "retry-mini-exam" : "generate-mini-exam", label: hasHistory ? "Refazer mini prova" : `Gerar ${block.exam.baseCount || block.exam.questions.length} questões`, variant: "primary" },
-        { action: "request-extra-mini-exam", label: "Gerar mais no premium", variant: "ghost" }
+        { action: hasHistory ? "retry-mini-exam" : "generate-mini-exam", label: hasHistory ? "Refazer mini prova" : `Gerar ${block.exam.questions.length || block.exam.baseCount || 5} questoes`, variant: "primary" },
+        { action: "request-extra-mini-exam", label: "Gerar mais 5 no premium", variant: "ghost" }
     ])}
     ${renderSessionNote(state, "mini-exam")}
 </section>`;
@@ -871,7 +1154,7 @@ ${renderSessionNote(state, "entry")}`;
             <p>${UI().escapeHtml(question.rationale)}</p>
         </div>
         ${UI().actionBar([
-            { action: session.index >= block.exam.questions.length - 1 ? "finish-mini-exam" : "continue-mini-exam", label: session.index >= block.exam.questions.length - 1 ? "Ver resultado" : "Próxima", variant: "primary" }
+            { action: session.index >= block.exam.questions.length - 1 ? "finish-mini-exam" : "continue-mini-exam", label: session.index >= block.exam.questions.length - 1 ? "Ver resultado" : "PrÃƒÂ³xima", variant: "primary" }
         ])}` : ""}
         ${renderSessionNote(state, "mini-exam")}
     </article>
@@ -894,18 +1177,105 @@ ${renderSessionNote(state, "entry")}`;
     <div class="premium-detail-grid">
         <article class="premium-detail-card">
             <span class="premium-detail-label">Leitura recomendada</span>
-            <strong>Volte ao resumo se errou critério ou linguagem.</strong>
+            <strong>Volte ao resumo se errou critÃƒÂ©rio ou linguagem.</strong>
         </article>
         <article class="premium-detail-card">
-            <span class="premium-detail-label">Próximo passo</span>
-            <strong>${result.ratio >= 70 ? "Você já pode seguir para o próximo bloco." : "Pratique mais antes de avançar."}</strong>
+            <span class="premium-detail-label">PrÃƒÂ³ximo passo</span>
+            <strong>${result.ratio >= 70 ? "VocÃƒÂª jÃƒÂ¡ pode seguir para o prÃƒÂ³ximo bloco." : "Pratique mais antes de avanÃƒÂ§ar."}</strong>
         </article>
     </div>
     ${UI().actionBar([
         { action: "retry-mini-exam", label: "Refazer mini prova", variant: "secondary" },
-        { action: "open-practice", label: "Ir para o modo prática", variant: "secondary" },
-        { action: "request-extra-mini-exam", label: "Gerar mais no premium", variant: "ghost" }
+        { action: "open-practice", label: "Ir para o modo pratica", variant: "secondary" },
+        { action: "request-extra-mini-exam", label: "Gerar mais 5 no premium", variant: "ghost" }
+    ])}
+</section>`;
+    }
+
+    function levelExam(state) {
+        const exam = state.levelExam || {};
+        const questions = Array.isArray(exam.questions) ? exam.questions : [];
+        const result = exam.result || { correct: 0, total: 0, ratio: 0 };
+        const counts = [10, 20, 30];
+
+        if (exam.isComplete) {
+            return `
+<section class="premium-result-shell">
+    <article class="premium-result-hero">
+        <span class="premium-detail-label">Prova de nivel premium</span>
+        <strong>${result.ratio}%</strong>
+        <p>${result.correct} de ${result.total} questoes corretas no teste geral.</p>
+        <div class="premium-result-badge">${result.ratio >= 75 ? "Pronto para acelerar" : result.ratio >= 50 ? "Base em construcao" : "Volte aos blocos principais"}</div>
+    </article>
+    ${UI().actionBar([
+        { action: "generate-level-exam", label: "Gerar nova prova", variant: "primary" },
+        { action: "back-to-mode-select", label: "Voltar para modos", variant: "secondary" }
+    ])}
+    ${renderSessionNote(state, "level-exam")}
+</section>`;
+        }
+
+        if (exam.started && questions.length) {
+            const question = questions[exam.index] || questions[0];
+            const hasAnswer = typeof exam.answers[exam.index] === "number";
+            const answer = exam.answers[exam.index];
+            const isCorrectAnswer = hasAnswer && answer === question.correctIndex;
+
+            return `
+<section class="premium-quiz-shell">
+    <div class="premium-question-meta">
+        <span>Prova ${exam.index + 1} de ${questions.length}</span>
+        <strong>${UI().escapeHtml(exam.title || "Prova de nivel")}</strong>
+    </div>
+    <article class="premium-question-card">
+        <h2>${UI().escapeHtml(question.prompt)}</h2>
+        <div class="premium-option-grid">
+            ${question.options.map((option, index) => `
+            <button
+                type="button"
+                class="premium-option-card
+                    ${hasAnswer && answer === index ? "is-selected" : ""}
+                    ${hasAnswer && index === question.correctIndex ? "is-correct" : ""}
+                    ${hasAnswer && answer === index && answer !== question.correctIndex ? "is-incorrect" : ""}"
+                data-premium-action="answer-level-exam"
+                data-answer-index="${index}"
+                ${hasAnswer ? "disabled" : ""}
+            >
+                <span>${String.fromCharCode(65 + index)}</span>
+                <strong>${UI().escapeHtml(option)}</strong>
+            </button>`).join("")}
+        </div>
+        ${hasAnswer ? `
+        <div class="premium-feedback-card ${isCorrectAnswer ? "is-positive" : "is-warning"}">
+            <strong>${isCorrectAnswer ? "Resposta correta." : "Resposta incorreta."}</strong>
+            <p>${UI().escapeHtml(question.rationale)}</p>
+        </div>
+        ${UI().actionBar([
+            { action: exam.index >= questions.length - 1 ? "finish-level-exam" : "continue-level-exam", label: exam.index >= questions.length - 1 ? "Ver resultado" : "Proxima", variant: "primary" }
+        ])}` : ""}
+        ${renderSessionNote(state, "level-exam")}
+    </article>
+</section>`;
+        }
+
+        return `
+<section class="premium-result-shell">
+    <article class="premium-result-hero premium-result-hero-compact">
+        <span class="premium-detail-label">Prova de nivel premium</span>
+        <strong>${exam.questionCount || 10} questoes</strong>
+        <p>Escolha o tamanho da prova para medir sua prontidao geral no PDF.</p>
+    </article>
+    <div class="premium-inline-actions premium-inline-actions-contextual">
+        ${counts.map((count) => `
+        <button type="button" class="premium-action ${Number(exam.questionCount || 10) === count ? "premium-action-primary" : "premium-action-secondary"}" data-premium-action="select-level-exam-count" data-item-value="${count}">
+            ${count} questoes
+        </button>`).join("")}
+    </div>
+    ${UI().actionBar([
+        { action: questions.length ? "start-level-exam" : "generate-level-exam", label: questions.length ? "Comecar prova" : "Gerar prova premium", variant: "primary" },
+        { action: "back-to-mode-select", label: "Voltar para modos", variant: "secondary" }
     ])}
+    ${renderSessionNote(state, "level-exam")}
 </section>`;
     }
 
@@ -916,7 +1286,7 @@ ${renderSessionNote(state, "entry")}`;
     <article class="premium-result-hero premium-result-hero-compact">
         <span class="premium-detail-label">Progresso geral</span>
         <strong>${progress.ratio}%</strong>
-        <p>${progress.completed} etapas concluídas de ${progress.total} nesta trilha.</p>
+        <p>${progress.completed} etapas concluÃƒÂ­das de ${progress.total} nesta trilha.</p>
     </article>
     <div class="premium-trail-list">
         ${state.blocks.map((block) => `
@@ -969,6 +1339,8 @@ ${renderSessionNote(state, "entry")}`;
                 return miniExam(state);
             case "exam-result":
                 return examResult(state);
+            case "level-exam":
+                return levelExam(state);
             case "premium-library":
                 return premiumLibrary(state);
             case "premium-checkout":
diff --git a/qts-lab.html b/qts-lab.html
index 5394fd3..8acd3d0 100644
--- a/qts-lab.html
+++ b/qts-lab.html
@@ -3,7 +3,7 @@
 <head>
 <meta charset="UTF-8">
 <meta name="viewport" content="width=device-width, initial-scale=1.0">
-<title>Study OS - QTS Lab</title>
+<title>RotaNota - QTS Lab</title>
 <link rel="preconnect" href="https://fonts.googleapis.com">
 <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
 <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;500;600;700;800&family=Space+Grotesk:wght@500;700&display=swap" rel="stylesheet">
diff --git a/questions-standalone.html b/questions-standalone.html
index 0da17d3..c8f5e46 100644
--- a/questions-standalone.html
+++ b/questions-standalone.html
@@ -3,7 +3,7 @@
 <head>
 <meta charset="UTF-8">
 <meta name="viewport" content="width=device-width, initial-scale=1.0">
-<title>Questions Solo | Study OS</title>
+<title>Questions Solo | RotaNota</title>
 <link rel="preconnect" href="https://fonts.googleapis.com">
 <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
 <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;500;600;700;800&family=Space+Grotesk:wght@500;700&display=swap" rel="stylesheet">
diff --git a/questions/app/application/legacySessionFallback.mjs b/questions/app/application/legacySessionFallback.mjs
index f4793e3..32c2b42 100644
--- a/questions/app/application/legacySessionFallback.mjs
+++ b/questions/app/application/legacySessionFallback.mjs
@@ -256,6 +256,9 @@ export function createQuestionsLegacySessionFallback(
                 );
 
             QuestionsStore.registerSession({
+                sourceMode:
+                    QuestionsState.getMeta()
+                        .sourceMode || "",
                 baseKey:
                     QuestionsContext.get()
                         .base,
@@ -287,6 +290,8 @@ export function createQuestionsLegacySessionFallback(
                 errors: summary.errors,
                 avgTimeMs:
                     summary.avgTimeMs,
+                elapsedAnsweredMs:
+                    summary.elapsedAnsweredMs,
                 topicCount:
                     summary.topicCount,
                 topicKeys: [
@@ -312,7 +317,22 @@ export function createQuestionsLegacySessionFallback(
                         ?.topicLabel || "",
                 strongTopicLabel:
                     summary.strongTopic
-                        ?.topicLabel || ""
+                        ?.topicLabel || "",
+                directSearchTerms: Array.isArray(
+                    QuestionsState.getMeta()
+                        .directSearchTerms
+                )
+                    ? [
+                        ...QuestionsState.getMeta()
+                            .directSearchTerms
+                    ]
+                    : [],
+                directSearchStrategy:
+                    String(
+                        QuestionsState.getMeta()
+                            .directSearchStrategy ||
+                            ""
+                    ).trim()
             });
             QuestionsStore.flushProfileState(
                 true
diff --git a/questions/app/application/routeUseCases.mjs b/questions/app/application/routeUseCases.mjs
index e3c7c83..78f41af 100644
--- a/questions/app/application/routeUseCases.mjs
+++ b/questions/app/application/routeUseCases.mjs
@@ -585,9 +585,9 @@ export function createQuestionsRouteUseCases(
             "smart_subjects"
         );
         page.smartSubjectEditorKey = "";
-        page.smartSubjectFocusKey = "";
         page.clearRuntimeNotice();
-        page.openLauncher("smart");
+        page.smartTopicReviewOpen = true;
+        page.render();
     }
 
     function clearSmartExclusions() {
diff --git a/questions/app/application/sessionUseCases.mjs b/questions/app/application/sessionUseCases.mjs
index 791f68f..05e8f83 100644
--- a/questions/app/application/sessionUseCases.mjs
+++ b/questions/app/application/sessionUseCases.mjs
@@ -819,6 +819,9 @@ export function createQuestionsSessionUseCases(
                 );
 
             QuestionsStore.registerSession({
+                sourceMode:
+                    QuestionsState.getMeta()
+                        .sourceMode || "",
                 baseKey:
                     QuestionsContext.get()
                         .base,
@@ -849,6 +852,8 @@ export function createQuestionsSessionUseCases(
                 errors: summary.errors,
                 avgTimeMs:
                     summary.avgTimeMs,
+                elapsedAnsweredMs:
+                    summary.elapsedAnsweredMs,
                 topicCount:
                     summary.topicCount,
                 topicKeys: [
@@ -874,7 +879,22 @@ export function createQuestionsSessionUseCases(
                         ?.topicLabel || "",
                 strongTopicLabel:
                     summary.strongTopic
-                        ?.topicLabel || ""
+                        ?.topicLabel || "",
+                directSearchTerms: Array.isArray(
+                    QuestionsState.getMeta()
+                        .directSearchTerms
+                )
+                    ? [
+                        ...QuestionsState.getMeta()
+                            .directSearchTerms
+                    ]
+                    : [],
+                directSearchStrategy:
+                    String(
+                        QuestionsState.getMeta()
+                            .directSearchStrategy ||
+                            ""
+                    ).trim()
             });
             QuestionsStore.flushProfileState(
                 true
diff --git a/questions/app/infrastructure/storage/indexedDbSchema.mjs b/questions/app/infrastructure/storage/indexedDbSchema.mjs
index 25eb130..ca3a4fd 100644
--- a/questions/app/infrastructure/storage/indexedDbSchema.mjs
+++ b/questions/app/infrastructure/storage/indexedDbSchema.mjs
@@ -1,6 +1,6 @@
 export const QUESTIONS_INDEXED_DB_SCHEMA =
     Object.freeze({
-        dbName: "study_os_questions_v3",
+        dbName: "rotanota_questions_v3",
         version: 1,
         stores: {
             runs: {
diff --git a/questions/questions.css b/questions/questions.css
index a5b0a0c..10bf226 100644
--- a/questions/questions.css
+++ b/questions/questions.css
@@ -217,14 +217,14 @@ border: 1px solid rgba(148, 163, 184, 0.18);
 .questions-entry-option {
 position: relative;
 overflow: hidden;
-padding: 22px;
+padding: 16px 18px;
 border-radius: 0;
 background:
 linear-gradient(180deg, rgba(255, 255, 255, 0.98), rgba(239, 246, 255, 0.88));
 display: grid;
-gap: 16px;
+gap: 12px;
 align-content: start;
-min-height: 196px;
+min-height: 132px;
 clip-path: polygon(20px 0, calc(100% - 20px) 0, 100% 20px, 100% calc(100% - 20px), calc(100% - 20px) 100%, 20px 100%, 0 calc(100% - 20px), 0 20px);
 box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.72), 0 14px 28px rgba(15, 23, 42, 0.05);
 }
@@ -241,17 +241,17 @@ display: grid;
 align-content: center;
 justify-items: center;
 text-align: center;
-gap: 10px;
+gap: 0;
 }
 
 .questions-entry-grid > .questions-entry-option .questions-entry-copy p {
-max-width: 30ch;
-margin: 0 auto;
+display: none;
 }
 
 .questions-entry-grid > .questions-entry-option .questions-primary-btn,
 .questions-entry-grid > .questions-entry-option .questions-secondary-btn {
-width: min(100%, 220px);
+width: min(100%, 184px);
+min-height: 42px;
 justify-self: center;
 }
 
@@ -831,10 +831,10 @@ color: #e5e7eb;
 
 .questions-hub-consistency-block {
 display: grid;
-width: min(100%, 880px);
+width: min(100%, 760px);
 margin: 0 auto;
-gap: 18px;
-padding: 18px 20px 20px;
+gap: 14px;
+padding: 16px 18px 18px;
 border-radius: 16px;
 border: 1px solid rgba(255, 255, 255, 0.06);
 background: linear-gradient(180deg, rgba(255, 255, 255, 0.035), rgba(255, 255, 255, 0.02));
@@ -859,12 +859,12 @@ color: #e5e7eb;
 .questions-hub-consistency-grid {
 display: grid;
 grid-template-columns: repeat(7, minmax(0, 1fr));
-gap: 10px;
+gap: 8px;
 }
 
 .questions-hub-consistency-cell {
 aspect-ratio: 1 / 1;
-border-radius: 10px;
+border-radius: 8px;
 background: rgba(255, 255, 255, 0.08);
 border: 1px solid rgba(255, 255, 255, 0.04);
 }
@@ -3337,6 +3337,127 @@ text-transform: uppercase;
 color: rgba(100, 116, 139, 0.78);
 }
 
+#questionsModule .questions-smart-topic-review-scrim {
+position: absolute;
+inset: 28px 12px 0;
+z-index: 6;
+border-radius: 28px;
+background: rgba(255, 255, 255, 0.5);
+backdrop-filter: blur(4px);
+}
+
+#questionsModule .questions-smart-topic-review {
+position: absolute;
+inset: 46px 20px 12px;
+z-index: 7;
+display: grid;
+grid-template-rows: auto minmax(0, 1fr) auto;
+gap: 14px;
+padding: 18px;
+border-radius: 28px;
+border: 1px solid rgba(186, 201, 221, 0.34);
+background: linear-gradient(180deg, rgba(255, 255, 255, 0.96), rgba(247, 250, 255, 0.94));
+box-shadow: 0 24px 40px rgba(148, 163, 184, 0.16);
+}
+
+#questionsModule .questions-smart-topic-review-head {
+display: grid;
+grid-template-columns: minmax(0, 1fr) auto;
+gap: 18px;
+align-items: start;
+}
+
+#questionsModule .questions-smart-topic-review-head h3 {
+margin: 4px 0 6px;
+font-size: clamp(1.2rem, 2.2vw, 1.55rem);
+}
+
+#questionsModule .questions-smart-topic-review-head p {
+margin: 0;
+color: rgba(71, 85, 105, 0.78);
+}
+
+#questionsModule .questions-smart-topic-review-summary {
+display: grid;
+grid-template-columns: repeat(2, auto);
+gap: 4px 12px;
+align-items: center;
+justify-items: start;
+padding: 12px 14px;
+border-radius: 18px;
+background: rgba(239, 246, 255, 0.92);
+border: 1px solid rgba(147, 197, 253, 0.28);
+}
+
+#questionsModule .questions-smart-topic-review-summary strong {
+font-family: "Space Grotesk", "Manrope", sans-serif;
+font-size: 22px;
+line-height: 1;
+color: rgba(15, 23, 42, 0.92);
+}
+
+#questionsModule .questions-smart-topic-review-summary span {
+font-size: 10px;
+font-weight: 700;
+letter-spacing: 0.14em;
+text-transform: uppercase;
+color: rgba(71, 85, 105, 0.7);
+}
+
+#questionsModule .questions-smart-topic-review-list {
+display: grid;
+gap: 12px;
+overflow: auto;
+padding-right: 4px;
+}
+
+#questionsModule .questions-smart-topic-review-group {
+display: grid;
+gap: 10px;
+padding: 14px;
+border-radius: 20px;
+background: rgba(255, 255, 255, 0.78);
+border: 1px solid rgba(186, 201, 221, 0.26);
+}
+
+#questionsModule .questions-smart-topic-review-group-head {
+display: flex;
+justify-content: space-between;
+align-items: center;
+gap: 16px;
+}
+
+#questionsModule .questions-smart-topic-review-group-head strong {
+display: block;
+margin-bottom: 4px;
+font-size: 18px;
+color: rgba(15, 23, 42, 0.92);
+}
+
+#questionsModule .questions-smart-topic-review-group-head span {
+font-size: 12px;
+color: rgba(71, 85, 105, 0.72);
+}
+
+#questionsModule .questions-smart-topic-review-group-actions {
+display: flex;
+gap: 8px;
+flex-wrap: wrap;
+justify-content: flex-end;
+}
+
+#questionsModule .questions-smart-topic-review-topic-grid {
+display: grid;
+grid-template-columns: repeat(2, minmax(0, 1fr));
+gap: 10px;
+}
+
+#questionsModule .questions-smart-topic-review-foot {
+display: flex;
+justify-content: flex-end;
+gap: 10px;
+}
+
 body.dark #questionsModule .questions-smart-start-card {
 background:
 radial-gradient(circle at 50% 58%, var(--questions-smart-card-glow-a), transparent 34%),
@@ -3948,8 +4069,8 @@ z-index: 5;
 
 .questions-smart-pizza-core .questions-smart-core {
 pointer-events: auto;
-width: clamp(126px, 19vw, 166px);
-height: clamp(126px, 19vw, 166px);
+width: clamp(156px, 21vw, 192px);
+height: clamp(156px, 21vw, 192px);
 border-radius: 999px;
 clip-path: none;
 background:
@@ -3975,7 +4096,7 @@ inset 0 -16px 26px rgba(147, 197, 253, 0.24),
 }
 
 #questionsModule .questions-smart-start-card .questions-smart-pizza-core .questions-smart-core strong {
-font-size: clamp(40px, 4.8vw, 54px);
+font-size: clamp(38px, 4.6vw, 52px);
 color: rgba(30, 41, 59, 0.92);
 }
 
@@ -4563,6 +4684,77 @@ font: inherit;
 color: #0f172a;
 }
 
+.questions-direct-search {
+display: grid;
+gap: 12px;
+padding: 16px 18px 18px;
+position: relative;
+z-index: 2;
+border-radius: 20px;
+border: 1px solid rgba(148, 163, 184, 0.18);
+background: linear-gradient(180deg, rgba(255, 255, 255, 0.94), rgba(241, 245, 255, 0.88));
+box-shadow: 0 16px 32px rgba(15, 23, 42, 0.06);
+}
+
+.questions-direct-search-copy {
+display: block;
+}
+
+.questions-direct-search-copy h3 {
+margin: 0;
+font-size: clamp(1rem, 1.8vw, 1.2rem);
+}
+
+.questions-direct-search-form {
+display: grid;
+grid-template-columns: minmax(0, 1fr) auto auto;
+gap: 10px;
+align-items: center;
+position: relative;
+z-index: 3;
+}
+
+.questions-direct-search-input {
+min-height: 48px;
+position: relative;
+z-index: 4;
+pointer-events: auto;
+}
+
+.questions-direct-search-add,
+.questions-direct-search-generate {
+min-height: 48px;
+padding-inline: 18px;
+}
+
+.questions-direct-search-meta {
+display: flex;
+justify-content: space-between;
+align-items: center;
+gap: 16px;
+font-size: 13px;
+color: rgba(71, 85, 105, 0.84);
+}
+
+.questions-direct-search-meta strong {
+font-size: 13px;
+color: rgba(15, 23, 42, 0.9);
+}
+
+.questions-direct-search-chips {
+align-items: center;
+}
+
+.questions-chip-removable {
+display: inline-flex;
+align-items: center;
+gap: 8px;
+}
+
+.questions-chip-ghost {
+background: rgba(255, 255, 255, 0.78);
+}
+
 .questions-inline-note {
 padding: 12px 14px;
 border-radius: 16px;
@@ -5635,11 +5827,7 @@ transform: var(--questions-smart-node-transform) scale(1);
 
 @media (max-width: 1180px) {
 .questions-entry-card .questions-entry-grid {
-grid-template-columns: repeat(2, minmax(0, 1fr));
-}
-
-.questions-entry-card .questions-entry-grid .questions-entry-option:last-child {
-grid-column: 1 / -1;
+grid-template-columns: repeat(3, minmax(0, 1fr));
 }
 
 .questions-launcher-overview {
@@ -6219,3 +6407,383 @@ background: rgba(239, 246, 255, 0.62);
 grid-template-columns: 1fr;
 }
 }
+
+/* =====================================================
+   Unified questions finish - light/dark color and radius
+===================================================== */
+
+#questionsModule {
+--questions-card-radius: 14px;
+--questions-card-cut: 0px;
+--questions-card-surface:
+radial-gradient(circle at top left, rgba(47, 109, 216, 0.08), transparent 36%),
+linear-gradient(180deg, rgba(249, 252, 255, 0.98), rgba(239, 245, 252, 0.94));
+--questions-card-border: rgba(113, 132, 156, 0.24);
+--questions-card-shadow: 0 18px 38px rgba(44, 58, 78, 0.1);
+--questions-card-line: linear-gradient(90deg, rgba(47, 109, 216, 0.08), rgba(47, 109, 216, 0.56), rgba(217, 95, 79, 0.08));
+--questions-kicker-color: #2f6dd8;
+--questions-text-main: #1b2430;
+--questions-text-muted: rgba(73, 85, 102, 0.78);
+--questions-text-soft: rgba(73, 85, 102, 0.52);
+--questions-panel-surface: rgba(246, 250, 255, 0.88);
+--questions-panel-surface-strong: rgba(255, 255, 255, 0.96);
+--questions-panel-surface-muted: rgba(238, 244, 251, 0.86);
+--questions-panel-border: rgba(113, 132, 156, 0.2);
+--questions-panel-border-strong: rgba(141, 158, 181, 0.28);
+--questions-panel-border-muted: rgba(141, 158, 181, 0.22);
+--questions-chip-surface: rgba(47, 109, 216, 0.08);
+--questions-option-bg: linear-gradient(180deg, rgba(255, 255, 255, 0.98), rgba(241, 246, 252, 0.94));
+--questions-option-border: rgba(113, 132, 156, 0.2);
+--questions-option-selected-bg: linear-gradient(180deg, rgba(232, 241, 253, 0.98), rgba(219, 232, 248, 0.94));
+--questions-option-selected-border: rgba(47, 109, 216, 0.34);
+--questions-primary-btn-bg: linear-gradient(135deg, #2f6dd8, #5d91e1);
+--questions-primary-btn-shadow: 0 14px 28px rgba(47, 109, 216, 0.22);
+--questions-primary-btn-text: #ffffff;
+--questions-secondary-btn-bg: linear-gradient(180deg, rgba(255, 255, 255, 0.98), rgba(238, 244, 251, 0.98));
+--questions-secondary-btn-border: rgba(113, 132, 156, 0.22);
+--questions-secondary-btn-text: #1b2430;
+--questions-secondary-btn-shadow: 0 10px 22px rgba(44, 58, 78, 0.08);
+--questions-smart-toggle-active-bg: linear-gradient(180deg, rgba(219, 232, 248, 0.99), rgba(232, 241, 253, 0.97));
+--questions-smart-toggle-active-border: rgba(47, 109, 216, 0.32);
+--questions-smart-toggle-active-text: #224f9a;
+}
+
+body.dark #questionsModule {
+--questions-card-surface:
+radial-gradient(circle at top left, rgba(75, 126, 210, 0.14), transparent 38%),
+linear-gradient(180deg, rgba(13, 22, 35, 0.96), rgba(8, 14, 24, 0.94));
+--questions-card-border: rgba(92, 122, 171, 0.34);
+--questions-card-shadow: 0 22px 46px rgba(0, 0, 0, 0.36);
+--questions-card-line: linear-gradient(90deg, rgba(111, 167, 255, 0.08), rgba(111, 167, 255, 0.5), rgba(224, 96, 85, 0.12));
+--questions-kicker-color: rgba(143, 190, 255, 0.96);
+--questions-text-main: rgba(247, 242, 241, 0.96);
+--questions-text-muted: rgba(186, 199, 222, 0.8);
+--questions-text-soft: rgba(150, 164, 193, 0.62);
+--questions-panel-surface: rgba(15, 26, 43, 0.9);
+--questions-panel-surface-strong: rgba(18, 31, 51, 0.96);
+--questions-panel-surface-muted: rgba(13, 23, 39, 0.86);
+--questions-panel-border: rgba(92, 122, 171, 0.3);
+--questions-panel-border-strong: rgba(111, 146, 196, 0.38);
+--questions-panel-border-muted: rgba(92, 122, 171, 0.3);
+--questions-chip-surface: rgba(111, 167, 255, 0.1);
+--questions-option-bg: linear-gradient(180deg, rgba(18, 31, 51, 0.96), rgba(12, 21, 36, 0.94));
+--questions-option-border: rgba(92, 122, 171, 0.34);
+--questions-option-selected-bg: linear-gradient(180deg, rgba(35, 58, 94, 0.98), rgba(25, 43, 72, 0.96));
+--questions-option-selected-border: rgba(111, 167, 255, 0.42);
+--questions-option-selected-shadow: inset 0 0 0 1px rgba(111, 167, 255, 0.14);
+--questions-primary-btn-bg: linear-gradient(135deg, rgba(42, 76, 128, 0.98), rgba(149, 63, 70, 0.96));
+--questions-primary-btn-shadow: 0 16px 32px rgba(10, 18, 30, 0.38);
+--questions-primary-btn-text: rgba(255, 255, 255, 0.98);
+--questions-secondary-btn-bg: linear-gradient(180deg, rgba(20, 34, 55, 0.98), rgba(13, 23, 39, 0.96));
+--questions-secondary-btn-border: rgba(111, 146, 196, 0.32);
+--questions-secondary-btn-text: rgba(247, 242, 241, 0.94);
+--questions-secondary-btn-shadow: 0 12px 26px rgba(0, 0, 0, 0.3);
+--questions-smart-toggle-active-bg: linear-gradient(180deg, rgba(37, 65, 108, 0.98), rgba(27, 49, 84, 0.96));
+--questions-smart-toggle-active-border: rgba(111, 167, 255, 0.34);
+--questions-smart-toggle-active-text: rgba(247, 242, 241, 0.98);
+}
+
+#questionsModule .questions-card,
+#questionsModule .questions-stats-card,
+#questionsModule .questions-entry-option,
+#questionsModule .questions-panel,
+#questionsModule .questions-route-card,
+#questionsModule .questions-hub-sidebar,
+#questionsModule .questions-hub-nav-btn,
+#questionsModule .questions-hub-stat-card,
+#questionsModule .questions-hub-radar-card,
+#questionsModule .questions-hub-focus-card,
+#questionsModule .questions-hub-recommendation,
+#questionsModule .questions-hub-bar-card,
+#questionsModule .questions-hub-session-pill,
+#questionsModule .questions-hub-filter-pill,
+#questionsModule .questions-hub-radar-note,
+#questionsModule .questions-hub-time-card,
+#questionsModule .questions-hub-action,
+#questionsModule .questions-smart-config-card,
+#questionsModule .questions-smart-start-shell,
+#questionsModule .questions-smart-ring-toggle,
+#questionsModule .questions-smart-node,
+#questionsModule .questions-smart-petal,
+#questionsModule .questions-smart-core,
+#questionsModule .questions-smart-slice-action,
+#questionsModule .questions-smart-subject-console,
+#questionsModule .questions-smart-subject-focus-chip,
+#questionsModule .questions-smart-subject-console-btn,
+#questionsModule .questions-primary-btn,
+#questionsModule .questions-secondary-btn,
+#questionsModule .questions-option,
+#questionsModule .questions-confirm-btn,
+#questionsModule .questions-simulado-panel,
+#questionsModule .questions-simulado-choice,
+#questionsModule .questions-pill,
+#questionsModule .questions-dialog-card,
+#questionsModule .questions-inline-notice,
+#utilityRoot .questions-floating-window,
+#utilityRoot .questions-floating-actions button {
+clip-path: none !important;
+}
+
+#questionsModule .questions-card,
+#questionsModule .questions-stats-card,
+#utilityRoot .questions-floating-window {
+border-radius: 14px !important;
+}
+
+#questionsModule .questions-entry-option,
+#questionsModule .questions-panel,
+#questionsModule .questions-route-card,
+#questionsModule .questions-hub-sidebar,
+#questionsModule .questions-hub-stat-card,
+#questionsModule .questions-hub-radar-card,
+#questionsModule .questions-hub-focus-card,
+#questionsModule .questions-hub-recommendation,
+#questionsModule .questions-hub-bar-card,
+#questionsModule .questions-hub-time-card,
+#questionsModule .questions-smart-config-card,
+#questionsModule .questions-smart-start-shell,
+#questionsModule .questions-smart-subject-console,
+#questionsModule .questions-simulado-panel,
+#questionsModule .questions-simulado-choice,
+#questionsModule .questions-dialog-card,
+#questionsModule .questions-inline-notice {
+border-radius: 10px !important;
+}
+
+#questionsModule .questions-primary-btn,
+#questionsModule .questions-secondary-btn,
+#questionsModule .questions-confirm-btn,
+#questionsModule .questions-hub-nav-btn,
+#questionsModule .questions-hub-filter-pill,
+#questionsModule .questions-hub-radar-note,
+#questionsModule .questions-hub-action,
+#questionsModule .questions-smart-ring-toggle,
+#questionsModule .questions-smart-slice-action,
+#questionsModule .questions-smart-subject-focus-chip,
+#questionsModule .questions-smart-subject-console-btn,
+#questionsModule .questions-option,
+#questionsModule .questions-pill,
+#utilityRoot .questions-floating-actions button {
+border-radius: 8px !important;
+}
+
+#questionsModule .questions-primary-btn::after,
+#questionsModule .questions-secondary-btn::after,
+#questionsModule .questions-confirm-btn::after {
+clip-path: none !important;
+border-radius: 7px !important;
+}
+
+#questionsModule .questions-entry-option {
+background: var(--questions-option-bg);
+color: var(--questions-text-main);
+}
+
+#questionsModule .questions-entry-option-smart {
+background:
+radial-gradient(circle at top left, rgba(31, 125, 117, 0.08), transparent 44%),
+var(--questions-option-bg);
+}
+
+#questionsModule .questions-entry-option-specific,
+#questionsModule .questions-entry-option-simulado-build,
+#questionsModule .questions-entry-option-simulado-ready {
+background:
+radial-gradient(circle at top left, rgba(47, 109, 216, 0.1), transparent 44%),
+var(--questions-option-bg);
+}
+
+#questionsModule .questions-entry-option-saved {
+background:
+radial-gradient(circle at top left, rgba(217, 95, 79, 0.1), transparent 44%),
+var(--questions-option-bg);
+}
+
+body.dark #questionsModule .questions-entry-option-smart {
+background:
+radial-gradient(circle at top left, rgba(31, 125, 117, 0.14), transparent 44%),
+var(--questions-option-bg);
+}
+
+body.dark #questionsModule .questions-entry-option-specific,
+body.dark #questionsModule .questions-entry-option-simulado-build,
+body.dark #questionsModule .questions-entry-option-simulado-ready {
+background:
+radial-gradient(circle at top left, rgba(111, 167, 255, 0.14), transparent 44%),
+var(--questions-option-bg);
+}
+
+body.dark #questionsModule .questions-entry-option-saved {
+background:
+radial-gradient(circle at top left, rgba(224, 96, 85, 0.14), transparent 44%),
+var(--questions-option-bg);
+}
+
+#questionsModule .questions-entry-card .questions-primary-btn {
+background: var(--questions-secondary-btn-bg);
+border-color: var(--questions-secondary-btn-border);
+color: var(--questions-secondary-btn-text);
+box-shadow: var(--questions-secondary-btn-shadow);
+}
+
+body.dark #questionsModule .questions-card p,
+body.dark #questionsModule .questions-card span,
+body.dark #questionsModule .questions-stats-card,
+body.dark #questionsModule .questions-entry-copy p,
+body.dark #questionsModule .questions-simulado-choice span {
+color: var(--questions-text-muted) !important;
+}
+
+body.dark #questionsModule .questions-card h2,
+body.dark #questionsModule .questions-card h3,
+body.dark #questionsModule .questions-stats-card h3,
+body.dark #questionsModule .questions-entry-copy h3,
+body.dark #questionsModule .questions-simulado-choice strong {
+color: var(--questions-text-main) !important;
+}
+
+@media (max-width: 760px) and (pointer: coarse) {
+.questions-root,
+.questions-launcher,
+.questions-session {
+padding-bottom: calc(84px + env(safe-area-inset-bottom, 0px));
+}
+
+.questions-entry-card .questions-entry-grid {
+grid-template-columns: 1fr;
+}
+
+.questions-direct-search-form,
+.questions-direct-search-meta,
+#questionsModule .questions-smart-topic-review-head,
+#questionsModule .questions-smart-topic-review-group-head,
+#questionsModule .questions-smart-topic-review-foot {
+grid-template-columns: 1fr;
+flex-direction: column;
+align-items: stretch;
+}
+
+.questions-direct-search-form {
+display: grid;
+}
+
+.questions-direct-search-add,
+.questions-direct-search-generate {
+width: 100%;
+}
+
+.questions-direct-search-meta {
+display: grid;
+gap: 6px;
+}
+
+.questions-head,
+.questions-session-top,
+.questions-feedback-foot {
+flex-direction: column;
+align-items: stretch;
+}
+
+#questionsModule .questions-smart-series-card .questions-entry-head,
+#questionsModule .questions-smart-subject-card .questions-entry-head {
+display: grid;
+grid-template-columns: minmax(0, 1fr);
+gap: 10px;
+margin-bottom: 8px;
+}
+
+#questionsModule .questions-smart-series-card .questions-smart-entry-actions,
+#questionsModule .questions-smart-subject-card .questions-smart-entry-actions {
+position: static;
+top: auto;
+right: auto;
+width: 100%;
+justify-self: stretch;
+}
+
+#questionsModule .questions-smart-series-card .questions-smart-back-btn,
+#questionsModule .questions-smart-subject-card .questions-smart-back-btn {
+min-width: 94px;
+min-height: 36px;
+padding: 8px 14px;
+justify-self: start;
+}
+
+#questionsModule .questions-smart-series-card .questions-smart-start-shell {
+padding-top: 50px;
+}
+
+#questionsModule .questions-smart-topic-review {
+inset: 34px 12px 10px;
+padding: 14px;
+}
+
+#questionsModule .questions-smart-topic-review-topic-grid {
+grid-template-columns: 1fr;
+}
+
+#questionsModule .questions-smart-series-card .questions-smart-ring-toggle {
+top: 4px;
+}
+
+#questionsModule .questions-smart-subject-card .questions-smart-start-shell {
+padding-top: 10px;
+}
+
+#questionsModule .questions-smart-entry-actions-subjects {
+width: 100%;
+justify-content: space-between;
+align-items: center;
+gap: 8px;
+flex-wrap: wrap;
+}
+
+#questionsModule .questions-smart-ring-toggle-inline,
+#questionsModule .questions-smart-subject-card .questions-smart-ring-toggle,
+#questionsModule .questions-smart-series-card .questions-smart-ring-toggle {
+max-width: 100%;
+white-space: normal;
+text-align: center;
+}
+
+.questions-smart-radial-stage {
+margin-top: 0;
+gap: 12px;
+}
+
+.questions-question-card--minimal {
+padding: 16px 14px 18px;
+gap: 12px;
+}
+
+.questions-session-card--minimal .questions-question-card h3 {
+font-size: clamp(1.35rem, 6.6vw, 1.75rem);
+line-height: 1.12;
+}
+
+.questions-session-card--minimal .questions-option {
+grid-template-columns: 30px minmax(0, 1fr);
+padding: 12px 12px;
+}
+
+.questions-session-card--minimal .questions-feedback--minimal {
+padding: 14px 12px;
+gap: 12px;
+}
+
+.questions-feedback-foot {
+display: grid;
+grid-template-columns: 1fr;
+gap: 8px;
+}
+
+.questions-session-card--minimal .questions-confirm-btn,
+.questions-feedback-foot .questions-secondary-btn {
+width: 100%;
+justify-content: center;
+}
+
+.questions-feedback-note {
+padding-bottom: 2px;
+}
+}
diff --git a/questions/questions.js b/questions/questions.js
index a436d1b..a7c45f3 100644
--- a/questions/questions.js
+++ b/questions/questions.js
@@ -13,6 +13,12 @@ window.QuestionsPage = {
         "Enviar sem comentar",
     smartSubjectEditorKey: "",
     smartSubjectFocusKey: "",
+    smartTopicReviewOpen: false,
+    directSearchTerms: [],
+    directSearchInput: "",
+    directSearchMatchCount: null,
+    directSearchLoading: false,
+    directSearchRefocusPending: false,
     simuladoBuilder: null,
     renderFrameId: 0,
     renderQueuedSync: false,
@@ -2301,7 +2307,8 @@ window.QuestionsPage = {
             safeMinutes * 60,
             {
                 open: true,
-                autostart: true
+                autostart: true,
+                simuladoCompact: true
             }
         );
     },
@@ -3946,6 +3953,95 @@ window.QuestionsPage = {
         this.render();
     },
 
+    buildSmartTopicReviewModel() {
+        const activeSubjects =
+            this.getSmartSubjectOptions().filter(
+                (item) =>
+                    item.active &&
+                    !item.disabled &&
+                    item.selectedTopicCount !==
+                        0
+            );
+
+        return {
+            activeSubjects,
+            totalTopics:
+                activeSubjects.reduce(
+                    (sum, subject) =>
+                        sum +
+                        Number(
+                            subject.selectedTopicCount ||
+                                0
+                        ),
+                    0
+                )
+        };
+    },
+
+    openSmartTopicReview() {
+        const selectedSeries =
+            this.getSelectedSmartSeries();
+
+        if (!selectedSeries.length) {
+            this.runtimeNotice =
+                "Selecione ao menos uma serie antes de escolher as materias.";
+            this.openLauncher(
+                "smart_start"
+            );
+            return;
+        }
+
+        const reviewModel =
+            this.buildSmartTopicReviewModel();
+
+        if (
+            !reviewModel.activeSubjects.length
+        ) {
+            this.runtimeNotice =
+                "Mantenha pelo menos uma materia com assuntos ativos para continuar.";
+            this.render();
+            return;
+        }
+
+        this.dismissCoachHint(
+            "smart_subjects"
+        );
+        this.smartSubjectEditorKey = "";
+        this.clearRuntimeNotice();
+        this.smartTopicReviewOpen = true;
+        this.render();
+    },
+
+    closeSmartTopicReview() {
+        if (!this.smartTopicReviewOpen) {
+            return;
+        }
+
+        this.smartTopicReviewOpen = false;
+        this.render();
+    },
+
+    continueSmartTopicReview() {
+        const reviewModel =
+            this.buildSmartTopicReviewModel();
+
+        if (
+            !reviewModel.activeSubjects.length
+        ) {
+            this.runtimeNotice =
+                "Mantenha pelo menos uma materia com assuntos ativos para continuar.";
+            this.smartTopicReviewOpen =
+                false;
+            this.render();
+            return;
+        }
+
+        this.smartTopicReviewOpen = false;
+        this.smartSubjectFocusKey = "";
+        this.clearRuntimeNotice();
+        this.openLauncher("smart");
+    },
+
     toggleSmartStartOption(optionKey) {
         if (
             this.routeUseCases
@@ -4148,10 +4244,241 @@ window.QuestionsPage = {
         this.dismissCoachHint(
             "smart_subjects"
         );
-        this.smartSubjectEditorKey = "";
-        this.smartSubjectFocusKey = "";
+        this.openSmartTopicReview();
+    },
+
+    setDirectSearchInput(value = "") {
+        this.directSearchInput =
+            String(value || "");
+    },
+
+    async refreshDirectSearchMatches(
+        options = {}
+    ) {
+        const shouldRender =
+            options.render !== false;
+        const terms =
+            this.directSearchTerms
+                .map((term) =>
+                    String(term || "").trim()
+                )
+                .filter(Boolean);
+
+        if (!terms.length) {
+            this.directSearchLoading = false;
+            this.directSearchMatchCount =
+                null;
+
+            if (shouldRender) {
+                this.render();
+            }
+
+            return [];
+        }
+
+        this.directSearchLoading = true;
+
+        if (shouldRender) {
+            this.render();
+        }
+
+        try {
+            await this.ensureDetailedCatalogLoaded();
+
+            const matches =
+                this.getDirectSearchMatches();
+
+            this.directSearchMatchCount =
+                matches.length;
+            this.directSearchLoading = false;
+
+            if (shouldRender) {
+                this.render();
+            }
+
+            return matches;
+        } catch (_error) {
+            this.directSearchLoading =
+                false;
+            this.directSearchMatchCount = 0;
+            this.runtimeNotice =
+                "Nao consegui carregar o banco completo para a busca agora.";
+
+            if (shouldRender) {
+                this.render();
+            }
+
+            return [];
+        }
+    },
+
+    addDirectSearchTerm(rawValue = "") {
+        const normalized =
+            String(rawValue || "")
+                .replace(/\s+/g, " ")
+                .trim();
+
+        if (!normalized) {
+            return;
+        }
+
+        const duplicate =
+            this.directSearchTerms.some(
+                (term) =>
+                    QuestionsService.normalizeText(
+                        term
+                    ) ===
+                    QuestionsService.normalizeText(
+                        normalized
+                    )
+            );
+
+        if (duplicate) {
+            this.directSearchInput = "";
+            this.directSearchRefocusPending =
+                true;
+            this.render();
+            return;
+        }
+
+        this.directSearchTerms = [
+            ...this.directSearchTerms,
+            normalized
+        ].slice(-8);
+        this.directSearchInput = "";
+        this.directSearchRefocusPending =
+            true;
         this.clearRuntimeNotice();
-        this.openLauncher("smart");
+        this.refreshDirectSearchMatches();
+    },
+
+    removeDirectSearchTerm(rawValue = "") {
+        const normalized =
+            QuestionsService.normalizeText(
+                rawValue
+            );
+        this.directSearchTerms =
+            this.directSearchTerms.filter(
+                (term) =>
+                    QuestionsService.normalizeText(
+                        term
+                    ) !== normalized
+            );
+        this.refreshDirectSearchMatches();
+    },
+
+    clearDirectSearchTerms() {
+        this.directSearchTerms = [];
+        this.directSearchInput = "";
+        this.directSearchLoading = false;
+        this.directSearchMatchCount =
+            null;
+        this.render();
+    },
+
+    getDirectSearchMatches() {
+        const terms =
+            this.directSearchTerms
+                .map((term) => String(term || "").trim())
+                .filter(Boolean);
+
+        if (!terms.length) {
+            return [];
+        }
+
+        return QuestionsService.getAllQuestions(
+            this,
+            {}
+        ).filter((question) => {
+            const haystack =
+                QuestionsService.normalizeText(
+                    [
+                        question.subjectLabel,
+                        question.topicLabel,
+                        question.subtopicLabel,
+                        question.axisLabel,
+                        question.frontLabel,
+                        ...(Array.isArray(
+                            question.tags
+                        )
+                            ? question.tags
+                            : []),
+                        ...(Array.isArray(
+                            question.competencies
+                        )
+                            ? question.competencies
+                            : []),
+                        question.prompt
+                    ].join(" ")
+                );
+
+            return terms.some((term) =>
+                QuestionsService.matchesFuzzySearch(
+                    haystack,
+                    term
+                )
+            );
+        });
+    },
+
+    async startDirectSearchSession() {
+        if (!this.directSearchTerms.length) {
+            this.runtimeNotice =
+                "Digite pelo menos um assunto ou subassunto para gerar o treino direto.";
+            this.render();
+            return;
+        }
+
+        const list =
+            await this.refreshDirectSearchMatches(
+                {
+                    render: true
+                }
+            );
+
+        if (!list.length) {
+            this.runtimeNotice =
+                "Nao encontrei questoes para os termos buscados agora. Tente outro assunto ou subassunto.";
+            this.render();
+            return;
+        }
+
+        const label =
+            this.directSearchTerms
+                .slice(0, 3)
+                .join(" + ");
+        const strategy =
+            "gradual";
+        const orderedList =
+            QuestionsService.orderDirectSearchQuestions(
+                list,
+                strategy
+            );
+
+        this.clearRuntimeNotice();
+        this.startSession({
+            sessionList: orderedList,
+            sourceMode: "direct_search",
+            meta: {
+                title: `Busca direta - ${label}`,
+                routeLabel: "Busca direta",
+                requestedCount:
+                    orderedList.length,
+                availableCount:
+                    orderedList.length,
+                readyCount:
+                    orderedList.length,
+                directSearchTerms: [
+                    ...this.directSearchTerms
+                ],
+                directSearchStrategy:
+                    strategy,
+                trainingModeLabel:
+                    "Busca por assunto",
+                trainingValueLabel:
+                    "Gradativa"
+            }
+        });
     },
 
     toggleSmartSeriesExclusion(
@@ -5493,7 +5820,10 @@ window.QuestionsPage = {
         const modeLabel =
             sourceMode === "smart"
                 ? "Treino inteligente"
-                : "Treino especifico";
+                : sourceMode ===
+                      "direct_search"
+                    ? "Busca por assunto"
+                    : "Treino especifico";
         const materia =
             meta.materiaLabel ||
             context.materia ||
@@ -6685,6 +7015,9 @@ window.QuestionsPage = {
                 );
 
             QuestionsStore.registerSession({
+                sourceMode:
+                    QuestionsState.getMeta()
+                        .sourceMode || "",
                 baseKey:
                     QuestionsContext.get()
                         .base,
@@ -6716,6 +7049,8 @@ window.QuestionsPage = {
                 errors: summary.errors,
                 avgTimeMs:
                     summary.avgTimeMs,
+                elapsedAnsweredMs:
+                    summary.elapsedAnsweredMs,
                 topicCount:
                     summary.topicCount,
                 topicKeys: [
@@ -6741,7 +7076,22 @@ window.QuestionsPage = {
                         ?.topicLabel || "",
                 strongTopicLabel:
                     summary.strongTopic
-                        ?.topicLabel || ""
+                        ?.topicLabel || "",
+                directSearchTerms: Array.isArray(
+                    QuestionsState.getMeta()
+                        .directSearchTerms
+                )
+                    ? [
+                        ...QuestionsState.getMeta()
+                            .directSearchTerms
+                    ]
+                    : [],
+                directSearchStrategy:
+                    String(
+                        QuestionsState.getMeta()
+                            .directSearchStrategy ||
+                            ""
+                    ).trim()
             });
             QuestionsStore.flushProfileState(
                 true
@@ -7216,6 +7566,8 @@ window.QuestionsPage = {
                 "";
             this.smartSubjectFocusKey =
                 "";
+            this.smartTopicReviewOpen =
+                false;
         }
 
         if (
diff --git a/questions/questions.service.js b/questions/questions.service.js
index 7c4ae9c..94d7f36 100644
--- a/questions/questions.service.js
+++ b/questions/questions.service.js
@@ -90,6 +90,179 @@ window.QuestionsService = {
             .replace(/\s+/g, " ");
     },
 
+    normalizeSearchTokens(value) {
+        return this.normalizeText(value)
+            .replace(/[^a-z0-9\s]/g, " ")
+            .split(/\s+/)
+            .map((token) => token.trim())
+            .filter((token) => token.length >= 2);
+    },
+
+    levenshteinDistance(a = "", b = "") {
+        const left = String(a || "");
+        const right = String(b || "");
+
+        if (left === right) {
+            return 0;
+        }
+
+        if (!left.length) {
+            return right.length;
+        }
+
+        if (!right.length) {
+            return left.length;
+        }
+
+        const matrix = Array.from(
+            { length: left.length + 1 },
+            () =>
+                new Array(
+                    right.length + 1
+                ).fill(0)
+        );
+
+        for (
+            let row = 0;
+            row <= left.length;
+            row += 1
+        ) {
+            matrix[row][0] = row;
+        }
+
+        for (
+            let col = 0;
+            col <= right.length;
+            col += 1
+        ) {
+            matrix[0][col] = col;
+        }
+
+        for (
+            let row = 1;
+            row <= left.length;
+            row += 1
+        ) {
+            for (
+                let col = 1;
+                col <= right.length;
+                col += 1
+            ) {
+                const cost =
+                    left[row - 1] ===
+                    right[col - 1]
+                        ? 0
+                        : 1;
+
+                matrix[row][col] = Math.min(
+                    matrix[row - 1][col] + 1,
+                    matrix[row][col - 1] + 1,
+                    matrix[row - 1][col - 1] +
+                        cost
+                );
+            }
+        }
+
+        return matrix[left.length][right.length];
+    },
+
+    fuzzyTokenMatch(leftToken, rightToken) {
+        const left =
+            this.normalizeText(leftToken);
+        const right =
+            this.normalizeText(rightToken);
+
+        if (!left || !right) {
+            return false;
+        }
+
+        if (left === right) {
+            return true;
+        }
+
+        if (
+            left.includes(right) ||
+            right.includes(left)
+        ) {
+            return true;
+        }
+
+        const distance =
+            this.levenshteinDistance(
+                left,
+                right
+            );
+        const maxLength = Math.max(
+            left.length,
+            right.length
+        );
+        const threshold =
+            maxLength >= 9
+                ? 2
+                : 1;
+
+        return distance <= threshold;
+    },
+
+    matchesFuzzySearch(
+        haystack,
+        rawTerm
+    ) {
+        const normalizedHaystack =
+            this.normalizeText(haystack);
+        const normalizedTerm =
+            this.normalizeText(rawTerm);
+
+        if (!normalizedHaystack || !normalizedTerm) {
+            return false;
+        }
+
+        if (
+            normalizedHaystack.includes(
+                normalizedTerm
+            )
+        ) {
+            return true;
+        }
+
+        const haystackTokens =
+            this.normalizeSearchTokens(
+                normalizedHaystack
+            );
+        const termTokens =
+            this.normalizeSearchTokens(
+                normalizedTerm
+            );
+
+        if (!termTokens.length) {
+            return false;
+        }
+
+        let matchedCount = 0;
+
+        termTokens.forEach((termToken) => {
+            const matched =
+                haystackTokens.some(
+                    (hayToken) =>
+                        this.fuzzyTokenMatch(
+                            hayToken,
+                            termToken
+                        )
+                );
+
+            if (matched) {
+                matchedCount += 1;
+            }
+        });
+
+        const minimumMatches =
+            termTokens.length <= 2
+                ? termTokens.length
+                : termTokens.length - 1;
+
+        return matchedCount >= minimumMatches;
+    },
+
     formatTime(ms) {
         const totalSeconds =
             Math.max(
@@ -1626,6 +1799,65 @@ window.QuestionsService = {
         return questions;
     },
 
+    orderDirectSearchQuestions(
+        questions = [],
+        strategy = "gradual"
+    ) {
+        const pool = Array.isArray(questions)
+            ? questions.filter(Boolean)
+            : [];
+        const normalizedStrategy =
+            String(strategy || "gradual")
+                .trim()
+                .toLowerCase();
+
+        if (normalizedStrategy === "random") {
+            return this.shuffle(pool);
+        }
+
+        const grouped = new Map();
+
+        pool.forEach((question) => {
+            const difficulty = Math.max(
+                Number(question?.difficulty) || 1,
+                1
+            );
+            const bucket =
+                grouped.get(difficulty) || [];
+
+            bucket.push(question);
+            grouped.set(difficulty, bucket);
+        });
+
+        return [...grouped.keys()]
+            .sort((left, right) => left - right)
+            .flatMap((difficulty) =>
+                [...grouped.get(difficulty)]
+                    .sort((left, right) => {
+                    const timeDiff =
+                        (Number(
+                            left?.expectedTime
+                        ) || 0) -
+                        (Number(
+                            right?.expectedTime
+                        ) || 0);
+
+                    if (timeDiff !== 0) {
+                        return timeDiff;
+                    }
+
+                    return String(
+                        left?.topicLabel || ""
+                    ).localeCompare(
+                        String(
+                            right?.topicLabel || ""
+                        ),
+                        "pt-BR"
+                    );
+                    })
+            );
+    },
+
     getWarmupExpandedTopicKeys(
         page,
         context = null,
@@ -3932,6 +4164,15 @@ window.QuestionsService = {
             errors,
             accuracy,
             avgTimeMs,
+            elapsedAnsweredMs:
+                safeResults.reduce(
+                    (acc, item) =>
+                        acc +
+                        (Number(
+                            item?.timeMs
+                        ) || 0),
+                    0
+                ),
             topics,
             weakTopic,
             strongTopic,
diff --git a/questions/questions.store.js b/questions/questions.store.js
index beaca98..934c003 100644
--- a/questions/questions.store.js
+++ b/questions/questions.store.js
@@ -1292,6 +1292,174 @@ window.QuestionsStore = {
         ) || null;
     },
 
+    getDirectSearchDashboard() {
+        const runs =
+            this.getRuns({
+                mode: "direct_search"
+            });
+        const resolvedRuns =
+            runs.filter((run) =>
+                Array.isArray(run?.answers) &&
+                run.answers.length
+            );
+        const totals =
+            resolvedRuns.reduce(
+                (acc, run) => {
+                    const answers =
+                        Array.isArray(
+                            run.answers
+                        )
+                            ? run.answers
+                            : [];
+
+                    answers.forEach((answer) => {
+                        acc.attempts += 1;
+                        acc.totalTimeMs +=
+                            Number(
+                                answer?.timeMs
+                            ) || 0;
+
+                        if (answer?.correct) {
+                            acc.hits += 1;
+                        } else {
+                            acc.errors += 1;
+                        }
+                    });
+
+                    acc.lastResolvedAt =
+                        Math.max(
+                            acc.lastResolvedAt,
+                            Number(
+                                run.completedAt
+                            ) ||
+                                Number(
+                                    run.updatedAt
+                                ) ||
+                                Number(
+                                    run.startedAt
+                                ) ||
+                                0
+                        );
+
+                    const meta =
+                        run?.routeSnapshot?.meta &&
+                        typeof run
+                            .routeSnapshot.meta ===
+                            "object"
+                            ? run.routeSnapshot.meta
+                            : run?.summary?.meta &&
+                                typeof run
+                                    .summary.meta ===
+                                    "object"
+                              ? run.summary.meta
+                              : {};
+                    const terms =
+                        Array.isArray(
+                            meta.directSearchTerms
+                        )
+                            ? meta.directSearchTerms
+                            : [];
+                    const strategy = String(
+                        meta.directSearchStrategy ||
+                            ""
+                    ).trim();
+
+                    terms.forEach((term) => {
+                        const key = String(
+                            term || ""
+                        ).trim();
+
+                        if (!key) {
+                            return;
+                        }
+
+                        acc.termUsage.set(
+                            key,
+                            (acc.termUsage.get(
+                                key
+                            ) || 0) + 1
+                        );
+                    });
+
+                    if (strategy) {
+                        acc.strategyUsage.set(
+                            strategy,
+                            (acc.strategyUsage.get(
+                                strategy
+                            ) || 0) + 1
+                        );
+                    }
+
+                    return acc;
+                },
+                {
+                    attempts: 0,
+                    hits: 0,
+                    errors: 0,
+                    totalTimeMs: 0,
+                    lastResolvedAt: 0,
+                    termUsage: new Map(),
+                    strategyUsage:
+                        new Map()
+                }
+            );
+
+        const topTerms = [
+            ...totals.termUsage.entries()
+        ]
+            .sort((left, right) =>
+                right[1] - left[1] ||
+                left[0].localeCompare(
+                    right[0],
+                    "pt-BR"
+                )
+            )
+            .slice(0, 6)
+            .map(([term, count]) => ({
+                term,
+                count
+            }));
+        const preferredStrategy =
+            [...totals.strategyUsage.entries()]
+                .sort((left, right) =>
+                    right[1] - left[1]
+                )[0]?.[0] || "gradual";
+
+        return {
+            runs,
+            resolvedRuns,
+            attempts: totals.attempts,
+            hits: totals.hits,
+            errors: totals.errors,
+            accuracy:
+                totals.attempts > 0
+                    ? totals.hits /
+                      totals.attempts
+                    : 0,
+            avgTimeMs:
+                totals.attempts > 0
+                    ? totals.totalTimeMs /
+                      totals.attempts
+                    : 0,
+            elapsedAnsweredMs:
+                totals.totalTimeMs,
+            totalRuns:
+                runs.length,
+            answeredRuns:
+                resolvedRuns.length,
+            avgAnsweredPerRun:
+                resolvedRuns.length > 0
+                    ? totals.attempts /
+                      resolvedRuns.length
+                    : 0,
+            lastResolvedAt:
+                totals.lastResolvedAt,
+            topTerms,
+            strategy:
+                preferredStrategy
+        };
+    },
+
     saveRun(run = {}) {
         const now = Date.now();
         const questionIds =
@@ -1527,6 +1695,14 @@ window.QuestionsStore = {
                     return false;
                 }
 
+                if (
+                    filters.sourceMode &&
+                    entry.sourceMode !==
+                        filters.sourceMode
+                ) {
+                    return false;
+                }
+
                 return true;
             })
             .sort(
@@ -1647,6 +1823,8 @@ window.QuestionsStore = {
         const focusedSessions =
             this.getFocusedSessions(filters)
                 .slice(0, 5);
+        const directSearch =
+            this.getDirectSearchDashboard();
         const totals =
             entries.reduce(
                 (acc, entry) => {
@@ -1691,6 +1869,7 @@ window.QuestionsStore = {
             strongTopics,
             modeBreakdown,
             focusedSessions,
+            directSearch,
             mostTrainedTopic:
                 [...entries].sort(
                     (left, right) =>
diff --git a/questions/questions.ui.js b/questions/questions.ui.js
index 25823a8..14f1607 100644
--- a/questions/questions.ui.js
+++ b/questions/questions.ui.js
@@ -23,6 +23,32 @@ window.QuestionsUI = {
             return;
         }
 
+        const activeElement =
+            document.activeElement instanceof
+            HTMLElement
+                ? document.activeElement
+                : null;
+        const shouldRestoreDirectSearchFocus =
+            activeElement?.id ===
+                "questionsDirectSearchInput" ||
+            this.page
+                ?.directSearchRefocusPending ===
+                true;
+        const directSearchSelectionStart =
+            shouldRestoreDirectSearchFocus &&
+            typeof activeElement
+                .selectionStart ===
+                "number"
+                ? activeElement.selectionStart
+                : null;
+        const directSearchSelectionEnd =
+            shouldRestoreDirectSearchFocus &&
+            typeof activeElement
+                .selectionEnd ===
+                "number"
+                ? activeElement.selectionEnd
+                : null;
+
         const launcher =
             document.getElementById(
                 "questionsLauncher"
@@ -92,6 +118,44 @@ window.QuestionsUI = {
 
         if (phase === "launcher") {
             this.bindLauncher();
+
+            if (
+                shouldRestoreDirectSearchFocus
+            ) {
+                this.page.directSearchRefocusPending =
+                    false;
+                requestAnimationFrame(() => {
+                    const input =
+                        document.getElementById(
+                            "questionsDirectSearchInput"
+                        );
+
+                    if (!input) {
+                        return;
+                    }
+
+                    input.focus({
+                        preventScroll: true
+                    });
+
+                    if (
+                        typeof input
+                            .setSelectionRange ===
+                            "function" &&
+                        Number.isInteger(
+                            directSearchSelectionStart
+                        ) &&
+                        Number.isInteger(
+                            directSearchSelectionEnd
+                        )
+                    ) {
+                        input.setSelectionRange(
+                            directSearchSelectionStart,
+                            directSearchSelectionEnd
+                        );
+                    }
+                });
+            }
         } else {
             this.bindSessionSurface(
                 session
@@ -1683,6 +1747,29 @@ window.QuestionsUI = {
                 minAttempts: 1,
                 minErrors: 1
             })[0] || null;
+        const directSearchTerms =
+            Array.isArray(
+                page.directSearchTerms
+            )
+                ? page.directSearchTerms
+                : [];
+        const directSearchMatchCount =
+            typeof page.directSearchMatchCount ===
+                "number" &&
+            Number.isFinite(
+                page.directSearchMatchCount
+            )
+                ? page.directSearchMatchCount
+                : null;
+        const directSearchStatusLabel =
+            !directSearchTerms.length
+                ? "Adicione um termo para buscar"
+                : page.directSearchLoading
+                    ? "Buscando questoes..."
+                    : directSearchMatchCount ===
+                          null
+                        ? "Pronto para gerar"
+                        : `${directSearchMatchCount} questoes encontradas`;
 
         return `
             <section class="questions-card questions-entry-card">
@@ -1717,7 +1804,6 @@ window.QuestionsUI = {
                     <article class="questions-entry-option questions-entry-option-smart">
                         <div class="questions-entry-copy">
                             <h3>Inteligente</h3>
-                            <p>Fluxo adaptativo com etapa de assuntos e fechamento mais enxuto.</p>
                         </div>
                         <button class="questions-primary-btn" type="button" data-launcher-view="smart_start" ${isLoading || isError ? "disabled" : ""}>
                             ${isLoading ? "Preparando..." : isError ? "Indisponivel" : "Comecar"}
@@ -1727,7 +1813,6 @@ window.QuestionsUI = {
                     <article class="questions-entry-option questions-entry-option-specific">
                         <div class="questions-entry-copy">
                             <h3>Simulado</h3>
-                            <p>Monte blocos por assunto agora e deixe prova pronta reservada para a proxima etapa.</p>
                         </div>
                         <button class="questions-secondary-btn" type="button" data-launcher-view="simulado" ${isLoading || isError ? "disabled" : ""}>
                             ${isLoading ? "Preparando..." : isError ? "Indisponivel" : "Entrar"}
@@ -1735,11 +1820,47 @@ window.QuestionsUI = {
                     </article>
                 </div>
 
-                <div class="questions-entry-footer">
-                    <button class="questions-secondary-btn" type="button" data-launcher-view="progress">
-                        Progresso
-                    </button>
-                </div>
+                <section class="questions-direct-search" aria-label="Busca direta por assunto">
+                    <div class="questions-direct-search-copy">
+                        <h3>Questoes por assunto</h3>
+                    </div>
+
+                    <div class="questions-direct-search-form">
+                        <input
+                            id="questionsDirectSearchInput"
+                            class="questions-search-field questions-direct-search-input"
+                            type="text"
+                            value="${this.escapeHtml(page.directSearchInput || "")}"
+                            placeholder="Ex.: funcao afim, organelas, predicado verbal"
+                        />
+                        <button class="questions-secondary-btn questions-direct-search-add" type="button" data-direct-search-add="true">
+                            Adicionar
+                        </button>
+                        <button class="questions-primary-btn questions-direct-search-generate" type="button" data-direct-search-generate="true" ${directSearchTerms.length ? "" : "disabled"}>
+                            Gerar questoes
+                        </button>
+                    </div>
+
+                    <div class="questions-direct-search-meta">
+                        <span>${directSearchTerms.length ? `${directSearchTerms.length} filtro(s) ativo(s)` : "Nenhum filtro ativo"}</span>
+                        <strong>${directSearchStatusLabel}</strong>
+                    </div>
+
+                    ${directSearchTerms.length ? `
+                        <div class="questions-chip-row questions-direct-search-chips">
+                            ${directSearchTerms.map((term) => `
+                                <button class="questions-chip questions-chip-removable" type="button" data-direct-search-remove="${this.escapeHtml(term)}">
+                                    <span>${this.escapeHtml(term)}</span>
+                                    <strong aria-hidden="true">x</strong>
+                                </button>
+                            `).join("")}
+                            <button class="questions-chip questions-chip-ghost" type="button" data-direct-search-clear="true">
+                                Limpar
+                            </button>
+                        </div>
+                    ` : ""}
+                </section>
+
             </section>
         `;
     },
@@ -5195,6 +5316,14 @@ window.QuestionsUI = {
             editorSubject?.key ||
             resolvedFocusSubject?.key ||
             "";
+        const reviewModel =
+            typeof page.buildSmartTopicReviewModel ===
+            "function"
+                ? page.buildSmartTopicReviewModel()
+                : {
+                    activeSubjects: [],
+                    totalTopics: 0
+                };
         const visibleSubjectSlices =
             this.buildSmartRadialSlices(
                 visibleSubjects,
@@ -5418,6 +5547,70 @@ window.QuestionsUI = {
                             </div>
 
                         </div>
+                        ${page.smartTopicReviewOpen ? `
+                            <div class="questions-smart-topic-review-scrim" data-smart-topic-review-close="true"></div>
+                            <section class="questions-smart-topic-review" role="dialog" aria-modal="true" aria-label="Revisar assuntos selecionados">
+                                <div class="questions-smart-topic-review-head">
+                                    <div>
+                                        <span class="questions-kicker">Antes de gerar</span>
+                                        <h3>Revise os assuntos das materias selecionadas</h3>
+                                        <p>Desmarque o que nao quer levar. Se estiver tudo certo, seguimos com todos os assuntos ativos.</p>
+                                    </div>
+                                    <div class="questions-smart-topic-review-summary">
+                                        <strong>${reviewModel.activeSubjects.length}</strong>
+                                        <span>materia(s)</span>
+                                        <strong>${reviewModel.totalTopics}</strong>
+                                        <span>assunto(s)</span>
+                                    </div>
+                                </div>
+
+                                <div class="questions-smart-topic-review-list">
+                                    ${reviewModel.activeSubjects.map((subject) => `
+                                        <article class="questions-smart-topic-review-group">
+                                            <div class="questions-smart-topic-review-group-head">
+                                                <div>
+                                                    <strong>${this.escapeHtml(subject.label)}</strong>
+                                                    <span>${subject.selectedTopicCount} assunto(s) ativo(s)</span>
+                                                </div>
+                                                <div class="questions-smart-topic-review-group-actions">
+                                                    <button class="questions-smart-topic-editor-btn" type="button" data-smart-topic-editor-select-all="${this.escapeHtml(subject.key)}">
+                                                        Todos
+                                                    </button>
+                                                    <button class="questions-smart-topic-editor-btn" type="button" data-smart-topic-editor-clear-all="${this.escapeHtml(subject.key)}">
+                                                        Limpar
+                                                    </button>
+                                                </div>
+                                            </div>
+                                            <div class="questions-smart-topic-review-topic-grid">
+                                                ${subject.topicOptions.map((topic) => `
+                                                    <button
+                                                        class="questions-smart-topic-row${topic.active ? " is-active" : " is-excluded"}"
+                                                        type="button"
+                                                        data-smart-topic-subject="${this.escapeHtml(subject.key)}"
+                                                        data-smart-topic-toggle="${this.escapeHtml(topic.key)}"
+                                                    >
+                                                        <span class="questions-smart-topic-row-check" aria-hidden="true"></span>
+                                                        <span class="questions-smart-topic-row-copy">
+                                                            <strong>${this.escapeHtml(topic.label)}</strong>
+                                                            <small>${topic.count || 0} questoes prontas${topic.subtopicCount ? ` · ${topic.subtopicCount} subtopico(s)` : ""}</small>
+                                                        </span>
+                                                    </button>
+                                                `).join("")}
+                                            </div>
+                                        </article>
+                                    `).join("")}
+                                </div>
+
+                                <div class="questions-smart-topic-review-foot">
+                                    <button class="questions-secondary-btn" type="button" data-smart-topic-review-close="true">
+                                        Voltar
+                                    </button>
+                                    <button class="questions-primary-btn" type="button" data-smart-topic-review-continue="true">
+                                        Seguir com selecao
+                                    </button>
+                                </div>
+                            </section>
+                        ` : ""}
                         ${hiddenSubjects ? `
                             <div class="questions-inline-note questions-smart-subject-note">
                                 +${hiddenSubjects} matéria(s) continuam disponíveis. Ajuste as séries para refinar mais se quiser.
@@ -7260,10 +7453,36 @@ window.QuestionsUI = {
         )?.addEventListener(
             "click",
             () => {
-                this.page.continueSmartSubjects();
+                this.page.openSmartTopicReview();
             }
         );
 
+        document
+            .querySelectorAll(
+                "[data-smart-topic-review-close]"
+            )
+            .forEach((button) => {
+                button.addEventListener(
+                    "click",
+                    () => {
+                        this.page.closeSmartTopicReview();
+                    }
+                );
+            });
+
+        document
+            .querySelectorAll(
+                "[data-smart-topic-review-continue]"
+            )
+            .forEach((button) => {
+                button.addEventListener(
+                    "click",
+                    () => {
+                        this.page.continueSmartTopicReview();
+                    }
+                );
+            });
+
         document
             .querySelectorAll(
                 "[data-smart-subject-option]"
@@ -7367,37 +7586,137 @@ window.QuestionsUI = {
             );
 
         document
-            .querySelector(
+            .querySelectorAll(
                 "[data-smart-topic-editor-select-all]"
             )
-            ?.addEventListener(
-                "click",
-                (event) => {
-                    this.page.setAllSmartSubjectTopics(
-                        event.currentTarget
-                            .dataset
-                            .smartTopicEditorSelectAll,
-                        true
-                    );
-                }
-            );
+            .forEach((button) => {
+                button.addEventListener(
+                    "click",
+                    (event) => {
+                        this.page.setAllSmartSubjectTopics(
+                            event.currentTarget
+                                .dataset
+                                .smartTopicEditorSelectAll,
+                            true
+                        );
+                    }
+                );
+            });
 
         document
-            .querySelector(
+            .querySelectorAll(
                 "[data-smart-topic-editor-clear-all]"
             )
-            ?.addEventListener(
-                "click",
-                (event) => {
-                    this.page.setAllSmartSubjectTopics(
-                        event.currentTarget
-                            .dataset
-                            .smartTopicEditorClearAll,
-                        false
-                    );
-                }
+            .forEach((button) => {
+                button.addEventListener(
+                    "click",
+                    (event) => {
+                        this.page.setAllSmartSubjectTopics(
+                            event.currentTarget
+                                .dataset
+                                .smartTopicEditorClearAll,
+                            false
+                        );
+                    }
+                );
+            });
+
+        const directSearchInput =
+            document.getElementById(
+                "questionsDirectSearchInput"
             );
 
+        directSearchInput?.addEventListener(
+            "input",
+            (event) => {
+                this.page.setDirectSearchInput(
+                    event.target.value
+                );
+            }
+        );
+
+        directSearchInput?.addEventListener(
+            "keydown",
+            (event) => {
+                if (event.key !== "Enter") {
+                    return;
+                }
+
+                event.preventDefault();
+                this.page.addDirectSearchTerm(
+                    event.currentTarget.value
+                );
+            }
+        );
+
+        directSearchInput?.addEventListener(
+            "pointerdown",
+            (event) => {
+                event.stopPropagation();
+            }
+        );
+
+        document
+            .querySelectorAll(
+                "[data-direct-search-add]"
+            )
+            .forEach((button) => {
+                button.addEventListener(
+                    "click",
+                    () => {
+                        const field =
+                            document.getElementById(
+                                "questionsDirectSearchInput"
+                            );
+                        this.page.addDirectSearchTerm(
+                            field?.value || ""
+                        );
+                    }
+                );
+            });
+
+        document
+            .querySelectorAll(
+                "[data-direct-search-remove]"
+            )
+            .forEach((button) => {
+                button.addEventListener(
+                    "click",
+                    () => {
+                        this.page.removeDirectSearchTerm(
+                            button.dataset
+                                .directSearchRemove
+                        );
+                    }
+                );
+            });
+
+        document
+            .querySelectorAll(
+                "[data-direct-search-clear]"
+            )
+            .forEach((button) => {
+                button.addEventListener(
+                    "click",
+                    () => {
+                        this.page.clearDirectSearchTerms();
+                    }
+                );
+            });
+
+        document
+            .querySelectorAll(
+                "[data-direct-search-generate]"
+            )
+            .forEach((button) => {
+                button.addEventListener(
+                    "click",
+                    () => {
+                        this.page.startDirectSearchSession();
+                    }
+                );
+            });
+
         document.getElementById(
             "questionsSmartStartBtn"
         )?.addEventListener(
